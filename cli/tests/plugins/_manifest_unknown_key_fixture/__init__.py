# A fixture plugin bundle whose one manifest carries a key the kind does
# not declare (FR19, issue #214). Everything else about the document is
# valid, so the only thing under test is the unknown key.
