Rendering this topic reads only packaged teaching. It does not inspect a configuration, resolve an
apt source, connect to a VM, or enable the plugin. The actions below are inert records: obtain the
named authorization before using either one.
