Rendering this topic reads only packaged teaching. It does not inspect a configuration, execute an
install command, connect to a VM, or enable the plugin. The actions below are inert records: obtain
the named authorization before using either one.
