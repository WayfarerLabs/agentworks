# Built-in resource manifests

App-shipped resources published with the `built-in` origin. Empty until the provider/backend split
ships the built-in secret backends; loaded by `agentworks/manifests/builtin.py` through the same
loader as operator manifests.
