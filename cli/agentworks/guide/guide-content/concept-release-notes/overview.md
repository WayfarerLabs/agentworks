Release history and current adoption answer different questions. This topic reads the installed
wheel's canonical changelog section as historical evidence. Use `concept-onboarding` to assess the
capabilities and configuration that are live now.
