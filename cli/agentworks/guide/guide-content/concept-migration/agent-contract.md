Treat every file read, workstation check, and edit below as a separate consent boundary. The action
records are inert instructions, not authorization. State the exact paths and resource names, obtain
consent for the named boundary, and stop at the refusal alternative when consent is withheld.

Never infer approval from rendering this topic. Under one explicit read boundary, inventory both the
retired config and any existing resources, choose every intended TOML manifest file, and freeze the
complete expected-identity union before requesting either backup. Backup destinations must be fresh,
distinct, and outside active trees; preserve an explicit absent baseline when no resources directory
exists. Verification and manifest edits consume that immutable union and never author or change an
expected path. Preserve operator-declared origin and manifest path while ignoring mutable source
lines. Never resolve secret values while reviewing secret references, and never remove the retired
TOML sections until the growing manifest set has been validated one manifest at a time.
