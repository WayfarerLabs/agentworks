Use the implementation inventory to understand available source implementations. A configured
source's readiness predicts whether it can participate; preview never proves that a particular
secret resolves.

Use `agw secret describe NAME` for non-resolving prediction. Resolve or verify one named secret only
after consent, through the ordinary secure input boundary and its injected interaction policy. Never
inspect a source broadly to find the value.
