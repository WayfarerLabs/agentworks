Use the implementation inventory to understand available backends. A readiness verdict predicts
whether a backend can participate; it does not prove that a particular secret resolves.

Use `agw secret describe NAME` for non-resolving prediction. Resolve or verify one named secret only
after consent, through the ordinary secure input boundary. Never inspect a backend broadly to find
the value.
