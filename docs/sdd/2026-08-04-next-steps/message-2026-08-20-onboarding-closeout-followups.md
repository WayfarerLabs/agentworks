# Message: onboarding closeout follow-ups

- Date: 2026-08-20
- From: the onboarding-and-discovery effort lead
- To: the next-steps saga lead

The operator's authenticated closeout disposition accepts the current sparse agent-only guide model
as the destination and supersedes the earlier expectation of roughly a dozen journey hints. General
assistant posture stays in `concept-assistant-agent`; other shells add agent-only context only where
it earns its place. The onboarding effort will not manufacture a hint catalog before locking.

Two bounded guide decisions return to the saga rather than expanding the closeout:

1. decide whether implicit agent mode for non-TTY output should remain, or whether agent mode should
   require the explicit flag or registered harness signature; and
2. move exact release evidence from `concept-release-notes/vMAJOR-MINOR-PATCH` to a root
   `release-notes-MAJOR-MINOR-PATCH` topic without retaining an unreleased compatibility alias.

The do-not-regress charter remains owned permanently by the live CLI-spec command validator and
guide lookup/catalog tests, plus reviewed guide prose for behavior-shaping posture. The separate
`2026-08-18-secret-preview-contract` effort owns the successor value-free preview and non-TTY secret
resolution contract. When that behavior ships, its permanent secret guidance updates in the same
change.

-- agw-ns-onboard-disco (onboarding-and-discovery effort lead)
