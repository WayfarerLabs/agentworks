# Nerf Tools -- Implementation Plan

## Definition of Done

- `nerf build` reads YAML manifests and produces standalone, executable shell scripts
- `nerf skill` reads the same manifests and produces rulesync skill markdown files
- `nerf build --install-nerfctl claude` installs the nerfctl-claude-\* permission management scripts
- Built-in nerf-git manifest ships with the package; all built-ins included by default (opt-out)
- `nerf build` clears outdir by default; `--keep-existing` preserves unmanaged files
- All nerfctl-claude-\* scripts are tested
- Agentworks CLI integration is config-driven only; nerf runs locally and artifacts are rsynced to
  VMs
- VM config supports flat nerf fields in `[vm.config]`
- Tests pass with mypy strict and ruff clean

---

## Phase 1: `nerftools` Package Foundation

- [x] Create `nerftools/` top-level directory
- [x] Write `nerftools/pyproject.toml` (package: `nerftools`, script: `nerf = "nerftools.cli:app"`)
- [x] Create `nerftools/nerftools/` Python package with `__init__.py`
- [x] Define manifest dataclasses in `nerftools/nerftools/manifest.py`
  - `PackageMeta` (name, description, skill_group, skill_intro)
  - `ParamSpec` (flag, positional, description, required, pattern, allow, deny, default)
  - `ToolSpec` (description, command, example, params, env)
  - `NerfManifest` (package, tools)
- [x] Implement YAML manifest loading and validation
  - Parse and type-check all fields
  - Validate: all `{param}` refs in command have a params entry
  - Validate: no param has both `flag` and `positional`
  - Validate: `allow` and `deny` not both set
  - Validate: `default` not set when `required = true`
  - Validate: `pattern` is a valid regex
- [x] Set up `nerftools/tests/` with pytest and a test manifest fixture

## Phase 2: `nerf build` Command

- [x] Implement `nerftools/nerftools/builder.py`
  - `build_script_text(tool_name, package_name, tool_spec) -> str` generates a complete bash script
  - Shebang + `set -euo pipefail`
  - Header comment (tool name, description, package, "do not edit")
  - `usage()` function with full parameter docs and example if present
  - Argument parsing loop (`case "$1" in`) for flag params
  - Positional param collection (in declaration order)
  - Per-param validation (required, pattern, allow, deny)
  - `env` exports before exec
  - `exec` with substituted arguments (never shell-interpolated)
- [x] Implement manifest merging (last-wins by tool name)
- [x] Implement `nerf build` CLI command
  - `--outdir <dir>` (default: current directory)
  - `--no-default` (skip built-in catalog)
  - `--install-nerfctl <framework>` (e.g. `claude`; may be repeated; installs pre-written scripts)
  - Positional `<manifest> ...` for user manifests
  - Writes scripts with `chmod +x`
  - Fails fast on validation errors before writing any output
- [x] Add `--keep-existing` flag to `nerf build` and `nerf skill`
  - Default behavior: clear outdir before writing (so removed tools do not linger)
  - `--keep-existing`: skip the clear step, preserving files not generated by this run
- [x] Tests for builder
  - Simple tool (no params)
  - Flag param with pattern and deny
  - Positional param
  - Required vs optional params
  - Env vars
  - Usage/help output
  - bash -n syntax validation

## Phase 3: `nerf skill` Command

- [x] Implement `nerftools/nerftools/skill.py`
  - `build_skill_text(manifest) -> str` generates markdown for a package
  - Package header using `skill_intro`
  - Section per tool: description, usage line, param table, example
  - Constraint details (pattern, deny, allow) surfaced per param
- [x] Implement `nerf skill` CLI command
  - Same `--outdir`, `--no-default`, `--keep-existing`, manifest args as `nerf build`
  - Writes `<skill_group>/SKILL.md` per package
- [x] Tests for skill generation

## Phase 4: Built-in Manifests

- [x] Write `nerftools/manifests/nerf-git/manifest.yaml`
  - `nerf-git-push-origin`: push HEAD to origin
  - `nerf-git-push-remote`: push HEAD to a non-origin remote (--remote, deny origin)
  - `nerf-git-fetch`: fetch from a specific remote (positional)
  - `nerf-git-log`: short one-line log (no params)
- [x] Verify built-in manifest loads and validates cleanly

## Phase 5: nerfctl-claude-\* Scripts

- [x] Write `nerftools/nerftools/nerfctl/claude/grant.sh`
  - Accepts `<tool> [--settings <path>]`
  - Default settings: `.claude/settings.local.json` in CWD (error if not present)
  - Adds `Bash(<tool>)` to `permissions.allow`, removes from `deny` if present
  - Uses `jq` for JSON manipulation
- [x] Write `nerftools/nerftools/nerfctl/claude/deny.sh`
  - Adds `Bash(<tool>)` to `permissions.deny`, removes from `allow` if present
- [x] Write `nerftools/nerftools/nerfctl/claude/reset.sh`
  - Removes `Bash(<tool>)` from both `allow` and `deny`
- [x] Write `nerftools/nerftools/nerfctl/claude/list.sh`
  - Lists all `Bash(nerf-*)` and `Bash(nerfctl-*)` entries from both lists
- [x] Implement `--install-nerfctl claude` in `nerf build`
  - Copies the four scripts to `--outdir` as `nerfctl-claude-{grant,deny,reset,list}`
  - Sets `chmod +x`
- [x] Tests for nerfctl scripts (pytest + subprocess; bats not available)

## Phase 6: Agentworks CLI Integration

Notes:

- VM config uses flat fields in `[vm.config]` (not a separate subsection) for consistency.
- No separate `agentworks nerf` command -- nerf is driven entirely by VM config.
- Build model: `nerf build` and `nerf skill` run on the operator's workstation; artifacts are
  rsynced to the VM. No Python or pipx dependency on the VM.
- `nerf_pkg_source` was added and then removed; it was a vestige of the "install on VM" model.

- [x] Add nerf config fields to `VMConfig` in `cli/agentworks/config.py`
  - `install_nerf_tools: bool` (default false, opt-in)
  - `skip_nerf_defaults: bool` (default false)
  - `nerf_addl_manifests: list[Path]` (additional user manifest paths)
- [x] Add remaining nerf config fields
  - `nerf_keep_existing: bool` (default false; rsync without --delete when true)
  - `nerf_bin_dir: str` (default `/opt/agentworks/nerf/bin`)
  - `nerf_skills_dir: str` (default `/opt/agentworks/nerf/skills`)
- [x] Remove `nerf_pkg_source` from `VMConfig`, `_VM_CONFIG_KEYS`, `_load_vm_config`, and
      `sample-config.toml`
- [x] No separate `agentworks nerf` command group -- nerf is driven entirely by vm.config
- [x] Rework VM init sequence: local build + rsync (replaces pipx install on VM)
  - Run `nerf build --outdir <tmp>` locally with configured options
  - Run `nerf skill --outdir <tmp>` locally with configured options
  - Rsync bin output to `nerf_bin_dir` on VM via SSH
  - Rsync skills output to `nerf_skills_dir` on VM via SSH
  - Write `/etc/profile.d/agentworks-nerf.sh` (via sudo) to add `nerf_bin_dir` to PATH system-wide
    (covers all users including agent users; user-level profile mechanism is not sufficient here)
- [x] Update `sample-config.toml` with commented nerf fields in `[vm.config]`
- [x] Update completions (removed stale `nerf.install` -> `vms` entry)

## Phase 7: Enhanced Manifest Format and Additional Manifests

Notes:

- Unified `ParamSpec` was replaced with separate `FlagSpec` (named flags) and `ArgSpec` (positional
  args) for cleaner semantics: flags are required by default, args are optional by default.
- Boolean flags (`boolean: true`) take no value, are always optional, and expand to the flag string
  itself in the command (e.g. `${DRAFT:+"--draft"}`).
- Short flag aliases (`short: -r`) are supported and rendered in usage and case statements.
- Flag injection prevention: positional args are checked against `== -*` before pattern validation.
- Template placeholder syntax uses `{{param}}` (double braces) to avoid YAML flow mapping conflicts
  with single-brace `{param}` syntax.
- Script guards allow inline bash snippets as pre-flight checks, enabling inverted logic (e.g.
  `! git diff --cached --quiet` to fail when nothing is staged). Both command and script guards use
  `check || { echo 'error: ...'; exit 1; }` pattern.
- Manifests are stored without prefix; a `--prefix` option (default `nerf-`) is applied at build
  time to all tool names, script filenames, and skill group names.
- `--all` variants for terragrunt require separate tools because they restructure the command
  (`terragrunt X` vs `terragrunt run --all X`).
- Nx tools use `npx` as a hardcoded runner; `find_pkg_runner` dynamic selection cannot be modeled.

- [x] Replace `ParamSpec` with `FlagSpec` and `ArgSpec` in `manifest.py`
  - `FlagSpec`: required by default, auto-derived flag name from key (`--flag-name`), `short` for
    single-char aliases, `boolean` for value-less flags
  - `ArgSpec`: optional by default, `variadic` to collect `"$@"` into array
  - `GuardSpec`: pre-flight check with `command` or `script` (mutually exclusive); fires (exit 1) on
    non-zero exit; `{{param}}` placeholders substituted in both forms
- [x] Update `builder.py` for new dataclasses and features
  - Boolean flags use `shift 1` and expand as `${VAR:+"--flag-string"}`
  - Short flags included in `case "$1" in` pattern: `--remote|-r)`
  - Flag injection prevention: `[[ "${VAR}" == -* ]]` check on all positional args and variadics
  - Script guards: single-line wrapped in `( ... )`, multi-line in indented subshell block
  - `prefix` parameter (default `"nerf-"`) prepended to all generated script filenames and names
- [x] Update `skill.py` for new dataclasses and prefix support
  - Boolean flags shown as `[--flag]` in usage line
  - Boolean flags labeled `(boolean)` in arguments section
  - `prefix` parameter applied to skill group directory, frontmatter name, H1, and all tool names
- [x] Update `cli.py` with `--prefix` option on `build` and `skill` commands (default `nerf-`)
- [x] Update all tests for new dataclass structure, `{{param}}` syntax, and prefix behavior
- [x] Write `nerftools/manifests/git/manifest.yaml` (merged nerf-git + gwat-git, no prefix)
  - 8 tools: git-add, git-commit, git-fetch, git-pull, git-push-main, git-push-branch, git-log,
    git-tag
  - Script guards: nothing-staged and co-authored-by on commit; not-on-main on push-branch;
    tag-already-exists on tag
- [x] Write `nerftools/manifests/az-pipelines/manifest.yaml`
  - 3 tools: az-pipelines-list, az-pipelines-runs, az-pipelines-check
- [x] Write `nerftools/manifests/az-wi/manifest.yaml`
  - 4 tools: az-boards-wi-show, az-boards-wi-comment, az-boards-mywi-show, az-boards-mywi-comment
  - Note: ownership verification (require_wi_owned_by_me) cannot be expressed as a guard
- [x] Write `nerftools/manifests/az-repos/manifest.yaml`
  - 3 tools: az-repos-pr-list, az-repos-pr-show, az-repos-pr-create
  - pr-create: --draft boolean flag, guards for detached HEAD and not-on-main
- [x] Write `nerftools/manifests/nx/manifest.yaml`
  - 6 tools: nx-show-projects, nx-show-project, nx-run, nx-affected, nx-graph, nx-reset
  - All tools guard with `test -f nx.json` (Nx workspace root check)
- [x] Write `nerftools/manifests/tg/manifest.yaml`
  - 10 tools: paired with/without --all variants for validate, init, plan, output, fmt
  - init and init-all: --upgrade boolean flag (flag: -upgrade)
  - fmt and fmt-all: --check boolean flag
- [x] Write `nerftools/manifests/pkgrun/manifest.yaml`
  - 3 tools: pkgrun-cspell v8.19.4, pkgrun-markdownlint v0.21.0, pkgrun-prettier v3.8.1
