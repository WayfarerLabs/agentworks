# Sweep Decision Inventory

**This is a working artifact for the duration of the prose-and-form-policing sweep, and it is
deleted when the sweep closes.** That is the effort lead's call and it is deliberate: this is a
subtraction pass, and a several-hundred-row ledger that outlives its purpose is exactly the kind of
thing the pass exists to remove. Where a keep row's justification is durable, it belongs at the test
site, not here. Nothing else in the repository should link to this file, and
[sweep-screen.py](sweep-screen.py) with the [sweep_screen](sweep_screen) package behind it, which
derive this file's group-1 estate, resolve its rows' anchors, and run its two screens, are deleted
with it.

It is step one of the sweep work item in [plan.md](plan.md). It decides nothing else: no deletion
lands with it. The deletions follow as the PRs the groups below are cut for: nine when the map
settled, eight after the 2026-08-19 re-baseline emptied group 2.

## Basis

**Cut fresh at `c310d05b` and rebased onto `0c8cf6bc`, 2026-09-06. The basis is `0c8cf6bc`.** Every
row here addresses something that exists at that commit, and every anchor resolves there or says on
its own row why it cannot. Main moved ten commits under the cut while it was in review (PRs `#756`
and `#759`), touching fourteen test files and adding one. Rather than re-cut, the map was rebased
and `restamp` was run, which is the workflow the grammar's `restamp` paragraph describes: it moved
five digests, each was read, and what it found is recorded on the rows themselves. Four were tests
main edited and every one of those rows still stands; the fifth was a digest written by hand in this
round's row fixes rather than by the command, and it was wrong, which is the argument for never
writing one by hand. The map before this one was cut at `c686cd6d`, re-derived at `426cccae`, and
re-anchored by identity on 2026-09-06; none of its counts survive into this file, because a count
carried forward is how it drifted twice. `ed0410c9` is the last commit that held it whole: the one
before the rows were keyed by identity, on this branch and so an ancestor of this commit, which is
what makes it reachable from a merge of this branch rather than only from a branch someone may
delete.

Four populations were re-derived to get here, and the fourth is the reason the other three were
needed.

**The 20 mixed-basis files.** Their rows had been lifted from line numbers whose tree nobody
recorded, and a measurement on 2026-09-06 found 144 rows that name a different test depending on
which of the two candidate bases they are read at. Auditing them was not possible, because 125 of
the 144 resolve cleanly and no tree can say whether they resolve to the RIGHT test. So the 128
non-group-1 rows among them were read from the file and written again from nothing.

**The 102 unsurveyed files.** Test files carrying no row at all, which the settled map's own
completeness scan had listed and left. 46 rows came out of them, over 30 files; the other 72 files
are accounted for as carrying nothing the criteria reach, each with its reason.

**The 8 drifted files.** Nobody's re-derivation covered these, because their rows were sound when
written; what moved was the tree under them, between `426cccae` where their anchors were taken and
this basis. Every row with an anchor that reached nothing was read at the basis: some are renames,
most are anchors whose assertion is gone because the work the row asked for has already landed, and
one is a finding rather than a change.

**The rows whose estate is gone.** 157 rows left the ledger because their file or their every site
no longer exists, group 2's whole estate among them, and part 2 retired 12 more on its own reading.
Three that looked gone were not: `test_create_resume_orchestrated.py` was renamed rather than
deleted, so G1-I06 moved with it. B-003 and B-004 moved with it too and then retired, because the
assertions they addressed had gone from the surviving function and only the function's name had been
rescued. `[dead]` and `[subtracted]` retire as markers with them; a row whose estate is gone is not
in the ledger for a marker to describe, and the subtraction those markers recorded is reversed
below.

### The 2026-08-19 subtraction is reversed

That round handed 117 rows to the secrets-preview and instance-model efforts on the premise that
each would take the estate into its own work. It did not hold. A scout re-check found the tests
still standing, and an independent cross-check by identity agreed with it on all 29 rows both said
were gone, so the disagreement was not about method.

**89 rows return to their groups with their original dispositions**: 45 that had gone to
instance-model and 44 to secrets-preview. The other 28 left with the ledger, their estate being gone
on any reading. The 34 deletes among the returning rows were re-screened as the grammar requires;
the injected-marker screen decided four of them against the batch and they are keeps now, as G1-I20
through G1-I23.

**instance-model is still a lock candidate with its live-validation gate open**, and its seven files
are back in this sweep's scope. Sequencing the sweep's edits against that gate is the operator's
call, not this map's; it is in the open questions.

### Reading this file mechanically

Several passes have counted this file and disagreed, always on how it was parsed rather than on what
it says. **[sweep_screen/inventory.py](sweep_screen/inventory.py) is the reference implementation of
the grammar**, so count with it rather than re-deriving one: `sweep-screen.py resolve` puts every
anchor here at its current line, and `sweep-screen.py attribute` checks the ownership claim below.

**Column 1 is the id, column 2 what the row addresses, column 3 the shape, column 4 the disposition,
and column 5 the justification.** The mechanical batch has no column 5, so it is four columns wide.
Every row state lives in column 3 as a bold marker, so an audit is "split the row, read column 1 and
column 3" with no heading state to track and no prose to read. Cells split on **unescaped** pipes
only, since `\|` inside a code span is content and a naive split turns one row into three, and only
a `##` heading changes the group, since the pulled-out blocks are `###` subsections of the group
they sit in.

#### Column 2 says what a row addresses, never where it sits

**This map has twice been killed by line numbers.** Each cut anchored every row to a line, an
unrelated edit above it moved every number in the file, and the map went on claiming sites it no
longer pointed at, silently, because a stale number still lands on something. A number cannot be
made stable, so it is no longer the key. Four anchor forms, in order of precision; a row lists as
many as it addresses, comma-separated, in one backticked group per file.

| Form                             | Addresses         | Resolves to                    |
| -------------------------------- | ----------------- | ------------------------------ |
| `path::qualname::Type::digest`   | one site group    | that group's current lines     |
| `path::qualname::Type::digest*3` | a group of three  | the same, three sites wide     |
| `path::qualname@dddddd`          | one test function | that span, and what it asserts |
| `path::L120,L204-211`            | literal lines     | nothing; go read them          |
| `path`                           | the whole file    | the file's existence           |

`::` separates the fields of one anchor and `,` separates anchors, so a group is one string a reader
can copy whole. Neither separator occurs inside a path, a qualname, a type name or a hex digest,
which is what makes the split unambiguous, and the `L` prefix is what tells a line anchor from a
qualname, since no test is named `L` followed by digits. A cell naming two files carries two
backticked groups; a tail with a second path in it is refused rather than guessed at, which is how
B-116 lost `test_secrets_resolver.py` for three cuts.

**A span anchor records what its function asserted when the row was cut**, as
`path::qualname@dddddd`: six hex of sha256 over the `ast.unparse` of every `assert` statement, every
`pytest.raises` or `assertRaises*` context manager, and every call to an `assert*` method in that
function's own body but not in a function nested inside it, taken in source order and with string
literals exactly as written. `resolve` reports `changed` when the tree disagrees, and a rewording
inside an anchored test is meant to trigger it.

**The two digests in this grammar have opposite rules, and that is deliberate.** A site digest
blanks interpolations, so a local rename cannot orphan a row. A span digest keeps literals as
written, because a reworded literal inside an anchored test is exactly what the row is about. Same
shape, opposite intent: do not read one as the other.

**A span anchor resolving is not the same as its row still being right**, which is why the digest
exists. Three rows retired in this cut because the assertion they addressed had gone while the
function survived and the anchor went on resolving; one of the three was a substitution of equal
size, which is why this is a digest and not a count.

**What the digest cannot see** is an assertion a test hands to a helper. Five anchored tests do that
and so digest as `e3b0c4`, over nothing: `TestCompleteness.test_powershell_contains_all_commands`
and `test_zsh_contains_all_commands` (F-152),
`test_nonempty_operational_lists_have_exact_ordered_json` (E-187), and
`test_no_color_env_forces_byte_plain_even_on_a_tty` and `test_non_tty_stream_is_byte_plain` (C-091).
A sixth, `test_split_shell_pane_warns_when_set_option_fails` (B-091), digests as `e3b0c4` for a
different reason found on 2026-09-06: it asserts nothing at all, which is a defect in the test
rather than a blind spot in the digest, and B-091 records it. Three more `e3b0c4` anchors are
helpers rather than tests and correctly assert nothing: `Exclusive._mutually_exclusive` (D-035),
`_agentworks_imports` (L-103) and `_read_module_source` (L-114). Following a call into a helper is a
whole-program question this grammar does not open. **The blind spot is wider than those nine.** Many
anchored tests have a non-empty digest and still hand part of their assertions to an `assert*`-named
helper, so the digest covers what the test asserts directly and says nothing about the rest.

**A digest is compared per anchor and never used as a key.** Two anchors sharing one is therefore
not a collision. It happens routinely: two tests generated from one `parametrize` assert the same
thing, and so do identical one-line helpers.

**`sweep-screen.py restamp` is what brings the digests up to date**, and it is the one command that
writes this file. Two things need it. An interpreter upgrade changes how `ast.unparse` spells a test
and invalidates every digest at once, which is why `resolve`, `restamp` and `totals` refuse a newer
interpreter rather than reporting every anchor as `changed`. The other five commands read the estate
rather than the digests and run anywhere at or above the floor. And a PR that edits an anchored test
changes its assertions on purpose, so it runs `restamp` and carries the map with it, instead of
leaving the next reader a `changed` verdict that means "we did that". It names every digest it
moves, and the reviewed diff is where an unexpected one gets caught. It checks what it is about to
write, and if it refuses it wrote nothing.

**The site identity is four fields, and none of them is a position.** `path` is the test file.
`qualname` is the innermost enclosing function, dotted through any class or nesting. Mind the
separator: that is a dotted path (`TemplateContractTests.test_shell_favicon_is_exact_and_shared`),
not pytest's `::` node-id spelling, so neither can be pasted into the other. `Type` is the asserted
exception or warning name exactly as written, or the assertion method's own name where the call
takes no type (`assertRegex`, `assertNotRegex`). `digest` is the first six hex of sha256 over the
needle's SKELETON: the literal with every interpolation blanked to `{}`, which covers f-strings,
`"..." % x` from its literal left operand, and `"...".format(...)` from its literal receiver. A
needle that is not a literal at all, being a name, a call or a subscript, has no skeleton to take
and digests as the string `<expr>`, whose digest is `d62e9a`; 20 sites at HEAD do, so such sites in
one test asserting one type are one identity with a multiplicity.

**The skeleton is deliberately blind to what is interpolated**, and that is a trade made on purpose.
Keying on the interpolated expression would tell `f"a{x}b"` from `f"a{y}b"`, and would also mean
that renaming a local variable inside a test orphans the row naming the site, which is exactly the
drift this whole grammar exists to retire; 24 sites in this estate carry an interpolation and would
have been that fragile. So two sites in one test that differ only in what they interpolate are one
identity with a multiplicity of two. Their disposition is the same, a row addresses both alike, and
`grown` and `shrunk` are what report the difference if one of them ever goes.

**`sweep-screen.py estate` reports the needle population and the shortest prefix that separates
it.** Six hex is the shortest prefix that separates all of them. Five collides once, over two
needles; four collides twice, over four.

**Sites that tie on all four are one identity, not several.** An earlier cut separated them by
1-based source order, which reintroduced the exact drift this grammar exists to retire: inserting an
identical assertion ABOVE a tied site reassigned the existing row to the new site, and nothing said
so. Order cannot be made stable. Identical assertions in one test carry identical evidence and a
row's disposition applies to all of them alike, so the identity names the whole group, a row claims
every site in it, and `*n` records how many the row was cut against. A group that has since grown or
shrunk then resolves as `grown` or `shrunk`, with both counts, rather than quietly covering a
different number of assertions than the row was written against.

**The tie population is small and named.** `sweep-screen.py estate` reports the estate's size, the
identities in it, and every identity that names more than one site, each of those a test asserting
the same needle against the same type twice. `Snapshot` refuses to build unless the groups cover
every site exactly once, so the partition the command prints is the whole estate rather than a
sample of it.

**The states an anchor resolves to.** `resolved` is what the row was cut against, at whatever line
it now sits. `grown` and `shrunk` are a site group that changed size, with both counts. `retargeted`
is the same test asserting the same type against a different needle, which is what a reworded
message leaves behind. `gone` is the file present and the anchor not in it, `file-gone` is the file
itself, and `line-anchored` is a line anchor, which resolves to nothing by construction. `changed`
is a span anchor whose function no longer asserts what it did, and `unstamped` is one that never
recorded what it asserted. Those are all of them.

**Only `resolved` and `line-anchored` pass.** `totals` faults on every other state, which is what
makes a row addressing something that is not there a failure rather than a line in a listing. A line
anchor is exempt because it declares up front that it resolves to nothing; a `changed` digest is not
exempt, because absorbing one deliberately is what `restamp` is for.

**A site anchor beats a span anchor in the same function, in group 1 only.** Group 1's rows address
`match=` sites, so where a function holds sites such a row cited, those sites are the whole claim: a
span anchor beside them would claim every other site in the same test for the same row, and that is
what keeps "every `match=` site is claimed by exactly one group-1 row" checkable. Every other
group's rows address assertions the estate scan cannot see, so their enclosing function is anchored
too, with any cited sites alongside it, unless a function the row already names encloses it: a
helper nested inside a cited test is named by that test. Dropping the span narrowed 39 rows onto
sites they were never about: 22 in group 5, 12 deferred and 5 in group 4.

**A span anchor is deliberately coarser than the row.** It names the test, not the assertion inside
it, so several rows can share one span anchor and column 3 is what tells them apart. That is the
price of anchoring groups 3 to 6 to a name rather than a number, and it is the right price: a test
name survives every edit above it, while the line does not. It also means a resolved span says the
test is still here, not that the assertion inside it is; only a site anchor says that.

**A line anchor is a declaration that the row will go stale**, so the row says on itself why, in a
`[line-anchored: <cause>]` marker in column 3, written by hand and refused if it names a cause
outside this set. Three causes remain:

- **not Python**: no AST to read, which is the `.mjs` rows that cite lines. The `.mjs` rows that
  name their file and no lines are file anchors instead.
- **between functions**: the number lands on a blank line, a divider comment or the gap after a
  test's last line, which is the cause that says the number was read at a tree this map no longer
  matches.
- **module level**: the lines are real and outside any function, as
  `test_builtin_entries_parity.py`'s `EXPECTED_*` constants are.

`sweep-screen.py resolve` counts the line anchors that remain, per group and in total, alongside the
anchors that resolve to a name, and it BOUND-CHECKS each one against its file. `line-anchored` is
the one failing-looking state that passes, which made it the one place a number could be wrong and
still report success: E-126 carried an `L787` into a file 351 lines long and every command stayed
green. A line past the end is not a declaration that a row will go stale, it is a row that already
has, so it resolves as `past-end` and `totals` refuses it. A fourth cause, **file gone**, retired
with the rows it described: those rows are no longer in this map, so nothing can be line-anchored to
a file that is not there and the parser refuses the cause.

#### A cell carries no bare number

**Every line citation this map has written is a standalone integer, so that is what is refused.**
Nine spellings were gated one at a time before this rule replaced them, and the seven that survived
the round before it (`Keep 325-326`, `Replace 327`, `444's`, `on 243-244`, `(104)`,
`(438, 439, 718, 719)`, `now own 125, 134, ... and 298`) differ from each other only in the
punctuation around the digits. Enumerating punctuation is how this gate kept losing.

Four things are not that integer and are blanked before the scan: **an anchor or identity token**,
which includes a row id, a line anchor's `L120-211` and everything inside a code span, since that is
where this file writes what it quotes; **a date**; **a SHA or hex digest**; and **an issue or PR
reference**. Two more are checked at the match, because a number there is doing something other than
pointing: it may NAME a thing (after `ruling`, `case`, `issue`, `PR`, `RFC`, `phase`, `version`,
`wave`, `priority` or `slot`) or COUNT one (before `sites`, `tests`, `rows`, `files`, `anchors`,
`attempts`, `pins`, `cycles`, `commits`, `values`, `names`, `topics`, `pixels`, or `lines`, which
counts only in that position because `line 412` with the noun first is a citation).

**Over-selection is the intended failure.** A sentence this refuses that was citing nothing is a
sentence to rewrite, and that costs one edit; a citation it misses is an executor editing whatever
moved into that line's place. A literal that is genuinely a value goes in a code span, which is
where a `0750` mode and a `404` page belong anyway.

**To write a row by hand**, run `sweep-screen.py estate` and copy the identity it prints for the
site, or `sweep-screen.py resolve` to see what the rows around yours address. A site digest is a
tiebreaker nobody needs to read, and `estate` prints the needle beside it. Do not write a SPAN
anchor's digest by hand at all: leave it off and run `restamp`, which is the only thing that gets it
right, and `totals` refuses an unstamped anchor so a forgotten one cannot pass quietly.

**One thing in column 2 carries no meaning**: backticks around a group are decoration, not a signal.
Every anchor names its file by full path, which is what lets the accounting be a subtraction rather
than a recipe; a cell that named a file any other way would be refused.

**The row tables carry `<!-- prettier-ignore -->` and are not padded.** Prettier pads every cell in
a table to the widest cell in its column, and an identity is much wider than a line number, so
letting it format these ten tables costs 2.2MB of trailing spaces and puts the file past what an
executor can load. The prose tables in this file are untouched and still padded.

## How a row was decided

The standard is the always-on `no-prose-policing-tests` rule, as generalized by R1.2 to every
authored artifact, plus [hla.md](hla.md)'s doctrine 2, its source-guard split, and its three-case
`match=` taxonomy. [frd.md](frd.md) R2.2 bounds every row: no row proposes a new production type or
a shipped-contract change, so where preserving an assertion would need a production discriminator,
the row is `delete` and the branch coverage goes with it (R2.4).

- **keep** names the invariant that earns the assertion and what regression it catches. A row that
  could only say "this looks behavioral" was not written; it was read again until it said something.
- **delete** says why nothing real is lost.
- **convert** names the structural replacement and confirms it needs no production change.

Two rules narrowed the keeps more than a shape-based reading would.

**A claim about what an assertion protects was executed, not read.** Four deletions earlier in this
pass were correct while their stated premises were false, each caught only by running. So where a
disposition turned on "some other test already covers this" or "production already enforces this",
the claim was tested. The rows where that changed the answer are called out in the report; they are
not marked in the table, because a row's justification should read the same whether or not it was
the one that surprised us.

**A check is only worth keeping against a guarantee the artifact can hold.** For anything in this
tree, the threat model is accidental regression, never adversarial tampering: whatever can edit the
thing under test can edit the test beside it. That is the correction W1 arrived at after four
rounds, and it is why no row here is justified as proving that nobody can subvert something.

### Two screens every delete row runs first

The mechanical batch's shared justification makes two claims about each site: that the raised type
discriminates the branch, and that the matched string is prose this repository authors. Each has its
own screen below, both are **mandatory for every delete row over a site the screen reaches** and not
only group 1's, and each has been shown false for a real population here. What they reach is every
site that asserts a TYPE and constrains its message: `pytest.raises(X, match=)` under `cli/tests`
and `assertRaisesRegex(X, ...)` under `website/tests`. The nine `assertRegex` and `assertNotRegex`
sites assert a message with no type at all, so there is no callee to ask about and they are
unscreened by design; the rows carrying them are F-070, F-094, F-099, F-100, F-103, F-104, F-118,
F-119 and F-120, and a delete over one of those rests on the injected-marker screen and the reader
alone. They ask different questions and neither sees the other's class: the callee screen asks about
raise paths, the injected-marker screen asks who wrote the string.

**Extending the screens to `website/tests` settled the injected-marker question and not the callee
one.** `injected` now walks both test roots and reports no injected-marker site under
`website/tests`, which is an answer rather than a silence. The callee screen indexes the website's
production modules now too, and all 49 `assertRaisesRegex` sites there still come back `unresolved`:
every one calls through `site_builder`, which `site_test_support.py` binds by
`importlib.util.spec_from_file_location` and re-exports through a star import. Following that would
mean constant-folding four `Path` expressions across a module boundary, and a resolver that guesses
wrong reports confident verdicts about the wrong module, so those 49 are unresolved by construction
and the rows over them rest on the reader.

Where either screen is silent, the row is not screened. Both are run by
[sweep-screen.py](sweep-screen.py) and neither can decide a site whose callee the resolver cannot
reach; for those, the third method below applies.

### The callee-side raise screen

**Every delete row that drops a `match=` or an `assertRaisesRegex` runs this screen first, and the
row records the result.**

`hla.md` case 1 says to delete the `match=` where "the raised type already discriminates". That is a
claim about the CALLEE, and the settled map established it from the caller: a test function raising
one exception type once was read as a type that discriminates. It does not follow. If the operation
under test can raise the SAME type from a different path, dropping the `match=` lets the wrong
failure satisfy the test, and case 1's rationale collapses into case 2, whose first arm is to
discriminate structurally on a handle the code already offers.

The screen asks one question per site: **within the operation this test calls, is the asserted
exception type raisable from more than one distinct path?** Three answers, three dispositions.

- **One path.** Case 1 holds. The row keeps its delete and gains the `[1-raise]` marker.
- **Several paths, and a structural handle tells the targeted one apart.** Case 2's first arm: the
  row **converts** onto that handle, which is already in production, so R2.2 is untouched.
- **Several paths and no discriminator.** Case 2's fallback: the row stays a **delete** and the row
  says what it costs, which is that a same-type failure from another path in the same operation will
  now satisfy the test.

#### Method, and what it can and cannot answer

Derived from the AST at whatever tree `screen` runs against, not from reading. Names resolve through
the calling module's own namespace (its imports, its module aliases, its top-level definitions), so
`_validate` in one module is not confused with `_validate` in another; a call on a value whose type
is not known statically does not resolve at all and is reported as unresolved rather than guessed.
From each `pytest.raises` body the walk follows every first-party function it calls, transitively,
and collects every `raise` of the asserted type or a subclass. The targeted raise is identified by
matching the `match=` text against each reachable raise's message template, and the handle question
is then asked of that raise specifically: is its `entity_kind` or `entity_name` unique among the
raises the same call can produce.

**Positives are sound; negatives are not.** A site the screen calls multi-raise-path genuinely has
more than one reachable raise of that type. A site it calls single-raise-path is single as far as
the resolver reached, and a site it cannot resolve is not screened at all. So `[1-raise]` means
"screened and single", never "checked by hand and safe".

**The outstanding screen debt is every site the screen has not settled**: the ones whose callee it
could not resolve, plus the ones it resolved to several raises but could not tie to a targeted one.
A multi-raise-path site with a verdict does NOT need re-screening; what it needs is the disposition
the screen already gave it. `sweep-screen.py screen` partitions the estate by verdict and counts
each part, so the debt is a number the command produces at whatever tree you run it against, rather
than one this file carries and has to keep true. Anywhere else in this file that talks about what
the executor still owes means that partition.

**Owing the screen on an unresolved site means reading the called code by hand and recording the
answer on the row, not re-running the script.** Re-running it returns "unresolved" again, by
construction: the resolver already told you it cannot reach that callee. The hand answer is the same
question the script asks. Open the operation the test calls, find every `raise` of the asserted type
it can reach, and if there is more than one, say on the row whether a handle tells the targeted one
apart. The sites the mutation screen below turned into named rows were all in this bucket, which is
what that debt looks like when it is paid.

#### What it found in group 1

`sweep-screen.py screen` runs over every `pytest.raises(..., match=)` site under `cli/tests` and
prints one line per site, with its verdict, the targeted raise and the handle values behind that
verdict. Five verdicts are possible:

- **Multi-raise-path, a structural handle tells the targeted raise apart.** Settled: convert.
- **Multi-raise-path, no discriminator.** Settled: delete.
- **Multi-raise-path, targeted raise not identified mechanically.** Not settled.
- **Single-raise-path.** Settled: delete.
- **Callee did not resolve, or reached no raise of the asserted type.** Not settled.

Only two of them are carried on rows: the `G1-C` converts, and the `[1-raise]` marker, which
`generate` derives from this screen rather than a reader copying it across. Every other site's
verdict lives in the command's output, which is why the command is committed beside this file.

**Most of what the screen can resolve is multi-raise-path, by a wide margin.** That is the finding,
and it is larger than the rows it moves: the group's defining claim, that the batch needs no
judgment because the raised type discriminates, does not survive contact with the code those tests
call. The dispositions mostly do not change, because case 2's fallback lands on the same edit as
case 1, but the reason does, and so does what the batch costs. The mechanical batch's shared
justification now says so.

The sites with a handle convert, and are the `G1-C` rows below. The delete rows the screen verified
single-raise-path across every site they claim carry `[1-raise]`. Every unsettled site is the
executor's, per row, before the edit lands.

### The injected-marker screen

**Every delete row runs this one too.** It tests the mechanical batch's other claim, that the
matched string is prose this repository authors, and the callee screen cannot see this class at all:
it asks about raise paths, this asks who wrote the string.

Where a test injects a failure and then matches on the string it injected, the `match=` is not
pinning anything authored. It is the proof that the INJECTED failure is the one observed, which is
the whole content of the assertion. L-004, L-008, L-009 and L-010 keep for exactly that reason, and
the screen finds the rest of that family rather than leaving it to whoever reads a row closely.

The rule, which `sweep-screen.py injected` implements: a site qualifies when its needle appears in a
string its own module hands to an exception constructor AND appears in no string production emits.
Both halves matter. Without the first it is not a marker. Without the second the test may be quoting
a shipped sentence through a fake, which is the shape the batch correctly deletes: two sites in
`test_activation.py` match `"manually stopped"`, which reads like a marker and is a copy of the
refusal `vms/nodes.py::LiveVMNode.auto_start` raises
(`f"VM '{self._row.name}' was manually stopped so it will not be auto-started"`), so they stay
deleted. Comments and docstrings are excluded from the production side, because a phrase a docstring
discusses is not one the code says.

**Every live site it finds in the mechanical batch is a keep**, rowed as a `G1-I` row below.
`sweep-screen.py injected` prints them, along with the one site whose needle wraps its marker in
authored prose, which the command reports as `mixed` rather than deciding. Verdict: on those sites
the batch's shared justification was not a judgment call that went the wrong way, it was a false
statement of fact.

### The mutation screen, for what neither of the above reaches

Both screens above are static, and the sites the resolver cannot reach are beyond both. For a site
an executor cannot decide by reading, the question has an empirical answer: **break the behavior the
test is named for, and see whether the test still fails once the `match=` is gone.** Fail with the
assertion and pass without it means the assertion is the only probe, and the row keeps.

This is not a screen to run over hundreds of sites; it is what to do with the handful a reading
leaves genuinely uncertain. The sites decided this way on 2026-08-19 are the `G1-M` rows, and the
result is why the method is recorded rather than the sites alone: **most of them keep and one does
not**, and no reading of the rows predicted which. The batch justification would have deleted every
one.

## Groups

The rows are batched by **shape**, not by domain, so each group can land as one PR and a review
round matches the kind of judgment the batch needs. Batching by domain instead would put the
no-judgment mechanical change into the same review as the sweep's riskiest deletions.

| #   | Group                                         | PR shape                                                    |
| --- | --------------------------------------------- | ----------------------------------------------------------- |
| 1   | Mechanical `match=` narrowing                 | No judgment: the `match=` argument goes, the `raises` stays |
| 3   | Report lines and hints                        | The judgment-heavy batch                                    |
| 4   | Schema, manifests, capabilities and platforms | Structured-refusal assertions                               |
| 5   | Authored-artifact form policing               | Config, completions, generated samples, CSS, website        |
| 6   | Source guards                                 | The family `hla.md` settles, decided per guard              |

## Totals

**`sweep-screen.py totals` is this section**, counted off the row markup rather than carried
forward, because a total carried forward is how this map drifted twice. Two figures matter and they
are different: the LEDGER is every row the map holds, and the EXECUTABLE SET is what the sweep owns
once the deferred block comes out.

**The executable set is every live row over five groups**, counted in the Totals table below.

**The ledger is the executable set plus the `[deferred]` rows.** Nothing else is subtracted from it:
a row whose estate is gone is not in the ledger at all, which is what changed on 2026-09-06 when
`[dead]` and `[subtracted]` retired.

<!-- prettier-ignore -->
| Group | Live | delete | convert | keep | Deferred | Ledger |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1. Mechanical `match=` narrowing | 220 | 155 | 7 | 58 | 0 | 220 |
| 3. Report lines and hints (four sub-batches) | 342 | 157 | 92 | 93 | 0 | 342 |
| 4. Schema, manifests, capabilities and platforms | 252 | 53 | 96 | 103 | 0 | 252 |
| 5. Authored-artifact form policing | 248 | 98 | 18 | 132 | 0 | 248 |
| 6. Source guards | 71 | 25 | 11 | 35 | 0 | 71 |
| Deferred (held for R4) | 0 | 0 | 0 | 0 | 25 | 25 |
| **All** | 1133 | 488 | 224 | 421 | 25 | 1158 |

Group 1's site counts are derived exactly, because that estate is scanned rather than read.
`sweep-screen.py estate` is where the site figures come from, one site per line, and
`sweep-screen.py attribute` is what checks the ownership claim; both are run at the basis and
`attribute` exits non-zero when the claim is false. **Every `match=` site under `cli/tests` is
claimed by exactly one group-1 row**, with none claimed by two and none unclaimed, and `generate`
exits zero against the same estate. The regex-family sites under `website/tests` are split three
ways rather than two, between group 1, group 5 and the deferred rows; `attribute` prints which row
owns each. The other groups were read rather than scanned, and one row there covers an assertion
group of one to a dozen lines, so no site total is claimed for them.

One row is one test or one contiguous assertion group, so a file that mixes wholly-policing tests
with prose assertions riding inside legitimate ones appears several times. Group 1's mechanical
batch is the one exception and says so where it starts: those rows are one per FILE, because the
sites they carry share a single shape, a single disposition, and a single justification.

This file is long for the repository's 500-line guidance, deliberately. It is a ledger, it is
temporary, and one row per line is what makes it checkable against a diff.

## Markers this file uses

Every row state rides in the shape cell as a bold marker, so a mechanical count can separate the
executable set from the rest without reading prose, and so nothing is deleted from the ledger that
the R4 reassessment might want to audit. An unmarked row is live and executable.

One marker takes a row out of the executable set:

- **`[deferred]`**: the row is held for the R4 reassessment, per the deferred block.

Two annotate a row that stays in it:

- **`[1-raise]`**: the callee-side raise screen verified every site this row claims as
  single-raise-path, per the screen's section above. It is carried on delete rows, which are the
  ones the screen decides; a keep row keeps its `match=` either way.
- **`[unverified]`**: the row's disposition turns on a coverage claim that was reasoned rather than
  executed, and its justification names which claim.

And one is derived rather than judged:

- **`[line-anchored: <cause>]`**: the row keeps literal lines, and this is why. Every one this map
  carries was written while `reanchor` existed; that command retired with the fresh cut, so a row
  that gains literal lines from here on carries its cause by hand. The parser still refuses a cause
  outside the vocabulary. It leaves the executable set exactly as it found it.

**`[dead]` and `[subtracted]` retired on 2026-09-06.** A row whose estate is gone is no longer in
the ledger, so there is nothing left for `[dead]` to describe, and the subtraction `[subtracted]`
recorded is reversed. Both are gone from the parser's vocabulary too, so one written by hand is now
a refusal rather than silent row state.

Ids say where a row came from. `RB-` marks the rows the 2026-08-19 re-baseline added, and `G1-C`,
`G1-M` and `G1-I` the rows its three screens pulled out of the mechanical batch (the callee screen,
the mutation screen and the injected-marker screen respectively). `G1-K17` and above are the ones
the 2026-08-16 harvest added, which is why those rules do not cover them. An id is never reused: a
row that leaves is retired, never replaced, and the retired list below says what happened to it. A
citation that names neither a row nor a retired id is a fault `totals` refuses rather than a row
someone has to go looking for.

## Rows this cut retired

**Every id this cut has used and no longer uses is here**, derived from the map at every commit of
this branch since the part fold rather than added by hand as rows left. The part cuts' own
retirements are in it too: C-177 and C-187 were removed as duplicates before the fold and are
recorded here rather than left to the branches they happened on. That is what makes the list worth
checking a citation against: a gap in it would resolve as "not a row" and send a reader looking.
`totals` reads it, so prose may name a retired id, and a citation of an id that is neither a row nor
retired is a fault it refuses. `generate` reads it too, so a new file takes an id above every one
this cut has ever issued rather than reissuing a retired one.

**What the tooling enforces about this list, and what it does not.** It refuses an id that is both
live and retired, it resolves a citation against it, and it allocates above its maximum. It cannot
check that the list is COMPLETE, because completeness is a statement about commits that are no
longer checked out. That was derived once, at this commit, from the map at every commit since the
part fold; a row retired after this one is added by whoever retires it.

**The scope is this cut's namespace only.** An id the 2026-08-19 map used and this cut never carried
is that map's, recorded by `ed0410c9`, and is not listed here; this list starts where that map's
numbering stopped being ours.

**Two numberings meet here.** The ids marked `(2026-08-19 map)` are that map's, which numbered its
mechanical batch by position, so regenerating the batch handed several of them to different rows.
They are a separate namespace: a citation meaning one of those says `(2026-08-19 map)` after it and
resolves only against this list, while an unqualified id always means a row of this cut. From this
cut forward the question does not arise, because `generate` keeps the id a file already has and
allocates a new one above every id the map has used.

<!-- prettier-ignore -->
| id | What happened |
| --- | --- |
| A-001 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-002 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-003 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-004 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-005 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-006 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-007 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-008 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-009 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-010 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-011 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-012 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-013 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-014 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-015 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-016 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-017 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-018 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-019 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-020 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-021 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-022 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-023 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-024 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-025 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-026 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-027 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-028 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-029 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-030 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-031 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-032 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-033 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-034 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-035 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-036 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-037 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-038 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-039 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-040 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-041 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-042 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-043 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-044 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-045 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-046 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-047 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-048 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-049 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-050 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-051 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-052 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-053 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-054 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-055 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-056 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-057 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-058 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-059 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-060 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-061 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-062 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| A-063 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-003 | Retired 2026-09-06. `d99835da` removed the banner pins and all four `Role.*` assertions from `test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary`, so the estate both rows addressed is gone. The fresh cut's rename rescue moved them with their file and did not notice, because a span anchor resolves on the function surviving. |
| B-004 | Retired 2026-09-06, with B-003 and for the same reason. |
| B-005 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-006 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-049 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-050 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-051 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-060 | Retired 2026-09-06. `b8f94fda`, the backend preview contract rewrite, removed the `format_outcome(...).endswith(...)` assertion this row addressed; `test_declared_source_accepts_string_subclass_plugin_attribution` survives and now asserts preview status and block reason, which no row here proposes to touch. The `remediation` and `remediation_target` fields the row credited with carrying the behavior are gone from production too: `format_outcome` (`secrets/outcomes.py::format_outcome`) renders status, reason, source, identifier and hint. |
| B-061 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-072 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| B-074 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-075 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-076 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-077 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-077a | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-078 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-093 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-094 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-095 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-096 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-097 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-098 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-099 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-100 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| B-103 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-104 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-105 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-106 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-107 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-108 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-109 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-110 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-111 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-112 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-124 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| B-125 | Dropped by `62d42215`: its estate is gone at this cut's basis. |
| C-004 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-005 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-006 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-007 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-031 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| C-071 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| C-072 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| C-099 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| C-100 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| C-103 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-147 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-148 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-149 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-150 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| C-177 | Removed as a duplicate of E-167, which carries the disposition. |
| C-187 | Removed as a duplicate of E-178, whose scope was widened to cover it. |
| D-024 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| D-107 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| D-145 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| D-169 | Split by `5be6e684` into lettered rows, which carry the evidence. |
| E-095 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| E-096 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| E-097 | Dropped by `62d42215`: its estate is gone at this cut's basis. |
| E-112 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| E-160 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| F-004 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| F-019 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| F-020 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| F-021 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| F-022 | Renumbered by `efffb7a4`, which folded the three part cuts into one numbering. |
| F-129 | `onboarding-copy.test.mjs` was replaced by `onboarding.test.mjs` at `1b9bbb9b` (2026-08-17); RB-012 is the successor and keeps the property. |
| F-149 | Split by `5be6e684` into lettered rows, which carry the evidence. |
| F-184 | Split by `5be6e684` into lettered rows, which carry the evidence. |
| G1-013 (2026-08-19 map) | Every site it claimed moved to G1-K19. |
| G1-062 (2026-08-19 map) | Every site it claimed moved to G1-C02. |
| G1-099 (2026-08-19 map) | Every site it claimed moved to an injected-marker row, the file holding nothing but injected markers. |
| G1-123 (2026-08-19 map) | As G1-099 (2026-08-19 map). |
| G1-135 (2026-08-19 map) | As G1-099 (2026-08-19 map). |
| G1-140 (2026-08-19 map) | As G1-099 (2026-08-19 map). |
| G1-141 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-142 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-143 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-144 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-145 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-146 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-147 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-148 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-149 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-150 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-151 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| G1-152 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-153 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-154 | Superseded by `24fd2281`, which regenerated group 1 at the basis; the sites are in the regenerated batch. |
| G1-155 | Superseded by `24fd2281`, which regenerated group 1 at the basis; its sites are in the regenerated batch. |
| G1-156 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| G1-I02 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| G1-K04 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| G1-K14 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| G1-M03 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| G1-M04 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| G1-018 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-023 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-031 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-034 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-057 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-067 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-070 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-113 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-114 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-115 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-125 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-126 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-136 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| G1-137 (2026-08-19 map) | A mechanical-batch row of that map. A screen pulled one of its sites out into a `G1-I` row here, and the rest were regenerated into this cut's batch under new ids. |
| L-001 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-013 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-018 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-102 | `guide/test_power_import_boundary.py` was deleted; E-211 is the guard that survived it. |
| L-115 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-122 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-301 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-302 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-303 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-304 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-305 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-306 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-307 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-308 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-309 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-310 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-311 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| L-312 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| RB-002 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| RB-003 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| RB-004 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| RB-006 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| RB-007 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| RB-008 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| RB-009 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |
| RB-010 | Dropped by `62d42215`: the row's file or its every site is gone at this cut's basis. |

## What the 2026-08-19 re-baseline measured

That round's estate table, its dead-row list and its rows-added list described a tree two bases ago
and are not carried forward; this cut re-derived all of it, and has re-derived it again since
against the basis the Basis section names. The Basis says what moved and the Totals section says
what the map now holds. `ed0410c9` is the last commit that held those figures, and git history is
where they live. Its vocabulary went with them: no row here is dead or subtracted, because a row
whose estate is gone left the ledger and is in the retired list instead.

## Files with no row, and why

**`sweep-screen.py totals` names every test file the map leaves alone.** The population is a
`test_*.py` module under `cli/tests` or `website/tests`, or a `*.test.mjs` suite under the
website's, and a file is addressed when any row's anchor names it. An anchor names its file
outright, so this is a subtraction the command performs rather than a recipe a reader re-executes,
and the command refuses if this table's membership and its own answer disagree.

Each of these files was read. Every one is out of scope for one of four reasons: it has no in-scope
operand at all (no `match=`, no `assertRaisesRegex` family member, and no multi-word string literal
in an assertion operand position); its literals are text the code emits to another system, which is
behavior at a boundary; its literals are values the test itself seeded; or its assertions are
derivation parity over a fixture the test authors. A file whose estate another effort also touches
says which one.

<!-- prettier-ignore -->
| File | Why no row |
| --- | --- |
| `cli/tests/agents/test_install_commands.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/capabilities/test_secret_backend_client.py` | Boundary validation at the secret-backend contract plus a redaction defense. In the secrets-preview estate, and hands over nothing. |
| `cli/tests/capabilities/test_secret_backend_relocation.py` | No in-scope operand: no `match=`, no regex-family member, and no multi-word string literal in an assertion operand position. |
| `cli/tests/db/test_read_transaction.py` | In the instance-model estate, which is back in scope. Snapshot isolation, nesting refusal and post-close behavior; the only strings are `entity_kind == "database"`. |
| `cli/tests/guide/test_release_history.py` | Topic-identifier mapping, unsafe-payload refusal, and size bounds. The topic strings are a route identifier, not prose. |
| `cli/tests/guide/test_shell_catalog.py` | Derivation parity over a fixture the test authors. Markdown in, rendered markdown out. |
| `cli/tests/guide/test_shell_commands.py` | Derivation parity between authored guide markdown and the live CLI spec. It asserts that documented commands exist, never how they are worded. |
| `cli/tests/manifests/test_decode_fill.py` | The one string needle is the value the test's own fixture template renders, so it pins nothing authored. |
| `cli/tests/manifests/test_editor_association.py` | The modeline expectations are built from `MODELINE_PREFIX`, `SCHEMA_DIRNAME` and `ENVELOPE_SCHEMA_FILENAME`, and the end-to-end test validates each written sample against the schema its own modeline names. Derivation parity, not a form pin. |
| `cli/tests/manifests/test_inherited_capability_config.py` | Schema-versus-loader divergence driven by a fixture plugin; the one quantified assertion (`required == ["name"]` per registered arm) is computed over the live capability registry. |
| `cli/tests/orchestration/test_secrets.py` | No in-scope operand, by the same scan. |
| `cli/tests/plugins/gcp/test_names.py` | Golden vectors for a collision-safe cloud naming formula. The names address real provider resources, so they are a contract, not a spelling. |
| `cli/tests/plugins/test_azure_logging.py` | Logger level and `isEnabledFor` behavior; no strings asserted. |
| `cli/tests/plugins/test_cloud_bootstrap_secret_boundaries.py` | Secret-redaction and single-ephemeral-join defenses; the cloud-init needle is a blank key slot, which is structure. |
| `cli/tests/plugins/test_grok.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/plugins/test_onepassword.py` | No in-scope operand, by the same scan. |
| `cli/tests/plugins/test_provider_config_strings.py` | Blank rejection at the operator-manifest boundary, plus model-to-JSON-schema parity. R2.1 keeps both. |
| `cli/tests/resources/test_declared_resource.py` | Field defaults, required-ness, and which fields reach the emitted schema for a model the test declares. |
| `cli/tests/resources/test_inheritance_merge.py` | Every value compared is one the test's own `declared` fixture supplied, and the suite carries its own non-vacuity guard. |
| `cli/tests/resources/test_live_publication.py` | Registry finalization, live graph edges, and snapshot semantics. The two CLI needles are machine-readable names-only lines, which is the contract. |
| `cli/tests/resources/test_reference.py` | Frozen-dataclass and subclass invariants; no assertion touches authored form. |
| `cli/tests/resources/test_schema_directed_domain_merge.py` | Merge semantics and provenance over test-authored inputs. |
| `cli/tests/resources/test_show.py` | Value identity on the test's own input: `test_show.py::test_service_projects_declarable_and_capability_rows` reads back the description seeded at `test_show.py::_request_context`. |
| `cli/tests/resources/test_singleton_publishing.py` | Registry structure and provenance (`variant`, `ALWAYS_MATERIALIZE_SOURCE`, one row per singleton kind) over manifests the test wrote. |
| `cli/tests/schema/test_extract.py` | Fixture models in, extracted references out. The one verbatim usage pin (`== "the Proxmox API token"`) reads back the string `_fixture_models.py`'s `token_secret` field declares. |
| `cli/tests/schema/test_extract_completeness.py` | An oracle built from the validated object is compared against the walker; both sides are the test's own fixtures. |
| `cli/tests/schema/test_extract_totality.py` | Totality over an adversarial corpus and a seeded generator, with per-model non-vacuity; the hand-written `_EDGELESS_BY_DESIGN` map is asserted in both directions, so it cannot go stale silently. |
| `cli/tests/schema/test_fill.py` | Fill behavior over models the test declares; every rendered value comes from a `default_template` the test wrote. |
| `cli/tests/schema/test_merge.py` | Merge behavior and contract-violation detection asserted as `merge_contract_error(...) is not None`, which pins no message. |
| `cli/tests/schema/test_owner_templates.py` | Emitted-schema structure for fixture models (required, nullable, marker placement), plus the validation-and-extraction agreement. |
| `cli/tests/secrets/test_batch_completion.py` | Batch dooming and typed block reasons. In the secrets-preview estate, and hands over nothing. |
| `cli/tests/secrets/test_disabled_plugin_attribution.py` | No in-scope operand: the preview status and block reason it asserts are enum members, and the plugin attribution is an identity check on the value the test seeded. B-060 addressed a `format_outcome` assertion that `b8f94fda` removed, and retired with it. |
| `cli/tests/secrets/test_line_safety.py` | In the secrets-preview estate, which is back in scope. A structured refusal plus the injected-sentinel leak defense named in the preamble. |
| `cli/tests/secrets/test_outcomes.py` | The rendered needle is the outcome's own status value, so it is derivation rather than a pin. Secrets-preview estate, nothing to hand over. |
| `cli/tests/secrets/test_result_precedence.py` | In the secrets-preview estate, which is back in scope. Precedence matrices over production enum members; every expected reason is `SomeEnum.MEMBER` or `.value`. |
| `cli/tests/sessions/test_grok_build_orchestrated.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/test_apt_declared_at.py` | Source-location provenance for manifest-loaded entries. |
| `cli/tests/test_azure_collision.py` | Fail-closed probe behavior at the provider SDK boundary, with the cause chain as the assertion. |
| `cli/tests/test_cli_helpers.py` | `parse_csv_filter` behavior over the test's own input. |
| `cli/tests/test_command_checks.py` | Login-shell resolution and a redaction defense. Platform-conditional through `requires_posix_shell`, which is CI coverage. |
| `cli/tests/test_config_line_capture.py` | `declared_at` file and line capture over manifests the test wrote. |
| `cli/tests/test_config_section_line_scanner.py` | Parser behavior over inputs the test authors. |
| `cli/tests/test_console_lifecycle.py` | Tmux model state plus emitted command text, per the preamble. |
| `cli/tests/test_console_streams.py` | Legacy-console encoding and newline behavior, byte-exact. Platform-conditional and CI coverage. |
| `cli/tests/test_consoles_attach.py` | Emitted command text: the three literals are the `tmux` invocations the console sends, socket and target included. |
| `cli/tests/test_consoles_reorder.py` | Database order, forwarded kwargs and exit codes. |
| `cli/tests/test_db_migration_harness_state.py` | Historical migration v29 column and backfill facts. Instance-model estate, nothing to hand over. |
| `cli/tests/test_debian.py` | Release parsing and registry-order classification, discriminated on `entity_kind`. |
| `cli/tests/test_debug_signal.py` | `debug_enabled()` and the `AGW_DEBUG` mirror; the only literal is the env var's own `"1"`. |
| `cli/tests/test_env_identity.py` | The `AGENTWORKS_*` variable names are the shipped on-VM contract, asserted as closed sets. |
| `cli/tests/test_git_config.py` | Assertions read `git config --get-all` output over values the test wrote. |
| `cli/tests/test_git_credential_contexts.py` | Secret scoping, per-declaration context identity, and out-of-scope refusal. |
| `cli/tests/test_git_credential_scoping.py` | Emitted text and seeded values at the git credential-helper boundary: the `username=`/`password=` payloads are git's own wire format, `timeout --signal=TERM --kill-after=1s 10s` is the dispatcher script's emitted command, and the two `failure` strings are what each test's own `ManagedHelper` was constructed with. |
| `cli/tests/test_git_credentials_subgraph_walk.py` | Transitive requirement walk and auto-declaration provenance. The secret names are what the owner template renders, so they pin nothing authored. |
| `cli/tests/test_git_token_verification.py` | Value identity on the test's own input: `test_git_token_verification.py::test_github_secret_runup_uses_declared_input_and_authenticated_probe` asserts the `Bearer` header built from the credential the test supplied. |
| `cli/tests/test_initializer_apt.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/test_initializer_workspaces_dir.py` | Emitted command text and the order the transport received it in, which is the observational form `hla.md` prefers. |
| `cli/tests/test_instance_descriptions.py` | Status and reason vocabularies, issue codes, and terminal-safety defenses asserted by Unicode category rather than by wording. |
| `cli/tests/test_instance_spec_cli.py` | Which lifecycle commands accept `--spec`, read off the real Typer command tree. That is the shipped CLI surface, not a second copy of it. |
| `cli/tests/test_list_names_only.py` | Machine-readable one-name-per-line output that the three completion scripts parse; the names are the test's own seeds. |
| `cli/tests/test_operational_json_persisted_enums.py` | Persisted-enum to JSON v1 parity, the `unknown` sentinel, and proof that raw persisted text never reaches any surface. |
| `cli/tests/test_path_rendering.py` | The three branches of the host-path spelling rule, with the expectation built the way the function builds it. Platform-conditional. |
| `cli/tests/test_resource_show.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/test_runnable_list_safety.py` | Consent confinement: which seams a plain and a status-enriched list may reach, and that neither writes. |
| `cli/tests/test_runtime_time.py` | No in-scope operand: added by `#759` after this cut's first basis, it asserts uptime derivation and clock behavior over values the test seeds, with no `match=`, no regex-family member and no multi-word string literal in an assertion operand position. |
| `cli/tests/test_secret_verify.py` | No in-scope operand, by the same scan. |
| `cli/tests/test_secrets_env_var.py` | No in-scope operand, by the same scan. |
| `cli/tests/test_secrets_prompt.py` | No in-scope operand, by the same scan. |
| `cli/tests/test_secrets_resolve.py` | In the secrets-preview estate, which is back in scope. A backend-boundary conformance and defense suite: enum reasons, call lists, exception identity, and injected-sentinel non-leakage. The backend is third-party code, so none of its validation is interior. |
| `cli/tests/test_session_console_filter.py` | Filter results, forwarded kwargs, exit codes and `entity_kind`/`entity_name` over seeded rows. |
| `cli/tests/test_session_create_ephemeral_secret_target_parity.py` | One equality between two builders' outputs (`pre == post`) and the same over `compute_needed_secrets`. |
| `cli/tests/test_ssh_identity.py` | Parsing at an operator-file boundary: `error.kind` classifications, a fingerprint checked against real `ssh-keygen`, a detail-length threshold, and the leak defense from the preamble. |
| `cli/tests/test_ssh_set_env.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/test_status_observation.py` | Future cancellation and the shutdown call shape. |
| `cli/tests/test_subprocess_io.py` | Byte-exact stdin delivery and a secret-redaction defense. Platform-conditional and CI coverage. |
| `cli/tests/test_tmux_model_conformance.py` | A differential oracle: the same operations against the hand model and a real tmux server, compared on structure with ids stripped. |
| `cli/tests/test_value_provenance.py` | Layer-fold provenance behavior and a single re-export identity assertion. |
| `cli/tests/transports/test_remote_lima.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/transports/test_sensitive_stdin.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/vms/test_applied_state_checkpoint.py` | Applied-state atomicity, rollback, and persisted event sequences. |
| `cli/tests/vms/test_describe_vm.py` | Call avoidance and warning counts. No assertion reads warning text. |
| `cli/tests/vms/test_live_vm_boundary.py` | Resolve counts, call order and values the test put in the environment. |
| `cli/tests/workspaces/test_acls.py` | Emitted command text, not prose we display: the assertion holds what the code sends to a shell, a transport, a completion script or a browser, which is behavior at a boundary. |
| `cli/tests/workspaces/test_backend_git_identity.py` | Emitted `git config` text, including the repo-local rather than global scope. |
| `website/tests/lander-phase4j.test.mjs` | Derivation parity over a fixture the test authors. Markdown in, rendered markdown out. |
| `website/tests/lander-phase4t.test.mjs` | Derivation parity over a fixture the test authors. Markdown in, rendered markdown out. |
| `website/tests/test_chromium_transport.py` | No in-scope operand, by the same scan. |
| `website/tests/test_lander_phase4m_browser.py` | Emitted command text: `test_lander_phase4m_browser.py::Phase4MBrowserTests.test_readiness_waits_for_the_navigated_complete_document_with_a_null_safe_root` builds the JavaScript expression the browser is asked to evaluate. |
| `website/tests/test_lander_phase4q_browser.py` | Emitted command text: `test_lander_phase4q_browser.py::Phase4QBrowserTests.test_fixed_scene_has_no_page_growth_or_scroll_across_lifecycle` is a fragment of the script under evaluation. |

## Group 1: mechanical `match=` narrowing

The group splits into four sections, which `sweep-screen.py totals` counts and the headings below
name. The **rows immediately below** are the sites the taxonomy does NOT decide mechanically: the
`L-` and `RB-` rows read by hand, the `G1-C` rows the callee screen converted, and the `G1-M` rows
the mutation screen decided. Then the **`G1-K` rows**, the sites whose matched text varies with the
test's input; then the **`G1-I` rows**, the ones the injected-marker screen pulled out; then the
**mechanical batch**, one row per file, sharing one justification. A reviewer who reads the first
three tables has read all the judgment in this PR.

**This group is no longer the no-judgment batch its name promises.** The callee-side raise screen
found that most of the sites it can resolve are multi-raise-path, so the mechanical batch's
deletions rest on `hla.md` case 2's fallback rather than its case 1. The injected-marker screen then
found sites where the batch's other claim was false outright. The edit is the same and the review is
not.

**Operator disposition, 2026-08-19: group 1 executes as TWO PRs.** The mechanical batch goes at
mechanical speed as one, and the judgment rows go as the other with their own review depth: every
`G1-C`, `G1-M` and `G1-I` row, and group 1's remaining converts and keeps, which are the rows whose
evidence a reviewer has to read rather than scan. The split follows the screens: what is left in the
mechanical batch after three screens have taken their sites out is the part that genuinely needs no
judgment.

**Corrected 2026-08-16, during execution.** The re-check that produced the `G1-K` rows found 30
input-varying sites. There are 43, plus two of the same shape in unittest's spelling. It had missed
a `manifests/` and `vms/` cluster, and the misses were sitting in delete rows, described by the
mechanical batch's shared justification as fixed literals that vary with nothing. What found them
was not re-reading the rows: it was partitioning every site in the estate by AST and classifying
each `match=` argument as a string literal or not. `ruff` corroborated one independently, raising
`B007` after the deletion because the loop variable existed only to feed the assertion. Two of the
affected tests say in their own docstrings that the label is deliberately carried into the failure
so each case reads as its own problem rather than as one sentence covering four. The lesson is the
method: a shape claim about a batch this size is checkable mechanically and should be checked that
way rather than asserted from reading.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| L-002 | `cli/tests/test_db.py::test_migration_19_fails_with_legacy_agent_session::IntegrityError::2c1282,test_agent_session_requires_socket_path::IntegrityError::2c1282` | `match="CHECK"` on sqlite's constraint text | keep | Upstream tool output. `IntegrityError` covers every constraint class; `CHECK` is the narrow token distinguishing a check constraint from a uniqueness or FK violation. |
| L-003 | `cli/tests/test_database_migration_safety.py::test_database_closes_connection_when_migration_raises::ProgrammingError::c3eefb` | `match="closed"` on sqlite's `ProgrammingError` | keep | Upstream tool output, pinned at the one token the assertion depends on: the connection was closed rather than failing for another programming reason. |
| L-004 | `cli/tests/plugins/gcp/test_instance.py::test_insert_retains_typed_request_and_ownership_before_wait::KeyboardInterrupt::6c45cb` | `match=` on a `KeyboardInterrupt` the test injected | keep | Not authored prose: the fake raises `KeyboardInterrupt("stop")` and the assertion proves that exact interrupt propagated out of `insert_instance_reconciled` rather than being swallowed and re-raised. Value identity. |
| L-005 | `cli/tests/plugins/gcp/test_network.py::test_wait_interrupt_leaves_operation_ownership_for_safe_rollback::KeyboardInterrupt::6c45cb` | injected `KeyboardInterrupt` | keep | Same shape as L-004, on the network reconcile path. |
| L-006 | `cli/tests/plugins/gcp/test_platform.py::test_first_interrupt_carries_operation_ownership_through_platform_rollback::KeyboardInterrupt::d62e9a,test_second_interrupt_abandons_platform_rollback_without_mutating_survivors::KeyboardInterrupt::a7937b` | injected `KeyboardInterrupt`, one parameterized by stage | keep | The match string is the parameter that selected which stage's fake raises, so it proves the interrupt from THAT stage escaped. Nothing authored is pinned. |
| L-007 | `cli/tests/assistance/test_generation.py::test_atomic_write_preserves_destination_when_replace_fails::OSError::83c874` | injected `OSError("refused ...")` | keep | The test monkeypatches `os.replace` to raise; the match proves the injected failure surfaced rather than being converted, alongside the real assertion that the destination is intact. **Anchors re-derived at HEAD, 2026-08-19; the previous anchor was a line number.** |
| L-008 | `cli/tests/test_ssh_logger.py::test_write_error_propagates_and_closes_handler::OSError::8c96d7` | injected `OSError("disk full")` | keep | Same shape. The surrounding assertions cover handler teardown; the match covers which exception escaped. |
| L-009 | `cli/tests/vms/test_verify_connection.py::test_verify_connection_surfaces_failure_without_activation_or_mutation::type::d62e9a` | `pytest.raises(type(failure), match=str(failure))` | keep | Fully derived from the injected failure object. Pins nothing; this is the ideal form of L-004's family. |
| L-010 | `cli/tests/test_database_backup.py::test_failed_backup_and_absent_restore_remove_incomplete_files::BackupError::38c863*2` | `match="forced"` against `BackupError("forced copy failure")` raised by the test's own fake at `test_database_backup.py::test_failed_backup_and_absent_restore_remove_incomplete_files.fail_copy` | keep | Injected sentinel, verified at HEAD: the string is authored by the test three lines above, so the assertion proves the injected copy failure escaped both the backup and the restore path. |
| L-011 | `cli/tests/test_name_filter_validation.py::test_console_list_rejects_unknown_workspace_and_agent::NotFoundError::58dd61,test_console_list_rejects_unknown_workspace_and_agent::NotFoundError::84b334` | two same-type `NotFoundError` branches in one function | convert | Verified by execution: both raises carry `entity_kind` (`'workspace'` / `'agent'`) and `entity_name='nope'` from `name_filters.py::_check_filter`. Replace both `match=` with `caught.value.entity_kind` assertions. Structural handle already in production, already the house pattern at 75 existing test sites, so no production change. |
| L-012 | `cli/tests/manifests/test_reference.py::test_an_unknown_kind_and_an_unknown_implementation_are_typed_refusals::ValidationError::0b5f13,test_an_unknown_kind_and_an_unknown_implementation_are_typed_refusals::ValidationError::ad65ce,test_an_unknown_kind_and_an_unknown_implementation_are_typed_refusals::ValidationError::9d6855` | three same-type `ValidationError` branches in one function | convert | Verified by execution: the three carry `entity_kind`/`entity_name` of `('resource','nope')`, `('vm-platform','nope')`, `('resource','secret/npm-token')`, which fully discriminate the three refusals. Assert those attributes instead. |
| L-014 | `cli/tests/capabilities/test_capability_descriptors.py::test_mapping_descriptor_requires_a_declarable_mapping_shaped_host::StateError::464033,test_mapping_descriptor_requires_a_declarable_mapping_shaped_host::StateError::543322` | two same-type `StateError` branches | delete | Verified by execution: neither raise sets `entity_kind`/`entity_name`, so only authored wording discriminates. R2.2 forbids adding a discriminator, so hla case 2's fallback applies and the branch coverage goes with the assertion. |
| L-015 | `cli/tests/capabilities/test_capability_descriptors.py::test_mapping_descriptor_key_is_a_hard_uses_reference::StateError::be88df,test_mapping_descriptor_key_is_a_hard_uses_reference::StateError::a5eb83` | two same-type `StateError` branches | delete | Same, verified the same way. |
| L-016 | `cli/tests/secrets/test_sources.py::test_backend_selection_rejects_framework_row_and_class_corruption::StateError::b765f3,test_backend_selection_rejects_framework_row_and_class_corruption::StateError::6a1b4d` | two same-type `StateError` branches | delete | Verified: no structural handle on either raise. |
| L-017 | `cli/tests/sessions/test_session_nodes.py::test_session_factory_requires_exactly_one_launch_identity::StateError::e31c28*2` | **[1-raise]** two `StateError` sites, identical `'exactly one'` text | delete | Verified single-raise-path across both sites, which is what the marker records. The two matches are the same string, so the assertion never distinguished the branches it appears to, and the raised type is the whole of what it checks. |
| L-019 | `cli/tests/test_consoles_specs_db.py::test_reorder_console_sessions_rejects_wrong_member_set::ValueError::49379f*3` | three `ValueError` sites, the same `'full member list'` text | delete | The three matches are identical, so they never discriminated. |
| L-020 | `cli/tests/test_doctor_schema.py::test_schema_check_rejects_invalid_scalar_versions::StateError::09c09f*2` | two `StateError` sites, identical text | delete | Same shape as L-019. |
| L-021 | `cli/tests/manifests/test_samples.py::test_bare_sample_requires_kind_or_all::ValidationError::11be58,test_bare_sample_requires_kind_or_all::ValidationError::175f8c` | two `ValidationError` branches | delete | Verified: no `entity_*` handle. |
| L-022 | `cli/tests/manifests/test_samples.py::test_write_sample_refuses_escapes_and_suffixes::ValidationError::9c035e*2,test_write_sample_refuses_escapes_and_suffixes::ValidationError::e67b5d` | three `ValidationError` branches | delete | Verified: no `entity_*` handle. **Anchors re-derived at HEAD, 2026-08-19; the previous anchors were line numbers.** |
| L-023 | `cli/tests/resources/test_registry_lifecycle.py::test_add_rejects_names_containing_slash::ConfigError::5bb7c2*2` | two `ConfigError` sites, identical `"contains '/'"` text | delete | Identical matches; the real invariant (a `/` is refused at the single publisher choke point) survives on the `raises` alone. |
| L-024 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_port_forward_bad_spec_fails_with_zero_resolves_and_zero_gate::ValidationError::1d87e5,test_port_forward_bad_spec_fails_with_zero_resolves_and_zero_gate::ValidationError::c8007a` | two `ValidationError` branches | delete | Verified: both carry `entity_kind='vm'`, `entity_name='box'`, identical across the two branches, so the handle exists but does not discriminate. |
| L-025 | `cli/tests/vms/test_vm_site_kind.py::test_site_names_obey_the_freeform_name_rules::ConfigError::46ede9,test_site_names_obey_the_freeform_name_rules::ConfigError::de487f,test_site_names_obey_the_freeform_name_rules::ConfigError::24f3ac` | three name-rule `ConfigError` branches | delete | Verified: no `entity_*` on the name-validation raises. Only the authored rule sentence separates lowercase, consecutive-hyphen, and length. |
| L-026 | `cli/tests/vms/test_platform_config_contract.py::test_lima_rejects_bad_host_and_unknown_keys::ConfigError::0bfdec,test_lima_rejects_bad_host_and_unknown_keys::ConfigError::26185c,test_azure_requires_the_three_location_keys_and_defaults_auth::ConfigError::a7c9ee,test_aws_ec2_requires_region::ConfigError::ea9010,test_aws_ec2_requires_region::ConfigError::a7c9ee,test_gcp_gce_location_auth_and_subnet_are_shape_checked::ConfigError::5d37ba,test_gcp_gce_location_auth_and_subnet_are_shape_checked::ConfigError::5eebcd,test_proxmox_validation_errors::ConfigError::e42340,test_proxmox_validation_errors::ConfigError::6d841c,test_proxmox_validation_errors::ConfigError::9e39e9,test_proxmox_no_longer_reads_a_string_verify_ssl_as_true::ConfigError::b4d44b,test_proxmox_no_longer_reads_a_string_verify_ssl_as_true::ConfigError::cd085b` | 12 same-type `ConfigError` sites across six functions | delete | Verified by execution and it is the interesting one: every raise DOES carry `entity_kind='vm-site'`, `entity_name='t'`, but identically for all fourteen, so the handle does not discriminate. (Two further sites in this file, in `test_platform_config_contract.py::test_azure_requires_the_three_location_keys_and_defaults_auth` and `test_platform_config_contract.py::test_gcp_gce_location_auth_and_subnet_are_shape_checked`, were on this row until the second re-check moved them to G1-K22, because their matched text interpolates a loop variable, so the parameter discriminates there where the handle does not.) The information that would (`_Problem.path`, the operator's dotted field address) is consumed inside `schema/errors.py::config_error_from` and never reaches `ConfigError`. Attaching it would be a shipped-contract change, which R2.2 excludes. Cost is stated plainly: a config rejected for the wrong reason will still pass. **Disposition 2026-09-06 (fresh cut).** The two sites in `test_proxmox_no_longer_accepts_a_quoted_template_vmid` are gone: `36cbbc67` renamed the test to `test_proxmox_does_not_accept_a_quoted_template_map_vmid` and changed what it asserts, from a bare vmid to one inside the release map, so the successor is a different assertion rather than the same one moved and no identity can follow it. The anchor is dropped from this row and the successor site in `test_platform_config_contract.py::test_proxmox_does_not_accept_a_quoted_template_map_vmid` is cut fresh with the other sites the tree gained. The row's other anchors, over six functions, are untouched and its delete stands on them. |
| L-401 | `website/tests/test_site_build.py::BuildAndInstallTests.test_favicon_rejects_every_unreviewed_svg_node::ValueError::83fb7e,BuildAndInstallTests.test_favicon_rejects_non_path_canonical_mark_geometry::ValueError::eeab47,BuildAndInstallTests.test_favicon_rejects_noncanonical_rocket_root_or_mark_hierarchy::ValueError::f261ac,BuildAndInstallTests.test_unapproved_external_url_fails_before_output_changes::ValueError::766f6a,BuildAndInstallTests.test_absent_local_route_has_no_partial_manifest_suppression::ValueError::fe209c,BuildAndInstallTests.test_cross_document_fragment_must_exist_on_its_actual_target::ValueError::2ef080,BuildAndInstallTests.test_source_heading_cannot_duplicate_a_shell_id::ValueError::a78aaf,BuildAndInstallTests.test_rendered_reference_validation_retains_duplicate_ids::ValueError::a78aaf,BuildAndInstallTests.test_manifest_without_root_index_cannot_suppress_root_reference_failure::ValueError::fe209c,BuildAndInstallTests.test_shared_css_cannot_conceal_reviewed_shell_content::ValueError::cbe359,BuildAndInstallTests.test_same_document_fragment_must_resolve_before_output_changes::ValueError::ee89e6,BuildAndInstallTests.test_duplicate_rendered_references_cannot_hide_first_browser_destination::ValueError::20a7d7,BuildAndInstallTests.test_output_inside_repository_is_rejected_without_source_writes@a29f04,BuildAndInstallTests.test_output_accepts_ordinary_spaced_and_hidden_directory_names@f3f6a0,BuildAndInstallTests.test_every_static_module_import_resolves_to_the_literal_manifest::ValueError::456e31,BuildAndInstallTests.test_model_ships_byte_for_byte_without_generated_proof_composition@5dd25d,BuildAndInstallTests.test_collision_and_world_ship_separately_in_the_exact_manifest@818a84,BuildAndInstallTests.test_retired_route_projection_sources_are_absent@8b2483,BuildAndInstallTests.test_authored_lander_modules_and_tests_stay_below_the_review_ceiling@217ac9,BuildAndInstallTests.test_output_rejects_dot_traversal::ValueError::c6b9ac,BuildAndInstallTests.test_output_rejects_parent_symlink_into_repository_when_supported::ValueError::e2885f,BuildAndInstallTests.test_existing_top_level_directory_symlink_is_rejected_without_touching_target::ValueError::4c5159,BuildAndInstallTests.test_existing_top_level_broken_symlink_is_rejected_without_replacement::ValueError::4c5159,BuildAndInstallTests.test_existing_output_may_contain_only_selected_owned_entries::ValueError::1ffbe9,BuildAndInstallTests.test_content_or_template_failure_leaves_existing_output_byte_identical@ad189c,BuildAndInstallTests.test_symlink_entry_in_existing_output_is_rejected_when_supported::ValueError::cc52c0,BuildAndInstallTests.test_fifo_entry_in_existing_output_is_rejected_when_supported::ValueError::25bf38,BuildAndInstallTests.test_non_directory_output_is_rejected::ValueError::4c5159,BuildAndInstallTests.test_remote_runtime_asset_references_fail_before_output_changes::ValueError::3a6b8d,BuildAndInstallTests.test_harmless_javascript_line_comment_is_allowed@9c7ee8,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output::OSError::9e8486,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output::RuntimeError::4183b7` | 24 `assertRaisesRegex` sites | delete | Every regex matches wording `site_builder` and `site_validation` author. The raised type is `ValueError` throughout, so the type does not discriminate and no structural handle exists, which is `hla.md` case 2's fallback: the assertion goes and the branch coverage with it (R2.4). The `assertRaises` stays. Where lane F's rows delete the whole test, the regex goes with it and this row does not double-count. |
| L-403 | `website/tests/test_site_content.py::SourceContractTests.test_assistance_projection_drift_and_framing_fail_closed::ValueError::d9687c,SourceContractTests.test_assistance_source_requires_exact_regular_lf_utf8::ValueError::0d71ce,SourceContractTests.test_missing_or_symlinked_assistance_source_fails_closed::ValueError::5f411f*2,SourceContractTests.test_unsupported_markdown_and_hard_breaks_fail_closed::ContractError::2fc8fe,SourceContractTests.test_unmatched_reserved_inline_markers_fail_instead_of_rendering_literally::ContractError::60945d,SourceContractTests.test_source_relative_links_are_explicitly_mapped_and_others_fail::ContractError::766f6a,SourceContractTests.test_invalid_utf8_bom_missing_and_symlinked_input_fail_closed::ContractError::05ac6a,SourceContractTests.test_invalid_utf8_bom_missing_and_symlinked_input_fail_closed::ContractError::8d8b89*2,SourceContractTests.test_security_reference_and_github_only_reporting_are_exact::ContractError::61b82e` | 11 `assertRaisesRegex` sites | delete | **Corrected 2026-08-19 (map re-baseline).** Sites listed rather than ranged, and re-derived at HEAD. The old anchor was a range about ten tests wide, and contained G1-K23's two keeps, so an executor working from the range would have deleted them. These eleven are the file's other `assertRaisesRegex` sites, over `site_content`'s authored refusals; the raised type is shared and no structural handle exists, which is `hla.md` case 2's fallback. Two of them (`test_site_content.py::SourceContractTests.test_assistance_source_requires_exact_regular_lf_utf8`, matching `"UTF-8\|NUL-free\|LF-terminated"`, and `test_site_content.py::SourceContractTests.test_security_reference_and_github_only_reporting_are_exact`) are alternations over authored phrases, a wording blacklist wearing a regex, which the rule calls worse than useless. |
| RB-011 | `cli/tests/test_grok_build_integration.py::test_validation_rejects_non_list_extra_args::ConfigError::d0b029` | `match="extra_args: must be a list"` on `ConfigError` | delete | Added by the 2026-08-19 re-baseline: the Grok Build integration landed at `703fb625` (2026-08-17), after this inventory's basis, so no row claimed the file and it is the one file at HEAD with a `match=` site and no group-1 row. The call is the generic schema validator (`validate_capability_config` through the test's own `_validate` helper at `test_grok_build_integration.py::_validate`), which raises one `ConfigError` per document however many problems it found, so the raised type discriminates nothing: this is hla case 2, not case 1. No `entity_*` handle is attached, and the address that would discriminate (`_Problem.path`) is consumed inside `schema/errors.py` before the `ConfigError` is built, so case 2's fallback applies and the branch coverage goes with the assertion (R2.4). Cost stated: a regression that rejects `extra_args` as an unknown field instead would still pass. The file's three sibling arms at `test_grok_build_integration.py::test_validation_rejects_unknown_field`, `test_grok_build_integration.py::test_validation_rejects_non_string_choices` and `test_grok_build_integration.py::test_construct_revalidates_config` already assert the bare `raises`, so deletion makes this site match its own file rather than diverge from it. |
| G1-C01 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_grant_empty_request_fails_with_zero_resolves_and_zero_gate::ValidationError::c671fd,test_revoke_unknown_agent_fails_with_zero_resolves_and_zero_gate::NotFoundError::23a674` | two refusals reachable from several raises of the asserted type | convert | **Callee-side raise screen, 2026-08-19,** re-derived by `sweep-screen.py screen`. Two sites, two different operations, and the row states them separately because they do not share a callee. `test_delete_grant_revoke_orchestrated.py::test_grant_empty_request_fails_with_zero_resolves_and_zero_gate` drives `grant_workspaces` (`cli/agentworks/agents/grants.py::grant_workspaces`), which reaches three `ValidationError` raises: its own empty-request refusal, carrying `entity_kind="agent"`, and the two name-rule raises in `cli/agentworks/naming.py::validate_name`, the length one and the character-rule one, which carry no `entity_kind` at all. `test_delete_grant_revoke_orchestrated.py::test_revoke_unknown_agent_fails_with_zero_resolves_and_zero_gate` drives `revoke_workspaces` (`grants.py::revoke_workspaces`), which reaches three `NotFoundError` raises: its own missing agent (`entity_kind="agent"`), the missing workspace inside `grants.py::_resolve_ws_group` (`"workspace"`), and the missing VM inside `grants.py::_require_vm` (`"vm"`). In both cases the asserted type does not discriminate and the handle does, so assert `caught.value.entity_kind` in place of each sentence. Under the rubric these are the only probe for a behavior that can regress: without `test_delete_grant_revoke_orchestrated.py::test_revoke_unknown_agent_fails_with_zero_resolves_and_zero_gate`'s, a run where `ws1` stops resolving satisfies the agent-not-found test. No production change. |
| G1-C02 | `cli/tests/test_agents.py::test_grant_workspaces_rejects_empty_request::ValidationError::c671fd,test_revoke_workspaces_rejects_empty_request::ValidationError::c671fd` | two same-type refusals the callee raises from several paths | convert | **Callee-side raise screen, 2026-08-19,** re-derived by `sweep-screen.py screen`. The grant path's empty-request refusal is `cli/agentworks/agents/grants.py::grant_workspaces` and the revoke path's is `grants.py::revoke_workspaces`, both `ValidationError` with `entity_kind="agent"`, and both operations also reach the two raises in `cli/agentworks/naming.py::validate_name`, the length one (`f"name '{name}' is too long ({len(name)} chars, max {max_length})"`) and the character-rule one (`f"invalid name '{name}'. Names must be lowercase alphanumeric "`), which raise the same type for a malformed name with no `entity_kind` at all. Three reachable raises per site, so the type does not discriminate and the handle does. Assert `caught.value.entity_kind == "agent"` in place of each sentence. This row is the whole of its file's group 1 content, so no mechanical row remains for it. No production change. |
| G1-C03 | `cli/tests/test_env_show.py::test_unknown_vm_raises_validation_error::ValidationError::623d9d` | one of two same-type refusals in one call | convert | **Callee-side raise screen, 2026-08-19,** re-derived by `sweep-screen.py screen`. `show_env` reaches nine `ValidationError` raises: six in `cli/agentworks/env/show.py::_resolve_context` (`"agw env show requires a context"`, `f"session {session_name!r} not found"`, `f"workspace {workspace_n!r} not found"`, `f"agent {agent_n!r} not found"`, `"could not resolve a VM from the supplied flags"` and `f"VM {vm_n!r} not found"`), the two name-rule raises in `cli/agentworks/naming.py`, and one in `cli/agentworks/secrets/line_safety.py::require_line_safe_secret`. The targeted one, `f"VM {vm_n!r} not found"`, is the only one carrying `entity_kind="VM"`, so assert that instead of `"VM 'nope' not found"`. The file's other site, in `test_env_show.py::test_no_flags_raises_validation_error`, keeps its delete under case 2's fallback: it targets `"agw env show requires a context"`, and `"could not resolve a VM from the supplied flags"` carries no `entity_kind` either, so nothing tells the two apart and the branch coverage goes with the assertion (R2.4). No production change. |
| G1-C04 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_unknown_workspace_fails_with_zero_resolves_and_zero_gate::NotFoundError::b21f99` | one of two same-type refusals in one call | convert | **Callee-side raise screen, 2026-08-19,** re-derived by `sweep-screen.py screen`. The repair path reaches exactly two `NotFoundError` raises, both in `cli/agentworks/workspaces/manager/repair.py::repair_workspace`: `f"workspace '{name}' not found"` for the missing workspace and `f"VM '{ws.vm_name}' not found"` for the missing VM behind it. Only the workspace one carries `entity_kind="workspace"`, the VM one being `entity_kind="vm"`, so assert that rather than `"workspace 'ghost' not found"`. Without it a regression that loses the VM instead of the workspace passes this test. The file's other ten sites stay with G1-155. No production change. |
| G1-C05 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_rehome_overlapping_paths_fail_with_zero_resolves_and_zero_gate::ValidationError::361297` | one of eleven same-type raises, with a handle | convert | **Callee-side raise screen.** `rehome_workspace` can raise `ValidationError` from eleven paths, so the raised type discriminates nothing and `hla.md` case 1 does not apply. The targeted raise at `cli/agentworks/workspaces/manager/rehome.py::rehome_workspace` carries `entity_kind='workspace'`, which is unique among the eleven, so case 2's first arm applies: assert on the handle production already offers rather than on the sentence. Replacement: `exc.value.entity_kind == "workspace"` beside the existing `pytest.raises(ValidationError)`. No production change, so R2.2 is untouched. Found by `generate` refusing to batch it, which is the check that now stops case 2 being decided by not noticing it. |
| G1-M01 | `cli/tests/orchestration/test_activation.py::test_gate_reader_is_scoped_to_declared_gate_secrets::StateError::afdbd7` | scoping refusal, one of two same-type raises | keep | **Mutation-proved 2026-08-19.** `ScopedSecrets.get` raises `StateError` twice: the undeclared-name refusal (`f"secret {name!r} was not declared by this node, so it "`) and the declared-but-unresolved one (`f"secret {name!r} is declared but was not resolved by "`), both in `cli/agentworks/orchestration/secrets.py::ScopedSecrets.get`. Bypassing the refusal (`if False:` in place of `if name not in self._names:`) leaves the second raise standing, so the bare `raises` still passes and only the `match=` fails. Measured: `test_gate_reader_is_scoped_to_declared_gate_secrets` fails with the assertion as shipped and PASSES once the mechanical row's edit lands. Under the rubric this is the only probe for a behavior that can regress, so it keeps. |
| G1-M02 | `cli/tests/orchestration/test_activation.py::test_auto_start_reader_refuses_undeclared_names::StateError::afdbd7` | declaration refusal, single raise path | delete | **Mutation-proved 2026-08-19, and the disposition stands.** This site targets a different class from G1-M01: `activation.py::_GateSecrets.get`, the lazy gate-and-repair reader, which has exactly one `raise StateError`. Bypassing its declaration check (`if True:` in place of `if name in self._repair_names():`) is caught by `test_auto_start_reader_refuses_undeclared_names` with the `match=` and without it, so the assertion adds nothing the raised type does not already carry. Recorded rather than left in the batch because the four sites around it went the other way and a reader will ask. |
| G1-M05 | `cli/tests/vms/test_shell_exec_orchestrated.py::test_shell_platform_transport_hands_a_secret_bearing_ctx::StateError::76cf8a` | scoping refusal on the platform-transport context | keep | **Mutation-proved 2026-08-19.** `test_shell_platform_transport_hands_a_secret_bearing_ctx` fails as shipped and passes after the edit. This is the site whose `match=` carries the longest needle of the family (`not declared by this node`), and the length is not why it keeps: the second raise at `secrets.py::ScopedSecrets.get` is what makes the type non-discriminating. |
| G1-M06 | `cli/tests/vms/test_sites_dispatch.py::test_ops_read_the_token_through_the_context::StateError::afdbd7` | scoping refusal on a bare run context | keep | **Mutation-proved 2026-08-19.** `test_ops_read_the_token_through_the_context` fails as shipped and passes after the edit. Its neighbor in the same test, `match=r"no\s+resolved secrets"`, is a `ConfigError` on a different arm and is unaffected; only this site probes the scoping contract. |
| G1-M07 | `cli/tests/vms/test_lima_remote_create_interrupt.py::test_cleanup_failure_warns_and_does_not_mask_the_original::SSHError::37a06e` | injected marker distinguishing two same-type failures | keep | **Mutation-proved 2026-08-19, and an injected-marker site as well.** Both `SSHError("original failure")` and `SSHError("cleanup broke")` are strings this test writes, so the `match=` is what proves the ORIGINAL failure propagated rather than the cleanup one, which is the property the test is named for. Making the cleanup re-raise over the original (`raise` after the warn at `cli/agentworks/capabilities/vm_platform/lima.py::LimaPlatform._create_remote`) leaves all 16 tests in the file passing once the assertion goes; with it, `test_cleanup_failure_warns_and_does_not_mask_the_original` fails. `test_wsl2_create_interrupt.py::test_cleanup_failure_warns_and_does_not_mask_the_original` is the same property on the WSL2 platform and keeps as G1-I14. |

### Sites the re-check pulled out of the mechanical batch

Some `match=` sites in this group match a string that VARIES with the test's input (an f-string over
a field name, a parametrize variable, an interpolated production constant). Those probe that the
diagnostic tracks the input, which is behavior, so they keep. Two `assertRaisesRegex` sites of the
same shape sit in L-403's range and keep with them, at G1-K23. What is left after this section, the
mutation screen's and the injected-marker screen's takes out is the mechanical batch, whose sites
match a fixed literal in a single-input test.

**One reason serves every row here, so it is stated once rather than per row.** These are not fixed
phrasing: the matched text varies with the case, so each assertion fails when the diagnostic stops
tracking the input, not only when someone rewords it. The cost is stated with it: they break on a
rewording too, and whoever rewords must update them. Each row below carries only what is specific to
its sites.

Rows G1-K01 through G1-K16 are the first pass; G1-K17 and above are the `match=` and two
`assertRaisesRegex` sites the second re-check added, as the group header explains.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| G1-K01 | `cli/tests/plugins/gcp/test_compute.py::test_missing_location_is_pre_mutation_config_error::ConfigError::d62e9a,test_required_live_machine_fields_fail_by_provider_shape_branch::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=helper` is the parameter selecting which location lookup failed, so it proves the `ConfigError` names project versus zone correctly. |
| G1-K02 | `cli/tests/plugins/gcp/test_platform.py::test_required_machine_shape_failure_has_zero_provider_mutations::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=message` is parameterized per failure case. |
| G1-K03 | `cli/tests/plugins/test_manifest_parity.py::test_ensure_recipe_enabled_excludes_capability_nodes::StateError::cc4d33` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `f"enable plugin '{PLUGIN}'"` names the plugin the operator must enable, derived from the fixture constant. |
| G1-K05 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestFailClosedBootstrap.test_bootstrap_failure_removes_private_stage_and_rolls_back::ProvisioningError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=message` is parameterized per failure case. |
| G1-K06 | `cli/tests/resources/test_install_resource_kinds.py::test_install_resource_kind_synthesize_raises::NoUnreferencedDefaultError::8b2e8a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `f"the {kind_name} kind has no reserved default name"` names the kind under test. |
| G1-K07 | `cli/tests/resources/test_template_kinds.py::test_inherits_typo_fires_framework_miss_policy::ConfigError::d8cefd` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `f"{spec.kind} kind only auto-declares"` names the kind under test. |
| G1-K08 | `cli/tests/secrets/test_sources.py::test_source_key_miss_schedule_preserves_cross_row_first_target_order::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=expected` is `"mapping-missing"` or `"ordinary-missing"` depending on row insertion order, so the assertion proves `finalize` reports the RIGHT missing host independently of ordering. Nothing else probes that. |
| G1-K09 | `cli/tests/test_aws_ec2_ops.py::TestCreateRollback.test_readiness_failure_is_safe_and_rolls_back_before_key_delivery::EC2Error::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=message` is parameterized per failure case. |
| G1-K10 | `cli/tests/test_codex_integration.py::test_validation_rejects_non_string_flag_fields::ConfigError::c4d186,test_new_fields_reject_wrong_types::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** Two parameterized sites. `f"{field_name}: must be a string"` and `match=field` over six fields prove the codex `_validate` names the field that was wrong; the type is the same for all six. **Anchors re-derived at HEAD, 2026-08-19; the previous anchors were line numbers.** |
| G1-K11 | `cli/tests/test_config.py::test_focused_database_config_rejects_unsafe_input::ConfigError::d62e9a,test_proxmox_config::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** Two parameterized sites driven by case tables of (bad config, expected diagnosis). They prove per-case routing: each malformed input produces ITS OWN diagnosis rather than a catch-all. **Anchors re-derived at HEAD, 2026-08-19.** The second site was line-anchored before this cut. |
| G1-K12 | `cli/tests/test_consoles_restore.py::test_restore_session_refuses_and_leaves_the_model_intact::StateError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=match` is parameterized per failure case. |
| G1-K13 | `cli/tests/test_database_migration_safety.py::test_prepare_refuses_unopenable_state_before_writable_open::StateError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=match` is parameterized per failure case. |
| G1-K15 | `cli/tests/test_operation_scope.py::test_system_scope_forbids_deeper_names::StateError::f88d2a,test_vm_scope_forbids_deeper_names::StateError::f88d2a,test_workspace_scope_requires_its_chain::StateError::e02816,test_workspace_scope_forbids_deeper_names::StateError::f88d2a,test_agent_scope_requires_its_chain::StateError::e02816,test_agent_scope_forbids_deeper_and_sideways_names::StateError::f88d2a,test_session_scope_requires_its_chain::StateError::e02816` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** Seven parameterized sites over `OperationScope.__post_init__`'s level rules (`capabilities/base.py::OperationScope.__post_init__`). `f"forbids '{field}'"` and `f"requires '{field}'"` name WHICH field violated the level's rule, across four fields; `StateError` is identical for all of them and nothing else probes the rules table. |
| G1-K16 | `cli/tests/test_session_create_ephemeral_vm_anchor.py::test_ephemeral_workspace_name_uses_workspace_cap::ValidationError::17cf2c,test_ephemeral_agent_name_uses_agent_cap::ValidationError::17cf2c,test_defaulted_ephemeral_agent_name_overflow_hints_override_flag::ValidationError::17cf2c,test_defaulted_ephemeral_workspace_name_overflow_hints_override_flag::ValidationError::17cf2c,test_session_name_uses_socket_derived_cap::ValidationError::17cf2c` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** Five sites matching `f"max {MAX_*_NAME_LENGTH}"`, which interpolate the real production constants. The assertion proves the refusal reports the LIMIT ACTUALLY IN FORCE rather than a number that drifted from it. |
| G1-K17 | `cli/tests/manifests/test_capability_shape.py::test_a_retired_session_selector_is_an_unknown_field_at_its_own_location::ConfigError::1e94df,test_tagged_table_requires_a_string_name_key::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16 (see the group header).** `test_capability_shape.py::test_a_retired_session_selector_is_an_unknown_field_at_its_own_location` is `rf"res\.yaml:2:.*{key}: unknown field"` over the three `_RETIRED_SELECTORS`, and the test's own docstring says the label is carried into the failure "so the spelling that stopped being refused still names itself". `test_capability_shape.py::test_tagged_table_requires_a_string_name_key` is `match=message` over four parametrize cases with four distinct messages, and that docstring says each way of getting it wrong must read "as that field's own problem rather than as one hand-written sentence covering four cases". Deleting them collapses each table to one assertion repeated. |
| G1-K18 | `cli/tests/manifests/test_decode_shapes.py::test_a_stray_top_level_key_names_the_one_field_this_kind_has::ConfigError::e74e6b,test_a_metadata_field_written_in_spec_is_refused::ConfigError::0365e4` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** `f"{key}: unknown field; expected one of: provider"` over the `type`/`org` loop and `f"{field} belong\(s\) in metadata, not in spec"` over the `description`/`name` loop. Both prove the refusal names WHICH field was misplaced; `ConfigError` is identical across the arms of each loop. |
| G1-K19 | `cli/tests/manifests/test_kind_models.py::test_a_malformed_expiry_is_refused::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** `match=message` over two parametrize cases whose messages differ in kind, not wording: pydantic's own `"Input should be a valid datetime"` versus our `"must be a date or an RFC 3339 timestamp"`. The assertion is what proves the bare-int carve-out produces its own diagnosis rather than the generic one. This row is the whole of its file's group 1 content, so no mechanical row remains for it. |
| G1-K20 | `cli/tests/manifests/test_loader_and_envelope.py::test_envelope_rejections::ConfigError::d62e9a,test_mise_inputs_fail_at_manifest_decode::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** Two parametrized sites taking `match=match` from their case tables: envelope mutations at `test_loader_and_envelope.py::test_envelope_rejections` and mise decode inputs at `test_loader_and_envelope.py::test_mise_inputs_fail_at_manifest_decode`. Each table's cases share `ConfigError`, so the parameter is the only thing proving a case routes to its own diagnosis. |
| G1-K21 | `cli/tests/sessions/test_session_template_surface.py::test_an_unknown_key_in_the_harness_block_errors_at_build::ConfigError::7f669b` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** `f"{field}: unknown field; expected one of:"` over the parametrized field, proving the refusal names the offending key inside `harness_integration`. |
| G1-K22 | `cli/tests/vms/test_platform_config_contract.py::test_azure_requires_the_three_location_keys_and_defaults_auth::ConfigError::7ebb18,test_azure_service_principal_arm_is_shape_checked::ConfigError::e879d8,test_azure_rejects_malformed_service_principal::ConfigError::d62e9a,test_gcp_gce_location_auth_and_subnet_are_shape_checked::ConfigError::d27201,test_aws_ec2_rejects_malformed_credentials::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** Three interpolated loops (`test_platform_config_contract.py::test_azure_requires_the_three_location_keys_and_defaults_auth` azure's three location keys, `test_platform_config_contract.py::test_azure_service_principal_arm_is_shape_checked` the service-principal identifiers, `test_platform_config_contract.py::test_gcp_gce_location_auth_and_subnet_are_shape_checked` gcp's `project_id`/`zone`) and two parametrized tables (`test_platform_config_contract.py::test_azure_rejects_malformed_service_principal`, `test_platform_config_contract.py::test_aws_ec2_rejects_malformed_credentials`) over seven and eight malformed-credential cases. This is the file L-026 covers, and L-026's finding stands for its remaining every one of the production raises it reaches: `entity_kind` is identical across all fourteen of them, which counts raises rather than sites. The sites this row claims are the ones where the matched text is not identical, so the parameter still discriminates where the handle cannot. |
| G1-K23 | `website/tests/test_site_content.py::SourceContractTests.test_document_structure_failures_are_closed::ContractError::d62e9a,SourceContractTests.test_home_selection_still_fails_on_missing_duplicate_or_drift::ContractError::3f37a7` | `assertRaisesRegex` varying with the input | keep | **Second re-check, 2026-08-16.** The same shape in unittest's spelling, inside the range L-403 used to carry. `test_site_content.py::SourceContractTests.test_document_structure_failures_are_closed` takes `reason` from a five-case table of document-structure defects; `test_site_content.py::SourceContractTests.test_home_selection_still_fails_on_missing_duplicate_or_drift` is `rf"HOME_IDENTITY README.md .*{reason}"` over three, and additionally names the contract and source path. `ContractError` is shared by every case in both. **Anchors re-derived at HEAD, 2026-08-19; the previous anchors were line numbers.** |

### Sites the injected-marker screen pulled out of the mechanical batch

Further sites match a string the test file itself wrote and handed to an exception constructor. For
those the mechanical batch's shared justification is false in the way that decides the row: the
matched text is not prose this repository authors, it is a marker, and the assertion is the proof
that the INJECTED failure is the one observed. That is word for word why L-004, L-008, L-009 and
L-010 already keep; the screen finds the rest of that family instead of leaving it to whoever
happens to read a row closely.

`sweep-screen.py injected` derives them: a site qualifies when its needle appears in a string its
own module passes to an exception constructor AND appears in no string production emits. Both halves
matter. Without the first it is not a marker; without the second the test may be quoting a shipped
sentence through a fake, which is what the batch deletes. Comments and docstrings are excluded from
the production side, because a phrase discussed in a docstring is not a phrase the code says: two
production docstrings mention "original failure" and no production message contains it, which is the
difference between keeping G1-M07 and deleting it.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| G1-I01 | `cli/tests/orchestration/test_activation.py::test_gate_span_closes_on_failure_in_the_body::RuntimeError::81f523` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`boom`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-018 (2026-08-19 map). |
| G1-I03 | `cli/tests/plugins/gcp/test_compute.py::test_unexpected_proto_presence_failure_is_not_masked_as_absence::RuntimeError::034471` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`provider bug`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-023 (2026-08-19 map). |
| G1-I04 | `cli/tests/plugins/test_plugin_framework.py::test_seated_plugin_round_trips_on_exception::RuntimeError::81f523` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`boom`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-031 (2026-08-19 map). |
| G1-I05 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestPlainFailure.test_failure_before_the_clone_makes_no_cleanup_calls::ProxmoxAPIError::d9562b` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`cluster unreachable`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-034 (2026-08-19 map). |
| G1-I06 | `cli/tests/sessions/test_create_start_restart_orchestrated.py::test_create_failure_cleans_session_slice_then_unwinds_ephemerals::RuntimeError::5df21f,test_compound_child_post_commit_failure_unwinds_owner_and_overlay_without_outcome::RuntimeError::226a04` | 2 injected-marker site(s) | keep | The matched text is a marker this test file writes and hands to an exception constructor (`tmux exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-057 (2026-08-19 map). **Re-anchored 2026-09-06 by the fresh cut.** `test_create_resume_orchestrated.py` was renamed to `test_create_start_restart_orchestrated.py` by `d193e48f`; this row's estate moved with the file rather than going, and its evidence is unchanged. |
| G1-I07 | `cli/tests/test_azure_create_interrupt.py::TestPlainFailureArmUnchanged.test_failure_still_cleans_up_and_wraps::AzureError::d467cd,TestVMDeleteFailureWarns.test_vm_delete_failure_during_failure_rollback_warns_and_sweep_continues::AzureError::6b3572` | 2 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`nic exploded`, `wait exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-067 (2026-08-19 map). |
| G1-I08 | `cli/tests/test_azure_nsg_exposure.py::TestTransientRoute.test_exit_removes_allow_when_body_raises::RuntimeError::59596d` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`kaboom`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-070 (2026-08-19 map). **Screen note 2026-09-06.** The injected-marker screen reports this site `mixed`, which is its one inherent false positive rather than a finding: the file raises `boom` elsewhere, `boom` is a substring of `kaboom`, and stripping it leaves the fragment `ka`. There is no authored prose near this needle and the keep stands. |
| G1-I09 | `cli/tests/test_operational_json_boundaries.py::test_vm_describe_propagates_operator_abort_in_service_and_both_cli_formats::UserAbort::5e2619` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`operator declined`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-099 (2026-08-19 map). |
| G1-I10 | `cli/tests/test_session_create_ephemeral_prompts.py::test_admin_non_interactive_on_vm_with_agents_does_not_prompt::RuntimeError::ea04fc,test_workspace_prompt_picks_create_new::RuntimeError::2acb09,test_mode_prompt_picks_create_new::RuntimeError::7829c5` | 3 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`stop after agent realize`, `stop after mode-prompt gate`, `stop after workspace realize`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-113 (2026-08-19 map). |
| G1-I11 | `cli/tests/test_session_create_ephemeral_rollback.py::test_failure_after_ephemeral_create_rolls_back_ephemerals::RuntimeError::daeb30,test_new_agent_inherits_vm_from_existing_workspace::RuntimeError::7829c5` | 2 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`simulated session-internal failure`, `stop after agent realize`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-114 (2026-08-19 map). |
| G1-I12 | `cli/tests/test_session_create_ephemeral_vm_anchor.py::test_explicit_vm_agreeing_with_workspace_passes_anchor_check::RuntimeError::c9441c,test_no_vm_anchor_with_single_vm_auto_selects::RuntimeError::18946c,test_new_agent_with_explicit_agent_name::RuntimeError::7829c5,test_ephemeral_agent_name_defaults_to_session_name::RuntimeError::756fb1` | 4 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`stop after VM resolution`, `stop after agent realize`, `stop after anchor check`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-115 (2026-08-19 map). |
| G1-I13 | `cli/tests/test_vm_shell_platform.py::test_native_transport_closes_allow_on_exception_for_azure::RuntimeError::dbbbc8` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`simulated post-open failure`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-123 (2026-08-19 map). |
| G1-I14 | `cli/tests/test_wsl2_create_interrupt.py::test_failure_mid_provision_cleans_up_and_reraises::RuntimeError::b98d9a,test_cleanup_failure_warns_and_does_not_mask_the_original::RuntimeError::37a06e` | 2 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`apt exploded`, `original failure`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-125 (2026-08-19 map). |
| G1-I15 | `cli/tests/test_wsl2_keepalive.py::test_vm_active_terminates_on_exception::RuntimeError::81f523` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`boom`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-126 (2026-08-19 map). |
| G1-I16 | `cli/tests/vms/test_bootstrap_vm_hooks.py::test_failed_path_secures_the_kept_vm::RuntimeError::c1fee0,test_failed_path_hook_failure_does_not_mask::RuntimeError::c1fee0` | 2 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`phase a exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-135 (2026-08-19 map). |
| G1-I17 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_rollback_failure_warns_and_never_masks::ProvisioningError::870493,test_create_init_failure_keeps_the_row::ExternalError::f79466,test_create_phase_a_failure_maps_to_provisioning_error::ProvisioningError::a4de2d` | 3 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`backend exploded`, `bootstrap exploded`, `init exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-136 (2026-08-19 map). |
| G1-I18 | `cli/tests/vms/test_create_vm_dispatch.py::test_proxmox_token_resolves_end_to_end::ProvisioningError::d1bfad,test_create_vm_metadata_failure_retains_owner_and_reports_once::RuntimeError::aa01a4,test_create_vm_logger_close_failure_retains_owner_and_reports_once::RuntimeError::99e237` | 3 injected-marker site(s) | keep | The matched text is a marker this test file writes and hands to an exception constructor (`halt after binding`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-137 (2026-08-19 map). |
| G1-I19 | `cli/tests/vms/test_lima_create_interrupt.py::test_failure_mid_create_cleans_up_and_reraises::SSHError::f65498` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`provision exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-140 (2026-08-19 map). |
| G1-I20 | `cli/tests/test_secrets_eager_resolve_console.py::test_console_add_shell_eager_resolve_fires_before_db_update::SecretUnavailableError::8c2840,test_console_add_sessions_with_shells_eager_resolves::SecretUnavailableError::8c2840,test_restore_session_window_missing_branch_eager_resolves::SecretUnavailableError::8c2840,test_start_console_eager_resolves_before_tmux::SecretUnavailableError::8c2840` | 4 injected-marker site(s) | keep | The matched text is a value this test seeded and handed to the exception it injected, and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed rather than a pin on authored prose. Screened on return from the 2026-08-19 subtraction; the callee-side screen does not reach these callees and does not need to, because the injected-marker screen decides them. |
| G1-I21 | `cli/tests/test_secrets_eager_resolve_sessions.py::test_session_create_eager_resolve_fires_before_db_insert::SecretUnavailableError::8c2840,test_session_restart_eager_resolve_fires_before_teardown::SecretUnavailableError::8c2840` | 2 injected-marker site(s) | keep | The matched text is a value this test seeded and handed to the exception it injected, and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed rather than a pin on authored prose. Screened on return from the 2026-08-19 subtraction; the callee-side screen does not reach these callees and does not need to, because the injected-marker screen decides them. |
| G1-I22 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_vm_shell_eager_resolve_fires_before_ssh::SecretUnavailableError::8c2840,test_vm_exec_eager_resolve_fires_before_ssh::SecretUnavailableError::8c2840,test_agent_exec_eager_resolve_fires_before_ssh::SecretUnavailableError::8c2840` | 3 injected-marker site(s) | keep | The matched text is a value this test seeded and handed to the exception it injected, and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed rather than a pin on authored prose. Screened on return from the 2026-08-19 subtraction; the callee-side screen does not reach these callees and does not need to, because the injected-marker screen decides them. |
| G1-I23 | `cli/tests/test_database_migration_safety.py::test_database_closes_connection_when_migration_raises::RuntimeError::81f523,test_get_db_selected_backup_failure_prevents_migration_and_guides_retry::BackupError::239dfc` | 2 injected-marker site(s) | keep | The matched text is a value this test seeded and handed to the exception it injected, and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed rather than a pin on authored prose. Screened on return from the 2026-08-19 subtraction; the callee-side screen does not reach these callees and does not need to, because the injected-marker screen decides them. |
| G1-I24 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_create_logger_close_failure_retains_owner_and_reports_once::RuntimeError::99e237,test_reinit_build_failure_reports_the_committed_overlay_once::RuntimeError::7ecb11` | 2 injected-marker site(s) | keep | The matched text is a value this test seeded and handed to the exception it injected, and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed rather than a pin on authored prose. Cut fresh 2026-09-06: the site is one the tree gained since the 2026-08-19 map and no row claimed it. |
| G1-I25 | `cli/tests/test_consoles_add_placement.py::test_manager_rolls_back_additions_when_reorder_fails::RuntimeError::32b82e` | 1 injected-marker site(s) | keep | The matched text is a value this test seeded and handed to the exception it injected, and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed rather than a pin on authored prose. Cut fresh 2026-09-06: the site is one the tree gained since the 2026-08-19 map and no row claimed it. |

### The mechanical batch

One row per file, every one a `delete`. Run `sweep-screen.py generate` to size a PR from: it reports
how many rows it emits and over how many sites, at whatever tree you run it against. The
justification is shared rather than restated per row, because it is the same sentence every time:
the matched string is a fixed literal that varies with nothing, and it is prose this repository
authors, which no case in the taxonomy licenses. The `raises` stays; only the `match=` argument
goes.

Each row's cell lists the sites that row owns, generated from the estate minus the sites the
judgment and keep rows above claim. Read it as the row's claim; there is no range to over-apply, and
`sweep-screen.py attribute` fails loudly if a site ends up owned by two rows or by none.
**`sweep-screen.py generate` is that derivation**, so a batch cut at a later tree is emitted rather
than assembled by hand: it prints these rows in the row grammar, with the header and the
prettier-ignore line, ready to paste. Then it runs four checks over the claims plus what it just
generated, none of them a count comparison, because the generated set is the complement of the
claims and adds up by construction whatever the claims are. Each refuses:

1. **Ownership.** Every `match=` site owned by exactly one row, which is what catches two claim rows
   reaching the same site.
2. **Widened claims.** A group-1 claim that reaches a site through a span or a line anchor, which
   covers every assertion its test gains after the row was written rather than the one it was
   written about.
3. **Stale claims.** A group-1 claim anchor that does not resolve at this tree, because its sites
   fall into the batch as ordinary deletes and the judgment that pulled them out goes with no word
   said. A `[deferred]` row is still a claim, because its sites are real and must not fall into the
   batch.
4. **A handle the screen found.** A site the callee-side raise screen settles as
   multi-raise-path-with-a-handle is `hla.md` case 2's first arm, so it converts onto the handle and
   must not leave as a mechanical delete. This is the check that stops case 2 being decided by
   nobody noticing it, and G1-C05 is the row it produced.

`generate` also reports the group-1 span claims that resolve and cover no site. Those do not refuse,
because one tree cannot tell a test that lost its sites from one that never had any.

**Corrected 2026-08-19 by the callee-side raise screen.** This paragraph used to say that no test
function in the set raises the same type twice, and conclude from that that each site is `hla.md`
case 1, the raised type already discriminating. The premise is a fact about the CALLER and the
conclusion is a claim about the CALLEE, and the screen shows the conclusion is false for most of the
sites it can resolve: the operations these tests call raise the asserted type from several paths, so
the type discriminates nothing. What that changes is the reason and the cost, not the edit. Where a
handle tells the targeted raise apart the row has been pulled out to a `G1-C` row and converts;
everywhere else this batch is `hla.md` case 2's fallback rather than its case 1, which R2.4 resolves
to the same deletion with the branch coverage going too. **Stated plainly, because it is what this
batch now costs: after the edit, a same-type failure raised from a different path in the same
operation will satisfy these tests.** A row carrying `[1-raise]` is one the screen verified as
genuinely single-path across every site it claims, and `generate` writes that marker from the
screen's own verdicts. The rest of the batch is unscreened, and the executor owes it the screen per
row before the edit lands.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition |
| --- | --- | --- | --- |
| G1-001 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_create_mutation_failure_cleans_up_and_leaves_no_row::ExternalError::faf476,test_reinit_mutation_failure_wraps_and_keeps_the_agent::ExternalError::5d8d65,test_reinit_unknown_update_template_raises_and_keeps_the_row::NotFoundError::788028,test_reinit_update_template_persists_before_convergence_so_a_mid_failure_keeps_the_new_binding::ExternalError::5d8d65` | 4 `match=` site(s) over ExternalError, NotFoundError | delete |
| G1-002 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_delete_sessions_guard_refuses_with_zero_resolves_and_zero_gate::StateError::54d6d7,test_delete_declined_confirm_aborts_with_zero_resolves_and_zero_gate::UserAbort::0cf0fb,test_delete_nested_rejects_a_mismatched_vm_node::StateError::207681` | 3 `match=` site(s) over StateError, UserAbort | delete |
| G1-003 | `cli/tests/agents/test_shell_exec_orchestrated.py::test_exec_dash_prefixed_command_fails_with_zero_resolves_and_zero_gate::ValidationError::05d8e1,test_exec_missing_command_after_double_dash_fails_pre_gate::ValidationError::1cfdd7,test_missing_grant_fails_with_zero_resolves_and_zero_gate::AuthorizationError::40fe55` | 3 `match=` site(s) over AuthorizationError, ValidationError | delete |
| G1-004 | `cli/tests/assistance/test_generation.py::test_unexpected_generated_file_is_rejected::GenerationError::d094e2,test_readme_requires_one_ordered_marker_pair::GenerationError::2b7814,test_metadata_schema_rejects_unknown_fields::GenerationError::1cc88e` | 3 `match=` site(s) over GenerationError | delete |
| G1-005 | `cli/tests/capabilities/test_capability_config.py::test_an_unknown_field_names_the_fields_that_are_valid::ConfigError::bc57ee,test_the_arm_is_selected_by_the_capability_name_not_by_the_blob::ConfigError::b118d9,test_construct_time_validation_raises_the_same_framed_error::ConfigError::57f465` | 3 `match=` site(s) over ConfigError | delete |
| G1-006 | `cli/tests/capabilities/test_capability_descriptors.py::test_descriptor_for_raises_on_a_kind_with_no_record::StateError::d8ae40,test_mapping_descriptor_requires_contract_and_host_together::StateError::173cbe,test_mapping_descriptor_requires_unique_host_field_ownership::StateError::b5796e` | 3 `match=` site(s) over StateError | delete |
| G1-007 | `cli/tests/capabilities/test_declare_once_end_to_end.py::test_the_declarations_constraint_reaches_validation::ConfigError::7f6e39` | 1 `match=` site(s) over ConfigError | delete |
| G1-008 | `cli/tests/manifests/test_capability_shape.py::test_a_kind_owned_key_inside_the_table_is_the_platforms_to_refuse::ConfigError::55786f` | 1 `match=` site(s) over ConfigError | delete |
| G1-009 | `cli/tests/manifests/test_decode_shapes.py::test_a_kind_owned_key_inside_the_provider_block_is_the_providers_to_refuse::ConfigError::b90cf6,test_provider_must_be_a_tagged_table::ConfigError::14fa63` | 2 `match=` site(s) over ConfigError | delete |
| G1-010 | `cli/tests/manifests/test_describe_kind.py::test_the_quoted_boolean_warning_describes_this_loader::ConfigError::96ba2c` | 1 `match=` site(s) over ConfigError | delete |
| G1-011 | `cli/tests/manifests/test_emit.py::test_a_capability_key_the_schema_rejects_is_rejected_on_every_host::ConfigError::6476e6` | 1 `match=` site(s) over ConfigError | delete |
| G1-012 | `cli/tests/manifests/test_loader_and_envelope.py::test_duplicate_within_one_file_errors::ConfigError::809c7f,test_invalid_yaml_reports_location::ConfigError::ecf065,test_unknown_kind_gets_kebab_hint::ConfigError::0b5f13,test_no_capability_kind_can_be_declared::ConfigError::8ef97a,test_singleton_kind_rejects_non_default_name::ConfigError::5ab464,test_unknown_metadata_key_rejected::ConfigError::ee6f26,test_missing_spec_key_rejected::ConfigError::d5bd17,test_duplicate_mapping_key_rejected::ConfigError::17aa38,test_unhashable_mapping_key_surfaces_clean_error::ConfigError::74d7af,test_merge_keys_rejected::ConfigError::6ffd92` | 10 `match=` site(s) over ConfigError | delete |
| G1-013 | `cli/tests/manifests/test_samples.py::test_all_kinds_concatenation_and_unknown_kind::ValidationError::0b5f13,test_write_sample_refuses_windows_drive_relative_path::ValidationError::9c035e,test_write_sample_refuses_unloadable_dot_paths::ValidationError::253a7b` | 3 `match=` site(s) over ValidationError | delete |
| G1-014 | `cli/tests/manifests/test_spec_model.py::test_a_capability_kind_has_no_row_to_describe::ValidationError::e20e73` | **[1-raise]** 1 `match=` site(s) over ValidationError | delete |
| G1-015 | `cli/tests/manifests/test_spec_templates.py::test_an_inheriting_row_refuses_a_default_template_at_import::StateError::89f386,test_an_empty_auth_key_never_reaches_the_resolved_template::ConfigError::11505c` | 2 `match=` site(s) over ConfigError, StateError | delete |
| G1-016 | `cli/tests/orchestration/test_activation.py::test_operator_stopped_refusal_propagates_from_the_node::StateError::836f41,test_refusal_precedes_the_span::StateError::836f41` | 2 `match=` site(s) over StateError | delete |
| G1-017 | `cli/tests/orchestration/test_walk.py::test_cycle_is_a_loud_error::StateError::3afe5d,test_self_cycle_is_a_loud_error::StateError::c50193,test_cycle_report_trims_the_acyclic_prefix::StateError::dfc6bd,test_two_objects_sharing_a_key_is_a_loud_error::StateError::c39242,test_two_objects_sharing_a_key_across_roots_is_loud_too::StateError::159139` | 5 `match=` site(s) over StateError | delete |
| G1-018 | `cli/tests/plugins/gcp/test_bootstrap.py::test_utf8_size_gate_accepts_exact_limit_and_rejects_plus_one::ConfigError::428af0` | **[1-raise]** 1 `match=` site(s) over ConfigError | delete |
| G1-019 | `cli/tests/plugins/gcp/test_compute.py::test_unrecognized_sdk_shape_treats_required_scalar_as_absent::ConfigError::7d834a,test_live_machine_architecture_mismatch_fails_before_mutation::ConfigError::b151c3,test_image_architecture_mismatch_is_rejected::ConfigError::54740a,test_exact_instance_collision_and_not_found_paths::AlreadyExistsError::321425,test_external_ipv4_is_read_live_from_named_nat_config::GCEError::11c86c` | 5 `match=` site(s) over AlreadyExistsError, ConfigError, GCEError | delete |
| G1-020 | `cli/tests/plugins/gcp/test_config.py::test_selection_failure_is_typed_and_names_the_largest_entry::ConfigError::6bda8b` | **[1-raise]** 1 `match=` site(s) over ConfigError | delete |
| G1-021 | `cli/tests/plugins/gcp/test_instance.py::test_indeterminate_wait_reconciles_only_matching_provider_id::AlreadyExistsError::4287e8,test_incomplete_operation_identity_is_never_owned::GCEOperationError::363691` | 2 `match=` site(s) over AlreadyExistsError, GCEOperationError | delete |
| G1-022 | `cli/tests/plugins/gcp/test_network.py::test_zone_to_region_is_exact_and_invalid_shape_rejected::ConfigError::071588,test_omitted_subnet_requires_and_resolves_default_network::ConfigError::e9eb7e,test_selected_network_is_re_read_and_identity_checked::ConfigError::ee2d42,test_shared_vpc_host_project_subnet_is_explicitly_unsupported::ConfigError::db3a37,test_network_policy_must_be_after_classic_firewall::ConfigError::f7cce7,test_applicable_priority_zero_allow_is_rejected::ConfigError::080e68,test_priority_zero_deny_overlapping_operator_ssh_is_rejected::ConfigError::604b28,test_firewall_name_collision_and_absence::AlreadyExistsError::321425,test_indeterminate_insert_mismatch_is_collision_and_never_deleted::AlreadyExistsError::c7f2c0,test_definite_already_exists_is_collision_without_shape_reconciliation::AlreadyExistsError::321425,test_same_request_retry_already_exists_remains_collision::AlreadyExistsError::321425,test_incomplete_or_wrong_operation_identity_is_never_owned::GCEOperationError::363691` | 12 `match=` site(s) over AlreadyExistsError, ConfigError, GCEOperationError | delete |
| G1-023 | `cli/tests/plugins/gcp/test_platform.py::test_known_machine_incompatibility_stays_pre_mutation::ConfigError::a89f38,test_delete_retains_same_name_different_id_instance_and_lifetime_deny::GCEOperationError::54eaaa,test_delete_refuses_noncanonical_persisted_allow_sources_before_any_mutation::StateError::a804c3,test_delete_keeps_deny_when_allow_read_and_instance_delete_are_indeterminate::GCEOperationError::54eaaa` | 4 `match=` site(s) over ConfigError, GCEOperationError, StateError | delete |
| G1-024 | `cli/tests/plugins/test_enablement_producer.py::test_disabled_plugin_platform_withholds_its_config_implied_secret::StateError::a2f8a5,test_mapping_to_a_disabled_plugin_backend_is_validated_like_any_other::ConfigError::8a9e29,test_mapping_to_an_absent_backend_reports_the_dangling_edge_not_a_shape_error::ConfigError::ee26df,test_git_credential_node_refuses_a_disabled_provider::StateError::a2f8a5,test_ensure_harness_integration_enabled_raises_for_a_disabled_plugin_integration::StateError::a2f8a5` | 5 `match=` site(s) over ConfigError, StateError | delete |
| G1-025 | `cli/tests/plugins/test_gcp.py::test_gcloud_recipe_is_gated_until_gcp_is_enabled::StateError::3dba48,test_disabled_gcp_site_is_not_ready_and_refused_with_enable_hint::StateError::3dba48` | **[1-raise]** 2 `match=` site(s) over StateError | delete |
| G1-026 | `cli/tests/plugins/test_plugin_framework.py::test_rejects_instance_instead_of_class_the_secret_backend_trap::PluginError::819288,test_rejects_impl_with_missing_name::PluginError::f2c5d2,test_rejects_impl_with_slash_bearing_name::PluginError::c14cdd,test_rejects_intra_descriptor_collision::PluginError::b287b3,test_a_non_conforming_impl_is_refused_before_any_registry_write::PluginError::26905d,test_secret_backend_subclass_under_a_taken_name_is_not_idempotent::PluginError::16a5bb` | 6 `match=` site(s) over PluginError | delete |
| G1-027 | `cli/tests/plugins/test_proxmox_api.py::TestErrorHandling.test_http_error_raises::ProxmoxAPIError::dcaada,TestErrorHandling.test_task_failure_raises::ProxmoxAPIError::973420,TestErrorHandling.test_task_timeout_raises::ProxmoxAPIError::3dcd80` | 3 `match=` site(s) over ProxmoxAPIError | delete |
| G1-028 | `cli/tests/plugins/test_proxmox_bootstrap_staging.py::test_failure_output_is_not_repeated_to_observable_error_text::ProvisioningError::47b304,test_forged_success_ip_is_not_returned_or_repeated::ProvisioningError::b7e6ca,test_execute_unwind_removes_stage::ProvisioningError::2ebb05` | 3 `match=` site(s) over ProvisioningError | delete |
| G1-029 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestFailClosedBootstrap.test_bootstrap_failure_cleanup_survivor_keeps_primary_and_manual_guidance::ProvisioningError::2ebb05` | 1 `match=` site(s) over ProvisioningError | delete |
| G1-030 | `cli/tests/plugins/test_publish.py::test_dirty_bundle_raises_config_error_not_assertion::ConfigError::7fa4b1,test_plugin_path_gates_missing_subdir_but_builtin_path_does_not::ConfigError::f18e8a,test_disabled_plugin_harness_integration_reaches_use_gate_not_unknown::StateError::5af925` | 3 `match=` site(s) over ConfigError, StateError | delete |
| G1-031 | `cli/tests/resources/test_admin_template_plurified.py::test_admin_template_kind_errors_on_unreserved_name_reference::ConfigError::ce1f89` | 1 `match=` site(s) over ConfigError | delete |
| G1-032 | `cli/tests/resources/test_cycle_detection.py::test_two_node_cycle_detected::ConfigError::c50193,test_three_node_cycle_detected::ConfigError::c50193` | 2 `match=` site(s) over ConfigError | delete |
| G1-033 | `cli/tests/resources/test_effective_template_dependencies.py::test_a_kind_nobody_registered_is_refused_rather_than_answered_empty::StateError::9707c5,test_the_effective_resolve_is_total_over_a_cyclic_chain::InheritanceCycleError::7b8fd2,test_a_cyclic_chain_still_reports_the_cycle_rather_than_a_degraded_graph::ConfigError::2a2415` | 3 `match=` site(s) over ConfigError, InheritanceCycleError, StateError | delete |
| G1-034 | `cli/tests/resources/test_finalize_pass.py::test_admin_template_rejects_non_default_name::ConfigError::901d43` | 1 `match=` site(s) over ConfigError | delete |
| G1-035 | `cli/tests/resources/test_git_credential_provider_kind.py::test_unknown_provider_errors_with_framework_shape::ConfigError::754f75` | 1 `match=` site(s) over ConfigError | delete |
| G1-036 | `cli/tests/resources/test_graph.py::test_graph_is_frozen_and_registry_rejects_refinalize::RuntimeError::77be2a,test_graph_property_before_finalize_raises::RuntimeError::7527a6` | 2 `match=` site(s) over RuntimeError | delete |
| G1-037 | `cli/tests/resources/test_harness_integration_kind.py::test_capability_kind_envelope_rejection::ConfigError::8ef97a` | 1 `match=` site(s) over ConfigError | delete |
| G1-038 | `cli/tests/resources/test_inheritance_layers.py::test_the_resolver_path_refuses_the_same_cycle::InheritanceCycleError::47b52f` | 1 `match=` site(s) over InheritanceCycleError | delete |
| G1-039 | `cli/tests/resources/test_inheritance_traversal.py::test_enablement_still_propagates_across_the_inheritance_edge::StateError::415a4a,test_the_gate_does_not_refuse_over_an_ancestor_leaf_the_child_overrode::StateError::a300da` | **[1-raise]** 2 `match=` site(s) over StateError | delete |
| G1-040 | `cli/tests/resources/test_install_resource_kinds.py::test_apt_package_typo_errors_with_source::ConfigError::9cf61e,test_system_install_command_typo_errors_with_source::ConfigError::eba334,test_user_install_command_typo_in_admin_errors::ConfigError::12d895,test_user_install_command_typo_in_agent_errors::ConfigError::12d895,test_unknown_apt_source_reference_errors_via_framework::ConfigError::a90f49` | 5 `match=` site(s) over ConfigError | delete |
| G1-041 | `cli/tests/resources/test_kind_synthesize_empty.py::test_secret_kind_raises_on_empty_requirements::NoUnreferencedDefaultError::884b23` | 1 `match=` site(s) over NoUnreferencedDefaultError | delete |
| G1-042 | `cli/tests/resources/test_readiness_fold.py::test_r9_9_mapping_is_validated_whether_or_not_its_backend_is_enabled::ConfigError::0efcc1,test_fold_does_not_throw_on_malformed_platform_config::ConfigError::fb3a37,test_r5_ready_site_with_unknown_field_fails_validation::ConfigError::1d0222` | 3 `match=` site(s) over ConfigError | delete |
| G1-043 | `cli/tests/resources/test_registry_collisions.py::test_builtin_over_operator_is_an_ordering_conflict::ConfigError::3cc894,test_operator_collision_has_no_singleton_exemption::ConfigError::f9adaf,test_line_zero_origin_collides_on_every_kind::ConfigError::52d9a9,test_two_system_plugins_on_one_name_is_a_curation_error::ConfigError::1a4114,test_system_plugin_over_builtin_collides::ConfigError::afdff2,test_builtin_over_system_plugin_collides_same_message::ConfigError::afdff2,test_operator_over_reserved_system_plugin_errors::ConfigError::cc2017,test_enabled_plugin_over_operator_reserved_kind_still_errors::ConfigError::df3328` | 8 `match=` site(s) over ConfigError | delete |
| G1-044 | `cli/tests/resources/test_registry_lifecycle.py::test_add_after_finalize_errors::RuntimeError::ffb304,test_finalize_twice_errors::RuntimeError::77be2a,test_unknown_kind_in_requirement_errors_clearly::ConfigError::39f539` | 3 `match=` site(s) over ConfigError, RuntimeError | delete |
| G1-045 | `cli/tests/resources/test_registry_weak_rows.py::test_weak_survivor_with_no_source_is_a_state_error::StateError::133b21,test_two_enabled_plugin_strong_rows_still_collide::ConfigError::1a4114` | 2 `match=` site(s) over ConfigError, StateError | delete |
| G1-046 | `cli/tests/resources/test_template_kinds.py::test_inherits_cycle_caught_by_framework::ConfigError::c6414d` | 1 `match=` site(s) over ConfigError | delete |
| G1-047 | `cli/tests/resources/test_vm_template_kind.py::test_non_default_self_reference_caught_by_framework::ConfigError::c6414d,test_inherits_cycle_through_default_caught_at_build_registry::ConfigError::c50193` | 2 `match=` site(s) over ConfigError | delete |
| G1-048 | `cli/tests/schema/test_shorthand.py::test_a_value_that_is_neither_spelling_is_still_refused::ValueError::be051b,test_a_boolean_is_not_an_integer_shorthand::ValueError::be051b,test_a_shorthand_folding_into_a_field_that_does_not_exist_is_refused_at_import::StateError::fde132,test_a_shorthand_spelled_as_something_no_document_carries_is_refused::StateError::4d99c7,test_the_declaration_is_the_only_place_the_spelling_is_written::ValueError::be051b,test_an_arm_shorthand_alone_does_not_dispatch_a_tagged_union::ValueError::be051b,test_a_collection_union_documents_only_its_selected_scalar_arm::ValueError::be051b,test_a_collapsed_collection_union_keeps_scalar_dispatch_explicit::ValueError::be051b` | 8 `match=` site(s) over StateError, ValueError | delete |
| G1-049 | `cli/tests/secrets/test_resolver_seed.py::test_unseeded_pre_pass_read_still_raises::StateError::52c863,test_seeding_after_the_pass_is_a_loud_error::StateError::8087b7` | 2 `match=` site(s) over StateError | delete |
| G1-050 | `cli/tests/secrets/test_sources.py::test_direct_secret_decl_requires_exact_string_keys::ValidationError::aa4c28,test_mapping_validation_does_not_accept_another_registered_models_shape::ConfigError::80464d,test_mapping_key_collector_rejects_unknown_source_even_for_false::ConfigError::35edc2` | 3 `match=` site(s) over ConfigError, ValidationError | delete |
| G1-051 | `cli/tests/sessions/test_console_attach_orchestrated.py::test_named_console_no_tailscale_fails_with_zero_resolves_and_zero_gate::StateError::d60755` | 1 `match=` site(s) over StateError | delete |
| G1-052 | `cli/tests/sessions/test_create_start_restart_orchestrated.py::test_restart_missing_binary_aborts_with_the_old_session_running::StateError::2dc28d` | 1 `match=` site(s) over StateError | delete |
| G1-053 | `cli/tests/sessions/test_effective_config_validation.py::test_a_parent_that_cannot_stand_alone_is_itself_a_load_error::ConfigError::89deb5` | 1 `match=` site(s) over ConfigError | delete |
| G1-054 | `cli/tests/sessions/test_session_nodes.py::test_scope_less_context_is_a_loud_error::StateError::2685b8,test_missing_command_is_a_typed_error::StateError::28899f,test_admin_mode_error_names_the_vm::StateError::2dc28d,test_missing_transport_defers_at_preflight_and_is_loud_at_runup::StateError::84d1fe,test_live_session_admin_mode_comes_from_the_row::StateError::220806,test_live_session_agent_row_with_no_agent_node_is_loud::StateError::1e8222,test_live_session_admin_row_with_an_agent_node_is_loud::StateError::01cebb,test_live_session_agent_name_mismatch_is_loud::StateError::211d74` | 8 `match=` site(s) over StateError | delete |
| G1-055 | `cli/tests/sessions/test_session_template_surface.py::test_manifest_flat_field_is_rejected::ConfigError::c1d69f,test_manifest_unknown_harness_name_errors_at_finalize::ConfigError::de2824` | 2 `match=` site(s) over ConfigError | delete |
| G1-056 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_batch_operator_stopped_vm_aborts_before_the_probes::StateError::836f41,test_batch_gate_refuses_an_undeclared_outside_union_name::StateError::c04fb6,test_unknown_session_refuses_with_zero_resolves_and_zero_gate::NotFoundError::7a369b,test_unknown_workspace_refuses_with_zero_resolves_and_zero_gate::NotFoundError::efb2b2,test_no_tailscale_vm_fails_pre_gate_with_zero_resolves::StateError::d60755` | 5 `match=` site(s) over NotFoundError, StateError | delete |
| G1-057 | `cli/tests/test_apt.py::test_apt_source_rejects_unsafe_source_file::ConfigError::b9f663,test_apt_source_requires_key_url::ConfigError::dd46e2,test_apt_source_must_be_table::ConfigError::d65852,test_apt_package_apt_must_be_list::ConfigError::805256,test_bad_apt_source_reference_errors_at_build_registry::ConfigError::7945bc` | 5 `match=` site(s) over ConfigError | delete |
| G1-058 | `cli/tests/test_aws_credentials.py::TestCredentialSelection.test_context_without_resolved_secrets_is_typed::ConfigError::880d18` | 1 `match=` site(s) over ConfigError | delete |
| G1-059 | `cli/tests/test_aws_ec2_ops.py::TestCreate.test_no_default_subnet_is_typed::EC2Error::cb137f,TestCreate.test_user_data_over_cap_is_typed::EC2Error::165daa,TestCreate.test_rejects_a_name_collision::StateError::321425,TestCreate.test_arch_cross_check_rejects_a_mismatch::ConfigError::808d46,TestTransientRoute.test_requires_a_security_group_id::StateError::346c89` | 5 `match=` site(s) over ConfigError, EC2Error, StateError | delete |
| G1-060 | `cli/tests/test_aws_instance_type_selection.py::TestInstanceCatalog.test_arch_error_names_the_ec2_vocabulary::ConfigError::8ceb92` | 1 `match=` site(s) over ConfigError | delete |
| G1-061 | `cli/tests/test_azure_create_interrupt.py::TestFailureDuringInlineWait.test_non_ssh_error_escaping_the_wait_rolls_back_vm_first_and_wraps::AzureError::7f5a55,TestFailureDuringInlineWait.test_ssh_readiness_exhaustion_fails_closed_without_key_delivery::AzureError::3cd0dc,TestFailureDuringInlineWait.test_cloud_init_wait_failure_fails_closed_without_key_delivery::AzureError::71cfc9` | 3 `match=` site(s) over AzureError | delete |
| G1-062 | `cli/tests/test_azure_credentials.py::TestCredentialSelection.test_service_principal_rejection_surfaces_through_the_ops::AzureError::453d97,TestCredentialSelection.test_service_principal_without_delivered_secrets_is_typed::ConfigError::880d18` | 2 `match=` site(s) over AzureError, ConfigError | delete |
| G1-063 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_unidentified_failure_raises_azure_error::AzureError::646976,TestSurvivingVMRaises.test_malformed_details_classify_as_generic_without_raising::AzureError::f9fd6c,TestSurvivingVMRaises.test_silent_survival_raises_even_without_a_captured_failure::AzureError::44f7d1,TestSurvivingVMRaises.test_unconfirmable_probe_raises::AzureError::724ede` | 4 `match=` site(s) over AzureError | delete |
| G1-064 | `cli/tests/test_azure_nsg_exposure.py::TestPrefixAssembly.test_invalid_extra_rejected::ConfigError::f5f627` | **[1-raise]** 1 `match=` site(s) over ConfigError | delete |
| G1-065 | `cli/tests/test_azure_vm_size_selection.py::TestValidateConfig.test_rejects_malformed_vm_sizes_at_load::ConfigError::f980ff,TestValidateConfig.test_still_rejects_unknown_field::ConfigError::1d0222` | 2 `match=` site(s) over ConfigError | delete |
| G1-066 | `cli/tests/test_capability_base.py::test_construct_validates_config::ConfigError::2e4421` | 1 `match=` site(s) over ConfigError | delete |
| G1-067 | `cli/tests/test_capability_config_contract.py::test_azdo_org_required::ConfigError::ce67de,test_azdo_rejects_unknown_blob_fields_yaml::ConfigError::da1edb,test_a_capability_with_no_config_rejects_every_key::ConfigError::13bc20,test_github_rejects_unknown_blob_fields::ConfigError::0d538d,test_unknown_provider_defers_to_miss_policy::ConfigError::8415f4,test_a_broken_blob_is_refused_whatever_the_plugins_state::ConfigError::570578,test_cycle_reported_before_malformed_block::ConfigError::c6414d,test_construct_time_validation_survives_the_flip::ConfigError::ce67de,test_env_var_mapping_validated_at_build_registry::ConfigError::f82117,test_prompt_rejects_any_mapping::ConfigError::4da920,test_declared_mapping_for_non_opted_in_source_is_validated_at_build::ConfigError::f82117` | 11 `match=` site(s) over ConfigError | delete |
| G1-068 | `cli/tests/test_claude_code_integration.py::test_validation_rejects_unknown_field::ConfigError::de5fea,test_validation_rejects_non_list_extra_args::ConfigError::d0b029,test_construct_revalidates_config::ConfigError::0aeeb1,test_probe_keeps_find_failure_distinct_from_a_clean_no_match::StateError::cff336,test_probe_that_could_not_execute_raises_rather_than_guessing::StateError::cff336,test_readiness_missing_claude_is_a_typed_error::StateError::89607a` | 6 `match=` site(s) over ConfigError, StateError | delete |
| G1-069 | `cli/tests/test_codex_integration.py::test_validation_rejects_unknown_field::ConfigError::cc73ed,test_validation_rejects_non_list_extra_args::ConfigError::d0b029,test_construct_revalidates_config::ConfigError::0aeeb1,test_recorder_file_that_exists_but_will_not_read_raises::StateError::8b3e04,test_discovery_with_an_unresolvable_workspace_raises::StateError::7c211d,test_discovery_probe_failure_raises_rather_than_guessing::StateError::cff336,test_rollout_probe_that_could_not_execute_raises_rather_than_guessing::StateError::cff336,test_rollout_probe_keeps_find_failure_distinct_from_a_clean_no_match::StateError::cff336,test_model_merge_never_launders_an_invalid_writable_dirs_entry::ConfigError::26b714,test_sh_probe_missing_workspace_dir_raises::StateError::7c211d,test_readiness_missing_codex_is_a_typed_error::StateError::c3366c` | 11 `match=` site(s) over ConfigError, StateError | delete |
| G1-070 | `cli/tests/test_completions.py::TestGeneration.test_unsupported_shell_raises::ValueError::999d26` | **[1-raise]** 1 `match=` site(s) over ValueError | delete |
| G1-071 | `cli/tests/test_config.py::test_clear_on_detach_rejects_unknown_value::ConfigError::8b860a,test_cycle_detection::ConfigError::c50193,test_invalid_git_credential_type::ConfigError::2a899e,test_unexpected_top_level_keys_fail::ConfigError::74d21b,test_dotfiles_keeps_its_removal_guidance::ConfigError::4c886a,test_extra_ssh_public_keys_missing_file::ConfigError::8a3dd8,test_workload_gated_issues_fatal_true_still_raises_by_default::ConfigError::5d6e5d,test_ssh_allow_cidrs_invalid_entry_rejected::ConfigError::2bad2a,test_ssh_allow_cidrs_scalar_rejected::ConfigError::84e943,test_user_section_is_rejected::ConfigError::973a2a,test_claude_marketplaces_rejects_string::ConfigError::0eeb61,test_named_console_tmux_layout_rejects_unknown::ConfigError::17b7bf,test_named_console_section_unexpected_keys_are_refused::ConfigError::68bf79` | 13 `match=` site(s) over ConfigError | delete |
| G1-072 | `cli/tests/test_config_env_and_secrets.py::test_invalid_env_key_raises::ConfigError::c44c01,test_env_inline_table_unknown_key_rejected::ConfigError::4e980d,test_env_secret_must_be_string::ConfigError::2bb80d,test_secret_true_in_backend_mappings_rejected::ConfigError::b5bea4,test_unknown_source_name_raises::ConfigError::f9f541,test_unreachable_secret_raises::ConfigError::5e6017,test_session_template_required_commands_must_be_list::ConfigError::7cef1e,test_session_template_required_commands_must_be_strings::ConfigError::ecfca8,test_explicit_secret_declaration_invalid_still_raises::ConfigError::65cc69` | 9 `match=` site(s) over ConfigError | delete |
| G1-073 | `cli/tests/test_config_plugins.py::test_plugins_section_not_a_table_raises::ConfigError::36e825,test_plugins_system_not_a_list_of_strings_raises::ConfigError::0e20d3,test_plugins_system_list_with_non_string_element_raises::ConfigError::0e20d3,test_plugins_unknown_key_is_a_hard_config_error::ConfigError::501ed4,test_plugins_old_enabled_key_is_a_hard_config_error::ConfigError::8824b4,test_plugins_unknown_key_alongside_valid_system_still_raises::ConfigError::501ed4` | 6 `match=` site(s) over ConfigError | delete |
| G1-074 | `cli/tests/test_config_vm_workspaces.py::test_rejects_home_paths::ConfigError::186931,test_rejects_home_path_on_windows_style_host::ConfigError::186931,test_load_config_rejects_home_vm_workspaces::ConfigError::186931` | 3 `match=` site(s) over ConfigError | delete |
| G1-075 | `cli/tests/test_consoles_orchestration.py::test_infer_vm_from_session_specs::ValidationError::617c1d,test_create_console_rejects_empty_without_all::ValidationError::7a888b,test_create_console_rejects_empty_fill_all::ValidationError::972d37,test_create_console_rejects_duplicate_name::AlreadyExistsError::321425,test_create_console_rejects_unknown_vm::NotFoundError::907ba7,test_create_console_rejects_unknown_session::NotFoundError::907ba7,test_create_console_rejects_cross_vm_session::ValidationError::b9fa0d,test_create_console_rejects_dup_in_args::ValidationError::7ed09c,test_create_console_rolls_back_on_failure::AlreadyExistsError::321425,test_add_sessions_rejects_duplicate::AlreadyExistsError::9f23d7,test_remove_sessions_rejects_non_member::NotFoundError::588bcf,test_reorder_sessions_rejects_non_member::NotFoundError::588bcf,test_reorder_sessions_rejects_duplicates::ValidationError::7ed09c,test_reorder_sessions_rejects_missing_console::NotFoundError::f78595,test_reorder_sessions_rejects_empty_input::ValidationError::52d9aa,test_add_shell_rejects_non_member::NotFoundError::588bcf` | 16 `match=` site(s) over AlreadyExistsError, NotFoundError, ValidationError | delete |
| G1-076 | `cli/tests/test_consoles_restore.py::test_restore_session_errors_when_console_missing::NotFoundError::f78595,test_restore_session_errors_when_session_not_member::NotFoundError::b24eed,test_restore_session_errors_when_tmux_not_running::StateError::f48f2b,test_restore_session_strict_on_untagged_pane::StateError::ddb535,test_restore_session_strict_on_out_of_range_tag::StateError::ddaf92,test_restore_session_strict_on_duplicate_tags::StateError::3d949f,test_restore_session_strict_message_when_configured_zero::StateError::973299,test_restore_session_rebuild_raises_when_new_window_fails::ExternalError::d24675,test_restore_session_rebuild_refuses_when_session_row_gone::StateError::9f5d13,test_restore_session_refuses_when_session_pane_killed_leaves_duplicate::StateError::50fcd9,test_restore_session_refuses_killed_session_pane_under_pane_base_index_one::StateError::881ffb,test_restore_session_raises_when_split_returns_no_pane_id::ExternalError::e04143` | 12 `match=` site(s) over ExternalError, NotFoundError, StateError | delete |
| G1-077 | `cli/tests/test_database_backup.py::test_restore_rejects_generic_sqlite_before_destination_open::StateError::052c20,test_restore_rejects_current_common_sentinel_lookalike::StateError::e8cb9b,test_restore_rejects_v1_with_first_committed_v2_ddl_before_destination_open::StateError::4f8486,test_restore_rejects_v3_with_first_committed_v4_table_before_destination_open::StateError::e08628,test_restore_rejects_missing_column_before_destination_open::StateError::93d619,test_restore_refuses_future_version_before_destination_open::StateError::a57e10,test_restore_rejects_identical_paths::ValidationError::5bf356,test_automatic_backup_refuses_a_version_that_does_not_match_its_source::StateError::aebe96,test_missing_or_malformed_backup_source_creates_nothing::StateError::60ec9b,test_restore_held_destination_lock_honors_fixed_deadline::BackupError::952480` | 10 `match=` site(s) over BackupError, StateError, ValidationError | delete |
| G1-078 | `cli/tests/test_database_migration_safety.py::test_stale_inspector_waits_then_refuses_changed_state::StateError::fa2a54,test_prepare_refuses_changed_stale_state_when_lock_released_before_first_acquire::StateError::fa2a54,test_prepare_refuses_identical_tainted_baseline_after_lock_released_before_first_acquire::StateError::4f8486,test_safe_open_refuses_changed_but_still_stale_state::StateError::b4799c,test_partial_migration_failure_reports_exact_backup_recovery::StateError::4f19d8,test_partial_migration_failure_without_backup_says_so::StateError::4f19d8,test_completion_probe_unavailable_state_fails_before_database_caller::StateError::5240c0,test_database_refuses_future_schema_without_advancing_it::StateError::a57e10,test_get_db_invalid_focused_config_stops_before_migration::ConfigError::37643a` | 9 `match=` site(s) over ConfigError, StateError | delete |
| G1-079 | `cli/tests/test_db.py::test_vm_retained_state_updates_rollback_together::RuntimeError::da2548,test_ordinary_crud_updates_join_explicit_transaction::RuntimeError::da2548,test_update_session_runtime_joins_explicit_transaction::RuntimeError::da2548` | 3 `match=` site(s) over RuntimeError | delete |
| G1-080 | `cli/tests/test_db_migration_vm_sites.py::test_unknown_platform_fails_loudly_and_retry_works_after_fix::IntegrityError::508dc2,test_remote_lima_site_name_collision_fails_loudly::IntegrityError::dcfd35,test_earlier_versions_checkpoint_when_a_later_one_fails::IntegrityError::508dc2` | 3 `match=` site(s) over IntegrityError | delete |
| G1-081 | `cli/tests/test_declared_resource.py::test_something_that_is_neither_is_a_loud_failure::StateError::6faf1e` | **[1-raise]** 1 `match=` site(s) over StateError | delete |
| G1-082 | `cli/tests/test_doctor_env_and_secrets.py::test_mapping_to_undeclared_kind_hard_errors_at_build::ConfigError::9da0f5,test_mapping_to_multiple_undeclared_kinds_hard_errors_at_build::ConfigError::62f437` | 2 `match=` site(s) over ConfigError | delete |
| G1-083 | `cli/tests/test_doctor_schema.py::test_schema_check_distinguishes_absent_table_from_malformed_shape::StateError::7dd5c5,test_schema_check_rejects_a_non_table_schema_entry::StateError::7dd5c5` | 2 `match=` site(s) over StateError | delete |
| G1-084 | `cli/tests/test_env_compose.py::test_compose_env_raises_loudly_on_uncovered_secret::RuntimeError::0b7a46` | **[1-raise]** 1 `match=` site(s) over RuntimeError | delete |
| G1-085 | `cli/tests/test_env_entry.py::test_neither_value_nor_secret_raises::ValueError::1229e2,test_both_value_and_secret_raises::ValueError::9f7534` | 2 `match=` site(s) over ValueError | delete |
| G1-086 | `cli/tests/test_env_merge.py::test_admin_and_agent_both_set_raises::ValueError::011b66` | **[1-raise]** 1 `match=` site(s) over ValueError | delete |
| G1-087 | `cli/tests/test_env_show.py::test_no_flags_raises_validation_error::ValidationError::ac308c` | 1 `match=` site(s) over ValidationError | delete |
| G1-088 | `cli/tests/test_initializer_mise.py::test_mise_validation_rejects_control_characters::ValueError::72b864` | 1 `match=` site(s) over ValueError | delete |
| G1-089 | `cli/tests/test_install_commands.py::test_legacy_test_field_rejected::ConfigError::16eca7,test_system_command_requires_command::ConfigError::412326` | 2 `match=` site(s) over ConfigError | delete |
| G1-090 | `cli/tests/test_instance_specs.py::test_owner_and_overlay_insert_rollback_together::RuntimeError::da2548,test_agent_template_and_overlay_replacement_rollback_together::RuntimeError::da2548` | 2 `match=` site(s) over RuntimeError | delete |
| G1-091 | `cli/tests/test_name_filter_validation.py::test_session_list_rejects_unknown_vm::NotFoundError::308f1a,test_session_list_rejects_unknown_workspace::NotFoundError::58dd61,test_session_list_rejects_unknown_agent::NotFoundError::84b334,test_session_list_csv_with_one_bad_element_rejects::NotFoundError::308f1a,test_stop_all_sessions_rejects_unknown_vm::NotFoundError::308f1a,test_start_all_sessions_rejects_unknown_vm::NotFoundError::308f1a,test_agent_list_rejects_unknown_vm::NotFoundError::308f1a,test_workspace_list_rejects_unknown_vm::NotFoundError::308f1a,test_console_list_rejects_unknown_vm::NotFoundError::308f1a` | 9 `match=` site(s) over NotFoundError | delete |
| G1-092 | `cli/tests/test_operation_scope.py::test_vm_scope_requires_its_vm::StateError::dea803,test_admin_is_session_vocabulary_only::StateError::8c6976,test_error_names_every_violation_at_once::StateError::ef410c,test_workspace_scope_forbids_admin::StateError::8c6976,test_agent_scope_forbids_admin::StateError::8c6976,test_session_scope_requires_exactly_one_launch_identity::StateError::410516` | 6 `match=` site(s) over StateError | delete |
| G1-093 | `cli/tests/test_resource_edit.py::test_builtin_capability_has_no_file_to_edit::ValidationError::5c73a5,test_builtin_declarable_resource_points_at_sample::ValidationError::5c73a5,test_auto_declared_resource_has_no_file_to_edit::ValidationError::6a7add` | 3 `match=` site(s) over ValidationError | delete |
| G1-094 | `cli/tests/test_resource_list.py::test_unknown_kind_filter_raises_unknown_kind_error::NotFoundError::189de4` | **[1-raise]** 1 `match=` site(s) over NotFoundError | delete |
| G1-095 | `cli/tests/test_run_context.py::test_secret_without_a_reader_is_a_typed_error::ConfigError::880d18` | 1 `match=` site(s) over ConfigError | delete |
| G1-096 | `cli/tests/test_sample_config_git_credentials.py::test_empty_secret_name_rejected::ConfigError::8e4a7b` | 1 `match=` site(s) over ConfigError | delete |
| G1-097 | `cli/tests/test_secret_sources_drive_harness.py::test_child_environment_drops_inherited_home_credentials_and_import_paths::RuntimeError::954fbe` | 1 `match=` site(s) over RuntimeError | delete |
| G1-098 | `cli/tests/test_secrets_eager_resolve_sessions.py::test_session_restart_broken_no_force_bails_before_eager_resolve::BrokenStateError::f52679` | 1 `match=` site(s) over BrokenStateError | delete |
| G1-099 | `cli/tests/test_secrets_orchestration.py::test_unknown_reference_raises_instead_of_dropping::StateError::82e531,test_admin_and_agent_in_same_target_raises::ValueError::d9c27d` | 2 `match=` site(s) over StateError, ValueError | delete |
| G1-100 | `cli/tests/test_secrets_resolver.py::test_get_before_resolve_raises::StateError::52c863,test_get_unregistered_name_raises::StateError::88c7aa,test_late_registration_then_resolve_raises::StateError::89be6e` | 3 `match=` site(s) over StateError | delete |
| G1-101 | `cli/tests/test_session_create_ephemeral_prompt_filtering.py::test_workspace_prompt_filters_by_vm_flag::RuntimeError::391447,test_workspace_prompt_filters_by_existing_agent_vm::RuntimeError::391447,test_mode_prompt_filters_by_resolved_vm_with_info_line::RuntimeError::391447,test_vm_and_existing_agent_mismatch_fails_before_workspace_prompt::ValidationError::342797,test_mode_prompt_picks_existing_agent_pins_vm_no_vm_prompt::RuntimeError::391447,test_mode_prompt_picks_admin_then_vm_prompt_fires::RuntimeError::391447` | 6 `match=` site(s) over RuntimeError, ValidationError | delete |
| G1-102 | `cli/tests/test_session_create_ephemeral_prompts.py::test_mode_required_in_non_interactive::ValidationError::09b0a2,test_workspace_required_in_non_interactive::ValidationError::62b672,test_workspace_prompt_picks_existing::RuntimeError::391447,test_mode_prompt_picks_admin::RuntimeError::391447,test_mode_prompt_picks_existing_agent::RuntimeError::391447` | 5 `match=` site(s) over RuntimeError, ValidationError | delete |
| G1-103 | `cli/tests/test_session_create_ephemeral_rollback.py::test_validation_failure_does_not_trigger_rollback::ValidationError::342797` | 1 `match=` site(s) over ValidationError | delete |
| G1-104 | `cli/tests/test_session_create_ephemeral_vm_anchor.py::test_cross_vm_existing_workspace_and_agent_fails_upfront::ValidationError::342797,test_explicit_vm_disagreeing_with_workspace_fails_upfront::ValidationError::342797,test_no_vm_anchor_with_multiple_vms_raises_in_non_interactive::ValidationError::b8d7d0,test_no_vm_anchor_with_zero_vms_raises::NotFoundError::87b24e,test_workspace_and_new_workspace_mutex::ValidationError::d0944f,test_workspace_template_requires_new_workspace::ValidationError::54f54b,test_admin_and_agent_mutex::ValidationError::a07a37,test_agent_template_requires_new_agent::ValidationError::a98c4c,test_ephemeral_workspace_name_collision_raises::AlreadyExistsError::229a18` | 9 `match=` site(s) over AlreadyExistsError, NotFoundError, ValidationError | delete |
| G1-105 | `cli/tests/test_session_transport.py::test_create_session_aborts_on_missing_required_command::StateError::4a8f54` | 1 `match=` site(s) over StateError | delete |
| G1-106 | `cli/tests/test_sessions_tmux_create.py::test_probe_transport_failure_raises_could_not_verify_without_kill::StateError::6ddd56,test_dead_pane_no_main_output_falls_back_to_alternate_screen::StateError::9fb10c,test_dead_pane_with_no_output_at_all_says_so::StateError::4c1706` | 3 `match=` site(s) over StateError | delete |
| G1-107 | `cli/tests/test_shell_integration.py::test_validation_rejects_unknown_field::ConfigError::07875e,test_validation_rejects_deprecated_runtime_field::ConfigError::971740,test_validation_rejects_non_string_command::ConfigError::734417,test_validation_rejects_non_string_required_commands::ConfigError::ecfca8,test_construct_revalidates_config::ConfigError::0aeeb1,test_merge_never_launders_an_invalid_required_commands_entry::ConfigError::5b23b8,test_missing_command_is_a_typed_error_naming_the_vm::StateError::28899f,test_agent_mode_missing_command_names_the_agent::StateError::2dc28d,test_scope_less_context_is_a_loud_error::StateError::2685b8,test_agent_mode_absent_target_is_a_loud_error::StateError::000689,test_missing_transport_defers_at_preflight_and_is_loud_at_runup::StateError::84d1fe,test_identity_guard_raises_on_vm_mismatch::StateError::f9c333,test_identity_guard_raises_on_agent_mismatch::StateError::445105,test_identity_guard_raises_on_mode_mismatch::StateError::8c6976` | 14 `match=` site(s) over ConfigError, StateError | delete |
| G1-108 | `cli/tests/test_templates.py::test_unknown_template::NotFoundError::4a43d4` | 1 `match=` site(s) over NotFoundError | delete |
| G1-109 | `cli/tests/test_topics.py::test_empty_prose_is_refused_rather_than_rendered_blank::StateError::2a6f8f` | 1 `match=` site(s) over StateError | delete |
| G1-110 | `cli/tests/test_traversal.py::test_a_hook_that_raises_refuses_the_input::ValueError::47b52f` | 1 `match=` site(s) over ValueError | delete |
| G1-111 | `cli/tests/test_vm_release.py::test_confirm_release_rolls_back_observation_when_pending_write_fails::RuntimeError::e1bdda` | 1 `match=` site(s) over RuntimeError | delete |
| G1-112 | `cli/tests/test_workspace_rooted_shells.py::test_shell_vm_workspace_unknown_raises_not_found::NotFoundError::ca3704,test_shell_vm_workspace_cross_vm_raises_validation::ValidationError::ff94b2,test_exec_vm_workspace_cross_vm_raises_validation::ValidationError::ff94b2,test_exec_agent_workspace_cross_vm_raises_validation::ValidationError::ff94b2,test_exec_agent_workspace_missing_grant_raises_authz::AuthorizationError::40fe55,test_exec_vm_rejects_dash_prefixed_command::ValidationError::05d8e1,test_exec_agent_rejects_dash_prefixed_command::ValidationError::05d8e1` | 7 `match=` site(s) over AuthorizationError, NotFoundError, ValidationError | delete |
| G1-113 | `cli/tests/test_wsl2_create_interrupt.py::test_pre_mutation_failure_makes_no_cleanup_calls::StateError::7a14f6` | 1 `match=` site(s) over StateError | delete |
| G1-114 | `cli/tests/test_wsl2_keepalive.py::test_vm_active_fast_fails_if_keepalive_subprocess_dies_immediately::RuntimeError::d2c99f,test_vm_active_closes_job_handle_on_fast_fail::RuntimeError::d2c99f` | 2 `match=` site(s) over RuntimeError | delete |
| G1-115 | `cli/tests/test_wsl2_paths.py::test_local_app_data_raises_when_missing::RuntimeError::d43a7c` | **[1-raise]** 1 `match=` site(s) over RuntimeError | delete |
| G1-116 | `cli/tests/transports/test_factories.py::test_transport_for_user_raises_state_error_without_tailscale_host::StateError::24e277,test_native_transport_empty_ssh_host_raises_typed_state_error::StateError::75d039` | 2 `match=` site(s) over StateError | delete |
| G1-117 | `cli/tests/transports/test_lima.py::test_run_check_true_raises_on_nonzero::SSHError::5da585,test_copy_to_raises_on_failure::SSHError::a107de` | 2 `match=` site(s) over SSHError | delete |
| G1-118 | `cli/tests/transports/test_ssh.py::test_run_check_true_raises_on_nonzero::SSHError::8aed2b,test_copy_to_raises_on_scp_failure::SSHError::19768f` | 2 `match=` site(s) over SSHError | delete |
| G1-119 | `cli/tests/transports/test_wsl2.py::test_run_check_true_raises_on_nonzero::SSHError::569c1e,test_copy_to_raises_on_failure::SSHError::f59051` | 2 `match=` site(s) over SSHError | delete |
| G1-120 | `cli/tests/vms/test_backup_vm.py::test_missing_tailscale_fails_before_the_boundary::StateError::d60755` | 1 `match=` site(s) over StateError | delete |
| G1-121 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_helper_redacts_every_non_redacting_progress_surface::SSHError::151d5f` | 1 `match=` site(s) over SSHError | delete |
| G1-122 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_reinit_refuses_an_operator_stopped_vm_at_the_gate::StateError::836f41` | 1 `match=` site(s) over StateError | delete |
| G1-123 | `cli/tests/vms/test_create_vm_dispatch.py::test_not_ready_site_errors_before_tailscale_and_slug_prompt::StateError::18aaaa` | 1 `match=` site(s) over StateError | delete |
| G1-124 | `cli/tests/vms/test_ensure_tailscale_wording.py::test_rejoin_rejects_a_redaction_free_operation_logger::StateError::70df2a` | 1 `match=` site(s) over StateError | delete |
| G1-125 | `cli/tests/vms/test_legacy_site_sections.py::test_defaults_platform_is_rejected::ConfigError::c3de67,test_defaults_vm_host_is_a_hard_error::ConfigError::c8dde5` | 2 `match=` site(s) over ConfigError | delete |
| G1-126 | `cli/tests/vms/test_lima_remote_create_interrupt.py::test_pre_mutation_failure_makes_no_cleanup_calls::StateError::321425` | 1 `match=` site(s) over StateError | delete |
| G1-127 | `cli/tests/vms/test_platform_config_contract.py::test_wsl2_accepts_no_configuration::ConfigError::13bc20,test_aws_ec2_optional_subnet_id_is_shape_checked::ConfigError::aa0236,test_aws_ec2_rejects_the_removed_ami_override::ConfigError::628c06,test_aws_ec2_rejects_bad_instance_type_arch::ConfigError::03a257,test_a_catalog_entry_is_addressed_by_its_index::ConfigError::638095,test_a_catalog_must_be_a_list_not_a_tuple_shaped_thing::ConfigError::2c8290,test_proxmox_does_not_accept_a_quoted_template_map_vmid::ConfigError::631f00` | 7 `match=` site(s) over ConfigError | delete |
| G1-128 | `cli/tests/vms/test_platform_runup.py::test_proxmox_runup_rejection_is_fatal::TokenRejectedError::601819,test_proxmox_runup_without_secrets_is_error::ConfigError::880d18,test_azure_runup_without_the_client_secret_is_typed::ConfigError::880d18,test_aws_ec2_runup_auth_rejection_is_fatal::TokenRejectedError::c08b29,test_aws_ec2_runup_without_the_secret_is_typed::ConfigError::880d18` | 5 `match=` site(s) over ConfigError, TokenRejectedError | delete |
| G1-129 | `cli/tests/vms/test_platform_support.py::test_declared_site_on_unsupported_platform_registers_not_ready::StateError::18aaaa,test_unknown_platform_site_is_a_hard_error::ConfigError::657953,test_bundled_site_names_are_reserved_on_every_host::ConfigError::c0d03c,test_defaults_site_naming_a_disabled_site_is_valid_config::StateError::90cec4,test_select_site_errors_with_reasons_when_none_ready::ValidationError::3b09ce` | 5 `match=` site(s) over ConfigError, StateError, ValidationError | delete |
| G1-130 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_rekey_running_check_runs_after_the_resolve_boundary::StateError::68e45c` | 1 `match=` site(s) over StateError | delete |
| G1-131 | `cli/tests/vms/test_shell_exec_orchestrated.py::test_exec_dash_prefixed_command_fails_with_zero_resolves_and_zero_gate::ValidationError::05d8e1,test_exec_missing_command_after_double_dash_fails_pre_gate::ValidationError::1cfdd7,test_cross_vm_workspace_mismatch_fails_with_zero_resolves_and_zero_gate::ValidationError::1ebb9d,test_shell_no_tailscale_fails_before_any_resolve::StateError::ab1f5e` | 4 `match=` site(s) over StateError, ValidationError | delete |
| G1-132 | `cli/tests/vms/test_sites_dispatch.py::test_resolve_site_unknown_raises_the_stranded_error::ConfigError::2f3f80,test_ops_read_the_token_through_the_context::ConfigError::2d0517,test_select_site_errors_between_several_when_non_interactive::ValidationError::ce6d45,test_select_site_errors_when_none_declared::ValidationError::3b09ce,test_resolving_a_not_ready_site_names_the_requirement::StateError::18aaaa` | 5 `match=` site(s) over ConfigError, StateError, ValidationError | delete |
| G1-133 | `cli/tests/vms/test_system_slug.py::test_validate_slug_bounds::ValidationError::b88487` | 1 `match=` site(s) over ValidationError | delete |
| G1-134 | `cli/tests/vms/test_tailscale_stdin_join.py::test_readiness_exhaustion_raises_typed_without_delivering_key::ProvisioningError::3cd0dc,test_cloud_init_wait_failure_raises_typed_without_delivering_key::ProvisioningError::71cfc9,test_gce_readiness_timeout_is_typed_labeled_and_stops_before_stdin::ProvisioningError::c88dd7` | 3 `match=` site(s) over ProvisioningError | delete |
| G1-135 | `cli/tests/vms/test_verify_connection.py::test_verify_connection_missing_vm::NotFoundError::4e84aa` | **[1-raise]** 1 `match=` site(s) over NotFoundError | delete |
| G1-136 | `cli/tests/vms/test_vm_platform_kind.py::test_vm_platform_is_not_manifest_declarable::ConfigError::8ef97a` | 1 `match=` site(s) over ConfigError | delete |
| G1-137 | `cli/tests/vms/test_vm_site_kind.py::test_platform_named_site_must_declare_that_platform::ConfigError::262be5,test_decode_requires_platform::ConfigError::b139a2,test_a_kind_owned_key_inside_the_block_is_the_platforms_to_refuse::ConfigError::55786f,test_decode_rejects_stray_spec_keys::ConfigError::8f6c72,test_build_registry_validates_the_blob_via_the_capability::ConfigError::56c8a8,test_unknown_platform_site_hard_errors_at_finalize::ConfigError::a848db,test_bundled_sites_are_reserved::ConfigError::a940e6` | 7 `match=` site(s) over ConfigError | delete |
| G1-138 | `cli/tests/workspaces/test_create_orchestrated.py::test_create_bad_template_bails_before_any_prompt_or_start::NotFoundError::ca3704,test_create_mutation_failure_cleans_up_and_leaves_no_row::ExternalError::666da0` | 2 `match=` site(s) over ExternalError, NotFoundError | delete |
| G1-139 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_create_rollback_removes_the_fresh_workspace_group::ExternalError::f5ed6a,test_create_rollback_tolerates_a_groupdel_failure::ExternalError::f5ed6a,test_create_rollback_on_clone_failure_leaves_no_residue::SSHError::73777c,test_delete_sessions_guard_refuses_with_zero_resolves_and_zero_gate::StateError::54d6d7,test_delete_declined_confirm_aborts_with_zero_resolves_and_zero_gate::UserAbort::0cf0fb,test_delete_nested_rejects_a_mismatched_vm_node::StateError::207681,test_rehome_confirm_sits_inside_the_span_after_the_dir_checks::UserAbort::9a39bc,test_copy_refusals_fail_with_zero_resolves_and_zero_gate::NotFoundError::29ecb8,test_copy_refusals_fail_with_zero_resolves_and_zero_gate::AlreadyExistsError::a9ad3d` | 9 `match=` site(s) over AlreadyExistsError, ExternalError, NotFoundError, SSHError, StateError, UserAbort | delete |

## Group 3: report lines and hints

This is the second largest convert population in the map, behind group 4.

**This group lands as four PRs, not one** (effort lead, 2026-08-16): it is too much for one review
round and it is the judgment-heavy batch, so the cut is by subsystem INSIDE the single shape. That
keeps the kind of judgment uniform within a round while making each round readable. Rows carry their
file path, which is what assigns them.

**A sub-batch is a scope, not a tally.** The table below carried per-disposition counts once and
they never reconciled with the group, so they are gone. Membership follows from each row's file
path: apply the scope to the rows and the four sub-batches partition the group by construction,
which is the property that matters and the one a count column kept breaking.

| Sub-batch | Scope                                                                                                                                                                                                                                                                                           |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3a        | Consoles, sessions and secrets: `cli/tests/sessions/`, `cli/tests/secrets/`, `cli/tests/transports/`, `orchestration/test_secrets.py`, and the `test_consoles_*`, `test_console_*`, `test_session*`, `test_secret*`, `test_tmux*`, `test_claude_code_*`, `test_codex_*` and `test_grok_*` roots |
| 3b        | VMs and platforms: `cli/tests/vms/`, `cli/tests/plugins/`, and the `test_aws_*`, `test_azure_*`, `test_wsl2_*`, `test_vm_*`, `test_lima*`, `test_proxmox*`, bootstrap and cloud-init roots                                                                                                      |
| 3c        | Workspaces and agents: `cli/tests/workspaces/`, `cli/tests/agents/`, `test_agents.py`                                                                                                                                                                                                           |
| 3d        | Everything else in the group: database, doctor, git credentials and the core CLI surfaces                                                                                                                                                                                                       |

3c is small enough to ride with 3a as one round if three PRs suits the review cadence better; the
other three are each a full round on their own.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-065 | `cli/tests/test_database_backup.py::test_restore_rejects_v1_with_first_committed_v2_ddl_before_destination_open@eab501,test_restore_rejects_v3_with_first_committed_v4_table_before_destination_open@4b4079,test_restore_rejects_missing_column_before_destination_open@cfef56` | verbatim hint equality (three sites) | delete | The same authored remediation sentence pinned by equality in three qualification tests. The behavioral content of each test is the raise plus `not live.exists()`, both of which stay; `hint is not None` is the surviving presence form used elsewhere (for example `test_database_migration_safety.py::test_safe_open_migrates_stale_state_and_preserves_historical_backup`). |
| A-067 | `cli/tests/test_database_cli.py::test_database_backup_stdout_is_only_the_completed_path@55619c` | verbatim stderr equality | delete | Pins an authored progress sentence. The stream invariant is `assert result.stdout == f"{path}\n"` beside it (stdout is exactly the path), which stays. |
| A-068 | `cli/tests/test_database_cli.py::test_database_restore_yes_uses_stderr_and_creates_no_implicit_backup@f27571` | labelled path substrings | convert | The substance is that the restore names both files it touched; the labels are authored. Replacement: assert `str(selected)` and `str(live)` appear in stderr and drop the `Backup:`/`Live database:` prefixes. No production change. |
| A-069 | `cli/tests/test_database_cli.py::test_database_restore_yes_uses_stderr_and_creates_no_implicit_backup@f27571` | verbatim completion sentence | delete | Authored sentence; the exit code (`assert result.exit_code == 0, result.output`), the restored value (`assert _value(live) == "selected"`) and the unchanged backup set (`assert {path.name for path in backup_directory(live).glob("*.db")} == before`) carry the behavior. |
| A-070 | `cli/tests/test_database_cli.py::test_database_restore_decline_prompts_on_stderr_and_changes_nothing@84f792` | prompt wording pin | delete | **Recipe verification, 2026-08-16.** **The recorded conversion is vacuous.** The added `result.stderr != ""` cannot bite, because `commands/database.py::database_restore` echoes `Backup:` and `Live database:` to stderr unconditionally, so silencing the prompt entirely left the test green. The half that does bite is the PRE-EXISTING `assert result.stdout == ""`, which fails if the prompt is routed to stdout. Delete the wording pin (`assert "Replace the live state database with this backup? [y/N]" in result.stderr`) and add nothing; that `stdout` assert carries the stream discipline and the two `_value(...)` asserts below it carry the changes-nothing half the test's name promises. |
| A-071 | `cli/tests/test_database_cli.py::test_database_restore_non_interactive_without_yes_refuses_cleanly@dd239e` | refusal wording substrings | delete | Two authored substrings. `assert exit_info.value.code == 1`, `assert captured.out == ""` and `assert not live.exists()` already prove the clean non-interactive refusal. |
| A-072 | `cli/tests/test_database_cli.py::test_interactive_migration_notice_and_prompt_keep_stdout_machine_pure@177cf7` | **[unverified]** migration notice and prompt wording | delete | **Corrected 2026-08-19 (map re-baseline).** Was delete over both of the test's stderr pins. The version pin keeps: `f"version {LATEST_VERSION - 1} to {LATEST_VERSION}"` interpolates the production constant `LATEST_VERSION`, imported at the module level from `agentworks.db.migrations`, so it proves the migration notice reports the version pair actually in force and fails when the notice drifts from the migration table, not only on a rewording. Same shape as G1-K16. Drop `assert "Back up the state database before migrating? [Y/n]" in result.stderr` alone, the prompt text, whose behavioral twin is `test_database_migration_safety.py::test_completion_open_sees_uncheckpointed_wal_content_that_immutable_mode_would_miss` (the confirm response actually drives whether a backup is created). Stdout purity, the point of this test, is the pair above them in the same test: `assert json.loads(result.stdout)["data"] == {"vms": []}` on the machine-output arm and `assert result.stdout == ""` on the other. |
| A-073 | `cli/tests/test_database_cli.py::test_interactive_migration_notice_and_prompt_keep_stdout_machine_pure@177cf7` | backup-completion sentence | convert | Replacement: assert a backup file now exists in `backup_directory(live)` instead of asserting the sentence that says so. That is the fact the operator cares about and the test currently never checks. No production change. |
| A-074 | `cli/tests/test_db_migration_vm_sites.py::test_prints_site_manifest_snippets@6fe09d` | stream discipline and snippet dedup count | keep | Migrations run on every `Database()` open including under stdout-capturing completion helpers, so `captured_output.info == []` is a real channel invariant; the `count(...) == 1` is the dedup invariant (one snippet per distinct host-named site), not a wording pin. |
| A-075 | `cli/tests/test_db_migration_vm_sites.py::test_prints_site_manifest_snippets@6fe09d` | emitted YAML snippet pins | convert | The substance is that the printed snippet carries the ssh target derived from the legacy row the test seeded. Replacement: assert `me@gpu-box` and `me@wsl2-host` appear in the emitted output and drop the exact `placement: { mode: ssh, host: ... }` spelling. No production change. |
| A-076 | `cli/tests/test_db_migration_vm_sites.py::test_prints_site_manifest_snippets@6fe09d` | verbatim warning sentence | delete | Pins the shadow-name warning wording. The behavior it narrates is asserted structurally by its sibling: `test_backfill_and_site_rename` requires `by_name["svm"].site == "wsl2-host"`, which fails if the shadow suffix regresses. |
| A-077 | `cli/tests/test_db_migration_harness_integration_state.py::test_v31_rejects_an_unexpected_state_column_set@d8082c` | exception-message substrings | delete | Asserts our own migration's `IntegrityError` text names both columns. The invariant is the raise itself (`with pytest.raises(sqlite3.IntegrityError) as exc_info`), which stays; the wording adds nothing a reviewer would not see in the diff. |
| A-078 | `cli/tests/test_db_migration_harness_integration_state.py::test_malformed_blob_degrades_to_empty_with_a_warning@717fd0` | warning substring with authored fragment | convert | The `"bad"` half is value identity (the row the test corrupted); `detail_fragment` is our authored phrasing, parametrized. Replacement: keep `any("bad" in message for message in captured_output.warnings)` so degradation still cannot go silent, and drop the fragment from the parametrization. No production change. |
| A-081 | `cli/tests/test_database_migration_safety.py::test_busy_state_error_message_and_hint_are_not_caller_suppliable@cc8461` | constructibility, hint presence, entity kind | keep | Behavioral: `BusyStateError()` constructs with no arguments, always carries a hint, and reports `entity_kind == "database"`, which is the structured field callers and doctor branch on. |
| A-082 | `cli/tests/test_database_migration_safety.py::test_prepare_refuses_changed_stale_state_when_lock_released_before_first_acquire@b36fa9` | verbatim hint equality | delete | Authored remediation sentence pinned by equality; the raise (`with pytest.raises(StateError, match="changed before its stale state could be qualified") as raised:`), the unchanged version (`assert _version(path) == LATEST_VERSION - 1`) and the absent backup directory (`assert not backup_directory(path).exists()`) carry the behavior. |
| A-083 | `cli/tests/test_database_migration_safety.py::test_prepare_refuses_identical_tainted_baseline_after_lock_released_before_first_acquire@558623` | verbatim hint equality | delete | Same shape as A-082 for the tainted-baseline path; the two pairs bracketing the hint in the same test hold the observable content: `assert len(observed_tokens) == 2` with `assert observed_tokens[0] == observed_tokens[1]` above it, and `assert _version(path) == 1` with `assert not backup_directory(path).exists()` below. |
| A-084 | `cli/tests/test_database_migration_safety.py::test_partial_migration_failure_reports_exact_backup_recovery@b34127` | hint equality around a derived command | convert | The load-bearing part is that the hint carries the exact restore command for the backup that actually exists, which is real operator recovery, not prose. Replacement: `render_restore_command(backups[0]) in raised.value.hint`, dropping the authored prefix. No production change. |
| A-085 | `cli/tests/test_database_migration_safety.py::test_partial_migration_failure_without_backup_says_so@8c263a` | authored sentence substring | delete | `assert raised.value.hint is not None` and `assert not backup_directory(path).exists()` already state the fact; the sentence is ours to review. |
| A-086 | `cli/tests/test_database_migration_safety.py::test_base_exception_migration_failure_preserves_identity_and_visible_recovery@8ff1dd` | notice equality with a hardcoded recovery sentence | convert | The invariant worth keeping is that recovery instructions still reach the operator on the notice channel even when the interrupt propagates. Replacement: assert `captured_output.notices` is non-empty and, in the backup arm, that `render_restore_command(backups[0])` appears in the last notice; drop the authored sentence in the no-backup arm. No production change. |
| A-087 | `cli/tests/test_database_migration_safety.py::test_recovery_command_rendering_quotes_posix_and_powershell@38ba03` | rendered-command quoting equality | keep | Shell quoting of a path containing a space and an apostrophe, per platform. The escaping is the invariant (a regression yields a command that breaks or misdirects when pasted), and the inputs are test-injected. |
| A-088 | `cli/tests/test_database_migration_safety.py::test_get_db_interactive_default_yes_selection_and_notice@f5b3a1` | migration-notice wording substring | keep | Rubric re-check, 2026-08-16. The only probe that the migration notice reports the ACTUAL version span: `cli/agentworks/cli/_helpers.py::get_db` interpolates `plan.inspection.current_version` and `.latest_version`, while its siblings in the same test say less: `assert _version(path) == LATEST_VERSION` proves only that the migration ran, and `assert len(captured_output.notices) == (2 if answer else 1)` only counts notices. A-072 stands because this one is kept. Cost: breaks if the notice is reworded. |
| A-089 | `cli/tests/test_database_migration_safety.py::test_get_db_interactive_default_yes_selection_and_notice@f5b3a1` | notice count | keep | Channel discipline: exactly one notice when the operator declines the backup and two when they accept, so a stray or missing notice is caught without asserting wording. |
| A-090 | `cli/tests/test_database_migration_safety.py::test_get_db_interactive_default_yes_selection_and_notice@f5b3a1` | notice equality around a derived path | convert | Replacement: `format_host_path(backups[0]) in captured_output.notices[-1]`, keeping the path identity (the operator must be told where the backup landed) and dropping the authored prefix. No production change. |
| A-091 | `cli/tests/test_database_migration_safety.py::test_get_db_selected_backup_failure_prevents_migration_and_guides_retry@af1c63` | branch-discriminating hint substrings | delete | The interactive and non-interactive arms are distinguished only by our own hint wording, which is the case operator ruling 10 and R2.4 resolve by deleting the assertion and accepting the reduced branch coverage. `assert raised.value.hint is not None` and the unchanged schema version (`assert _version(path) == LATEST_VERSION - 1`) stay. The `auto_backup_before_migration = false` fragment is a config-key mention, not a behavioral handle. |
| A-092 | `cli/tests/test_database_migration_safety.py::test_notice_survives_machine_presentation_suppression@ab5972` | injected notice identity | keep | The strings are the test's own; the invariant is that `suppress_presentation()` hides info and lets notices through. |
| B-001 | `cli/tests/sessions/test_console_attach_orchestrated.py::test_named_console_attach_holds_across_the_interactive_attach@a01d87` | report-line substring | delete | The test's subject is the hold span, carried structurally by `events == ["hold-open", "interactive", "hold-close"]` in the same test. The announce line adds nothing that can regress independently. |
| B-002 | `cli/tests/sessions/test_console_attach_orchestrated.py::test_restore_session_stopped_vm_drives_the_real_gated_composition@5ad02b` | report-line substring | delete | The gated-composition claim is carried by the rest of the same test (`events`, `resolve_counter`, three tmux commands). The "already matches config" line is the report only. |
| B-007 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_offered_and_deleted_interactive@a253fc,test_now_empty_agent_offered_and_deleted_interactive@6c6f9f,test_agent_becomes_candidate_once_grant_removed@a3ac1c,test_delete_session_interactive_offers_now_empty_workspace@4ef8c0` | interactive-offer wording substring | convert | The real invariant is that the operator was ASKED rather than silently acted upon; `calls`/`db.get_*` siblings cannot see a prompt. Replace with a prompt-count assertion. VERIFIED by executing: `len(prompts) == 1` passes on this row's first three anchors, while the fourth, `test_delete_resource_cleanup.py::test_delete_session_interactive_offers_now_empty_workspace`, is the end-to-end flow and issues two prompts, so that site needs `== 2`. **Which anchor each verified site is, is inferred from the row's structure**: the four numbers ran in order against a tree this map no longer matches, and their drift against these four anchors is monotonic. The verification lane executes the mapping rather than trusting it. No production change. |
| B-008 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_created_interactive_offer_notes_provenance@7f3cfd,test_now_empty_agent_created_interactive_offer_notes_provenance@577be3` | provenance-cue wording pin | delete | The whole assertion is the sentence `(created with this session)`. The provenance branch it fronts has no other observable, so R2.4 applies; `calls == ["ws-vm1"]` beside it keeps the behavioral half. |
| B-009 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_interactive_offer_discloses_grants@b7093e,test_now_empty_workspace_interactive_offer_plain_with_only_grant_all_row@4f962c` | disclosure wording pin plus phrase blacklist | delete | The first anchor pins a full authored sentence (`"Workspace 'ws-vm1' now has no sessions (deleting revokes explicit grant(s) held by: bot). Delete it?"`); the second is a phrase-absence blacklist (`assert all("revokes explicit grant" not in p for p in prompts)`), the shape the rule names as worse than useless. The grant-guard behavior itself is pinned structurally in `test_delete_resource_cleanup.py::test_now_empty_workspace_yes_created_kept_when_external_explicit_grant` and `test_delete_resource_cleanup.py::test_now_empty_workspace_yes_created_auto_deletes_with_only_grant_all_row`. |
| B-010 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_yes_not_created_reports_but_keeps@9694a5,test_now_empty_workspace_yes_created_kept_when_external_explicit_grant@e4e9fd,test_now_empty_agent_yes_not_created_reports_but_keeps@7d2ebd,test_delete_session_yes_reports_now_empty_uncreated_workspace@c11fe3` | report-but-keep warning substring triples | delete | Every one of these sits directly under `calls == []` and `db.get_workspace(...) is not None` (or the agent equivalent), which is the entire report-but-keep invariant. The warning text and the suggested `agw ... delete` command are the report. |
| B-011 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_delete_failure_warns@1e58fa,test_now_empty_agent_delete_failure_warns@d60805,test_delete_session_warns_when_now_empty_workspace_delete_raises@621cff` | failure-warning substring | convert | Rubric re-check, 2026-08-16. `cleanup_now_empty_resource`'s `except AgentworksError -> output.warn` branch (`sessions/_resource_cleanup.py::cleanup_now_empty_resource._run_delete`) has no other observable: swallowing silently and swallowing with a warning are indistinguishable in the surviving row assertions. Convert to a warning-count assertion. No production change. B-010 and B-083 stand only because this lands. |
| B-012 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_non_interactive_without_yes_reports_and_never_prompts_or_deletes@fcd0ab` | report-but-keep warning substring | convert | Rubric re-check, 2026-08-16. The mechanism test for the same shared helper; its report-but-keep branch's only effect is the warning, since `confirms == []` and `deletes == []` are identical between reporting and doing nothing. Same conversion. No production change. |
| B-015 | `cli/tests/sessions/test_session_list_harness_integration.py::test_names_only_stays_pure_and_pays_no_registry_cost@99cedf` | exact output equality | keep | `captured_output.info == ["s1"]` is the `--names-only` contract from the `cli-conventions` rule: exactly one line per row, no header, no decoration. The value is the session name the test seeded, so this is value identity plus arity, not wording. |
| B-016 | `cli/tests/sessions/test_session_nodes.py::test_missing_command_is_a_typed_error@fb2f09` | hint substring | keep | `"--template" in exc.value.hint` names a real CLI flag, not prose about it. A rename that broke the hint's advice would be caught; treat it as the option-name reference it is. Same shape as B-062. |
| B-017 | `cli/tests/sessions/test_session_nodes.py::test_admin_mode_error_names_the_vm@1eb7c0` | error-message substring | delete | `"for VM 'box'."` pins the authored trailing clause. The typed error and its `entity_name` already discriminate the branch. |
| B-018 | `cli/tests/sessions/test_session_nodes.py::test_pending_agent_teardown_failure_names_the_artifact@78748a` | **[unverified]** error-message substring pair | delete | `"left standing"` is authored prose; `"ephemeral agent 'dev'"` is the artifact name, already carried by the exception's entity fields in the same test file's neighbors. Nothing structural is lost. |
| B-019 | `cli/tests/sessions/test_session_nodes.py::test_pending_session_teardown_warns_and_never_raises@1796b4` | warning-message substring pair | delete | The invariant (a failed pending-session teardown warns and never raises) is carried by the call returning at all; `"db locked"` is the fixture's own injected error string, which survives as value identity if the row is trimmed rather than dropped. |
| B-020 | `cli/tests/sessions/test_session_nodes.py::test_a_non_dict_namespace_value_degrades_to_empty_with_a_warning@6a8f25` | degraded-value warning substring | delete | The degradation itself (namespace value becomes empty) is the invariant and is asserted structurally beside it; `"got str"` pins our diagnostic's wording. |
| B-022 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_stop_all_reachable_vms_is_one_boundary_burst@1d752b,test_batch_empty_vm_set_is_a_complete_noop@090fb5,test_stop_all_mixed_site_batch_resolves_the_union_once@0c2502` | report-line substring (three sites) | delete | `any("No running sessions to stop" in m for m in captured_output.info)` in three tests. Each sits beside the claim the test is named for: `resolve_counter == [["proxmox-token"]]`, `resolve_counter == []`, and the union assertion `sorted(resolve_counter[0]) == ["proxmox-token", "proxmox-token-b"]`. The report line adds nothing those do not already hold. |
| B-023 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_stop_session_reachable_vm_is_one_boundary_burst@012bc5` | report-line substring | delete | `any("already stopped" in m ...)` beside `resolve_counter == [["proxmox-token"]]`. Same shape as B-022: the burst count is the invariant and the line is the report. |
| B-024 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_describe_session_observes_without_a_vm_hold@606734` | rendered-line verbatim pin | delete | `any("Status:         stopped" in m ...)` pins a rendered line, column whitespace and all. **This row's own argument, demonstrated:** at the previous basis the same assertion read `"Status:     stopped"`, and a change that widened the label column by four spaces broke it while changing no behavior at all. The claim the test is named for is `resolve_counter == []`, that describe observes without opening the VM gate, and it stays. |
| B-025 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_describe_session_shows_harness_integration_line@956434` | label probe, plus a line-order pin | keep | Scoped keep. `"Harness integration: shell" in info` is the only probe that a session's RESOLVED harness integration reaches describe: the golden-bytes sibling in `test_machine_output_cli.py` stubs a description whose harness integration is `None`, so a regression rendering a placeholder for every session passes everywhere else. Cost: the kept assertion breaks if that label is reworded, and R2.2 forbids adding a structural handle to avoid it. The `template_idx < harness_integration_idx < mode_idx` ordering pin below it is not kept: it is the sentence-order shape the doctrine names, and the golden bytes cover it. |
| B-026 | `cli/tests/agents/test_agent_home_permissions.py::test_shared_primary_group_warns@d9ba9b,test_private_primary_group_does_not_warn@efd2f3` | warning-wording substring plus absence pin | keep | Rubric re-check, 2026-08-16. The shared-primary-group post-condition guard changes no command and no state, so the warning IS the behavior. With these gone `test_shared_primary_group_warns` asserts nothing and the private/shared fork is unprobed. Cost: breaks if the warning is reworded. |
| B-027 | `cli/tests/agents/test_agent_home_permissions.py::test_create_absent_user_reports_created_not_creating@51151c,test_reinit_gone_user_reports_created_not_silently_reinitialized@1294ca,test_reinit_converged_user_reports_already_exists_and_skips_usermod@20a459,test_reinit_diverged_shell_reports_fixed_and_runs_usermod@e06cd7` | Created/already exists/Fixed report-line pins with wording blacklists | delete | Four tests whose entire subject is which of three authored report lines fires. The `not any("Creating user" ...)` clauses are wording blacklists. The behavioral half (whether `usermod` ran) is asserted separately in each test on `target.commands`; the OK/Created/Fixed distinction is a sentence-only observable, so R2.4 applies. See summary: this is the same family as B-034. |
| B-028 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary@403446,test_reinit_stopped_vm_gate_resolves_once_and_seeds_the_boundary@69826e` | header-banner and "Checking ..." substring pins | convert | Rubric re-check, 2026-08-16. `assert (Role.HEADER, 0, "Preflight") in captured_output.lines` and `assert (Role.BODY, 1, "Checking agent-template/default...") in captured_output.lines` duplicate only the Preflight header; the Resolving Secrets and Agent Initialization sections (`with output.section("Resolving Secrets"):` and `with output.section("Agent Initialization"):`, both in `agents/manager/lifecycle.py::create_agent`) lose every probe, and reinit keeps no structural header assertion at all. Same `Role.HEADER` conversion as B-003. No production change. |
| B-029 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary@403446,test_reinit_stopped_vm_gate_resolves_once_and_seeds_the_boundary@69826e,test_reinit_reconcile_is_idempotent_across_repeated_reinits@27dd2f,test_reinit_skips_stale_grant_and_reconciles_the_rest@b7b1a0` | structural presence on (role, level, message) | keep | Section/level/result-role shape, the same invariant as B-004: a result emitted as body text, or a check line escaping to level 0, is caught here. |
| B-030 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_reinit_stopped_vm_gate_resolves_once_and_seeds_the_boundary@69826e` | report-line substring plus prefix blacklist | delete | `assert any("reinitialized" in m for m in captured_output.info)` is redundant with the `Role.RESULT` tuple below it. `assert not any(m.startswith("Reconciled ") for m in captured_output.info)` is a phrase blacklist whose real subject, that no grant was reconciled, is a DB fact the test can read directly. |
| B-031 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_reinit_skips_stale_grant_and_reconciles_the_rest@b7b1a0` | warning substring pair | delete | The stale-grant skip is observable in the DB rows the test already checks; `"skipping stale grant"` is the report. |
| B-032 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_reinit_reconciles_grant_all_agent_via_materialized_rows@f0d028` | **[unverified]** count-in-report-line pin | delete | "Reconciled 1 workspace grant" restates the grant row the test seeded and can assert on directly. |
| B-033 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_grant_reachable_vm_is_one_boundary_burst@d9a61b,test_revoke_reachable_vm_is_one_boundary_burst@ed375c,test_delete_choreography_end_to_end_standalone@69dd1f,test_grant_all_sets_flag_and_adds_every_vm_workspace@635a69,test_grant_missing_workspace_warns_and_skips@b99352,test_revoke_named_workspace_with_implicit_access_keeps_membership@fb2fd5,test_revoke_all_warns_about_remaining_implicit_access@641375` | **[unverified]** Granted/Revoked/Deleted report-line pins | delete | Every one of these has a DB-state or call-spy sibling in the same test (grant rows, session rows, the delete spy). The rendered per-item lines and the "(still has implicit access via sessions)" parenthetical are the report. |
| B-034 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_grant_reachable_vm_is_one_boundary_burst@d9a61b,test_revoke_reachable_vm_is_one_boundary_burst@ed375c` | **[unverified]** Role.RESULT tuple equality carrying a full authored sentence | delete | Unlike B-004, the assertion here is `(Role.RESULT, 0, "Agent 'a1' granted access to 1 workspace")`: the role and level are incidental and the sentence is the payload. The role-and-level invariant is already covered by B-029 in the sibling file. |
| B-035 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_delete_socket_dir_runs_even_when_userdel_fails@ea7b46,test_grant_missing_workspace_warns_and_skips@b99352,test_revoke_all_warns_about_remaining_implicit_access@641375` | warning-wording substring | convert | Three tests where "did it warn at all" is the invariant (a failed userdel must not be silent, a missing workspace must not be silently skipped, remaining implicit access must be surfaced) and no sibling sees it. VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes at all three. No production change. |
| B-036 | `cli/tests/agents/test_shell_exec_orchestrated.py::test_exec_dash_prefixed_command_fails_with_zero_resolves_and_zero_gate@ea5444` | hint substring pair | keep | `"put '--' before"` and `"sh -c"` are the literal argv syntax the operator must type, and the test's subject is that a dash-prefixed command fails with a recovery instruction rather than a bare parse error. Trim to the `sh -c` token if the lead wants it narrower. Flagged: `"put '--' before"` is closer to prose than to a token. |
| B-037 | `cli/tests/workspaces/test_create_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary@c58c46` | count-of-report-line plus prefix blacklist | delete | `sum(... "Workspace 'ws1' created" ...) == 1` pins the sentence and its non-duplication; the no-section-header clause is a form blacklist. The create itself is a DB fact asserted beside it. |
| B-038 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_reachable_vm_is_one_boundary_burst@a46484` | report-line pin | delete | `assert resolve_counter == [["proxmox-token"]]` and `assert any("chmod -c 2770 /srv/ws1" in c for c in target.commands)` carry the boundary-burst claim; the "Repairing workspace ..." announce line is the report. |
| B-039 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_skips_git_identity_when_not_a_repo@4785e6` | warning-absence substring | convert | The invariant (identity on a non-repo workspace is a quiet no-op) is real and only the warning stream sees it. VERIFIED by executing: `assert captured_output.warnings == []` passes. No production change. |
| B-040 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_git_identity_warns_on_unexpected_probe_failure@2f3b8f` | warning-presence substring | convert | Companion to B-039: the branch that must NOT be silent. VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes. No production change. |
| B-041 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_healthy_workspace_reports_ok_for_every_step@d2e75a,test_repair_fully_damaged_workspace_reports_fixed_per_step@2e5e60,test_repair_partial_damage_reports_only_the_diverged_step@13cdd6,test_repair_sgid_only_damage_reports_permissions_fixed@530851,test_repair_mode_only_damage_reports_permissions_fixed@88f0d7,test_repair_owner_only_damage_reports_permissions_fixed@88f0d7,test_repair_hardens_other_bits_on_a_pre254_workspace@d756c9` | `OK:` / `Fixed:` / `Repaired N issue(s)` report-line pins | keep | **Operator ruling, 2026-08-16**, applying the rubric that this row provoked. These assertions do not fail only on a rewording: each tracks a DETECTION OUTCOME, whether a repair step found something to converge or found the state already correct, and `repair_workspace` returns `None` (`workspaces/manager/repair.py::repair_workspace`) so nothing else observes it. Convergence is idempotent, so the emitted command list is identical either way, which is what makes this the only probe. The three `*_only_damage` tests pin the owner/mode/sgid OR-collapse at `repair.py::repair_workspace` against an AND-collapse regression the code comments record as having really happened. **What the keep costs, stated so nobody later reads this as a prose pin nobody noticed**: these assertions break if someone rewords `OK:`, `Fixed:` or `Repaired N issue(s)`, and whoever does that must update them. That is the price of the only probe there is, not an oversight. |
| B-042 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_acl_churn_does_not_report_false_fixed@6efe1e` | OK/Fixed report-line pins | convert | The invariant here IS separable from the report: `_acls_changed` is a module-level helper (`repair.py::_acls_changed`). VERIFIED by calling it directly at HEAD: churn-only snapshots give `False`, a real per-path ACL change gives `True`, empty-vs-empty gives `False`. Replace the whole test with a direct `_acls_changed` unit test over the three inputs. No production change. |
| B-043 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_acls_indeterminate_when_getfacl_fails@f60f1d` | OK-absence, Fixed-prefix blacklist, warning substring, qualified closing-line pin | keep | Moves with B-041 under the same ruling: the indeterminate ACL branch must warn and must NOT report a verified `OK:`, and that outcome has no observable but the report line. The `Fixed:`-prefix blacklist half is the exception and still goes, because a blacklist cannot hold the property and the surviving `setfacl` convergence command in the same test carries what it was reaching for. Same rewording cost as B-041. |
| B-044 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_first_repair_after_create_is_a_noop@0a258e,test_first_repair_after_rehome_is_a_noop@527d82,test_first_repair_after_copy_is_a_noop@4a6192` | first-repair-is-a-noop report pins | keep | Moves with B-041: the claim is that the first repair after create, rehome and copy finds NOTHING to fix, which is exactly a detection outcome and is exactly what a regression in the canonical ACL spec would break. The `setfacl` assertions inside those same tests (`assert any("setfacl -R -m g::rwx -m m::rwx -m o::---" in c for c in create_acls)` and its `copy_acls` twin) prove the spec was applied, not that repair then saw it as already correct, so they are not a sibling for this. Same rewording cost as B-041. |
| B-045 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_delete_reachable_vm_is_one_boundary_burst@7594e0,test_delete_group_removal_failure_does_not_break_the_delete@08f033,test_copy_cross_vm_runs_two_sequential_boundaries_with_nested_holds@8c86f7,test_copy_same_vm_reuses_the_source_composition@366c4d` | **[unverified]** closing report-line pins | delete | "Workspace 'ws1' deleted" / "copied to 'ws2'" restate DB facts the same tests assert directly. |
| B-046 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_delete_group_removal_failure_does_not_break_the_delete@08f033,test_create_rollback_tolerates_a_groupdel_failure@4c16b2` | warning-absence blacklist | convert | The invariant (a tolerated groupdel failure does not surface as a rollback warning) is real and only the warning stream sees it. VERIFIED by executing: `assert captured_output.warnings == []` passes at both sites. No production change. |
| B-046a | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_create_rollback_on_clone_failure_leaves_no_residue@9b28bd` | warning-absence blacklist | convert | Same invariant, different count. VERIFIED by executing: `warnings == []` FAILS here because an unrelated git-credential hint warning always fires; `assert len(captured_output.warnings) == 1` passes. This premise-check changed the replacement I would have written. |
| B-046b | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_copy_without_grant_all_agents_makes_no_grant_calls@06390b` | detail-absence blacklist | delete | I expected to need a call-spy replacement; VERIFIED by executing that the test ALREADY has one, `assert calls == []` plus `assert db.list_agent_grants("dev") == []`, which is the whole invariant. (`detail == []` fails here: an unrelated VS Code workspace line is emitted.) Clean delete. |
| B-047 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_copy_materializes_grant_all_agents_on_the_dest_vm@c34975` | **[unverified]** count-in-report-line pin | delete | "Added 1 grant-all agent(s) to workspace" restates the grant row the test can read from the DB. |
| B-048 | `cli/tests/orchestration/test_activation.py::test_operator_stopped_refusal_propagates_from_the_node@063903` | hint equality | keep | Rubric re-check, 2026-08-16. **The row was mistaken.** The pinned hint is authored by the test's own `_Target` double at `orchestration/test_activation.py::_Target.auto_start`, not by production, so it is an injected sentinel. It probes that `ensure_active` propagates the node's refusal untouched, which nothing else does once G1-018 (2026-08-19 map) removes the `match=`. |
| B-052 | `cli/tests/orchestration/test_readiness.py::test_skip_and_degrade_skips_rejected_and_reports_them@9089a6` | tuple equality carrying an injected reason | keep | `rejected == [("gh", "gh: token rejected")]` pins the identity and pairing of the rejection record; the string is the fixture's own. |
| B-053 | `cli/tests/orchestration/test_unwind.py::test_failed_teardown_warns_and_never_masks@125e91` | warning substring | convert | "a failed teardown warns and never masks the original error" is the invariant; the never-masks half is structural (the original error propagates) but the warns half only the warning stream sees. VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes. No production change. |
| B-054 | `cli/tests/transports/test_factories.py::test_native_transport_none_raises_typed_state_error@3b2d19` | hint substring | convert | Rubric re-check, 2026-08-16. The claim is that the PLATFORM's own hint is carried, and `hint is not None` does not carry it. Assert `ProxmoxPlatform.no_native_transport_hint in exc_info.value.hint`, deriving from the canonical constant the test already reads. No production change. |
| B-055 | `cli/tests/transports/test_factories.py::test_native_transport_probe_exhaustion_warns_platform_hint@3cd489` | warning list membership on an authored phrase | keep | Rubric re-check, 2026-08-16. **The row's premise was wrong.** `"scoped allow guidance"` is the test's OWN fixture value, set at `transports/test_factories.py::test_native_transport_probe_exhaustion_warns_platform_hint`, not authored prose, so it is value identity of the kind L-010 keeps, and it is the only probe that `probe_failure_hint` reaches the operator. |
| B-058 | `cli/tests/transports/test_ssh.py::test_interactive_reports_a_dropped_connection@595c44` | warning substring pair | convert | The "dropped connection is reported, not swallowed" invariant is only visible in the warning stream. Replace with a warning-count assertion; VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes. `"agentworks@vm1"` is the host/user the test injected and can stay as value identity if the lead wants the propagation kept. |
| B-066 | `cli/tests/assistance/test_generation.py::test_command_check_exits_nonzero_without_repairing_drift@f2051a` | path-in-output substring | keep | The asserted string is a repository path (`plugins/codex/agentworks/.codex-plugin/plugin.json`), and the test's subject is that the drift check names the file that drifted while exiting nonzero without repairing. Path reference, not prose (the charter's named keep case). Recommend pairing it with a path-existence assertion. |
| B-067 | `cli/tests/test_agents.py::test_assert_agent_ssh_works_raises_on_ssh_transport_failure@a3583f,test_assert_agent_ssh_works_wraps_ssh_error_as_connectivity@4d6cde` | **[unverified]** hint substring | delete | `"agent reinit claude" in hint` and `"alpha" in hint` pin fragments of authored recovery sentences; the typed `ConnectivityError` and its entity fields already discriminate, and the agent name is asserted through `entity_name` in the same tests' neighbors. |
| B-069 | `cli/tests/test_codex_integration.py::test_raise_hints_name_a_recovery_the_operator_can_actually_take@21bb10` | hint substring plus wording blacklist | convert | This is the one test in the file where three same-type `StateError`s are told apart only by their hints, and a sibling delegates its discrimination to it by name. Keep the fixture-determined path fragment (`"Remove ~/.agentworks/codex/s1.thread"` names the file the test created) and replace `"nothing is orphaned" in hint` and `"reachable" not in hint` with a distinctness check over the three hints. No production change. |
| B-070 | `cli/tests/test_codex_integration.py::test_raise_hints_name_a_recovery_the_operator_can_actually_take@21bb10` | hint substring and hint verbatim equality | convert | The rest of the same test: `"codex sessions directory" in hint` and `hint == "Retry once the launch target is reachable."`. Same replacement as B-069; treat the two rows as one edit. |
| B-071 | `cli/tests/test_codex_integration.py::test_recorded_thread_id_binds_a_session_that_had_nothing_stored@e1e27e,test_recorder_content_that_is_not_a_uuid_is_treated_as_unbound@2c8b22,test_single_candidate_is_adopted_and_resumed@7d4b2a,test_zero_candidates_launches_fresh@3fee5a,test_several_candidates_open_the_picker_instead_of_raising@c49f3a,test_a_candidate_whose_filename_has_no_uuid_opens_the_picker@710c45,test_discovery_without_a_sessions_dir_launches_fresh@3da5de,test_gone_rollout_drops_the_id_and_falls_through_to_a_fresh_launch@b3d34d,test_wrong_typed_stored_id_is_swept_and_treated_as_absent@76ccae,test_bound_id_is_resumed_verbatim_on_the_next_op@96671f,test_sh_probe_counts_an_unreadable_rollout_as_a_candidate_it_cannot_name@9f8e96,test_recreated_namesake_never_inherits_the_dead_binding@5e1045` | pane-echo wording substrings | delete | The `_echo(command)` assertions across twelve tests: `"resuming session s1"`, `"starting new session s1"`, `"archived or gone; starting new session s1"`, `"identified this session's codex conversation from codex's on-disk state"`, `"could not identify this session's codex conversation with confidence"`, `"press esc to start a fresh conversation"`. Every one sits beside the structural claim the test is about: `state == {}` or `state == {"session_id": _SID}`, plus `f"resume {_SID}" in command` or `"resume" not in _sh_argv(...)`. The adopted uuid already rides in the command as value identity. |
| B-073 | `cli/tests/test_codex_integration.py::test_writable_dirs_emit_one_add_dir_each_in_order_and_quoted@7c5021` | argv token containing a space | keep | `"/data/shared dir" in argv` proves a writable directory whose name contains a space survives shell nesting as ONE argv token. The string is the path the test injected, so this is value identity, and the invariant is quoting. |
| B-079 | `cli/tests/test_consoles_layout.py::test_apply_layout_aw_session_vertical_silent_on_single_pane@79708d,test_apply_layout_aw_session_vertical_warns_on_genuine_failure@27c1e5,test_apply_layout_aw_session_vertical_applies_under_pane_base_index_one@7be9ed` | warning substring and absence | convert | The invariant, that a genuine layout failure warns while a single-pane no-op and a `pane-base-index 1` layout do not, is real and only the warning stream sees it. VERIFIED by executing the PROPOSED assertions, which are replacements rather than anything in the tree today: `captured_output.warnings == []` in `test_consoles_layout.py::test_apply_layout_aw_session_vertical_silent_on_single_pane` and `test_consoles_layout.py::test_apply_layout_aw_session_vertical_applies_under_pane_base_index_one`, and `len(...) == 1` in `test_consoles_layout.py::test_apply_layout_aw_session_vertical_warns_on_genuine_failure`, all pass. No production change. |
| B-080 | `cli/tests/test_consoles_live_sync.py::test_add_session_live_sync_adds_window_for_bare_spec@5ba8b4,test_reorder_sessions_live_sync_bails_on_duplicate_window_names@34c278,test_kill_session_windows_transport_failure_warns@95184d` | warning substring and absence | convert | Same shape as B-079: live-sync silently-succeeded vs bailed-with-a-reason. VERIFIED by executing: `warnings == []` and both `len(...) == 1` assertions all pass. The window names `alpha`/`beta` are the test's own and can stay as value identity. No production change. |
| B-081 | `cli/tests/test_consoles_live_sync.py::test_delete_session_reports_affected_consoles@851b59,test_delete_session_yes_reports_but_keeps_empty_console@c535ef,test_remove_sessions_warns_when_offered_console_delete_raises@8255ba` | **[unverified]** report-line equality | delete | `report == ["Removed 's' from consoles: alpha, beta"]` and its two siblings pin an authored sentence that restates console membership the tests read from the DB directly. |
| B-082 | `cli/tests/test_consoles_live_sync.py::test_delete_session_offers_and_deletes_now_empty_console@19d526,test_delete_session_does_not_offer_console_with_remaining_sessions@9ed480,test_remove_sessions_empties_console_offers_and_deletes@c50f20,test_remove_sessions_with_remaining_not_offered@63ce4e` | prompt-wording substring and absence | convert | Same invariant and same replacement as B-007 (was the operator offered the delete, or not), VERIFIED there. Replace with prompt-count assertions. No production change. |
| B-083 | `cli/tests/test_consoles_live_sync.py::test_delete_session_yes_reports_but_keeps_empty_console@c535ef,test_delete_session_warns_when_offered_console_delete_raises@d39ece,test_remove_sessions_yes_reports_but_keeps_empty_console@b283f2,test_remove_sessions_warns_when_offered_console_delete_raises@8255ba,test_remove_sessions_non_interactive_without_yes_reports_but_keeps@b283f2` | report-but-keep warning triples | delete | Identical family to B-010: every one sits under a console-row assertion that carries the keep. |
| B-084 | `cli/tests/test_consoles_orchestration.py::test_reorder_sessions_noop_when_already_in_requested_order@f13da7` | **[unverified]** report-line substring | delete | "already in the requested order" restates the no-op the test asserts structurally (no tmux move commands, unchanged rows). |
| B-085 | `cli/tests/test_consoles_orchestration.py::test_describe_console_uses_iteration_index@7e2320` | rendered-line list equality | convert | Rubric re-check, 2026-08-16. The iteration-index-not-stored-position invariant is real (a remove leaves a position gap in the DB) and the rendered-line equality inside it (`assert member_lines == [` over `"[0] b  (no extra shells)"` and `"[1] c  (no extra shells)"`) is its only probe. Keep the leading `[N]` indices, drop the surrounding sentence. The row already prescribed this; the re-check makes it binding. No production change. |
| B-086 | `cli/tests/test_consoles_restore.py::test_restore_session_noop_when_live_matches_config@abbc04,test_restore_session_healthy_under_pane_base_index_one@0e984a,test_restore_session_is_a_noop_on_a_healthy_model@73fc42` | report-line substring | delete | Three no-op tests whose structural half (no rebuild commands issued) is asserted beside them. |
| B-087 | `cli/tests/test_consoles_restore.py::test_restore_session_rebuild_raises_when_new_window_fails@312540,test_restore_session_rebuild_raises_when_shell_split_fails@b18e5b,test_restore_session_rebuild_raises_when_shell_tag_fails@b18e5b` | report-line absence | delete | `not any("Rebuilt window" in m ...)` is a phrase blacklist standing in for "the rebuild did not complete", which the raised error and the absent tmux commands already carry. |
| B-088 | `cli/tests/test_consoles_restore.py::test_restore_session_rebuild_raises_when_shell_split_fails@b18e5b,test_restore_session_rebuild_raises_when_shell_tag_fails@b18e5b,test_restore_session_refuses_when_session_pane_killed@864b9b` | hint substring naming a CLI command | keep | `"agw console attach con --recreate" in hint` is the literal recovery command an operator types, keyed on the console name the test seeded. Same class as B-016: a command reference, not prose about one. |
| B-089 | `cli/tests/test_consoles_restore.py::test_restore_session_reorders_relative_to_the_session_pane@aa6865` | warning-absence blacklist | convert | Replace with `captured_output.warnings == []`. VERIFIED by executing: it passes. No production change. |
| B-090 | `cli/tests/test_consoles_shell_panes.py::test_split_shell_pane_agent_branch_no_probe_without_composed_env@8dbbec` | phrase-filtered warning counting | delete | I expected a plain warning count to work here; VERIFIED by executing that it does not. The build emitted five warnings at one site and one unrelated warning at another, so the count only means anything through the phrase filter. With no way to identify the once-per-build warning except by its wording, and no production change permitted to give it one (R2.2), R2.4 directs deletion. This premise-check changed my disposition. **The measurement is historical**: it was taken at a tree this map no longer matches, and the two sites it counted at cannot be named here, since no test in this file asserts on warnings at HEAD. **Re-derived 2026-09-06 at `c310d05b`.** The once-per-build test is gone. The phrase-filtered warning family it belonged to survives in `test_split_shell_pane_agent_branch_warns_and_falls_back_when_setenv_missing`, which B-092 already rows, so nothing is unrowed and this row stands on its remaining anchor. |
| B-091 | `cli/tests/test_consoles_shell_panes.py::test_split_shell_pane_warns_when_split_returns_no_pane_id@661161,test_split_shell_pane_warns_when_set_option_fails@e3b0c4` | required-phrase lists in warnings | convert | Rubric re-check, 2026-08-16. **Re-derived 2026-09-06.** The phrase list is in `test_consoles_shell_panes.py::test_split_shell_pane_warns_when_split_returns_no_pane_id` (`"couldn't capture its id"`, `"untagged"` and `"restart con"` over one warning), and it is that test's only warning assertion, so deleting it leaves the no-pane-id degrade unprobed. Convert it to a warning-count assertion. No production change. **Out-of-scope finding:** the row's other anchor, `test_consoles_shell_panes.py::test_split_shell_pane_warns_when_set_option_fails`, asserts NOTHING at HEAD. It drives `add_shell` with `set-option` failing and then ends, so the tag-failure degrade its docstring describes is unprobed today and no edit of this row's making. Raising it rather than fixing it: giving a test its missing assertion is not a subtraction pass's work. |
| B-092 | `cli/tests/test_consoles_shell_panes.py::test_split_shell_pane_agent_branch_warns_and_falls_back_when_setenv_missing@9e5a1a` | **[unverified]** required-phrase list on a warning | delete | `"Defaults:admin setenv"`, `"agw vm reinit vm1"`, `"not allowed to set the following environment variables"`: the first two are sudoers syntax and a CLI command (keepable as tokens), the third is authored prose. The fall-back behavior itself is asserted through the emitted command in the same test. |
| B-102 | `cli/tests/test_secret_sources_drive_harness.py::test_fake_provider_path_is_closed_and_cannot_fall_through@087ab1` | injected-sentinel equality | keep | `completed.stdout == "fixture-sentinel"` proves the fake provider path is closed and cannot fall through to a real one. Value identity on a string the test injected (the charter's named keep case). |
| B-113 | `cli/tests/test_secrets_inspect.py::test_no_operator_secrets_still_shows_auto_declared@c93e03` | auto-declared description verbatim equality | convert | Rubric re-check, 2026-08-16. The auto-declared description is SYNTHESIZED from the requirement's usage and source, and its siblings do not probe the synthesis (`assert "tailscale-auth-key" in names` and `assert table.auto_count >= 1`): name presence and `auto_count >= 1` both survive an empty description. Assert the `(auto)` prefix plus the source substring. No production change. |
| B-114 | `cli/tests/test_secrets_inspect.py::test_grid_cell_not_ready_wins_over_identifier@e86569,test_grid_cell_wont_attempt_wins_over_not_ready@d43b6e` | injected-reason equality | keep | `not_ready_reason == "op CLI not installed"` is the fixture's own reason propagating into the grid cell; value identity. |
| B-116 | `cli/tests/test_secrets_orchestration.py::test_resolve_for_command_rejects_multiline_environment_target_after_delivery@b6bc0b`, `cli/tests/test_secrets_resolver.py::test_operation_resolver_rejects_multiline_environment_target_after_delivery@d918ef` | error-message substring | delete | `"cannot be used for environment injection"` is authored prose; both tests already discriminate on the raised type, and the multiline-value refusal is the behavior. |
| B-117 | `cli/tests/test_session_agent_filter.py::test_session_stop_agent_without_all_errors@451e0b,test_session_start_agent_without_batch_errors@451e0b,test_session_stop_admin_and_agent_are_mutually_exclusive@b9e3df,test_session_stop_admin_without_all_errors@033a44,test_session_start_admin_and_agent_are_mutually_exclusive@b9e3df` | flag-name substring in CLI error output | keep | Each asserts that the error output names the flag(s) involved in a mutually-exclusive or missing-companion combination. These are option names, not prose, and they pair with `exit_code != 0` in every case. Flagged as borderline: if the lead reads them as prose pins, the exit-code siblings make them clean deletes. **Re-derived 2026-09-06 at `c310d05b`.** The lifecycle rename moved both `session resume` tests to `session start`; each still asserts the flag name beside `exit_code != 0`, so the keep and its borderline flag are unchanged. |
| B-118 | `cli/tests/test_session_create_ephemeral_prompt_filtering.py::test_workspace_prompt_filters_by_vm_flag@d6f989,test_workspace_prompt_filters_by_existing_agent_vm@9385cb,test_mode_prompt_filters_by_resolved_vm_with_info_line@e0b23a` | **[unverified]** info-line substring | delete | "Only showing workspaces on VM 'vm-A'" restates the filtering the same tests assert by checking the option list they were offered. |
| B-119 | `cli/tests/test_session_create_ephemeral_prompt_filtering.py::test_workspace_prompt_filters_by_vm_flag@d6f989,test_mode_prompt_filters_by_resolved_vm_with_info_line@e0b23a,test_mode_prompt_picks_existing_agent_pins_vm_no_vm_prompt@c8e325,test_mode_prompt_picks_admin_then_vm_prompt_fires._choose_spy@30a6ed` | prompt-option and prompt-label equality | keep | `opts[-1] == "[Create new workspace]"` pins that the create-new escape hatch is last in the list (position is the invariant, and the option string is a menu token the code branches on by index). `msg == "Run session as:"` is a weaker case; recommend trimming that one to an option-list assertion. |
| B-120 | `cli/tests/test_session_create_ephemeral_rollback.py::test_session_create_frames_phases_like_a_plan@21dd05` | header-banner and "Checking ..." substring pins | convert | **Re-measured 2026-09-06, and the disposition changed.** The structural block in the same test covers four of the seven observables these pins carry; `=== Resolving Secrets ===`, `Checking workspace-template/` and `Checking agent-template/` have no structural twin, so a plain delete would drop three phases from the output tree's coverage. Replacement: add those three to the structural block through `captured_output.lines`, then delete only the pins the block covers once it does. No production change. **Re-justified 2026-09-06 by the verification lane.** This row used to rest on B-003 landing its conversion first. B-003 and B-004 have since retired, because `d99835da` removed the banner pins and all four `Role.*` assertions from the test they addressed. The structural coverage this row rests on is inside its own test and never depended on B-003, so the conversion stands on its own evidence. `agents/test_create_reinit_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary` carries the same shape for the agent path and is where an executor should look for the pattern to copy. |
| B-121 | `cli/tests/test_session_create_ephemeral_rollback.py::test_session_create_frames_phases_like_a_plan@21dd05` | structural presence on (role, level, message) | keep | Same invariant as B-004: the plan-shaped output tree (which phases are sections, what nests at level 1). |
| B-122 | `cli/tests/test_session_create_ephemeral_vm_anchor.py::test_defaulted_ephemeral_agent_name_overflow_hints_override_flag@3e67f4,test_defaulted_ephemeral_workspace_name_overflow_hints_override_flag@d2c41a` | hint substring pairs | convert | `"--agent-name"` / `"--workspace-name"` are the override flags the operator needs and stay; `"defaulted to the session name"` is authored prose and goes. The name-overflow branch is discriminated by the typed error. No production change. |
| B-123 | `cli/tests/test_session_liveness.py::test_legacy_admin_session_without_socket_raises_state_error@8cf4c1` | hint substring plus wording blacklist | delete | `"session resume" in hint` with `"session delete" not in hint` is a blacklist pair pinning which command the hint recommends. The legacy-socket branch is discriminated by the typed `StateError`. |
| B-126 | `cli/tests/test_sessions_tmux_create.py::test_dead_pane_at_first_check_captures_and_skips_grant@cf3bdd,test_dead_pane_at_post_unset_recheck_captures_and_raises@05c3ea,test_probe_transport_failure_raises_could_not_verify_without_kill@0a3c92` | error substring plus wording blacklist | delete | Same family as B-125; the `"exited immediately" not in str(...)` assert is a blacklist standing in for "a transport failure took the other branch", which the raised type discriminates. |
| B-127 | `cli/tests/test_sessions_tmux_create.py::test_dead_pane_at_first_check_captures_and_skips_grant@cf3bdd` | injected upstream text substring | keep | `"error: invalid value 'on-failure' for '--ask-for-approval <APPROVAL_POLICY>'"` is the codex CLI's own error, injected by the test's fake pane; the invariant is that captured pane output propagates verbatim into our diagnostic. Value identity plus the outside-prose exception. |
| B-128 | `cli/tests/test_sessions_tmux_create.py::test_pane_command_wraps_in_login_interactive_shell@268b33,test_tmux_env_flags_quotes_values_with_spaces@399307,test_admin_create_session_passes_env_to_run_command@5b9772,test_alive_pane_remain_on_exit_lifecycle@cf14cc,test_trim_captured_output_strips_blanks_and_caps@ffab2f` | emitted-command and captured-output token pins | keep | `cd /workspace && exec claude`, `-e 'GREET=hello world'`, `remain-on-exit on`, and the trim helper's last-line identity are the commands and data the code produces at the tmux boundary. Behavior, not prose. |
| B-129 | `cli/tests/test_shell_integration.py::test_missing_command_is_a_typed_error_naming_the_vm@793b87` | error substring plus hint substring | convert | Keep `"--template" in hint` (an option name, per B-016); delete `"for VM 'box'." in str(exc.value)`, which pins the authored trailing clause. No production change. |
| B-130 | `cli/tests/test_templates.py::test_unknown_template@898c1a` | hint prefix pin | delete | `hint.startswith("available workspace templates: ")` pins the authored lead-in. |
| B-131 | `cli/tests/test_templates.py::test_unknown_template@898c1a` | injected-name membership loop | keep | The loop asserts every declared template name appears in the hint; the names come from the test's own config fixture, so this is value identity and it is the substance of "the hint lists the declared names". |
| B-132 | `cli/tests/test_templates.py::test_unknown_template_hint_when_none_declared@f87f3a` | hint verbatim equality | keep | Rubric re-check, 2026-08-16. The empty-set branch raises the same `NotFoundError` as the populated branch, so the hint is its only discriminator and the hint equality is the test's only assertion. Cost: breaks if the hint is reworded. |
| B-134 | `cli/tests/test_tmuxinator.py::test_ensure_agent_socket_root_missing_warns_by_default@4e79ed,test_ensure_agent_socket_root_misconfigured_warns_even_when_missing_suppressed@3d75ee,test_ensure_agent_socket_root_probe_failed_warns_even_when_missing_suppressed@2d20b9,test_ensure_agent_socket_dir_missing_warns_by_default@c0afa2,test_ensure_agent_socket_dir_misconfigured_warns_even_when_missing_suppressed@3d75ee,test_ensure_agent_socket_dir_probe_failed_warns_even_when_missing_suppressed@2d20b9` | warning-classification substrings | convert | Six branches distinguished only by which word appears in the warning (`missing`, `misconfigured`, `probe failed`) plus a suppression flag. The invariant, that suppressing the missing-warning does not suppress the misconfigured or probe-failed ones, is real and structural in nature. VERIFIED by executing: `assert len(warnings) == 1` passes at all six sites, so each branch's warning is the only one in its run and the count carries the classification. No production change. |
| B-135 | `cli/tests/test_tmux_model.py::test_new_window_reports_the_session_pane_index_via_dispatch@03e286` | dispatch-result equality | keep | `res.stdout.strip() == "1"` is the pane index the fake tmux returned; value identity, and the invariant is that the dispatch reports the session pane index rather than a fixed 0. |
| B-136 | `cli/tests/test_sources.py::test_fetch_file_clones_copies_and_cleans_git_source@c41fb7` | emitted-command substring | keep | `"git clone --depth 1 --branch v1"` is the command the fetcher runs at the subprocess boundary. Behavior. |
| C-040 | `cli/tests/test_completions.py::TestUninstall.test_uninstall_when_nothing_installed_is_clean@84cdb1` | parametrized empty-state message pin | keep | `f"no {shell} completions found" in result.stdout.lower()` interpolates the parametrize value, so it proves each shell's empty-state report names the shell the operator asked about. A powershell uninstall that reported about bash passes the exit-code assertion beside it and fails only here, so the failure mode is not only a rewording. |
| C-041 | `cli/tests/test_completions.py::TestUninstall.test_powershell_uninstall_fails_when_no_binary@531765` | refusal-wording substring | delete | `"could not determine PowerShell $PROFILE path" in result.stderr` is an authored sentence. `result.exit_code != 0` beside it carries the invariant the test is named for, that a missing profile fails loudly rather than silently succeeding. |
| C-051 | `cli/tests/test_completions.py::test_direct_probe_keeps_stdout_pure_while_warning_lands_on_stderr@67daa1` | authored warning pin used as a control | convert | `"Config: unexpected keys in [operator]" in direct.stderr` is authored wording, but its role is to show the direct invocation does emit something on stderr, which is what makes the sibling test's `2>/dev/null` suppression mean anything. Replacement: `direct.stderr != ""`. The stdout-purity half and the no-writes check stay. No production change. |
| C-056 | `cli/tests/test_doctor.py::test_machine_output_serializes_the_same_health_check_facts@a84a3c` | projection value identity | keep | The message and hint the test itself put on the group must survive the JSON projection, and `"note" not in check` says the projection carries no field the check did not have. Field-carrying behavior over the test's own values, not authored prose. |
| C-057 | `cli/tests/test_doctor.py::test_config_exception_becomes_one_shared_health_fact@894952` | shared-fact propagation | keep | A config exception's message and hint reach the projected check with the injected `secret://credentials/production` marker intact, proving one health fact rather than a reworded copy per surface. The asserted strings are the ones the test's own fake raised. |
| C-058 | `cli/tests/test_doctor.py::TestCompletionChecks.test_unavailable_shell_reports_info_not_warn@072296,TestCompletionChecks.test_stale_available_shell_still_warns@0ebe8a` | message substring beside an asserted status | delete | `"not found on this machine" in message` and `"stale" in message` are authored wording, and the INFO-versus-WARN status asserted immediately above each discriminates exactly the same branch. |
| C-059 | `cli/tests/test_doctor.py::test_run_checks_returns_report@60c0f5,test_run_checks_group_order_and_config_failure_placeholder@d43e01` | group ordering | keep | The report's twelve-group list is an ordered identity check, and the point of the second test is that no registry-dependent group vanishes in degraded mode: each keeps its presentation slot around the Configuration group that explains the failure. Ordering of records is structure, not layout. |
| C-060 | `cli/tests/test_doctor.py::test_run_checks_group_order_and_config_failure_placeholder@d43e01` | required-phrase pair | delete | `"skipped" in message` and `"Configuration" in message` police the placeholder's wording. The structural facts in the same loop (exactly one check, status INFO) already prove the group renders a placeholder instead of vanishing. |
| C-061 | `cli/tests/test_doctor.py::test_run_checks_group_order_and_config_failure_placeholder@d43e01` | verbatim HealthCheck equality | convert | The trailing `source_placeholder.checks == [HealthCheck(name="Declared sources", status=INFO, message="skipped (config or manifests unavailable; see the Configuration group)", hint=None)]`. Secret sources is the one group the loop above does not cover, so the structural half must stay. Replacement: `[(c.name, c.status) for c in source_placeholder.checks] == [("Declared sources", doctor.Status.INFO)]`. No production change. |
| C-062 | `cli/tests/test_doctor.py::test_run_checks_secrets_group_skips_not_vanishes_when_config_broken@48875a` | cross-group parity | keep | `secrets.checks[0].message == vm_sites.checks[0].message` pins that both skip lines come from one shared helper rather than two hand-written sentences. It fixes no wording of its own, which is what makes it parity rather than a pin. |
| C-063 | `cli/tests/test_doctor_cli.py::TestDoctorColorOnATty.test_ok_label_is_green@8fcb4b,TestDoctorColorOnATty.test_warn_label_is_yellow@0a6a8c,TestDoctorColorOnATty.test_fail_label_is_red@39988f,TestDoctorColorOnATty.test_info_label_is_unstyled@993f83,TestDoctorColorOnATty.test_label_column_width_unaffected_by_ansi@95d14e,TestDoctorColorOnATty.test_summary_counts_are_colored@420f6f` | synthetic-fixture formatting | keep | The charter's explicit carve-out: check names and messages are the test's own fixture, and the assertions are about ANSI styling, the `[ok]` and its two trailing spaces/`[warn]` label column, and that `.ljust(6)` alignment survives ANSI stripping. |
| C-064 | `cli/tests/test_doctor_cli.py::TestDoctorPlainFallback.test_non_tty_stdout_is_byte_plain@1368fe,TestDoctorPlainFallback.test_no_color_env_forces_byte_plain_even_on_a_tty@d98694,TestDoctorPlainFallback.test_non_interactive_does_not_change_tty_presentation@661857` | TTY fallback behavior | keep | No ANSI may reach a piped stream, `NO_COLOR`, or non-interactive mode, and the plain layout must stay byte-exact: terminal behavior over a synthetic report. **Re-derived 2026-09-06 at `c310d05b`.** Production inverted: non-interactive no longer forces plain output, and the test asserts the opposite of what it did, that ANSI survives it. The row's keep covers the same behavior under the new rule, and the `NO_COLOR` and non-tty fallbacks it also names are untouched. **Only the ANSI-absence half is unique to these tests.** The two byte-exact layout literals are caught elsewhere: `TestDoctorColorOnATty::test_label_column_width_unaffected_by_ansi` (`test_doctor_cli.py::TestDoctorColorOnATty.test_label_column_width_unaffected_by_ansi`) catches the first and `test_summary_counts_are_colored` (`test_doctor_cli.py::TestDoctorColorOnATty.test_summary_counts_are_colored`) the second, so an executor narrowing this row should narrow toward the ANSI-absence assertions rather than the layout ones. |
| C-065 | `cli/tests/test_doctor_schema.py::test_schema_check_distinguishes_absent_table_from_malformed_shape@cea23c,test_schema_check_rejects_invalid_scalar_versions@35c831,test_installed_doctor_reports_malformed_schema_in_human_and_json@c1ef09` | redaction defense | keep | The private column name and the absolute database path must not reach the operator or any output stream. A leak defense, not prose policing. |
| C-066 | `cli/tests/test_doctor_schema.py::test_doctor_distinguishes_absent_stale_and_newer_schema@a74f85` | expected-text column beside an asserted status | delete | The parametrization already discriminates absent, stale and newer schema by `Status.OK`/`WARN`/`FAIL`; `expected_text in check.message` adds only our wording. |
| C-067 | `cli/tests/test_doctor_schema.py::test_doctor_warns_without_echoing_an_unexpected_vm_initialization_state@433f3f` | verbatim message equality | convert | The test's own name says "without echoing": replace `warning.message == "unexpected initialization state"` with `"PRIVATE_FUTURE_STATE" not in (warning.message or "")` beside the asserted WARN status, turning a wording pin into the redaction defense it stands in for. No production change. |
| C-068 | `cli/tests/test_doctor_schema.py::test_doctor_database_errors_remain_shared_actionable_facts@fcea9e` | authored prefix around an injected marker | convert | Replace the three `== f"could not check the database: {marker}"` equalities with `marker in message`. The invariant is that one database error reaches the system, database and vm-sites rows intact; the prefix is ours to reword. No production change. |
| C-069 | `cli/tests/test_doctor_schema.py::test_installed_doctor_reports_malformed_schema_in_human_and_json@c1ef09` | human-output sentence pin | delete | The malformed-schema fact is asserted structurally in the JSON projection below and by the shared exit code; these two pin the human render's wording. |
| C-070 | `cli/tests/test_doctor_schema.py::test_installed_doctor_reports_malformed_schema_in_human_and_json@c1ef09` | verbatim check dict | convert | Replace the dict equality with `list(checks[0]) == ["name", "status", "message", "hint"]` plus `(checks[0]["name"], checks[0]["status"]) == ("Database", "fail")`, keeping the projection's key shape and the failure identity and dropping the sentence. No production change. |
| C-073 | `cli/tests/test_doctor_env_and_secrets.py::test_secret_backends_group_reports_readiness@dac5b2,test_secret_backends_group_skips_disabled_plugin_backend@a3aa05` | readiness-reason and enablement wording | delete | `by_name["onepassword"].message == "not ready: op CLI not installed"` and `"not enabled in [plugins].system" in message` pin authored reason text. Each sits beside `status is Status.INFO`, which discriminates the same branch, and the second test's structural claim (`"onepassword" not in by_name`, plus the empty focused-check tuple) is what it is named for. |
| C-074 | `cli/tests/test_doctor_env_and_secrets.py::test_r9_3_manifest_malformed_block_surfaces_under_resource_registry@e1d42b` | authored fragment around an injected field name | convert | `"bogus: unknown field" in message`. The invariant is that the field name the test injected into the manifest reaches the Resource registry row, so an operator can find it. Replacement: `"bogus" in message`. The row-name assertions beside it (`"Resource registry" in fails`, `"Manifest" not in fails`) and `registry is None` stay. No production change. |
| C-075 | `cli/tests/test_doctor_env_and_secrets.py::test_agentworks_identity_override_surfaces_in_configuration@23d20d,test_manifest_issues_surface_as_doctor_rows@ae358f,test_manifest_load_failure_keeps_other_rows@faa01a,test_prompt_only_site_secret_leaves_the_site_row_ok@75f720,test_focused_vm_site_preserves_bulk_preflight_exception_facts@869540` | injected-value propagation | keep | Each asserts that a name the test itself created reaches the doctor row that should carry it: `AGENTWORKS_SESSION` (the env key it wrote), `AGENTWORKS_AGENT` and `agent.yaml` (the key and the filename it wrote), `broken.yaml` and `oops` (the manifest that would not parse and the unexpected setting), `azure-vm` (the platform its site declares), and `row.hint == "repair the local prerequisite"` (the hint its own fake node raised). Value identity, not authored prose. |
| C-078 | `cli/tests/test_resource_edit.py::test_cli_edit_launches_editor_on_manifest@0755a3` | report-line pin | delete | `"Editing secret/npm-token" in result.output` and `"secrets.yaml:1" in result.output`. The material fact, which file the editor was launched on and at what line, is asserted structurally one line above by `calls == [["test-editor", str(resources / "secrets.yaml")]]`. |
| C-079 | `cli/tests/test_resource_edit.py::test_cli_edit_requires_editor_env@5276e7` | message pin | convert | `"$EDITOR is not set" in result.output` is the only thing distinguishing this failure from an unrelated one: with `$EDITOR` set, the same invocation also fails with exit code 1, as a `NotFoundError` for `secret/x`. So `result.exit_code == 1` alone does not prove the `$EDITOR` gate fired. Replacement: assert the exception type, `isinstance(result.exception, SystemExit)`. No production change. |
| C-080 | `cli/tests/test_resource_edit.py::test_cli_edit_works_when_manifests_fail_validation@d5afe9` | report-line pins | convert | `"config is currently failing validation" in result.output` and `"prompt backend has no mapping vocabulary" in result.output` are the only witness that the tolerant fallback path was taken at all, and the fixture can rot silently: if `prompt: broken-on-purpose` ever validates, the strict path succeeds and the test stops testing the fallback. Replacement: assert the precondition directly with `pytest.raises(ConfigError)` on the strict registry build, and keep the argv equality. No production change. |
| C-081 | `cli/tests/test_resource_edit.py::test_fallback_miss_names_unreadable_files@6f1e69` | injected-value propagation | keep | `"broken.yaml" in str(result.exception)` names the file the test itself made unreadable, so an operator can find it. Value identity. |
| C-082 | `cli/tests/test_resource_edit.py::test_fallback_miss_names_unreadable_files@6f1e69` | **[unverified]** hint pin | delete | `"edit the file directly" in (result.exception.hint or "")` is authored remediation with no structural counterpart, sitting beside C-081's value-identity half in the same test. |
| C-083 | `cli/tests/test_resource_list.py::test_description_populated_for_operator_and_auto_resources@be1dbe` | authored display-prefix pin | delete | `ts.description.startswith("(auto) ")` pins a display convention on synthesized descriptions. `origin.variant == "auto-declared"` is the structural fact and is asserted three times elsewhere in the same file. |
| C-085 | `cli/tests/test_resource_list.py::test_cli_invalid_origin_filter_is_rejected@eee34e` | allowed-list echo | delete | `"operator" in str(result.exception)` asserts the rejection enumerates valid origins. The typed `ValidationError` asserted immediately above it is the invariant, and the enumeration is authored help text. |
| C-086 | `cli/tests/test_resource_kinds.py::test_table_shows_categories_and_counts@71861a` | table layout | keep | The `KIND`/`CATEGORY`/`RESOURCES` headers and the per-row `split()` positions assert the kinds table's shape and alignment over a synthetic fixture, which is the formatting carve-out. One count is derived from production (`str(len(SECRET_BACKEND_REGISTRY))`); the other, `["secret-source", "declarable", "2"]`, carries a hand-written `2`. That literal stays because deriving it would need a production handle for the built-in secret-source count, which R2.2 forbids adding; an executor should expect it to need updating when a built-in source is added. |
| C-087a | `cli/tests/test_resource_render.py::test_format_origin_line_handles_none@bb1aad,test_format_origin_line_operator_declared_with_file_and_line@6a5bbc,test_format_origin_line_operator_declared_without_file_returns_bare_label@2eae02,test_format_origin_line_auto_declared_with_source@3bccce,test_format_origin_line_built_in_with_source@e3d070,test_format_origin_line_system_plugin_with_source@d69f25,test_format_origin_line_system_plugin_without_source_returns_bare_label@f074fd,test_format_origin_line_system_plugin_without_plugin_degrades_gracefully@df3316` | formatter label equality | delete | **Ruled by the lead 2026-08-16** (formatter output splits on derivation): these pin the formatter's output as text (`== "unknown"`, `== "operator-declared"`, `.startswith("operator-declared (")`) with no canonical source behind the labels, so they are authored wording. The `Origin` variant contract itself keeps its own guard: `project_origin`'s variant checks were deleted and restored in PR #548 for exactly this reason. |
| C-087b | `cli/tests/test_resource_render.py::test_format_origin_line_operator_declared_with_file_and_line@6a5bbc` | origin data projecting into the rendered cell | keep | The other half of the same ruling. `.endswith(":42)")` proves the origin's file path and line number reach the rendered cell rather than being dropped, which is derivation from a canonical source: the `Origin` record the test builds (`Origin.operator_declared(file=Path("/tmp/config.toml"), line=42)`). Keep the projection, drop the label text around it. |
| C-088 | `cli/tests/test_list_truncation.py::_assert_truncated_and_aligned@3bdf64,test_vm_list_names_only_emits_full_name@22de9d,test_agent_list_names_only_emits_full_name@457240,test_workspace_list_names_only_emits_full_name@457240,test_console_list_names_only_emits_full_name@457240,test_agent_list_shows_legend_when_an_implicit_grant_is_marked@6a283f,test_agent_list_omits_legend_when_no_grant_is_marked@7d4b4c,test_agent_list_legend_matches_visibility_when_marker_is_truncated_away@2e9ce7` | column truncation and alignment | keep | Ellipsis placement, truncated prefix length, per-column caps, second-column offset equality across two rows, `--names-only` emitting the untruncated name in all four views, and the legend printing exactly when a marker is visible. Layout behavior over a synthetic fixture, and the `--names-only` half is the carve-out that stops a truncation change corrupting completion candidates. The old row's anchor list omitted the workspace and console tests; this one covers all four views. |
| C-089 | `cli/tests/test_output.py::test_count_pluralizes_regular_nouns@98228c,test_count_multiword_noun@b9e56f,test_count_irregular_plural@9bd5b2,test_render_table_headers_and_rule_line@894088,test_render_table_short_column_keeps_natural_width@1f22a3,test_render_table_caps_over_width_cell_with_ellipsis@a4ef15,test_render_table_exact_cap_cell_not_truncated@65aada,test_truncate_returns_fitting_text_unchanged@42d720,test_truncate_overflow_is_exactly_width_with_ellipsis@a81b45,test_truncate_degenerate_widths_never_overflow@28cd1a,test_render_table_degenerate_max_col_width@a7d20f,test_render_table_over_cap_column_pads_short_cells_to_cap@650ec6` | pluralization, truncation, table layout | keep | Pure formatting functions whose return value is the behavior, plus header, rule and column alignment; every string is the test's own input. |
| C-090 | `cli/tests/test_output.py::test_headerless_section_pushes_without_header@f48699,test_no_section_emits_at_column_zero@2dbd6b,test_primitives_capture_role_and_ambient_level@aa2f3b,test_detail_nests_via_headerless_section@83fd0c,test_style_status_delegates_to_current_handler@bc4fdf,test_result_always_reports_level_zero@125634,test_warn_from_thread_pool_worker_is_captured@d244d3,test_default_handler_body_byte_identical_at_level_zero@7808e1,test_default_handler_body_indents_with_level@ccf481,test_default_handler_detail_matches_legacy_indent@db3924,test_default_handler_warn_byte_identical_at_level_zero@36d180,test_default_handler_warn_indents_with_level@1b350e,test_default_handler_top_level_header@62bc3e,test_default_handler_header_decoration_by_level@d9ffbf,test_default_handler_result_renders_at_column_zero@72dc38,test_default_handler_prompt_byte_identical_at_level_zero@3df696,test_default_handler_prompt_indents_label_with_level@904580,test_default_secret_prompt_abort_is_context_free_user_abort@35f494,test_default_handler_style_status_never_colorizes@e1dbcc` | role, level and channel routing | keep | The assertions are about which role and indent level a line carries and which stream it lands on; the text is the test's own fixture. |
| C-091 | `cli/tests/test_typer_output.py::test_header_role_is_bold_on_a_tty@1024f7,test_warning_role_prefix_is_yellow_on_a_tty@98b790,test_error_role_prefix_is_red_on_a_tty@3efc42,test_result_role_is_dim_green_on_a_tty@c5bef1,test_detail_role_is_dim_on_a_tty@171631,test_body_role_is_never_colored_on_a_tty@d67aa5,test_style_status_good_is_green_on_a_tty@53d94f,test_style_status_warn_is_yellow_on_a_tty@36bfb7,test_style_status_bad_is_red_on_a_tty@80ad77,test_style_status_neutral_is_unstyled_on_a_tty@c1424d,test_style_status_returns_plain_text_off_tty@5be0be,test_style_status_returns_plain_text_under_no_color@5be0be,test_style_status_keeps_color_under_non_interactive@53d94f,_assert_all_roles_byte_plain@5262e5,test_no_color_env_forces_byte_plain_even_on_a_tty@e3b0c4,test_non_tty_stream_is_byte_plain@e3b0c4,test_non_interactive_does_not_change_tty_presentation@8a567b,test_confirm_resets_mouse_tracking_before_prompting_on_a_tty@f5bdd4,test_confirm_emits_no_escape_when_stream_is_not_a_tty@03a18b,test_confirm_emits_no_escape_under_non_interactive_even_on_a_tty@03a18b` | ANSI styling and plain-text parity | keep | Each case pins the styled bytes against `click.style` and the same line with ANSI stripped, plus the mouse-disable escape emitted before a confirm prompt: formatting and terminal behavior over fixture text. **Re-derived 2026-09-06 at `c310d05b`.** The same inversion as C-064, in the handler's own tests: non-interactive keeps color now. Both assertions moved with it and the keep is unchanged. |
| C-092 | `cli/tests/test_machine_output_cli.py::test_malformed_config_doctor_json_and_human_share_structured_facts@b1c193` | authored message inside a JSON check | delete | `"invalid config file" in message` pins wording; `status == "fail"` on the same check is the invariant. |
| C-093 | `cli/tests/test_machine_output_cli.py::test_config_failure_json_writes_no_stdout_before_service_work@0c25bf` | message pin | delete | `"Configuration error: configuration is invalid" in captured.err` pins wording; the assertions around it (nothing on stdout, no ANSI in stderr) are the boundary. |
| C-094 | `cli/tests/test_operational_json_boundaries.py::_assert_json_envelope_only@d08c33` | **[unverified]** chatter blacklist | delete | `b"Resolved " not in stdout` / `b"skipping " not in stdout` blacklist two human-path phrases someone already thought of; the envelope assertions in the same helper (single trailing newline, exactly one line, exactly the three envelope keys) already prove stdout is nothing but the document. Not executed: reasoned from the helper. |
| C-095 | `cli/tests/test_operational_json_boundaries.py::test_vm_describe_json_suppresses_the_ordinary_resolver_presentation@8235a7,test_session_describe_bypasses_activation_for_both_formats@b7f222,test_session_list_status_bypasses_provider_resolution_and_activation@51a3ad` | human-path sentence pins used as controls | convert | Replace `human.stdout_bytes.startswith(b"Name:")` and the `b"VM 'box' is stopped. Starting..."` line with `human.stdout_bytes != b""`. Their role is to show the human path emits what the JSON path must not. No production change. **Re-derived 2026-09-06 at `c310d05b`.** All three tests were renamed, and the convert this row proposes has already landed in two of its three sites: `assert human.stdout_bytes` is what they hold now, which is the non-empty control this row asked for. Only the third still pins, and it pins the token `b\"STATUS\"` rather than a sentence, so the convert that remains is smaller than the row was written for. **The replacement this row originally proposed is wrong and does not land.** `human.stdout_bytes != b""` is satisfied by the progress line alone, so it passes when the report body is empty, which is the regression the control exists to catch. The control must name the report body: a row token from the listing, or more than one line of output. The two sites that already converted to `assert human.stdout_bytes` carry the same defect and want the same narrowing. |
| C-096 | `cli/tests/test_operational_json_boundaries.py::test_vm_describe_json_suppresses_the_ordinary_resolver_presentation@8235a7,test_session_describe_bypasses_activation_for_both_formats@b7f222,test_session_json_never_enters_activation_around_the_envelope@ae245c,test_machine_presentation_suppression_keeps_prompts_interactive_on_stderr@67a95e` | sentinel redaction | keep | Platform secrets, boot ids and activation output injected by the test must not appear in the machine stream: security assertions wearing a blacklist's clothes. **Re-derived 2026-09-06 at `c310d05b`.** Both renamed, both still asserting the sentinel is absent from the machine stream. The keep is unchanged: these are security assertions wearing a blacklist's clothes, as the row says. |
| C-109 | `cli/tests/test_env_show.py::test_secret_redacted_by_default@757e49` | placeholder equality | keep | The unrevealed rendering must be a placeholder naming the secret rather than its value; this is `env show`'s redaction boundary, and the equality is what proves no value leaked into the cell. |
| C-110 | `cli/tests/test_env_show.py::test_secret_reveal_rejects_multiline_before_rendering@68df00,test_secret_reveal_guard_rejects_nul_before_building_render_input@745501` | verbatim error text in a value and a dict | convert | Replace with the structural pair: the failing key set (`set(errors) == {"shared-token"}`) and the resolved value absent from `rendered_value`. Keeps "which secrets failed" and the no-leak boundary; drops the sentence. No production change. |
| C-111 | `cli/tests/test_env_show_flag.py::test_help_shows_resolve@a03cfa` | help-text flag pin plus retired-spelling blacklist | delete | `"--resolve" in plain` / `"--reveal-secrets" not in plain` police rendered help for a flag whose behavior is asserted in `test_env_show_flag.py::test_resolve_flag_resolves` through the captured call. |
| C-114 | `cli/tests/test_error_wrapper.py::test_record_unhandled_error_writes_traceback_with_context@078e6c` | separator-form pin | delete | `"=" * 40 in text` pins the log file's decorative rule; the diagnostic content assertions around it are the invariant. |
| C-115 | `cli/tests/test_error_wrapper.py::test_record_unhandled_error_writes_traceback_with_context@078e6c,test_main_wrapper_catches_unhandled_exception@0d5767` | diagnostic capture | keep | The crash log must carry the argv, the exception line and a traceback: a real diagnostic contract with no other guard, and "Traceback" is Python's own output. |
| C-116 | `cli/tests/test_error_wrapper.py::test_main_wrapper_lets_vendored_abort_through@67da08,test_main_wrapper_maps_escaped_abort_to_exit_1@f76553` | message pin | delete | `"Aborted." in err` pins our abort wording; `"Traceback" not in err` (Python's own text) and the absence of an error log are the invariants. |
| C-117 | `cli/tests/test_error_wrapper.py::test_main_wrapper_renders_click_usage_error_cleanly@368524` | **[unverified]** our own label restated | delete | `"Error:" in err` restates the prefix already pinned as formatting in `test_error_wrapper.py::test_domain_error_renders_red_error_prefix_on_a_tty` and `test_error_wrapper.py::test_domain_error_renders_plain_off_tty`. |
| C-118 | `cli/tests/test_error_wrapper.py::test_main_wrapper_renders_click_usage_error_cleanly@368524` | upstream error text | keep | `"'bogus' is not one of 'comment', 'delete'." in err` is Click's own message reaching the operator without a traceback: the rule's outside-the-repository exception. |
| C-119 | `cli/tests/test_error_wrapper.py::test_domain_error_renders_red_error_prefix_on_a_tty@f093c7,test_domain_error_renders_plain_off_tty@d314ac` | styled-output equality | keep | The `Error:` prefix styled red on a TTY and plain off it, carrying the message the test injected: formatting plus value identity. |
| C-120 | `cli/tests/test_error_wrapper.py::test_create_session_surfaces_dead_workload_output_through_rollback@164a11` | message pin | convert | Replace `"exited immediately after launch (status 2)" in msg` with `"status 2" in msg` (the injected exit status). No production change. |
| C-121 | `cli/tests/test_error_wrapper.py::test_create_session_surfaces_dead_workload_output_through_rollback@164a11` | command-effect absence | keep | That no `server-access` or `chmod g+rwx` command ran during rollback asserts what the code did, not what it said. |
| C-122 | `cli/tests/test_operator_path_rendering.py::_assert_home_relative@e6f001,test_doctor_spells_every_path_on_one_screen_the_same_way@d94c94` | path-leak defense with a witness | keep | The file's whole invariant: no absolute `$HOME` path reaches operator-facing text, and the surface still renders a path so the guard has a subject. A leak defense over data, and the bug it caught was real. |
| C-123 | `cli/tests/test_operator_path_rendering.py::test_doctor_names_a_missing_config_file_home_relative@8024b4,test_doctor_names_an_unreadable_ssh_key_home_relative@06291c` | branch-witness sentences | convert | Replace `"not found" in rows["Config file"]` and `"not readable" in rows["SSH public key"]` with the check's status: `agentworks/doctor.py::_check_config` fails the missing-config row and oks the present one, and `agentworks/doctor.py::_check_ssh_key` fails an existing-but-unreadable key, so status plus the file's existence pins the branch. Keeping the check objects instead of flattening to messages is test-side only. |
| C-124 | `cli/tests/test_operator_path_rendering.py::test_the_pre_cli_config_load_failures_name_the_file_home_relative@4c4d55,test_config_edit_names_the_missing_file_home_relative@6910fb` | branch-witness sentences | delete | Each branch is fixed by the scenario the test sets up (config unlinked, invalid TOML written, `config edit` on a missing file), and each output is already asserted to render a `~/`-spelled path; the sentences add only wording. |
| C-125 | `cli/tests/test_operator_path_rendering.py::test_a_config_error_names_the_setting_the_way_the_operator_wrote_it@202091` | setting name plus sentence | convert | Replace with `"operator.ssh_public_key" in message`: naming the setting the way the operator wrote it is the invariant the test's own name states. No production change. |
| C-126 | `cli/tests/test_name_filter_validation.py::test_unknown_vm_error_shape@98fe86,test_repeated_unknown_name_reported_once@d9e485` | error-sentence and hint pins | delete | `entity_kind`/`entity_name` are asserted on the same exception (`assert excinfo.value.entity_kind == "vm"`, `assert excinfo.value.entity_name == "wf-test"`), so the sentence adds nothing, and `"agw vm list" in hint` is authored remediation. |
| C-127 | `cli/tests/test_name_filter_validation.py::test_multiple_unknown_names_all_reported@926118,test_repeated_unknown_name_reported_once@d9e485` | value identity and non-duplication | keep | That the message names every unknown filter value, does not name the valid one, and names each unknown exactly once are data-level properties of the aggregation. |
| C-128 | `cli/tests/test_name_filter_validation.py::test_multiple_unknown_names_all_reported@926118,test_repeated_unknown_name_reported_once@d9e485` | singular-versus-plural wording | delete | `"unknown workspaces" in message` and `"unknown VMs" not in message` police pluralization: a sentence-only observable (R2.4). |
| C-129 | `cli/tests/test_name_filter_validation.py::test_session_list_valid_filter_empty_result_succeeds@d0a94f,test_stop_all_sessions_valid_filter_empty_result_succeeds@80ade0,test_list_commands_valid_filter_empty_result_succeeds@41ff69,test_agent_filter_valid_for_agent_with_no_sessions@d0a94f` | empty-state message pins | convert | Rubric re-check, 2026-08-16. **The row's justification was inaccurate**: five of these six sites are the only assertion in their test, so the delete leaves assertion-free tests. Convert each to a channel assertion on the captured output. No production change. **Re-derived 2026-09-06 at `c310d05b`.** The `resume` test became `test_start_all_sessions_valid_filter_empty_result_succeeds` and lost both its `captured_output` fixture and its assertion, so it now asserts nothing at all. That is the outcome this row's convert exists to prevent, already realized by another effort; it is recorded in the open questions as an operator finding rather than fixed here. The convert stands for the row's four surviving sites. |
| C-136 | `cli/tests/test_ssh_config.py::test_sync_ssh_config_announce_controls_the_synced_line@d37444` | message pin | convert | Rubric re-check, 2026-08-16. `assert "SSH config synced" in captured_output.info` is the only witness of the announcing arm; the test exists to prove `announce` CONTROLS the line, and `assert captured_output.info == before` carries only the silent arm. Capture the channel before and assert it grew. No production change. |
| C-139 | `cli/tests/test_ssh_input.py::test_ssh_run_translates_sensitive_native_failure_without_exception_link@ca53bb,test_ssh_run_sensitive_timeout_drops_partial_output_and_native_exception@5ee00d` | verbatim error equality | convert | Replace with the exception type plus the injected command substring (`"cat > /tmp/template.yaml" in str(caught.value)`); the sentence around it is ours. No production change. |
| C-140 | `cli/tests/test_ssh_logger.py::test_close_appends_traceback_when_called_inside_except@38f6c6,test_close_without_exception_omits_traceback_block@f8c3c2,test_ssh_logger_structurally_satisfies_bootstrap_progress@a98342,test_path_retargeting_moves_subsequent_records@3612ed` | log-structure markers | keep | `EXCEPTION:` and `# Finished:` are the log file's own structure: their absence means a truncated or silently swallowed run, which is what the log exists to reveal. |
| C-141 | `cli/tests/test_ssh_logger.py::test_write_sink_sanitizes_every_persistent_surface@d195b6,test_warning_text_is_sanitized_before_durable_logging@774a3e,test_redactions_cannot_be_registered_after_writes@4aa8ff,test_shell_quoted_secret_is_redacted_from_command@3921ca,test_close_redacts_secrets_from_traceback@47d676` | redaction defense | keep | Secret values may appear only as `[REDACTED]` in the durable log, including on the retry and warning paths, plus the occurrence threshold. Security, not prose. |
| C-143 | `cli/tests/test_admin_home_permissions.py::test_harden_admin_home_shared_primary_group_warns@8846be,test_harden_admin_home_private_primary_group_is_quiet@34e77b,test_harden_admin_home_ssh_error_warns_not_raises@4699e2` | warning wording pins | convert | Rubric re-check, 2026-08-16. Verified by execution on a scratch copy: the shared-group and SSH-error arms emit exactly one warning each and the healthy arm none, so the warn-versus-quiet discrimination, which is security-relevant (a `0750` home readable by a shared primary group), converts exactly to a warning count. No production change. |
| C-152 | `cli/tests/test_initializer_reconcile.py::test_apply_sve_mask_installs_dropin_as_root_and_warns_restart@0cf296` | **[unverified]** warning sentence pin | delete | `"Restart the VM and reinit" in warned` pins remediation prose; the same test already asserts the warning names the kernel flag that changed. |
| C-153 | `cli/tests/test_initializer_reconcile.py::test_apply_sve_mask_warns_and_stops_on_update_grub_failure@8dfe0b` | warning sentence pin | delete | **Recipe verification, 2026-08-16: the recorded conversion is vacuous, so the row becomes a delete.** Executed both branches of `test_initializer_reconcile.py`: the SVE-mask failure branch emits one warning and the SUCCESS branch also emits one warning, so `warnings != []` and `len(warnings) == 1` both pass when the behavior regresses. The discriminator that actually works is already in the test: `assert not any("/proc/cmdline" in c[0][0] for c in target.run.call_args_list)`, which is `False` in the failure branch and `True` in the success branch, and `logger.warning.assert_called_once()` stays with it. Nothing is lost by deleting `assert "update-grub failed" in "\n".join(warnings)`. This is the row that produced the screening question now recorded with the recipes: does the HEALTHY branch emit zero warnings? |
| C-155 | `cli/tests/test_initializer_env_fragments.py::test_sshd_accept_env_restores_prior_file_on_validation_failure@cf8f71,test_sudoers_env_keep_rejects_on_visudo_failure@d07e20` | warning keyword pins | convert | Replace `any("reinit" in w.lower() ...)` and `any("visudo" in w ...)` with `logger.warnings != []`. Both tests already assert the structural outcome (the backup moved, the staged sudoers file not installed); what remains is that the operator is told. No production change. |
| C-156 | `cli/tests/test_initializer_hardening.py::test_vm_initialization_step_and_subresult_roles@136702` | progress-line pins | convert | Rubric re-check, 2026-08-16. These tuples are not sentence pins so much as the only probe of output ROLE and nesting LEVEL (`Role.BODY` at level 1 for steps, `Role.DETAIL` for the sub-result), which is the bug the comment inside that test names. Assert the role-and-level sequence from `captured_output.lines`. No production change. |
| C-159 | `cli/tests/test_tailscale_dns.py::test_check_warns_when_dns_fails_and_resolv_unreadable@7da6dc,test_check_warns_when_dns_fails_but_resolv_not_tailscaled@7da6dc` | keyword-in-warning | delete | `"DNS" in warning_text` asserts one word appears; the structural facts (which probe commands ran and which did not) are asserted beside it. |
| C-160 | `cli/tests/test_tailscale_dns.py::test_check_warns_when_resolved_not_active@5067a6` | disjunction of authored phrasings | delete | `"latched" in w or "issue #117" in w` and `"not implemented" in w or "No heal" in w` are a phrase blacklist's mirror image: two spellings someone already thought of, with a third sailing through. |
| C-161 | `cli/tests/test_tailscale_dns.py::test_check_raises_state_error_with_heal_hint@cdfbb2` | hint command pins | keep | Rubric re-check, 2026-08-16. Scoped to `assert "vm shell --platform" in hint`. The three heal-command asserts beside it (`assert "systemctl stop tailscaled" in hint`, `assert "ln -sf /run/systemd/resolve/stub-resolv.conf" in hint`, `assert "systemctl start tailscaled" in hint`) still delete, their only failure mode being a hint edit. The `vm shell --platform` assert keeps: the comment above it records a real prior regression, a simple-block hint that locked the operator out of the very Tailscale-SSH session running the first heal command, and nothing else probes it. |
| C-163 | `cli/tests/test_vm_cli_surface.py::test_vm_create_template_override_flags_removed@93eebd,test_vm_create_platform_flag_removed@e79074,test_vm_shell_provisioner_alias_is_removed@48a730,test_vm_shell_help_only_lists_platform@4f5929` | help-text flag pins and retired-flag blacklist | delete | `--platform` and `--provisioner` presence and absence in rendered help police the help surface; what backstops the flags' behavior is the captured call, pinned as `assert captured == {}` in `test_vm_cli_surface.py::test_vm_shell_provisioner_alias_is_removed`. Two of this row's four anchors drive `vm shell` rather than `vm create`, and `test_vm_cli_surface.py::test_vm_shell_help_only_lists_platform` asserts exit 0 on the help itself rather than a refusal. |
| C-164 | `cli/tests/test_vm_cli_surface.py::test_doctor_vm_sites_defers_on_pending_migration@363dff,test_doctor_system_group@e49a41,test_doctor_vm_sites_not_ready_and_preflight_rows@61c0af` | doctor message wording | delete | "pending database migration", "will ask", "declined" and "preflight" are authored row text; the check's status discriminates each branch. |
| C-165 | `cli/tests/test_vm_cli_surface.py::test_doctor_system_group@e49a41,test_doctor_vm_sites_group@903e7a,test_doctor_vm_sites_not_ready_and_preflight_rows@61c0af` | data projections into a row | keep | Derivation from a canonical source, which the lead's 2026-08-16 formatter ruling keeps and asks to be named: the source is the finalized registry plus the platform's own readiness verdict, and each assertion proves that value (the injected site, the platform, the placement, the missing tool) reaches the row an operator acts on. The reason text is produced by the platform, not authored here. |
| C-166 | `cli/tests/test_vm_cli_surface.py::test_doctor_vm_sites_group@903e7a` | **[unverified]** hint form pin | delete | `"name: gone-box" in hint` pins a YAML fragment's spelling inside a hint; the same test already asserts the stranded VM is named. |
| C-167 | `cli/tests/test_vm_cli_surface.py::test_doctor_warns_on_references_to_not_ready_sites@c56195` | reason propagation | keep | Same ruling, same canonical source: one readiness reason produced by the platform must reach both the VM row and the default-site row. The test proves the derived copies agree with it, which is propagation between surfaces rather than a fixed sentence. |
| C-168 | `cli/tests/test_vm_shell_platform.py::test_shell_vm_raises_when_no_tailscale_host_and_no_platform_flag@e23d4a,test_native_transport_wraps_missing_native_transport@d70152,test_native_transport_proxmox_hint_points_at_web_console@97b49c,test_native_transport_raises_defensively_on_empty_host@e08e51,test_shell_vm_warns_but_continues_on_failed_init@f28583,test_shell_vm_still_raises_on_failed_provisioning@4480ca,test_exec_vm_warns_but_continues_on_failed_init@79f8a8` | error and hint sentence pins | convert | Rubric re-check, 2026-08-16. The only evidence the warn path fired: a silent `allow_failed_init` softening would pass, and no other site in `cli/tests` touches it. Convert to a warning-count assertion. No production change. |
| C-169 | `cli/tests/test_remote_exec.py::test_stale_status_cleared_when_reuse_disabled@5a7c9d` | warning sentence pin | convert | Rubric re-check, 2026-08-16. `target.write_file.assert_called_once()` proves a fresh run started but nothing witnesses that the stale state was REPORTED, and the docstring's contract is both. Assert the warning channel is non-empty. No production change. |
| C-170 | `cli/tests/test_remote_exec.py::test_run_detached_fresh_start@9ddb29,test_polling_survives_transient_ssh_failures@eeaf1a,test_run_detached_timeout_kills_and_returns_partial@3136a6` | injected-output propagation | keep | The strings are what the fake target emitted, so asserting they reach `result.output` proves the streaming path carries remote output, including after recovery and on partial failure. |
| C-171 | `cli/tests/test_workspace_rooted_shells.py::test_exec_vm_rejects_dash_prefixed_command@d4e353,test_exec_agent_rejects_dash_prefixed_command@32de86` | hint wording pins | delete | Five authored fragments telling the operator where to put `--`; the exit code and the unparsed argv assertions carry the behavior. |
| C-173 | `cli/tests/test_azure_create_interrupt.py::TestInterruptDuringInlineWait.test_vm_delete_failure_during_rollback_warns_and_reraises@df961b` | injected-value propagation | keep | The VM name, resource group and the injected `vm delete boom` failure text must reach the may-remain report: without them an operator cannot finish the cleanup by hand. |
| C-176 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_rbac_denial_raises_authorization_error@49ce1b,TestSurvivingVMRaises.test_denied_probe_after_rbac_delete_failure_raises_authorization_error@1e53e3,TestSurvivingVMRaises.test_denied_probe_alone_raises_authorization_error@d87420` | authored preamble and success-line pins | delete | **Deduplicated 2026-08-16.** Two inventory lanes both covered this file, a scoping error in the fan-out rather than a disagreement; the platform lane's rows are kept because they are finer-grained and this file was its assignment. Narrowed to the three sites the platform lane did not row; E-166, E-167, E-168, E-169 and E-170 own the others. These three are fixed preamble text whose only failure mode is a rewording. |
| C-179 | `cli/tests/test_azure_nsg_exposure.py::TestCreate.test_create_fails_typed_when_detection_fails_and_no_extras@f0bff8,TestPrefixAssembly.test_detection_failure_without_extras_is_typed_error@16c57c` | authored narration pins | delete | **Deduplicated 2026-08-16.** Two inventory lanes both covered this file, a scoping error in the fan-out rather than a disagreement; the platform lane's rows are kept because they are finer-grained and this file was its assignment. Narrowed: the compound warning-phrase predicate in `test_azure_nsg_exposure.py::TestPrefixAssembly.test_detection_failure_with_extras_proceeds_with_warning` moves to E-172, which the re-check flips to convert. This row's two remaining anchors are fixed narration with no other failure mode; **that they are what the narrowing left is inferred from the row's structure**, the two numbers it named resolving to nothing at any tree this map can reach. |
| D-043 | `cli/tests/manifests/test_samples.py::test_capability_kinds_report_no_sample@9cff20` | two authored-phrase pins inside a real test | delete | Drop `"capability kind" in str(err)` and `"no sample manifest" in str(err)`. Keep `kind in str(err)` (value identity on the looped kind), `err.hint is not None`, and the loop asserting every name in `declarable_kinds()` appears in the hint, which is derived from production and is the invariant that can actually regress. |
| D-047 | `cli/tests/manifests/test_samples.py::test_sample_capability_kind_is_a_clean_cli_error@669c62` | full-sentence pin of a CLI error line | delete | `"'harness-integration' is a capability kind; it has no sample manifest" in err`. The test's contract is exit code 1, no traceback escaping, and no `error.log` written, all of which are asserted beside it and all of which survive (F-174 covers the traceback half). The sentence adds nothing the diff did not show. |
| D-066 | `cli/tests/manifests/test_describe_kind.py::test_secret_source_kind_describes_the_backend_union_and_override_provenance@b82487,test_a_kind_points_at_the_sample_that_renders_the_same_fields@bbc88a` | pointer and title pins | convert | The real invariant is that a kind's page points at the sample command for THAT kind; assert the pointer is built from the target (`f"agw resource sample {kind}"` derived from the loop variable) rather than spelled per kind. |
| D-081 | `cli/tests/manifests/test_describe_kind.py::test_unknown_kind_hint_enumerates_every_kind_in_sorted_order@04e8f1` | hint enumeration, derived | keep | Builds the expected kind list from `KIND_REGISTRY` and asserts the hint enumerates exactly it in sorted order. Derivation parity against a canonical source; a kind added to the registry and missing from the hint fails. |
| D-082 | `cli/tests/manifests/test_describe_kind.py::test_describe_kind_is_a_clean_cli_error_for_an_unknown_target@d546b0` | CLI contract plus one sentence pin | delete | Drop `assert "unknown kind 'vm-platfrom'" in err`. Keep `assert excinfo.value.code == 1` and `assert "Traceback" not in err`, which is the interpreter's own output and the thing separating a domain error from an unexpected one. |
| D-092 | `cli/tests/manifests/test_schema_command.py::test_a_bad_kind_is_a_clean_domain_error@2b2176` | parametrized phrase pins plus a wording blacklist | convert | Two cases assert `expected in err` and `hint in err` for authored fragments ("unknown kind"/"known kinds:", "capability kind"/"declarable kinds:"), then `"full traceback written to" not in err`. Replacements, both structural: assert the ARGUMENT the operator typed appears in stderr (value identity, and it is what distinguishes the two cases' inputs), and replace the traceback blacklist with the file check the phrase stands for, `monkeypatch.setattr("agentworks.config.CONFIG_DIR", tmp_path)` before asserting `not (tmp_path / "logs" / "error.log").exists()`. Cost: the two cases stop proving which of the two refusal branches ran, which is `hla.md` case 2's fallback. No production change. |
| D-096 | `cli/tests/manifests/test_contributed_validation.py::test_the_shared_diagnostic_is_the_good_one@3a6484` | phrase pins on a shared diagnostic | keep | `"key_dearmor"` is the field the test misspells and `error.hint` carrying the sample pointer is the same derived-pointer invariant as D-066. The load-bearing claim (a contributed bundle gets the SAME diagnostic an operator gets) is asserted structurally by `assert contributed.hint == operator.hint`, which is derivation parity and the strongest thing here. |
| D-103 | `cli/tests/manifests/test_spec_apt.py::test_every_rejection_points_at_the_sample_surface@03feda` | hint equality | convert | The pointer discipline is real; derive it (`f"'agw resource sample {kind}' prints this kind's fields"` from the kind under test, or better, assert every kind's rejection hint names that kind's own sample command) instead of spelling one kind's sentence. |
| E-005 | `cli/tests/resources/test_inherited_reference_attribution.py::test_a_miss_on_an_inherited_name_blames_the_template_that_wrote_it@9d1662` | miss-message substring carrying injected identities | keep | Invariant: an inherited-name miss is blamed on the template that WROTE it (`base`), not the one that inherited it; `base`/`nope` are test-injected. Narrow the assertion to those identities and drop the surrounding authored sentence frame. |
| E-008 | `cli/tests/resources/test_inherited_reference_attribution.py::test_reference_formatter_says_an_inherited_reference_was_inherited@cbaeb7` | rendered-line equality | convert | Replace with assertions on the `ReferenceEntry` fields the renderer reads (`entry.source`, `entry.declared_by`; see cli/agentworks/resources/render.py::sanitize_fact_line): the inherited edge carries `declared_by == ("vm-template", "base")` and the declarer's own edge carries none. No production change. |
| E-009 | `cli/tests/resources/test_inherited_reference_attribution.py::test_a_row_that_declared_the_name_itself_is_blamed_for_it@db2d60` | miss-message substring carrying injected identities | keep | The override case: `kid` declared the name itself and must be blamed. Same narrowing as E-005. |
| E-015 | `cli/tests/resources/test_registry_collisions.py::test_operator_over_operator_errors_with_both_locations@296740` | authored phrase substring | delete | `"duplicate secret"` is our own error wording; `pytest.raises(ConfigError)` plus the two location assertions below already carry the collision claim. |
| E-016 | `cli/tests/resources/test_registry_collisions.py::test_operator_over_operator_errors_with_both_locations@296740` | injected file:line coordinates in a message | keep | Invariant: an operator-over-operator collision names BOTH origins with their real file and line. The coordinates come from this test's own `_operator(1)` / `_operator(9)` origins, so this is value identity, and losing it would let a collision report one location. |
| E-017 | `cli/tests/resources/test_registry_lifecycle.py::test_error_miss_policy_includes_reference_usage@565637` | miss-message substrings carrying injected values | keep | The kind, the missing name, and the reference usage are all defined by this test's `_RefEmitter`; the invariant is that a finalize miss names the unknown target AND the usage that asked for it. |
| E-019 | `cli/tests/plugins/test_aws.py::test_site_on_disabled_aws_is_not_ready_with_hint@6839fb,test_resolve_site_refuses_disabled_aws_with_hint@35fa63` | enable-hint substring | delete | Both sit beside the structural claim (`not is_ready(...)` / `pytest.raises(StateError)`); `"enable plugin 'aws'"` is authored remediation wording. |
| E-020 | `cli/tests/plugins/test_aws.py::test_doctor_roster_lists_the_aws_plugin@21e41b` | doctor-message substring | delete | `row.status is Status.INFO` / `Status.OK` on the adjacent lines is the enablement claim; the message text adds only wording. |
| E-022 | `cli/tests/plugins/test_azure.py::test_site_on_disabled_azure_is_not_ready_with_hint@9a3a59,test_resolve_site_refuses_disabled_azure_with_hint@823cb7,test_credential_on_disabled_azdo_is_not_ready_with_hint@5da01d,test_recipe_gate_refuses_disabled_az_cli_with_hint@077700` | enable-hint substring (four one-line tests, one idiom) | delete | Each sits beside `not is_ready(...)` or `pytest.raises(StateError)`; the hint sentence is authored remediation. |
| E-023 | `cli/tests/plugins/test_azure.py::test_resolve_git_credential_refuses_disabled_azdo@e4184a` | authored phrase substring | delete | `"not ready" in str(exc.value)` is our own wording; the `StateError` type already discriminates the refusal. |
| E-025 | `cli/tests/plugins/test_azure.py::test_doctor_roster_lists_the_azure_plugin@21e41b` | doctor-message substring | delete | Same as E-020; `Status.INFO` / `Status.OK` carry it. |
| E-027 | `cli/tests/plugins/test_claude.py::test_ensure_harness_integration_enabled_refuses_disabled_claude_code_with_hint@8098b2,test_recipe_gate_refuses_disabled_claude_install_with_hint@b77092` | enable-hint substring | delete | Structural siblings are the raised `StateError` and the disabled enablement mark. |
| E-029 | `cli/tests/plugins/test_claude.py::test_doctor_roster_lists_the_claude_plugin@21e41b` | doctor-message substring | delete | Status assertions carry it. |
| E-031 | `cli/tests/plugins/test_codex.py::test_ensure_harness_integration_enabled_refuses_disabled_codex_with_hint@c829ce,test_recipe_gate_refuses_disabled_codex_install_with_hint@2df2f1` | enable-hint substring | delete | Same as E-027. |
| E-033 | `cli/tests/plugins/test_codex.py::test_doctor_roster_lists_the_codex_plugin@21e41b` | doctor-message substring | delete | Status assertions carry it. |
| E-035 | `cli/tests/plugins/test_enablement_producer.py::test_mapping_to_an_absent_backend_reports_the_dangling_edge_not_a_shape_error@6335e1` | wording blacklist | convert | Rubric re-check, 2026-08-16. A structural handle the row assumed away: the shape error chains a pydantic `ValidationError` while the dangling-edge raise chains nothing, so assert `exc.value.__cause__ is None`. No production change. |
| E-039 | `cli/tests/plugins/test_gcp.py::test_disabled_gcp_site_is_not_ready_and_refused_with_enable_hint@42f48d,test_doctor_roster_tracks_gcp_enablement@cc67a8` | enable-hint and doctor-message substrings | delete | Same as E-019/E-020 for gcp. |
| E-040 | `cli/tests/plugins/test_manifest_parity.py::test_ensure_reference_enabled_refuses_disabled_row_with_enable_hint@df3884,test_ensure_recipe_enabled_refuses_disabled_contribution_in_closure@6d4557` | enable-hint substring | keep | **Corrected 2026-08-19 (map re-baseline).** Was delete, and it contradicted G1-K03, which keeps the identical expression at `test_manifest_parity.py::test_ensure_recipe_enabled_excludes_capability_nodes` of this same file. Resolved toward the keep: `f"enable plugin`{{PLUGIN}}`"` interpolates the fixture constant naming the disabled plugin, so all three sites prove the refusal names the plugin the operator must enable rather than a fixed sentence. The row also already kept the neighboring fixed literals (`assert "user-install-command" in message` and `assert "fixture-user-cmd" in message` in `test_manifest_parity.py::test_ensure_reference_enabled_refuses_disabled_row_with_enable_hint`, and `assert "fixture-user-cmd" in message` in `test_manifest_parity.py::test_ensure_recipe_enabled_refuses_disabled_contribution_in_closure`: contribution kind and name, value identity on the call's own arguments), so keeping the enable-hint assert in each of them is what makes the row internally consistent as well. |
| E-041 | `cli/tests/plugins/test_proxmox.py::test_site_on_disabled_proxmox_is_not_ready_with_hint@bad0bf,test_resolve_site_refuses_disabled_proxmox_with_hint@521c9a,test_doctor_roster_lists_the_proxmox_plugin@21e41b` | enable-hint and doctor-message substrings | delete | Same idiom as E-019/E-020 for proxmox. |
| E-044 | `cli/tests/plugins/test_publish.py::test_bad_plugin_manifest_anchor_raises_typed_plugin_attributed_error@dcbdd5,test_plugin_manifest_anchor_without_subdir_raises_typed_plugin_attributed_error@1c0151` | authored phrase substrings | convert | Rubric re-check, 2026-08-16. **The row's 'R2.2 forbids adding a discriminator' premise was wrong**: the handle already exists. `manifests/package.py::load_manifest_package` raises `f"manifest package {anchor!r}/{subdir} could not be resolved: {exc}"` with `from exc` and the raise below it (`f"plugin manifest package {anchor!r} declares manifests but ships no {subdir!r} "`) without, so `isinstance(__cause__, ImportError)` versus `__cause__ is None` discriminates. No production change. |
| E-045 | `cli/tests/plugins/test_publish.py::test_plugin_manifest_anchor_without_subdir_raises_typed_plugin_attributed_error@1c0151` | wording blacklist | delete | `"no-op" not in message` tries to enforce "loud, not silent" by forbidding one word. The loudness is already proven by `pytest.raises(ConfigError)`. |
| E-047 | `cli/tests/plugins/test_surfaces.py::test_list_renders_from_plugin_provenance@bd502f` | rendered-cell substring carrying an injected plugin name | keep | Invariant: the rendered DESCRIPTION cell attributes a plugin row to its plugin, which is the operator's only provenance signal in `resource list`. `alpha` is the fixture's name. |
| E-048 | `cli/tests/plugins/test_surfaces.py::test_include_disabled_render_marks_disabled_and_not_ready_distinctly@e7a600` | rendered state-marker presence and absence | keep | Invariant: disabled, not-ready-but-enabled, and ready rows render three distinct signals per row rather than only in the count line. This is synthetic-fixture rendering (the absorbed FRD's R5 carve-out); the marker text is authored, and a deliberate rename is the accepted cost. |
| E-049 | `cli/tests/plugins/test_surfaces.py::test_full_fixture_end_to_end@8da91e,test_roster_lists_enabled_ok_and_disabled_info@5125fd,test_default_config_builds_green_with_zero_enabled_system_plugins@795ebd` | doctor-message substrings | delete | Each sits beside the `Status` assertion that carries the enablement claim. |
| E-050 | `cli/tests/plugins/test_surfaces.py::test_roster_lists_enabled_ok_and_disabled_info@5125fd,test_roster_empty_index_renders_present_but_empty@bf5c83` | authored group-title equality | delete | `group.name == "System plugins"` pins a heading; `_check_plugins` returns exactly one group, so nothing selects on it. |
| E-052 | `cli/tests/plugins/test_surfaces.py::test_roster_empty_index_renders_present_but_empty@bf5c83` | authored empty-state sentence equality | delete | Keep the structural presence (one check in the group); the sentence itself is prose. |
| E-054 | `cli/tests/plugins/test_surfaces.py::test_roster_required_scopes_inert_when_empty@5df3a6` | phrase blacklist over check names | convert | The claim is that no scope row is emitted for a plugin with no reserved scopes. Replace with a count/identity assertion over `checks` (the bare plugin yields exactly its roster row), which does not depend on the row's title. No production change. |
| E-056 | `cli/tests/plugins/test_proxmox_bootstrap_staging.py::test_write_failure_is_safe_and_removes_private_stage@d63f15,test_standalone_removal_failure_surfaces_and_models_residue@1dd2ad` | authored error-message equality | delete | The typed raise plus the residue assertions carry both cases; the sentence is our own wording. |
| E-057 | `cli/tests/plugins/test_proxmox_bootstrap_staging.py::test_removal_failure_does_not_mask_primary@2046cf,test_removal_control_flow_does_not_mask_primary@8237c2` | verbatim warning list equality | convert | The real invariant is "warn exactly once and never mask the primary". Replace the list-of-sentences equality with `len(warnings) == 1`, keeping the existing `caught.value is failure` and the secret-absence check. No production change. |
| E-058 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestInterruptDuringBootstrapWait.test_stops_and_deletes_the_cloned_vm_and_reraises@71aa31` | **[unverified]** warning-phrase presence/absence | delete | The path is already pinned structurally in the same test: `("stop_vm", ...)` and `("delete_vm", ...)` in `fake.calls`, the stop-before-delete index check, and `"stop_task" not in fake.ops()`. |
| E-059 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestInterruptDuringBootstrapWait.test_second_interrupt_abandons_cleanup_loudly@df6e37` | phrase-selected warning plus injected coordinates | delete | **Was a convert; executing the test flipped it.** `test_second_interrupt_abandons_cleanup_loudly` emits TWO warnings, not one, so a count selector picks the wrong one, and both warnings carry `Proxmox VM 100 on node 'pve1'`, so the injected coordinates do not select either. The only discriminator is our own `"Cleanup abandoned:"` versus `"Interrupted: cleaning up..."`, which R2.4 resolves by deletion. The abandon behavior survives structurally: `exc.value is interrupt`, `"delete_vm" not in fake.ops()`, `"stop_task" not in fake.ops()`. |
| E-060 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestFailClosedBootstrap.test_cloud_init_timeout_rolls_back@c21ff8` | **[unverified]** authored error-message equality | delete | `pytest.raises(ProvisioningError, match=...)` is group 1's; the full-sentence equality here restates our own timeout wording, and the rollback behavior is asserted structurally below. |
| E-061 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestFailClosedBootstrap.test_bootstrap_failure_cleanup_survivor_keeps_primary_and_manual_guidance@1cea2a` | phrase-selected warning plus injected coordinates | convert | Same treatment as E-059: keep the `VM {_NEWID}` / `node 'pve1'` identity, drop the `"delete it there manually"` remediation sentence, and select the warning by count. |
| E-062 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestEscalationFromFailureRollback.test_interrupt_during_the_failure_arms_rollback_escalates@6e457f` | warning-phrase presence | delete | The escalation is proven by the raised interrupt identity and the recorded API calls. |
| E-063 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestOrphanBackstopWarning.test_interrupt_path_warns_when_the_vm_survives_the_delete@20d7c5,TestOrphanBackstopWarning.test_failure_path_warns_when_the_vm_survives_the_delete@650a9a,TestTransportFailureDuringRollback.test_interrupt_path_absorbs_it_with_the_abandon_warning@793e27,TestTransportFailureDuringRollback.test_interrupt_path_probe_blip_still_warns_via_containment@793e27` | phrase-selected warning (`may remain` / `Cleanup abandoned` / `Rollback incomplete`) | convert | Rubric re-check, 2026-08-16. **The row's stated sibling does not exist in these four tests**: with the warning legs gone they assert nothing but `exc.value is ...`, and the class-body comment above `test_proxmox_create_interrupt.py::TestTransportFailureDuringRollback.test_failure_path_probe_blip_still_warns_via_containment` records that a swallowed containment produces zero warnings. Convert to warning counts, verified against the real counts (proxmox abandon emits 2). No production change. |
| E-064 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestTransportFailureDuringRollback.test_failure_path_warns_and_reraises_the_original_error@a6d01c,TestTransportFailureDuringRollback.test_failure_path_probe_blip_still_warns_via_containment@a6d01c` | compound warning-phrase predicate | convert | Rubric re-check, 2026-08-16. Same as E-063 for the remaining two tests. No production change. |
| E-065 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestPlainFailure.test_failure_mid_create_cleans_up_and_reraises_the_typed_error@04b2a3` | **[unverified]** detail/warning phrase presence and absence | delete | The plain-failure path is fully pinned structurally in the same test (`("delete_vm", ...) in fake.calls`, `"stop_vm" not in fake.ops()`, template untouched). |
| E-067 | `cli/tests/plugins/gcp/test_auth.py::test_service_account_remediation_accepts_downloaded_json_without_compaction@667a0c` | hint required-phrase pair | delete | Authored remediation wording; the `ProvisioningError` raise carries the refusal, and E-066 carries the behavior the wording talks about. |
| E-068 | `cli/tests/plugins/gcp/test_compute.py::test_known_live_machine_incompatibility_is_actionable_detached_config_error@6c9212` | hint required-phrase list | delete | Four sentences of authored guidance beside a `ConfigError` raise; the discriminating value (`caller-authored-type`) is injected and stays. |
| E-069 | `cli/tests/plugins/gcp/test_compute.py::test_exact_instance_collision_and_not_found_paths@2f9a90` | required-phrase pair plus wording blacklist | delete | `"Do not delete the instance by name"` is authored safety prose and `"remove" not in hint.lower()` is a blacklist that any rephrasing walks through. `AlreadyExistsError` is the discriminator that survives. |
| E-070 | `cli/tests/plugins/gcp/test_errors.py::test_done_http_503_capacity_is_definitive_typed_safe_and_uses_no_refresh@9063cd` | hint equality | delete | `GCECapacityError` already discriminates the branch and `_ZONE in str(caught.value)` carries the zone-naming claim. |
| E-071 | `cli/tests/plugins/gcp/test_errors.py::test_global_operation_capacity_guidance_does_not_claim_the_vm_zone@a4624a` | hint equality | delete | The real invariant in this test is the line above it (`_ZONE not in f"{caught.value} {caught.value.hint}"`, no false zone attribution for a global operation), which stays. |
| E-072 | `cli/tests/plugins/gcp/test_errors.py::test_non_done_timeout_is_the_only_indeterminate_operation_outcome@cc584a,test_non_done_provider_poll_failure_is_indeterminate_and_safe@55fcb7` | hint substring | delete | `GCEIndeterminateOperationError` is the discriminator; the "inspect the named resource" sentence is authored. |
| E-074 | `cli/tests/plugins/gcp/test_instance.py::test_done_capacity_failure_never_reconciles_matching_instance_to_success@4a23dc` | hint substring | delete | `GCECapacityError` plus `[name for name, _ in client.calls] == ["insert"]` carry the branch and the no-retry claim. |
| E-075 | `cli/tests/plugins/gcp/test_instance.py::test_residual_definitive_insert_rejection_has_fixed_machine_disk_guidance@208a94` | hint required-phrase list | delete | Authored guidance beside `type(caught.value) is GCEOperationError` and the injected `caller-authored-type`, both of which stay. |
| E-076 | `cli/tests/plugins/gcp/test_instance.py::test_residual_definitive_insert_rejection_has_fixed_machine_disk_guidance@208a94` | sentence-order pin via `.index()` | delete | This pins that one authored sentence precedes another inside a hint. hla.md deletes lexical order pins outright; there is no behavior behind it. |
| E-077 | `cli/tests/plugins/gcp/test_network.py::test_firewall_name_collision_and_absence@ef65a6` | **[unverified]** required-phrase pair plus wording blacklist | delete | The firewall twin of E-069, same reasoning. |
| E-078 | `cli/tests/plugins/gcp/test_network.py::test_done_capacity_firewall_failure_does_not_attribute_the_vm_zone@ab4ac9` | hint equality | delete | The zone-attribution invariant is the line above (`"us-central1-a" not in f"{caught.value} {caught.value.hint}"`), which stays. |
| E-081 | `cli/tests/plugins/gcp/test_platform.py::test_combined_progress_does_not_claim_completion_when_readiness_fails@651efe` | progress/output sentence presence and absence | delete | The two prose legs of the same test; the mutation and delivery claims are carried by E-080 and the surviving cache assertions. |
| E-082 | `cli/tests/plugins/gcp/test_platform.py::test_second_interrupt_abandons_platform_rollback_without_mutating_survivors@e70fad` | warning-phrase presence plus destructive-command absence | delete | Redundant here: the same test's structural assertions already say the instance survived, both firewall rules survived, and no delete request was issued. See E-093 for where this family's real property lands. |
| E-083 | `cli/tests/plugins/gcp/test_platform.py::test_residual_definitive_insert_rejection_rolls_back_and_stays_detached@ec507f` | **[unverified]** hint required-phrase pair | delete | Duplicate of E-075's guidance sentences at the platform level; the rollback behavior is asserted structurally in the same test. |
| E-084 | `cli/tests/plugins/gcp/test_platform.py::test_delete_retains_same_name_different_id_instance_and_lifetime_deny@b2274d` | hint substring | delete | The retention claim is structural on the lines above (`cache.instances.delete_requests == []`, the deny rule still in `cache.firewalls.resources`). |
| E-085 | `cli/tests/plugins/gcp/test_platform.py::test_delete_retains_same_id_mutated_allow_but_still_deletes_instance_and_deny@76210c,test_delete_continues_after_allow_read_failure_and_reports_retained_allow@5fa267,test_delete_keeps_deny_when_allow_read_and_instance_delete_are_indeterminate@d0c9aa` | guidance-sentence substrings | delete | Three tests, same shape. Each sits directly beneath the mutation record that proves the behavior (which rules survived, which delete requests were issued). |
| E-087 | `cli/tests/plugins/gcp/test_platform.py::test_stable_allow_close_hooks_fail_closed_on_provider_read_error@f71de7,test_transient_route_close_failure_warns_and_retains_only_its_exact_rule@893ebc` | warning-phrase presence | convert | The claim worth keeping is that these fail-closed paths warn at all rather than failing silently. Replace `"not proven absent" in "\n".join(warnings)` with a non-empty/count assertion on `warnings`; the retained-rule identity (`route_name in message`) is injected and stays. No production change. |
| E-088 | `cli/tests/plugins/gcp/test_platform.py::test_stable_allow_close_hooks_fail_closed_on_provider_read_error@f71de7,test_transient_route_close_failure_warns_and_retains_only_its_exact_rule@893ebc` | redaction defense | keep | Same invariant as E-086 on the hook and transient-route paths: a provider's private detail must never reach operator output. Regrouped from `source-guard` to `report-hints` by the lead, since it lives in the file the report-lines PR edits. |
| E-089 | `cli/tests/plugins/gcp/test_cleanup.py::test_ordinary_cleanup_cannot_replace_primary_failure@ad23e3` | coordinate and guidance sentence substrings | delete | Ten substring pins of one authored guidance string. The coordinates (`project-a`, `vm-a`, the provider IDs) are test-injected and could be narrowed to, but the sentences around them are ours and the branch is already fixed by the test's fixture states. |
| E-090 | `cli/tests/plugins/gcp/test_cleanup.py::test_ordinary_cleanup_cannot_replace_primary_failure@ad23e3` | destructive-command absence | convert | Folded into the single property test named in E-093: with ownership unknown, the guidance names no `gcloud ... delete` for any resource. Verified by execution. |
| E-091 | `cli/tests/plugins/gcp/test_cleanup.py::test_interrupt_after_partial_ordinary_rollback_converges_and_supersedes_primary@3e67fd,test_first_interrupt_rolls_back_and_reraises_original_identity@ac0ce8` | verbatim warning list equality | convert | Replace with `len(warnings) == 1` (the first interrupt announces cleanup exactly once before proceeding); the mutation record in each test carries the rest. No production change. |
| E-092 | `cli/tests/plugins/gcp/test_cleanup.py::test_second_interrupt_after_partial_ordinary_rollback_is_only_abandon_signal@6bd347,test_second_interrupt_abandons_promptly_with_exact_safe_actions@fe123b` | guidance sentence substrings on the abandon path | convert | Rubric re-check, 2026-08-16. Scoped: the `test_cleanup.py::test_second_interrupt_after_partial_ordinary_rollback_is_only_abandon_signal` leg stands, the `test_cleanup.py::test_second_interrupt_abandons_promptly_with_exact_safe_actions` leg converts, being the only evidence its warn path fired. E-082 stands only because this converts. No production change. |
| E-093 | `cli/tests/plugins/gcp/test_cleanup.py::test_second_interrupt_abandons_promptly_with_exact_safe_actions@fe123b,test_manual_guidance_recommends_delete_only_for_verified_owned_survivors@9cb42b,test_manual_guidance_for_provider_id_collisions_is_inspect_and_escalate_only@670cc7,test_manual_guidance_for_unknown_provider_ids_is_inspect_and_escalate_only@3b566e` | ~35 hint-equality and destructive-command pins across four tests | convert | Replace all four with one state-driven property over `manual_cleanup_guidance`: for every combination of `InstanceState`, `FirewallState`, ownership presence, and observed provider ID, the guidance names a `gcloud ... delete` for a resource IF AND ONLY IF that resource is proven owned (realized/surviving, ownership known, observed id == expected id). VERIFIED by executing that property against HEAD: it holds across the full state space. This is a real safety invariant (never recommend deleting a resource whose ownership is unproven), it needs no production change, and it subsumes every individual pin. The residual prose (which sentence explains why, and the deny rule's "keep it until the instance is absent" precondition) goes per R2.4. |
| E-094 | `cli/tests/plugins/gcp/test_cleanup.py::test_manual_guidance_for_provider_id_collisions_is_inspect_and_escalate_only@670cc7,test_manual_guidance_for_unknown_provider_ids_is_inspect_and_escalate_only@3b566e` | authored phrase count | delete | `message.count("do not delete by name; escalate ownership") == 3` counts occurrences of one authored sentence. The "all three resources got the escalate treatment" claim is carried by E-093's iff property. |
| E-100 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_structured_failure_output_is_redacted_from_console_log_and_error@0b3b1b,test_reflected_copy_failure_is_secret_free_across_every_surface@b5368e,test_invalid_reflected_tailscale_ip_never_reaches_output_or_return@85c150,test_standalone_guest_removal_failure_surfaces_and_models_residue@ce041c,test_local_removal_failure_surfaces_after_guest_cleanup@d60480` | authored error-message equality | delete | Five verbatim pins of our own error sentences. In each case the surrounding assertions (typed raise, secret absence, residue model) carry the real claim. |
| E-102 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_real_wsl_timeout_drops_reflected_output_and_removes_both_stages@940ec8,test_invalid_reflected_tailscale_ip_never_reaches_output_or_return@85c150` | prefix assertions on an error and a detail line | delete | `str(...).startswith("WSL2 command timed out after 900s:")` and the "Tailscale IP:" prefix absence pin authored wording; the timeout and the never-returned-IP claims are carried by the typed raise and the return value. |
| E-103 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_guest_removal_failure_does_not_mask_primary@6833ce,test_guest_removal_control_flow_does_not_mask_primary@38b99b,test_local_removal_failure_does_not_mask_staging_primary@f2071e` | verbatim warning list equality | convert | Same treatment as E-057: `len(warnings) == 1`, keeping `caught.value is failure` and the `_AUTH_KEY not in repr(warnings)` redaction check. No production change. |
| E-104 | `cli/tests/vms/test_bootstrap_vm_hooks.py::test_failed_path_hook_failure_does_not_mask@05351f,test_interrupt_hook_failure_does_not_mask_the_interrupt@5d0ca3` | warning-phrase presence | convert | Rubric re-check, 2026-08-16. A silently swallowed `secure_failed_vm` failure would otherwise pass. Convert to a warning-count assertion. No production change. |
| E-105 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_failure_closes_logger_once_without_replacing_primary@befaf2` | log-path substring in a warning and an error | keep | Invariant: a failed create tells the operator where the provisioning log is. The path is derived from the test's own VM name and log directory, and losing it would let the log path silently stop being reported. Narrow to the path, drop the `"Log: "` / `"Details: "` framing. |
| E-106 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_failure_closes_logger_once_without_replacing_primary@befaf2,test_successful_create_propagates_fresh_logger_close_interrupt@66ba13,test_logger_close_warning_failure_does_not_replace_primary@deee52` | verbatim warning equality and absence | convert | **Narrowed after executing each of the three tests.** `vms/test_create_reinit_orchestrated.py::test_successful_create_propagates_fresh_logger_close_interrupt` emits zero warnings, so the PROPOSED `captured_output.warnings == []` is exact; `vms/test_create_reinit_orchestrated.py::test_logger_close_warning_failure_does_not_replace_primary` emits one, which its `assert warning_messages == [` equality over a single entry already shows, so a count holds. The warning equality in `vms/test_create_reinit_orchestrated.py::test_create_failure_closes_logger_once_without_replacing_primary` is struck from this row and moves to E-106b: that test emits THREE warnings under the `[interrupt]` parameter, so no count selector works there. `closes == ["close"]` and the primary-identity assertions stay in all three. **Which test is which is inferred from the row's structure**, one of the numbers this was written from having landed on a blank line, so the verification lane executes the mapping rather than trusting it. No production change. |
| E-106b | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_failure_closes_logger_once_without_replacing_primary@befaf2` | verbatim warning equality | delete | Split out of E-106 by execution: `test_create_failure_closes_logger_once_without_replacing_primary[interrupt]` emits three warnings, so no count selector distinguishes the logger-close warning from the other two, and the discriminator is our own wording. `closes == ["close"]` and the primary-identity assertion carry the invariant the test is named for. |
| E-107 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_rollback_on_keyboard_interrupt_unwinds_the_row@eb53f8` | **[unverified]** warning-phrase presence | delete | The rollback is proven by `db.get_vm(...) is None` in the same test. |
| E-108 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_rollback_failure_warns_and_never_masks@01de3d` | authored warning prefix | delete | `warning.startswith("rollback: teardown of vm/wvm failed:")` pins our own prefix; the two lines below it carry the substance. |
| E-109 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_rollback_failure_warns_and_never_masks@01de3d` | injected identities in a rollback warning | keep | Invariant: a failed rollback names what survived (the DB row for the named VM) and chains the underlying cause. `wvm` and `"db locked"` are both injected by the test. |
| E-110 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_phase_a_failure_maps_to_provisioning_error@c4ced3,test_reinit_refuses_an_operator_stopped_vm_at_the_gate@286dc3` | hint substring naming a CLI command | delete | Same as E-097; the gate behavior is carried by the typed raise and the row's `provisioning_status`. |
| E-111 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_phase_a_sync_failure_is_non_fatal@6d2791` | warning-phrase presence | convert | Rubric re-check, 2026-08-16. A silently swallowed SSH-config sync failure would otherwise pass. Convert to a warning-count assertion. No production change. |
| E-113 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_reinit_runs_initialization_through_the_gate@8acaa0` | result-line sentence pins | convert | Rubric re-check, 2026-08-16. Convert to a role-and-level predicate over `captured_output.lines`, keeping the routing claim without the sentence. No production change. |
| E-115 | `cli/tests/vms/test_delete_vm_gating.py::test_hold_failure_does_not_skip_delete@aa0e0c` | warning-phrase presence | delete | The invariant (a broken keepalive hold does not skip the delete) is `counts["delete"] == 1` and `db.get_vm(...) is None` on the two lines above. |
| E-116 | `cli/tests/vms/test_delete_vm_gating.py::test_stranded_site_warns_with_hint_and_still_deletes_row@9a7c8a` | warning substrings including a manifest fragment | convert | Keep the stranded-site claim structurally (`resolve_counter == []`, the row deleted) and, for the `"kind: vm-site"` fragment, fold into E-127's approach: parse the emitted manifest hint and validate it rather than pinning its text. No production change. |
| E-117 | `cli/tests/vms/test_ensure_tailscale_wording.py::test_already_running_probe_says_verifying_not_reconnect@51f006,test_cold_start_probe_keeps_reconnect_wording@73f53e,test_start_vm_through_wsl2_shaped_hold_verifies_exactly_once@cc211d` | wording-only test file | delete | Three tests whose entire subject is which sentence the probe prints (the file's docstring says so). The behavioral core, `_tailscale_rejoin_required(...) is False` and the once-only verification, is asserted separately; the exactly-once claim at `test_ensure_tailscale_wording.py::test_start_vm_through_wsl2_shaped_hold_verifies_exactly_once` counts an authored line and goes with it per R2.4. |
| E-118 | `cli/tests/vms/test_legacy_site_sections.py::test_manifest_token_secret_conforming_emits_no_warning@967d29` | phrase blacklist over issue text | convert | Rubric re-check, 2026-08-16. `assert not any("secret naming rules" in issue for issue in manifests.issues), manifests.issues` is the test's ONLY assertion, so the delete empties it. Assert `manifests.issues == ()`. No production change. |
| E-119 | `cli/tests/vms/test_lifecycle_orchestrated.py::test_start_stopped_vm_resolves_once_starts_and_clears_flag@6566b5,test_start_running_vm_short_circuits_but_still_clears_flag@05818f,test_stop_running_vm_resolves_once_stops_and_sets_flag@9a82ff` | info-line presence and absence | delete | Three start/stop tests whose structural siblings (`events == [...]`, `row.operator_stopped`, `resolve_counter`) already carry every claim; the phrases only assert which sentence was chosen. |
| E-120 | `cli/tests/vms/test_lifecycle_orchestrated.py::test_stop_sets_flag_before_already_stopped_shortcut@968348` | required-phrase pair | delete | The recorded intent is asserted structurally (`row.operator_stopped is True`, `events == ["status"]`); the two phrases police the wording of the explanation. |
| E-121 | `cli/tests/vms/test_lifecycle_orchestrated.py::test_stop_of_a_manually_stopped_vm_is_a_true_noop@5c5b0d` | authored message equality (sole assertion) | convert | The claim is that a re-stop of an already-manually-stopped VM is a true no-op. Capture the `_fake_power` events (the fake already records them, as the sibling tests do) and assert `events == ["status"]` with no stop op, instead of pinning the sentence. No production change. |
| E-123 | `cli/tests/vms/test_lima_create_flow.py::test_probe_failure_warns_and_does_not_restart@0c6a03` | warning substring | delete | `"needs a restart to finish provisioning"` is authored wording; the no-restart behavior is the same test's command-absence check. |
| E-123b | `cli/tests/vms/test_lima_create_flow.py::test_probe_failure_warns_and_does_not_restart@0c6a03` | injected cause substring | keep | `"connection reset"` is the probe failure this test injected; its presence proves the cause is chained into the warning rather than swallowed. |
| E-125 | `cli/tests/vms/test_lima_create_flow.py::test_ip_probe_failure_returns_missing_ip_after_successful_join@f84a9b` | warning-phrase presence | delete | The behavior (missing IP returned after a successful join) is the return value asserted above it. |
| E-127 | `cli/tests/vms/test_lima_create_interrupt.py::test_failure_mid_create_cleans_up_and_reraises@da7939,test_interrupt_during_start_cleans_up_and_reraises_the_original@1c93e0,test_second_interrupt_abandons_cleanup_loudly@e02f43` | **[unverified]** phrase-selected warnings | delete | `"Cleanup abandoned"` / `"Ctrl-C again to abandon"` name branches whose behavior is already pinned by `_deletes(ran)` and `len(_deletes(ran)) == 1` in the same tests; the quoted `'limactl delete --force myvm'` inside the abandon warning is the tool command and can be kept by selecting the sole warning positionally. |
| E-128 | `cli/tests/vms/test_lima_remote_create_interrupt.py::test_remote_abandon_warning_names_the_vm_host@6c93b2,test_remote_interrupt_preserves_original_when_artifact_cleanup_fails@e22dd6,test_remote_exception_kills_cleans_artifacts_then_deletes_without_masking@65a724,test_cleanup_failure_warns_and_does_not_mask_the_original@9ad8aa,test_pre_mutation_failure_makes_no_cleanup_calls@36a9bc` | phrase-selected warnings and detail lines | convert | Rubric re-check, 2026-08-16. Same family: a swallowed teardown failure leaving a partial instance. Convert to a warning-count assertion. No production change. |
| E-138 | `cli/tests/vms/test_platform_runup.py::test_proxmox_runup_other_status_warns@92c5cc,test_proxmox_runup_network_warns@15b928` | stderr warning substrings | convert | Four runup tests whose real invariant is "warns rather than raising" (each is annotated `# no raise`). Assert that stderr is non-empty instead of pinning which sentence; the API-error vs network-error distinction is authored-only and goes per R2.4. No production change. **Re-derived 2026-09-06 at `c310d05b`.** Both AWS runup tests became `_is_fatal`: runup raises `EC2Error` now instead of warning, and the tests hold a bare `pytest.raises` with no stderr pin left to convert. The convert stands for the two proxmox sites, whose warn-rather-than-raise invariant is unchanged. |
| E-139 | `cli/tests/vms/test_platform_runup.py::test_azure_runup_missing_group_is_fatal@8004de` | hint required-phrase pair | delete | The typed error's structural fields are already asserted (`entity_kind == "resource-group"`, `entity_name == "rg-dev"`); the two hint sentences are authored remediation. |
| E-143 | `cli/tests/vms/test_platform_support.py::test_doctor_lists_platforms_and_not_ready_sites@ede1b5` | doctor-message substring | delete | `"not ready"` is our own framing, sitting beside `by_name["wsl2"].status is doctor.Status.INFO`, which carries the state. |
| E-143b | `cli/tests/vms/test_platform_support.py::test_doctor_lists_platforms_and_not_ready_sites@ede1b5` | injected reason substring | keep | `"Windows only"` is the reason `_support()` injected; the assertion proves doctor surfaces the platform's own reason rather than a generic one. |
| E-144 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_describe_running_vm_is_one_boundary_burst_and_reads_only@f20f06,test_describe_operator_stopped_vm_never_gates@c8de01` | describe-report line substrings | delete | Both tests already pin the behavior (`resolve_counter`, `events == ["status"]`, no live read on a stopped VM); the rendered status text is the report's wording. |
| E-145 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_rekey_one_boundary_burst_covers_key_and_site_secret@e71612` | result-line phrase presence | delete | The rekey behavior is fully asserted above it (the exact sensitive stdin call, the key never in a rendered command, the row's new Tailscale host). |
| E-146 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_rekey_wraps_steps_in_a_section@2ad8a9,test_backup_wraps_phases_in_a_section@dedb64` | narration-structure tests with sentence pins | convert | Two "wraps in a section" tests. Keep the Role/level structure claims (a level-0 HEADER exists, the IP is a DETAIL not a step, the terminal is a level-0 RESULT), drop the four authored step-sentence assertions (`"Joining new tailnet..."` and `"Reading new Tailscale IP..."` in the rekey test, `"Reading database (consistent snapshot)..."` and `"Exporting VM metadata..."` in the backup one) and the header/result text. No production change. |
| E-148 | `cli/tests/vms/test_shell_exec_orchestrated.py::test_exec_dash_prefixed_command_fails_with_zero_resolves_and_zero_gate@ea5444` | hint required-phrase pair | delete | Authored remediation beside a `pytest.raises(ValidationError, match=...)` (group 1) and the zero-resolve/zero-gate assertions that carry the pre-gate refusal. |
| E-150 | `cli/tests/vms/test_sites_dispatch.py::test_resolving_a_not_ready_site_names_the_requirement@f5dfaf` | negative fragment presence | delete | `"kind: vm-site" not in hint` distinguishes the not-ready hint from the undeclared-site hint; the `StateError` vs `ConfigError` types already discriminate those two paths. |
| E-152 | `cli/tests/vms/test_tailscale_logout_wording.py::test_deregistered_prints_inside_the_route_window@932c33,test_nullcontext_route_platform_output_is_unchanged@ebd5c5,test_failed_dispatch_still_warns_without_success_line@6cd5a6` | full info-transcript equality (three tests) | convert | The load-bearing claim is ordering: the logout dispatch and its success report happen INSIDE the transient route window. Replace the transcript equality with one ordered event log recorded by the test's own fakes (route open, transport command, route close) and assert the command index falls between open and close, plus that the failure path issues no success event. Both fakes already record; no production change. hla.md explicitly prefers this observational twin to a lexical order pin. |
| E-154 | `cli/tests/vms/test_tailscale_reachable.py::test_missing_binary_warns_once_and_degrades@2cb753` | warning-phrase substring | delete | The degradation claim is the function's return value; the warning sentence is authored. |
| E-156 | `cli/tests/vms/test_verify_connection.py::test_verify_connection_cli_reports_service_result@6ab1e5` | CLI stdout equality | delete | `result.stdout == "VM 'worker' connection verified via ssh.\n"` pins an authored CLI sentence; the exit code and the service result are the behavior. |
| E-157 | `cli/tests/vms/test_vm_nodes.py::test_manually_stopped_raises_and_skips_the_ping@d2d0ec` | error and hint substrings | delete | The gate's behavior is `platform.start_calls == 0` plus the typed raise (whose `match=` is group 1's); `"not be auto-started"` and `"agw vm start gvm"` are authored. |
| E-161 | `cli/tests/test_aws_ec2_ops.py::TestCloseProvisioningHooks.test_hook_tolerates_missing_security_group@7e287c` | warning-phrase presence | convert | Rubric re-check, 2026-08-16. Same warn-fired family. Convert to a warning-count assertion. No production change. **Re-derived 2026-09-06 at `c310d05b`.** The test is gone and no warning assertion survives anywhere in `TestDelete`, so there is no warning count to convert to. The convert stands for the hook site. |
| E-162 | `cli/tests/test_azure_create_interrupt.py::TestInterruptDuringInlineWait.test_rolls_back_the_full_resource_set_and_reraises@f099bb,TestInterruptDuringResourceCreation.test_rolls_back_what_exists_and_reraises@d863a3,TestFailureDuringInlineWait.test_non_ssh_error_escaping_the_wait_rolls_back_vm_first_and_wraps@3fa86e,TestPlainFailureArmUnchanged.test_failure_still_cleans_up_and_wraps@383a55` | warning-phrase presence and absence | delete | Four tests where the deleted-resource lists and the propagated exception identity already fix the path; `"Ctrl-C again to abandon"`, `"cleaning up partial Azure resources"`, and `"Cleanup abandoned" not in` only name branches. |
| E-163 | `cli/tests/test_azure_create_interrupt.py::TestInterruptDuringInlineWait.test_second_interrupt_abandons_cleanup_loudly@85e551` | verbatim abandon-warning equality | delete | **Was a convert; executing flipped it.** `test_second_interrupt_abandons_cleanup_loudly` emits three warnings under both parameters. `network_interfaces.deleted == []` and `public_ip_addresses.deleted == []` carry the abandon. |
| E-164 | `cli/tests/test_azure_create_interrupt.py::TestInterruptDuringInlineWait.test_vm_delete_failure_during_rollback_warns_and_reraises@df961b` | phrase-selected warning with injected coordinates | convert | Rubric re-check, 2026-08-16. **Corrected against keep row C-173**, which keeps the three injected-coordinate asserts (`assert "Azure VM 'vm1'" in may_remain`, `assert "resource group 'rg1'" in may_remain`, `assert "vm delete boom" in may_remain`). Keep those, delete the two authored-phrase pins beside them (**which two is inferred from the row's structure**, from C-173's three keeps and this row's shape cell, the numbers it named having been bare) (`assert "delete it there manually" in may_remain` and `assert not any("Cleanup abandoned" in w for w in captured_output.warnings)`), and replace the phrase selector (`(may_remain,) = [w for w in captured_output.warnings if "may remain" in w]`) with a predicate over the injected coordinate tokens. Supersedes this row's earlier delete-by-count reasoning, which execution had already shown unusable (three warnings, not one). No production change. |
| E-166 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_rbac_denial_raises_authorization_error@49ce1b` | hint substrings | delete | `"Contributor"` and `"re-run 'agw vm delete'"` are authored remediation; `AuthorizationError` plus `"AuthorizationFailed"` (the Azure SDK's own code) discriminate the branch. |
| E-167 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_rbac_denial_raises_authorization_error@49ce1b,TestGoneVMSucceeds.test_clean_delete_succeeds@bdf504` | success-line absence and presence | keep | Rubric re-check, 2026-08-16. The only probe that a raising `delete()` never prints its success line. Verified by execution that the preamble and the success line are both `Role.BODY` at level 0, so no role-and-level handle can replace it. The positive twin, `assert any("Azure VM 'vm1' deleted" in line for line in captured_output.info)` in the succeeding-delete test, must stay, without which the negative assertion goes vacuous. Cost: a rewording breaks two tests. (C-177 was a duplicate of this row and is removed.) |
| E-168 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_denied_probe_after_rbac_delete_failure_raises_authorization_error@1e53e3,TestSurvivingVMRaises.test_denied_probe_alone_raises_authorization_error@d87420` | same-type branch discriminators in our own prose | convert | Rubric re-check, 2026-08-16. `__cause__` is the injected delete failure in one test and the probe failure in the other (`plugins/azure/network.py::verify_vm_deleted._rbac_refusal`), so the two do NOT collapse to differing only in setup as the row assumed. Assert the cause. No production change. (Absorbs two of the sites C-176 narrowed off.) |
| E-169 | `cli/tests/test_azure_delete_verify.py::TestGoneVMSucceeds.test_sweep_straggler_warns_without_failing_the_delete@8bb915,TestGoneVMSucceeds.test_disk_sweep_failure_still_warns@70f248` | phrase-selected straggler warnings | convert | The invariant is "a genuine straggler failure warns exactly once and never fails the delete" (and its twin, `warnings == []` when the resource was already gone, which is already structural). Select by count instead of by `"vm1-nic"` / `"OS disk"`, and drop the `"delete it there manually"` / `"agentworks-tagged disk"` phrases. No production change. |
| E-170 | `cli/tests/test_azure_delete_verify.py::test_no_resource_id_short_circuits@b66d00` | warning-phrase presence | delete | The short-circuit is `fakes.events == []` on the line above: nothing was touched backend-side. |
| E-171 | `cli/tests/test_azure_nsg_exposure.py::TestCreate.test_create_announces_the_bootstrap_route_open@02688f` | announcement-line equality (whole test) | delete | `test_create_announces_the_bootstrap_route_open` asserts only that one authored sentence was printed. The behavior it narrates (the NSG allow is created scoped to exactly those prefixes) is asserted structurally by `test_azure_nsg_exposure.py::TestCreate.test_create_provisions_deny_baseline_and_scoped_bootstrap_allow` through `test_azure_nsg_exposure.py::_assert_allow_shape` (`assert rule.source_address_prefixes == prefixes`). |
| E-172 | `cli/tests/test_azure_nsg_exposure.py::TestPrefixAssembly.test_detection_failure_with_extras_proceeds_with_warning@3e6835` | compound warning-phrase predicate | convert | Rubric re-check, 2026-08-16. The only evidence the egress-detection warning fired; a silent degradation would pass. Convert to a warning-count assertion. No production change. (Absorbs C-179's claim over this same predicate.) |
| E-173 | `cli/tests/test_azure_nsg_exposure.py::TestPerOperationAllows.test_band_exhaustion_raises_typed_error@bdc4fe` | error-message substring | delete | `"no free NSG priority" in str(exc.value)` is authored; the `StateError` raise plus the `TRANSIENT_ALLOW_RULE_PREFIX` hint check (a production constant) carry the band-exhaustion claim. |
| E-174 | `cli/tests/test_azure_vm_size_selection.py::TestCreateProvisioningOutput.test_exact_match_emits_spec_without_requested@420172,TestCreateProvisioningOutput.test_round_up_warns_and_line_shows_selected_spec@3fdc3f,TestCreateProvisioningOutput.test_non_burstable_override_selected_and_emitted@e6ba5b` | provisioning-line equality plus a wording blacklist | delete | Three tests pinning the announcement sentence. VERIFIED: the selection invariant they claim to protect is covered structurally by `TestSelectVMSize`, where `_select_vm_size` is asserted directly, including the 4/8 round-up to `Standard_B4ms`; that class runs green on its own. `"for requested" not in line` is a blacklist. |
| E-175 | `cli/tests/test_azure_vm_size_selection.py::TestCreateProvisioningOutput.test_round_up_warns_and_line_shows_selected_spec@3fdc3f,TestCreateOSDiskClamp.test_below_floor_clamps_up_and_warns@bc4f95` | verbatim warning list equality | convert | The invariant is "warn if and only if the request was rounded up or clamped". Replace the sentence equality with `len(captured_output.warnings) == 1`, which pairs with the existing `assert not captured_output.warnings` in the no-round-up and at-or-above-floor twins. No production change. |
| E-178 | `cli/tests/test_wsl2_create_interrupt.py::test_failure_mid_provision_cleans_up_and_reraises@2697a5,test_interrupt_during_primary_bootstrap_cleans_up_and_reraises_the_original@10a462,test_second_interrupt_abandons_cleanup_loudly@134872,test_cleanup_failure_warns_and_does_not_mask_the_original@bacce8` | phrase-selected warnings | convert | Rubric re-check, 2026-08-16. Scope corrected to include `assert not any("Interrupted" in w for w in captured_output.warnings)` in `test_wsl2_create_interrupt.py::test_failure_mid_provision_cleans_up_and_reraises`, which the duplicate C-187 row covered. A swallowed teardown failure leaving a partial distro would otherwise pass silently. Convert to a warning-count assertion. No production change. |
| RB-001 | `cli/tests/sessions/test_claude_code_orchestrated.py::test_create_produces_launch_string_and_persists_the_minted_id@7a3de1` | **[unverified]** launch-note decision line riding inside a behavioral test | delete | Added by the 2026-08-19 re-baseline; the file carried no row. `"starting new session s1" in captured["command"]`. The decision the line reports is already structural in the same test: the `f"--session-id {sid}"` and `"--resume" not in` asserts are the argv `claude` actually receives (`cli/agentworks/plugins/claude/harness_integration.py`'s launch-argv assembly). What the assertion adds beyond that is the sentence we author in `claude/harness_integration.py::ClaudeCodeIntegration._resume_or_launch` (`f"agentworks harness integration (claude-code): starting new session {self._session_name}"`), so its only failure mode is a rewording. Same shape and same disposition as B-068 on the sibling `test_claude_code_integration.py`. |
| RB-005 | `cli/tests/sessions/test_codex_orchestrated.py::test_create_launches_fresh_without_probing_any_session_state@c25981` | **[unverified]** launch-note decision line riding inside a behavioral test | delete | `"starting new session s1"` on the create path. The fresh launch is structural in the same test: `_SID not in command` proves nothing was adopted, and the `rm -f "$HOME"/.agentworks/codex/s1.thread` beside it is emitted only by `_fresh_command` (`cli/agentworks/plugins/codex/harness_integration.py`'s recorder wiring). |
| C-039 | `cli/tests/test_completions.py::TestResolveShell.test_autodetect_failure_exits_with_message@1be167,TestCompletionCli.test_show_autodetect_failure_exits_1@8db2ff` | **[unverified]** abort-wording substring (two sites) | delete | `"unable to detect the shell" in ...err.lower()` pins our authored abort sentence at both sites. Each already asserts the behavior beside it: `exc_info.value.exit_code == 1` in the first, `result.exit_code == 1` in the second. |
| E-180 | `cli/tests/test_doctor_env_and_secrets.py::test_doctor_cli_keeps_database_inspection_value_free_after_live_publication_failure@f47e06` | redaction defense | keep | The test stores a desired overlay whose key carries a bidirectional-override character and a `PRIVATE_KEY_SENTINEL` marker, and whose value is `PRIVATE_PAYLOAD_SENTINEL`, then asserts none of the three reaches doctor's combined output while the structural fact (an `unconsumed-record` instance-state fact naming the agent) still does. Injected sentinels proving a redaction boundary, which `hla.md` keeps by name. |
| E-181 | `cli/tests/test_doctor_env_and_secrets.py::test_manifest_issues_surface_as_doctor_rows@ae358f` | phrase blacklist standing in for silence | convert | `assert "Manifest:" not in captured.out + captured.err` polices our own ambient print by its prefix. The invariant is that `_check_config` prints nothing at all, because it hands the pre-loaded manifest set to `build_registry` instead of letting the loader warn ambiently. Replacement: `captured.out == "" and captured.err == ""`, which is stronger and names no wording. No production change. |
| E-182 | `cli/tests/test_doctor_env_and_secrets.py::test_secret_checks_use_only_explicit_tty_access@1335f3` | production-constant restated as a literal | convert | `available_row.secret_preview.reason == "operator-input-required"` spells out a value the same file reads through its enum elsewhere (`check.secret_preview.reason == BlockReason.TTY_UNAVAILABLE.value`). Replacement: `IndeterminateReason.OPERATOR_INPUT_REQUIRED.value`, which is the member at `cli/agentworks/capabilities/secret_backend/client.py`'s `OPERATOR_INPUT_REQUIRED`. No production change. |
| E-183 | `cli/tests/test_secret_cli.py::test_missing_ssh_keys_do_not_block_secret_list@83391c,test_missing_ssh_keys_do_not_block_secret_describe@83391c` | resource-name presence in rendered output | keep | `"tailscale-auth-key" in result.output` is the auto-declared secret's own name, not prose about it. It is what separates the soft-load path working from a command that exits 0 with an empty listing, which is the whole subject of both tests; `exit_code == 0` alone would pass on an empty list. Value identity over a registry-computed name. |
| E-184 | `cli/tests/test_version_command.py::test_resolve_version_falls_back_when_not_installed@e8e0f8` | fallback-value equality | keep | `resolve_version() == "unknown"` is the whole test, and the call sits inside it, so this is what proves the fallback path runs at all rather than `PackageNotFoundError` propagating out of `agw version`. The token is a one-word sentinel the command prints in place of a version, not a sentence. Cost, stated plainly: we author the token, so renaming it fails this test with nothing broken; the only sentinel-free replacement is a weaker `isinstance(..., str)`, which is why this keeps rather than converts. |
| E-185 | `cli/tests/test_graph_cli.py::test_missing_ssh_keys_do_not_block_graph_show@542eb0` | resource-identity presence in rendered output | keep | Same shape as E-183. `"secret/npm-token" in result.output` names the manifest the test wrote, and it is the only assertion separating a real graph render from a clean exit over nothing. |
| E-186 | `cli/tests/test_graph_cli.py::test_graph_show_json_projects_the_same_result@2d079d` | JSON envelope and projection equality | keep | `schema_version`, `command`, and the `query`/`nodes`/`edges` key names are the shipped machine contract, which is a consumer interface rather than authored prose; every value in the expectation is read back off the `GraphResult` the test built. A rename here breaks every machine consumer and nothing else catches it. |
| E-187 | `cli/tests/test_operational_json_cli.py::test_nonempty_operational_lists_have_exact_ordered_json@e3b0c4,test_nonempty_operational_describes_have_exact_safe_json@ea49a7` | byte-exact JSON document equality | keep | The expectations are built from the row objects the test constructed (`{"name": row.name, ...}`), so what is pinned is the projection's field set, nesting and order, plus determinism (invoked twice), an ANSI-free stdout and an empty stderr. Same reasoning as B-015: this is the machine contract plus arity, not layout. The describe test additionally proves the unsafe stored state (`platform_metadata`, raw config) never reaches the document. |
| E-188 | `cli/tests/resources/test_graph_query.py::test_json_projection_has_only_the_closed_safe_scalar_shape_and_nulls@ddca83` | JSON projection dict equality | keep | The same contract one layer below the CLI, and the equality is what pins that no field is added, dropped or renamed and that absent facts project as explicit `null` rather than being omitted. All values come from the `GraphResult` the test built. |
| E-189 | `cli/tests/resources/test_graph_query.py::test_human_projection_emits_every_unique_fact_once_in_result_order@dc400b` | rendered-line counts, order and nesting over injected sentinels | keep | Every string asserted is a `*_SENTINEL` the test injected, so nothing authored is pinned. What is pinned is structure: each unique fact appears exactly once, nodes precede edges, edge order follows result order, and each edge's detail lines nest at level 3. A renderer that duplicated an edge or emitted a detail at top level is caught here and nowhere else. Cost: `detail_counts == [2, 1, 0]` is the tightest part and moves if the detail lines per edge change. |
| E-190 | `cli/tests/resources/test_access.py::test_resolve_resource_rejects_unknown_kind_before_name_lookup@ac6261` | hint enumeration derived from a production registry | keep | The regex assertion compares what the hint enumerates against `sorted(KIND_REGISTRY)`, so the expectation is computed from the production table rather than restated. Same shape as C-011: the remediation silently degrades to a hint that names the wrong kinds, or misses a newly registered one, and only this catches it. |
| E-191 | `cli/tests/test_instance_specs.py::test_admin_effective_validation_rejects_invalid_folded_declaration@5d241e,test_admin_effective_validation_does_not_echo_custom_validator_input@d3f44c,test_admin_effective_validation_preserves_safe_mise_error_category@3b1a24` | field-name substring in a validation message | keep | `"mise_install_before" in str(...)`, `"mise_lockfile" in str(...)` and `"mise_packages" in str(...)` assert that the refusal names the field the operator's spec actually carried, which is their only route back to the offending key. The field names are the test's own input, not authored prose. Same reasoning as B-062/B-133; the paired `marker not in str(...)` halves are the leak defense named in the preamble. |
| E-192 | `cli/tests/schema/test_markers.py::test_owner_display_reproduces_the_shipped_owner_string@e9b89d` | message-framing convention pin | delete | The whole test is `RefOwner(kind="git-credential", name="prod").display == "git-credential/prod"`. `display` exists only to frame operator-facing error text: its docstring says so (`schema/markers.py::RefOwner.display`) and its consumers are all message interpolations, in `manifests/decode.py::_check_declared_name`, `decode.py::_check_declared_description`, `decode.py::_env_entry_issues` and `decode.py::_nonconforming_secret`, and in `schema/errors.py::_inherited_from`, `schema/errors.py::_owner_framed` and `schema/errors.py::_framed_batch`. Nothing parses it back, so the assertion's only failure mode is a decision to spell the owner differently. Delete the test; the `label` override branch it never reaches loses nothing. |
| E-193 | `cli/tests/resources/test_instances.py::test_list_view_renders_dash_for_kind_without_used_by_contract@de8f36` | rendered placeholder cell plus column-header navigation | keep | Invariant: a kind with no used-by contract renders a dash, not `0`. Those are different operator-facing claims, "this kind has no such relationship" against "it has none right now", and the renderer is the only place the difference exists. The header strings `USED BY` and `DESCRIPTION` locate the column rather than being asserted; the assertion is on the cell. Cost: renaming either header breaks the locator. |
| E-194 | `cli/tests/sessions/test_console_status.py::test_console_listing_status_column_follows_explicit_render_request@c850f6` | rendered column-header presence and absence, plus column alignment | keep | Invariant: `render_console_listing` emits the status column only when the caller asks for it, and the value sits under its own header. `include_status` changes nothing but the rendering, so the rendered header is the only observable, and a regression that always emits the column makes every plain `console list` pay for status it never observed. The alignment half is structural. |
| E-195 | `cli/tests/vms/test_status_observation.py::test_vm_listing_status_column_follows_explicit_render_request@c850f6` | rendered column-header presence and absence, plus column alignment | keep | The VM twin of E-194, same invariant over `inspect.render_vm_listing`, same reasoning. Keep both: they guard two independent renderers and neither covers the other. |
| E-196 | `cli/tests/vms/test_status_observation.py::test_vm_listing_only_caps_name_column@141a7d` | truncation-marker pin plus uncapped-column assertions | keep | Invariant: the name column is the only one truncated, and it truncates rather than silently dropping characters. The width comes from production (`inspect._NAME_CELL_WIDTH`), so only the three-dot marker is authored, and the other three assertions are the real content: a long site, template or tailscale host survives whole. A regression that caps every column hides operator data with no error. |
| E-197 | `cli/tests/db/test_instance_state.py::test_malformed_persisted_record_raises_instead_of_becoming_absent@bbf87c` | owner-name substring in a raised message | keep | Invariant: a malformed persisted record names the record it failed on. `entity_name` does not carry this to the operator: `AgentworksError` never interpolates `entity_kind` or `entity_name` into its message (`agentworks/errors.py::AgentworksError.__init__`) and the CLI renders `str(e)` and the hint alone in the branch this raise lands in (`cli/agentworks/cli/_entry.py::main`, which catches `StateError` among its siblings), so the substring is the only probe that the operator can tell which record failed. The sibling `secret not in str(...)` is the redaction half and stays either way. |
| E-198 | `cli/tests/test_doctor_env_and_secrets.py::test_auto_declared_secrets_are_reported@4e226f,test_secret_backends_group_reports_readiness@dac5b2` | bare group-label equality | delete | `g.name == "Secrets"` and `g.name == "Secret backends"` assert a group's authored label and nothing else; each test's real content is the per-check status and preview assertions below it. Two lines, no coverage. |
| E-202 | `cli/tests/test_resource_edit.py::test_cli_edit_names_the_manifest_home_relative@dcae1f` | derived path framing plus a home-leak guard | convert | `f"Editing secret/npm-token ({framed}:1)" in result.output` embeds the report sentence around a path derived from `format_host_path`. The invariant, which the docstring records, is that the CLI frames the path through the shared helper rather than hand-rolling `f"{path}:{line}"`, and the `str(home) not in result.output` assertion beside it is what proves the home prefix collapsed. Replacement: `framed in result.output` plus the existing leak check, dropping the sentence around it. This is C-078's sentence deletion applied here without losing the framing property. No production change. |

## Group 4: schema, manifests, capabilities and platforms

Once group 3 splits into its sub-batches this is the largest single PR by converts; the Totals
section counts the group.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-064 | `cli/tests/test_database_backup.py::test_schema_sentinels_match_every_historical_version@a21eb1` | schema-sentinel parity | keep | Derivation parity: the hand-maintained `SCHEMA_SENTINELS` must match the schema produced by replaying `MIGRATIONS` at every historical version, and `validate_restore_source` must agree. This is the guard that finding D2's proposed derivation would replace, not one this sweep should touch. |
| A-079 | `cli/tests/test_db_migration_harness_integration_state.py::test_v31_renames_column_preserving_raw_and_decoded_state@96b612` | persisted column shape | keep | Schema shape of persisted state that crosses executions: column renamed, old name gone, NOT NULL with the `'{}'` default. A migration regression here corrupts real operator databases. |
| B-013 | `cli/tests/sessions/test_effective_config_validation.py::test_an_error_on_an_inherited_key_names_the_template_that_declared_it@f31b19` | error-message provenance substring | convert | The provenance attribution is a real invariant with a shipped structural handle: `effective_template(...).harness.provenance`. VERIFIED by executing a probe: for the inherited lineage it yields `{'command': 'base', 'timeout': 'base'}`. Replace the message reads with a provenance-map assertion on a lineage that resolves. No production change. |
| B-014 | `cli/tests/sessions/test_effective_config_validation.py::test_a_key_the_child_overrode_is_not_attributed_to_the_parent@a0a910` | error-message substring plus phrase blacklist | convert | Same handle as B-013; VERIFIED the overriding lineage yields `{'command': 'base', 'timeout': 'kid'}`, which is exactly what `"inherited from" not in message` was groping for, without a phrase blacklist. |
| B-021 | `cli/tests/sessions/test_session_template_surface.py::test_declared_harness_integration_emits_a_reference@b59a9f` | usage-string equality | convert | **Ruled 2026-09-06 on the lane's evidence, which overrode both a keep and a narrower convert.** The reference's identity is already pinned two lines above by its kind and name, so the equality adds nothing there. Every consumer of `usage` DISPLAYS it, and the only code that tests it tests presence (`self.usage is not None` on the declared arm of `graph_query.py::GraphEdge.__post_init__` and `self.usage is None` on the live-instance arm), so presence is the property the system actually branches on and the sentence is authored text we render. Measured: rewording it fails exactly one test in 8,465, the suite total CI last reported, and it is this one. Replacement: assert `harness_refs[0].usage` is a non-empty string and drop the equality. No production change. |
| B-059 | `cli/tests/secrets/test_backends.py::test_direct_backend_name_in_chain_gets_source_rewrite@0c63c4` | diagnostic substring pair | convert | The invariant is real and structural in nature: a bare backend name used where a source name belongs produces a rewrite diagnostic naming `kind: secret-source`. Both strings interpolate names the test declared. Trim to the injected-name half (`'onepassword'` appears, referring to the fixture's backend) and drop the authored framing clauses. No production change. |
| B-062 | `cli/tests/secrets/test_sources.py::test_source_backend_must_use_the_tagged_table_shape@6abc0d,test_source_backend_config_errors_keep_manifest_location@417e35` | manifest-diagnostic substring | keep | `"secret-source/ci-env.backend: must be a table"` and `".unknown: unknown field"` are keyed on the manifest path the test wrote plus the field name; the invariant is that a config error names the exact manifest location that caused it, which is the operator's only route back to their file. Recommend trimming the trailing authored clause and keeping the `kind/name.field` prefix. |
| B-063 | `cli/tests/secrets/test_sources.py::test_mapping_reference_extraction_uses_the_same_source_selector@843080,test_descriptor_derived_mapping_key_collector_is_ordered_and_value_blind@11caa5` | reference-tuple equality carrying declared usage | keep | These pin the identity, ordering, and source attribution of extracted reference records; the usage strings are the fixture's own declarations. Structural presence, not wording. |
| B-064 | `cli/tests/secrets/test_sources.py::test_unknown_source_uses_identical_source_key_framing@05ac47,test_same_target_mapping_validation_and_candidate_emit_one_diagnostic@4526be,test_mapping_key_collection_runs_for_later_materialized_host@ccd730` | phrase-occurrence counting (`.count(...) == 1`) | convert | The real invariant is de-duplication: one diagnostic per unknown source, not one per validation pass. Counting an authored phrase is the wrong instrument. Replace with a count of the diagnostics the error aggregates (the error already aggregates a list; count entries mentioning the source name the test declared). No production change. Flagged as needing the lead's eye: if the aggregate is not reachable as a list, this falls back to delete. |
| B-065 | `cli/tests/secrets/test_sources.py::test_direct_onepassword_table_mapping_gets_exact_source_rewrite@886d8a` | rewrite-hint verbatim pins | delete | `'account: "work.example.com"'` and `'<source-name>: "op://Work/Fixture/password"'` pin the exact YAML snippet our hint suggests. That snippet is authored content whose correctness is a review concern; the branch is discriminated by the raised type and the named source. |
| B-133 | `cli/tests/test_templates.py::test_git_identity_resolves@0f3547,test_git_identity_inherits_and_overrides@02bf09,test_unknown_workspace_template_key_is_refused@aca384` | injected-value equality and manifest-diagnostic substring | keep | `git_user_name == "Ada Lovelace"` / `"Base Bot"` are values the test's manifests declared (inheritance and override propagation). The `assert "workspace-template/default.git_user_emial: unknown field; expected one of: " in str(caught.value)` in the typo test should be trimmed to the `kind/name.field` prefix per B-062, but the manifest-location half earns its place. |
| C-008 | `cli/tests/test_config_setting_references.py::test_every_setting_reference_source_is_covered@74c074` | quantifier parity | keep | Pins that the module's case tables cover `_SETTING_REF_SOURCES` exactly, so a settings value added to production cannot ship with no dangling-name case exercising it. This is the quantifier every behavioral test in the file leans on. |
| C-009 | `cli/tests/test_config_setting_references.py::test_every_referenced_kind_is_a_registered_kind@f34930` | structural presence | keep | Each declared reference source must name a kind in `KIND_REGISTRY`. A typo'd kind reports every value of that setting as dangling, including correct ones, and is invisible until an operator hits it. |
| C-010 | `cli/tests/test_config_setting_references.py::test_dangling_setting_reference_is_a_hard_error@e6224b` | report-line substring | keep | **Corrected 2026-08-19 (map re-baseline).** Was delete. `source` is resolved per parametrize case from the production table `_SETTING_REF_SOURCES`, so `f"references unknown {{source.kind}}"` varies with the setting under test and fails when the dangling-reference diagnostic names the wrong kind. Nothing else in the file checks the kind once C-013's convert narrows `test_config_setting_references.py::test_a_misspelled_source_reports_as_a_bad_name_not_an_unreachable_secret` to the injected token, and deleting it would leave `source` an unused local. The `assert setting in message` and `assert "no-such-row" in message` asserts were never this row's and stay: the setting spelling and the operator's own value are value identity. |
| C-011 | `cli/tests/test_config_setting_references.py::test_dangling_hint_enumerates_what_is_declared@014a40` | hint content | keep | The hint must enumerate the rows the kind actually has (`_RESOLVING_NAME[setting] in hint`), which is registry-derived data, not authored prose; the remediation silently degrades to "declare one" if `_dangling_hint`'s enumeration is dropped. |
| C-012 | `cli/tests/test_config_setting_references.py::test_declarable_kind_hint_points_at_the_sample_command@c7f640,test_secret_source_hint_points_at_the_declarable_sample@defd26` | hint command pin | delete | `"agw resource sample vm-site"` / `"agw resource sample secret-source"` pin the authored remediation spelling. The branch they distinguish (declarable versus app-provided kind) has no production handle, and R2.2 forbids adding one, so the coverage goes with the sentence per R2.4. |
| C-013 | `cli/tests/test_config_setting_references.py::test_a_misspelled_source_reports_as_a_bad_name_not_an_unreachable_secret@fe2c49` | sentence pin plus wording blacklist | convert | Rubric re-check, 2026-08-16. `assert "references unknown secret-source 'envvar'" in message` and `assert "unreachable" not in message` are the ONLY assertions in their test, so the delete removes the whole test and with it the ordering invariant that `validate_setting_references` runs before `secret` chain validation. Keep the injected token `envvar`: the unreachable path names `npm-token` instead. No production change. |
| C-015 | `cli/tests/test_builtin_entries_parity.py::test_installer_plugin_rows_match_oracle_and_are_disabled_by_default@7a8f89` | provenance and enablement structure | keep | That the moved rows carry `variant == "system-plugin"`, the right plugin and manifest source, and `Enablement.disabled` by default is structure the registry computes, and is what makes the installer plugins genuinely opt-in. |
| C-016 | `cli/tests/test_builtin_entries_parity.py::test_remaining_builtin_manifest_origin_is_truthful@441fbe` | origin identity | keep | The built-in publisher must report its real package-and-file source rather than a generic label; this is a reference to a real shipped file, and provenance is what `agw resource list` renders. |
| C-076 | `cli/tests/test_resource_edit.py::test_builtin_capability_has_no_file_to_edit@ad2664` | **[unverified]** hint wording pin plus blacklist | delete | `"capability provided by the app" in hint` with `"resource sample" not in hint` polices the authored non-editable hint and blacklists one phrase someone already thought of. The typed `ValidationError` carries the raise. **The cost, stated as C-077 states it:** the raise at `resources/inspect.py::edit_location` sets no entity attributes at all, and the sibling test that asserts `entity_kind`/`entity_name` is on a `NotFoundError` from `resolve_resource`, a different path. So with C-076 and C-077 both applied, the capability-versus-declarable hint branch loses its only probe: a regression that gives a capability the declarable hint, or the reverse, passes. |
| C-077 | `cli/tests/test_resource_edit.py::test_builtin_declarable_resource_points_at_sample@dd9649` | hint command pin | delete | `"agw resource sample vm-site" in hint` pins the command spelling inside a declarable-kind hint. There is no production handle for it and R2.2 forbids adding one, so the assertion goes and the branch distinction goes with it. |
| C-084 | `cli/tests/test_resource_list.py::test_cli_old_kind_filter_is_an_unknown_kind_error@7c246e` | error-sentence pin | convert | `"unknown kind 'harness'" in str(result.exception)`. Replacement, using handles that already exist: `isinstance(result.exception, NotFoundError)` plus `result.exception.entity_name == "harness"`. `cli/agentworks/resources/inspect.py::list_resources` sets `entity_kind="resource-kind"` and `entity_name=unknown` on that raise. No production change. |
| C-097 | `cli/tests/test_config.py::test_git_credential_conforming_name_no_warning@64f22c,test_git_credential_nonconforming_name_with_explicit_secret_no_derived_warning@648c45,test_git_credential_name_deriving_secret_at_length_cap_no_warning@64f22c` | negative wording match, and it is vacuous | convert | Each asserts `not any("does not follow the naming rules" in issue for issue in _manifest_issues(config_file))`. **The needle never occurs**: the production warning at `cli/agentworks/manifests/decode.py::_nonconforming_secret` reads "does not follow the secret naming rules", so all three assertions pass whatever the loader does. Replacement: `_manifest_issues(config_file) == ()`, which is what each test's docstring actually claims. No production change. |
| C-098 | `cli/tests/test_config.py::test_orphaned_key_under_commented_section@53bcae` | keyword-in-issue | keep | `"platform"` and `"operator"` are the orphaned key and the section it landed in, both written by the test, so the assertion is that each bad placement produced its own issue naming it. The tokens are data. |
| C-101 | `cli/tests/test_config.py::test_code_workspaces_fails_before_default_vscode_write@81b70e` | message pin around an injected key | convert | `"unexpected keys in [paths]: code_workspaces" in str(result.exception)`. The invariant is that the retired key the test wrote is named back. Replacement: `"code_workspaces" in str(result.exception)`. The typed-exception check and `not (home / "aw-vscode-workspaces").exists()` stay. No production change. |
| C-102 | `cli/tests/test_config_env_and_secrets.py::test_agentworks_prefix_env_emits_warning@22d866,test_plaintext_env_with_newline_warns@b66f51` | injected name plus authored fragment | convert | Drop the `and "identity variable" in issue` / `and "newline" in issue` halves, keeping the injected-name half (`AGENTWORKS_VM`, `MULTILINE`). The invariant is that the offending entry is named. No production change. |
| C-104 | `cli/tests/test_config_env_and_secrets.py::test_unreachable_secret_error_message_and_hint@9434f8` | required-phrase list over an error and hint | delete | `"unreachable secret"`, `"active source chain"`, `"attemptable secret-source"` and `"remove"` are a four-phrase checklist over authored text with no structural counterpart. |
| C-105 | `cli/tests/test_config_env_and_secrets.py::test_unreachable_secret_error_message_and_hint@9434f8` | injected-value propagation | keep | The test's own secret name (`stranded`) and the configured source name (`env-var`) must appear so the operator knows which secret and which chain; value identity. |
| C-106 | `cli/tests/test_config_env_and_secrets.py::test_agentworks_prefix_warning_fires_for_every_scope@151809` | quantified coverage | keep | `unwarned == []` is computed by iterating every env table, so it cannot go stale as tables are added; that quantifier is the point of the test. |
| C-107 | `cli/tests/test_config_env_and_secrets.py::test_vm_template_tailscale_auth_key_nonconforming_warns_but_loads@9b14cf` | **[unverified]** authored fragment | delete | `any("the Tailscale auth key" in issue ...)` pins wording; the sibling above it (`assert any("GITHUB_TOKEN" in issue and "vm-template/tester" in issue for issue in issues)`) already asserts the issue names both the secret and the row that misuses it. |
| C-108 | `cli/tests/test_config_env_and_secrets.py::test_conforming_secret_ref_names_emit_no_warning@0407e2` | **[unverified]** negative wording match | convert | Same shape and same replacement as C-097 (`issues == ()` for the clean config). Not executed: this config was not in the executed probe. |
| C-112 | `cli/tests/test_env_block_references.py::test_env_entry_secret_ref_emits_secret_requirement@22e048,test_vm_template_dependencies_uses_template_name_in_source@789dcc,test_undeclared_env_secret_auto_declares_through_build_registry@f57bd9` | usage-template equality | delete | `usage == "the API_KEY env var"` and `== "the Tailscale auth key"` pin an authored template; the reference's name, kind and source assertions beside them carry the structure. |
| C-113 | `cli/tests/test_env_block_references.py::test_multiple_env_refs_from_one_resource_each_contribute_usage@ce6368` | usage-template list equality | convert | Replace `texts == ["the KEY_A env var", "the KEY_B env var"]` with `len({u.usage for u in dependents}) == 2`. The invariant is that two env refs from one row contribute two distinguishable usages. No production change. |
| C-146 | `cli/tests/test_git_credentials_typo_errors.py::test_admin_referencing_undeclared_git_credential_errors_at_finalize@7f1876,test_agent_template_referencing_undeclared_git_credential_errors@c26f60` | dangling-reference identity | keep | The error must name both the missing credential and the row referencing it; every asserted token is a name the test declared, and this is what turns a typo into a findable one. |
| C-175 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_rbac_denial_raises_authorization_error@49ce1b` | provider error code | keep | `"AuthorizationFailed" in str(exc.value)` pins Azure's own error taxonomy reaching the operator: the rule's outside-the-repository exception covers exactly this. |
| C-180 | `cli/tests/test_azure_nsg_exposure.py::_assert_deny_shape@396e36,_assert_allow_shape@690554,TestCreate.test_create_provisions_deny_baseline_and_scoped_bootstrap_allow@2c6922,TestCloseProvisioningAccessHooks.test_post_tailscale_ready_deletes_the_bootstrap_allow@b1af31,TestCloseProvisioningAccessHooks.test_secure_failed_vm_deletes_the_bootstrap_allow@b1af31,TestTransientRoute.test_enter_converges_then_pokes_and_exit_removes@5c61dc,TestTransientRoute.test_poke_attempt_cleanup_failure_warns@343662,TestTransientRoute.test_exit_removes_allow_when_body_raises@4b2515,TestTransientRoute.test_exit_removes_allow_when_body_raises::RuntimeError::59596d,TestTransientRoute.test_exit_removal_failure_warns_with_rule_and_prefixes@340923` | NSG rule shape and lifecycle | keep | Access, direction, protocol, port, the created and deleted rule identities, and the injected CIDR appearing in the warning are the exposure boundary itself: a wrong value here opens SSH to the world. |
| C-182 | `cli/tests/test_azure_nsg_exposure.py::TestPerOperationAllows.test_band_exhaustion_raises_typed_error@bdc4fe` | production-constant echo | keep | `TRANSIENT_ALLOW_RULE_PREFIX in hint` compares against the production constant, so it is parity rather than a copy, and it is what points the operator at the rules to clean up. |
| C-183 | `cli/tests/test_azure_nsg_exposure.py::TestPerOperationAllows.test_description_carries_timestamp_marker@3e10c9` | timestamp shape in a rule description | keep | The transient rule's description carries a UTC timestamp the sweep path relies on to age rules out; the regex pins the shape, not the wording around it. |
| C-185 | `cli/tests/test_aws_ec2_ops.py::TestCreate.test_user_data_over_cap_is_typed@fa4070,TestCreate.test_disk_describe_images_failure_is_typed@6d0fd0` | message and IAM-action pins | convert | Rubric re-check, 2026-08-16. `"10-byte"` is NOT authored wording: `10` is the value the test monkeypatches into `_MAX_USER_DATA_BYTES` (`monkeypatch.setattr("agentworks.plugins.aws.platform._MAX_USER_DATA_BYTES", 10)`), and production interpolates the real constant. Assert against the monkeypatched cap so it stays derived. **Open for the group-3 executor, 2026-09-06:** the IAM-action pin the shape cell names is not at this basis. `test_aws_ec2_ops.py::TestCreate.test_disk_describe_images_failure_is_typed` asserts `entity_kind`, `entity_name`, a non-`None` hint and a non-`None` `__cause__` and nothing about the action, so that anchor carries no work; whether it leaves the row is the executor's call, not this round's. No production change. |
| C-186 | `cli/tests/test_aws_ec2_ops.py::TestCreate.test_provisions_and_records_metadata_without_public_ip_fields@0c3144,TestCreate.test_security_group_is_created_with_no_ingress@a04673,TestCreate.test_bootstrap_allow_scoped_to_detected_and_configured@6699b9,TestCreate.test_launch_pins_a_permanent_auto_public_ip@fad63d,TestCreate.test_resolves_default_subnet_when_unconfigured@fd06ed,TestCreate.test_no_default_subnet_is_typed@ab85d5,TestCreate.test_no_default_subnet_is_typed::EC2Error::cb137f,TestCreate.test_default_subnet_pick_is_deterministic_lowest_az@67c5e8,TestCreate.test_user_data_is_gzipped_and_under_cap_with_a_large_key@2e3070,TestCreate.test_user_data_over_cap_is_typed@fa4070,TestCreate.test_user_data_over_cap_is_typed::EC2Error::165daa,TestCreate.test_always_resolves_debian_ami_from_ssm_for_arch@9af321,TestCreate.test_rejects_a_name_collision@64dd3a,TestCreate.test_rejects_a_name_collision::StateError::321425,TestCreate.test_collision_check_fails_closed@ecd013,TestCreate.test_arch_cross_check_rejects_a_mismatch@ac068c,TestCreate.test_arch_cross_check_rejects_a_mismatch::ConfigError::808d46,TestCreate.test_disk_request_sizes_the_root_volume@c15199,TestCreate.test_every_create_sizes_the_root_volume@db1970,TestCreate.test_disk_describe_images_failure_is_typed@6d0fd0,TestCreate.test_tailscale_path_waits_for_bootstrap@afbd88` | SDK call and payload assertions | keep | Which EC2 calls were made, with which security group, subnet, image, device mapping and user-data encoding, is the platform's behavior against the provider's own contract. **Re-derived 2026-09-06 at `c310d05b`.** The test survives and its `match=` does not: it discriminates on `entity_kind`, `entity_name`, `hint` and `__cause__` now, which is the structural handle `hla.md` case 2 asks for. The narrowing this row wanted has already landed there. |
| C-188 | `cli/tests/test_capability_config_contract.py::test_cycle_reported_before_malformed_block@83ae1c` | negative wording match | delete | `"unknown shell harness field" not in str(exc.value)` identifies a branch by the absence of one phrasing, which is the blacklist failure mode; the raised type is what the test should lean on. |
| C-189 | `cli/tests/test_capability_config_contract.py::test_capability_refs_attributed_to_consuming_resource@4a9150` | auto-description template | convert | Replace `"the signing key for git-credential/signer" in decl.description` with `"git-credential/signer" in decl.description`: the auto-declared row must name its source, which is data. No production change. |
| C-190 | `cli/tests/test_capability_config_contract.py::test_azdo_org_required@2be960,test_azdo_rejects_unknown_blob_fields_yaml@769f73,test_a_broken_blob_is_refused_whatever_the_plugins_state@213400,test_a_valid_blob_on_a_disabled_plugin_loads_with_the_row_marked@69bed3` | injected-value propagation | keep | The manifest filename and the platform name the test declared must reach the error and the not-ready reason: value identity across the manifest boundary. |
| C-191 | `cli/tests/test_name_validation.py::test_valid_names@e7030f,test_invalid_names@46745e,test_single_letter@ec6970,test_single_digit@9c51fa,test_single_hyphen@974228,test_single_underscore@184673,test_double_hyphen_allowed_when_flag_set@ed2ed9,test_double_hyphen_flag_does_not_relax_other_rules@7d2314,test_name_at_cap_ok_over_cap_rejected@a48614,test_kind_caps_have_expected_values@0cc120,test_agent_cap_yields_max_length_username@0407ff,test_workspace_cap_yields_max_length_group@a1ab76,test_vm_cap_yields_max_length_azure_vnet_name@8fa114,test_session_cap_yields_max_length_socket_path@5cc018,test_secret_cap_is_larger_than_agent_cap@14cb15,test_secret_name_over_agent_cap_passes@015b08,test_secret_cap_error_message_reports_secret_max@ac1145,test_secret_cap_does_not_relax_character_rules@776ce7` | validator behavior | keep | The name grammar, the caps, the prefix arithmetic against `MAX_*_LENGTH`, and the boundary cases (exactly at cap, one over) are the validator's behavior at a real operator-input boundary. |
| D-001 | `cli/tests/schema/test_errors.py::test_an_unknown_key_names_the_fields_that_are_valid@6f5180` | whole-rendered-line pin | convert | The invariant is that an unknown key is addressed at its own path and enumerates the container's real fields; replace with `_problems` `path`/`unknown_field` plus a field list derived from `PrincipalLike.model_fields`. Verified by execution: `_problems` returns `('client_ids', ..., unknown_field=True)`. |
| D-002 | `cli/tests/schema/test_errors.py::test_structural_collapse_uses_unknown_field_state_not_rendered_wording@69591c` | monkeypatched wording probe + `(path, message)` equality | convert | Keep `[(p.path, p.unknown_field)] == [("secrit", True)]` (the structural-collapse invariant) and drop both the monkeypatch and the message. **Verified by execution: the monkeypatch is inert.** `_structural_shape_problems` (`schema/errors.py::_structural_shape_problems`) builds its own `f"unknown field; expected one of: {expected}"` and never calls the patched `_unknown_field` (`schema/errors.py::_contextual_message` is its only caller), so the test's stated premise is false and the message pin is a second copy of a string production authors 14 lines away. |
| D-003 | `cli/tests/schema/test_errors.py::test_a_wrong_type_says_what_the_field_must_be@8869c2,test_a_missing_required_field_says_it_is_required@349e22` | whole-rendered-line pins | delete | "must be a string" / "is required" are authored normalizations of a pydantic error type; the path halves are unremarkable and are re-asserted by the path-rendering section. Nothing structural is lost. |
| D-004 | `cli/tests/schema/test_errors.py::test_a_missing_required_UNION_also_names_the_choices_it_is_between@3f37e7` | whole-rendered-line pin | convert | Rubric re-check, 2026-08-16. **Verified by execution**: with `_missing_union_block` stubbed to return `None`, exactly ONE assertion fails across the full 7,424-test suite, this one. Replace with a tags-derived-from-`SiteLike` cross-check. No production change. |
| D-005 | `cli/tests/schema/test_errors.py::test_a_bad_capability_name_lists_the_names_that_are_registered@9fea90` | whole-rendered-line pin | delete | Same shape as D-004 for a misspelled tag. The registry itself is proven non-empty by D-006's cross-check, which survives. |
| D-006 | `cli/tests/schema/test_errors.py::test_an_absent_capability_name_lists_the_same_names_a_wrong_one_does@250d0c` | two spelled literals plus a line-to-line equality | delete | **Corrected 2026-08-19 (map re-baseline).** The range read the whole test while the prose kept the non-vacuity guard and also listed that guard for deletion. Drop `assert absent == ["vm-site/lab.platform: name is required; registered: 'lima', 'proxmox'"]` alone, the spelled literal. Keep `assert "'lima', 'proxmox'" in misspelled[0]`, which the comment above it (`# Not vacuous: an empty registry would leave both lines listing nothing.`) names as the non-vacuity guard, and `assert absent[0].endswith(misspelled[0].split("; ", 1)[1])`, which pins no wording at all: it says one union answers with one list, which is the invariant and the thing that can regress. **The kept line's own disposition is DEFERRED, and recorded rather than left silent:** the 2026-08-19 charter covered the contradiction between this row's range and its prose, not the question of whether the non-vacuity guard should itself convert. It pins `'lima', 'proxmox'`, which is value identity on the fixture's own registered arms, so deriving it from that registry instead is a real option and nobody has ruled on it. The row keeps that guard as it stands until someone does. |
| D-007 | `cli/tests/schema/test_errors.py::test_a_capability_name_the_loader_read_as_a_boolean_is_not_quoted_back_as_python_wrote_it@d0a7b3` | pyyaml-derived assertions plus two spelled literals | delete | **Corrected 2026-08-19 (map re-baseline).** Range narrowed from the whole test to the two lines it actually drops, because the range swept up the three the justification keeps. Drop `assert "quote it to name one" in line, line` and `assert "registered: 'lima', 'proxmox'" in line, line`, which are authored sentence fragments. Keep the two pyyaml-derived asserts (`assert spellings, "pyyaml resolves no plain scalar to this boolean, so the message has nothing to name"` and `assert all(f"'{text}'" in line for text in spellings), line`), whose spellings derive from `yaml.constructor.SafeConstructor.bool_values`, an upstream table and the sanctioned external exception, and `assert repr(str(written)) not in line, line`, a guard against a Python repr reaching an operator rather than a wording pin. |
| D-008 | `cli/tests/schema/test_errors.py::test_one_error_renders_in_decodes_shipped_single_line_shape@30454e,test_one_error_without_a_location_is_just_the_owner_framed_line@82b698` | full-message equality, one-error framing | convert | The invariant is decode's single-line framing (`<location>: <owner>.<path>:` plus a trailing space), not the normalized message; assert the framing prefix and let the message tail go. No production change: the prefix is composed from `SourceLocation` and `RefOwner`, both already constructed in the test. |
| D-009 | `cli/tests/schema/test_errors.py::test_a_synthesized_location_frames_nothing@82b698` | full-message equality, synthesized sentinel | convert | Real invariant (a synthesized location must render no navigable path). Replace the spelled message with the derived equality `str(_raised(..., location=synthesized())) == str(_raised(..., location=None))`, which states the property exactly and pins no wording. |
| D-010 | `cli/tests/schema/test_errors.py::test_a_location_with_no_line_still_names_the_file@cb3e68` | full-message equality, `line == 0` sentinel | convert | Real invariant and NOT the same answer as D-009 (file named, position dropped). Replace with `startswith("sites.yaml: ")` plus `":0" not in`, the shape `test_error_framing.py::test_an_unreadable_file_names_the_file_and_no_line` already uses for the same sentinel. |
| D-011 | `cli/tests/schema/test_errors.py::test_the_owner_rides_along_as_the_errors_entity@1fc438,test_a_hint_is_carried_through@6fefd7` | attribute equality | keep | `entity_kind`, `entity_name` and `hint` are structured fields of `ConfigError` carrying the owner and a caller-supplied hint; a bridge that stopped populating them would break every downstream consumer silently. Not prose. |
| D-012 | `cli/tests/schema/test_errors.py::test_several_errors_render_under_one_located_header@d692f1` | verbatim four-line block pin | delete | Keep the batch's structural facts (a located header, one indented line per problem, problem count), which D-013's two tests already assert (`schema/test_errors.py::test_no_line_of_a_multi_error_batch_is_unlocated` and `schema/test_errors.py::test_a_multi_error_batch_without_a_location_still_reads_as_one_message`), and drop the spelled body. The three problem paths can be asserted through `_problems` if the lead wants the address coverage retained. **Corrected 2026-08-19 (map re-baseline).** The 2026-08-18 cross-group re-check listed this row as a range hazard; it is not one. This row addresses one test whose whole body is the verbatim block pin, and the structural facts live in D-013's separate tests, which this row does not touch. |
| D-013 | `cli/tests/schema/test_errors.py::test_no_line_of_a_multi_error_batch_is_unlocated@cd8d03,test_a_multi_error_batch_without_a_location_still_reads_as_one_message@178ff9` | header/indent structure | keep | The design's load-bearing invariant: no line of a multi-error batch is unlocated. Asserted as `startswith(location)` plus all-body-indented, which pins layout and no wording, and fails when a call-site framing regresses. |
| D-014 | `cli/tests/schema/test_errors.py::test_a_capped_batch_says_how_many_it_did_not_show@53170e` | count assertions plus `"  ... and 3 more"` literal | convert | Keep the header and body-length asserts (both derived from `MAX_ERROR_LINES`). Replace `assert body[-1] == "  ... and 3 more"` with a check that the final line carries the residual count (`str(3)`), so the cap's arithmetic stays guarded without pinning the sentence around it. |
| D-015 | `cli/tests/schema/test_errors.py::test_the_cap_hides_nothing_from_the_rendering_core@861ecf,test_every_error_the_exception_carries_gets_a_problem@9befdd` | `_problems` cardinality | keep | The cap is presentation only: every problem survives for a diagnostic surface, and `len(_problems(...)) == exc.error_count()` catches a walk that drops errors. Pure counting. |
| D-016 | `cli/tests/schema/test_errors.py::test_the_union_arm_tag_is_dropped_from_the_path@11881a,test_an_unknown_key_inside_a_union_arm_lists_that_arms_fields@fd49e8` | whole-line pins carrying a path | convert | The union-arm-tag drop is real logic that regresses to `platform.lima.vm_host`; assert `[p.path for p in _problems(...)]`. Verified by execution: paths are `platform.vm_host` and `platform.regions`. |
| D-017 | `cli/tests/schema/test_errors.py::test_a_nested_block_renders_as_a_dotted_path@14c200,test_a_deeper_block_keeps_its_whole_path@50a98c` | whole-line pins carrying a path | convert | Same conversion; verified path `accounts_by_name.prod.secret` and `middle.deep.entry`. The dotted-path walk is the code under test and the message is incidental. |
| D-018 | `cli/tests/schema/test_errors.py::test_a_list_index_belongs_to_the_field_that_holds_it@a7f6a3,test_a_marked_list_element_is_addressed_by_its_index@0aac79` | whole-line pins carrying a path | convert | Same conversion; verified paths `vm_sizes[1].cpus` and `inherits[1]`. Index placement is exactly what an index-walk bug moves. |
| D-019 | `cli/tests/schema/test_errors.py::test_a_whole_document_problem_carries_no_path_and_is_located_like_any_other@1c8ba3` | whole-message equality | convert | **Recipe verification, 2026-08-16.** Convert, with the guard stated honestly: the `path == ""` half cannot bite, because pydantic's `loc` for a root model's own problem is empty and the input forces it. The half that does bite is the located framing, verified against a `noloc` break. State the invariant as the location half. |
| D-020 | `cli/tests/schema/test_errors.py::test_a_root_model_addresses_the_fields_of_what_it_wraps@0ed44c` | whole-line pin carrying a path | convert | The walk has to start inside what a root model wraps or it loses the first segment; assert the path. |
| D-021 | `cli/tests/schema/test_errors.py::test_a_single_arm_unions_tag_is_dropped_like_any_other@330139` | whole-line pins carrying a path | convert | The single-arm-union collapse is a live shape (one registered implementation); verified path `platform.bogus` with `unknown_field=True`. Assert path plus the unknown-field flag. |
| D-022 | `cli/tests/schema/test_errors.py::test_an_undiscriminated_unions_arm_name_is_dropped_too@92a71b,test_an_undiscriminated_union_keeps_its_arms_field_list@85d3ab` | indented-line substring pins | convert | Same: the class-name segment drop is observable as `path == "platform.vm_host"` / `"platform.bogus"` on the `_problems` record (verified). The indentation half is already covered by D-013. |
| D-023 | `cli/tests/schema/test_errors.py::test_a_constrained_table_key_is_addressed_without_pydantics_key_marker@b864bb,test_a_table_key_spelled_like_the_marker_is_still_addressed@6b1747` | whole-line pins carrying a path | convert | The `[key]` marker drop is a path-walk rule; verified paths `env.lower` and `env.[key]`. The message tail is the fixture's own validator text (test-injected), so it may ride along or go, but the path is what earns the test. |
| D-025 | `cli/tests/schema/test_errors.py::test_a_value_matching_no_alternative_of_a_union_is_one_problem@7aa05e,test_a_collapsed_union_line_replaces_pydantics_member_labels@44cd9c` | whole-line pin plus problem counts | convert | Keep the second anchor's `assert exc.error_count() == 3` and `assert len(_problems(exc, MappingValueLike, OWNER)) == 1`, which is the collapse invariant stated structurally. Convert the first anchor's `_lines(MappingValueLike, {"backend_mappings": {"npm": 3}})` equality to the same counting plus `alternatives`, which `_Problem` exposes (verified `('a string', 'a table', 'False')`). |
| D-026 | `cli/tests/schema/test_errors.py::test_the_arm_that_got_past_the_shape_answers_for_the_whole_union@3c89a8` | whole-line pin | convert | The rule ("the arm that got furthest answers") is observable as one problem at path `reference` (verified). Assert cardinality plus path; the message is incidental. |
| D-027 | `cli/tests/schema/test_errors.py::test_a_shape_rejection_is_noise_whichever_end_of_the_batch_it_lands_on@0b37f6` | whole-line pin | convert | Mirror case, same conversion: one problem, empty path (verified). |
| D-028 | `cli/tests/schema/test_errors.py::test_a_union_no_arm_could_enter_still_lists_every_alternative@2de79f` | whole-line pin | convert | "Every alternative survives when no arm entered" is structurally exposed as `_Problem.alternatives` (verified `('a string', 'a table')`), so the invariant keeps a guard without the sentence. |
| D-029 | `cli/tests/schema/test_errors.py::test_container_types_read_as_the_shapes_an_operator_writes@bc8b49,test_a_non_table_where_a_capability_block_belongs_says_table@72e8b7` | parametrized normalization-message pins | convert | **Corrected 2026-08-19 (map re-baseline).** Was delete on a false premise. `_Problem.path` is a structural handle for the address half, `_problems` is already imported by this file, and `blob`/`expected` are columns of a parametrize table, so each case proves its own malformed input routes to its own address (`vm_sizes` versus `vm_sizes[0]` versus `accounts_by_name`). That is per-case routing, which G1-K11 keeps for the same reason. Convert on D-016's recipe: assert `[p.path for p in _problems(...)]` per case. Only the phrasing `_TYPE_MESSAGES` supplies (`schema/errors.py`'s pydantic-error phrasing table) has no handle, and that half goes under R2.4. |
| D-030 | `cli/tests/schema/test_errors.py::test_closed_choices_and_emptiness_are_normalized@79ccd1` | parametrized normalization-message pins | convert | **Corrected 2026-08-19 (map re-baseline).** Same correction as D-029 and the same recipe: the two cases route to `.arch` and `.label`, which `_Problem.path` carries. Cost stated: the rendered choices list (`'arm64' or 'x86_64'`) loses its only assertion here, and the choices themselves stay covered structurally by `test_spec_admin_and_console.py::test_the_layouts_reach_the_explain_surface_as_choices` for its own model. |
| D-031 | `cli/tests/schema/test_errors.py::test_a_constrained_string_shape_reads_as_the_rule_it_must_match@b2b1fa,test_a_pattern_containing_a_slash_is_still_readable@df41f7` | whole-line pins around a pattern | convert | The load-bearing half is that the field's OWN pattern reaches the operator unmangled, and the pattern is supplied by the test's fixture (`Field(pattern=...)`), so assert the fixture's pattern string appears in the rendered line and drop the "must match `...`" framing around it. Both tests convert identically. |
| D-032 | `cli/tests/schema/test_errors.py::test_a_length_floor_above_one_keeps_pydantics_exact_wording@b656df` | upstream-message pin, spelled | convert | The invariant (a `min_length` above 1 is not paraphrased) is real, but the assertion spells pydantic's sentence instead of deriving it. Convert to the shape its own sibling `schema/test_errors.py::test_an_unmapped_error_type_falls_through_verbatim` already uses: read `detail['msg']` off the pydantic error and compare. |
| D-033 | `cli/tests/schema/test_errors.py::test_an_unmapped_error_type_falls_through_verbatim@3c7857` | fall-through parity, derived | keep | Derives the expectation from pydantic's own `detail["msg"]` rather than spelling it, and guards that an unmapped error type is carried through instead of silently blanked. Derivation parity against an external source. |
| D-034 | `cli/tests/schema/test_errors.py::test_every_corpus_shape_renders_something_an_operator_can_read@1f82bc` | non-emptiness over a corpus | keep | Guards that no corpus shape falls through the walk to an empty message. Asserts existence, never content. |
| D-035 | `cli/tests/schema/test_errors.py::Exclusive._mutually_exclusive@e3b0c4,test_a_validators_own_message_is_carried_without_pydantics_prefix@597bca` | rendered-line pin of a fixture-authored message | keep | The message is raised by the test's own `Exclusive._mutually_exclusive` validator, so this is value identity proving pydantic's "Value error, " prefix is read off the context rather than sliced off the text. A regression re-introduces the prefix and this catches it. |
| D-036 | `cli/tests/schema/test_errors.py::test_the_bridge_produces_a_plain_config_error@538fc3` | exception-type identity | keep | `type(raised) is ConfigError` exactly, never a subclass: the finalize pass has one framing to leave alone. Error-type assertion, explicitly a keep shape. |
| D-037 | `cli/tests/schema/test_errors.py::test_an_owner_label_overrides_the_kind_slash_name_framing@502959` | prefix assertion on a test-injected label | keep | `"[azure]"` is constructed by the test (`owner = RefOwner(kind="vm-site", name="lab", label="[azure]")`); asserting it reaches the front of the message proves the label override propagates. Value identity, not authored prose. |
| D-038 | `cli/tests/schema/test_block.py::test_an_omitted_tag_reads_as_a_missing_field@5baa6c,test_an_empty_tag_is_refused@703716,test_a_non_table_reads_as_a_table_requirement@64cd79` | three whole-message equalities | convert | **Corrected 2026-08-19 (map re-baseline).** The "or delete" is closed toward convert, which also discharges the row's unverified tag: the duplication claim against `test_errors.py` no longer carries the disposition, so the row is no longer one of the reasoned-not-executed set. The block-level facts are ADDRESSES: an omitted tag reads at `platform.name` rather than as a shape error at `platform`, and a bare string reads at `platform`. `_Problem.path` carries that and varies with the defect, so under `hla.md` case 2 the handle exists and the row converts on D-016's recipe. Cost stated: `is required` versus `must not be empty` (pydantic's `missing` versus its length rule) share the path, and no handle exposes the error type, so that one distinction goes under R2.4. The framing half is disclaimed by the file's own helper docstring at `test_block.py::_message`. |
| D-039 | `cli/tests/schema/test_structural_union.py::test_overlapping_table_languages_are_loud_even_without_markers@2db9ab,test_optional_overlapping_table_languages_stay_loud_without_markers@2db9ab` | reason substring naming fixture classes | keep | `First` and `Second` are classes each of this row's two tests defines itself; the diagnostic naming the two colliding arms is what a plugin author needs to find the collision, and the assertion is value identity on test-injected names. The `structural_union_error(...) is not None` half already carries the branch. |
| D-040 | `cli/tests/schema/test_structural_union.py::test_a_marker_free_validation_alias_is_still_loud@87253b` | reason substring naming a fixture field | keep | `Aliased.value` is defined by the test itself; the reason naming the aliased field is value identity and the actionable half of the diagnostic. |
| D-041 | `cli/tests/schema/test_structural_union.py::_assert_selector_is_refused@bcaa90` | authored-phrase pins | delete | `"selector-free"` is authored wording with nothing injected; `"combines StructuralUnion with discriminator 'mode'"` is authored framing whose only injected token is the fixture's discriminator name. The family is already discriminated by calling `structural_union_error` directly (`reason = structural_union_error(model)`). Keep the `is not None`; drop both phrases. |
| D-042 | `cli/tests/schema/test_shorthand.py::test_an_arm_shorthand_alone_does_not_dispatch_a_tagged_union@320bda` | authored-phrase pin | delete | `"declares no UnionScalarShorthand"` is our own sentence; `union_scalar_shorthand_error(Holder)` returning non-None already says which check fired, since calling the specific checker is the discriminator. Verified by execution (experiment D): the three sub-checkers each answer for exactly one family. |
| D-044 | `cli/tests/manifests/test_samples.py::test_all_kinds_concatenation_and_unknown_kind@c0e8ac` | per-kind header regex | keep | `re.search(rf"^## kind: {re.escape(kind)}(?=[:\s])", everything, re.MULTILINE)` for every declarable kind. Structural presence derived from the registry: a kind silently missing from the `--all` concatenation is the regression, and the pattern is built with `re.escape(kind)` rather than spelled out. |
| D-048 | `cli/tests/manifests/test_reference.py::test_an_authored_example_is_what_the_sample_writes@69f4fc,test_a_closed_field_with_ONE_value_writes_it@a8791a` | rendered-line pins of fixture values | keep | `me@gpu-box` is the fixture's own `host: str = Field(examples=["me@gpu-box"])` and `lima` is the fixture union's tag; asserting they reach the rendered document is value identity proving the authored example propagates, which is the whole point of the example plumbing. |
| D-049 | `cli/tests/manifests/test_reference.py::test_a_closed_field_with_SEVERAL_values_writes_its_default@d738bb,test_a_default_worth_showing_is_shown_and_an_empty_one_is_not@0cf37f,test_a_field_with_no_default_and_no_example_gets_a_typed_placeholder@f118e5` | rendered-line pins of fixture defaults | keep | `mode: safe`, `replicas: 3`, `enabled: false`, `tags: [<string>]`, `note: <string>` all restate values the fixture model declares (`mode: Literal["fast", "safe"] = "safe"` and its neighbors); what is guarded is which default is written and whether a falsy one is treated as absent, which `manifests/field_tree.py::worth_showing` can and did get wrong. |
| D-050 | `cli/tests/manifests/test_reference.py::test_only_required_fields_are_live_document_lines@57c0a4` | live-versus-commented partition | keep | "Only required fields are live document lines" is the property the whole sample design rests on, and the assertion computes the partition from the prefix rather than pinning prose. A renderer that emitted an optional field live fails here. |
| D-051 | `cli/tests/manifests/test_reference.py::test_an_owner_templated_field_is_not_the_operators_to_write@599554` | authored explanatory sentence, reflowed | convert | Keep `assert "#  # token: <string>" in lines` (the owner-templated field renders commented, not live). Replace the `defaults to the resource named` assert with a check that the fixture's own `default_template` (`git-token-{owner_name}`, declared on `GithubLike`) reaches the rendered text, so what is guarded is template propagation rather than the sentence wrapped around it. |
| D-052 | `cli/tests/manifests/test_reference.py::test_a_reference_field_says_what_it_names@195ca6` | rendered-line substring | keep | `"names a vm-template"` is `f"names a {kind}"` where the kind comes from the fixture's `ResourceRef`; the only load-bearing token is the fixture-supplied kind, so this is value identity. Trim to the kind if the lead wants it tighter. |
| D-053 | `cli/tests/manifests/test_reference.py::test_a_nested_block_renders_as_a_block@ef31d2,test_a_collection_of_blocks_renders_one_element_under_a_placeholder@e0d092` | rendered YAML-structure pins | keep | These pin generated document STRUCTURE (a nested block opens as a block; a sequence element has no key, which `-:` would silently change into a different document; a table element hangs under `<key>`), all derived from fixture models. Structural presence, and the sequence/mapping distinction is a real defect this caught. |
| D-055 | `cli/tests/manifests/test_reference.py::test_an_uncommented_required_list_of_tables_loads_and_validates@d35046` | uncomment-and-validate | keep | Derivation-and-behavior: the skeleton is stripped by the documented rule, parsed as YAML, and validated against the model. Pins no wording and is the module's headline promise. |
| D-056 | `cli/tests/manifests/test_reference.py::test_an_enum_default_renders_as_the_value_a_document_carries@e8aaa6` | rendered-line pin of a fixture enum default | keep | `layout: vertical` is the fixture's declared default; the guarded defect (an enum MEMBER reaching pyyaml and taking the document down with a `RepresenterError`) is a real crash, not a wording question. |
| D-057 | `cli/tests/manifests/test_reference.py::test_one_arm_is_rendered_and_the_rest_are_listed@0005a8` | authored "One of / Shown here" sentence plus structure | convert | Keep `assert "#    name: lima" in lines` and `assert not any("proxmox" in line and "name:" in line for line in lines)` (exactly one arm is written into the document, and the other arm is not). Replace `assert any("One of: lima, proxmox. Shown here: lima." in line for line in lines)` with a derived check that every `alternatives` name from the record appears in the rendered text, which preserves "the operator is told the other arms exist" without pinning the sentence template. Not redundant with `manifests/test_reference.py::test_the_rendered_arm_is_the_first_registered_one`, which asserts the record only. |
| D-058 | `cli/tests/manifests/test_reference.py::test_the_rendered_arm_is_the_first_registered_one@4133c5,test_an_alternative_gets_an_address_only_when_the_address_exists@14d0da,test_a_table_of_tagged_blocks_hangs_its_element_under_a_placeholder_key@4735a0,test_the_tree_offers_every_element_arm_the_stream_does@d10192,test_a_tagged_collection_arm_reachable_from_itself_stops_rather_than_recurring@632fed,test_two_sibling_fields_sharing_an_arm_each_expand_it@95e380` | record-based assertions | keep | Assert `rendered`, `alternatives`, `target`, `recurring`, `fields` and cross-check the tree against the field-doc stream. Structural, derived, and the model the rest of this file should be converted toward. |
| D-059 | `cli/tests/manifests/test_reference.py::test_a_collection_of_tagged_blocks_renders_an_element_naming_its_arms@51d599` | authored "One of / Shown here" sentence plus structure | convert | Same as D-057: keep `#  nodes:`, `#    -`, `#      name: group` (generated structure, fixture values), replace the sentence pin with the derived arm-name presence check. The placeholder sentence (`assert "#    # one element, as an example:" in lines`) goes with D-054. |
| D-061 | `cli/tests/manifests/test_reference.py::test_an_unexpanded_arm_does_not_name_what_is_shown@1963d0` | absence assertion on a Python repr | keep | `"Shown here: None" not in flowed` is not a wording blacklist: it guards a Python `None` leaking into operator-facing text, the same defect class as `schema/test_errors.py::test_a_capability_name_the_loader_read_as_a_boolean_is_not_quoted_back_as_python_wrote_it`. What regresses is a renderer branch, not a sentence. |
| D-062 | `cli/tests/manifests/test_reference.py::test_a_nested_tagged_union_expands_every_arm_because_none_is_addressable@a1614d` | docstring pin of a shipped model | delete | `"Run limactl on another host over SSH."` is `SshPlacement`'s own docstring maintained a second time inside a test. The structural half in the same test (arm names, targets and per-arm field lists, from `assert [alt.name for alt in placement.alternatives] == ["local", "ssh"]` down) carries the invariant and survives. |
| D-063 | `cli/tests/manifests/test_describe_kind.py::test_a_kind_shows_its_prose_then_its_two_blocks@cc1521` | title, summary and overview pins | convert | Keep `assert "\nmetadata:\n" in text` and `assert "\nspec:\n" in text` (the two blocks are present, structural). The `assert text.startswith("VM sites (vm-site, resource kind)")` is half derived (target, category) and half authored title; `assert "Configured places to create VMs" in text` and `assert "A vm-site is a configured place VMs come from" in text` are `topics.py` prose maintained twice. Convert to a check that the rendered head carries the kind's own registered title/summary read from the registry, which is derivation parity rather than a second copy. |
| D-064 | `cli/tests/manifests/test_describe_kind.py::test_a_field_carries_its_type_requiredness_and_description@084a84` | rendered-line pins of shipped field docs | convert | The invariant worth keeping is that the type/requiredness/example parenthetical is rendered from the model (`e.g.` values as YAML, not Python `repr`), and `FieldEntry`/`doc` carry all of it. Assert against the record for the same field. `assert "Operator-facing text shown when the secret has to be entered by hand" in text` is the shipped `hint` description maintained twice and simply goes. |
| D-065 | `cli/tests/manifests/test_describe_kind.py::test_secret_source_kind_describes_the_backend_union_and_override_provenance@b82487` | **[unverified]** shipped capability descriptions pinned three times | delete | `"env-var: resolves from ..."`, `"prompt: ..."`, `"onepassword: ..."` are the shipped backends' `description` ClassVars restated in a test, and `assert "operator declaration with either name replaces that built-in row" in text` is a sentence from `secrets/kinds.py`. That every registered backend is named at all is worth keeping and is a derived check (see D-066); the descriptions are not. |
| D-067 | `cli/tests/manifests/test_describe_kind.py::test_an_optional_root_union_keeps_its_outer_null_spelling@0f93ea` | **[unverified]** rendered parenthetical pin | delete | **Recipe verification, 2026-08-16.** **The recorded conversion is not bitable and the row therefore becomes a delete.** The pure-record form (`doc.required is False`, `NoneType in get_args(doc.annotation)`) reads the declaration straight off the model, so it restates what it claims to check and no break makes it fail. Pairing it with a rendered check would reintroduce the message assertion this sweep is removing. Nothing real is lost: the declaration is the source of truth and mypy carries it. |
| D-068 | `cli/tests/manifests/test_describe_kind.py::test_a_field_that_folds_a_scalar_offers_both_spellings@6c61a9` | six rendered-line pins over a shipped model | convert | The two arm summaries (`- plaintext: An env var whose exported value is written as plaintext.` and `- secret: An env var whose exported value comes from a declared secret.`) are shipped docstrings maintained twice and go. The field/type/requiredness lines are structured facts of `vm-template`'s `env` field; assert them on the record, which `test_reference.py` already does for the same shape. |
| D-069 | `cli/tests/manifests/test_describe_kind.py::test_secret_backend_describes_its_source_config@d8062c,test_tag_only_secret_backend_source_config_is_a_table@247ff4` | **[unverified]** rendered parenthetical pins of shipped constraints | delete | **Corrected 2026-08-19 (map re-baseline).** The "or delete" is closed toward delete. `min length 1`, `default 30.0` and `gt 0` restate constraints the model declares, and each half that could regress is held elsewhere by construction rather than by a second copy. Constraint EXTRACTION is generic: `_constraints_in` (`cli/agentworks/schema/fields.py::_constraints_in`) loops over the module-level `_CONSTRAINT_KEYS` tuple in `cli/agentworks/schema/fields.py`, which contains `gt`, so a spelling asserted for one key is asserted for all of them, and `cli/tests/schema/test_fields.py::test_constraints_are_normalized_to_plain_keys_and_values` asserts that loop on `min_length`, `pattern`, `ge` and `le`, and `cli/tests/schema/test_fields.py::test_a_collection_reports_one_carriers_constraints_not_a_mixture` adds `max_length`. Constraint RENDERING is equally generic: `cli/agentworks/manifests/describe.py::_facts_of` iterates `entry.doc.constraints.items()` with no per-key branch. The default is the one half that renders on its own branch (`describe.py::_facts_of`), and `worth_showing(default)` is exercised across this file's neighboring rows. What is left in the two anchored tests is the spelling of the parenthetical, whose only failure mode is a rewording. |
| D-070 | `cli/tests/manifests/test_describe_kind.py::test_a_root_value_that_may_be_a_table_says_what_is_in_the_table@c0b068,test_a_scalar_root_value_has_no_table_form@ee6c7f` | rendered-line pins over test fixtures | keep | `StringOrTableRoot` / `StringRoot` are fixtures, and the asserted lines restate their declared fields; the second anchor's `assert "as a table:" not in text` is the load-bearing half (a scalar-only root must not advertise a table form), a structural absence rather than a wording blacklist. |
| D-071 | `cli/tests/manifests/test_describe_kind.py::test_a_capability_kind_is_an_index_of_implementations@92f663,test_a_host_kind_lists_every_arm_it_could_hold@d5158a` | implementation-index pins | convert | Keep "the index lists every registered implementation", derived from `registered_implementations(kind)` rather than spelling `lima` / `proxmox` and their shipped one-liners (`assert "Lima VMs (local, or on a remote host via SSH)" in text` in the first anchor, and `assert "- proxmox: Proxmox VE cluster VMs (clone + cloud-init)" in text` in the second). The description text is authored prose maintained twice. |
| D-072 | `cli/tests/manifests/test_describe_kind.py::test_an_implementation_shows_the_config_it_declares@7b5904` | rendered parenthetical pin | convert | **Recipe verification, 2026-08-16.** Conversion holds, with one correction to its detail: `FieldDoc.default` is the model instance `LimaLocalPlacement(mode='local')`, not the literal dict the row implied, so the record assertion must route through `agentworks.manifests.yaml_value.render_value`, the same function `describe._facts_of` uses. No production change; just do not write the literal dict. |
| D-073 | `cli/tests/manifests/test_describe_kind.py::test_a_nested_tagged_union_renders_every_arm_with_its_own_fields@005eec` | six rendered-line pins plus one absence | convert | The `_field_entry(text, "host").endswith(...)` assert over `ssh`'s host docstring and the two arm summaries (`- local: Run limactl on this machine.` and `- ssh: Run limactl on another host over SSH.`) are second copies. The three `mode`/`host` asserts (each arm shows its OWN `mode` const and `ssh`'s required `host`) and `assert "vm_host" not in text` (a retired field must not resurface) are real; both are expressible on the arm records, which this file's own `test_describe_kind.py::test_every_arm_this_surface_names_is_one_it_also_answers_for` does. |
| D-074 | `cli/tests/manifests/test_describe_kind.py::test_a_nested_union_arm_summary_reaches_the_terminal_as_plain_text@51db3b` | markdown-normalization pin | keep | `"``" not in text` guards that RST double-backticks are normalized before reaching a terminal, which is a transform, not a wording choice; the `- ambient:` half is the arm whose summary legitimately carries code spans. Trim the sentence tail if the lead wants it narrower, but the transform guard earns its place. |
| D-075 | `cli/tests/manifests/test_describe_kind.py::test_every_arm_this_surface_names_is_one_it_also_answers_for@32cd8b` | record walk over every describable target | keep | The file's best test and the pattern the rest should follow: every named arm is answered for by an address, its fields, or a recurrence marker, asserted over the record across every registry target, so a NEW union nobody wrote a case for fails. |
| D-076 | `cli/tests/manifests/test_describe_kind.py::test_a_capability_kind_summary_does_not_enumerate_its_implementations@aee23e` | absence assertion on a summary | convert | The invariant (a capability summary must not carry a second enumeration that can go stale beside the live registry list) is real. `"(" not in summary` is a crude wording proxy for it; assert instead that no registered implementation name appears in the summary line, derived from `registered_implementations`. Keep `assert "codex" in text, "the registry list is what names them"` only as the non-vacuity guard it is, or derive it too. |
| D-077 | `cli/tests/manifests/test_describe_kind.py::test_a_capability_no_config_enables_still_documents_itself@e25753` | rendered-line pins of test-fixture text | keep | `"a fixture platform no config opts into"` and `"Where this fixture platform creates its VMs."` are declared by this file's own fixture models, and `region (string, optional, default westus2)` restates the fixture field. Value identity proving a capability documents itself with no config and no registry, which is this surface's whole promise. |
| D-078 | `cli/tests/manifests/test_describe_kind.py::test_a_collection_of_tagged_blocks_names_its_arms_and_expands_one@c5720c,test_each_element_arm_shows_that_arms_own_fields@340460` | rendered-line pins of test-fixture text | keep | Same: `LocalDisk` / `NetworkDisk` and their docstrings are defined by this file's own fixture models, so every asserted string is test-injected. The guarded defect (a tagged collection rendered as an opaque "list of table", and `volume` documented nowhere) is structural. |
| D-079 | `cli/tests/manifests/test_describe_kind.py::test_a_boolean_warns_that_quoted_strings_are_invalid@1d3cea` | required-phrase list plus wording blacklist | delete | `'Write booleans unquoted; quoted strings such as'`"no"`'are invalid.'` asserted across ten targets is a required-phrase list, and `"used to" not in entry` is a wording blacklist that rejects only the phrasings someone already thought of. The behavior the sentence describes is separately and properly guarded by `test_describe_kind.py::test_the_quoted_boolean_warning_describes_this_loader`, which loads a real manifest both ways. |
| D-080 | `cli/tests/manifests/test_describe_kind.py::test_the_quoted_boolean_warning_describes_this_loader@830e5a,test_the_quoted_boolean_warning_describes_this_loader::ConfigError::96ba2c` | loader behavior for bare versus quoted booleans | keep | The observational twin of D-079: a bare `no` decodes to `False` and a quoted `"no"` is refused. Behavior at the loader boundary, no wording involved. |
| D-083 | `cli/tests/manifests/test_describe_kind.py::test_the_command_completes_its_argument@ee6e99` | completion-registration assertion | keep | Structural: the command's argument has a completer registered, read from `DYNAMIC_COMPLETIONS`. Not prose. |
| D-084 | `cli/tests/manifests/test_error_framing.py::test_every_manifest_error_frames_its_path_through_the_shared_helper@8bccc6` | framed-prefix plus absolute-path absence, over a refusal corpus | keep | A redaction defense in the charter's exact sense: no manifest error may show an operator an absolute path, asserted as a derived home-relative prefix plus `str(home) not in message`. Nothing authored is pinned. |
| D-085 | `cli/tests/manifests/test_error_framing.py::test_the_advisory_channel_frames_the_same_way@5169f2` | same defense on the advisory channel | keep | Same invariant on the non-raising warning channel, which the refusal sweep cannot reach. |
| D-086 | `cli/tests/manifests/test_error_framing.py::test_an_unreadable_file_names_the_file_and_no_line@8f00bd` | framed-prefix pin plus `:0` absence | keep | `"not valid UTF-8"` rides along, but the assertion's substance is the `line == 0` sentinel rendering the file without a line no editor has. Trim the message tail from `assert message.startswith(f"{format_host_path(root / 'a.yaml')}: not valid UTF-8")` if the lead wants it minimal; the `assert ":0" not in message` half is the guard. |
| D-087 | `cli/tests/manifests/test_error_framing.py::test_a_yaml_error_without_a_problem_mark_names_the_file_and_no_line@ee538a` | whole-message equality | convert | Same sentinel on the other read path, but asserted as a full authored message. Convert to the sibling's shape in `test_error_framing.py::test_an_unreadable_file_names_the_file_and_no_line` (framed prefix plus `assert ":0" not in message`), keeping the pyyaml-supplied `"boom"` if the propagation matters. |
| D-088 | `cli/tests/manifests/test_error_framing.py::test_the_duplicate_error_frames_both_locations@27a55a` | whole-message equality | convert | The stated invariant is that BOTH locations are present and neither is absolute, and that a later "fix" cannot satisfy the sweep by dropping the inline one. Assert both framed paths appear; drop the sentence between them. |
| D-089 | `cli/tests/manifests/test_emit.py::test_the_capability_union_replaces_the_open_block_at_the_host_field@8b7121` | shipped description pinned in a test | convert | The invariant (field descriptions reach the emitted schema) is real and structural; derive the expectation from the model's own field description rather than restating it. Everything else in this test (the discriminator name, the mapping set and the absent `CapabilityBlock`) is already structural and stays. |
| D-090 | `cli/tests/manifests/test_emit.py::test_the_modeline_is_relative_to_the_manifest@60cd01` | modeline equality | keep | The `# yaml-language-server: $schema=` directive is an external editor extension's format, the sanctioned exception, and the path halves are composed from `SCHEMA_DIRNAME` / `ENVELOPE_SCHEMA_FILENAME` rather than spelled. |
| D-091 | `cli/tests/manifests/test_emit.py` | n/a | keep | Not inventoried: C10's finding against this file's 22 load cycles is **withdrawn**, and they are the loader half of a two-parser soundness pairing. I read the file only for prose assertions and found none beyond D-089 and D-090. |
| D-093 | `cli/tests/manifests/test_loader_and_envelope.py::test_duplicate_across_files_cites_both_locations@e0bd96` | phrase pin plus filename presence | delete | Drop `assert "duplicate secret" in message`; keep `assert "a.yaml" in message` and `assert "b.yaml" in message`, which assert both files are cited and are value identity on names the test wrote. The duplicate refusal's framing is separately guarded by D-088. |
| D-094 | `cli/tests/manifests/test_loader_and_envelope.py::test_secret_name_over_secret_cap_errors@62a97b` | phrase pin plus derived cap | delete | **Corrected 2026-08-19 (map re-baseline).** Range narrowed from the whole test. Drop `assert "is too long" in message`. Keep `assert f"max {MAX_SECRET_NAME_LENGTH}" in message`, derived from the production constant, so it catches the wrong cap being reported, which is the defect this test names, and `assert "a.yaml:2" in message`, the location. |
| D-095 | `cli/tests/manifests/test_loader_and_envelope.py::test_an_advisory_carries_the_documents_location@a3472c` | advisory sentence pin | convert | **Corrected 2026-08-19 (map re-baseline).** Was delete over the whole test, which is not executable as written: the location prefix the justification keeps and the authored tail it drops are ONE assertion (`assert "a.yaml:2: vm-template/t1.env sets agentworks-managed identity variable" in manifests.issues[0]`), so the row is a conversion, not a deletion. Replace `"a.yaml:2: vm-template/t1.env sets agentworks-managed identity variable" in manifests.issues[0]` with `manifests.issues[0].startswith("a.yaml:2: vm-template/t1.env")`, which keeps the navigability invariant the docstring names and drops the advisory sentence. The count above it (`assert len(manifests.issues) == 1`) is untouched. No production change. |
| D-097 | `cli/tests/manifests/test_capability_shape.py` | n/a | keep | Inventoried and nothing found in my lane. C10's anchor is stale: it is a line range, and at this basis those lines are the `test_capability_shape.py::_load_one` helper and a parametrize opening rather than anything C10 addressed. This file's message assertions are all `match=` sites, in `test_capability_shape.py::test_a_retired_session_selector_is_an_unknown_field_at_its_own_location`, `test_capability_shape.py::test_tagged_table_requires_a_string_name_key` and `test_capability_shape.py::test_a_kind_owned_key_inside_the_table_is_the_platforms_to_refuse`, which belong to group 1, and `assert "session-template/htop" in result.output` in `test_capability_shape.py::test_cli_tagged_shape_loads_cleanly` is value identity on a resource the test declared. Raising it so the lead knows C10's third bullet resolves to group 1, not to this sweep. |
| D-098 | `cli/tests/manifests/test_spec_apt.py::test_an_unknown_key_names_the_fields_that_are_valid@d0919e` | framed whole-message equality | delete | `rejection()` returns the rendered string and `ConfigError` carries no structured problems, so preserving "which field" would need a production handle, which R2.2 forbids. Behavior (`ConfigError` raised) survives via `rejection`'s own `pytest.raises`. R2.4 deletion. |
| D-099 | `cli/tests/manifests/test_spec_apt.py::test_a_missing_required_field_says_it_is_required@73c6e3` | framed multi-line batch equality | convert | The batch structure (four problems, one line each, under one located header) is the D-013 invariant at the decode layer and is worth keeping as counts plus indentation; the four `is required` tails are authored. |
| D-100 | `cli/tests/manifests/test_spec_apt.py::test_a_bare_string_where_a_list_belongs_is_refused@c32e27` | framed whole-message equality | delete | Same family as D-098; the behavioral claim (a bare string is not wrapped into a list) is the raise plus the absence of a decoded row, not the sentence. |
| D-101 | `cli/tests/manifests/test_spec_apt.py::test_a_source_file_with_a_path_separator_is_refused@64b56e` | framed whole-message equality carrying a pattern | convert | Keep the pattern itself (`^[a-zA-Z0-9][a-zA-Z0-9._-]*$` is the production constraint, and a `source_file` reaching a shell command is a real boundary) by asserting the field's declared pattern appears; drop the framing sentence. Same conversion as D-031. |
| D-102 | `cli/tests/manifests/test_spec_apt.py::test_a_description_written_in_spec_is_sent_to_metadata@94f19a` | framed whole-message equality | delete | `"description belong(s) in metadata, not in spec"` is an authored steer with no structural handle. |
| D-104 | `cli/tests/manifests/test_spec_hosts.py::test_a_table_with_no_tag_reads_as_a_missing_field@8816cc` | per-kind derived message comparison | convert | Not a spelled pin: the expected string is built from the loop's `kind`/`name`/`field`, so what is asserted is cross-kind UNIFORMITY of the missing-tag refusal, which is a real invariant a new host kind can break. Convert to comparing each kind's message against one kind's actual message with the identifiers substituted, so the uniformity survives and the authored tail is no longer spelled. |
| D-105 | `cli/tests/manifests/test_spec_hosts.py::test_a_non_table_reads_as_a_table_requirement@8816cc` | per-kind derived message comparison | convert | Same conversion as D-104 for the non-table case. |
| D-106 | `cli/tests/manifests/test_spec_hosts.py::test_a_host_that_requires_a_capability_says_so@8816cc` | per-kind derived message comparison | convert | Same conversion as D-104 for the required-capability case, including the deliberate `session-template` exclusion. |
| D-108 | `cli/tests/manifests/test_spec_hosts.py::test_a_site_named_after_a_platform_must_declare_it@69dcf4` | framed whole-message equality | delete | The shadowing rule is real and the refusal survives as `ConfigError`; the sentence explaining it does not need a test. |
| D-109 | `cli/tests/manifests/test_spec_hosts.py::test_a_site_name_takes_the_freeform_cap@7e7e1e` | cap boundary, `endswith` on the framed tail | convert | The cap arithmetic (accepted at the cap, refused one over, using the freeform constant) is a real threshold and is asserted from `MAX_FREEFORM_NAME_LENGTH`; keep the boundary pair and drop the authored tail from the `endswith`. |
| D-110 | `cli/tests/manifests/test_spec_hosts.py::test_a_non_conforming_secret_inside_the_block_is_warned_about@75dee2` | advisory `startswith` pin | convert | Keep the location prefix and the offending secret name (`'Bad_Name'`, written by the test); drop the authored explanation after it. Same shape as D-095. |
| D-111 | `cli/tests/manifests/test_spec_secret.py::test_a_missing_description_says_it_is_required@afb859,test_an_empty_description_is_refused_too@1f4845` | framed whole-message equality | delete | Missing and empty `description`; both are the authored normalization family (D-003, D-030) at the decode layer. |
| D-112 | `cli/tests/manifests/test_spec_secret.py::test_a_non_table_backend_mappings_says_table@cdfa51` | framed whole-message equality | delete | Same family (a non-table `backend_mappings` says table). |
| D-113 | `cli/tests/manifests/test_spec_secret.py::test_a_non_conforming_declared_name_is_refused@840d99,test_the_secret_cap_is_the_larger_one@6a00a9` | naming refusal and cap boundary | convert | The cap pair keeps its threshold assertion derived from `MAX_SECRET_NAME_LENGTH` (this is the cap that differs from the freeform one, which is the point); the `startswith`/`endswith` authored tails go. |
| D-114 | `cli/tests/manifests/test_spec_admin_and_console.py::test_an_unknown_layout_lists_the_ones_that_exist@8a835a` | framed message pin listing valid layouts | delete | **Corrected 2026-08-19 (map re-baseline).** The "or delete" is closed toward delete, and the row is narrowed: it read the whole test and swept up two lines that keep. Drop the framed sentence prefix (`assert message.startswith("res.yaml:7: named-console-template/default.tmux_layout: must be one of: ")`) alone. Its two halves are covered elsewhere on D-003's reasoning: the authored normalization ("must be one of") is the same family D-003 deletes as a paraphrase of a pydantic error type, and the located address prefix is re-asserted by the path-rendering section D-003 names, so neither half is this row's only probe. Keep the loop below it (`for layout in VALID_TMUX_LAYOUTS:` / `assert layout in message`): it is derived from the production constant, so it proves the refusal enumerates the layouts actually in force, which is the same shape as G1-K16 and is not a rewording. The `choices` half is additionally structural in `test_spec_admin_and_console.py::test_the_layouts_reach_the_explain_surface_as_choices`, as `assert field.choices == VALID_TMUX_LAYOUTS`. |
| D-115 | `cli/tests/manifests/test_spec_admin_and_console.py::test_a_bad_mise_package_says_what_the_syntax_is@81c93d,test_a_bad_mise_install_before_says_what_it_accepts@df71f4` | framed whole-message equality | delete | Two authored "says what the syntax is" messages from field validators. Sentence-only observables. |
| D-116 | `cli/tests/manifests/test_spec_admin_and_console.py::test_a_bad_mise_lockfile_carries_the_source_refs_own_message@632a7d` | authored-phrase substring | delete | `"mise_lockfile is invalid: "` is our own framing; the refusal is the behavior. |
| D-117 | `cli/tests/manifests/test_spec_admin_and_console.py::test_an_unknown_admin_key_names_the_fields_that_are_valid@d8df98` | framed whole-message equality | delete | Unknown-key field list at the decode layer, same as D-098. |
| D-118 | `cli/tests/manifests/test_spec_install_commands.py::test_a_missing_command_says_it_is_required@8816cc` | per-kind derived message comparison | convert | Same shape and conversion as D-104: the expected string is built from the loop's kind, so the invariant is cross-kind uniformity of the required-field refusal. |
| D-119 | `cli/tests/manifests/test_spec_install_commands.py::test_a_bare_test_key_keeps_its_remedy@07e784,test_an_unknown_key_names_the_fields_that_are_valid@ba4441` | framed whole-message equality | delete | A remedy sentence and an unknown-key field list; both authored, both leaving `ConfigError` behind. |
| D-120 | `cli/tests/manifests/test_spec_install_commands.py::test_a_non_string_command_is_no_longer_coerced@4c7010` | framed whole-message equality | delete | "no longer coerced" is proven by the raise; the message is authored normalization. |
| D-121 | `cli/tests/manifests/test_spec_templates.py::test_a_quoted_integer_is_no_longer_coerced@d398e4` | framed whole-message equality, two inputs | delete | Both a quoted integer and a bool must be refused rather than coerced; the two raises carry that and the identical spelled message carries nothing extra. |
| D-122 | `cli/tests/manifests/test_spec_templates.py::test_a_non_string_auth_key_is_refused@0fc20a` | framed whole-message equality | delete | Same family. |
| D-123 | `cli/tests/manifests/test_spec_templates.py::test_a_non_conforming_auth_key_is_warned_about_and_kept@6be4fa` | advisory `startswith` plus kept-value assertion | convert | Keep the `decode("vm-template", "big", {"tailscale_auth_key": "GITHUB_TOKEN"}).tailscale_auth_key` equality (the non-conforming key is warned about and still decoded, which is the behavioral claim) and the location prefix; drop the authored tail. |
| D-124 | `cli/tests/manifests/test_spec_templates.py::test_a_bad_mise_package_says_what_the_syntax_is@a511fe` | framed whole-message equality | delete | Same as D-115. |
| D-125 | `cli/tests/manifests/test_spec_templates.py::test_a_key_that_never_did_anything_is_now_refused@fd8db3` | per-field derived `startswith` comparison | convert | The expected prefix is built from the loop's field name, so this is the uniformity family (D-104): every retired key is refused the same way. Convert to a substituted-identifier comparison. |
| D-126 | `cli/tests/manifests/test_spec_templates.py::test_an_empty_auth_key_is_refused_rather_than_overriding_the_default@04d7a3` | framed whole-message equality | delete | An empty auth key is refused rather than silently overriding the default; the raise is the behavior and the sibling `test_spec_templates.py::test_an_empty_auth_key_never_reaches_the_resolved_template` asserts the resolved-template consequence structurally. |
| D-127a | `cli/tests/manifests/test_spec_workspace_template.py::test_an_env_entry_with_neither_source_field_is_refused_as_one_problem@09b04e,test_an_env_entry_with_both_source_fields_is_refused_as_one_problem@a1f58a,test_a_malformed_selected_env_arm_keeps_only_its_real_error@f85c98` | union-collapse cardinality behind a whole-message equality | convert | **Ruled by the lead 2026-08-16**: cardinality is a structural claim about output shape, it is cheap, and it fails when the collapse actually breaks. These are the three sites that carry it, and their own names say so (`..._refused_as_one_problem` twice, `..._keeps_only_its_real_error`). Convert each to a problem count plus the offending path, following the shape D-026 already converts to; drop the message text. |
| D-127b | `cli/tests/manifests/test_spec_workspace_template.py::test_an_unknown_key_names_the_fields_that_are_valid@4be33a,test_an_env_key_that_is_not_a_variable_name_is_refused@c08206,test_an_env_entry_that_is_neither_shape_is_refused@088eaf,test_an_env_entry_with_an_unknown_inner_key_is_refused@e04e04,test_a_bare_string_where_the_inherits_list_belongs_is_refused@795ac2` | framed whole-message equality | delete | The other five sites of the same family, verified individually: none carries a cardinality claim. Each pins one authored normalization (unknown-field alternatives, the env-var-name regex, `must be a table`, an unknown inner key, `must be a list`), and the refusal itself survives on the raise. |
| D-128 | `cli/tests/manifests/test_spec_workspace_template.py::test_an_agentworks_prefixed_key_is_warned_about@e0578b,test_a_newline_in_a_plaintext_value_is_warned_about@f795a4,test_a_non_conforming_secret_name_is_warned_about@788d3e` | advisory `startswith` pins | convert | Keep the location prefix and the test-written identifiers (`Bad_Name!`, the prefixed key, the newline-bearing value); drop the authored explanations. Same shape as D-110. |
| D-129 | `cli/tests/manifests/test_spec_workspace_template.py::test_a_clean_document_earns_no_advisories@204ff9` | advisory absence | keep | "A clean document earns no advisories" is a structural emptiness assertion. |
| D-130 | `cli/tests/capabilities/test_conformance.py::test_a_capability_name_that_is_not_a_registry_key_is_refused@fbe57c` | authored-phrase pin plus injected-value pin | convert | Keep `assert repr(name) in reason` (the bad name the test supplied) and the `is not None`; drop `assert "'name' class attribute" in reason`. This rule has no sub-checker to call, so the family discrimination goes with it, which R2.4 accepts. |
| D-131 | `cli/tests/capabilities/test_conformance.py::test_a_config_model_that_cannot_be_built_is_refused@05cf4d` | authored-phrase pin | delete | The structural discriminator is already asserted in this test: `assert not model_is_complete(Unbuildable)` and `assert reason is not None`. `"cannot be built"` adds only our wording on top of a branch that is already pinned two lines up. |
| D-132 | `cli/tests/capabilities/test_conformance.py::test_a_marker_nested_below_the_config_model_is_refused_too@b3c98c` | reason substring naming a fixture field | keep | `MarkedBlock.credentials` is defined by the test; the diagnostic naming the offending field is value identity and the actionable half. |
| D-133 | `cli/tests/capabilities/test_conformance.py::test_overlapping_structural_arms_are_refused_at_registration_without_markers@5f270b` | authored-phrase pin plus fixture-identifier pin | convert | Drop `assert "invalid structural union" in reason` (`conformance_error`'s own framing); keep `assert "overlapping arms First and Second" in reason` (test-defined classes). Add `structural_union_error(Overlapping) is not None` to keep the family discriminated. **Verified by execution:** `structural_union_error` answers for this model while `union_scalar_shorthand_error` and `reference_marker_error` both return `None`. |
| D-134 | `cli/tests/capabilities/test_conformance.py::test_a_discriminated_structural_union_is_refused_at_registration_without_markers@e66cf7` | two authored-phrase pins | convert | Neither phrase carries an injected identifier. Replace both with `structural_union_error(Selected) is not None` (verified non-None), which discriminates the family; within-family discrimination (overlapping versus discriminated) is wording-only and goes per R2.4. |
| D-135 | `cli/tests/capabilities/test_conformance.py::test_a_marker_on_a_structural_union_holder_is_refused_at_registration@10f969` | authored-phrase pin | convert | Same: replace `"selects a structural union arm"` with the specific checker call. The structural half of this test (`assert extract_references(config_model, blob) == ()`, the marker shadowing arm traversal) is the real invariant and already stands. |
| D-136 | `cli/tests/capabilities/test_conformance.py::test_an_aliased_structural_arm_is_refused_before_marker_placement@09b31f` | authored-phrase pin plus fixture-identifier pin | convert | Drop `assert "invalid structural union" in reason`; keep `assert "_AliasedSecretArm.secret_value declares validation alias" in reason` (a fixture identifier). Add the sub-checker call for the family. |
| D-137 | `cli/tests/capabilities/test_conformance.py::test_a_marker_bearing_scalar_structural_arm_is_refused@23d4d0` | authored-phrase pin plus fixture-identifier pin | convert | **Recipe verification, 2026-08-16.** The recorded conversion named the wrong checker: executed at HEAD, `union_scalar_shorthand_error(...)` returns `None` for `_ScalarSecretHolder`, so the replacement would have passed without ever biting. The checker that actually answers is `reference_marker_error`. Corrected replacement: `assert reference_marker_error(_ScalarSecretHolder) is not None`, which passes on HEAD and fails under a dropped-marker break. All three checkers are public exports of `agentworks.schema`, so no production change either way. |
| D-138 | `cli/tests/capabilities/test_conformance.py::test_a_marker_no_walker_could_ever_read_is_refused@93bf3e` | authored-phrase pin | delete | **Disposition changed after reading HEAD:** the test already calls the specific sub-checker (`reason = reference_marker_error(NestedCollection)`) and asserts non-None, so the family is discriminated without any wording, and `assert conformance_error(VM_PLATFORM, _impl(NestedCollection)) is not None` with `assert extract_references(NestedCollection, {"tokens": {"k": ["named"]}}) == ()` carry the rest. Verified by execution (experiment D) that `reference_marker_error` is the only one of the three that answers for this model. Nothing replaces `"cannot classify"` because nothing is lost. |
| D-139 | `cli/tests/capabilities/test_conformance.py::test_a_default_template_on_an_element_marker_is_refused@ee5d31` | authored-phrase pin | delete | Same as D-138: `reason = reference_marker_error(TemplatedElements)` is called and asserted non-None, so `"default template"` is surplus. The premise assertions above it (the fill writing nothing for an omitted collection, and `assert extract_references(TemplatedElements, blob) == ()`) are the behavioral core and stay. |
| D-140 | `cli/tests/capabilities/test_conformance.py::test_every_structural_union_on_the_shipped_surface_is_a_true_one_of@a69cba,test_every_union_scalar_shorthand_on_the_shipped_surface_is_complete@a69cba` | shipped-surface conformance sweeps | keep | `assert not faults` over every declarable kind's metadata and spec models, with the reasons only used to BUILD the failure report. Structural. **Open for the group-4 executor, 2026-09-06:** the non-vacuity guard this row credited is not at this basis. Neither `test_conformance.py::test_every_structural_union_on_the_shipped_surface_is_a_true_one_of` nor its twin `test_conformance.py::test_every_union_scalar_shorthand_on_the_shipped_surface_is_complete` asserts that `declarable_kinds()` is non-empty, so an empty registry passes both; the row's disposition is unaffected, but the guard has to be added or the claim dropped. |
| D-141 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_a_structural_lookalike_is_rejected_before_its_members_are_considered@40ba5a` | authored-phrase pin | convert | Rubric re-check, 2026-08-16. `assert "does not derive from SecretBackend" in str(conformance_error(DESCRIPTOR, Lookalike))` is the only assertion in its test and, because it reads `str(conformance_error(...))`, doubles as the non-None check, so unlike D-131 and D-138 there is no adjacent `is not None` to fall back on. Replace with an `is not None` assertion on `conformance_error`. No production change. |
| D-142 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_an_abstract_backend_is_rejected_as_not_constructible@9b6514` | authored-phrase `startswith` plus identifier | convert | Drop `reason.startswith("it is abstract")`; keep `"create_client" in reason`, which names the abstract member the test left unimplemented (value identity). |
| D-143 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_a_non_callable_factory_hits_the_generic_operation_check_first@fe60a1` | authored framing plus injected operation name | convert | Keep `"create_client"` (the operation the test nulled out); drop `"required secret-backend operations: "`. |
| D-144 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_a_missing_required_class_attribute_is_rejected@8a47cf` | whole-reason equality | convert | Keep `"interactive"` (the attribute the fixture omits); drop the sentence around it. |
| D-146 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_every_secret_reference_shape_in_source_config_is_rejected@596e42` | authored-phrase pin plus derived path | convert | Keep `assert path in reason`, where `path` is the parametrized structural address such as `root.tokens{value}`: that address is derived by the walker and is exactly what regresses. Drop `assert "references forbidden kind 'secret'" in reason`, which is authored framing. |
| D-147 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_python_only_mapping_annotation_families_are_rejected@2fb374` | authored-phrase pin plus path fragment | convert | **Recipe verification, 2026-08-16.** Verified but thin: `"at root"` is the walk's zero value, a much weaker address than D-146's `root.tokens{value}` or D-148's `root.rows[]{value}`. Tighten it to the model's own address when converting, rather than accepting the zero value as the assertion. |
| D-148 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_a_non_string_mapping_key_is_rejected_with_its_nested_path@5a5b51,test_a_python_only_type_nested_in_a_container_is_rejected@762542` | whole-sentence pins carrying an address | convert | The addresses `root.lookup{key}` and `root.rows[]{value}` are the derived observables worth keeping (nested-path computation is the code under test); the surrounding wording is not. Keep the address substrings only. |
| D-149 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_source_contract_operations_must_be_classmethods@8e716c` | whole-reason equality, parametrized | convert | Keep the injected `operation` name; drop `"must be declared as @classmethod"`. The parametrization already discriminates which operation was mis-declared. |
| D-150 | `cli/tests/capabilities/test_capability_config.py::test_a_bad_field_is_owner_framed_and_located@f8373b` | whole-message equality | convert | Same family as D-008: the invariant the docstring names is that the bridge owns the framing and the finalize pass adds none of its own, which is a prefix-and-shape fact. Assert the framing, drop the normalized tail. |
| D-151 | `cli/tests/capabilities/test_capability_config.py::test_an_unregistered_name_is_rejected_by_the_union_naming_what_is_registered@b79aa2` | `startswith` plus registered-name presence | convert | Keep `"'fixture-platform'" in message` (the seated fixture's own name, value identity, and the proof the assembled union dispatches on the tag); drop the authored `"unknown name 'nope'; registered: "` framing, keeping only the `'nope'` echo if the lead wants the input reflected. |
| D-152 | `cli/tests/capabilities/test_declare_once_end_to_end.py::test_the_rendered_sample_offers_the_capability_as_an_alternative@6fea8e,test_no_sample_mentions_the_capability_when_it_is_not_seated@c2add0` | presence and absence of a fixture name in generated text | keep | `"declare-once"` is the seated fixture platform's own name; the paired presence/absence assertions prove the sample renders from the live registry rather than from a file. Value identity, and the absence half is not a wording blacklist but a seating-state fact. |
| D-153 | `cli/tests/capabilities/test_declare_once_end_to_end.py::test_the_field_reference_reads_the_same_one_declaration@531e98,test_the_field_reference_makes_the_same_templated_subtraction@7de276` | rendered-line pins built from fixture constants | keep | `REGION_DEFAULT` and `REGION_DESCRIPTION` are module constants the fixture declares, and the test asserts the record (`region.doc.default`, `region.doc.description`) beside the rendering. This is declared-once/rendered-everywhere parity, which is exactly the derivation-parity keep shape. The `defaults to` fragment of the second anchor's `token` assert is the one authored tail here and could be trimmed to the fixture's template. |
| D-154 | `cli/tests/capabilities/test_capability_descriptors.py` | n/a | keep | Inventoried and nothing found: the assertions are registry/descriptor structure, non-vacuity guards, and cache identity. No authored-prose pins. |
| D-155 | `cli/tests/schema/test_fields.py::test_a_models_own_description_is_the_first_docstring_paragraph@89aee9,test_each_arm_carries_its_own_identity@2ad29a,test_each_element_arm_carries_its_own_identity@191c43` | description equality against fixture docstrings | keep | Every asserted string is a docstring on a model this file or `_fixture_models.py` defines, so these prove docstring-to-`FieldDoc` propagation (including the two-source-line wrap case). Value identity on test-injected text. |
| D-156 | `cli/tests/schema/test_base.py::test_attribute_docstrings_become_descriptions@a62bca,test_an_explicit_description_wins_over_the_docstring@412e43` | description equality against fixture docstrings | keep | Same: the `AgwModel` base's docstring-to-description behavior, asserted with strings the test declares. |
| D-157 | `cli/tests/manifests/test_accepted_type_parity.py` | n/a | keep | Inventoried and nothing found. These assert reference-edge identity, JSON-schema shape, fill behavior, marker payloads, per-kind parity and modeline paths composed from production constants. No authored-prose or authored-form pins in my lane. |
| E-003 | `cli/tests/resources/test_graph.py::test_chain_edges_dependents_and_reachability@0a2286` | edge tuple equality incl. usage | keep | `usage="c needs d"` is defined by this test's own fixture; the assertion proves the edge's source and usage survive the graph round trip. |
| E-004 | `cli/tests/resources/test_graph.py::test_full_incoming_edges_preserve_authoritative_parallel_facts@73548d` | usage-list equality | keep | All three usage strings are injected by this test itself (`usage="runtime dependency"`, `usage="inherited runtime dependency"` and `usage="a parent declaration"`); the invariant is that parallel incoming edges are retained in order, not that any sentence reads a certain way. |
| E-010 | `cli/tests/resources/test_readiness_fold.py::test_not_ready_platform_dependency_propagates_its_reason@ea343a` | reason equality against an injected reason | keep | Invariant (R9.1): a site passes the platform's verdict through without re-wrapping. The reason string is the one this test handed to `Readiness.blocked`, so equality is pass-through identity. |
| E-011 | `cli/tests/resources/test_readiness_fold.py::test_disabled_platform_node_folds_end_to_end_to_enable_its_unit@b066a2` | authored reason equality | convert | Rubric re-check, 2026-08-16. `assert verdict.reason == "depends on vm-platform 'lima', which is disabled; enable its unit"` is the ONLY assertion in `test_readiness_fold.py::test_disabled_platform_node_folds_end_to_end_to_enable_its_unit`; the structural siblings the row cited are not in that test. Replace with a `readiness_of(...).is_ready` assertion off the graph. No production change. |
| E-012 | `cli/tests/resources/test_readiness_fold.py::test_typo_in_the_placement_host_is_refused_rather_than_changing_readiness@d63052` | required-phrase pair plus a wording blacklist | delete | `"placement.hst: unknown field"` / `"placement.host: is required"` are authored validation prose and `"limactl" not in message` is a blacklist; the surviving `pytest.raises(ConfigError)` carries the real claim that a typo is refused rather than silently changing readiness. |
| E-013 | `cli/tests/resources/test_readiness_fold.py::test_not_ready_is_total_over_malformed_config@b98530` | reason-set membership | convert | The invariant is totality (`not_ready` returns rather than raising over malformed config). Replace the `in (None, "limactl not installed")` membership with `isinstance(LimaPlatform.not_ready(config), Readiness)`. No production change. |
| E-014 | `cli/tests/resources/test_reference_relationship.py::test_inherits_reference_marks_the_relationship_and_takes_the_kind_from_the_source@a98d5a` | **[unverified]** authored usage-string equality | delete | The test's stated claim (relationship marked, kind taken from the source) is fully carried by the three asserts above it (`assert ref.relationship is RefRelationship.INHERITS`, `assert (ref.kind, ref.name) == ("session-template", "base")` and `assert ref.source == ("session-template", "kid")`); `usage == "a parent template"` restates a literal in cli/agentworks/resources/reference.py::inherits_reference. |
| E-018 | `cli/tests/resources/test_template_kinds.py::test_template_dependencies_emits_template_requirement@8c751b` | **[unverified]** authored usage-string equality | delete | The parametrized claim is kind and source per inherited name (`assert by_name["base"].kind == spec.kind` and `assert by_name["base"].source == (spec.kind, "child")`); the usage literal (`assert by_name["base"].usage == "a parent template"`) is production prose restated. |
| E-024 | `cli/tests/plugins/test_azure.py::test_operator_override_of_az_cli_wins@69d5e2` | value identity of an injected override | keep | Invariant: an operator-declared install-command overrides the disabled plugin row. `"echo operator-az"` is the value this test wrote into config, so the assertion proves precedence, not wording. |
| E-028 | `cli/tests/plugins/test_claude.py::test_operator_override_of_claude_install_wins@c42711` | value identity of an injected override | keep | Same invariant as E-024 for the claude install command. |
| E-032 | `cli/tests/plugins/test_codex.py::test_operator_override_of_codex_install_wins@a7548a` | value identity of an injected override | keep | Same invariant as E-024. |
| E-034 | `cli/tests/plugins/test_enablement_producer.py::test_vm_site_on_disabled_plugin_platform_is_not_ready_with_enable_plugin@08eedb,test_git_credential_on_disabled_plugin_provider_is_not_ready@2113fd,test_git_credential_not_ready_falls_back_when_mark_absent@9f6c38` | authored reason equality | convert | Rubric re-check, 2026-08-16. All three sites are the only assertion in their test, so the delete empties three tests, and `test_enablement_producer.py::test_git_credential_on_disabled_plugin_provider_is_not_ready` is the only probe anywhere that a git-credential on a disabled provider is not ready. Replace with `assert not verdict.is_ready`. No production change. |
| E-036 | `cli/tests/plugins/test_enablement_producer.py::test_second_stub_source_composes_through_finalize_and_precedence_holds@926474` | enable-hint substring used as a precedence discriminator | delete | Redundant: `verdict.reason is not None` plus `"stub-reason" not in verdict.reason` already prove the plugin source won over the stub. |
| E-037 | `cli/tests/plugins/test_enablement_producer.py::test_second_stub_source_composes_through_finalize_and_precedence_holds@926474` | negative value identity of an injected sentinel | keep | Invariant: source precedence, first source wins. `stub-reason` is the sentinel this test's stub emitted, so its absence is what proves the losing source did not supply the reason. |
| E-042 | `cli/tests/plugins/test_publish.py::test_not_enabled_plugin_row_and_manifest_present_but_disabled@0ef88d` | authored reason equality | delete | Redundant with the two structural assertions above it (row present-but-disabled, `enablement_of(...) is Enablement.disabled`). |
| E-043 | `cli/tests/plugins/test_publish.py::test_bad_plugin_manifest_anchor_raises_typed_plugin_attributed_error@dcbdd5,test_plugin_manifest_anchor_without_subdir_raises_typed_plugin_attributed_error@1c0151` | injected plugin name in an attributed error | keep | Invariant: a manifest-anchor failure is attributed to the plugin that caused it rather than escaping as a bare `ImportError`. `PLUGIN` is this file's own constant, so the assertion is attribution identity. |
| E-046 | `cli/tests/plugins/test_surfaces.py::test_list_hides_disabled_but_shows_not_ready_enabled@e3afc9` | injected not-ready reason substring | keep | The fixture capability supplies `"unsupported on this host (fixture)"`; the assertion proves the plugin-published row's readiness reason reaches the resource listing. |
| E-051 | `cli/tests/plugins/test_surfaces.py::test_roster_lists_enabled_ok_and_disabled_info@5125fd` | value identity of the fixture's own description | keep | The fixture plugin declares `"the opted-in fixture plugin"`; the assertion proves the declared description reaches the roster row. |
| E-053 | `cli/tests/plugins/test_surfaces.py::test_roster_required_scopes_informational_when_set@7a7c66` | value identity of injected scopes | keep | The fixture plugin declares `required_scopes = ("system", "vm")`; the assertion proves declared scopes reach the roster. |
| E-055 | `cli/tests/plugins/test_proxmox_bootstrap_staging.py::test_success_stages_mode_0600_and_removes_verified_path@c9aa7e` | exact guest command tuple | keep | Invariant: the staging file is removed AND its absence verified (`rm -f -- "$1" && test ! -e "$1"`) with the path passed as an argument rather than interpolated. This is the behavior, not a description of it. |
| E-066 | `cli/tests/plugins/gcp/test_auth.py::test_real_env_source_and_operation_resolver_deliver_exact_downloaded_json@956d4e` | exact credential-factory input dict | keep | Invariant: the downloaded service-account JSON reaches Google's factory unmodified and without whitespace stripping, including the trailing newline and the private-key block. Every value is test-injected, and this is the security property the remediation hint below only describes. |
| E-073 | `cli/tests/plugins/gcp/test_instance.py::test_final_instance_body_is_key_free_and_pins_security_fields@c7172a` | provider request-body field equality | keep | Invariant: the insert body carries the security-relevant GCE fields (no service account, `block-project-ssh-keys`, oslogin off, IPv4-only, the exact injected SSH key) and no key material. `"External NAT"` and `"ONE_TO_ONE_NAT"` are GCE's own vocabulary, not ours. |
| E-079 | `cli/tests/plugins/gcp/test_platform.py::test_combined_progress_precedes_readiness_and_fixed_stdin@455028` | ordering pin over a recorded event log | convert | The ordering claim is real and observational, but two of its four legs are authored progress/output sentences. Keep the legs keyed on the imported production constants (`GCE_READINESS_COMMAND` before `TAILSCALE_JOIN_STDIN_COMMAND`) and drop the `progress:` and `output:` legs. No production change. |
| E-080 | `cli/tests/plugins/gcp/test_platform.py::test_combined_progress_does_not_claim_completion_when_readiness_fails@651efe` | negative event presence | keep | Invariant: when the startup-marker readiness check fails, the Tailscale auth key is never delivered over stdin. Keyed on a production command constant, not on prose. |
| E-098 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_success_uses_private_random_stages_and_removes_both@34c603` | exact guest cleanup command | keep | Invariant: the staging file is removed and its absence verified on the guest. Same family as E-055. |
| E-114 | `cli/tests/vms/test_create_vm_dispatch.py::test_create_vm_request_shape_and_row@2cf2c5` | value identity of an injected key | keep | `request.ssh_public_key == "public ssh key"` proves the operator's key reaches the provision request; the value is the test's. |
| E-122 | `cli/tests/vms/test_lima_create_flow.py::test_no_restart_when_sentinel_absent@c15d54,test_probe_failure_warns_and_does_not_restart@0c6a03` | command absence in a recorded call log | keep | Invariant: no restart is issued when the sentinel is absent or the probe fails. `limactl restart` is the tool's command, recorded from the fake, not our prose. |
| E-124 | `cli/tests/vms/test_lima_create_flow.py::test_submitted_lima_configuration_never_contains_tailscale_key@64c6cb` | exact sensitive-command list | keep | Invariant: the auth key is delivered only through the fixed stdin command and never appears in a rendered command line. Values are injected. |
| E-126 | `cli/tests/vms/test_lima_create_interrupt.py::test_local_create_streams_template_without_persistent_file@ce874e,test_local_lima_sensitive_stdin_failure_omits_reflected_key@1b29c4,test_remote_lima_failure_omits_sensitive_input_and_raw_results@dafac0,test_failure_mid_create_cleans_up_and_reraises@da7939,test_interrupt_during_start_cleans_up_and_reraises_the_original@1c93e0,test_interrupt_during_post_start_steps_cleans_up_too@ef1d59,test_interrupt_during_ephemeral_tailscale_join_cleans_up_and_does_not_render_key@665afb,test_ephemeral_tailscale_join_failure_cleans_up_without_key_in_error@2d4cc8` | recorded `limactl` command equality | keep | These pin the commands actually issued to the tool (create over stdin, forced delete, no persistent template file), which is the rollback behavior itself. `limactl` syntax is upstream, not ours. The two `str(caught.value) ==` equalities (`"limactl stdin command failed (exit 1): limactl shell myvm cat"` and its `SSH stdin command failed` twin) pin an authored error sentence and should be trimmed to the typed raise. **Line anchor dropped 2026-09-06:** it addressed `L787` of a file 351 lines long, so it reached nothing, and its own cause said it was between functions at a tree this map no longer matches. The eight named tests are the row's whole estate. |
| E-129 | `cli/tests/vms/test_lima_remote_create_interrupt.py::test_remote_interrupt_kills_the_detached_limactl_before_deleting@933159,test_remote_stdin_write_failure_removes_partial_template_and_preserves_error@049b0a,test_remote_template_staging_is_mode_0600_stdin_and_verified_cleanup@166c21,test_remote_template_uses_private_random_directory_not_predictable_path@7fca20,test_remote_template_rejects_untrusted_allocation_path@0414df,test_remote_interrupt_preserves_original_when_artifact_cleanup_fails@e22dd6,test_delete_op_issues_the_shared_teardown_command@90fb29` | recorded remote command equality | keep | Invariant: the remote template is staged into a private random directory with `umask 077`, written over stdin, and removed with verification; the detached `limactl` is killed before delete. These are the commands executed on the remote host, and the predictable-path test proves a legacy path is not reused. |
| E-130 | `cli/tests/vms/test_lima_template.py::test_subuid_cap_step_present_and_first@d5f687` | script-content substring | keep | Invariant: the subuid capability step is present and first in the generated template; `awk -F:` is the step's own content, and its absence would mean the step vanished. Weak but structural; a count/position assertion would serve as well. |
| E-131 | `cli/tests/vms/test_platform_config_contract.py::test_proxmox_no_longer_reads_a_string_verify_ssl_as_true@820987` | authored remediation substring | delete | `"write it unquoted" in str(excinfo.value)` is our own hint text beside a `pytest.raises(ConfigError, match=...)` that group 1 owns. |
| E-132 | `cli/tests/vms/test_platform_config_contract.py::test_an_unknown_mode_names_the_modes_there_are@e0f40f` | message-prefix pin with a computed address | convert | The invariant worth keeping is the ADDRESS: the error names `vm-site/t.{field}` with no arm-tag segment spliced in. Narrow the `startswith` to the computed address prefix (`f"vm-site/t.{field}:"`) and drop the authored tail. No production change. |
| E-133 | `cli/tests/vms/test_platform_config_contract.py::test_an_unknown_mode_names_the_modes_there_are@e0f40f` | required-phrase list over the union's tags | delete | **Corrected 2026-08-19 (map re-baseline).** Premise corrected, disposition unchanged. The old reason ("a usability property expressed only in prose") was wrong: the loop varies per parametrize case. The reason it still deletes is that the module-level `_MODES` map in `cli/tests/vms/test_platform_config_contract.py` is a second copy of the union's arms maintained BY HAND in the test, which is the shape `no-prose-policing-tests` names explicitly ("do not maintain a second copy inside a test"). Deriving the arms instead would need an accessor this pass may not add (R2.2), so the enumeration goes and E-132's computed address is what survives. |
| E-134 | `cli/tests/vms/test_platform_config_contract.py::test_a_field_from_the_other_arm_is_refused@a6cf18,test_an_extra_field_inside_an_arm_is_refused@5ff0b0` | message-prefix pin with a computed address | convert | Same as E-132: keep `f"vm-site/t.{field}.{stray}:"` and `f"vm-site/t.{field}.hst:"` as the address claim (this is the closed-arm invariant that actually bit lima), drop the `"; expected one of: ..."` tail. No production change. |
| E-135 | `cli/tests/vms/test_platform_config_contract.py::test_gcp_gce_returns_one_complete_service_account_secret_reference@397fb5,test_aws_ec2_returns_the_secret_access_key_reference@0408e1` | **[unverified]** authored usage-string equality | delete | The extraction claims are `(ref.kind, ref.name)` on the adjacent lines; the usage sentence is production prose restated. |
| E-136 | `cli/tests/vms/test_platform_config_contract.py::test_azure_extraction_is_total_on_malformed_config@bbbe71,test_proxmox_extraction_matches_valid_config_extraction@9b908f` | whole-`ConfigReference` equality including usage | convert | Both are edge-identity golden values. Compare `(kind, name)` (and emptiness) rather than the full dataclass, so the usage prose is not pinned. No production change. |
| E-137 | `cli/tests/vms/test_platform_idempotency_guards.py::test_lima_start_proceeds_when_stopped@058318` | recorded command substring | keep | Invariant: a stopped lima VM is started (the idempotency guard does not swallow the start). `limactl start` is the tool's command from the recorded call log. |
| E-140 | `cli/tests/vms/test_platform_support.py::test_not_ready_reasons_chain@398bdc,test_declared_site_on_unsupported_platform_registers_not_ready@89aa7d,test_resource_layer_surfaces_not_ready_state@4473c1` | reason equality against an injected reason | keep | `"limactl not installed"` is the string `_support()` injected into `LimaPlatform.not_ready`; equality proves it propagates to the site verdict and to the rendered row unmodified. |
| E-141 | `cli/tests/vms/test_platform_support.py::test_not_ready_reasons_chain@398bdc,test_r9_5_host_unsupported_platform_is_present_and_not_ready_on_the_graph@eab1ef,test_r9_5_host_unsupported_platform_renders_not_ready_row@0b85f5,test_declared_site_on_unsupported_platform_registers_not_ready@89aa7d,test_resource_layer_surfaces_not_ready_state@4473c1` | reason equality mixing an injected token with an authored wrapper | convert | The injected half is `"Windows only"`; the wrapper `"platform 'wsl2' is unsupported here: ..."` is production prose. Replace with (a) the injected token present in the reason and (b) `site_verdict.reason == platform_verdict.reason`, which is the propagation invariant these six copies are really asserting. No production change. |
| E-142 | `cli/tests/vms/test_platform_support.py::test_declared_site_on_unsupported_platform_registers_not_ready@89aa7d,test_select_site_errors_with_reasons_when_none_ready@be229b` | injected reason substrings inside a raised error | keep | Invariant: refusing to use a not-ready site, and refusing to auto-select when none is ready, both surface the underlying platform reasons rather than a generic message. Both tokens are injected. |
| E-147 | `cli/tests/vms/test_shell_exec_orchestrated.py::test_exec_copied_workspace_pin_no_longer_crashes@b52d89,test_exec_reachable_vm_is_one_boundary_burst@072cb0,test_exec_double_dash_separator_strips_only_the_first@43ad60,test_exec_double_dash_separator_preserves_an_adjacent_second@7dac07,test_exec_bare_flag_after_command_word_still_works@3a1f7b` | executed command equality | keep | Invariant: argv assembly, `--` separator handling (only the first is consumed), and the copied-workspace pin produce exactly these remote command lines. This is the behavior. |
| E-149 | `cli/tests/vms/test_sites_dispatch.py::test_resolve_site_unknown_raises_the_stranded_error@b980b7,test_site_manifest_hint_carries_the_host_in_the_ssh_arm@9fc719` | manifest-fragment substrings in a hint | convert | The real invariant is that the stranded-site hint is a VALID, current-schema vm-site manifest. Replace the fragment pins by parsing the hint as YAML and validating it through the manifest reader, asserting it yields a `vm-site` named `gpu-box` with the ssh placement (and the local arm for the no-host case). This is stronger than the pins and needs no production change. |
| E-153 | `cli/tests/vms/test_tailscale_logout_wording.py::test_deregistered_prints_inside_the_route_window@932c33,test_nullcontext_route_platform_output_is_unchanged@ebd5c5` | recorded command substring | keep | `"tailscale down && tailscale logout"` is the command actually dispatched to the guest, recorded from the fake transport. |
| E-155 | `cli/tests/vms/test_tailscale_stdin_join.py::test_initializer_join_uses_fixed_stdin_once_then_updates_the_ip@699763,test_readiness_exhaustion_raises_typed_without_delivering_key@3e2d7f,test_cloud_init_wait_failure_raises_typed_without_delivering_key@cf1bc6,test_default_readiness_contract_preserves_azure_and_aws_behavior@506662,test_gce_readiness_runs_after_ssh_and_before_fixed_stdin_join@e329bd,test_gce_readiness_timeout_is_typed_labeled_and_stops_before_stdin@096a26` | recorded call-log equality against production constants | keep | Invariant: the readiness command runs after SSH and before the fixed-stdin join, the auth key is never delivered when readiness fails, and the default/GCE readiness commands are the imported constants. Ordering asserted through a recorded call log, not through source or prose. |
| E-158 | `cli/tests/vms/test_vm_nodes.py::test_template_node_declares_the_key_and_the_sweep_predicts_it@e3a4d9` | reference-tuple equality including usage | delete | `(ref.kind, ref.name)` carries the declaration; the usage sentence is production prose (the comment even says "usage prose included"). |
| E-159 | `cli/tests/vms/test_vm_site_kind.py::test_decode_nests_platform_config@bda071` | value identity of an injected description | keep | `"Dev subscription"` is the description this test's manifest declared; the assertion proves decode carries it through the nested platform block. |
| E-165 | `cli/tests/test_azure_create_interrupt.py::TestFailureDuringInlineWait.test_ssh_readiness_exhaustion_fails_closed_without_key_delivery@77d563,TestFailureDuringInlineWait.test_cloud_init_wait_failure_fails_closed_without_key_delivery@bccef6` | recorded call-log equality | keep | Invariant: readiness exhaustion and a cloud-init wait failure fail closed with no `input_text` ever delivered. Commands are the production constants. |
| E-176 | `cli/tests/test_wsl2_create_interrupt.py::_assert_teardown_ran@1f5c7f,test_delete_op_issues_the_shared_teardown_sequence@df9d2c` | recorded `wsl` command equality | keep | Invariant: teardown issues exactly `wsl --unregister <distro>` and `delete` reuses the same shared sequence. Upstream tool syntax from a recorded call log. |
| E-177 | `cli/tests/test_wsl2_create_interrupt.py::test_success_runs_primary_bootstrap_before_create_returns@7bf117` | bootstrap kwargs equality | keep | Invariant: the primary bootstrap runs before create returns, with exactly the injected admin user, key, and options. All values are the test's. |
| D-158 | `cli/tests/test_config.py::test_git_credential_nonconforming_name_warns_and_loads@4eb70d` | injected-name propagation | keep | `any("git-credential/GITHUB" in issue and "git-token-GITHUB" in issue for issue in issues)`. Both names are the test's own: the credential it declared and the secret name derived from it. The invariant is that the warning names the credential AND the derived secret, so an operator knows which of the two to rename. Value identity. |
| D-159 | `cli/tests/test_config.py::test_git_credential_name_conforming_but_derived_over_cap_warns@dad0f4` | injected name plus authored clause | convert | `f"git-credential/{name}" in issue and "does not follow the secret naming rules" in issue`. The first half is D-158's value identity; the second pins the authored sentence. Replacement: `len(issues) == 1` with `f"git-credential/{name}" in issues[0]`, which under this config is exactly the warning under test. This is C-097's positive twin, and doing both together is what stops the pair drifting apart again. No production change. |
| D-160 | `cli/tests/test_config.py::test_terminal_unknown_key_is_a_soft_issue@aa0f24` | injected-key propagation | keep | `any("bogus" in issue for issue in issues)` where `bogus` is the key the test put in the `[terminal]` table. The invariant is that an unknown settings key warns rather than hard-failing AND names the offending key, which is what an operator needs to act on. Value identity, and the test's own comment says so. |
| D-161 | `cli/tests/test_config.py::test_workload_gated_issues_fatal_false_softens_missing_keys_to_issues@b84dfb` | setting name plus authored clause | convert | Three assertions of the form `"ssh_public_key does not exist" in issue`. The setting names are the invariant, that each missing key file produces its own issue naming the setting it came from; "does not exist" is our wording. The third assertion already splits the two (`"extra_ssh_public_keys" in issue and "does not exist" in issue`). Replacement: match `operator.ssh_public_key` and `operator.extra_ssh_public_keys`, which is how production spells them in `config/loaders_core.py::_load_operator` (`f"operator.ssh_public_key does not exist: {format_host_path(pub)}"` and `f"operator.extra_ssh_public_keys: file does not exist: {format_host_path(p)}"`). Matching the bare setting name does not work: `ssh_public_key` is a substring of `extra_ssh_public_keys`, so the narrowed assertion would pass on the wrong issue and the recipe could never bite. The two path-value assertions below stay. No production change. |
| D-162 | `cli/tests/test_config.py::test_code_workspaces_fails_before_default_vscode_write@81b70e` | ordering guard | keep | `monkeypatch.setattr("agentworks.cli.commands.config.get_db", lambda: pytest.fail("database access must happen after config validation"))` makes any database open before config validation a failure. The retired `[paths]` key must be refused before the command reaches state, and this is the only assertion that says so; the message pin in the same test is C-101 and converts. |
| D-163 | `cli/tests/vms/test_applied_state.py::test_hardware_marker_codec_is_strict_and_empty@9df358,test_ssh_payload_codec_round_trips_closed_arms@752dac,test_initialization_slice_builder_uses_post_write_private_key_reference@268f56,test_hardware_marker_codec_refuses_an_unsupported_version_as_version_skew@5bfba3,test_ssh_payload_codec_refuses_an_unsupported_version_before_decoding_value@069f79` | persisted payload wire-shape equality | keep | `VersionedPayload(1, {"fingerprint": ..., "private_key_ref": ..., "status": "verified"})` is the shape written into the applied-state store and read back by a later execution, which principle 3 lists as a trust boundary. Same reasoning as A-079: a codec change that ships is a change to operator databases already on disk, and the version-skew refusals now anchored beside the codecs (`UnsupportedAppliedStateVersionError` on a payload the running code is too old to read) are what make a forward payload a clean refusal rather than a misread. **Anchored 2026-09-06**: this row cited those refusals as evidence while naming only the three codec tests, so the two that carry the refusal are now in the anchor rather than referred to from outside it. |
| D-164 | `cli/tests/test_instance_specs.py::test_spec_refuses_framework_fields@507811` | hand-listed copy of a production constant | convert | The twelve parametrize values are exactly `OVERLAY_EXCLUDED_FIELDS` (verified by executing: `sorted(OVERLAY_EXCLUDED_FIELDS)` at HEAD is `apiVersion, declared_at, description, expires, framework, inherits, kind, metadata, name, origin, source, spec`). Replacement: parametrize over `sorted(OVERLAY_EXCLUDED_FIELDS)`, which the file already imports. A field added to the exclusion set then gains a refusal case with no test edit, and `test_overlay_exclusions_follow_declared_resource_metadata` already guards how that set is composed. No production change. |
| D-165 | `cli/tests/vms/test_vm_platform_debian_release.py::test_every_code_owned_platform_map_has_the_trixie_selector@ca7d85` | provider image identifiers restated per platform | keep | The six assertions restate the image selectors each code-owned map holds (`"debian-13-generic-amd64.qcow2"`, `"trixie"`, `"13"`, the Azure publisher/offer/sku tuple, the GCP family dict). They are a second copy of production data whose real correctness only a live boot proves, so every legitimate image update becomes a two-file edit. The invariant worth keeping is coverage: a map with no entry for the release Agentworks creates makes `code_owned_release_value` raise at provisioning time. **Re-decided 2026-09-06: keep.** The identifiers are third-party vocabulary, Debian image names and codename, the AWS SSM release token, the Azure publisher/offer/sku tuple and the GCP families, which is exactly the `no-prose-policing-tests` exception: they arrive from outside this repository and change without our review, so pinning what we depend on is a real regression test. **Two of the six DO have a first copy**, found by the lane: `_DEBIAN_OCI_TAGS[TRIXIE]` is `DebianRelease.TRIXIE.value` spelled again, and `_DEBIAN_SSM_RELEASES[TRIXIE]` is `profile_for_release(TRIXIE).version_id`. Those two are derivable and an executor may derive them. The keep rests on the other four, the Lima image filenames per architecture and the Azure and GCP tuples, which have no in-repo source at all: the test is where that dependency is written down. The proposed `CURRENT_DEBIAN_RELEASE in <map>` replacement is also weaker than the row credited it: it drops the per-architecture coverage this test gives, where amd64 and arm64 are asserted separately and a map that gained only one would pass. |
| D-166 | `cli/tests/vms/test_vm_platform_debian_release.py::test_all_platforms_declare_contract_version_one@b4a9dc` | hand-listed platform enumeration | convert | The set of six platform classes is maintained by hand, so a platform added at a different contract version is silently uncovered, which is exactly what a contract-version check exists to prevent (operator ruling 12). Replacement: after `from agentworks.capabilities.descriptor import descriptor_for`, assert `{cls.contract_version for cls in descriptor_for("vm-platform").registry().values()} == {1}`. Verified by executing: the registry yields `aws-ec2, azure-vm, gcp-gce, lima, proxmox, wsl2`, all at contract version 1, so this is the same claim quantified over the live registry instead of a list. **The imports change with it, and there are five.** Running ruff over the simulated edit reports `LimaPlatform`, `WSL2Platform`, `EC2Platform`, `AzureVMPlatform` and `GCEPlatform` all unused (F401): the replacement names none of them, because it quantifies over the registry rather than over a hand-listed set. None is load-bearing for registration either, since importing any `agentworks.plugins.*` module registers every platform. The convert drops all five. The other imports from those modules, the `_LIMA_IMAGE_BLOCKS` and `_DEBIAN_OCI_TAGS` D-165 keeps, stay. No production change. |
| D-167 | `cli/tests/resources/test_resolved_spec.py::test_projection_is_complete_ordered_and_maps_layer_roles@acad42` | hardcoded copy of a resolved model's field list | convert | The ten-name literal restates `ResolvedVMTemplate`'s dataclass field order minus `name`, so adding a legitimate field to the model breaks it. The invariant worth keeping is that the projection covers every resolved field in declaration order. Replacement, no production change: build the expected sequence in the test as `[f.name for f in dataclasses.fields(ResolvedVMTemplate) if f.name != "name"]`. Verified by execution at this basis: that sequence equals the current literal exactly. **Executed 2026-09-06, and the objection to it was wrong.** A previous round called the replacement self-asserting because it is character for character the body of `_resolved_field_names`; that is a resemblance, not a call. The inlined expression reads `dataclasses.fields` directly and does not go through that helper, so it has an independent source and it does discriminate: applied to the test, it fails when the last projected field is dropped. (The other mutation tried, sorting the field order, proves nothing: both sides derive from the same `fields` call and move together.) **Do not derive it through `resolved_spec_default_paths` or `_resolved_field_names`**, because `project_resolved_spec` builds the spec from that same helper (`resources/resolved_spec.py::project_resolved_spec`), and a version routed through it would assert the function against itself. |
| D-168 | `cli/tests/schema/test_markers.py::test_a_template_the_extractor_could_not_render_is_refused_at_construction@3793f7` | authored refusal-message fragments discriminating same-type branches | delete | Seven parametrized cases each pair a bad template with a fragment of the message it should produce, then assert `expected in str(exc.value)`. **Callee-side raise screen, run by hand at this basis:** `SecretRef(...)` reaches `StateError` from five distinct paths, all five in `schema/markers.py::_check_owner_template` (entered from `__post_init__` at `markers.py::RefMarker.__post_init__`), so case 1 does not apply. **No structural handle:** none of the five passes `entity_kind` or `entity_name`, so they are indistinguishable except by our own wording, and R2.2 forbids adding a discriminator for a test. **Injected-marker screen:** the needles are fragments of those production templates rather than markers the test wrote, so the screen is silent and the delete stands. Case 2's fallback applies: drop the `expected` parameter and the assertion, keep all seven templates under `pytest.raises(StateError)`. Cost, stated per the screen: a template refused by the wrong rule will now satisfy the test. |
| D-169a | `cli/tests/test_machine_output.py::test_machine_output_commands_are_the_complete_v1_contract@438c86` | production enum restatement | keep | **Verified by mutation, 2026-09-06, and the disposition changed.** **The "only pin" claim was false** and is withdrawn: the shipped values are also asserted at `test_machine_output.py::test_envelope_is_utf8_deterministic_and_has_one_trailing_newline` and `test_machine_output.py::test_writer_writes_directly_without_ansi_presentation_bytes`, `test_machine_output_cli.py::test_operational_list_json_commands_are_closed_parseable_envelopes` and `test_machine_output_cli.py::test_resource_list_json_is_parseable_deterministic_and_uses_safe_fields`, and `test_operational_json_cli.py::test_nonempty_operational_lists_have_exact_ordered_json`. What holds is narrower and still carries the row. This is the only statement of the COMPLETE ORDERED set, so a value added in the wrong place, or dropped, fails here and nowhere else; and `secret.list` and `secret.describe` are pinned nowhere else at all. The list is a restatement of `MachineOutputCommand`, but it is the restatement that guards the wire contract, which is what separates it from the closed-enum test beside it. |
| D-169b | `cli/tests/test_machine_output.py::test_output_formats_are_closed_to_human_and_json@c05593` | production enum restatement | delete | A literal copy of `OutputFormat`'s two members plus a constructor call that raises because a `StrEnum` is closed, so it can only fail when someone edits the enum it restates. **Verified by mutation, 2026-09-06:** adding a YAML member to the enum fails six sibling tests, so the closure this asserts is covered where the formats actually ship, and `--output yaml` is refused through the real CLI in `test_machine_output_cli.py::test_operational_json_usage_errors_have_empty_stdout_before_work`, with a non-zero exit and empty stdout. |
| D-170 | `cli/tests/test_machine_output.py::test_envelope_is_utf8_deterministic_and_has_one_trailing_newline@3a850d` | byte-verbatim pin of an encoded envelope | convert | The `first == b'{"schema_version":1,...}'` equality is the only assertion in the file that constrains separator spelling, which no JSON consumer branches on. It does also prove the data is carried through unchanged, which nothing else in the test states. Replacement, no production change: `assert json.loads(first) == {"schema_version": 1, "command": "resource.list", "data": data}`. The neighbors carry everything else and stay: determinism (`first == second`), the single trailing newline, no byte-order mark, the snowman emitted as UTF-8 rather than escaped, and the envelope key order. |
| D-171 | `cli/tests/db/test_instance_state.py::test_desired_overlay_round_trip_upsert_clear_and_canonical_storage@5566c1` | stored-JSON byte form pin | keep | Invariant: persisted payloads are stored canonically. `_encode_payload` sets `sort_keys=True`, `separators=(",", ":")` and `ensure_ascii=False` deliberately (`db/instance_state.py::_encode_payload`), and this is the only assertion anywhere that reads the stored text rather than the decoded value, so deleting it leaves the storage form unstated. **Gap, recorded rather than fixed here:** the asserted payload is the single-key `{"value":"new"}`, which exercises the separators and neither the key sort nor the non-ASCII handling. Closing it costs one line, asserting the raw text of a multi-key non-ASCII payload instead, and the owning effort may take it or leave it. |

## Group 5: authored-artifact form policing

**Precondition, operator disposition 2026-08-19: the trap-carrying recipes are re-verified before
their rows execute.** Four recipes verified in the #573 round carry a recorded trap, and they belong
to this group and group 6: L-202 and F-117 here, L-120 and L-121 there. The sampling is per row and
per site, owed by the PR that carries the conversion, and the PR says how many sites it sampled. It
was written against groups 3 and 4, which carry none of the four.

The fresh cut grew this group most, the unsurveyed files and the twenty re-derived ones both landing
mostly here.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-066 | `cli/tests/test_database_backup.py::test_backup_names_are_disjoint_and_collisions_are_reserved@26ce0f` | backup filename format pins | convert | The filename format is load-bearing (retention parses it with `db/backup.py`'s `_AUTOMATIC_NAME`), but its functional consequences are already asserted behaviorally in `test_database_backup.py::test_automatic_retention_uses_timestamp_and_ignores_manual_and_unrelated`, where a manual backup and a timestamp lookalike both survive retention. What is left unique here is collision reservation. Replacement: assert the two manual backups differ and both exist, and that the automatic name is disjoint from them. No production change. |
| B-056 | `cli/tests/transports/test_factories.py::test_tailscale_wait_wording_is_a_closed_set@732462` | closed-set verbatim wording pin | delete | `test_tailscale_wait_wording_is_a_closed_set` exists only to pin four operator-facing strings byte for byte; its docstring says so. The canonical text of the enum is the enum. Delete the test. |
| B-057 | `cli/tests/transports/test_factories.py::test_wait_for_reconnect_default_context_is_reconnect@6c6f4e,test_wait_for_reconnect_verify_context_emits_verify_wording@3a6806` | wording substring plus wording blacklist | convert | The invariant is that `context=` selects the message pair, which a regression that ignores the parameter would break. Replace substring literals with derivation from the canonical source: `TailscaleWait.VERIFY.waiting_message in captured_output.detail`, and the blacklist with `TailscaleWait.RECONNECT.waiting_message not in captured_output.detail`. VERIFIED by executing: both tests pass under this rewrite. No production change. |
| B-068 | `cli/tests/test_claude_code_integration.py::test_present_transcript_resumes@ad9ff7,test_absent_transcript_launches_fresh@437df2` | pane-echo wording substring (two sites) | delete | `"resuming session s1" in command` and `"starting new session s1" in command`. Each test's structural half (`f"--resume {_SID}" in command` with `"--session-id" not in command`, and the mirror) proves which branch ran; the echo is what the operator reads in the pane. The old justification said these tests were nothing but a launch-note equality; at `ed0410c9` they are not, so only the echo halves go. |
| B-101 | `cli/tests/test_secret_sources_drive_harness.py::test_unsupported_windows_refuses_before_environment_or_provider_work@ccc287` | harness-constant equality | keep | `captured.err == f"{HARNESS._UNSUPPORTED_HOST_MESSAGE}\n"` derives the expected text from the canonical constant instead of restating it, which is exactly the derivation the rule prescribes; the invariant is that the refusal reaches stderr and nothing else runs. |
| B-115 | `cli/tests/test_secrets_inspect.py::test_render_grid_uses_readiness_vocabulary@baf060` | rendered-vocabulary substring | delete | `test_render_grid_uses_readiness_vocabulary` names its own shape: it asserts the renderer uses particular authored words. The underlying precedence (readiness wins over identifier, wont-attempt wins over not-ready) is pinned structurally in `test_secrets_inspect.py::test_grid_cell_not_ready_wins_over_identifier` and `test_secrets_inspect.py::test_grid_cell_wont_attempt_wins_over_not_ready`. |
| C-001 | `cli/tests/test_sample_config.py::test_sample_config_parses_and_loads_as_shipped@4bafe9` | production-loader round trip | keep | The shipped sample must parse and load through the real loader, and its top-level roots must equal `EXPECTED_TOP_LEVEL_KEYS`: derivation parity against a production constant, not a copy of the file's text. Catches a settings root added to the loader but never surfaced in the sample. |
| C-002 | `cli/tests/test_sample_config.py::test_sample_config_examples_uncomment_cleanly@7e74f7` | uncomment-convention round trip | keep | Stripping one `#` from every `#<toml>` line must still parse and load, and the fully-uncommented roots must equal `EXPECTED_TOP_LEVEL_KEYS`. The assertion runs the production parser rather than restating text, and catches the real regression: an example written `# key = value` no longer uncomments in place. |
| C-003 | `cli/tests/test_package_exports.py::test_every_exported_name_resolves@b8eef3` | structural presence | keep | Every package's `__all__` must name a symbol that exists. Ruff's F822 is disabled exactly inside `__init__.py`, so this is the only guard, and the failure it catches (a stale export raising AttributeError on `import *`) has already happened once. |
| C-014 | `cli/tests/test_builtin_entries_parity.py::test_installer_plugin_rows_match_oracle_and_are_disabled_by_default@7a8f89,L37-215` | **[line-anchored: module level]** verbatim pin of authored manifests | delete | The `EXPECTED_*` constants are, by the module docstring's own admission, hand-transcribed copies of the bundled plugin manifests kept inside the test. That is the second copy the rule forbids, not derivation parity: the canonical source is the YAML, so any legitimate manifest edit becomes a two-file ceremony. |
| C-017 | `cli/tests/test_completions.py::TestTopLevelGroups.test_expected_top_level_groups_match@436dac` | hand-maintained copy of the command tree | delete | `EXPECTED_GROUPS` is a second copy of the CLI's own top-level group list, and the test's only content is comparing it to the live tree. It can only fail when someone changes the tree and does not paste the change here. Delete the test and the constant; the class holds nothing else. |
| C-018 | `cli/tests/test_completions.py::TestRetiredCommandsAbsent.test_workspace_group_carries_no_shell_or_console@a368dd,TestRetiredCommandsAbsent.test_vm_group_carries_no_console@c90088` | retired-surface absence blacklist | delete | Four `not in` assertions saying `workspace shell`, `workspace console` and `vm console` are still gone, plus `("vm.console", "name") not in DYNAMIC_COMPLETIONS`. Reintroducing a command is a deliberate, reviewed act, not an accident a blacklist catches. Delete the whole class. |
| C-019 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_all_keys_resolve_to_real_commands@402587` | structural validity of a mapping table | keep | Every `DYNAMIC_COMPLETIONS` key must name a command path that exists in the live spec and a param that exists on it. A rename anywhere in the CLI leaves a silently dead completion, and nothing else in the suite walks the table against the tree. |
| C-020 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_completer_ids_are_known@221bc5` | cross-map completeness | keep | Every completer id in `DYNAMIC_COMPLETIONS` must have an entry in all three shell maps. The bash generator silently skips ids it does not know, so an id renamed in two maps and missed in the third ships as dead completion with no other symptom. |
| C-021 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_show_topics_use_the_list_stream_in_every_shell@e45e11` | invocation contract | keep | The guide-topic completer must run `agw guide list` in all three shells. The other guide entry point renders prose, so a completer that called it would dump rendered guide text into the candidate list. The pinned substring is the command the snippet runs, not wording. |
| C-022 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_database_backed_snippets_share_hidden_probe_contract@db6761` | derivation parity against production sets | keep | The probe marker and the stderr redirection must be present exactly for the completers in `DATABASE_BACKED_DYNAMIC_COMPLETERS` and absent for the others, the three shell maps must have equal key sets, and the paths in `DATABASE_BACKED_DYNAMIC_COMPLETIONS` must equal `DATABASE_BACKED_COMPLETION_PATHS`. Every expectation is computed from a production constant, so this is parity between shipped declarations rather than a copy of one. |
| C-023 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_database_backed_invocations_all_carry_hidden_probe_marker@53b54e` | generated-invocation guard | keep | Every generated database-backed invocation must carry the probe marker. A marker-free one opens the state database during tab completion, which is the exact bug the probe exists to prevent, and the regex quantifies over `DATABASE_BACKED_COMPLETION_PATHS` rather than over a hand list. |
| C-024 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_database_restore_uses_native_file_completion_in_every_shell@acb2b2` | native-file-completion contract | keep | `database restore`'s path argument must route to each shell's own file completion (`compgen -f`, `_files`, `CompleteFilename`), and the generated scripts must carry the matching call. Those tokens are the shells' vocabulary rather than ours, and losing one silently breaks path completion for the only command that takes a path. |
| C-025 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_database_restore_uses_native_file_completion_in_every_shell@acb2b2` | **[unverified]** weak presence pins on a whole generated script | delete | The trailing loop asserting `"database"`, `"restore"` and `"--yes"` each appear somewhere in each generated script. Substring presence anywhere in a script this size is not a probe of anything, and the assertions above it in the same test already pin `database restore`'s file-completion wiring specifically. |
| C-026 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_bash_preserves_spaces_in_restore_file_completion@e04ec1` | executed behavior | keep | Runs the generated bash completion under a real bash and asserts a filename containing spaces survives as one candidate. This is the only assertion in the file that proves the emitted quoting works. |
| C-027 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_secret_verify_variadic_completion_contract_in_every_shell@5863f1` | generated variadic-position contract | keep | The `-ge` operator and its `0` threshold in bash and powershell, and zsh's `*:` catchall, are what make a variadic positional complete at every trailing position rather than at one. That is generator behavior expressed in the target shell's syntax, and no test in this file runs zsh or powershell unconditionally to observe it. |
| C-028 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_secret_verify_variadic_completion_contract_in_every_shell@5863f1` | invocation contract | keep | `agw secret list --names-only` must be what the secrets completer runs in all three shells, for the same reason as C-021: the alternative paths render or resolve rather than list. |
| C-029 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_secret_verify_variadic_completion_contract_in_every_shell@5863f1` | **[unverified]** flag spelling pin plus typo blacklist | delete | The closing loop asserts `"--allow-interaction" in block` and `"--allow-interactive" not in block` for all three blocks. The tree is generated from Typer, so a declared flag reaches the script by construction, and the blacklist rejects one misspelling someone already thought of. `"--allow-interaction) continue"` earlier in the same test carries the bash option-skipping behavior and is not this row. |
| C-030 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_topic_completion_stream_uses_the_package_catalog@482d33` | catalog parity | keep | `response.markdown.splitlines()` must equal the sorted union of the discovered concept shells and the release-history topics, both read from production. Derivation parity against a canonical source: a topic that stops reaching the completion stream fails here. |
| C-032 | `cli/tests/test_completions.py::TestOptionFlagsInSpec.test_resolve_flag_is_in_the_completion_spec@e6532f,TestOptionFlagsInSpec.test_removed_reveal_secrets_spelling_is_absent_from_the_spec@b9bde3` | per-flag spec restatement plus retired-spelling blacklist | delete | One test asserts `--resolve` is among `env show`'s option flags and its sibling asserts `--reveal-secrets` is not. The spec is introspected live from Typer, so a declared flag reaches it by construction and a removed one cannot return without a reviewed edit to the command. Delete both tests. |
| C-033 | `cli/tests/test_completions.py::TestOptionFlagsInSpec.test_include_disabled_flag_reaches_the_completion_spec@64b25f` | per-flag spec restatement | delete | `"--include-disabled" in opts` for `resource list`, the same shape as C-032's first half with no blacklist beside it. |
| C-034 | `cli/tests/test_completions.py::TestOptionFlagsInSpec.test_machine_output_options_reach_every_shell_completion@e193de` | closed-choice contract over an enumerated surface | keep | Every JSON v1 command must expose `--output` with exactly `["human", "json"]`. That closed set is the machine-output contract, and the sixteen-path list is the quantifier that stops one command dropping or widening the flag unnoticed. The trailing `"--output" in generate(shell)` is C-025's weak-presence shape and goes with this row's edit. |
| C-035 | `cli/tests/test_completions.py::TestOptionFlagsInSpec.test_canonical_lifecycle_is_discoverable@41ad10` | **[unverified]** command-tree and completion-table restatement | delete | `{"start", "restart"} <= session.subcommands.keys()` restates the command tree, and the loop asserting five `("session.start", param)` and `("session.restart", param)` keys are in `DYNAMIC_COMPLETIONS` restates the table's contents. C-019 already proves every key in that table resolves to a real command and param. |
| C-036 | `cli/tests/test_completions.py::TestGeneration.test_zsh_generates_nonempty@bba671,TestGeneration.test_powershell_generates_nonempty@f39e39` | shell-required header directives and the version stamp | keep | `#compdef agentworks` and `Register-ArgumentCompleter` are each shell's own requirement for a script to be loaded at all, so they are outside vocabulary rather than ours, and `agentworks-completion-version:` is what doctor's staleness check reads out of an installed script. The `len(output) > 100` smoke threshold rides with them. |
| C-037 | `cli/tests/test_completions.py::TestRegistrySourcedCompleters.test_bash_snippets_source_from_registry@401af4,TestRegistrySourcedCompleters.test_zsh_functions_source_from_registry@e55737,TestRegistrySourcedCompleters.test_powershell_snippets_source_from_registry@87cadd` | cross-artifact sourcing contract | keep | `f"--kind {kind}" in snippet` for each of the seven registry-backed completers proves each one sources from `agw resource list --kind X` rather than scraping `config.toml`. The expected kind comes from the case table beside it, and drift here ships wrong candidates: the greedy-regex bug the class docstring records. |
| C-038 | `cli/tests/test_completions.py::TestRegistrySourcedCompleters.test_bash_snippets_source_from_registry@401af4,TestRegistrySourcedCompleters.test_zsh_functions_source_from_registry@e55737,TestRegistrySourcedCompleters.test_powershell_snippets_source_from_registry@87cadd` | **[unverified]** technique blacklist | delete | `"sed " not in snippet` in the bash and zsh tests and `"Select-String" not in snippet` in the powershell one ban the superseded scraping technique by substring. C-037 in the same three tests already asserts the shape the snippets must have, and a snippet cannot both source from the registry and scrape TOML. |
| C-042 | `cli/tests/test_completions.py::TestUninstall.test_powershell_uninstall_removes_script_and_profile_line@6d5f76,TestUninstall.test_powershell_uninstall_preserves_user_lines_mentioning_filename@ba53ff` | installer-line surgery | keep | The `$PROFILE` strip must remove the installer's exact dot-source line and leave user lines that merely mention the filename: a comment, a conditional, a dot-source of a different file. Every string asserted is one the test itself wrote into the profile, so this is value identity over a real edit rather than a pin on anything we ship. |
| C-043 | `cli/tests/test_completions.py::TestVariadicPositionalCompletion.test_zsh_uses_star_for_variadic@7b4fd4,TestVariadicPositionalCompletion.test_bash_uses_ge_for_variadic@18ec72,TestVariadicPositionalCompletion.test_powershell_uses_ge_for_variadic@a2344d` | generated variadic syntax | keep | The same invariant as C-027 for `console create`'s variadic sessions positional, asserted per shell: `*:` in zsh, and both `-ge` and `-eq` present in bash and powershell so the variadic and non-variadic forms stay distinct. |
| C-044 | `cli/tests/test_completions.py::TestKindsSourcedCompleter.test_all_shells_call_resource_kinds@bdc26f` | invocation contract | keep | The kinds completer must call `resource kinds --names-only`, the config-free path. Calling the config-loading sibling makes tab completion depend on a loadable config, which is the failure this completer exists to avoid. |
| C-045 | `cli/tests/test_completions.py::TestKindsSourcedCompleter.test_all_shells_call_resource_kinds@bdc26f` | **[unverified]** technique blacklist | delete | `"resource list" not in source` in the same loop bans the superseded scrape by substring, the same shape as C-038, and C-044 above it already asserts what the snippet must call. |
| C-046 | `cli/tests/test_completions.py::TestStaticChoiceCompletion.test_shell_option_choices_extracted@3ec87c` | upstream-extraction guard | keep | `by_name["shell"] == ["bash", "zsh", "powershell", "pwsh"]` proves Typer's `FuncParamType` is still unwrapped so `click.Choice` values reach the spec. The regression is driven by an upstream version bump rather than by our editing, and it is silent: every Choice-typed option loses static completion at once. |
| C-047 | `cli/tests/test_completions.py::TestStaticChoiceCompletion.test_schema_kind_completes_dynamically@f10e0f` | **[unverified]** complete-option-list restatement | delete | `[opt for param in schema.params for opt in param.opts] == ["--install"]` pins the entire option list of `resource schema`, which is a restatement of the command definition and fails on any added option, good or bad. |
| C-048 | `cli/tests/test_completions.py::TestStaticChoiceCompletion.test_graph_choices_and_depth_suggestions_are_distinct@e7ab6d` | choice versus suggestion mechanism | keep | `depth` carries `suggestions` and no `choices` while `direction` and `output_format` carry closed `choices`. Those are two different completion mechanisms, and collapsing `depth` into a choice list makes `--depth 7` unusable, which nothing else catches. |
| C-049 | `cli/tests/test_completions.py::TestStaticChoiceCompletion.test_all_shells_emit_graph_static_values@19adf5` | vacuous presence assertion | keep | Asserts `"dependencies"`, `"dependents"`, `"both"`, `"1"`, `"2"`, `"3"` and `"all"` each appear somewhere in each generated script. **Both halves bite, checked by mutation 2026-09-06.** The single-character values do occur throughout each script for unrelated reasons, but clearing the depth suggestions drops `3` to zero occurrences and the test fails, so the depth half is not vacuous either. **Re-decided 2026-09-06: the row keeps.** Two twins were named for the direction half and neither covers it. `test_all_shells_emit_shell_choices` (`test_completions.py::TestStaticChoiceCompletion.test_all_shells_emit_shell_choices`) asserts the `--shell` option's own choices, a different parameter. C-048 (`test_graph_choices_and_depth_suggestions_are_distinct`, `test_completions.py::TestStaticChoiceCompletion.test_graph_choices_and_depth_suggestions_are_distinct`) asserts the completion SPEC holds `dependencies`, `dependents` and `both`, and says nothing about what any shell emits. Nothing else asserts that a generated script carries the direction values, so deleting this row would drop that coverage entirely. |
| C-050 | `cli/tests/test_completions.py::TestStaticChoiceCompletion.test_all_shells_emit_shell_choices@79eb47` | per-shell emission shape | keep | Each generator's own syntax for a static choice list (`:shell:(...)` in zsh, `compgen -W` in bash, one `CompletionResult` per value in powershell) is behavior that is invisible from the spec, and a regression silently drops static completion in that one shell. |
| C-052 | `cli/tests/test_terminal.py::test_sanitize_supersets_the_mouse_reset@45d9b1` | shared-constant superset | keep | `MOUSE_TRACKING_DISABLE in TERMINAL_SANITIZE` proves the attach payload reuses the prompt-level constant instead of a divergent copy, and `;1006` is SGR mouse mode, the externally defined sequence that garbles a dropped attach. |
| C-053 | `cli/tests/test_terminal.py::test_sanitize_clears_each_mode_a_remote_program_can_leave_set@66ff6d` | externally-defined escape inventory | keep | Each pinned escape is a terminal-defined reset for a mode a remote program can leave set. The payload's effect is unobservable in CI, so this table is the only guard that sanitize still restores each mode. |
| C-054 | `cli/tests/test_terminal.py::test_sanitize_grounds_the_parser_first@e47bd9,test_sanitize_leaves_the_alternate_screen_before_resetting_margins@af2608,test_sanitize_brackets_the_cursor_homing_resets@de2dd5,test_sanitize_resets_rendering_after_the_cursor_restore@8f0936` | ordering of an escape sequence | keep | The `.index()` comparisons encode terminal semantics, not lexical taste: ground the parser first, leave the alternate screen before resetting margins, bracket the cursor-homing resets in DECSC/DECRC, and put the rendering resets after DECRC (which restores charset and SGR). Reordering the constant corrupts a real terminal and nothing else would catch it. |
| C-055 | `cli/tests/test_terminal.py::test_sanitize_does_not_hard_reset@437f87` | single-token blacklist | keep | `"\x1bc" not in TERMINAL_SANITIZE` bans RIS, an externally defined sequence with one spelling and a destructive effect (it clears the scrollback the operator most wants after a dropped attach). Unlike a phrase blacklist there is no paraphrase to slip through. |
| C-130 | `cli/tests/test_bootstrap_script.py::test_generate_bootstrap_script_all_steps@817c43,test_generate_bootstrap_script_preserves_ssh_host_keys@9ac456,test_generate_bootstrap_script_swap_disabled@34e7ba,test_generate_bootstrap_script_writes_shell_rc_seeds@4a0487` | step-marker checklist | delete | The `##STEP##` labels are a required-phrase list over a script we author. Verified by search: nothing in `cli/agentworks` reads `##STEP##` or `##SUCCESS##` (only `capabilities/vm_platform/bootstrap_script.py` emits them), so they are display-only, and each step's payload is asserted separately. |
| C-131 | `cli/tests/test_bootstrap_script.py::test_generate_bootstrap_script_all_steps@817c43,test_generate_bootstrap_script_masks_sve_gated_on_apple@8eb392,test_authorized_keys_install_is_idempotent@31cf97,test_ssh_key_install_is_skipped_when_the_key_is_empty@3c2939,test_swap_fstab_append_is_guarded_against_re_execution@403d23` | generated-script behavior | keep | `set -euo pipefail`, the injected auth key and swap size, the SVE probe paths, the idempotent `grep -qxF ... \|\| echo` append and its single-append count are what the script does on a real VM, unobservable in CI any other way. |
| C-132 | `cli/tests/test_bootstrap_script.py::test_generate_bootstrap_script_can_omit_join_from_retained_payload@22ad48,test_ssh_key_install_is_skipped_when_the_key_is_empty@3c2939` | echoed sentence pins | delete | `"Tailscale join deferred to platform"` and `"SSH key provisioned out of band"` pin operator-facing prose inside the script; each branch is pinned structurally beside it (`TAILSCALE_AUTH_KEY=''`, `SSH_PUBLIC_KEY=''`). |
| C-133 | `cli/tests/test_bootstrap_script.py::test_sve_gate_matches_sve_and_sve2_as_whole_words@24c1c2` | probe regex against real `/proc/cpuinfo` shapes | keep | The SVE gate is evaluated against real feature strings including the near-miss `sveaes`-without-`sve` case: behavior over externally shaped input. |
| C-134 | `cli/tests/test_cloud_init.py::test_provisioning_packages_minimal@ab1f53,test_init_system_packages@2dee62` | membership pins on two authored constants | convert | Replace the per-package membership and absence pins with the boundary they express, `set(PROVISIONING_PACKAGES).isdisjoint(INIT_SYSTEM_PACKAGES)`, keeping only the packages bootstrap cannot proceed without (`openssh-server`, `ca-certificates`). Otherwise the test is a second copy of two lists. No production change. |
| C-135 | `cli/tests/test_ssh_config.py::test_ensure_include_adds_to_top@44b912` | **[unverified]** marker literal restated | delete | `lines[0] == "# Added by agentworks"` restates `ssh_config.py`'s `_INCLUDE_COMMENT`'s `_INCLUDE_COMMENT`; it can only fail when someone edits that constant, and the include directive's own assertions carry the behavior. |
| C-137 | `cli/tests/test_ssh_config.py::test_rebuild_config_dir@384227` | header prose | delete | `"Managed by agentworks" in content` pins the generated block's authored header; the `Host`/`User`/address assertions are the behavior. |
| C-138 | `cli/tests/test_ssh_config.py::test_remove_legacy_section@78fb0b,test_rebuild_config_dir@384227,test_rebuild_config_dir_emits_per_agent_blocks@6d9a75,test_rebuild_config_dir_no_agent_blocks_when_vm_has_none@70a102,test_rebuild_config_dir_agent_blocks_global_namespace@4b6333,test_legacy_rebuild_emits_per_agent_blocks@082312,test_legacy_rebuild_no_agents_no_per_agent_blocks@70a102` | generated-config behavior | keep | Host aliases, users, addresses, the count of address occurrences, and the absence of retired composite aliases are the SSH config's actual content; wrong output breaks every `ssh awvm--x`. |
| C-142 | `cli/tests/test_authorized_keys.py::test_header_present@fac28b` | generated-file header prose | delete | "Managed by agentworks", "manual edits will be overwritten" and the setting name pin an authored header block; the keys, path, ownership and mode assertions are the behavior. |
| C-144 | `cli/tests/test_admin_home_permissions.py::test_bootstrap_forces_private_group_idempotently@4aaa7a` | superseded-form blacklist | delete | `"useradd -m -U" not in script` bans the old spelling by substring; `assert 'groupadd -f "$VM_USER"' in script` and `assert 'useradd -m -g "$VM_USER" -s /bin/bash "$VM_USER"' in script` already pin the pair the script must emit. |
| C-145 | `cli/tests/test_admin_home_permissions.py::test_bootstrap_forces_private_group_idempotently@4aaa7a` | order in a generated script | keep | The index comparison is over generated shell, which executes top to bottom, so the order is the behavior (create the group before the user that references it), not a lexical pin on our own source. |
| C-151 | `cli/tests/test_initializer_reconcile.py::test_preserve_ssh_host_keys_emits_same_bytes_as_phase_a@eea567,test_apply_sve_mask_emits_same_grub_bytes_as_phase_a@825dc8` | derivation parity between two production constants | keep | `"".join(f"{line}\n" for line in LINES) == CONTENT` proves the rendered blob is derived from the line list rather than a divergent second copy: exactly the shape the rule endorses. |
| C-154 | `cli/tests/test_initializer_reconcile.py::test_preserve_ssh_host_keys_writes_dropin@417795,test_preserve_ssh_host_keys_emits_same_bytes_as_phase_a@eea567,test_apply_sve_mask_noop_when_gate_closed@5cf383,test_apply_sve_mask_emits_same_grub_bytes_as_phase_a@825dc8,test_install_claude_plugins_runs_when_claude_present@58a996,test_reconcile_authorized_keys_direct_write_when_owner_none@4fa771,test_reconcile_authorized_keys_stage_and_install_when_owner_set@4cad99` | command-effect assertions | keep | Which command ran, with which sudo flag, path, owner, mode and staging file, is the reconcile pass's behavior against a real target. |
| C-157 | `cli/tests/test_initializer_hardening.py::test_sysctl_baseline_writes_when_missing@6f99f0,test_sysctl_baseline_noop_when_content_matches@f873cc,test_sysctl_baseline_rewrites_when_content_differs@6f99f0,test_fstab_hidepid_appends_when_no_proc_line@d1b0c6,test_fstab_hidepid_adds_option_when_proc_line_has_no_hidepid@cc6587,test_fstab_hidepid_upgrades_0_to_1@045074,test_fstab_hidepid_noop_when_already_1@c592d4,test_fstab_hidepid_preserves_admin_set_hidepid_2@08bd7f,test_ensure_proc_hidepid_appends_when_no_proc_line@ec43f3,test_ensure_proc_hidepid_added_option@1b74fb,test_ensure_proc_hidepid_upgraded@fbd098,test_ensure_proc_hidepid_no_op@44e6a6` | fstab and sysctl edit behavior | keep | The hidepid option's append, add, upgrade and no-op outcomes, the preserved unrelated fstab line, and the remount commands are the behavior of a real edit against a real file. |
| C-158 | `cli/tests/test_tailscale_dns.py::test_dropin_content_orders_after_network_online@cd2899,test_dropin_content_includes_nss_lookup_target@ed5760,test_dropin_content_does_not_use_requires@43c080` | systemd unit directives | keep | `After=`/`Wants=network-online.target`, `nss-lookup.target`, and the absence of `Requires=` are systemd's own vocabulary and decide boot ordering; the effect is not observable in CI any other way. |
| C-162 | `cli/tests/test_tailscale_dns.py::test_dropin_written_when_missing@5c3fca,test_dropin_noop_when_content_matches@ce3af9,test_dropin_rewritten_when_content_differs@cdce3e,test_dropin_does_not_restart_tailscaled@a112c3,test_apply_invokes_dropin_install@6d5f75,test_check_silent_when_dns_works@21b2bf,test_check_warns_when_dns_fails_and_resolv_unreadable@7da6dc,test_check_dns_probe_runs_first@e735a7` | command-effect and ordering | keep | Which install, daemon-reload, restart and probe commands ran, in which order, and which did not run, is the drop-in reconcile's behavior. |
| C-192 | `cli/tests/test_topics.py::test_overview_is_dedented_so_prose_can_be_written_where_it_belongs@2ac70c,test_summary_of_reads_the_declaration_that_already_exists@f6eb9e` | fixture prose equality | keep | The pinned text is the test's own fixture docstring, so this asserts the extractor returns what it was given: value identity, not our shipped prose. |
| D-045 | `cli/tests/manifests/test_samples.py::test_a_field_that_folds_a_scalar_names_both_spellings_and_shows_the_block@e9dfb6` | verbatim pin of generated comment lines, plus one absence blacklist | delete | Six literals pinning the renderer's own commented output line by line (`"#  # (table of string or table, optional)"`, `"#    # one entry, as an example:"`, `"#    # One of: plaintext, secret. Shown here: plaintext."`, `"#    # <key>:"`, `"#      # value: <string>"`) plus `"#      # secret: <string>" not in lines`, a wording blacklist on our own output. Every literal is renderer-authored text inside a fully commented block, so no uncomment-and-load test can ever be affected by it. The renderer's real invariants survive in `tests/schema/test_fields.py` and `test_shorthand.py`. |
| D-046 | `cli/tests/manifests/test_samples.py::test_appending_to_a_file_with_no_trailing_newline_still_separates@6fd0aa` | verbatim pin of written file bytes | delete | `assert "\n  cpus: 4\n\n#---\n" in manifest.read_text()`. **Verified by execution.** With `_joined` broken so the separator lands on the operator's last line (`cpus: 4#---`, the case the docstring names), the activate-and-load half at the end of the same test raises `ConfigError: duplicate mapping key 'apiVersion'`. With only the blank line dropped, nothing breaks at all. So this pin's sole unique coverage is a cosmetic blank line in a generated file. |
| D-054 | `cli/tests/manifests/test_reference.py::test_a_collection_of_blocks_renders_one_element_under_a_placeholder@e0d092` | **[unverified]** authored placeholder sentences | delete | `"one element, as an example:"` / `"one entry, as an example:"` are renderer prose; the structural twins that say a placeholder was emitted at all are the surviving `#    # -` and `#    # <key>:` assertions beside them. |
| D-060 | `cli/tests/manifests/test_reference.py::test_an_unexpanded_arm_does_not_name_what_is_shown@1963d0` | authored sentence fragment | delete | `"One of: group."` is renderer prose; the arm inventory it stands for is asserted structurally two tests above. |
| E-001 | `cli/tests/resources/test_always_materialize.py::test_polish_empty_usage_format@33f676` | authored description-format pin | convert | The claim is that the polish fires on an empty description; replace with "polished.description is non-empty and differs from the stub's input" instead of pinning `"(auto) auto-declared default vm-template"`. No production change. |
| E-002 | `cli/tests/resources/test_always_materialize.py::test_polish_skips_operator_set_description@b7a31c` | value identity of an injected string | keep | Invariant: the polish never overwrites an operator-set description. `"operator's own text"` is the value the test injected, so this is propagation identity, not authored prose. |
| E-006 | `cli/tests/resources/test_inherited_reference_attribution.py::test_an_auto_declared_row_records_the_declaring_template_as_its_origin@ba4645` | authored description-format pin | delete | The attribution claim is already carried structurally by `row.origin.source == ("vm-template", "base")` on the line above; the description sentence adds only wording. |
| E-007 | `cli/tests/resources/test_inherited_reference_attribution.py::test_two_descendants_of_one_declarer_do_not_read_as_two_declarations@1af22d` | authored description-format pin (sole assertion) | convert | Rubric re-check, 2026-08-16. The asserted description varies with a real computed quantity, the distinct-declarer count feeding `registry.py::_polish_auto_declared_description`'s more-declarers suffix, and it is the only probe of it. Verified by execution: `registry.graph.dependents_of("secret", "base-secret")` returns three entries with three distinct `source` values and exactly one distinct `declared_by`. No production change. |
| E-021 | `cli/tests/plugins/test_azure.py::test_all_three_rows_disabled_system_plugin_by_default@19ddd5` | verbatim content pin of a shipped manifest | delete | The comment calls it a byte-for-byte content pin so "a typo is caught here". It is not: the pin was written by the same author in the same change as the manifest. The structural claims (row present, origin `system-plugin`, plugin `azure`, disabled) sit directly above. |
| E-026 | `cli/tests/plugins/test_claude.py::test_install_command_row_is_disabled_system_plugin_by_default@21e703` | verbatim content pin of a shipped install command | delete | Same shape as E-021: an authored manifest value copied into a test. |
| E-030 | `cli/tests/plugins/test_codex.py::test_install_command_row_is_disabled_system_plugin_by_default@4d0e95` | verbatim content pin of a shipped install command | delete | Same as E-021/E-026. |
| E-038 | `cli/tests/plugins/test_gcp.py::test_gcp_bundle_publishes_disabled_gcloud_apt_resources@26a04c` | verbatim content pin of a shipped apt-source line | delete | Same shape as E-021: an authored manifest value copied into a test, with the row's presence and provenance already asserted around it. |
| E-151 | `cli/tests/vms/test_system_slug.py::test_slug_prompt_matches_the_frd_text@352690` | **[unverified]** verbatim prompt pin (whole test) | delete | `test_slug_prompt_matches_the_frd_text` pins an operator prompt paragraph verbatim, with a docstring that says the pin exists because "drift is user-visible". This is the canonical shape the rule forbids; the slug's real contract (bounds, validation, non-interactive behavior, blank-is-final) is covered by the tests around it. |
| F-001 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_all_pages_have_metadata_landmarks_skip_link_and_one_h1@98fedf` | per-page title identity | keep | `assertIn(f"<title>{expected[name][0]}</title>", self.pages[name])` where `name` is the loop variable over the built documents and `expected` is the per-page table at the top of the test. The assertion is per-page routing, that this page carries this title, and the same table's canonical-URL half is asserted the same way two lines below and is F-003's, so deleting this would split one property in two. |
| F-002 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_all_pages_have_metadata_landmarks_skip_link_and_one_h1@98fedf` | verbatim pin against a second copy of the policy | convert | `policies[0]["content"] == CSP` compares each page's Content-Security-Policy against `site_test_support.CSP`, a hand-maintained second copy of the template's policy. Ruled by the lead 2026-08-16: a shipped security header's security-relevant directives are behavior, not form. Assert at directive level (that `script-src` carries no `unsafe-inline`, and the equivalent for the other directives the policy relies on) and drop the comparison against the constant. The constant itself cannot be deleted in this sweep: F-046 holds its other consumer and is deferred with the rest of `test_site_templates.py`, so leave it in place until R4 decides that file. No production change. |
| F-003 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_all_pages_have_metadata_landmarks_skip_link_and_one_h1@98fedf` | structural presence | keep | Every built page keeps `<html lang="en">`, one header, one main, one footer and one h1, unique element ids, exactly one untagged `<title>`, a canonical link whose href matches the per-page table, a description meta, and exactly one skip link to `#main-content`. A shell-template refactor can drop a landmark or duplicate an id and nothing else notices. |
| F-005 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths@79b75a` | id blacklist | delete | `assertNotIn("problem", document.ids)`, `assertNotIn("principles", document.ids)` and `assertFalse(document.tags("article"))` assert that a retired home-page design has not returned. A blacklist of shapes someone already thought of, with nothing forcing them back. |
| F-006 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths@79b75a` | derivation parity | keep | The served onboarding prompt must equal `site_builder.AGENT_ONBOARDING_PROMPT_SOURCE` byte for byte, asserted twice: once against the module-level read and once against the file itself. Build-time escaping or whitespace handling can silently ship a prompt that differs from the file operators are told to copy. |
| F-007 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths@79b75a` | exact attribute-dict equality | delete | The run of `assertEqual` calls pinning whole attribute dictionaries for the tab list, the two tabs, the two panels, the prompt `<pre>`, the copy button, the copy icon and the status paragraph. Recipe verification, 2026-08-16: the a11y attributes a conversion would assert are emitted unconditionally by the builder, so the replacement would pass always and could only fail through a `RuntimeError` in `build.py`. Delete rather than convert. |
| F-008 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths@79b75a` | structural presence | keep | Exactly one `#onboarding` section, labelled by exactly one `h2` that is its own descendant, with non-empty text and a non-empty tab-list label. A template refactor can break the `aria-labelledby` wiring with no visible symptom. |
| F-009 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact@8365d4` | per-page breadcrumb label identity | keep | `assertIn(f'aria-current="page">{current_labels[name]}</span>', self.pages[name])` where `current_labels` is the per-page table at the top of the test and is consumed nowhere else. The structural half beside it (exactly one `aria-current` span) cannot tell WHICH page got marked, so a breadcrumb marking a constant label passes without this. |
| F-010 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact@8365d4` | authored-string occurrence counts | delete | `count("Product of Wayfarer Labs, LLC") == 1`, `count(">Agentworks Manifesto</a>") == 1` and `count(">We take security seriously</a>") == 1`. The link-uniqueness property is already held by the href counts earlier in the same loop, keyed on destinations rather than on prose. |
| F-011 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact@8365d4` | structural presence | keep | Each shared destination appears exactly once per page (twice for the repository link on home), the breadcrumb separator stays `aria-hidden`, the two service icons stay `aria-hidden` and `focusable="false"`, the decorative header mark keeps an empty `alt`, and the footer game link keeps a non-empty accessible name equal to its `title`. These are the a11y and navigation invariants a shell edit can regress silently. |
| F-012 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_long_form_pages_are_complete_semantic_source_projections@03387b` | derivation parity | keep | The rendered long-form page is a complete, ordered projection of its Markdown source, blocks and links alike, with no script tag. Verified load-bearing: with `site_validation._validate_template` stubbed out, injecting `<h1>Template title</h1>` into `manifesto.html` still fails here. |
| F-013 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_long_form_contents_navigation_matches_source_h2_h3_structure@d31581` | derivation parity | keep | The table of contents is derived from the source h2/h3 structure, the article and layout child sequences are fixed, and every ToC entry resolves to exactly one id on the page. A dangling anchor is invisible in review. |
| F-014 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_404_retains_fallback_and_has_only_its_local_module@66f235` | retired-shape blacklist | delete | Asserts a retired `home-link` id and an `error-code` class are absent. Nothing reintroduces them. |
| F-015 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_404_retains_fallback_and_has_only_its_local_module@66f235` | structural presence | keep | The `404` page keeps exactly one home link and exactly one module script pointing at the local game bundle. The `404` page is the one nobody looks at, so a broken fallback ships unnoticed. |
| F-016 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_lander_and_404_share_one_byte_identical_game_subtree@264495` | byte parity | keep | The Lander and `404` pages must serve one byte-identical game subtree, each with one `#lander-game` id, a hidden outcome element and exactly one script, and only the pages meant to carry scripts carry them. Verified: with the template validator stubbed, dropping `{{LANDER_GAME}}` from `404.html` fails here. |
| F-017 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_footer_game_target_and_focus_area_are_pinned_in_ordinary_flow@af9a96` | CSS declaration pin | delete | Pins `display: inline-flex`, `width: 24px`, `height: 24px`, the absence of `position:` in one rule, and the presence of an `a:focus-visible` selector anywhere in the stylesheet. Authored CSS whose form review owns; the browser tests measure real geometry where it matters. |
| F-018 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_runtime_assets_are_local_and_privacy_surfaces_are_absent@7e716f` | privacy defense (blacklist) | keep | **Settled 2026-09-06: keep.** The row was left as a convert conditional on what a later consolidation would preserve, which is not a disposition an executor can act on. Three parts, decided separately. The local-asset check (no `src` or `href` leaves the origin) and the forbidden-tag check (`form`, `iframe`, `audio`, `canvas`) are structural and were never in question. The third is `forbidden_runtime_patterns`, and its list is the BROWSER's vocabulary, not ours: `fetch`, `XMLHttpRequest`, `WebSocket`, `EventSource`, `sendBeacon`, `document.cookie` and the storage APIs are named by a platform that changes without our review, which is the `no-prose-policing-tests` exception rather than an authored blacklist. Under the rubric above it is the only probe that a runtime surface has not reappeared in built output that ships to the public, and that is a regression a paste can cause. **Its non-vacuity is NOT established, and this row does not claim otherwise.** The canary self-test belongs to `test_lander_404.py`, which guards eleven surfaces and proves each pattern matches a deliberately planted string. This row's tuple at `test_site_documents.py::GeneratedDocumentTests.forbidden_runtime_patterns` has no canary of its own, so a mistyped pattern here would pass green and nothing would say so, and it carries no pattern at all for two of the eleven: **location navigation** (`location.href`, `assign`, `replace`) and **history mutation** (`pushState`, `replaceState`). Whoever executes this row should give this list a canary and the two missing surfaces. |
| F-023 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_chromium_geometry_owns_the_devtools_process_and_cleanup@336fa8` | argv-shape pin | delete | **Narrowed 2026-09-06 to the `about:blank` half.** `command[-1] == "about:blank"` pins the ordering of an argv our own helper builds, and that is the delete. `--remote-debugging-port=0` is not ours: it is Chromium's flag, third-party vocabulary that changes without our review, which is the `no-prose-policing-tests` exception. Its assertion at `test_site_documents.py::GeneratedDocumentTests.test_chromium_geometry_owns_the_devtools_process_and_cleanup` is also the only guard that the debug port is ephemeral rather than a fixed one a second process could reach, so it stays. **Verified 2026-09-06:** a fixed port fails the geometry test too, so the two assertions guard the same property from different sides rather than one covering the other. The old row also named a `--dump-dom` blacklist, which is gone. The behavior that matters (readiness bounding, shutdown ownership, profile isolation) is asserted by F-024 and F-156 in the same test. |
| F-024 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_chromium_geometry_owns_the_devtools_process_and_cleanup@336fa8,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process@14da82,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors@dbabf4` | resource-lifecycle behavior | keep | The geometry harness closes its DevTools connection, terminates and then kills a stuck browser, joins its server thread, bounds readiness polling at 200 attempts, and re-raises the primary error over a cleanup error while surfacing a cleanup error when there is no primary. Leaked Chromium processes and swallowed errors are exactly the CI defect the 2026-08-15 checklist entry records. |
| F-027 | `website/tests/test_site_templates.py::TemplateContractTests.test_extracted_content_cannot_expand_a_template_token@09b010` | injection defense | keep | Content extracted from README, manifesto and SECURITY must not be able to expand a template token. The input is prose that doc authors edit without ever running the build, so a `{{...}}` sequence arriving in content is a real accident, and re-expansion would splice source text into a position nobody reviewed. This is the one row in the file the operator's deferral does not cover. |
| F-050 | `website/tests/test_site_build.py::BuildAndInstallTests.test_dual_base_builds_have_the_exact_complete_manifest@c8c441` | second copy of a production constant | delete | **Recipe verification, 2026-08-16.** **Recorded as a convert, but the assertion it proposes is already enforced unconditionally by production**, so the replacement can only go red via a `RuntimeError` from `build.py`, never from the assertion itself. That makes it untestable rather than structural, so the row is a plain delete. |
| F-051 | `website/tests/test_site_build.py::BuildAndInstallTests.test_dual_base_builds_have_the_exact_complete_manifest@c8c441` | structural presence | keep | No symlink lands in the output, no unsubstituted `{{` survives, and the site base reaches the rendered hrefs. All three ship silently broken otherwise. |
| F-054 | `website/tests/test_site_build.py::BuildAndInstallTests.test_repeated_builds_are_byte_deterministic@527a65` | byte determinism | keep | Repeated builds are byte-identical, in place and to a fresh directory. This is what the CI double-build-and-diff gate depends on. |
| F-055 | `website/tests/test_site_build.py::BuildAndInstallTests.test_favicon_rejects_every_unreviewed_svg_node@0baf4d,BuildAndInstallTests.test_favicon_rejects_every_unreviewed_svg_node::ValueError::83fb7e` | mutate-one-string-assert-raises | delete | Six mutations of the favicon SVG proving the asset validator rejects extra nodes. In-tree asset, validated on every build; this only proves the guard fires. |
| F-056 | `website/tests/test_site_build.py::BuildAndInstallTests.test_favicon_rejects_non_path_canonical_mark_geometry@ae71e5,BuildAndInstallTests.test_favicon_rejects_non_path_canonical_mark_geometry::ValueError::eeab47,BuildAndInstallTests.test_favicon_rejects_noncanonical_rocket_root_or_mark_hierarchy@7190aa,BuildAndInstallTests.test_favicon_rejects_noncanonical_rocket_root_or_mark_hierarchy::ValueError::f261ac` | mutate-one-string-assert-raises | delete | Three rocket-SVG structure mutations, same shape as F-055. |
| F-057 | `website/tests/test_site_build.py::BuildAndInstallTests.test_unapproved_external_url_fails_before_output_changes@b5b604,BuildAndInstallTests.test_unapproved_external_url_fails_before_output_changes::ValueError::766f6a,BuildAndInstallTests.test_absent_local_route_has_no_partial_manifest_suppression@54f477,BuildAndInstallTests.test_absent_local_route_has_no_partial_manifest_suppression::ValueError::fe209c,BuildAndInstallTests.test_cross_document_fragment_must_exist_on_its_actual_target@19cbe0,BuildAndInstallTests.test_cross_document_fragment_must_exist_on_its_actual_target::ValueError::2ef080,BuildAndInstallTests.test_source_heading_cannot_duplicate_a_shell_id@74651f,BuildAndInstallTests.test_source_heading_cannot_duplicate_a_shell_id::ValueError::a78aaf,BuildAndInstallTests.test_rendered_reference_validation_retains_duplicate_ids@74651f,BuildAndInstallTests.test_rendered_reference_validation_retains_duplicate_ids::ValueError::a78aaf,BuildAndInstallTests.test_manifest_without_root_index_cannot_suppress_root_reference_failure@54f477,BuildAndInstallTests.test_manifest_without_root_index_cannot_suppress_root_reference_failure::ValueError::fe209c` | link and reference integrity | keep | Unapproved external links, absent local routes, absent cross-document fragments, and duplicate element ids all fail the build before any output changes. This is the half of `site_validation.py` that W2 says is real and stays: a broken internal link or a colliding heading id ships silently and no reviewer of a prose edit would see it. |
| F-058 | `website/tests/test_site_build.py::BuildAndInstallTests.test_shared_css_cannot_conceal_reviewed_shell_content@5a770d,BuildAndInstallTests.test_shared_css_cannot_conceal_reviewed_shell_content::ValueError::cbe359` | adversarial obfuscation corpus | delete | Eighteen mutations feeding CSS escape sequences and comment-split property names to prove concealment cannot slip past. The stylesheet is in this tree and changes in the same commits; the threat model for in-tree artifacts is accidental regression, and no accident writes `d\69 splay: none`. |
| F-059 | `website/tests/test_site_build.py::BuildAndInstallTests.test_same_document_fragment_must_resolve_before_output_changes@1522fe,BuildAndInstallTests.test_same_document_fragment_must_resolve_before_output_changes::ValueError::ee89e6` | reference integrity | keep | Renaming the main landmark id breaks the skip link, and the build refuses before touching existing output. Same family as F-057. |
| F-060 | `website/tests/test_site_build.py::BuildAndInstallTests.test_duplicate_rendered_references_cannot_hide_first_browser_destination@685099,BuildAndInstallTests.test_duplicate_rendered_references_cannot_hide_first_browser_destination::ValueError::20a7d7` | bypass canary | convert | Rubric re-check, 2026-08-16. Not an in-tree tampering test: it feeds four SYNTHETIC duplicate-attribute canaries to `_validate_local_references`, so the same-tree argument does not apply. If `_attribute_map`'s check (`site_validation.py::validate_site_base`) were dropped, `dict(attrs)` keeps the LAST, benign attribute and the canaries raise nothing. A repo-wide grep finds only two probes for that raise, this one and F-047, and F-047 is inside the deferred block. Convert per L-401's own rule that the `assertRaises` stays. No production change. |
| F-061 | `website/tests/test_site_build.py::BuildAndInstallTests.test_output_inside_repository_is_rejected_without_source_writes@a29f04,BuildAndInstallTests.test_output_accepts_ordinary_spaced_and_hidden_directory_names@f3f6a0,BuildAndInstallTests.test_output_rejects_dot_traversal@bd765b,BuildAndInstallTests.test_output_rejects_dot_traversal::ValueError::c6b9ac,BuildAndInstallTests.test_output_rejects_parent_symlink_into_repository_when_supported@d2e9e4,BuildAndInstallTests.test_output_rejects_parent_symlink_into_repository_when_supported::ValueError::e2885f,BuildAndInstallTests.test_existing_top_level_directory_symlink_is_rejected_without_touching_target@f609ab,BuildAndInstallTests.test_existing_top_level_directory_symlink_is_rejected_without_touching_target::ValueError::4c5159,BuildAndInstallTests.test_existing_top_level_broken_symlink_is_rejected_without_replacement@424af2,BuildAndInstallTests.test_existing_top_level_broken_symlink_is_rejected_without_replacement::ValueError::4c5159,BuildAndInstallTests.test_existing_output_may_contain_only_selected_owned_entries@05d15a,BuildAndInstallTests.test_existing_output_may_contain_only_selected_owned_entries::ValueError::1ffbe9,BuildAndInstallTests.test_symlink_entry_in_existing_output_is_rejected_when_supported@05c75d,BuildAndInstallTests.test_symlink_entry_in_existing_output_is_rejected_when_supported::ValueError::cc52c0,BuildAndInstallTests.test_fifo_entry_in_existing_output_is_rejected_when_supported@3cef80,BuildAndInstallTests.test_fifo_entry_in_existing_output_is_rejected_when_supported::ValueError::25bf38,BuildAndInstallTests.test_non_directory_output_is_rejected@956532,BuildAndInstallTests.test_non_directory_output_is_rejected::ValueError::4c5159` | boundary guard | keep | The output path is operator-supplied input: dot traversal, symlinked parents, symlinked or broken output roots, FIFOs, non-directories, and unowned entries are all rejected without writing or destroying anything. This is `development-principles` principle 3's operator-input boundary, and the assertions prove the source tree and pre-existing files survive. |
| F-062 | `website/tests/test_site_build.py::BuildAndInstallTests.test_every_static_module_import_resolves_to_the_literal_manifest@a75f7f,BuildAndInstallTests.test_every_static_module_import_resolves_to_the_literal_manifest::ValueError::456e31` | reference integrity | keep | Every JavaScript module import resolves to a file in the shipped manifest. A renamed module ships a page whose script silently 404s. |
| F-063 | `website/tests/test_site_build.py::BuildAndInstallTests.test_model_ships_byte_for_byte_without_generated_proof_composition@5dd25d,BuildAndInstallTests.test_collision_and_world_ship_separately_in_the_exact_manifest@818a84` | byte parity | keep | The Lander model, world, and collision modules ship byte for byte from source and are not inlined into one another. Guards against the build quietly reacquiring a composition step. |
| F-065 | `website/tests/test_site_build.py::BuildAndInstallTests.test_collision_and_world_ship_separately_in_the_exact_manifest@818a84` | schema shape | keep | `validate_game_manifest` rejects a manifest missing the collision module or carrying an extra one. Structural check on a production function, not on any authored spelling. |
| F-067 | `website/tests/test_site_build.py::BuildAndInstallTests.test_authored_lander_modules_and_tests_stay_below_the_review_ceiling@217ac9` | style-rule threshold | delete | Asserts every authored Lander module and test is under 1,000 lines. **Ruled by the lead 2026-08-16**: a style rule mechanized as a test is not a doctrine 2 threshold, because style belongs in the linter and a rule enforced by one package's test suite is enforced inconsistently anyway (no other package in the repository carries this check). The ceiling itself does matter, so if it is worth keeping it moves to `scripts/lint-files.sh` where it would bind the whole repository, rather than being lost here. |
| F-068 | `website/tests/test_site_build.py::BuildAndInstallTests.test_content_or_template_failure_leaves_existing_output_byte_identical@ad189c,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output@bd0d3c,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output::OSError::9e8486,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output::RuntimeError::4183b7` | failure-injection / atomicity | keep | A content, template, rename, or verification failure leaves the previously installed output byte identical. Whether this machinery should exist at all is W7, deferred; while it exists these are the only tests of its failure paths, and a half-installed site is the failure it prevents. |
| F-069 | `website/tests/test_site_build.py::BuildAndInstallTests.test_backup_cleanup_failure_warns_after_successful_commit@4d1554` | verbatim pin (our own warning text) | delete | Pins the exact wording of the retained-backup warning. The behavioral half of the test (a cleanup failure does not fail the build, and the output is still correct) is the `build_site` call and the `href="/agentworks/security/"` assertion below the warning, and stays. |
| F-070 | `website/tests/test_site_build.py::BuildAndInstallTests.test_cli_requires_full_shape_and_reports_one_line_errors@0f9e2b,BuildAndInstallTests.test_cli_requires_full_shape_and_reports_one_line_errors::assertRegex::aad29f` | exit code + output format | keep | The CLI exits 1 and writes exactly one line to stderr. This asserts the shape (single line, `error:` prefix, no traceback), not the message, and a leaked traceback is a real regression. |
| F-071 | `website/tests/test_site_build.py::BuildAndInstallTests.test_remote_runtime_asset_references_fail_before_output_changes@aa3162,BuildAndInstallTests.test_remote_runtime_asset_references_fail_before_output_changes::ValueError::3a6b8d` | boundary guard | keep | No shipped CSS or JS may reference a remote origin, and the build refuses before touching output. This is the site's self-containment guarantee at the point where it can actually be enforced. |
| F-072 | `website/tests/test_site_build.py::BuildAndInstallTests.test_harmless_javascript_line_comment_is_allowed@9c7ee8` | negative control | keep | An ordinary local comment survives the remote-reference scan. Proves F-071's guard is not over-broad, which is the failure mode that would otherwise be discovered by a blocked PR. |
| F-073 | `website/tests/test_site_content.py::SourceContractTests.test_permanent_sources_render_the_complete_content_vocabulary@bae4b3` | vocabulary set pin | delete | `set(content) == {eight token names}` restates the content vocabulary. Verified by execution: dropping `HOME_IDENTITY` from the substitutions fails the build with "missing substitutions for required tokens", so the vocabulary is enforced by building. |
| F-074 | `website/tests/test_site_content.py::SourceContractTests.test_permanent_sources_render_the_complete_content_vocabulary@bae4b3` | **[unverified]** verbatim pin | delete | `assertIn("<strong>Durable agents</strong>", content["HOME_IDENTITY"])` and the SSH-over-Tailscale twin pin two authored README phrases inside the rendered identity block. The drift contract F-082 already ties the rendered identity to the README. |
| F-075 | `website/tests/test_site_content.py::SourceContractTests.test_permanent_sources_render_the_complete_content_vocabulary@bae4b3` | derivation parity | keep | The home meta description is exactly the HTML-escaped canonical value from the contract, each document renders exactly one `h1`, every document has a non-empty meta description, and the security content carries the reporting URL. Escaping regressions are silent and security-relevant. |
| F-076 | `website/tests/test_site_content.py::SourceContractTests.test_assistance_projection_is_exactly_canonical_and_html_escaped@ce0314` | derivation parity | keep | The prompt served in the page is byte-identical to the packaged source, through the README projection and the HTML escape, with the block split round-tripping and the soft-break markup carrying `aria-hidden`. This is the single-source-of-truth guarantee for the one string operators paste into an agent. |
| F-077 | `website/tests/test_site_content.py::SourceContractTests.test_website_sources_do_not_duplicate_the_authored_prompt_body@15c055` | structural presence | keep | The template carries exactly one prompt token and no inlined copy of the prompt body. Cheap, structural, and it is what makes F-076's parity meaningful. |
| F-078 | `website/tests/test_site_content.py::SourceContractTests.test_website_sources_do_not_duplicate_the_authored_prompt_body@15c055` | whole-tree duplicate scan | delete | The loop walking every non-test file under `website/` asserting the prompt body appears nowhere. A stray duplicate is a review finding, and F-076's parity is what guarantees the served copy is canonical. |
| F-080 | `website/tests/test_site_content.py::SourceContractTests.test_assistance_projection_drift_and_framing_fail_closed@064a8f` | derivation parity | keep | The README projection of the assistance prompt must not drift from the packaged source, and the markers and fence must stay well formed: five mutations, each of the projection rather than of either copy's wording. Two files that must agree, with the check on the derivation. |
| F-081 | `website/tests/test_site_content.py::SourceContractTests.test_assistance_source_requires_exact_regular_lf_utf8@4b9c5e,SourceContractTests.test_missing_or_symlinked_assistance_source_fails_closed@688e94` | boundary guard | keep | The assistance source must be UTF-8, NUL-free, LF-terminated, non-empty, present and not a symlink. A BOM or CRLF here silently changes the bytes agents receive, and an editor introduces both by accident. |
| F-082 | `website/tests/test_site_content.py::SourceContractTests.test_home_selection_still_fails_on_missing_duplicate_or_drift@7f9052` | drift detection | keep | The home identity fails closed when the README heading goes missing, duplicates, or drifts from the contract. The input is prose edited by people who never build the site, which is exactly where a silent break happens. |
| F-083 | `website/tests/test_site_content.py::SourceContractTests.test_reviewed_link_url_is_escaped_as_one_href_attribute@88e717` | injection defense | keep | A reviewed destination URL carrying quotes and an event-handler payload is escaped into exactly one `href` attribute and cannot break out. Security test, correctly narrow. |
| F-084 | `website/tests/test_site_content.py::SourceContractTests.test_invalid_utf8_bom_missing_and_symlinked_input_fail_closed@9b1476` | boundary guard | keep | Invalid UTF-8, a BOM, a missing file and a symlinked source each fail closed on the document inputs. Same boundary as F-081. |
| F-085 | `website/tests/test_site_content.py::SourceContractTests.test_security_reference_and_github_only_reporting_are_exact@f17f68,SourceContractTests.test_reference_definition_is_consumed_but_not_rendered@4d313a` | content contract | keep | The security document keeps exactly one reference definition pointing at the reporting URL, that definition is consumed rather than rendered, and an email reporting path fails the build. SECURITY.md is edited by people writing prose, and a stray contact address is a real, silent policy break. The positive case at the end of the first test keeps it from over-firing. |
| F-086 | `website/tests/test_site_content.py::SourceContractTests.test_getting_started_introduction_projects_from_readme@7629d3,SourceContractTests.test_supported_source_additions_render_without_contract_edits@4eb38d,SourceContractTests.test_contents_navigation_derives_nested_h2_h3_links_and_escapes_labels@e4c461,SourceContractTests.test_document_without_h2_or_h3_omits_contents_navigation@f05ab3,SourceContractTests.test_crlf_does_not_break_complete_documents@35cff1,SourceContractTests.test_document_structure_failures_are_closed@4a0385,SourceContractTests.test_unsupported_markdown_and_hard_breaks_fail_closed@678413,SourceContractTests.test_unmatched_reserved_inline_markers_fail_instead_of_rendering_literally@da278c,SourceContractTests.test_source_relative_links_are_explicitly_mapped_and_others_fail@a164f8,SourceContractTests.test_inline_renderer_escapes_text_and_supports_both_emphasis_markers@dbdc42` | parser behavior over synthetic fixtures | keep | Ten tests exercising the Markdown subset renderer against documents the test itself writes: heading structure, list and emphasis rendering, ToC derivation, escaping and placement, CRLF handling, unsupported-syntax refusal, unmatched-marker refusal, and link mapping. The asserted strings are the renderer's output for the test's own input, which is value identity rather than authored prose. W3 defers the question of whether this renderer should exist; while it does, these are its only tests. |
| F-087 | `website/tests/test_lander_404.py::SiteBaseTests.test_root_and_project_bases_pass@c775d5,SiteBaseTests.test_invalid_bases_are_rejected@b24a4e` | boundary guard | keep | `validate_site_base` rejects 28 malformed or hostile `--site-base` values. A CLI argument is operator-authored input, the canonical trust boundary. |
| F-088 | `website/tests/test_lander_404.py::BuildTests.test_root_and_project_builds_have_the_exact_output_set@c11ad5,L142` | **[unverified]** **[line-anchored: between functions]** second copy of a production constant | delete | **Recipe verification, 2026-08-16.** Same as F-050: the proposed replacement restates something `build.py` enforces on every run, so it cannot fail on its own. Delete. |
| F-089 | `website/tests/test_lander_404.py::BuildTests.test_root_and_project_builds_have_the_exact_output_set@c11ad5` | structural presence (duplicate) | keep | Both site bases render the `404` and Lander routes with every asset href rebased and no unsubstituted token. Overlaps test_site_build.py::BuildAndInstallTests.test_dual_base_builds_have_the_exact_complete_manifest (F-050/F-051) but runs against the real repository rather than a fixture copy; worth one row, and the duplication is flagged in the summary. |
| F-090 | `website/tests/test_lander_404.py::BuildTests.test_builder_replaces_only_an_owned_output_set@befb9b,BuildTests.test_builder_rejects_every_repository_output_before_writing@88988d` | boundary guard | keep | The builder refuses every output path inside the real repository and leaves the real source files and staging directories untouched. Unlike F-061 this runs against the working tree, which is the case that would actually destroy someone's work. |
| F-091 | `website/tests/test_lander_404.py::BuildTests.test_closed_template_vocabulary_and_required_references_fail_closed@f0f1d8` | **[unverified]** token-set pin + mutations | delete | The `404` template's token set restated, plus mutations proving `_validate_template` rejects an unknown token and a missing required reference. Same shape and same reasoning as F-025 and F-026. |
| F-092 | `website/tests/test_lander_404.py::StaticDocumentTests.test_no_javascript_document_is_a_useful_semantic_404@329009,StaticDocumentTests.test_preflight_controls_are_hidden_but_scene_and_breadcrumb_are_not@ffaf32` | structural presence | keep | Without JavaScript the `404` page is still a page with landmarks, one home link and a visible message, and the game's preflight controls start hidden while the scene does not. This is the no-JS contract the page exists to honour. |
| F-093 | `website/tests/test_lander_404.py::StaticDocumentTests.test_accessible_names_live_region_and_initial_focus_surface_are_pinned@02004f` | exact attribute pins + tag blacklist | delete | **Recipe verification, 2026-08-16.** Same shape as F-007: production enforces the attributes unconditionally, so the proposed assertion cannot bite. Delete. |
| F-094 | `website/tests/test_lander_404.py::StaticDocumentTests.test_scene_geometry_and_start_target_are_fixed_and_responsive@fca8c7,StaticDocumentTests.test_scene_geometry_and_start_target_are_fixed_and_responsive::assertNotRegex::9ff0a4,StaticDocumentTests.test_scene_geometry_and_start_target_are_fixed_and_responsive::assertNotRegex::e16855` | verbatim pin (CSS and SVG coordinates) | delete | Roughly forty exact pins: `viewBox`, CSS percentages, transform origins, terrain vertex coordinates, path prefixes, and an `M` count of `75`. Verified by execution: `site_game_validation.py::validate_game_contract` enforces the `0 0 1000 640` viewBox at every build (drifting it to `0 0 1000 641` fails `build_site`), and the scaffold/terrain paths are held by derivation parity in the `.mjs` suite (F-131, F-137). |
| F-095 | `website/tests/test_lander_404.py::StaticDocumentTests.test_css_has_only_bounded_keyframes_and_reduced_motion_preserves_live_plumes@5b4cea` | **[unverified]** verbatim pin (CSS declarations) | delete | Keyframe-name set, animation shorthands, and reduced-motion declarations pinned as strings. The reduced-motion behavior has observational twins that run in a real browser (test_lander_phase4j_browser.py::browser_arcade_contract, `test_lander_phase4l_browser.py`'s module-level fixture block, `test_lander_phase4q_browser.py`'s module-level fixture block). |
| F-096 | `website/tests/test_lander_404.py::StaticDocumentTests.test_engine_transforms_keep_both_nozzle_anchors_fixed@c20a53` | computed geometric invariant | keep | Executes each engine's CSS transform list against its declared origin and proves the nozzle anchor is fixed for every thrust angle and plume scale. This computes a property rather than pinning a value, and a wrong transform makes the plume visibly detach. |
| F-098 | `website/tests/test_lander_404.py::StaticDocumentTests.test_forbidden_runtime_surfaces_are_absent@a11dcf` | source blacklist + self-testing canaries | keep | **Aligned with F-018's keep, 2026-09-06.** F-018 was settled as a keep and this row carried its abandoned conditional. Same reasoning applies here and more strongly: this is the copy with the canary self-test, so its non-vacuity is established rather than owed, by injecting `window["fetch"]`, so if only one survives it is this one. |
| F-099 | `website/tests/test_lander_404.py::RocketAssetTests.test_root_is_self_contained_and_named@325937` | verbatim pin | delete | Pins the rocket SVG's `viewBox` and the absence of `width`/`height`. Authored asset geometry; scaling behavior is visible on the page. |
| F-100 | `website/tests/test_lander_404.py::RocketAssetTests.test_root_is_self_contained_and_named@325937,RocketAssetTests.test_root_is_self_contained_and_named::assertNotRegex::afa8f9` | self-containment guard | keep | The inlined rocket asset carries `role="img"` with a labelling pair and contains no `<script>`, `<image>`, `<animate>`, or remote URL. An SVG re-export from a design tool routinely adds exactly those, and the asset is inlined into every page. |
| F-101 | `website/tests/test_lander_404.py::RocketAssetTests.test_stable_global_ids_and_group_hierarchy_are_exact@cabaeb` | id-set pin | delete | **Recipe verification, 2026-08-16.** **The row's justification is factually wrong** in the same way as F-007: the SVG fragment contract it proposes to derive is already enforced unconditionally at build time. Delete. |
| F-102 | `website/tests/test_lander_404.py::RocketAssetTests.test_selected_geometry_and_temperature_layers_are_exact@5bedcf` | verbatim pin (SVG path data and fills) | delete | Nine full path strings and seven fill colors copied into the test. Authored artwork; every future logo tweak becomes a two-file ceremony. |
| F-103 | `website/tests/test_lander_404.py::FaviconAssetTests.test_favicon_is_a_self_contained_flame_free_mark@e02cc2,FaviconAssetTests.test_favicon_is_a_self_contained_flame_free_mark::assertNotRegex::54b0b7` | self-containment guard | keep | The favicon is a bare `viewBox` root with four known ids and no script, style, image, use, or remote reference. Same real risk as F-100, on the asset browsers fetch on every page. |
| F-104 | `website/tests/test_lander_404.py::FaviconAssetTests.test_favicon_is_a_self_contained_flame_free_mark@e02cc2,FaviconAssetTests.test_favicon_is_a_self_contained_flame_free_mark::assertNotRegex::c5bf42` | wording blacklist | delete | `assertNotRegex(source, r"plume\|engine\|#d94a1e\|#ff7a00\|#ffe09a")`: a five-token blacklist standing in for "the favicon has no flame". A sixth spelling passes; the id-set assertion above it is the structural form of the same fact. |
| F-105 | `website/tests/test_lander_404.py::FaviconAssetTests.test_favicon_geometry_matches_the_selected_mark_exactly@7fd615` | derived-copy parity | keep | Every shared mark element in the favicon has attributes identical to the rocket's. This is the sanctioned parity shape: two artifacts that must agree, checked by derivation rather than by a third copy. |
| F-106 | `website/tests/test_lander_404.py::StaticDocumentTests.test_fixed_color_contrast_meets_text_and_graphic_thresholds@a9370e` | three-decimal ratio pin | convert | `math.isclose(antenna, 3.788, abs_tol=0.001)` is the exact shape W4 converted in `test_site_documents.py`, surviving here because W4's landed change did not touch this file. Replacement: keep the two WCAG inequalities, drop the 3.788 pin, and read the colors out of the built `lander.css` (they are literals there) instead of restating them. No production change. See the overlap note in the summary. |
| F-107 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_scene_stage_contains_world_and_chrome_before_the_normal_flow_rail@ae04dd` | source-order pin + CSS declaration pins | delete | Pins the source order of twelve element ids in the fragment and six exact CSS declarations. Lexical order of our own markup plus stylesheet spelling; the layout consequences are measured by the phase 4J browser suite. |
| F-108 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_fuel_has_one_hidden_non_live_value_and_one_silent_gauge@5d6407` | tag blacklist + exact attribute dicts + JS source pins | delete | `assertNotIn("output"/"meter"/"progress")`, whole-dict equality on three fuel elements, and three assertions on `lander-game.js` source text (`this.lander_fuel_value.textContent` present, `.value` absent, `data-fuel-band` absent anywhere). Spelling pins and a retired-shape blacklist. |
| F-109 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_fuel_has_one_hidden_non_live_value_and_one_silent_gauge@5d6407` | structural presence | keep | Exactly one polite live region and one `role="status"` in the whole fragment. Two live regions is a real screen-reader defect that nothing else catches. |
| F-110 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_action_source_order_identities_and_static_focus_exclusion_are_exact@d6eba6` | source-order pin + exact attribute dicts + CSS pins | delete | Whole-dict equality on the restart and exit buttons and three CSS declarations including `min-inline-size: 44px`. The touch-target minimum has an observational twin that measures the real rect: test_lander_phase4j_browser.py::ArcadeBrowserTests.test_empty_gauge_pauses_and_reduced_motion_keeps_a_static_red_warning and test_lander_phase4l_browser.py::Phase4LBrowserTests.test_real_chromium_two_line_reflow_and_exact_retry_restore (both verified passing against a real Chromium in this worktree). |
| F-112 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_mutations_catch_duplicate_authorities_wrong_order_and_missing_static_agent_state@cd7281` | mutate-one-string-assert-raises | delete | Five fragment mutations proving `validate_game_contract` rejects wrong element types, a missing agent state, a moved button, and a duplicate live region. The `site_game_validation.py` counterpart runs on every build; the same in-tree-tampering reasoning as F-026. |
| F-113 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_exact_action_rail_shape_rejects_anonymous_wrappers_and_interposed_children@098f46` | mutate-one-string-assert-raises | delete | Eleven mutations wrapping or interposing elements in the action rail. Pins the fragment's exact child structure through the validator. |
| F-114 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_control_line_sources_reject_empty_duplicate_and_moved_structure@c29068` | mutate-one-string-assert-raises | delete | Three control-line mutations (emptied, duplicated, moved). Same shape. |
| F-115 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_reviewed_root_and_required_tag_identities_reject_stray_structure@43a330` | mutate-one-string-assert-raises | delete | Nine mutations changing root framing and element tag names. Same shape. |
| F-116 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_gauge_has_independent_graphite_boundary_level_color_and_height@b92a24` | verbatim pin (CSS declarations) | delete | Seven exact gauge declarations including `height: min(7rem, calc(100cqh - 1rem))`. Stylesheet spelling. |
| F-117 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_gauge_has_independent_graphite_boundary_level_color_and_height@b92a24` | threshold | convert | **Recipe verification, 2026-08-16.** Verified, with a trap to record: the token regex also matches `--fuel-level-color: var(...)`, so without an explicit non-vacuity assertion the loop can iterate over nothing and assert nothing. Add one. |
| F-118 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_visually_hidden_fuel_and_direction_use_clipping_not_removal@971175,ArcadeCssTests.test_visually_hidden_fuel_and_direction_use_clipping_not_removal::assertNotRegex::3f9115` | verbatim pin (CSS declarations) + property blacklist | delete | Six exact declarations of the `.visually-hidden` rule plus a three-property regex blacklist. The behavior (the hidden fuel text stays measurable and readable to assistive tech) is measured for real at `test_lander_phase4j_browser.py`'s module-level fixture block, which asserts the 1px rect, the clip, and that display and visibility are not removed. |
| F-119 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_pseudo_can_is_the_exact_six_layer_graphite_orange_silhouette@8c159d,ArcadeCssTests.test_pseudo_can_is_the_exact_six_layer_graphite_orange_silhouette::assertNotRegex::51c557` | verbatim pin (six-layer gradient list) | delete | Pins six `linear-gradient` layers with exact colors and positions, plus a four-property blacklist. Pixel art expressed as CSS; the browser test `test_lander_phase4j_browser.py::ArcadeBrowserTests.test_computed_pseudo_can_font_touch_scope_and_requests_match_the_contract` renders it and counts changed pixels (`self.assertGreater(result["canDifference"]["changed"], 100)`), which is the property that matters. |
| F-120 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_arcade_font_banner_rail_and_touch_scope_have_no_external_surface@3707e7,ArcadeCssTests.test_arcade_font_banner_rail_and_touch_scope_have_no_external_surface::assertNotRegex::386c80` | verbatim pin (font stack, colors, selectors) | delete | Pins the font stack, two banner data attributes, two inset shadows, and the single `touch-action` selector. The no-external-resource half (`@font-face\|@import\|url(`) duplicates the build-time guard verified at F-071, which fails the build rather than a test. |
| F-121 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_installed_and_absent_paints_are_exact_and_add_no_css_generated_text@89c275` | verbatim pin (CSS declarations) | delete | Seven exact paint declarations for the two agent states, plus a `content: ""` count. Stylesheet spelling. |
| F-123 | `website/tests/test_lander_phase4j_browser.py::ArcadeBrowserTests.test_computed_pseudo_can_font_touch_scope_and_requests_match_the_contract@3ff193` | computed-style verbatim pin | delete | The pure-value CSS reads: `backgroundSize`, `backgroundPosition` and `backgroundRepeat` as six-item literal lists, `assertIn("Cascadia Mono", result["fontFamily"])`, and the `animationDuration` entry of the `emptyGauge` dict. These are the authored stylesheet's values read back out of the browser. What survives is F-170. **Two corrections from execution, 2026-09-06.** The recipe is not line-applicable: `animationDuration` is one key of the whole-dict `assertEqual` over `emptyGauge` in that same test, so the executor splits that dict into per-key assertions before removing anything. And `animationTimingFunction` is NOT a pure value and stays: changing `steps(1)` puts intermediate opacities on screen, which F-170's pixel assertion then fails, so it is load-bearing rather than decorative. |
| F-124 | `website/tests/test_lander_phase4j_browser.py` | browser-observed behavior | keep | Seven tests driving a real Chromium over the built site: native input and focus-departure contracts, accessibility description refresh, focus and tab order, departure authorities, live-region updates, computed touch-target rects, hidden-fuel clipping, and reduced-motion behavior. These are the observational twins the CSS-text pins in F-123 are deleted in favour of. |
| F-125 | `website/tests/test_lander_phase4k_browser_cleanup.py::Phase4KBrowserCleanupTests.test_null_root_navigation_race_times_out_without_evaluation_or_cleanup_failure@81022f` | **[unverified]** verbatim pin (our own expression text) | delete | Three `assertIn` calls asserting substrings of the readiness expression the harness itself builds, two lines after the collected expressions are compared against `phase4k_browser._readiness_expression(loaded_url)` exactly. That comparison holds the invariant and stays; these three restate its contents. |
| F-126 | `website/tests/test_lander_phase4k_browser_cleanup.py::Phase4KBrowserCleanupTests.test_probe_uses_browser_events_instead_of_controller_input_shortcuts@defbaf` | source blacklist | delete | Asserts the probe source does not mention three controller methods, that is, that the probe drives the page through real events. A three-name blacklist over our own harness source; a fourth spelling passes, and the probe's realism is a review concern. |
| F-127 | `website/tests/test_lander_phase4k_browser_cleanup.py` | resource-lifecycle behavior | keep | The phase 4K harness owns and tears down its browser, profile, server and thread, bounds its DevTools handshake and readiness deadlines, restores its artifacts on allocation and spawn failure, and surfaces the primary error. Same invariant as F-024, for the other harness. |
| F-128 | `website/tests/test_lander_phase4l_browser.py` | browser-observed behavior and harness plumbing | keep | Examined in full for the target shapes and none found beyond F-122/F-123/F-125/F-126: these files assert measured geometry, computed styles compared against each other, input outcomes, and DevTools plumbing. They are the observational twins that make the CSS and source pins deletable. |
| RB-012 | `website/tests/onboarding.test.mjs` | non-example | keep | Added by the 2026-08-19 re-baseline as F-129's successor: `onboarding-copy.test.mjs` was replaced by this file at `1b9bbb9b` (2026-08-17). It keeps F-129's property (status text asserted as non-empty and mutually distinct, never word by word) and adds ARIA structure assertions that are structural rather than prose. No rows, and still the pattern worth citing for a sentence-only observable. |
| F-130 | `website/tests/lander-model.test.mjs::L712-728` | **[line-anchored: not Python]** derivation parity | keep | The static scaffold path in the template equals the path the controller renders from the model, member for member. This is the real guarantee behind every scaffold coordinate pinned elsewhere, and it is the reason those pins are deletable. |
| F-131 | `website/tests/lander-phase4i.test.mjs::L272-306` | **[line-anchored: not Python]** derivation parity | keep | The no-script template's scaffold, battery, and antenna geometry equals what the model computes for the same site, and the colliders contain the members. This is the cross-artifact agreement the static SVG exists to satisfy; nothing else holds it. |
| F-132 | `website/tests/lander-phase4i.test.mjs::L307-314` | **[line-anchored: not Python]** verbatim pin (CSS declarations) | delete | Six regexes pinning exact antenna rules with colors (`.antenna-signal-1 { stroke: #d94a1e; opacity: 1; }`). Stylesheet spelling; the staged-signal behavior is exercised in the browser suites. |
| F-134 | `website/tests/lander-phase4l.test.mjs::L70-78` | **[line-anchored: not Python]** structural presence | keep | The controls element has exactly two leaf lines with non-empty text and stable class names, asserted through the real controller. Structural, and it does not touch the copy itself. |
| F-135 | `website/tests/lander-phase4l.test.mjs::L79-87` | **[line-anchored: not Python]** verbatim pin (CSS declarations) | delete | Two regexes pinning `display: block; white-space: nowrap` and a media-query grid declaration. Stylesheet spelling; test_lander_phase4l_browser.py measures the rendered result. |
| F-137 | `website/tests/lander-phase4m.test.mjs::L240-246` | **[line-anchored: not Python]** derivation parity | keep | The template's static sky paths equal the projection the world module computes for the static seed, and each layer appears once. Same family as F-130 and F-131. |
| F-138 | `website/tests/lander-phase4m.test.mjs::L247-255` | **[line-anchored: not Python]** structural presence (third copy) | delete | Asserts each shell template's footer Lander link has a non-empty `aria-label` equal to its `title`. The third of three copies of one fact; test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths (F-011) holds it on the built output, which is where it ships. |
| F-139 | `website/tests/lander-phase4s.test.mjs::L28-36` | **[line-anchored: not Python]** module-graph structure | keep | The collision module imports nothing and the world module imports it exactly once. A real layering invariant that an ordinary refactor can break and that the type system cannot express. |
| F-142 | `website/tests/lander-world.test.mjs::L202-209` | **[line-anchored: not Python]** derived path shape | keep | The terrain surface is one open path with no curve or close commands, derived from the vertex list. A shape property computed from the derivation, not a coordinate pin. |
| F-143 | `website/tests/lander-world.test.mjs::L210-211` | **[line-anchored: not Python]** verbatim pin (template attributes) | delete | Pins `stroke-linejoin="miter"` and `stroke-miterlimit="2"` on the terrain layer and the layer's path count. Authored markup attributes. |
| F-144 | `website/tests/lander-model.test.mjs` | state-machine and physics behavior | keep | Examined in full; outside the target family. These assert simulation outcomes, collision classification, retention and camera bounds, input mixing, determinism, and controller state transitions against computed expectations and the suite's own fixtures. Nothing here pins authored copy. |
| F-145 | `website/tests/site_test_support.py::L54-57,L58-75` | **[line-anchored: module level]** second copies of production constants | delete | **Recipe verification, 2026-08-16.** The `EXPECTED_FILES` half retires under F-050, which is now itself a delete for the reason recorded there. The `CSP` half is covered by F-002's verified directive-level conversion, and the constant survives this sweep anyway because its third consumer (F-046) sits in the deferred block. Nothing here needs a conversion. |
| L-201 | `cli/tests/test_skel.py::test_seeds_open_with_managed_by_agentworks_marker@263588` | header-marker pin | delete | Pins an authored comment line in a shell seed. |
| L-202 | `cli/tests/test_skel.py::test_seeds_reference_agentworks_rc_filename@b5dbcf` | `AGENTWORKS_RC in BASHRC/ZSHRC` | convert | **Recipe verification, 2026-08-16.** Verified by seeding a temp HOME with the real seeds, driving `_write_agentworks_rc` twice through a transport that actually executes the emitted grep-or-append command via `sh -c`, and asserting the source-line count is unchanged, plus a non-vacuity assertion that the seeds carry a source line to begin with. Renaming `AGENTWORKS_RC` in `vms/initializer/shell_env.py`'s `AGENTWORKS_RC` makes it fail. **Record the trap**: the first attempt counted lines containing the literal current filename and PASSED while the invariant was broken, because the appended duplicate carries the NEW name. The counter must be filename-agnostic. Design cost: this is the only row in its group that runs a real local shell and writes into a tmp HOME. |
| L-203 | `cli/tests/test_skel.py::test_seeds_use_guarded_source_line@c5570f` | exact guarded source-line pin | delete | Pins authored shell text. The property (a shell opening before init does not error) is only observable by executing a shell, which R2.4 does not ask us to build. |
| L-204 | `cli/tests/test_skel.py::test_seeds_banner_has_agw_prefix_and_mode_label@81e09e` | banner prefix and mode labels | delete | Authored prompt text. |
| L-205 | `cli/tests/test_skel.py::test_seeds_banner_uses_labeled_status_fields@e45ede` | labeled status fields (`vm:`, `ws:`, `ag:`, `se:`, each preceded by a space) | delete | Authored prompt text. |
| L-206 | `cli/tests/test_skel.py::test_seeds_banner_field_order_is_vm_ws_ag_se@8f1c42` | `find()`-based field-ORDER pin over the seed text | delete | A lexical order pin over an authored artifact, the doctrine's explicit delete. |
| L-207 | `cli/tests/test_skel.py::test_seeds_mode_coded_colors@fe47b9` | ANSI color-code pins | delete | Authored presentation detail. |
| L-208 | `cli/tests/test_skel.py::test_seeds_vm_hostname_fallback@26ebfd` | hostname-fallback expansion pins | delete | Authored shell text. |
| L-209 | `cli/tests/test_skel.py::test_seeds_status_fields_render_conditionally@15d245` | conditional-render pins | delete | Authored shell text. |
| L-210 | `cli/tests/test_skel.py::test_seeds_have_git_branch_support@28d134` | git-branch support pins | delete | Authored shell text, including a hardcoded Debian path. |
| L-211 | `cli/tests/test_skel.py::test_seeds_two_line_prompt_with_banner_above@43b083` | two-line prompt layout pin | delete | Authored presentation detail. |
| L-212 | `cli/tests/test_skel.py::test_seeds_use_prompt_command_or_precmd_hooks@2e3f1f` | `PROMPT_COMMAND` / `precmd` hook pins | delete | Authored shell text. |
| L-213 | `cli/tests/test_skel.py::test_bash_seed_keeps_debian_baselines@311b73` | Debian baseline pins in the bash seed | delete | Authored shell text. |
| L-214 | `cli/tests/test_skel.py::test_zsh_seed_includes_compinit@7e7a01` | `compinit` pin in the zsh seed | delete | Authored shell text. |
| L-215 | `cli/tests/test_skel.py::test_write_skel_seeds_writes_both_files_to_etc_skel@298545,test_write_skel_seeds_skel_paths_are_etc_skel@437a92` | `_write_skel_seeds` writes both files, via `sudo tee`, `chmod 644` | keep | Behavioral: the writer's effects are what make `useradd -m` inherit the seed. Asserted on recorded calls, not on text. |
| L-216 | `cli/tests/test_skel.py::test_write_skel_seeds_runs_after_apt_install_in_initializer@f8fcc0` | seed write ordering relative to apt install in the initializer | keep | Behavioral ordering observed through the recorded call sequence, which is the observational form the doctrine prefers over a lexical pin. |
| F-146 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_zsh_function_mappings_match_declared_functions@7a9857` | declared-name parity between a map and the function body it names | keep | `DYNAMIC_FUNCTIONS.keys() == COMPLETER_FUNC_NAMES.keys()`, plus a regex that reads each zsh function body and checks the name it declares equals the name the map advertises. Derivation parity, not a spelling pin: when the two disagree the generated script calls a function that was never defined, and zsh reports nothing. |
| F-147 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_uses_ordinary_subcommands_and_one_dynamic_show_argument@8cad18` | group-versus-subcommand option placement | keep | The first block of this test: `set(guide.subcommands) == {"list", "show"}`, `not listed.params`, the `topic` param's name, requiredness, non-multiplicity and completer, `group_options == ["--agent", "--human"]`, and `not show_options`. It states the grammar the rest of the test and its three executed shell twins depend on, that the display flags live on the group and `show` carries exactly one dynamic argument. A flag migrating from the group onto `show` silently changes what every shell offers. |
| F-148 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_uses_ordinary_subcommands_and_one_dynamic_show_argument@8cad18` | generated-text assertions computed from the spec, plus the invocation contract | keep | The assertions that quantify over `group_options` (every group option present in the zsh group block, absent from the zsh show block, and emitted as a powershell `CompletionResult`), the zsh positional binding `1:topic:_agentworks_guide_topics`, and `"agw guide list"` in the bash and powershell scripts. The first three are parity against values the test read from the spec rather than literals it carries, and the last is C-021's invocation contract at the generated-script level. |
| F-149a | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_uses_ordinary_subcommands_and_one_dynamic_show_argument@8cad18` | hardcoded bash-template strings | delete | The literal pins on emitted bash: the candidate list `compgen -W "list show --agent --human --help"`, `--agent\|--human) continue ;;`, `case "$group_command" in` and `word_index=command_index + 1`. Each pins how the generator spells one line. Three tests run the real shells and assert the resulting candidate lists, which is the observational twin `hla.md` prefers, and the bash twin runs wherever bash does. The generator is pure Python and does not vary by host. |
| F-149b | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_uses_ordinary_subcommands_and_one_dynamic_show_argument@8cad18` | hardcoded zsh and powershell template strings | keep | The same shape as F-149a over the zsh and powershell blocks: the presence of `list` and `show`, `$groupFlagOptions = @('--agent', '--human')`, `switch ($groupCommand)` and `$index = $groupCommandIndex + 1`. The delete rests on the real-shell twins covering the grammar, and those twins are host-conditional. **CI check, 2026-09-06, and it does not settle the question.** Run `34050713243` on `main` (`eebb6f91`, CI success) reports `8456 passed, 6 skipped` from each Linux Pytest job and `8321 passed, 70 skipped` from the Windows one. The suite runs without `-ra` or `-rs` (`cli/pyproject.toml` sets `addopts = ["-n", "auto", "--dist", "loadgroup"]`), so the log gives counts and never says which tests skipped. **Verified 2026-09-06 as far as a host can verify it:** on a Linux host with zsh installed the zsh twin runs and only the powershell twin skips, so the zsh half's twin does execute somewhere. What remains unknown is whether CI's hosts have zsh, which the log cannot say. So this half keeps, on the sweep's own doctrine rather than on evidence: coverage nobody has shown to run cannot carry a deletion. The cheap way to settle it is `-ra` on the Pytest step, which is a CI change and not the sweep's to make; it is in the open questions. |
| F-150 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_secret_verify_variadic_completion_contract_in_every_shell@5863f1` | vacuous assertion | delete | `assert all(zsh_position.group(1) == "*" for _position in (1, 2, 6))` re-tests one already-asserted value three times: the loop variable is unused and the tuple reaches nothing. It cannot fail unless the assertion two lines above it already failed. Delete the line; the `zsh_position.group(1) == "*"` check above it stays. |
| F-151 | `cli/tests/test_completions.py::TestOptionFlagsInSpec.test_console_add_sessions_placement_reaches_the_completion_spec@c8a9e9,TestOptionFlagsInSpec.test_secret_preview_opt_in_reaches_describe_and_verify_completions@ec3523,TestOptionFlagsInSpec.test_instance_spec_options_reach_every_shell_completion@03e8a5,TestOptionFlagsInSpec.test_runnable_status_option_reaches_every_shell_completion@2c8e4b` | per-flag spec restatement, four more tests | delete | The same shape as C-032 and C-033 for `--to-index`, `--allow-interaction`, `--spec`/`--workspace-spec`/`--agent-spec`, and `--status`: assert the flag is among the command's options, and in three of the four also assert the flag's text appears somewhere in each generated script. Typer introspection puts a declared flag in the spec by construction, and whole-script substring presence adds nothing (C-025). Delete all four tests. **Cost, measured 2026-09-06:** after the delete the flag can leave the completion spec entirely with 111 tests still green, so nothing asserts `--to-index` is in the spec at all. It is an ordinary option with help text (`cli/commands/console.py::console_add_sessions`), not a hidden one, so the loss is completion coverage rather than a flag going quietly missing. |
| F-152 | `cli/tests/test_completions.py::TestCompleteness.test_zsh_contains_all_commands@e3b0c4,TestCompleteness.test_powershell_contains_all_commands@e3b0c4` | derived presence sweep over a generated script | keep | `_assert_all_commands_present` walks the live spec and asserts every command and subcommand name appears in the generated script, so the expectation comes from the canonical source rather than a hand list, and a generator that dropped a whole subcommand fails here. Weak by construction (substring presence anywhere) and bash is not covered, but nothing else in the file quantifies over the whole tree. |
| F-153 | `cli/tests/test_completions.py::TestCompletionCli.test_show_with_explicit_shell_prints_script@5b65ee` | routing probe through a shell-required directive | keep | `"#compdef" in result.stdout` with `result.exit_code == 0` is how this test proves `completion show --shell zsh` emitted the zsh script rather than another shell's. The token is zsh's own requirement, not our prose, and it is the only discriminator available on stdout. |
| F-154 | `cli/tests/test_completions.py::TestStaticChoiceCompletion.test_sample_kind_completes_dynamically@d30210,TestStaticChoiceCompletion.test_schema_kind_completes_dynamically@f10e0f` | absence of a closed choice on a kind argument | keep | `not kind.choices` with `kind.dynamic_completer == "resource_kinds"` on `resource sample` and `resource schema`. The invariant is that a kind the operator types, including a capability kind or a typo, reaches the service layer for a domain error instead of failing click parsing (issue #276). Adding a `click.Choice` here is the plausible well-meaning edit that breaks it, and this is the only probe. The trailing option-list equality in the schema test is C-047, not this row. |
| F-155 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths@79b75a` | script inventory and hero-mark identity | keep | `scripts == [{"type": "module", "src": "/static/onboarding.js"}]` is a closed inventory: the home page ships exactly one local module and no third-party script, which is the privacy posture the whole page is built around. Beside it, the repository link count and the hero mark's `src` and `alt` are structural facts about the one decorative image on the page. |
| F-156 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_chromium_geometry_owns_the_devtools_process_and_cleanup@336fa8` | profile isolation | keep | The spawned browser's `HOME` must equal the throwaway profile directory passed as `--user-data-dir`, asserted by reading both back off the recorded spawn call rather than by restating either. A harness that let Chromium read the operator's real home would write into it, and this is the only assertion that says it does not. |
| F-157 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_palette_contrast_meets_text_and_non_text_thresholds@e67089` | WCAG threshold over derived tokens | keep | The palette is read out of the BUILT stylesheet and every shipped foreground is asserted to clear WCAG's contrast ratio against the surface it is painted on: 4.5 for text, 3.0 for graphics. A threshold, not a value: adjusting a color is free until it crosses the line. `hla.md` keeps thresholds by name. |
| F-158 | `cli/tests/test_secret_describe.py::test_json_v1_static_resolution_categories_remain_frozen@403e56` | wire-contract enum values | keep | `tuple(category.value for category in StaticResolutionCategory) == ("attemptable", "refused-interaction", "unavailable")`. This restates an enum, but the enum's VALUES are the JSON v1 wire contract that machine consumers branch on, so the restatement is the contract rather than a copy of it. Same reasoning as C-034's closed `--output` choices. |
| F-159 | `cli/tests/test_secret_describe.py::test_secret_description_rejects_unsafe_name_independently_of_preview@153f9a` | rendering-safety refusal at an operator boundary | keep | Constructing a `SecretDescription` whose name carries an embedded newline must raise, and it must do so independently of the preview being well formed. Secret names arrive from operator manifests and are rendered into a line-oriented report, so a newline forges output lines. Not an interior re-validation: the value crosses the operator-authored-input boundary. |
| F-160 | `cli/tests/test_secret_describe.py::test_default_impact_goes_as_far_as_possible_without_prompting@e6ea0e` | consent confinement | keep | `monkeypatch.setattr("agentworks.output.prompt_secret", lambda *a, **k: pytest.fail("zero-impact describe prompted"))` makes any prompt at `OperatorImpact.NONE` a failure, while the assertions below prove the preview still walked both sources. A describe that prompted would take an action the operator did not ask for, and this is the only test that forbids it. |
| F-161 | `cli/tests/test_secret_describe.py::test_allow_impact_gets_definitive_preview_without_returning_the_value@8bcf4a` | redaction defense | keep | With prompting allowed and a `sentinel-value` returned, the preview reports AVAILABLE while the sentinel appears in neither `repr(description)` nor `repr(secret_description_data(description))`. An injected sentinel proving a value never escapes into a describe surface. |
| F-162 | `cli/tests/test_secret_describe.py::test_json_v1_reports_prompt_input_requirement_as_the_preview_reason@70dea3,test_json_v1_static_fields_remain_and_provider_preview_is_nested@10c74d` | JSON v1 schema shape | keep | The nesting (`secret.resolution.preview`), the required key set on `resolution`, `would_attempt` on a source mapping, and the reason values read through `IndeterminateReason` rather than spelled. Schema shape over a machine-output contract, with the expected values derived from production enums. |
| F-163 | `cli/tests/test_codex_integration.py::test_launch_intents_report_distinct_decisions@0bae81` | distinctness over three notes | keep | `None not in {missing.note, created.note, forced.note}` and `len({...}) == 3`. This asserts that the three launch decisions report differently without pinning what any of them says, which is exactly the shape B-069 and B-070 convert toward and the shape that replaced the deleted `test_launch_note_*` family. Rowed so an executor sweeping note assertions does not take it too. |
| F-164 | `cli/tests/test_codex_integration.py::test_fresh_developer_instructions_are_toml_quoted_and_not_prompt_mediated@f72948,test_config_string_fields_escape_toml_structural_characters@e7c3a2,test_session_name_needing_quotes_stays_one_recorder_path_word@aaf0b4,test_extra_args_with_shell_metacharacters_cannot_inject@36c1b2` | quoting and escaping defenses | keep | Each takes a string the test injected (embedded quotes and newlines, a session name needing shell quoting, a metacharacter payload) and asserts the emitted command quotes it so it stays one token and never becomes a command word. The literals asserted are the test's own input transformed by the code under test, which is value identity, and the invariant is injection resistance at a shell and TOML boundary. |
| F-165 | `cli/tests/test_codex_integration.py::test_fresh_string_is_a_single_sh_c_that_provisions_then_execs@814733,test_resume_string_execs_codex_resume_with_the_id_and_the_cwd_pin@538f10,test_picker_string_execs_bare_codex_resume_with_the_cwd_pin@e4a575,test_rollout_probe_keeps_find_failure_distinct_from_a_clean_no_match@303604,test_rollout_probe_is_rooted_at_codex_home_and_finds_by_the_bound_id@9cb35f` | generated shell-command shape | keep | Substrings of the shell string this integration hands to a VM: the `sh -c` wrapper, the provisioning `mkdir`/`chmod`/`rm -f` steps, `exec codex resume {id} -c tui.resume_cwd=current`, and the probe's `[ -d` directory test, `exit 5` and `CODEX_HOME` rooting. The command IS the behavior and nothing in the suite runs it against a real VM, so this is the completions generators' case (C-027, C-050): generator behavior expressed in a target language, with no observational twin available. |
| F-166 | `cli/tests/test_codex_integration.py::test_generated_pieces_carry_no_template_var_tokens@c24c96` | unexpanded-placeholder guard | keep | `"{{" not in command.command` across every generated launch form. This is not a wording blacklist: `{{` is the template marker, and one that survives into the emitted string means an unexpanded placeholder is about to be handed to a shell. Two characters, one failure mode, no wording. |
| F-167 | `cli/tests/test_claude_code_integration.py::test_launch_intents_report_distinct_decisions@0bae81` | distinctness over three notes | keep | The same assertion and the same reasoning as F-163, in this file. |
| F-168 | `cli/tests/test_claude_code_integration.py::test_extra_args_with_shell_metacharacters_cannot_inject@0c2f5a` | injection defense | keep | A metacharacter payload passed through `extra_args` must survive the `sh -c` nesting as one inner token and must not become a standalone command word. The payload is the test's own, and the invariant is quoting. |
| F-169 | `cli/tests/test_claude_code_integration.py::test_returned_string_is_a_single_sh_c_that_echoes_then_execs@c27a01,test_probe_is_slug_independent_and_finds_by_stored_id@9a6c67,test_probe_keeps_find_failure_distinct_from_a_clean_no_match@5632b9` | generated shell-command shape | keep | The `sh -c` wrapper, `exec claude`, and the probe's `[ -d` directory test, `exit 6`, `find`, `CLAUDE_CONFIG_DIR` and `{id}.jsonl` rooting. Same reasoning as F-165: the emitted command is the behavior and nothing runs it against a real VM. |
| F-170 | `website/tests/test_lander_phase4j_browser.py::ArcadeBrowserTests.test_computed_pseudo_can_font_touch_scope_and_requests_match_the_contract@3ff193` | computed style with a behavioral consequence | keep | What survives F-123, all in the same test: the gradient COUNT rather than its geometry (`len(backgroundImage.split("linear-gradient")) - 1 == 6`), the empty-gauge animation name and running play state (whose reduced-motion twin is the sibling test), `animationTimingFunction`, which F-123 keeps because changing `steps(1)` puts intermediate opacities into this row's own pixel assertion, the rendered pixel difference (`colors == [graphite, orange]` with more than 100 pixels changed), the touch-action map, the hidden-fuel clip rect that keeps the value screen-reader-available while visually clipped, and the resource check that no font is fetched and every resource is local. `animationDuration` is not among them: F-123 deletes it as a pure value. Each survivor fails when something the page DOES changes, not when a value is re-tuned. |
| F-171 | `website/tests/test_lander_phase4j_browser.py::ArcadeBrowserTests.test_phase4k_accessibility_rejects_nonnumeric_fuel_mutations@70cd12` | non-vacuity guard over the test's own probe | keep | Mutates the TEST's probe source so the fuel value becomes non-numeric, and asserts `assert_phase4k_accessibility_contract` then raises. It proves the contract helper can actually fail, which is the only thing standing between that helper and silent vacuity, and it mutates a test-owned artifact rather than production (which is what separates it from F-122). The two anchor strings it splices are guarded by `assertNotEqual(mutation, probe)`, so a reworded probe fails loudly rather than silently doing nothing. |
| F-172 | `cli/tests/manifests/test_samples.py::test_samples_are_fully_commented@2f901d,test_commented_samples_are_inert_through_the_loader@fbe281,test_uncommented_samples_load_through_the_real_loader@61f7a2,test_the_whole_set_uncomments_as_one_file@61f7a2` | renderer inertness and uncomment parity | keep | Every non-blank rendered line starts with `#`, a written sample declares nothing through the real loader, and stripping one `#` per line yields documents the real loader accepts, for each kind and for the whole `--all` set as one multi-document file. The inertness half is a safety property (a sample an operator drops in must not declare anything), and the parity half is what makes "only required fields are live lines" a property rather than a preference. These are the invariants D-045's justification names as surviving its deletion. |
| F-173 | `cli/tests/manifests/test_samples.py::test_a_third_append_separates_from_the_second@6a8ed1,test_writing_into_a_file_that_exists_and_is_blank_emits_no_separator@696fea` | separator arity, derived from production constants | keep | `count(f"\n{_SEPARATOR}\n") == 2` after three appends, and `_SEPARATOR not in text` with `MODELINE_PREFIX not in text` for the fill-a-blank-file outcome. Both read the production constants rather than spelling them, so these are arity and absence over a derived value. Rowed explicitly because D-046 deletes a literal byte pin two tests away and an executor should not sweep these with it. |
| F-174 | `cli/tests/manifests/test_samples.py::test_sample_capability_kind_is_a_clean_cli_error@669c62` | stdlib-token absence guarding a real branch | keep | `"Traceback" not in err` and `"StopIteration" not in err`, beside exit code 1 and the untouched `error.log`. `Traceback` is Python's own word, not ours, and its absence is what separates a clean domain refusal from an unexpected failure escaping the top-level handler, which is the regression the test is named for. The authored sentence in the same test is D-047 and goes. |
| F-175 | `cli/tests/test_resource_list.py::test_format_origin_line_renders_each_variant@f8ef60` | rendered-prefix pin over a closed variant set | convert | `s.startswith(("operator-declared", "auto-declared", "built-in"))` spells the three origin variants a third time; they are already asserted structurally as `row.origin.variant` three times in the same file. Replacement: assert each rendered line starts with its own row's `origin.variant`, which is parity against the value the renderer was given, and keep the `any(...operator-declared...)` non-vacuity check as `any(row.origin.variant == "operator-declared" for row in listing.rows)`. No production change. |
| F-176 | `cli/tests/test_resource_list.py::test_missing_ssh_keys_do_not_block_resource_list@59cdb9` | table-header smoke pin | delete | `"KIND" in result.output and "NAME" in result.output`. The test is named for not being blocked, and `result.exit_code == 0` is that fact; the header check only says a table of some sort printed. C-086 keeps the one place where the table's shape is actually asserted. |
| F-177 | `cli/tests/test_resource_kinds.py::test_capability_kinds_are_exactly_the_code_backed_ones@19ed58` | hand-maintained copy of a registry partition | delete | A four-name set literal compared against the registry's capability category. It can only fail when someone adds or removes a capability kind and does not paste the change here, and the property that can actually break, that the two categories cover the registry exactly, is asserted from the registry in `test_samples.py::test_every_registered_kind_is_declarable_or_a_capability`. |
| F-178 | `cli/tests/test_resource_kinds.py::test_names_only_needs_no_config@5c7ac6` | redundant membership checks | delete | `"harness-integration" in lines`, `"secret-source" in lines` and `"harness" not in lines` sit one line below `lines == sorted(KIND_REGISTRY)`, which already fixes the output exactly. Given that equality the three cannot fail independently, and the last is a retired-spelling blacklist. The equality and the exit code stay. |
| F-179 | `cli/tests/test_resource_kinds.py::test_missing_ssh_keys_do_not_block_kind_listing@cb30c7` | table-header smoke pin | convert | **Verified by mutation, 2026-09-06, and the disposition changed.** `"KIND" in result.output` does pin the header's spelling, but `result.exit_code == 0` does not replace it: a regression that exits zero and lists nothing passes this test once the assertion is deleted. Assert the listing's content instead, that the kinds appear or that the output is not empty, which is the observational form `hla.md` prefers and keeps the silent-empty regression caught. No production change. |
| F-180 | `cli/tests/manifests/test_schema_command.py::test_write_lands_where_the_modeline_says@635ed7,test_write_tolerates_missing_ssh_keys@30feba` | install-destination parity | keep | The written file set must equal the envelope schema filename together with one `<kind>.schema.json` per declarable kind, derived from production rather than listed, and the confirmation must name the directory through `format_host_path`, which is the shared production renderer rather than a hand-built string. A partial set leaves some manifest's modeline pointing at a file that is not there, and a hand-rolled path renders wrong on Windows. |
| F-181 | `cli/tests/test_secrets_base.py::test_secret_config_default_chain@3ded6e` | production-constant restatement | delete | `DEFAULT_SOURCE_CHAIN == ("env-var", "prompt")` is a second copy of the constant imported two lines above it, so it can only fail when someone edits the thing it restates, which is the shape the amended principle 3 calls cost rather than coverage. The line above it, `SecretConfig().sources == DEFAULT_SOURCE_CHAIN`, is derivation parity and stays: it is what proves a `SecretConfig` that declares no sources picks up the default chain. |
| F-182 | `cli/tests/test_sample_config_tailscale.py::test_sample_config_builds_registry@ecf6f7` | shipped sample driven through the production loader | keep | The shipped `sample-config.toml` is run through `load_config` and `build_registry` rather than having its text asserted, and what is checked afterwards is registry-computed provenance (`origin.variant == "auto-declared"`, `origin.source == ("vm-template", "default")`). Same shape as C-001. It catches a sample that stops parsing and an auto-declare path that stops firing, neither of which any other test covers for the shipped file. |
| F-183 | `cli/tests/manifests/test_yaml_value.py::test_a_value_renders_as_the_yaml_a_document_carries@6413bc,test_a_value_pyyaml_cannot_represent_renders_rather_than_raising@a56913,test_a_set_renders_in_a_stable_order@4c9afc` | rendered-YAML text equality | keep | Each rendered string is asserted together with `yaml.safe_load(rendered) == loaded`, so the pair is a round trip through the real loader rather than a pin on our spelling, and the module docstring says why both halves are needed. The one-line flow form (`[zsh, ripgrep]`, `{K: v}`) is load-bearing rather than cosmetic: these values are pasted inline into a generated sample line, so block style would break the document. The stable-order test is determinism, which every sample-pinning test depends on. |
| F-184a | `cli/tests/resources/test_kind_registry.py::test_named_console_template_kind_attributes@92740c` | `named-console-template.miss_policy` | keep | **Verified by mutation, 2026-09-06.** Breaking `named-console-template`'s `miss_policy` in `KIND_REGISTRY` fails this assertion and nothing else in the suite, so this copy of the per-kind attribute shape is the only place that policy is stated. It is the one member of the family where the keep reasoning survived contact with the suite. The rest of this test's assertions are F-184b. |
| F-184b | `cli/tests/resources/test_kind_registry.py::test_phase_1a_kinds_registered@c52666,test_secret_kind_attributes@56854b,test_admin_template_kind_attributes@92740c,test_named_console_template_kind_attributes@92740c` | every other registry attribute in the same test | delete | **Verified by mutation, 2026-09-06.** Every attribute here except the one F-184a keeps has observational twins that fail when it is broken: `secret`, `admin-template` and `named-console-template` each have twins on the attributes this row deletes. The twins assert what the policy does rather than what the table says, which is the form `hla.md` prefers, so these assertions restate the table without guarding it. |
| F-185 | `cli/tests/test_workflow_policy.py::test_only_the_deployment_job_holds_write_permissions@73cc01,test_the_deployment_job_runs_only_the_deploy_action@0a40ab,test_the_deployment_job_waits_for_the_build_and_uses_the_pages_environment@5e5677` | confinement checks over an authored workflow file | keep | R1.2 puts workflow files inside the rule's target, so this file is in scope, and it survives on what the assertions protect rather than how they read. Each is a boundary an ordinary edit can regress silently: exactly one job holds a write scope and every job that runs repository code is read-only; the token-holding job runs only `actions/deploy-pages` and nothing of ours beside it; the deploying job names the `github-pages` environment, which is where the GitHub-side branch restrictions and reviewers attach, so a rename drops those protections with no local signal. The deploying and producing jobs are found by their grants and their upload action rather than by name, so this is not a name pin. The file's own docstring already scopes the threat model to accidental regression, which is the model `hla.md` accepts for an in-tree guard. |
| F-186 | `cli/tests/test_workflow_policy.py::test_the_gate_is_wired_to_every_dependency_result@59abd8` | quantifier over the workflow's own job set | keep | `set(_needs(gate)) == set(_jobs(CI)) - {"ci-success"}` is derived from the workflow rather than restated, and it catches the real regression: a new CI job that the merge gate does not require, which is invisible until something red merges. `gate["if"] == "always()"` is what stops the gate skipping (and so reporting success) when a dependency fails. The docstring is honest that this is wiring and not a claim about what the gate script does. |
| F-187 | `cli/tests/test_session_lifecycle_cli.py::test_retired_lifecycle_forms_fail_before_state@a22dc7` | retired-form rejection plus a no-state-opened guard | keep | The retired spellings themselves would not earn this (see F-188), but the test also monkeypatches `get_db` and `load_config` to fail if called, so it holds that a grammar error opens no database and reads no config. That ordering is a real property a change moving state loading into the group callback would regress, and it is the same invariant `test_graph_cli.py::test_graph_grammar_errors_precede_config` holds for its own command. Cost: the three retired spellings are hand-listed. |
| F-188 | `cli/tests/test_graph_cli.py::test_retired_resource_spellings_do_not_dispatch@257ee9` | retired-surface absence | delete | `assert result.exit_code != 0` is the test's whole body for three argv forms. Typer rejects an unknown subcommand (`resource describe`, `resource describe-kind`) and an unknown option (`--write` on the live `resource schema` command) by construction, so this can only fail if someone deliberately adds the surface back, which is a reviewed edit that arrives with its own tests. Unlike F-187 there is no state guard beside it. Same reasoning as C-018 and F-053. |
| F-189 | `cli/tests/guide/test_shell_service.py::test_cli_exposes_only_default_list_and_single_topic_show@5dad89,test_removed_evidence_option_is_rejected_by_the_cli@257ee9` | retired-option and never-built-form rejection | delete | Delete `test_removed_evidence_option_is_rejected_by_the_cli` entirely (`guide --evidence` is a retired option, rejected by Typer by construction) and, in the first test, the three negative assertions: `old_option.exit_code != 0` (retired `--names-only`), `direct.exit_code != 0` (a bare topic is an unknown subcommand) and `multiple.exit_code != 0` (a surplus positional). Each is refused by the parser rather than by anything we wrote. The positive half stays and is the test's real content: the default, `list` and `show` all exit 0 with output, and `topics` is non-empty. |
| F-190 | `cli/tests/guide/test_shell_service.py::test_packaged_foundational_topics_are_adjacent_and_render_through_the_shell_service@5bef92` | authored index-order adjacency pin | convert | `indexed[start : start + len(slugs)] == slugs` pins that four hand-listed shipped topics sit next to each other in the index, which is a fact about authored `index-order:` frontmatter (`core-model: 15`, `prerequisites: 16`, `virtual-machines: 17`, `tailscale: 18`). Inserting a topic between them breaks the test although nothing behavioral broke, and the reading order is a review concern. The half worth keeping is that shipped shells actually render. Replacement: drop the slug tuple and the adjacency assertion, and assert every indexed topic renders: `assert all(render_guide(topic.slug, GuideMode.HUMAN).markdown for topic in catalog.indexed_topics())`. Verified by executing: 12 indexed topics at HEAD, all rendering non-empty. That is strictly more coverage than the four. No production change. |
| F-191 | `cli/tests/guide/test_shell_render.py::test_core_model_uses_root_mapping_for_real_images_and_fragment@0beb61` | authored-content count and link pin | convert | `rendered.count(".../main/docs/images/") == 2` and the exact `command-reference.md#named-consoles` link pin how many images the shipped core-model shell holds and which link it carries, so an author adding an image or retargeting a link fails a test about URL rewriting. The invariant is the rewrite itself, on real content, where the repository-root mapping is exercised. Replacement: keep one real-content assertion that at least one repository-root raw URL appears, and replace the link pin with idempotence, `rewrite_relative_destinations(rendered, topic.source) == rendered`, which says no relative destination survived the rewrite. Verified by executing at HEAD: the root-mapped count is 2 and the second pass is a no-op. No production change. |
| F-192 | `cli/tests/guide/test_shell_package.py::test_wheel_and_source_distribution_vendor_the_same_canonical_guide_sources@bec87d` | hand-listed packaged-file inclusion checks | convert | Seven `in packaged` assertions name individual guide markdown files by path. The real invariant is that every authored guide markdown ships in the wheel, and the hand list does not carry it: the tree holds 18 such files at this basis over four `guide-content` directories (`guide/`, `plugins/apt/`, `plugins/install_command/` and `secrets/`), and the whole of `agentworks/secrets/guide-content/` is unnamed, so a packaging rule that dropped it passes today. Replacement, no production change: derive the expected names from the source tree and require each to be packaged, as `sorted(p.relative_to(project).as_posix() for p in (project / "agentworks").rglob("guide-content/*.md"))`. Verified by execution at this basis: that expression yields the 18 wheel-relative names in exactly the spelling `packaged` holds, including the seven the test names today. Keep the `.markdownlint.jsonc` exclusion assertion as it stands. |
| F-193 | `cli/tests/guide/test_shell_package.py::test_wheel_and_source_distribution_vendor_the_same_canonical_guide_sources@bec87d` | count pin on an authored document's image links | convert | Inside the installed-wheel probe, `core.markdown.count("raw.githubusercontent.com") == 2` restates how many relative images `README.md` currently carries, so adding one to the README fails the packaging suite with no bug. **Verified duplicate:** `guide/test_shell_render.py::test_core_model_uses_root_mapping_for_real_images_and_fragment` asserts the same count of two against the same topic in the source tree, more cheaply. What this probe uniquely proves is that an installed render with no source tree still rewrites relative image destinations to absolute raw URLs (`guide/render.py::_rewrite_destination`). Replacement, no production change: assert `"raw.githubusercontent.com" in core.markdown` and drop the count. |
| F-194 | `cli/tests/resources/test_template_kinds.py::test_kind_attributes@9ca7f6` | registry attribute restatement | delete | **Family ruling re-decided 2026-09-06 by mutation, and the disposition changed.** The family question was settled as keep on the reasoning that the registry table is the contract each registration is checked against and nothing else says what this kind's policy should be. The verification lane tested that reasoning the only way it can be tested: it broke each attribute in `KIND_REGISTRY` and ran the suite. The claim is false here, because every one of `vm-template`, `session-template`, `agent-template` and `workspace-template`, on both `miss_policy` and `auto_declare_names`, has at least one twin. Those twins are the observational coverage `hla.md` prefers, so the assertion restates the table without guarding it and the delete stands. F-184a is the one place in the family where the claim held. |
| F-195 | `cli/tests/resources/test_install_resource_kinds.py::test_install_resource_kind_attributes@dc5984` | registry attribute restatement, split by attribute | convert | **Family ruling re-decided 2026-09-06 by mutation, and the disposition changed.** The family question was settled as keep on the reasoning that the registry table is the contract each registration is checked against and nothing else says what this kind's policy should be. The verification lane tested that reasoning the only way it can be tested: it broke each attribute in `KIND_REGISTRY` and ran the suite. **Split 2026-09-06, because the two attributes do not answer alike.** The `miss_policy` assertions delete: `apt-source`, `apt-package`, `system-install-command` and `user-install-command` have observational twins that fail when the policy is broken, so those assertions restate the table without guarding it. The `auto_declare_names` assertions KEEP: every one of those kinds declares `auto_declare_names` as `None`, so there is nothing for a twin to observe and no test anywhere states it. Convert the test to drop the `miss_policy` assertions and retain the `auto_declare_names` ones. The family reasoning behind the original keep was tested the only way it can be tested, by breaking each attribute and running the suite, and it is half right rather than wrong. It was recorded as `apt-source`, `apt-package`, `system-install-command` and `user-install-command` all have twins on `miss_policy` and on `auto_declare_names` where they declare names. Those twins are the observational coverage `hla.md` prefers for the `miss_policy` half. |
| F-196 | `cli/tests/resources/test_harness_integration_kind.py::test_kind_attributes@90fbcc` | registry attribute restatement, split by attribute | convert | **Family ruling re-decided 2026-09-06 by mutation, and the disposition changed.** The family question was settled as keep on the reasoning that the registry table is the contract each registration is checked against and nothing else says what this kind's policy should be. The verification lane tested that reasoning the only way it can be tested: it broke each attribute in `KIND_REGISTRY` and ran the suite. **Split 2026-09-06, because the two attributes do not answer alike.** The `miss_policy` assertion deletes: `harness-integration` have observational twins that fail when the policy is broken, so those assertions restate the table without guarding it. The `auto_declare_names` assertions KEEP: its only `auto_declare_names` twin is inside the test this row would delete, so the delete would take its own evidence with it. Convert the test to drop the `miss_policy` assertions and retain the `auto_declare_names` ones. The family reasoning behind the original keep was tested the only way it can be tested, by breaking each attribute and running the suite, and it is half right rather than wrong. It was recorded as `harness-integration` has a twin on `miss_policy` and on `auto_declare_names`. Those twins are the observational coverage `hla.md` prefers for the `miss_policy` half. |
| F-197 | `cli/tests/resources/test_secret_backend_kind.py::test_kind_attributes@8b4eb3` | registry attribute restatement | keep | **Verified by mutation, 2026-09-06.** Breaking `secret-backend`'s `miss_policy` fails this assertion and nothing else in the suite, so this copy states a policy no twin observes. Its `auto_declare_names` half has thirty twins and would delete on its own, but the two sit in one assertion block over one kind and splitting them would leave a row per attribute; the row keeps whole, and the executor may narrow it to the `miss_policy` half when the edit lands. |

## Group 6: source guards

**Precondition: L-120 and L-121 carry trap-carrying recipes**, so group 5's re-verification note
applies to them on the same terms.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-080 | `cli/tests/test_database_migration_safety.py::test_busy_state_error_message_and_hint_are_not_caller_suppliable@cc8461` | constructor-signature pin | keep | Rubric re-check, 2026-08-16. Not text at all: `list(inspect.signature(BusyStateError.__init__).parameters) == ["self"]` varies with the real constructor shape and is the sole enforcement of an invariant `agentworks/errors.py`'s `BusyStateError` docstring states in prose alone, that no caller can inject remediation prose. A-081 still passes if a defaulted `message` parameter is added, so it does not carry it. Cost: any signature change, including a good one, breaks it. |
| E-086 | `cli/tests/plugins/gcp/test_platform.py::test_delete_continues_after_allow_read_failure_and_reports_retained_allow@5fa267,test_delete_keeps_deny_when_allow_read_and_instance_delete_are_indeterminate@d0c9aa` | redaction defense | keep | Invariant: a provider's private error detail never reaches an operator-facing warning or hint. `provider-private-detail` is the sentinel this test injected into the fake's error. |
| E-099 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_helper_does_not_close_manager_owned_progress@a38ec5` | log-marker presence and absence | keep | Invariant: the helper does not close a manager-owned progress logger (the `# Finished:` marker appears only after the owner closes it). The marker is a structural sentinel in a durable log, not operator prose. |
| E-101 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_helper_redacts_every_non_redacting_progress_surface@809f49` | redaction defense | keep | Invariant: every progress surface (steps, outputs, warnings, errors) is redacted; `_AUTH_KEY` is the injected secret and `[REDACTED]` is the substitution being proven. |
| E-179 | `cli/tests/test_wsl2_paths.py::test_ps_quote_escapes_embedded_single_quote@e692a4` | quoting-function equality | keep | Invariant: `_ps_quote` doubles embedded single quotes. This is an injection defense at a shell boundary, not prose. |
| F-052 | `website/tests/test_site_build.py::BuildAndInstallTests.test_builder_has_no_partial_output_api_or_cli_option@bc157b` | source-text scan for retired spellings | delete | Reads `build.py` as text and asserts three retired identifiers are absent. Pins the spelling of our own source; the absence is visible in the diff that removed them. |
| F-053 | `website/tests/test_site_build.py::BuildAndInstallTests.test_builder_has_no_partial_output_api_or_cli_option@bc157b` | retired-CLI-option guard | delete | Runs the builder with `--only 404` and asserts argparse rejects it. Argparse rejects every unknown flag by construction, so this can only fail if someone adds the option back deliberately. The stderr assertion is stdlib prose (the outside-input exception) but guards nothing of ours. |
| F-064 | `website/tests/test_site_build.py::BuildAndInstallTests.test_model_ships_byte_for_byte_without_generated_proof_composition@5dd25d` | source-text scan for retired spellings | delete | `assertNotIn("lander-route-proofs")` and `assertNotIn("REFERENCE_PROOF")` over `lander-model.js` source. Retired-spelling pin; duplicated at `lander-phase4u.test.mjs`'s assertion block over the same output (F-138). |
| F-066 | `website/tests/test_site_build.py::BuildAndInstallTests.test_retired_route_projection_sources_are_absent@8b2483` | retired-path absence | delete | Asserts three deleted files do not exist. Nothing recreates them; the deletion is in the history. |
| F-079 | `website/tests/test_site_content.py::SourceContractTests.test_manifesto_source_path_is_single@c936cc` | **[unverified]** constant pin plus source-text scan | delete | Asserts `MANIFESTO_CONTRACT.source == Path("docs/manifesto.md")`, a restatement of the production constant, and then reads `website/site_content.py` as text asserting a retired path string is absent. Both are spelling pins over our own code, and the second is the doctrine's "pins how our code is written" in its plainest form. |
| F-097 | `website/tests/test_lander_404.py::StaticDocumentTests.test_input_clear_restores_zero_command_and_renders@fce900,StaticDocumentTests.test_native_actions_share_keyboard_controller_operations_and_focus_lifecycle@fd9957` | source-text substring pins | delete | About thirty assertions slicing `lander-game.js` source and asserting exact statements appear in it (`this.lander_start.focus({ preventScroll: true })`, `commanded: { ...ZERO_INPUT }`, and so on). This is the spelling of our own source in the plainest form; the behavior is exercised for real by the browser suites (focus order, exit/restart, input clearing) and by `lander-model.test.mjs`. |
| F-111 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_banner_projection_uses_model_state_flags_without_status_copy@645ea8` | JS source substring pin | delete | Slices the banner assignment out of `lander-game.js` source and asserts which identifiers appear in it. Pins the shape of one expression in our own code. |
| F-122 | `website/tests/test_lander_phase4j_browser.py::ArcadeBrowserTests.test_phase4k_browser_departures_require_production_input_listeners@456c5e` | production-source mutation plus verbatim pin | delete | Pins four exact `this.listen(...)` statements from the built `lander-game.js`, then rewrites the shipped file to delete each one and asserts the browser contract fails. Mutating our own source to prove a listener is load-bearing pins that statement's spelling: a rename or a refactor to an equivalent binding breaks the test without breaking the page. The browser contract already fails if input stops working, which is the assertion that matters. |
| F-133 | `website/tests/lander-phase4k.test.mjs::L70-84` | **[line-anchored: not Python]** retired-spelling blacklist over five source files | delete | Asserts `lander-launch` and `launch-button` appear in none of the template, controller, stylesheet, Python validator, or fake DOM. A two-name blacklist proving a retired control did not come back. |
| F-136 | `website/tests/lander-phase4m.test.mjs::L42-45` | **[line-anchored: not Python]** retired-spelling source pin | delete | `!source.includes("legDepartureFuel")` over two production modules. Retired identifier blacklist. |
| F-140 | `website/tests/lander-phase4s.test.mjs::L37-39` | **[line-anchored: not Python]** source-text scan of `build.py` from a JS test | delete | `assert.match(build, /Path\("static\/lander-collision\.js"\)/)` and a retired-symbol `doesNotMatch`. Pins Python source spelling from the Node suite; the manifest fact is held structurally at test_site_build.py::BuildAndInstallTests.test_collision_and_world_ship_separately_in_the_exact_manifest (F-063, F-065). |
| F-141 | `website/tests/lander-phase4u.test.mjs::L319-325` | **[unverified]** **[line-anchored: not Python]** retired-symbol blacklist over three sources | delete | Seven retired identifiers asserted absent from two modules and `build.py`. Duplicates F-064 and F-066; same reasoning. |
| L-101a | `cli/tests/resources/test_graph_guard.py` | AST guards pinning call-graph shape, plus their positive honest-path pins | delete | The three call-shape patterns: the `dependencies()` re-walk (`test_graph_guard.py::find_dependencies_calls`, `test_graph_guard.py::test_pattern1_no_dependencies_rewalk_outside_the_builder`), the lazy readiness recompute (`test_graph_guard.py::_is_not_ready_call`, `test_graph_guard.py::find_not_ready_calls`, `test_graph_guard.py::test_pattern3_no_not_ready_recompute_outside_the_fold`), and the `references` field or `getattr` (`test_graph_guard.py::find_getattr_references`, `test_graph_guard.py::find_references_fields`, `test_graph_guard.py::test_pattern4_no_references_field_or_getattr_on_resources`), with their three module-level allow-lists (`_DEPENDENCIES_ALLOWLIST`, `_NOT_READY_ALLOWLIST`, `_REFERENCES_FIELD_ALLOWLIST`), their three `test_every_exemption_is_load_bearing` parameters, and the five positive pins that assert the honest call appears (`test_graph_guard.py::test_collect_secrets_for_reads_the_runtime_closure`, `test_graph_guard.py::test_the_recipe_gate_reads_both_closures`, `test_graph_guard.py::test_node_factories_read_edges_of`, `test_graph_guard.py::test_cycle_detection_reads_the_built_edge_map`, `test_graph_guard.py::test_readiness_projections_read_readiness_of`). Each says which call shapes may appear in which module, which is `hla.md`'s delete side. |
| L-101b | `cli/tests/resources/test_graph_guard.py` | AST reachability guard over the capability registries | keep | **Was part of a whole-file delete row; the lead accepted the reservation 2026-08-16 and corrected `hla.md`'s delete bullet with it.** A registry probed outside the builder and publishers is a rule about what a module may REACH, not how the reaching is spelled, which puts it beside the import-boundary guard on the keep side. It is also the one guard in this inventory with no observational twin available even in principle: a consumer that re-derives the graph correctly agrees with it until the two diverge, so no behavioral test can tell a second derivation from the first. Keeps both detectors (`test_graph_guard.py::find_registry_reads` with `test_graph_guard.py::_registry_aliases` and `test_graph_guard.py::_is_registry_read`, and `test_graph_guard.py::find_descriptor_registry_calls`, which watches the same probe with the registry's name filed off), `test_graph_guard.py::test_pattern2_no_registry_probe_outside_builder_and_publishers`, both module-level allow-lists (`_REGISTRY_READ_ALLOWLIST` and `_DESCRIPTOR_REGISTRY_ALLOWLIST`), the two function-scoping tests (`test_graph_guard.py::test_vms_sites_exempt_reads_are_function_scoped`, `test_graph_guard.py::test_descriptor_exempt_reads_are_function_scoped`), the two matching `test_every_exemption_is_load_bearing` parameters, and the shared `test_graph_guard.py::_iter_agentworks_modules` / `test_graph_guard.py::_scan` plus the non-vacuity coverage for these two detectors. The module docstring loses its three deleted patterns and keeps this one. |
| L-103 | `cli/tests/schema/test_package_boundary.py::_agentworks_imports@e3b0c4,test_the_schema_package_imports_only_top_level_leaves@fbd3a8,test_the_import_scan_sees_real_imports@d2b747` | per-module import scan plus a subprocess import check | keep | Layering boundary: the schema package is a leaf, and a reach upward makes importing any capability module a circular import. The two halves cover different mistakes, and the file says so: the subprocess check catches the cycle, the scan catches a function-local import in a branch nothing calls, which the subprocess check cannot reach. |
| L-104 | `cli/tests/test_registry_warning_boundary.py::test_build_registry_use_detection@af967c,test_build_registry_use_detection_ignores_unrelated_references@18f1ad,test_only_intentional_pure_callers_use_build_registry@ff25fe` | enumerated-caller scan for `build_registry` plus two detector self-tests | keep | The invariant is that the old-selector warning renders once at the request boundary; a new direct `build_registry` caller on a request path leaks or duplicates it. No observational test can detect the absence of a caller, so the doctrine's twin tiebreaker leaves this standing. |
| L-105 | `cli/tests/test_registry_warning_boundary.py::test_build_registry_with_explicit_manifest_set_does_not_render_warnings@1f586b` | observational: explicit manifest set renders no warning | keep | The behavioral half of the same boundary, and the fixture deliberately carries a real advisory so an issue-free set cannot pass vacuously. |
| L-106 | `cli/tests/sessions/test_harness_integration_gate_drift.py::test_every_session_factory_caller_gates_the_harness_integration@31497d` | per-function AST scan that every session-node factory caller gates the harness integration | keep | Verified: `plugins/test_enablement_producer.py::test_session_template_on_disabled_plugin_harness_integration_lists_ready` proves the gate FUNCTION refuses a disabled integration, and nothing proves every call site calls it. That is the gap this guard holds, and no observational test can observe a caller that does not exist. Carries its own non-vacuity assertion. |
| L-107 | `cli/tests/sessions/test_harness_integration_gate_drift.py::test_guard_is_not_vacuous@f3f7f4` | detector self-test | keep | Non-vacuity for L-106; a scan that silently matched nothing would pass forever. |
| L-108 | `cli/tests/agents/test_recipe_gate_drift.py::test_runner_caller_sets_are_exactly_the_enumerated_entries@e7f0c7` | enumerated caller-set pin for the recipe runners | keep | Same reasoning as L-106: `plugins/test_manifest_parity.py::test_ensure_recipe_enabled_refuses_disabled_contribution_in_closure` proves `ensure_recipe_enabled` refuses, and only this guard proves no path reaches, without its gate, a declarable-consuming runner. The comment block names the real hole it was written for (`session create --new-agent`, whose realize and gate sit in different functions). |
| L-109 | `cli/tests/agents/test_recipe_gate_drift.py::test_every_command_entry_gates_its_recipe@99ada5` | gate-presence assertion per command entry | keep | Presence at each of the six entries; this is reachability, not spelling. |
| L-110 | `cli/tests/agents/test_recipe_gate_drift.py::test_every_command_entry_gates_its_recipe@99ada5` | `assert calls[_GATE] < calls[realize]`, a lexical statement-order pin | convert | `hla.md` is explicit: delete the pin that says a statement comes first, keep the test that says what goes wrong when it does not. Replace with: drive the entry with a DISABLED recipe and assert nothing was persisted. **Corrected 2026-09-06 after the lane executed both spellings.** An earlier re-cite changed this to "an injected failure", which does not exercise the invariant: with the gate moved after `realize_agent`, a failure-injection test still passes and only this row's own test fails, so the replacement would have covered nothing. The disabled recipe is what makes the gate's position observable. `agents/test_create_reinit_orchestrated.py::test_create_mutation_failure_cleans_up_and_leaves_no_row` (`test_create_mutation_failure_cleans_up_and_leaves_no_row`) is the assertion SHAPE to copy, and is not a twin for this invariant. No production change. |
| L-111 | `cli/tests/agents/test_recipe_gate_drift.py::test_guard_is_not_vacuous@e1cf0a` | detector self-test | keep | Non-vacuity for L-108 and L-109. |
| L-112 | `cli/tests/plugins/test_plugin_framework.py::test_each_derived_site_reads_the_descriptor_table_in_its_own_body@349949` | **[unverified]** `test_each_derived_site_reads_the_descriptor_table_in_its_own_body`, an AST pin that each switchboard body references the table by name | delete | This pins how the code is written, and its own docstring concedes the point: the sibling set-equality test against the real descriptor table catches drift, and this one exists only to forbid a hand-written enumeration that stays correct. A correct enumeration is not a bug; it becomes one when it drifts, and the sibling catches that. |
| L-113 | `cli/tests/test_database_migration_safety.py::test_production_has_one_writable_database_construction_site@fe1ee6` | AST scan enumerating writable `Database(...)` construction sites | keep | Reachability, not spelling: write access to the state database is confined to one construction site, and an ordinary edit adding `Database(path)` elsewhere silently grants write access on a read-only path. No runtime test can observe the absence of such a site. |
| L-114 | `cli/tests/test_tailscale_legacy_removed.py::_read_module_source@e3b0c4,test_initializer_has_no_read_env_with_legacy_for_tailscale@8069ff,test_vm_manager_module_does_not_reference_legacy_tailscale_env_var@fa82aa,test_vm_manager_does_not_read_legacy_env_for_tailscale_in_collect@d90a98` | three `getsource` identifier-spelling tripwires for the removed `AW_TAILSCALE_AUTH_KEY` fallback | convert | **Recipe verification, 2026-08-16.** **The replacement recorded earlier was verified at the wrong level and would have retired a guard that was doing real work.** Driving the RESOLVER directly does bite a legacy fallback inside `Resolver.resolve`, but it PASSES when the fallback is reintroduced inside `create_vm`, which is where the removed code actually lived. Corrected recipe, verified in both halves: drive `create_vm` end to end with the platform, Tailscale transport and Phase B faked, `AW_SECRET_TAILSCALE_AUTH_KEY` deleted and `AW_TAILSCALE_AUTH_KEY` set, and assert `SecretUnavailableError` plus no VM row. That bites BOTH reintroduction sites and is strictly stronger than all three greps combined. No production change. Separate finding about the file being deleted: the module-level `_MODULES_TO_SCAN` tuple in `cli/tests/test_tailscale_legacy_removed.py` lists `agentworks.secrets.resolver`, but no test in the file ever scans it, so the third entry is dead and the greps miss a resolver-level reintroduction. |
| L-116 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_vm_create_does_not_eager_resolve_operator_env@d05db2` | `getsource` scan for `SecretTarget(` across the vm-create call chain | convert | **Recipe verification, 2026-08-16.** **PARTIAL as recorded, and the correction matters: the register-targets spy is blind to half the family.** L-118 and L-119's guards name `resolve_for_command`, a FREE function at `secrets/orchestration.py::validate_target_values`, so a spy on `Resolver.register_targets` cannot see it: injecting a live `resolve_for_command` walk into `create_agent` left the spy green. L-116 and L-117 verified only because their guards name the `SecretTarget` mechanism the spy does watch. Corrected recipe covering all four, verified against BOTH mechanisms on every row: declare an operator-env secret that nothing can resolve and assert provisioning still completes. No production change. |
| L-117 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_vm_reinit_does_not_eager_resolve_operator_env@a4b876` | `getsource` mirror for `reinit_vm` | convert | **Recipe verification, 2026-08-16.** Same corrected recipe as L-116 (declare an unresolvable operator-env secret, assert provisioning still completes), verified on the `reinit_vm` path against both reintroduction mechanisms. |
| L-118 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_agent_create_does_not_eager_resolve_operator_env@e7adf9` | `getsource` scan for `resolve_for_command` in `create_agent` | convert | **Recipe verification, 2026-08-16.** **The recorded replacement FAILED to bite.** This guard names `resolve_for_command`, a free function at `secrets/orchestration.py::validate_target_values`, which a `Resolver.register_targets` spy cannot observe; a live `resolve_for_command` walk injected into `create_agent` left it passing green. Use L-116's corrected recipe instead: declare an unresolvable operator-env secret and assert provisioning still completes. Verified to bite both mechanisms. |
| L-119 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_agent_reinit_does_not_eager_resolve_operator_env@b509a7` | `getsource` mirror for `reinit_agent` | convert | **Recipe verification, 2026-08-16.** **Same failure as L-118** and the same correction: the register-targets spy is blind to `resolve_for_command`. Use L-116's corrected recipe on the agent reinit path. |
| L-120 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_agent_setup_runners_have_no_env_injection@93db0f` | `getsource` scan for `env=agent_env` / `agent_env = compose_env` in `create_agent_on_vm` | convert | **Verified 2026-09-06: the grep is the only thing in the suite that catches a gated injection on either path, so until the replacement asserts the runners fired, deleting it covers nothing.** Recipe verification, 2026-08-16: verified, and it bites when an operator-env `env=` kwarg is added to the claude-plugins runner. **Record the vacuity trap with the row**: every runner in `create_agent_on_vm` is configuration-gated, so under a default template none of them fire and the assertion covers nothing. The first break attempt PASSED for exactly that reason. The replacement is honest only if the fixture declares an admin and agent template carrying `user_install_commands`, `dotfiles_source` plus `dotfiles_install_cmd`, `mise_packages`, `claude_marketplaces` and `claude_plugins`, and the dotfiles runner additionally needs a real local source directory and a `copy_dir_to` on the fake transport. **So the convert lands only with an assertion that each gated runner actually fired**, in the test itself rather than as a reviewer's checklist item: the fixture is what makes the replacement non-vacuous, and a fixture can go quietly wrong later. Without that assertion this row is a keep and the `getsource` grep stays, because the grep covers the whole function unconditionally and a silently gated replacement covers nothing. |
| L-121 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_vm_provisioning_runners_have_no_env_injection@7d4c15` | `getsource` scan for `env=admin_env` in `_phase_b_setup` | convert | **Recipe verification, 2026-08-16.** Same recipe and the same vacuity trap as L-120, on `_phase_b_setup`, and the same condition: the convert lands only with an assertion that each gated runner fired, otherwise this is a keep and the grep stays. Verified: adding an operator-env `env=` to `_admin_run_cmd` makes it fail. |
| L-123 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_create_agent_on_vm_ends_with_ensure_files_sourced@55929f` | `getsource` presence pin, agent mirror | convert | **Recipe verification, 2026-08-16.** Same recipe, agent path, and **the verification showed the replacement is strictly stronger than the grep it replaces**: with the call site replaced by `pass`, the original `test_create_agent_on_vm_ends_with_ensure_files_sourced` still PASSED, because the surviving `from ... import _ensure_agentworks_files_sourced` line inside the function body satisfies the substring scan. The grep could not tell an import from a call. Patch `vm_initializer._ensure_agentworks_files_sourced`, since this path imports the name inside the function body. |
| L-124 | `cli/tests/vms/test_phase_a_platform_bootstrap.py::test_fallback_era_contract_shapes_are_structurally_absent@4c6ccd` | structural half: dataclass field presence/absence and the `_phase_a_bootstrap` parameter set | keep | Real structural invariant over live types: the fallback-era `bootstrap_complete` result field and the `BootstrapCompletion` attribute are gone, and Phase A's signature is exactly the seven parameters. Asserted against real objects, not source text. |
| L-125 | `cli/tests/vms/test_phase_a_platform_bootstrap.py::test_fallback_era_contract_shapes_are_structurally_absent@4c6ccd` | `request_fields["tailscale_auth_key"].type == "str"` | delete | An annotation-equality assertion, the same shape PR #546 deleted from `secret_backend/conformance.py`. Under strict typing mypy carries this; the assertion compares an annotation STRING and breaks on a purely cosmetic re-spelling. |
| L-126 | `cli/tests/vms/test_phase_a_platform_bootstrap.py::test_fallback_era_contract_shapes_are_structurally_absent@4c6ccd` | `"run_wsl2_bootstrap" not in driver_source`, `"generate_bootstrap_script" not in driver_source` | keep | Rubric re-check, 2026-08-16. **The row's premise was false at HEAD.** `run_wsl2_bootstrap` is genuinely gone and that half still deletes, but `generate_bootstrap_script` is a LIVE symbol, exported by `capabilities/vm_platform/bootstrap_script.py` and called by the proxmox, azure and aws platforms, so a reintroduced call in `vms/initializer/driver` would import cleanly and pass silently rather than failing to import as the row claimed. That half is the only probe and keeps. |
| L-127 | `cli/tests/test_config_resource_read_guard.py::test_no_config_resource_reads_outside_publishers@b3acda` | regex source scan for `config.<resource-attr>` reads with an EMPTY allowlist | delete | **Verified by execution and it sharpens the row.** `Config` is a dataclass whose fields are exactly `config_issues, database, defaults, deprecation_issues, enabled_system_plugins, operator, paths, secret_config_data, session, source_path`: none of the fifteen forbidden attributes exists. A reintroduced read is a mypy error and an `AttributeError`, so the guard protects a surface the type system already closed, while its regex can still false-positive on any local named `config` or `cfg` that legitimately has a `.vm` or `.admin` attribute. |
| L-128 | `cli/tests/resources/test_phase0_vocabulary.py::test_kind_registry_keys_are_lower_kebab@efe550` | `test_kind_registry_keys_are_lower_kebab` | keep | Structural check over the live registry, and the registry keys are the shipped manifest `kind:` vocabulary. A newly registered kind spelled `vm_template` is an ordinary edit that regresses an operator-facing contract. |
| L-129 | `cli/tests/resources/test_phase0_vocabulary.py::test_kind_handlers_match_their_registry_key@5668e2` | `test_kind_handlers_match_their_registry_key` | keep | Structural: every handler answers to the key it is filed under. A mismatch mis-dispatches at runtime and no other test asserts the pairing. |
| L-130 | `cli/tests/resources/test_phase0_vocabulary.py::test_code_declared_vocabulary_is_gone@733dfa` | source-wide grep for `code-declared` / `code_declared` | delete | Retired-spelling grep over source text, the doctrine's identifier-spelling delete. The `Origin` variant set is a real enum; a reintroduced variant would have to be added to it, and `test_origin.py` covers the live variants. |
| L-131 | `cli/tests/resources/test_phase3_naming_consistency.py::test_no_framework_module_has_old_vocabulary@dc70b5` | `_BANNED_SUBSTRINGS` scan over public symbol names | convert | **Merged with L-132 on 2026-09-06, because they were one decision written as two.** This row was a delete and L-132's convert proposed extending THIS scan into class bodies, so deleting it and extending it were the same round's instructions to remove and to keep the same test. The scan as written is a blacklist over module-level symbol names, which rejects `Requirement` and `UsageEntry` and waves through any other spelling; extended one level into class bodies it catches the identifiers L-132's text scan was reaching for, which the verification lane confirmed by reintroducing them. Replacement: extend the symbol scan into class bodies AND narrow the banned list to the identifiers that actually shipped, since a list that rejects the bare word `requirements` catches legitimate use. **Prose coverage is dropped by design**: comments and docstrings stop being scanned, which is the whole point of L-132 deleting into this row rather than converting beside it. No production change. |
| L-132 | `cli/tests/resources/test_phase3_naming_consistency.py::test_resources_package_has_no_old_vocabulary_in_source@0ff141` | forbidden-wording blacklist over the package's source text | delete | The test reads every source file in the package and scans its text, comments and docstrings included, for banned words, which is the shape `no-prose-policing-tests` calls worse than useless; its list includes the bare word `requirements`, so any legitimate future use trips it. **Merged into L-131 on 2026-09-06**: the property worth keeping, that the old vocabulary stays out of the package's public surface, is what L-131's extended symbol scan checks, so this row deletes into that one rather than converting alongside it. |
| L-133 | `cli/tests/resources/test_phase3_naming_consistency.py::test_resource_reference_carries_usage_field_not_text@d89232` | **[unverified]** `test_resource_reference_carries_usage_field_not_text` | delete | Restates the dataclass definition: it can only fail when someone edits the fields it lists, and every consumer of `ResourceReference.usage` fails first. The `text` half is a retired-spelling pin. |
| L-134 | `cli/tests/resources/test_phase3_naming_consistency.py::test_graph_node_producers_expose_dependencies_not_referenced_resources@b7185c` | `test_graph_node_producers_expose_dependencies_not_referenced_resources` | delete | `hasattr` presence pins on methods the graph builder calls on every build; the builder's own tests fail loudly if `dependencies` disappears. The `not hasattr` halves are retired-spelling pins. |
| L-135 | `cli/tests/resources/test_phase3_naming_consistency.py::test_resource_kinds_carry_no_inbound_reference_field@26452c` | `test_resource_kinds_carry_no_inbound_reference_field` | keep | This one is not a spelling pin: it asserts across every resource kind that inbound references live on the graph rather than on the dataclass. A new kind declaring a `references` field would silently reintroduce a second source of truth for `dependents_of`, and nothing else checks the whole kind set. |
| L-136 | `cli/tests/assistance/test_contract.py::test_generator_has_no_network_or_process_boundary@a1f3e8` | import boundary on the package generator script | keep | The generator must stay inert: no `subprocess`, sockets, or network. Layering boundary the type system cannot express. |
| L-137 | `cli/tests/assistance/test_contract.py::test_generated_packages_are_inert_and_share_the_canonical_handoff@273af4` | `body == BODY` plus generated file-set equality | keep | Derivation parity, which the rule endorses by name: the two shipped plugin skills are proven byte-identical to the canonical `packaging/agentworks/assistance.md`, and the file-set equality proves nothing executable was added beside them. |
| L-138 | `cli/tests/assistance/test_contract.py::test_catalogs_and_manifests_have_neutral_identity_and_complete_codex_policy@138e8d` | `METADATA`-derived assertions on the catalogs and manifests | keep | Parity against the canonical `metadata.json`: display name, publisher, description and the interface block are proven to be projections rather than copies. |
| L-139 | `cli/tests/assistance/test_contract.py::test_catalogs_and_manifests_have_neutral_identity_and_complete_codex_policy@138e8d` | `codex_catalog == {...}`, a verbatim dict literal | delete | **Corrected 2026-08-19 (map re-baseline).** Re-derived at HEAD and narrowed off L-138's range. `assert codex_catalog == {` opens the `codex_catalog == {...}` literal: a second copy of an authored config file maintained inside the test, which R1.2 generalized the rule to cover. The `METADATA`-derived parity L-138 keeps is elsewhere in the same test: `"description": METADATA["description"]` inside the `claude_catalog["plugins"]` equality, the three `interface` asserts, and `assert codex_manifest["version"] == claude_manifest["version"] == METADATA["packageVersion"]`, all outside this row. |
| L-140 | `cli/tests/assistance/test_contract.py::test_readme_projection_is_exact@fed682` | `test_readme_projection_is_exact` | keep | Derivation parity again: the README's generated block is proven to be `BODY` plus its fence. This is the canonical example the rule points at. |
| L-141 | `cli/tests/test_shell_integration.py::test_capability_imports_neither_sessions_nor_orchestration@201600` | subprocess probe asserting `agentworks.sessions` / `agentworks.orchestration` are not imported | keep | An OBSERVATIONAL import boundary: it launches a real interpreter and reads `sys.modules` rather than reading source. This is the twin form `hla.md` prefers, not a source guard. |
| E-199 | `cli/tests/test_doctor_env_and_secrets.py::test_manifest_issues_surface_as_doctor_rows@ae358f,test_clean_manifests_keep_config_valid_row@834fbb,test_manifest_load_failure_keeps_other_rows@faa01a` | label-keyed row selection and withheld-row assertions | keep | These select rows by their authored names (`c.name == "Manifest"`, `c.name == "Config"`) and assert the ok row is withheld when any issue exists (`not any(c.name == "Config is valid" ...)`). The invariant is real and regressable: doctor must not report a clean config beside a failing manifest. The label is the only handle a check row offers, and R2.2 forbids adding a structural row identity to replace it. Cost: renaming a row breaks these three tests. |
| E-200 | `cli/tests/test_doctor_env_and_secrets.py::test_focused_checks_reject_non_exact_tty_access_before_resource_work@1ad95b` | caller-provenance boundary guard | keep | Passes a bare `"unavailable"` string where `TtyInteractionAccess` is declared and asserts `checks_for_resource` raises before touching config or registry. This is the boundary FRD ruling 11 and `hla.md` name: an argument from a caller the type checker does not check, which is exactly how a live `interaction="refuse"` call once resolved a real secret. Deleting it is the mistake PR #523 already made once. |
| E-201 | `cli/tests/test_doctor_env_and_secrets.py::test_secret_checks_use_only_explicit_tty_access@1335f3` | consent confinement | keep | `monkeypatch.setattr(output, "non_interactive", lambda: pytest.fail("doctor read ambient TTY policy"))` makes any read of ambient TTY policy a failure, while the assertions below prove the two explicit access values produce different preview outcomes. Doctor must decide from what it was told, not from what the terminal happens to be, and this is the only test that forbids the ambient read. |
| E-203 | `cli/tests/test_doctor.py::test_health_group_convenience_methods@50de8a` | wire-contract enum values | keep | `[status.value for status in Status] == ["ok", "info", "warn", "fail"]`, riding beside the convenience-method assertions. Those four strings are what doctor's JSON output emits and what machine consumers branch on, so the restatement is the contract. Same reasoning as F-158 and C-034. |
| E-204 | `cli/tests/test_doctor.py::test_run_checks_rejects_non_exact_tty_access_before_health_work@1ad95b` | caller-provenance boundary guard | keep | The `run_checks` twin of E-200: a bare string where the enum is declared must raise before any health work runs. Same boundary, same reason. |
| E-205 | `cli/tests/test_doctor.py::test_machine_output_serializes_value_free_instance_state_fact@bc0b09` | JSON projection shape over the test's own values | keep | The projected `instance_state` fact must equal the exact dictionary of the values the test put on the `InstanceStateHealthFact`, key names included. The key names are the JSON v1 field names machine consumers read, and the values are the test's own, so this is schema shape plus value identity rather than a pin on anything authored. |
| E-206 | `cli/tests/resources/test_origin.py::test_external_plugin_still_not_constructible@51d055` | attribute-absence pin plus an annotation-substring scan | delete | `not hasattr(Origin, "external_plugin")` and `"external-plugin" not in variant_field.type` read the class and its raw annotation string and assert an identifier is absent. That is the doctrine's delete side, "pins how our code is written": adding the factory under any other name sails through, and the in-tree threat model is accidental regression, not deliberate reintroduction, which is what adding a variant to a `Literal` and a factory beside it would be. `"system-plugin" in variant_field.type` is the positive half of the same substring scan and goes with it; the four factory tests above already prove `system_plugin` exists and stamps its variant. Operator ruling 1 keeps checks that defend a real dynamic boundary, and this defends none: nothing loads external plugins yet. **The whole function goes, not a line of it**, and `dataclasses.fields` at `test_origin.py::test_external_plugin_still_not_constructible` is its only use of the module, so `import dataclasses` at `test_origin.py`'s `import dataclasses` is orphaned by the delete and goes too. The `from dataclasses import FrozenInstanceError` on the next line has other users and stays. |
| E-207 | `cli/tests/orchestration/test_node_protocol.py::test_real_capability_instances_are_readiness_not_nodes@ddba8d` | attribute-absence pin beside a protocol check | delete | Drop `not hasattr(instance, "key")` and `not hasattr(instance, "deps")`. The claim the test is named for, that a shipped capability instance is not walkable, is carried by `not isinstance(instance, Node)` on the line above, which fails the moment such an instance becomes structurally a node (`Node` is `runtime_checkable` and its members are `key`, `deps` and `secret_refs`). The two `hasattr` lines pin two specific attribute names on our own classes, so they also fail on a rename, or on a capability gaining an unrelated `key` attribute that does not make it a node. |
| E-208 | `cli/tests/resources/test_access.py::test_resource_identity_is_frozen_and_slotted@1f4b3f,test_resolve_resource_returns_exact_identity_row_and_origin@2da58e` | slots-presence pin | delete | `assert not hasattr(identity, "__dict__")` and `assert not hasattr(resolved, "__dict__")` can only fail when someone removes `slots=True` from the dataclass they restate, and nothing in the accessor layer behaves differently without it: attribute injection is already refused by the frozen dataclass, which each test proves on the next line with `pytest.raises(FrozenInstanceError)`. Those `FrozenInstanceError` assertions stay. |
| E-209 | `cli/tests/test_vm_create_tailscale_eager_resolve.py::test_join_tailscale_signature_requires_auth_key_kwarg@76ae94` | signature-shape pin | delete | The test reads `inspect.signature(_join_tailscale)` and asserts `auth_key` is keyword-only with no default. Both call sites are first-party and inside the mypy run (`vms/initializer/credentials.py::rejoin_tailscale` and the `vms/initializer` re-export), so strict typing already refuses a caller that omits it, and `vms/test_tailscale_stdin_join.py` drives the real function with a key and asserts the key never reaches the command line. Unlike A-080, which survives because it is the sole enforcement of a prose-only invariant, the invariant here is expressed in the signature the type checker reads. Flagged as borderline: the one regression this and only this would catch is someone giving `auth_key` a `None` default, after which an omitting caller would join the tailnet with no key; that failure is loud (the join fails) rather than silent. |
| E-210 | `cli/tests/test_typer_output_abort.py::test_typer_and_click_abort_are_distinct_classes@848ad1` | upstream class-taxonomy premise | keep | `typer.Abort is not click.exceptions.Abort` plus the two `issubclass` refusals pin a third-party fact, which is the one exception `no-prose-policing-tests` allows: it can change under us on a version bump, and when it does the widened `except` tuple in `TyperHandler` becomes half dead while every conversion test still passes. Narrow, and about the library rather than about us. |
| E-211 | `cli/tests/guide/test_shell_service.py::test_static_index_list_and_selected_render_do_not_load_operator_state_modules@b4c975` | import-boundary confinement guard | keep | `hla.md`'s standing keep example, in its current home: the deleted `guide/test_power_import_boundary.py` (row L-102, retired) is gone and this is the guard that survived it. **Verified across the suite, 2026-09-06: nothing else catches that.** Rendering the index, listing topics and rendering a topic must not import `agentworks.config`, `agentworks.db`, `agentworks.resources` or `agentworks.secrets`, checked in a clean subprocess before and after the calls. It is a layering boundary the type system cannot express, an ordinary import regresses it, and nothing else catches that. |
| E-212 | `cli/tests/resources/test_graph_query.py::test_human_projection_sanitizes_controls_from_every_dynamic_fact@80acf4,test_existing_envelope_encodes_graph_data_before_write_and_escapes_controls@760955` | control-character defense at the two output boundaries | keep | The first asserts no rendered line carries a character in the `Cc`, `Cf`, `Cs`, `Zl` or `Zp` categories while ordinary unicode survives, over kind and name values a hostile document could set; the second asserts the JSON envelope escapes `\x7f` and `\x80` rather than emitting them. These are terminal-injection and encoding defenses over untrusted input, the same family as E-179, and the absence halves are defenses rather than wording blacklists. |
| E-213 | `cli/tests/transports/test_abc.py::test_abc_surface_is_complete@b41e03` | abstract-method set pinned against a module constant | keep | Reads the live class with `inspect.getmembers` rather than the source text, and asserts the abstract set equals `REQUIRED_METHODS`. It is the only probe that an `@abstractmethod` decorator has not silently been dropped: `test_concrete_transports_implement_abc` and `test_incomplete_subclass_cannot_be_instantiated` both still pass when a method stops being abstract, and the consequence is a subclass that constructs cleanly and raises `NotImplementedError` at attach time. The file's own comment records that this boundary already moved twice, when `copy_dir_to`, `write_file` and `interactive` became concrete defaults. Cost, stated: adding a genuine abstract method means editing `REQUIRED_METHODS` in the same change. |
| E-214 | `cli/tests/db/test_instance_state.py::test_owner_inspection_keeps_recognized_and_future_records_closed@44ba74` | forbidden-substring list over executed SQL | convert | `assert not any("EXISTS(" in statement.upper() for statement in statements)` guards a real property: `inspect_owner_state` must take the known-owner branch, which selects the literal `1` instead of the four-way `EXISTS` case (`db/instance_state.py::InstanceStateRepository._inspect`), rather than re-probing an owner the caller already established in its snapshot. The blacklist is a weak way to say it, because `EXISTS (` with a space, or a join-shaped re-probe, both pass. An observational twin exists and is cheap. Replacement, no production change: delete the owner row (`DELETE FROM vms WHERE name = 'alpha'`) after seeding and before inspecting, then assert `inspect_owner_state` still reports `owner_exists is True`. **Verified by execution at this basis:** with the owner row gone, `inspect_owner_state` reports `True` (the known-owner branch) while `inspect_all_instance_state` reports `False` for the same records (the `EXISTS` branch), so the twin separates the two and no rewording of the SQL can bypass it. The rest of the test is unaffected. |

## Deferred: held for the R4 reassessment

**These rows are NOT part of the sweep's executable set.** They are listed here, out of their
groups, so that an executor scanning for delete rows cannot pick them up by accident.

**Operator ruling, 2026-08-16.** `website/tests/test_site_templates.py` is held until the
reassessment decides the fate of `site_validation.py`'s element-by-element half, which the FRD lists
as a deferred subsystem redesign. The reason is an asymmetry rather than a doubt about the rows: if
R4 deletes that validator, these tests die with it at no cost, and if R4 keeps it, deleting them now
permanently removes its negative-path coverage. Deferring is safe in both directions and nothing
else is.

R4 also inherits an open question with them, deliberately not chased here: whether they police the
authored form of the templates rather than the validator's rejection behavior. The answer does not
change the deferral, which is why it was left for whoever takes W2.

Three rows beyond the operator's 22 are held with them, and this is the lead's extension rather than
the ruling: **F-030 and F-039 are converts**, and rewriting two tests in a file that R4 may delete
wholesale is work R4 may discard. **L-402 is group 1's**, covering the 12 `assertRaisesRegex` sites
in this same file; most of those regexes sit inside tests that are now deferred, so the row cannot
be executed independently of them. Reverse either if you would rather they rode with their groups.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| F-025 | `website/tests/test_site_templates.py::TemplateContractTests.test_each_template_has_only_its_closed_vocabulary@147cce` | **[deferred]** token-set pin (second copy) | delete | Asserts each template's token set equals `site_builder.TEMPLATE_TOKENS`, a production constant. Verified by execution: a token with no substitution fails the build outright, so the agreement is enforced by building rather than by this restatement. |
| F-026 | `website/tests/test_site_templates.py::TemplateContractTests.test_unknown_missing_duplicate_wrong_template_and_brace_tokens_fail@b24a4e` | **[deferred]** mutate-one-string-assert-raises | delete | Eight template mutations proving `_validate_template` rejects unknown, missing, duplicate, foreign, brace-like and relocated tokens. The templates live in this tree and are validated on every build; this only proves the guard fires, and the guard's own half of `site_validation.py` is the deferred W2 deletion candidate. |
| F-028 | `website/tests/test_site_templates.py::TemplateContractTests.test_content_tokens_cannot_move_to_hostile_or_unreviewed_contexts@b24a4e` | **[deferred]** mutate-one-string-assert-raises | delete | Thirteen mutations moving content tokens into `<script>`, `<style>`, and containers nobody reviewed. Adversarial tampering with an in-tree template: anything that can make this edit can delete this test. |
| F-029 | `website/tests/test_site_templates.py::TemplateContractTests.test_onboarding_paths_copy_icon_and_shared_destinations_are_guarded@b24a4e` | **[deferred]** mutate-one-string-assert-raises | delete | Fifteen mutations renaming onboarding ids and classes, changing the icon viewBox and the live-region politeness, and repointing the module src. Pins the template's spelling through the validator; the onboarding structure that matters is asserted on the built page by F-007 and F-008. |
| F-030 | `website/tests/test_site_templates.py::TemplateContractTests.test_shell_destination_labels_are_bound_to_their_reviewed_hrefs@e9dbd7` | **[deferred]** mutate-one-string-assert-raises | convert | The property is real: a service link's visible label must match the destination it points at, and swapping the GitHub and PyPI labels is a plausible copy-paste error with a user-visible consequence. Replacement: on the BUILT page, assert the anchor whose text is "GitHub" carries `REPOSITORY_URL` and the one whose text is "PyPI" carries `PYPI_URL`. Structural, on output, no production change. |
| F-031 | `website/tests/test_site_templates.py::TemplateContractTests.test_shell_css_classes_landmark_order_and_breadcrumb_order_fail_closed@302700` | **[deferred]** mutate-one-string-assert-raises | delete | Ten mutations per shell template renaming CSS classes, changing aria labels, reversing the breadcrumb and moving the footer above main. Pinning the spelling of our own class names is the exact target of the amended `no-prose-policing-tests` rule. |
| F-032 | `website/tests/test_site_templates.py::TemplateContractTests.test_detail_page_headings_reject_provenance_eyebrows@184e7b` | **[deferred]** mutate-one-string-assert-raises | delete | Proves the validator rejects an eyebrow paragraph above a detail-page `h1`. A house style rule enforced by a test; review owns it. |
| F-033 | `website/tests/test_site_templates.py::TemplateContractTests.test_long_form_templates_allow_only_the_complete_source_document@2eadc7` | **[deferred]** mutate-one-string-assert-raises | delete | Verified by execution: with `_validate_template` stubbed, injecting `<h1>Template title</h1>` before the content token is still caught by F-012, which compares the rendered blocks against the source. The observational twin exists. |
| F-034 | `website/tests/test_site_templates.py::TemplateContractTests.test_service_ctas_reject_hidden_ancestors_and_icon_bypasses@b24a4e` | **[deferred]** mutate-one-string-assert-raises | delete | Twelve mutations per template hiding service CTAs via `hidden`, inline style, an injected `<style>`, `aria-hidden`, and extra icons. Defends an in-tree template against deliberate concealment, and that guarantee cannot be held by a test living beside the template. |
| F-035 | `website/tests/test_site_templates.py::TemplateContractTests.test_reviewed_shell_text_cannot_come_from_hidden_or_structural_descendants@b24a4e` | **[deferred]** mutate-one-string-assert-raises | delete | Six mutations sourcing reviewed shell text from hidden, `aria-hidden`, `<template>` and `visibility: hidden` descendants. Same adversarial shape as F-034. |
| F-036 | `website/tests/test_site_templates.py::TemplateContractTests.test_service_icon_paths_are_exact_single_direct_children@bad42e` | **[deferred]** verbatim pin (SVG path data) | delete | Pins the exact `d` attribute of each service icon against `site_builder.SERVICE_ICON_PATHS`, then six mutations around it. Authored artwork; a wrong icon is a review finding, not a test finding. |
| F-037 | `website/tests/test_site_templates.py::TemplateContractTests.test_rocket_inventory_rejects_unclassified_extra_and_misplaced_images@b24a4e` | **[deferred]** mutate-one-string-assert-raises | delete | Rocket-image inventory mutations: extra, unclassified, and misplaced marks. The built-page assertions in F-011 (header mark count and empty `alt`) and the same test (the hero mark's `src` and `alt`) already hold the placement and `alt` facts that ship. |
| F-038 | `website/tests/test_site_templates.py::TemplateContractTests.test_fragment_scene_svg_cannot_move_outside_its_reviewed_section@db03ae` | **[deferred]** mutate-one-string-assert-raises | delete | One mutation moving the scene SVG out of its section. In-tree template tampering. |
| F-039 | `website/tests/test_site_templates.py::TemplateContractTests.test_fragment_accessible_name_sources_must_be_structural_and_nonempty@151e53` | **[deferred]** mutate-one-string-assert-raises | convert | The property is a real a11y invariant (three accessible names must be non-blank) and blanking a label is a plausible edit. Replacement: on the built Lander page, assert `#lander-game`, `#lander-start` and the scene `<title>` have non-blank accessible names. `test_lander_404.py` already asserts two of the three on the template, so this is one added assertion moved to output. No production change. |
| F-040 | `website/tests/test_site_templates.py::TemplateContractTests.test_fragment_support_and_battery_geometry_fail_closed@b24a4e` | **[deferred]** verbatim pin (SVG coordinates) | delete | Pins literal path coordinates (`M312 457.21999999999997H498`) and battery-bar geometry through the validator. Authored artwork geometry, and the real agreement, that the static SVG matches what the model renders, is held by derivation parity in the `.mjs` suite. |
| F-041 | `website/tests/test_site_templates.py::TemplateContractTests.test_fragment_variants_cannot_duplicate_local_route_destinations@b24a4e` | **[deferred]** mutate-one-string-assert-raises | delete | Verified by execution: with `_validate_template` stubbed, a non-relative `/manifesto/#...` link fails at `_validate_local_references` and an aliased duplicate fails there too. The link-integrity half of the validator, which W2 says stays, is the guard. |
| F-042 | `website/tests/test_site_templates.py::TemplateContractTests.test_shared_game_fragment_has_one_exact_shell_placement@b24a4e` | **[deferred]** mutate-one-string-assert-raises | delete | Verified by execution: with the validator stubbed, removing `{{LANDER_GAME}}` fails F-016. |
| F-043 | `website/tests/test_site_templates.py::TemplateContractTests.test_footer_game_link_is_final_icon_only_and_accessibly_named@6153dd` | **[deferred]** mutate-one-string-assert-raises | delete | Five mutations around the footer game link. The built-page assertions in F-011 hold the same accessible-name and destination facts, and `lander-phase4m.test.mjs` holds them a third time. |
| F-044 | `website/tests/test_site_templates.py::TemplateContractTests.test_shell_title_and_canonical_metadata_fail_closed@3582d8` | **[deferred]** mutate-one-string-assert-raises | delete | Title and canonical-link mutations. The canonical URL per page is asserted on the built output by F-003, and the empty-title case is a review concern. |
| F-045 | `website/tests/test_site_templates.py::TemplateContractTests.test_shell_favicon_is_exact_and_shared@d25f21` | **[deferred]** verbatim pin plus mutations | delete | Carries a copy of the exact favicon `<link>` markup, asserts it appears once per template, and then five mutations of it. Authored markup spelling. |
| F-046 | `website/tests/test_site_templates.py::TemplateContractTests.test_game_shell_description_structure_and_csp_are_shared@aaf904` | **[deferred]** mutate-one-string-assert-raises plus verbatim pin | delete | CSP and description mutations on two templates, plus `policies == [CSP, CSP]`, a third copy of the policy string. The built-page CSP check (F-002) covers all five pages, and the description-presence fact is held by F-003. Note for F-002: this row is the constant's other consumer, so `site_test_support.CSP` cannot be removed while this row is deferred. |
| F-047 | `website/tests/test_site_templates.py::TemplateContractTests.test_duplicate_attributes_cannot_bypass_template_contracts@685099` | **[deferred]** mutate-one-string-assert-raises | delete | Proves the validator's own parser is not fooled by duplicate HTML attributes. Guarding the guard against a bypass nobody is attempting, and the same shape appears in `test_site_build.py`. |
| F-048 | `website/tests/test_site_templates.py::TemplateContractTests.test_security_template_rejects_email_address_reporting_canary@0e743b` | **[deferred]** phrase canary | delete | Injects an email address into the security template and expects rejection. The production GitHub-only reporting contract still runs on every build and is exercised on its real input by F-085; this template-side copy adds a second canary for the same rule. |
| F-049 | `website/tests/test_site_templates.py::TemplateContractTests.test_long_form_tokens_cannot_move_out_of_reviewed_article_or_metadata@b74282` | **[unverified]** **[deferred]** mutate-one-string-assert-raises | delete | Long-form token context mutations, the manifesto and security twin of F-028. |
| L-402 | `website/tests/test_site_templates.py::TemplateContractTests.test_extracted_content_cannot_expand_a_template_token@09b010,TemplateContractTests.test_extracted_content_cannot_expand_a_template_token::ValueError::d71096,TemplateContractTests.test_shell_destination_labels_are_bound_to_their_reviewed_hrefs@e9dbd7,TemplateContractTests.test_shell_destination_labels_are_bound_to_their_reviewed_hrefs::ValueError::b5c755,TemplateContractTests.test_detail_page_headings_reject_provenance_eyebrows@184e7b,TemplateContractTests.test_detail_page_headings_reject_provenance_eyebrows::ValueError::5b0daa,TemplateContractTests.test_long_form_templates_allow_only_the_complete_source_document@2eadc7,TemplateContractTests.test_long_form_templates_allow_only_the_complete_source_document::ValueError::70cc4b,TemplateContractTests.test_service_icon_paths_are_exact_single_direct_children@bad42e,TemplateContractTests.test_service_icon_paths_are_exact_single_direct_children::ValueError::cda99a,TemplateContractTests.test_footer_game_link_is_final_icon_only_and_accessibly_named@6153dd,TemplateContractTests.test_footer_game_link_is_final_icon_only_and_accessibly_named::ValueError::55bf99,TemplateContractTests.test_shell_title_and_canonical_metadata_fail_closed@3582d8,TemplateContractTests.test_shell_title_and_canonical_metadata_fail_closed::ValueError::44f5d9,TemplateContractTests.test_shell_favicon_is_exact_and_shared@d25f21,TemplateContractTests.test_shell_favicon_is_exact_and_shared::ValueError::053233,TemplateContractTests.test_game_shell_description_structure_and_csp_are_shared@aaf904,TemplateContractTests.test_game_shell_description_structure_and_csp_are_shared::ValueError::f837f1,TemplateContractTests.test_duplicate_attributes_cannot_bypass_template_contracts@685099,TemplateContractTests.test_duplicate_attributes_cannot_bypass_template_contracts::ValueError::20a7d7,TemplateContractTests.test_security_template_rejects_email_address_reporting_canary@0e743b,TemplateContractTests.test_security_template_rejects_email_address_reporting_canary::ValueError::39f0fc,TemplateContractTests.test_long_form_tokens_cannot_move_out_of_reviewed_article_or_metadata@b74282,TemplateContractTests.test_long_form_tokens_cannot_move_out_of_reviewed_article_or_metadata::ValueError::8e634c` | **[deferred]** 12 `assertRaisesRegex` sites | delete | Same, over the template validator's authored refusals (`"brace-like token syntax"`, `"reviewed literals"`). Note the interaction: 22 of this file's 25 tests are group 5 delete rows, so most of these regexes disappear with their tests rather than needing a separate edit. **Corrected 2026-08-19 (map re-baseline).** Re-derived at HEAD as an explicit site list: the file changed after this map's basis and one of its 14 sites was already narrowed to a bare `assertRaises` by unrelated work, leaving 12 sites. The old anchor was a range over the whole file, and would also have swept whatever the file grew in between. |

## Stack topology and recipe verification

Recorded for the executors of the sweep's PRs. Like the rest of this file it dies when the sweep
closes.

**The stack topology was re-cut out of this section on 2026-08-19 rather than re-derived.** It was
measured over nine groups with no re-scope subtraction, and both premises are gone, so its numbers
described a sweep that no longer exists. What survives below is the measurement method and the
findings that outlive the counts.

**Operator disposition, 2026-08-19: the re-cut happens at the restart, not in the PR that re-derived
this map.** The restart owns choosing the order, and it inherits the method here plus the group-1
split the group 1 header records.

### What the overlap measurement found

The matrix and the stack diagram that used to sit here were measured over nine groups with no
re-scope subtraction, and both are gone rather than left to mislead: group 2 has no live row, and
the subtraction they were measured across has since been reversed, so every cell in them was wrong
twice over. The measurement's METHOD survives and is what a re-cut should repeat: count only the
files a group actually EDITS, its delete and convert rows, since keeps need no edit, and split group
3 into its four sub-batches.

Four facts it established, three of which do not depend on the counts that went stale:

1. **Group 1 is the hub.** It touched more files than any other group and overlapped every group
   except group 2. It goes at the bottom regardless of judgment weight.
2. **The four group 3 sub-batches have ZERO overlap with each other.** They are genuinely
   independent and never need to stack.
3. **Group 6 is not independent**, which was the standing guess: it shared files with both group 5
   and group 1. Group 3c was the closest thing to independent, overlapping group 1 alone.
4. **Group 4 carries the rework risk.** Group 3 holds more converts in total, but it lands as four
   PRs and no one of them approaches group 4, so **group 4 is the largest single PR by converts**
   and the likeliest to need rework. That is why the old stack put it at the top with only leaves
   above it. Whatever order the re-cut chooses, nothing should be built on group 4 that a rework of
   its converts would drag with it.

### Recipe verification

Every distinct convert recipe was executed against a sample, in both halves: it must PASS against
unmodified production AND FAIL when the invariant it claims to guard is broken. A conversion that
passes but never bites is the failure this effort has hit repeatedly, so the second half is the one
that counts.

**55 distinct recipes. The nine listed below did not hold as recorded; the other 46 verified.** Zero
required a production change, so R2.2 is clean throughout.

**Corrected 2026-08-19 (map re-baseline).** This paragraph read "44 verified as recorded, 11 did not
hold" over a list of nine, so the three figures could not all be right. The list is the evidence and
the counts now follow it. Re-running the verification to decide whether 55 or 9 was the wrong number
was outside the re-baseline's reach, so if two further failures existed they are unrecorded and
their rows carry the verified recipe.

A second sentence here used to add that nine convert rows became deletes and fourteen kept their
conversion. It is gone rather than footnoted: it did not reconcile with the nine failed recipes,
which name sixteen rows between them, and no record this file carries says which population it
counted. The row dispositions are authoritative.

The nine that did not hold:

| Recipe                                                   | Rows                | What execution showed                                                                                                                                                                                                         | Resolution                                                                                                                         |
| -------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Hermetic provisioning via a `register_targets` spy       | L-116 to L-119      | **The spy is blind to half the family.** L-118 and L-119's guards name `resolve_for_command`, a free function at `secrets/orchestration.py::resolve_for_command`; a live walk injected into `create_agent` left the spy green | Corrected for all four: declare an unresolvable operator-env secret and assert provisioning still completes. Bites both mechanisms |
| Tailscale legacy var, observed at the resolver           | L-114               | Bites a resolver-level reintroduction but MISSES one inside `create_vm`, which is where the removed code lived                                                                                                                | Corrected to drive `create_vm` end to end; bites both sites                                                                        |
| `warnings != []` on the reconcile SVE path               | C-153               | Both branches emit exactly one warning, so it passes when the behavior regresses                                                                                                                                              | Withdrawn; the row is a `delete` and the sibling `/proc/cmdline` assertion already discriminates                                   |
| `FieldDoc` record read                                   | D-067               | Reads the declaration straight off the model, so no break makes it fail                                                                                                                                                       | Withdrawn; the row is a `delete`                                                                                                   |
| `union_scalar_shorthand_error` for family discrimination | D-137               | Returns `None` for the fixture, so the replacement never bites                                                                                                                                                                | Corrected to `reference_marker_error`                                                                                              |
| Retire the duplicate manifest constant                   | F-050, F-088, F-145 | Production enforces the assertion unconditionally, so it can only fail via a `RuntimeError` in `build.py`                                                                                                                     | Withdrawn; all three are `delete`                                                                                                  |
| Assert the a11y attributes                               | F-007, F-093        | Same: enforced unconditionally at build time. Both justifications were factually wrong                                                                                                                                        | Withdrawn; both are `delete`                                                                                                       |
| Derive the SVG fragment contract                         | F-101               | Same                                                                                                                                                                                                                          | Withdrawn; `delete`                                                                                                                |
| Consolidate the runtime-surface scan                     | F-018, F-098        | The consolidation drops `.html` coverage, and deleting the canaries removes the only non-vacuity guard (a typo'd pattern made the scan pass green)                                                                            | Settled as KEEP for both, 2026-09-06: neither the merge nor the delete is safe                                                     |

**The trap-carrying recipes owe every site they cover a screening, and that debt is still open.** It
was recorded in the #573 round as a gating condition on the PRs carrying those conversions rather
than something that round closed, and the re-baseline neither closed it nor carried it forward until
now. The number of sites is not derivable here, because a recipe's coverage is a property of the
replacement rather than of any anchor, so this is stated as the operator's precondition on those PRs
and not as a figure: the PR carrying a trap-carrying conversion samples the sites it has not
sampled, and says how many that was. It is NOT superseded by the callee-side raise screen: that
screen asks whether a delete row's raised type discriminates, and this asks whether a convert row's
replacement actually bites at every site the recipe covers. Different question, different rows, both
open. Treat it exactly as the screen debt above: per-site, owed by the PR that carries the
conversion, and paid by sampling the unsampled sites rather than by re-reading the recipe.

Four verified recipes carry a trap, each of which was silently vacuous on the first attempt. These
are recorded on their rows because a reviewer cannot see them in the diff:

- **L-120, L-121**: every provisioning runner is configuration-gated, so under a default template
  the assertion covers nothing and the first break PASSED. The fixture must turn every gated runner
  on, which additionally needs a real local source directory and a `copy_dir_to` on the fake
  transport.
- **L-202**: a duplicate-line counter keyed on the current filename PASSED while the invariant was
  broken, because the appended duplicate carries the NEW name. The counter must be
  filename-agnostic.
- **F-117**: the token regex also matches `--fuel-level-color: var(...)`, so the loop can iterate
  over nothing and assert nothing without an explicit non-vacuity assertion.
- **Every warning presence or count assertion**: the screening question is whether the HEALTHY
  branch emits zero warnings. C-153 is the row where the answer was no, and it is why that recipe
  was withdrawn rather than reused.

Two verifications also graded the guards being retired, and both came out in the replacement's
favour. Breaking the `_ensure_agentworks_files_sourced` call site left the ORIGINAL source-scanning
test passing, because the surviving import line satisfies its substring scan: the grep cannot tell
an import from a call. And `test_tailscale_legacy_removed.py`'s `_MODULES_TO_SCAN` lists
`agentworks.secrets.resolver`, but no test in that file ever scans it, so that entry is dead and the
greps miss a resolver-level reintroduction outright.

One methodological note worth keeping while the sweep runs: a break that is not a FUNCTIONAL
reintroduction proves nothing. Two first attempts bit nothing because an earlier line raised first,
which looks identical to a recipe that does not work.

## Sequencing between the groups

Four constraints bind the order the sweep's PRs land in, beyond the plan's existing ordering against
the `phase7` item, the contained cli items, and P5.

1. **Group 1 lands first, and no other group may lean on it.** A `match=` is a fallback that is
   about to be deleted. Where a row in group 3 or group 4 justified deleting a prose assertion by
   pointing at the `match=` on the same `raises`, that justification expires the moment group 1
   lands. Those rows were re-read and now cite the surviving `raises` type or a structural sibling
   instead. Hold any new row to the same rule.
2. **Group 2 sits downstream of the guide work item.** Resolved rather than binding: the guide item
   landed as `4ac084cd` and deleted the whole estate, so no group 2 row survived into this cut and
   there is no group 2 PR to order.
3. **Group 5 carries the W2 question**, which is an ordering decision rather than something this
   inventory can settle. See the open questions.
4. **Group 3 is too big for one PR.** It is the judgment-heavy batch and the largest by a wide
   margin. The natural cut is by subsystem inside the one shape (consoles and sessions; workspaces
   and agents; vms and platforms; the rest), which keeps the review's judgment uniform while making
   each round reviewable.

## Overlaps found

Per the plan, the sweep records an overlap, keeps the file, and raises the ordering.

**Re-derived 2026-09-06 at `c310d05b`, and three entries went.** They named
`cli/tests/guide/test_release_notes.py`, `cli/tests/guide/test_contract_catalog.py` and
`cli/tests/secrets/test_resolution_lifecycle.py`, none of which exists at this basis, so the
orderings they raised have nothing left to order. The first also carried a claim that was wrong on
its own terms: `cli/tests/test_completions.py:163-171` is not "the same four assertions" written
twice. At this basis those lines are a loop asserting that every declared completer id has a bash
snippet mapping, with its own failure message, which is a different property from anything the
guide-side file held. What remains was each checked against the basis.

1. **P5 (gcp fixture extraction)** shares five files with this sweep's gcp rows. The plan already
   orders the sweep first. One fact for whoever runs P5: the E-093 replacement asserts through the
   public `manual_cleanup_guidance`, so it does not touch the per-file `_api_error` or
   extended-operation fakes and rebases cleanly under the extraction.
2. **P4 (gcp's five-class error taxonomy)** is load-bearing for roughly fifteen delete rows that
   rest on "the raised type already discriminates". Nothing in production dispatches on those
   subclasses today, so after this sweep the tests become the taxonomy's only consumer. If the
   reassessment collapses it toward the sibling shape, that branch coverage goes too. Recorded for
   R4.
3. **D2 (deriving `SCHEMA_SENTINELS` by replaying `MIGRATIONS`)** would rewrite
   `cli/tests/test_database_backup.py:61-79`, which this inventory keeps as derivation parity. If D2
   ever runs, that test is its territory.
4. **The CLI grammar rewrite** (saga `phasing.md` puts it directly after wave 1) churns exactly the
   rows that pin the command tree and per-flag help. They are deletes either way, so landing them
   before the rewrite strictly reduces its diff.
5. **`cli/tests/test_ssh_config.py:132-151`** is the unit home of the `"SSH config synced"` line
   that a group 3 row deletes at the orchestration level. Both are inventoried; they should land
   together.
6. **`cli/tests/capabilities/test_secret_backend_conformance.py`** is the same family as
   `capabilities/test_conformance.py` and was never on the absorbed survey's list. Inventoried here;
   recorded so the reassessment knows the survey's file list was not exhaustive.
7. **The website overlaps.** `test_site_documents.py` has two owners, the sweep and the website
   trims item, and the rows here are the sweep's half; `attribute` says which row owns each site, so
   the boundary is checked on every run rather than described once. Two orderings the plan did not
   have:
   - W4's conversion did not reach
     `test_lander_404.py::StaticDocumentTests.test_fixed_color_contrast_meets_text_and_graphic_thresholds`,
     which still carries `math.isclose(antenna, 3.788, abs_tol=0.001)` and palette hex literals
     restated from `lander.css`. The lead may prefer this ride with W4's pattern rather than with
     the sweep.
   - **New**: the W8/W10 boundary. The plan assigned "the W8 browser row" (singular, 636-653); the
     three chromium-harness cleanup tests at 655-823 are separate tests from the 2026-08-15
     reliability correction and are inventoried here. If the trims item considers the whole harness
     family its own, those two rows move.
   - **New**: W6 residue. `lander-model.js` still duplicates a status literal between two of its own
     branches, but no test asserts it, so there is no sweep row and it stays a website-source
     defect.

## Open questions

Consolidated here because a question dropped at the end of a commit message is a question nobody
answers. Most are now closed; what remains is listed last.

### Ruled by the operator, 2026-08-16

Both escalations came back, and the first came back as a rubric rather than a row decision, so it
was applied across the whole inventory rather than to the three rows that raised it. The rubric is
recorded once in [hla.md](hla.md)'s doctrine 2, beside the `match=` taxonomy:

> **Keep where the assertion is the only probe for a behavior that has demonstrably regressed or
> plausibly can. Delete where the assertion's only failure mode is a rewording.**

1. **The workspace-repair report family keeps** (B-041, and B-043 and B-044 with it, since they turn
   on the same observable). These assertions track a DETECTION OUTCOME, not a sentence: whether a
   repair step found something to converge. `repair_workspace` returns `None` and convergence is
   idempotent, so nothing else observes it. Each row states what the keep costs, which is that it
   breaks when someone rewords `OK:` or `Fixed:`, so no later reader mistakes it for a prose pin
   nobody noticed.
2. **The `test_site_templates.py` rows defer to R4** rather than landing either way, and they are
   the whole of the deferred block the Totals table counts. The reason is an asymmetry: if R4
   deletes `site_validation.py`'s element-by-element half, these tests die with it at no cost, and
   if R4 keeps it, deleting them now permanently removes its negative-path coverage. They are moved
   out of the executable set into the deferred block below, so an executor scanning for delete rows
   cannot pick them up. The operator's question about whether they police authored template form
   rather than rejection behavior is deliberately not chased; R4 inherits it with the rows.

**Applying the rubric to every delete row, and then verifying every convert recipe, moved the
dispositions substantially.** The split that came out of that pass was 564 delete, 225 convert, 357
keep across 1,146 executable rows; the Totals section carries the current figures, which the
2026-08-19 re-baseline re-derived. The rows that moved are the coverage this sweep would otherwise
have destroyed quietly, and each says which behavior it is the only probe for.

### Ruled by the effort lead, 2026-08-16

Each was ruled for the family rather than the row, and the rows now carry the ruling.

1. **A shipped security header is behavior, not form.** Its security-relevant directives are the
   invariant; the header's exact text is not. F-002 converts to directive-level assertions (that
   `script-src` carries no `unsafe-inline`, and so on), and the verbatim pins plus the
   `site_test_support.CSP` second copy goes with F-145. F-046 held a third copy and is deferred with
   the W2 block, so the constant itself survives this sweep by one consumer.
2. **A style rule mechanized as a test is not a doctrine 2 threshold.** Style belongs in the linter,
   and a rule enforced by one package's suite is enforced inconsistently anyway. F-067 deletes, and
   the row says that the 1,000-line ceiling should move to `scripts/lint-files.sh` if it is worth
   keeping, rather than being lost.
3. **Formatter-output equality splits on derivation.** Keep where the test proves a derived copy
   still matches its canonical source, which doctrine 2 names as a keep shape, and delete where it
   pins the formatter's output as text; where a row is genuinely both, keep it and name the
   canonical source. This split the `test_resource_render.py` row in two, C-087a and C-087b (the
   label equalities delete, the assertion that the origin's file and line reach the rendered cell
   stays) and made C-165 and C-167 name theirs.
4. **Union-collapse cardinality converts.** `len(splitlines()) == 1` is a structural claim about
   output shape rather than about wording, it is cheap, and it fails when the collapse actually
   breaks. Verified per site: three of the eight carry the cardinality claim and convert (D-127a),
   the other five carry only authored normalization and delete (D-127b).
5. **A subtraction pass does not add coverage opportunistically.** R2.3 permits an addition only
   where a real invariant loses its ONLY guard in the same change. The two rows this was decided
   against, over CLI resolution, were not that case, because the invariant such a test would add has
   no guard today and so loses none, so both became plain deletions carrying a recorded drift risk.
   Their estates went with `62d42215` and the rows went with them, so the ruling outlives the rows
   it was made about; it is kept because the next row of that shape gets the same answer.
6. **The graph-guard reservation is accepted**, and it corrected `hla.md` rather than just this row.
   Banned pattern 2 governs what a module may reach rather than how the reaching is spelled, and it
   has no observational twin available even in principle. That detector stays; the other three
   patterns go. Rows L-101a and L-101b carry the split, and `hla.md`'s delete bullet says so too.

### Raised by the fresh cut, 2026-09-06

These are the operator's, not the sweep's: each is something the re-derivation found in passing that
no row of this map fixes.

1. **Sequencing the sweep against instance-model's open gate.** The 2026-08-19 subtraction is
   reversed, so instance-model's seven files are back in this sweep's scope. That effort is a lock
   candidate with live VM validation still open, and its plan's own commitment was to trim these
   tests itself. Whether the sweep's edits to those files wait on that gate, land before it, or land
   beside it is the operator's call, and here is what it is a call about:
   - `cli/tests/db/test_instance_state.py`: D-171, E-197, E-214
   - `cli/tests/db/test_read_transaction.py`: no rows
   - `cli/tests/test_db.py`: G1-079, L-002
   - `cli/tests/test_db_migration_harness_integration_state.py`: A-077, A-078, A-079
   - `cli/tests/test_db_migration_harness_state.py`: no rows
   - `cli/tests/test_db_migration_vm_sites.py`: A-074, A-075, A-076, G1-080
   - `cli/tests/test_instance_specs.py`: D-164, E-191, G1-090

   Sequencing against a list is possible; sequencing against a count is not, which is why the files
   and the ids are here rather than a total.

2. **A live coverage gap in `cli/tests/test_config.py`.** Three assertions at lines 285, 311 and 326
   match on `does not follow the naming rules` while production says
   `does not follow the secret naming rules` at `manifests/decode.py:367-371`. They are vacuous
   today: the needle cannot match, so the tests pass whatever the code does. C-097's convert fixes
   it when that row executes, but the gap exists now and is worth knowing about before then.
3. **`cli/tests/test_subprocess_io.py:5-7` says CI runs Linux.** It has been stale since PR #760
   added the `windows-latest` job, and a comment that tells the next reader the wrong thing about
   the CI matrix will mislead someone deciding whether a platform-conditional test can be deleted.
4. **Rows whose assertions were deleted from outside the sweep.** The injected-marker screen ruled
   that `test_readiness.py`'s `match="not a rejection"` should keep, because the needle is a marker
   the test file writes and production emits nowhere. `658ea819`, the secrets-preview effort's
   boundary hardening, dropped the `match=` anyway; the fake that raises the marker is still there
   at line 80 and the assertion that observed it is not. B-003 and B-004 went the same way:
   `d99835da` removed the banner pins and all four `Role.*` assertions from
   `test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary`, and both rows retired in this
   cut. That pair also shows the limit of a span anchor. The fresh cut rescued them through a file
   rename and recorded the rescue as evidence unchanged, but a span resolves on the function
   surviving and says nothing about the assertions inside it, so what the rescue verified was that
   the file had moved. Only a site anchor carries the assertion itself, and groups 3 to 6 have none.
   The rows died with their sites in this cut, so nothing here records the losses except this note.
   Whether to restore any of them is the operator's call.
5. **A convert's premise realized badly.** C-129 converts empty-state message pins because deleting
   them outright would leave assertion-free tests. One of its sites has already gone that way:
   `test_start_all_sessions_valid_filter_empty_result_succeeds` in
   `cli/tests/test_name_filter_validation.py` lost its `captured_output` fixture and its assertion
   together and now asserts nothing at all, while its `stop` and `list` siblings kept theirs.
6. **CI cannot say which tests skip.** The suite runs without `-ra` or `-rs`, so a run's log reports
   `8456 passed, 6 skipped` and never which six. That is why F-149b keeps rather than deletes: the
   twins that deletion would rest on cannot be shown to run in CI. On a Linux host with zsh
   installed the zsh twin does run and only the powershell twin skips, so the question is narrower
   than it looks and still unanswerable from the log. One flag on the Pytest step in
   `.github/workflows/ci.yml` would settle it, and would settle the same question for every other
   tool-conditional test the sweep meets.

### Still open

1. **Does the synthetic-fixture carve-out cover per-row state markers?** **E-048** keeps the
   rendered disabled, not-ready-but-enabled and ready markers on that basis, and the marker strings
   are authored. The lead let the recorded decision stand rather than overruling it without the row
   in front of him, so it is recorded as decided-by-default rather than settled. **Corrected
   2026-09-06:** this item read "two rows" and named neither. E-048 is the only row at this cut that
   keeps a rendered state marker on the carve-out; the other carve-out keeps (C-063, C-086, C-088)
   are color and layout, not state markers. The count is replaced by the id so a reader can check
   it.
2. **Are `agentworks-manual-*` / `agentworks-pre-migration-*` backup filenames a documented operator
   interface?** If they are, one pin is a real contract check rather than a naming restatement.
3. **CLOSED 2026-09-06: a reference record's `usage` is both, and the split is presence against
   value.** B-021 asked whether `usage` is a shipped data field or authored prose. Execution
   answered it: the reference's identity is already pinned by its kind and name two lines above,
   every consumer displays `usage`, and the only code that tests it tests presence. So presence is
   the shipped contract and the sentence is prose we render, and B-021 converts to a non-empty
   check. Recorded here rather than deleted because the same question will be asked of the next
   record field, and this is the shape of the answer.
4. **The digest cannot see an assertion a test hands to a helper.** Nine anchored functions digest
   as `e3b0c4` over nothing, and many more have a real digest while still delegating part of their
   assertions to an `assert*`-named helper. Following a call into a helper is a whole-program
   question this grammar declines to open, so those rows are guarded on what their test asserts
   directly and on nothing else. Whether that is worth closing is the operator's call.
5. **`ast.unparse` stability across a Python minor is unverified.** The digests were stamped on 3.12
   and `resolve`, `restamp` and `totals` refuse a newer interpreter rather than reporting every
   anchor as `changed`. Nobody has tested whether 3.13 actually spells these trees differently. If
   it does not, the ceiling can be relaxed; if it does, the upgrade is a re-stamp and the guard
   already says so.
6. **F-018's pattern list has no canary of its own** and no pattern at all for two of the eleven
   surfaces `test_lander_404.py` guards, location navigation and history mutation. The row keeps and
   names the gap; closing it is an edit to the test rather than a decision.
7. **The doctrine says twelve rules and `frd.md` carries eleven.** `hla.md` and `plan.md` both say
   twelve, and both take it from `frd.md`, which is the operator's document: it lists eleven rule
   files, one of them carrying globs rather than a single rule, and there is no `always-consider-*`
   file at all. Whether the count is wrong, the file list is short, or "rules" means something other
   than files is the operator's to say; this map does not correct another effort's artifact and
   records the discrepancy instead.
8. **Deletes whose coverage claim was reasoned rather than executed.** A delete row that says the
   coverage lives elsewhere is only as good as that claim, and the verification lane executed a
   sample of them: it was wrong about one in five. So the marker is now carried by every row of that
   shape rather than by a hand-picked few, and `[unverified]` keeps the meaning it had, that the PR
   carrying the row executes its claim before the edit lands.

   **Operator disposition, 2026-08-19, still standing: the tag is the gate**, and these rows go to
   their execution PRs with it.

   The population is derived, and here is exactly how, so it can be re-run rather than believed. It
   is every delete row whose justification cell contains any of `asserted elsewhere`,
   `already covered`, `already covers`, `already asserts`, `covered by`, `in the same test`,
   `the sibling`, `restates`, `restate`, `duplicates`, `duplicate of` or `twin`, minus the rows
   whose justification also says `verified` or `by execution`, minus the rows a lane has executed.
   The phrase set is a judgment about wording rather than a measurement, and it over-selects on
   purpose: a row it catches that rests on no coverage claim costs its executor one wasted check,
   while a row it misses ships an unexecuted claim.

   **The executed rows, named so the subtraction is checkable rather than asserted.** These rest on
   a coverage claim that somebody broke the coverage to test, so they owe nothing and carry no
   marker: G1-C01, G1-C02, G1-C03, G1-C04, D-169a, D-169b, E-208, F-149a, F-181, F-184a, F-184b,
   F-194, F-195, F-196 and F-197. A row not on this list and not carrying the marker is a row whose
   justification the phrase set did not catch, which is the gap this population knowingly has.

   **Two marked rows the recipe does not select: C-094 and C-108.** They carried the marker before
   it was derived, on the earlier hand-picked basis, and they keep it: their claims are genuinely
   unexecuted and the phrase set simply does not reach their wording. Re-running the recipe
   therefore returns two fewer ids than the map marks, and that difference is these two rather than
   a drift.

   **Corrected 2026-09-06.** This item read "four rows, `C-005`, `C-025`, C-094, C-108" while only
   C-094 and C-108 ever carried the marker: `C-025` never had it and `C-005` is not a row in this
   map at all. That is exactly the failure the fresh cut's citation check now refuses, and it is why
   the count here is the marker count rather than a list maintained beside it.

## Corrections to lead-owned artifacts

Re-derived at HEAD and contradicting `findings.md` or the plan's estate enumeration. Flagged rather
than fixed, because those artifacts belong to the effort lead. The two marked **applied** were
carried into `hla.md` on 2026-08-19 under the effort lead's explicit authorization for exactly those
edits; everything else here is still a flag.

- **The #470 manifesto pin, which the plan names as part of this sweep's estate, is already gone.**
  Commit `8043d438 refactor(guide): remove command-owned fact views` replaced the verbatim
  manifesto-text pin with a structural link assertion, and the file that carried it,
  `cli/tests/guide/test_authored_coverage.py`, is not in the tree at this basis either.
- **G12's counts are stale, and its subject is gone.** The `test_migration_topic.py` required-phrase
  list was 30 entries at this inventory's basis, not 47 and not the 31 this file claimed until the
  2026-08-19 re-baseline counted the tuple; `test_render_service.py` had one prose-shaped line
  rather than "scattered pins". Both files were deleted by `4ac084cd`, so the finding is historical
  and its rows went with it.
- **C10's `cli/tests/capabilities/test_retired_shapes.py` no longer exists.** It and its production
  module were both deleted by `ab0a6303 feat(config)!: remove retired compatibility rewrites`, so
  the "three blacklists redundant with its own structural tests" item is moot and its redundancy
  claim could not be tested.
- **C10's `test_capability_shape.py:21-32` anchor is stale.** Those lines are a helper and a
  `parametrize` opening at HEAD, and every message assertion in that file is a `match=` site, so the
  finding resolves into group 1 rather than into its own rows.
- **APPLIED. `hla.md`'s site counts were both wrong**, and it no longer states any: `match=` is
  under `cli/tests` and none under `website/tests`, after the wave 1 landings and the one site the
  Grok Build integration added, where `hla.md` said 663. The website suite carries 49
  `assertRaisesRegex` sites, not the 51 it recorded. And nine further regex-family sites in that
  suite were in neither count, one `assertRegex` and eight `assertNotRegex`, all of them already
  covered by group 5 rows. All three corrections are now in `hla.md`.
- **APPLIED. `hla.md`'s case 1 needed the callee-side condition.** "The raised type already
  discriminates" is a claim about the callee, and the map established it from the call site, which
  does not follow. The callee-side raise screen found the single-path premise false for roughly four
  in five resolvable sites. Case 1 now carries the condition and points at the screen.
- **The absorbed purge FRD's R4 item 2 is already satisfied and needs no production change.**
  `agentworks.schema.errors._problems` exposes `path`, `unknown_field`, `alternatives` and
  `union_path`, and `cli/tests/schema/test_errors.py` already imports and asserts on it. That is why
  group 4 carries so many converts: the structural seam the purge wanted promoted exists.
- **The absorbed survey's file list was not exhaustive.**
  `capabilities/test_secret_backend_conformance.py` is the same family as
  `capabilities/test_conformance.py` and was unlisted; the overlaps section carries it.

## The tooling that retired with this cut

`carry` and `reanchor` are gone, and the `Tree(ref)` layer that let either read a historical commit
went with them. Both existed for one job: turning a map's line numbers into identities, and moving
an older map's evidence into a newer one. No row here addresses anything by line, and none cites
anything by line either, so there is nothing left to lift and nothing left to carry. The eighteen
line anchors that remain are declarations that a row could not be named, which `resolve` reports and
`reanchor` never wrote. `resolve` answers survival on its own now, by identity, against the tree in
front of it.

What remains is `estate`, `attribute`, `injected`, `screen`, `resolve`, `generate`, `restamp` and
`totals`, and all eight read the working tree only. `restamp` also WRITES it, the only one that
does: it brings every span anchor's assertion digest up to date, which is how a PR that edits an
anchored test carries the map with it. The commands that could read history are in git history,
which is where a reader who needs to re-run the 2026-09-06 cut against its own basis will find them.
