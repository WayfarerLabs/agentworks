# Sweep Decision Inventory

**This is a working artifact for the duration of the prose-and-form-policing sweep, and it is
deleted when the sweep closes.** That is the effort lead's call and it is deliberate: this is a
subtraction pass, and a several-hundred-row ledger that outlives its purpose is exactly the kind of
thing the pass exists to remove. Where a keep row's justification is durable, it belongs at the test
site, not here. Nothing else in the repository should link to this file, and
[sweep-screen.py](sweep-screen.py) with the [sweep_screen](sweep_screen) package behind it, which
derive this file's group-1 estate, resolve its rows' anchors, and run its two screens, are deleted
with it.

It is step one of the sweep work item in [plan.md](plan.md). It decides nothing else: no deletion
lands with it. The deletions follow as the PRs the groups below are cut for: nine when the map
settled, eight after the 2026-08-19 re-baseline emptied group 2.

## Basis

Derived at `c686cd6d` (the merge of PR #559), after the wave 1 work that landed before the sweep,
namely PRs #546, #548, #559 and #560. Every `file:line` was read at that commit. Line numbers drift
as the deletion PRs land, so a later reader should treat them as anchors and re-derive, as
[findings.md](findings.md) says of its own.

**Re-derived at `426cccae` on 2026-08-19**, against the post-#573 audit and the operator's re-scope.
Four things came out of it and each has its own section: the completeness re-scan, the re-scope
subtraction, the callee-side raise screen, and a Totals section counted off the row markup rather
than carried forward. Every row-level correction is marked in place, so
`grep 'Corrected 2026-08-19'` lists them all. The whole of group 1's estate was re-derived and every
one of its rows now resolves at HEAD; rows in other groups were re-derived only where the
re-baseline touched them, so a line number elsewhere may still be a `c686cd6d` anchor.

**Re-anchored on 2026-09-06**, mechanically, at `426cccae`. Every row now keys what it addresses by
identity rather than by line, per the grammar below, and nothing but column 2 and the line-anchored
markers moved: no disposition, justification or id changed and no site was reclassified.

**The rows' own basis is what limits this, and the fresh cut owes work because of it.** Group 1's
line numbers were `426cccae` readings, so its anchors are exact. The other groups were re-derived
only where the 2026-08-19 re-baseline touched them, so some of their span anchors were lifted from a
number that had been read at `c686cd6d`, and a number read at one tree and lifted at another names
whatever function now sits there.

**Measured 2026-09-06**, by lifting the pre-rewrite map (the `96aa6617` cells, with B-116 corrected
by hand because no parser could read its two-file cell) at `c686cd6d` and at `426cccae` and
comparing what each named: **987 rows agree, 144 disagree, 84 are not applicable** because the file
is absent at a base or is not Python. That measurement is history, not a live check; the command
that produced it was deleted with this round, because it answers a question about a map that is
about to be replaced.

**Do not read the 144 as loud failures.** Of them, 125 resolve cleanly at HEAD, which is exactly the
quiet case: the anchor names a real test, so nothing reports anything, and whether it names the
RIGHT test is not a question any tree can answer. 11 resolve nothing and 8 are mixed. Group 1 is not
at risk, because its basis is known; the exposure is the non-group-1 rows.

**The recourse is re-derivation, not repair.** The fresh cut re-derives from scratch every
non-group-1 row in the 20 files below, rather than carrying their anchors forward, so the mixed
basis dies with the cut instead of being audited row by row. Eight of these files disagree on every
row asked, and four of them hold 93 of the 144.

| File                                                     | Rows asked | Disagreeing |
| -------------------------------------------------------- | ---------: | ----------: |
| `cli/tests/test_completions.py`                          |         36 |          34 |
| `website/tests/test_site_templates.py`                   |         26 |          24 |
| `website/tests/test_site_documents.py`                   |         24 |          20 |
| `website/tests/test_site_content.py`                     |         16 |          15 |
| `cli/tests/test_doctor_env_and_secrets.py`               |          6 |           6 |
| `cli/tests/test_config.py`                               |          7 |           5 |
| `cli/tests/test_secret_describe.py`                      |          8 |           5 |
| `cli/tests/test_resource_edit.py`                        |          8 |           5 |
| `cli/tests/test_codex_integration.py`                    |          7 |           4 |
| `cli/tests/manifests/test_samples.py`                    |          8 |           4 |
| `cli/tests/secrets/test_resolution_lifecycle.py`         |          3 |           3 |
| `cli/tests/sessions/test_singular_batch_orchestrated.py` |          5 |           3 |
| `cli/tests/test_doctor.py`                               |          7 |           3 |
| `cli/tests/test_claude_code_integration.py`              |          2 |           2 |
| `website/tests/test_lander_phase4j_browser.py`           |          2 |           2 |
| `website/tests/test_lander_phase4k_browser_cleanup.py`   |          2 |           2 |
| `cli/tests/test_resource_list.py`                        |          4 |           2 |
| `cli/tests/test_resource_kinds.py`                       |          1 |           1 |
| `cli/tests/test_list_truncation.py`                      |          1 |           1 |
| `cli/tests/manifests/test_schema_command.py`             |          1 |           1 |

### Reading this file mechanically

Several passes have counted this file and disagreed, always on how it was parsed rather than on what
it says. **[sweep_screen/inventory.py](sweep_screen/inventory.py) is the reference implementation of
the grammar**, so count with it rather than re-deriving one: `sweep-screen.py resolve` puts every
anchor here at its current line, and `sweep-screen.py attribute` checks the ownership claim below.

**Column 1 is the id, column 2 what the row addresses, column 3 the shape, column 4 the disposition,
and column 5 the justification.** The mechanical batch has no column 5, so it is four columns wide.
Every row state lives in column 3 as a bold marker, so an audit is "split the row, read column 1 and
column 3" with no heading state to track and no prose to read. Cells split on **unescaped** pipes
only, since `\|` inside a code span is content and a naive split turns one row into three, and only
a `##` heading changes the group, since the pulled-out blocks are `###` subsections of the group
they sit in.

#### Column 2 says what a row addresses, never where it sits

**This map has twice been killed by line numbers.** Each cut anchored every row to a line, an
unrelated edit above it moved every number in the file, and the map went on claiming sites it no
longer pointed at, silently, because a stale number still lands on something. A number cannot be
made stable, so it is no longer the key. Four anchor forms, in order of precision; a row lists as
many as it addresses, comma-separated, in one backticked group per file.

| Form                             | Addresses         | Resolves to                |
| -------------------------------- | ----------------- | -------------------------- |
| `path::qualname::Type::digest`   | one site group    | that group's current lines |
| `path::qualname::Type::digest*3` | a group of three  | the same, three sites wide |
| `path::qualname`                 | one test function | that function's line span  |
| `path::L120,L204-211`            | literal lines     | nothing; go read them      |
| `path`                           | the whole file    | the file's existence       |

`::` separates the fields of one anchor and `,` separates anchors, so a group is one string a reader
can copy whole. Neither separator occurs inside a path, a qualname, a type name or a hex digest,
which is what makes the split unambiguous, and the `L` prefix is what tells a line anchor from a
qualname, since no test is named `L` followed by digits. A cell naming two files carries two
backticked groups; a tail with a second path in it is refused rather than guessed at, which is how
B-116 lost `test_secrets_resolver.py` for three cuts.

**The site identity is four fields, and none of them is a position.** `path` is the test file.
`qualname` is the innermost enclosing function, dotted through any class or nesting. Mind the
separator: that is a dotted path (`TemplateContractTests.test_favicon_is_exact_and_shared`), not
pytest's `::` node-id spelling, so neither can be pasted into the other. `Type` is the asserted
exception or warning name exactly as written, or the assertion method's own name where the call
takes no type (`assertRegex`, `assertNotRegex`). `digest` is the first six hex of sha256 over the
extracted match template, which is the needle with its interpolations blanked, so the site is keyed
on what it matches; six is the shortest prefix that separates every distinct needle in this estate,
five colliding once and four twice.

**Sites that tie on all four are one identity, not several.** An earlier cut separated them by
1-based source order, which reintroduced the exact drift this grammar exists to retire: inserting an
identical assertion ABOVE a tied site reassigned the existing row to the new site, and nothing said
so. Order cannot be made stable. Identical assertions in one test carry identical evidence and a
row's disposition applies to all of them alike, so the identity names the whole group, a row claims
every site in it, and `*n` records how many the row was cut against. A group that has since grown or
shrunk then resolves as `grown` or `shrunk`, with both counts, rather than quietly covering a
different number of assertions than the row was written against.

**The tie population is small and named.** At `a64b1b9c` the estate is 678 sites over 669
identities; the 8 identities that name more than one cover 17 sites, each a test asserting the same
needle against the same type twice. `sweep-screen.py estate` prints them, and `Snapshot` refuses to
build unless the groups cover every site exactly once.

**The states an anchor resolves to.** `resolved` is what the row was cut against, at whatever line
it now sits. `grown` and `shrunk` are a site group that changed size, with both counts. `retargeted`
is the same test asserting the same type against a different needle, which is what a reworded
message leaves behind. `gone` is the file present and the anchor not in it, `file-gone` is the file
itself, and `line-anchored` is a line anchor, which resolves to nothing by construction. `carry`
holds two trees and so adds its own labels on top of these; its docstring is where those live,
because they describe a comparison between commits rather than what a row says.

**A site anchor beats a span anchor in the same function, in group 1 only.** Group 1's rows address
`match=` sites, so where a function holds sites such a row cited, those sites are the whole claim: a
span anchor beside them would claim every other site in the same test for the same row, and that is
what keeps "every `match=` site is claimed by exactly one group-1 row" checkable. Every other
group's rows address assertions the estate scan cannot see, so their enclosing function is always
anchored, with any cited sites alongside it. Dropping the span there narrowed 39 rows onto sites
they were never about: 22 in group 5, 12 deferred and 5 in group 4.

**A span anchor is deliberately coarser than the row.** It names the test, not the assertion inside
it, so several rows can share one span anchor and column 3 is what tells them apart. That is the
price of anchoring groups 3 to 6 to a name rather than a number, and it is the right price: a test
name survives every edit above it, while the line does not. It also means a `carries` from `carry`
on such a row says the test is still here, not that the assertion is.

**A line anchor is a declaration that the row will go stale**, so the row says on itself why, in a
`[line-anchored: <cause>]` marker `reanchor` writes into column 3. Four causes, and this map holds
93 line anchors across them:

- **file gone** (66): the file did not exist at the tree the anchors were derived from. 50 of these
  are group 2's whole estate, deleted by the guide rework, and the other 16 sit in live groups
  (group 1: 2, group 3: 1, group 4: 6, group 5: 6, group 6: 1), so this cause is not group 2's alone
  and a reader counting dead rows by it will over-count.
- **not Python** (14): no AST to read, which is the `.mjs` rows that cite lines; the other three
  `.mjs` rows name their file and no lines, so they are file anchors.
- **between functions** (10, and one row that is both): a stale number landing on a blank line, a
  divider comment or the gap after a test's last line, which is the commonest in-tree cause and the
  one that says the number was read at a tree this map no longer matches.
- **module level** (2, and that same mixed row): the lines are real and outside any function, as
  `test_builtin_entries_parity.py`'s `EXPECTED_*` constants are.

`resolve` reports 27 of the 93 as `line-anchored` and the other 66 as `file-gone`, since a line
anchor in a deleted file cannot even be read.

**To write a row by hand**, run `sweep-screen.py estate` and copy the identity it prints for the
site, or `sweep-screen.py resolve` to see what the rows around yours address. The digest is a
tiebreaker nobody needs to read; `estate` prints the needle beside it.

**Two things in column 2 carry no meaning.** Backticks around a group are decoration, not a signal.
And three rows (D-154, D-157, F-128) name further files by bare basename after a leading directory,
so a scan that only matches full paths under-counts what is rowed by sixteen files.

**The row tables carry `<!-- prettier-ignore -->` and are not padded.** Prettier pads every cell in
a table to the widest cell in its column, and an identity is much wider than a line number, so
letting it format these ten tables costs 2.2MB of trailing spaces and puts the file past what an
executor can load. The prose tables in this file are untouched and still padded.

## How a row was decided

The standard is the always-on `no-prose-policing-tests` rule, as generalized by R1.2 to every
authored artifact, plus [hla.md](hla.md)'s doctrine 2, its source-guard split, and its three-case
`match=` taxonomy. [frd.md](frd.md) R2.2 bounds every row: no row proposes a new production type or
a shipped-contract change, so where preserving an assertion would need a production discriminator,
the row is `delete` and the branch coverage goes with it (R2.4).

- **keep** names the invariant that earns the assertion and what regression it catches. A row that
  could only say "this looks behavioral" was not written; it was read again until it said something.
- **delete** says why nothing real is lost.
- **convert** names the structural replacement and confirms it needs no production change.

Two rules narrowed the keeps more than a shape-based reading would.

**A claim about what an assertion protects was executed, not read.** Four deletions earlier in this
pass were correct while their stated premises were false, each caught only by running. So where a
disposition turned on "some other test already covers this" or "production already enforces this",
the claim was tested. The rows where that changed the answer are called out in the report; they are
not marked in the table, because a row's justification should read the same whether or not it was
the one that surprised us.

**A check is only worth keeping against a guarantee the artifact can hold.** For anything in this
tree, the threat model is accidental regression, never adversarial tampering: whatever can edit the
thing under test can edit the test beside it. That is the correction W1 arrived at after four
rounds, and it is why no row here is justified as proving that nobody can subvert something.

### Two screens every delete row runs first

The mechanical batch's shared justification makes two claims about each site: that the raised type
discriminates the branch, and that the matched string is prose this repository authors. Each has its
own screen below, both are **mandatory for every group's delete rows** and not only group 1's, and
each has been shown false for a real population here. They ask different questions and neither sees
the other's class: the callee screen asks about raise paths, the injected-marker screen asks who
wrote the string.

Where either screen is silent, the row is not screened. Both are run by
[sweep-screen.py](sweep-screen.py) and neither can decide a site whose callee the resolver cannot
reach; for those, the third method below applies.

### The callee-side raise screen

**Every delete row that drops a `match=` or an `assertRaisesRegex` runs this screen first, and the
row records the result.**

`hla.md` case 1 says to delete the `match=` where "the raised type already discriminates". That is a
claim about the CALLEE, and the settled map established it from the caller: a test function raising
one exception type once was read as a type that discriminates. It does not follow. If the operation
under test can raise the SAME type from a different path, dropping the `match=` lets the wrong
failure satisfy the test, and case 1's rationale collapses into case 2, whose first arm is to
discriminate structurally on a handle the code already offers.

The screen asks one question per site: **within the operation this test calls, is the asserted
exception type raisable from more than one distinct path?** Three answers, three dispositions.

- **One path.** Case 1 holds. The row keeps its delete and gains the `[1-raise]` marker.
- **Several paths, and a structural handle tells the targeted one apart.** Case 2's first arm: the
  row **converts** onto that handle, which is already in production, so R2.2 is untouched.
- **Several paths and no discriminator.** Case 2's fallback: the row stays a **delete** and the row
  says what it costs, which is that a same-type failure from another path in the same operation will
  now satisfy the test.

#### Method, and what it can and cannot answer

Derived from the AST at HEAD, not from reading. Names resolve through the calling module's own
namespace (its imports, its module aliases, its top-level definitions), so `_validate` in one module
is not confused with `_validate` in another; a call on a value whose type is not known statically
does not resolve at all and is reported as unresolved rather than guessed. From each `pytest.raises`
body the walk follows every first-party function it calls, transitively, and collects every `raise`
of the asserted type or a subclass. The targeted raise is identified by matching the `match=` text
against each reachable raise's message template, and the handle question is then asked of that raise
specifically: is its `entity_kind` or `entity_name` unique among the raises the same call can
produce.

**Positives are sound; negatives are not.** A site the screen calls multi-raise-path genuinely has
more than one reachable raise of that type. A site it calls single-raise-path is single as far as
the resolver reached, and a site it cannot resolve is not screened at all. So `[1-raise]` means
"screened and single", never "checked by hand and safe".

**The outstanding screen debt, stated once and counted in sites, is 532 of the estate's 664.** That
is 413 the resolver could not reach plus 119 it reached but could not tie to a targeted raise. The
other 132 are settled: 52 single-raise-path, 74 multi-raise-path with no discriminator, and 6
multi-raise-path with one. The 199 multi-raise-path sites do NOT need re-screening; what they need
is the disposition the screen already gave them. Anywhere else in this file that talks about what
the executor still owes means this number.

**Owing the screen on an unresolved site means reading the called code by hand and recording the
answer on the row, not re-running the script.** Re-running it returns "unresolved" again, by
construction: the resolver already told you it cannot reach that callee. The hand answer is the same
question the script asks. Open the operation the test calls, find every `raise` of the asserted type
it can reach, and if there is more than one, say on the row whether a handle tells the targeted one
apart. The seven sites the mutation screen below turned into named rows were all in this bucket,
which is what that debt looks like when it is paid.

#### What it found in group 1

Run over all 664 `pytest.raises(..., match=)` sites under `cli/tests` at `426cccae`.

| Verdict                                                              | Sites | Settled by the screen |
| -------------------------------------------------------------------- | ----: | --------------------- |
| Multi-raise-path, a structural handle tells the targeted raise apart |     6 | yes, convert          |
| Multi-raise-path, no discriminator                                   |    74 | yes, delete           |
| Multi-raise-path, targeted raise not identified mechanically         |   119 | no                    |
| Single-raise-path                                                    |    52 | yes, delete           |
| Callee did not resolve, or reached no raise of the asserted type     |   413 | no                    |

`sweep-screen.py screen` prints this partition one site per line, with the targeted raise and the
handle values behind each verdict, so a later executor regenerates it rather than trusting the
table. The six converts and the twelve `[1-raise]` rows are the only verdicts carried on rows; the
other 645 sites live in the script's output, which is why the script is committed beside this file.

**199 of the 251 sites the screen could resolve, four in five, are multi-raise-path.** That is the
finding, and it is larger than the six rows it moves: the group's defining claim, that the batch
needs no judgment because the raised type discriminates, does not survive contact with the code
those tests call. The dispositions mostly do not change, because case 2's fallback lands on the same
edit as case 1, but the reason does, and so does what the batch costs. The mechanical batch's shared
justification now says so.

Six sites over four files have a handle and convert; they are G1-C01 to G1-C04 below. Twelve delete
rows verified single-raise-path across every site they claim and carry `[1-raise]`. The 532
unsettled sites are the executor's, per row, before the edit lands.

### The injected-marker screen

**Every delete row runs this one too.** It tests the mechanical batch's other claim, that the
matched string is prose this repository authors, and the callee screen cannot see this class at all:
it asks about raise paths, this asks who wrote the string.

Where a test injects a failure and then matches on the string it injected, the `match=` is not
pinning anything authored. It is the proof that the INJECTED failure is the one observed, which is
the whole content of the assertion. L-004, L-008, L-009 and L-010 keep for exactly that reason, and
the screen finds the rest of that family rather than leaving it to whoever reads a row closely.

The rule, which `sweep-screen.py injected` implements: a site qualifies when its needle appears in a
string its own module hands to an exception constructor AND appears in no string production emits.
Both halves matter. Without the first it is not a marker. Without the second the test may be quoting
a shipped sentence through a fake, which is the shape the batch correctly deletes: two sites in
`test_activation.py` match `"manually stopped"`, which reads like a marker and is a copy of
`vms/nodes.py:243`, so they stay deleted. Comments and docstrings are excluded from the production
side, because a phrase a docstring discusses is not one the code says.

**It found 30 live sites over 19 files in the mechanical batch, all of them keeps**, rowed as G1-I01
to G1-I19 below, plus one that the mutation screen had already taken (G1-M07) and eleven inside rows
already subtracted, named in the subtraction hand-off. Verdict: on those 30 sites the batch's shared
justification was not a judgment call that went the wrong way, it was a false statement of fact.

### The mutation screen, for what neither of the above reaches

Both screens above are static, and 532 sites are beyond the resolver. For a site an executor cannot
decide by reading, the question has an empirical answer: **break the behavior the test is named for,
and see whether the test still fails once the `match=` is gone.** Fail with the assertion and pass
without it means the assertion is the only probe, and the row keeps.

This is not a screen to run over hundreds of sites; it is what to do with the handful a reading
leaves genuinely uncertain. Seven sites were decided this way on 2026-08-19 and are rowed as G1-M01
to G1-M07, and the result is why the method is recorded rather than the sites alone: **five of the
seven keep and two do not**, and no reading of the rows predicted which. The batch justification
would have deleted all seven.

## Groups

The rows are batched by **shape**, not by domain, so each group can land as one PR and a review
round matches the kind of judgment the batch needs. Batching by domain instead would put the
no-judgment mechanical change into the same review as the sweep's riskiest deletions.

| #   | Group                                         | PR shape                                                       |
| --- | --------------------------------------------- | -------------------------------------------------------------- |
| 1   | Mechanical `match=` narrowing                 | No judgment: the `match=` argument goes, the `raises` stays    |
| 2   | Guide and migration topics                    | The largest single concentration, mostly required-phrase lists |
| 3   | Report lines and hints                        | The judgment-heavy batch                                       |
| 4   | Schema, manifests, capabilities and platforms | Structured-refusal assertions                                  |
| 5   | Authored-artifact form policing               | Config, completions, generated samples, CSS, website           |
| 6   | Source guards                                 | The family `hla.md` settles, decided per guard                 |

## Totals

**`sweep-screen.py totals` is this section**, counted off the row markup rather than carried
forward, because a total carried forward is how this map drifted twice. Two figures matter and they
are different: the LEDGER is every row this map has ever held, and the EXECUTABLE SET is what a
restart of the sweep actually owns after the dead rows and the re-scope subtraction come out.

**Executable set: 1,006 rows, 476 delete, 191 convert, 339 keep**, over five groups. Group 2 has
none left.

**Ledger: 1,215 rows**, which is the executable 1,006 plus 67 `[dead]`, 117 `[subtracted]`, and 25
`[deferred]`.

| Group                                            | Live | delete | convert | keep | Dead | Subtracted | Deferred | Ledger |
| ------------------------------------------------ | ---: | -----: | ------: | ---: | ---: | ---------: | -------: | -----: |
| 1. Mechanical `match=` narrowing                 |  204 |    146 |       7 |   51 |    2 |         25 |        0 |    231 |
| 2. Guide and migration topics                    |    0 |      0 |       0 |    0 |   50 |          0 |        0 |     50 |
| 3. Report lines and hints (four sub-batches)     |  322 |    158 |      88 |   76 |    1 |         59 |        0 |    382 |
| 4. Schema, manifests, capabilities and platforms |  228 |     52 |      81 |   95 |    6 |         16 |        0 |    250 |
| 5. Authored-artifact form policing               |  200 |     95 |      11 |   94 |    7 |          7 |        0 |    214 |
| 6. Source guards                                 |   52 |     25 |       4 |   23 |    1 |         10 |        0 |     63 |
| Deferred (held for R4)                           |    0 |      0 |       0 |    0 |    0 |          0 |       25 |     25 |
| **All**                                          | 1006 |    476 |     191 |  339 |   67 |        117 |       25 |   1215 |

**Corrected 2026-09-06 by `totals`.** The Deferred row used to read 25 live, 23 delete, 2 convert,
which contradicted both this section's own executable-set figure of 1,006 and the markers section
above, where `[deferred]` is one of the three markers that takes a row OUT of the executable set.
The dispositions those 23 and 2 recorded are still on the rows; what was wrong was counting them as
live. Deferred is now its own column, every group is counted the same way, and the row that used to
disagree is the one the command found.

Group 1's site counts are derived exactly, because that estate is scanned rather than read.
`sweep-screen.py estate` run at `426cccae` is where the 664 comes from, one site per line, and it is
what to re-run when the tree moves; `sweep-screen.py attribute` is what checks the claim below
against the rows, and it now exits non-zero when the claim is false. **All 664 `match=` sites under
`cli/tests` at `426cccae` are claimed by exactly one group-1 row**, and `attribute` reports zero
sites claimed by no row and zero claimed by more than one. Of the suite's 49 `assertRaisesRegex`
sites under `website/tests`, group 1 claims 37 and the deferred L-402 claims the other twelve. The
other groups were read rather than scanned, and one row there covers an assertion group of one to a
dozen lines, so no site total is claimed for them. Do not add these numbers to the absorbed
survey's: that survey counted test FUNCTIONS.

**That claim is about `426cccae`, and the tree has moved since.** The counts in this section are the
2026-08-19 cut's and are not re-derived here; the fresh cut is what re-derives them. Measured at
`a64b1b9c` after the 2026-09-06 re-anchoring, the estate is 620 `match=` sites under `cli/tests`
plus 58 regex-family sites under `website/tests`, `attribute` finds 595 of the 620 claimed by
exactly one group-1 row and none claimed by two, and the 25 unclaimed are sites the tree gained.

Of the 654 site groups the group-1 rows were cut against under `cli/tests`, `sweep-screen.py carry`
finds 295 at the same line and 293 moved, 2 retargeted onto a reworded needle, 50 gone from a file
that remains, and 14 gone with their file; its other 35 group-1 site groups are the regex family
under `website/tests`, all 35 found. Those are groups rather than sites, which is why they are 654
and 35 rather than the 664 and 37 the ordinal grammar counted. `carry` is how a judgment row takes
its evidence into the fresh cut.

**Corrected 2026-08-19 by the fix round.** The claim above was false when first made: rows accounted
for 640 of the 664. Three files were re-derived (`test_resolution_lifecycle.py`, whose five cited
lines held no site at all and whose eight real sites now split between a re-anchored L-018 and a new
G1-156; `test_claude_code_integration.py`; `test_config.py`), five judgment and keep anchors were
re-derived, six mechanical rows had range endpoints that excluded real sites, and 25 sites sat
inside a mechanical row's range while a judgment or keep row above it owned them. The fix is
structural rather than case by case: **every mechanical row's file cell is now the explicit list of
the sites it owns**, generated from the estate at HEAD minus what the rows above it claim, so a
range can no longer sweep up a kept site and the "exactly one row" property is checkable rather than
asserted.

One row is one test or one contiguous assertion group, so a file that mixes wholly-policing tests
with prose assertions riding inside legitimate ones appears several times. Group 1's mechanical
batch is the one exception and says so where it starts: those rows are one per FILE, because the
sites they carry share a single shape, a single disposition, and a single justification.

**File counts, ledger and executable, because they differ by enough to mis-size a PR.** The
mechanical batch's ledger is 149 rows over 149 files and 517 sites. Its EXECUTABLE half, once the
dead and subtracted rows come out, is **131 rows over 131 files and 454 sites**: eighteen of those
files now belong to the secrets-preview and instance-model efforts, so an executor sizing the PR
from the ledger figure would open eighteen files that are not the sweep's to touch. Group 1 as a
whole is 231 rows over 163 files on the ledger and **204 rows over 144 files executable**.

This file is long for the repository's 500-line guidance, deliberately. It is a ledger, it is
temporary, and one row per line is what makes it checkable against a diff.

## Completeness re-scan, 2026-08-19

The tree moved between this inventory's basis (`c686cd6d`) and the re-baseline commit (`426cccae`),
so the map was re-derived against HEAD rather than trusted. Every figure below was measured from the
AST at HEAD, never estimated.

### Markers this file uses

Every row state rides in the shape cell as a bold marker, so a mechanical count can separate the
executable set from the rest without reading prose, and so nothing is deleted from the ledger that
the R4 reassessment might want to audit. An unmarked row is live and executable.

Three markers take a row OUT of the executable set:

- **`[dead]`**: the row's target file or site no longer exists at HEAD. The row keeps its original
  disposition as the record of what was intended.
- **`[subtracted: <owner>]`**: the row's estate left the sweep's scope, per the re-scope subtraction
  section below, and the named effort inherits it.
- **`[deferred]`**: the row is held for the R4 reassessment, per the deferred block. These rows were
  always out of the executable set; before 2026-08-19 that was carried by the heading they sat under
  rather than by the row.

Three annotate a row that stays in it:

- **`[1-raise]`**: the callee-side raise screen verified every site this row claims as
  single-raise-path, per the screen's section above. It is carried on delete rows, which are the
  ones the screen decides; a keep row keeps its `match=` either way.
- **`[unverified]`**: the row's disposition turns on a coverage claim that was reasoned rather than
  executed, and its justification names which claim.
- **`[line-anchored: <cause>]`**: the row keeps literal lines, and this is why. Unlike the other
  five it is DERIVED rather than judged: `sweep-screen.py reanchor` writes every one of them and
  nothing else does, so it is regenerated rather than maintained and a hand edit to one is a defect
  the parser refuses. It annotates and does not gate: a line-anchored row is live or dead by its
  other markers, and this one leaves the executable set exactly as it found it.

Ids say where a row came from. `RB-` marks the twelve rows the 2026-08-19 re-baseline added, and
`G1-C`, `G1-M` and `G1-I` the rows its three screens pulled out of the mechanical batch (the callee
screen, the mutation screen and the injected-marker screen respectively). `G1-K17` through `G1-K23`
are the seven the 2026-08-16 harvest added, which is why those rules do not cover them.

Six rows were removed rather than marked, each because every site it claimed moved to another row:
G1-013 into G1-K19, G1-062 into G1-C02, and G1-099, G1-123, G1-135 and G1-140 into the
injected-marker rows, those four files having held nothing but injected markers.

### The estate, re-measured at HEAD

| Estate                                           | At the basis | At HEAD | What moved                                                                                 |
| ------------------------------------------------ | -----------: | ------: | ------------------------------------------------------------------------------------------ |
| `pytest.raises(..., match=)` under `cli/tests`   |          663 |     664 | The Grok Build integration (`703fb625`, 2026-08-17) added one site, rowed below as RB-011  |
| `assertRaisesRegex` under `website/tests`        |           51 |      49 | `test_site_templates.py` lost two to an unrelated `assertRaises` narrowing                 |
| Test files under `cli/tests` and `website/tests` |          n/a |     362 | A guide rework deleted fifteen files at `4ac084cd` and added four, with two more following |

Two site families in `website/tests` sit in the same suite and were never part of the 51: one
`assertRegex` (`test_site_build.py:530`) and eight `assertNotRegex` (five in `test_lander_404.py`,
three in `test_lander_phase4j.py`). All nine already fall inside group 5 rows (F-070, F-098 to
F-104, F-116 to F-121), so they are mapped; only the headline count in `hla.md` omitted them.
`hla.md` now records all nine, along with both corrected site counts, under the effort lead's
authorization; the corrections section below marks those bullets applied.

### Dead rows

Sixty-seven rows over thirteen files are `[dead]`. Twelve of the files are guide tests
`4ac084cd feat(guide): replace typed topics with markdown shells` deleted; the thirteenth is
`website/tests/onboarding-copy.test.mjs`, replaced at `1b9bbb9b` by `onboarding.test.mjs`.
`4ac084cd` deleted fifteen guide test files in all, three of which this map never rowed.

| File                                              | Rows | Ids                 |
| ------------------------------------------------- | ---: | ------------------- |
| `cli/tests/guide/test_migration_topic.py`         |   35 | A-001 to A-035      |
| `cli/tests/guide/test_authored_coverage.py`       |    7 | A-036 to A-042      |
| `cli/tests/guide/test_json_action_contract.py`    |    6 | A-043 to A-048      |
| `cli/tests/guide/test_render_service.py`          |    3 | A-049 to A-051      |
| `cli/tests/guide/test_release_notes.py`           |    3 | A-052, A-053, A-054 |
| `cli/tests/guide/test_probe_free_registry.py`     |    2 | A-055, A-056        |
| `cli/tests/guide/test_trail_sign.py`              |    2 | A-057, A-058        |
| `cli/tests/guide/test_source_review_offer.py`     |    2 | A-059, A-060        |
| `cli/tests/guide/test_installer_plugin_guides.py` |    2 | A-061, A-062        |
| `cli/tests/guide/test_read_only_database.py`      |    2 | L-001, G1-008       |
| `cli/tests/guide/test_package_data.py`            |    1 | A-063               |
| `cli/tests/guide/test_power_import_boundary.py`   |    1 | L-102               |
| `website/tests/onboarding-copy.test.mjs`          |    1 | F-129               |

**Group 2 is entirely dead.** All 50 of its rows targeted files that `4ac084cd` deleted, so the
guide-and-migration batch has no live row and no PR to cut. That is the single largest consequence
of the re-scan and it is a sequencing fact the restart inherits, not a decision this map can make.
The other dead rows are scattered: two in group 1, one in group 3, six in group 4, seven in group 5,
one in group 6.

Six guide test files stand in their place, and they arrived from three commits rather than one:
`4ac084cd` added `test_shell_catalog.py`, `test_shell_package.py`, `test_shell_render.py` and
`test_shell_service.py`; `908ee681` added `test_release_history.py`; `52a17d86` added
`test_shell_commands.py`. All six were scanned and carry no in-scope site: their string assertions
are markdown fixtures the tests author as INPUT and compare against rendered output, which is
derivation parity rather than a pin on shipped prose.

### Rows added

Twelve. Ten (RB-001 to RB-010) row the two orchestrated session files the settled map missed
entirely, `cli/tests/sessions/test_claude_code_orchestrated.py` and
`cli/tests/sessions/test_codex_orchestrated.py`; they sit in group 3, sub-batch 3a, because the
assertions ride inside behavioral tests rather than constituting whole tests (which is what puts
their siblings B-068 and B-072 in group 5). RB-011 rows the one file at HEAD carrying a `match=`
site with no group-1 row. RB-012 replaces F-129 with its successor file.

`cli/tests/sessions/test_grok_build_orchestrated.py` was read for the same launch-note shape and has
none: it asserts only argv (`--session-id`, `--resume`) and persisted state.

### Files with no row, and why

**Sixty-nine test files at HEAD carry no row**, measured this way so a second auditor lands on the
same number: take every `git ls-files` path under `cli/tests` and `website/tests` whose basename
starts with `test_` and ends `.py`, or which ends `.test.mjs` (362 files); a file counts as rowed if
any row's file cell names it, resolving a bare basename against the NEAREST PRECEDING directory in
the same cell and expanding a `*` glob, and dead and subtracted rows count as rowed. Skipping the
basename resolution answers 82 instead of 69, because D-154, D-157 and F-128 between them name
sixteen files that way.

Of the sixty-nine, fifty-four have zero derivable in-scope operands: no `match=`, no
`assertRaisesRegex` family member, and no multi-word string literal in an assertion operand
position. The remaining fifteen were read individually and are excluded for one of three reasons.

| Reason                                                                                                                                                                   | Files                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Emitted command text, not prose we display.** The assertion holds what the code sends to a shell, a transport or a completion script, which is behavior at a boundary. | `agents/test_install_commands.py`, `plugins/test_grok.py`, `test_initializer_apt.py`, `test_resource_show.py`, `test_ssh_set_env.py`, `transports/test_remote_lima.py`, `transports/test_sensitive_stdin.py`, `workspaces/test_acls.py`, `sessions/test_grok_build_orchestrated.py` |
| **Value identity on the test's own input.** The literal equals a value the test seeded, so it pins nothing authored.                                                     | `resources/test_show.py` (`:192` against the description seeded at `:88`)                                                                                                                                                                                                           |
| **Derivation parity over a fixture the test authors.** Markdown in, rendered markdown out.                                                                               | `guide/test_shell_catalog.py`, `guide/test_shell_render.py`, `guide/test_shell_service.py`, `website/tests/lander-phase4j.test.mjs`, `website/tests/lander-phase4t.test.mjs`                                                                                                        |

## Re-scope subtraction, 2026-08-19

[The 2026-08-19 re-scope message](message-2026-08-19-sweep-rescope.md) takes two estates out of the
sweep's scope, because two efforts started after this map settled and will rewrite those tests
wholesale. The trim moves INTO the owning efforts rather than behind them, so nothing here is
cancelled: it changes owner.

| Estate leaving the sweep                                                                                              | Owner                                          |
| --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| The secret-backend contract, the three in-tree backends, and the secrets estate                                       | `2026-08-18-secret-preview-contract` (PR #619) |
| The database and persistence layer, the resource show and inspect surfaces, and doctor's resource-attributable checks | `2026-08-19-instance-model` (seed PR #621)     |

Subtracted rows keep their id, disposition and justification and gain a `**[subtracted: <owner>]**`
marker in their shape cell. Nothing is deleted, so the R4 reassessment can audit what moved rather
than infer it, and the owning effort inherits a decided row rather than a question.

### What the estate rule is

Neither owning effort's SDD is on `main` yet, so the boundary is drawn here from the re-scope
message's own words against the file layout at HEAD, and it is stated as a path rule so a reader can
check it rather than take it.

**To `2026-08-18-secret-preview-contract`**: `cli/tests/secrets/**`, `cli/tests/test_secret*.py`,
`cli/tests/test_secrets*.py`, `cli/tests/capabilities/test_secret_backend*.py`,
`cli/tests/resources/test_secret_backend_kind.py`, `cli/tests/plugins/test_onepassword.py`,
`cli/tests/orchestration/test_secrets.py`, and `cli/tests/test_doctor_env_and_secrets.py`. The
production surfaces behind them are `cli/agentworks/capabilities/secret_backend/**` (the contract
plus the `env_var` and `prompt` backends), `cli/agentworks/secrets/**`, and
`cli/agentworks/plugins/onepassword/**`, which is the third backend.

**To `2026-08-19-instance-model`**: `cli/tests/db/**`, `cli/tests/test_db.py`,
`cli/tests/test_db_*.py`, `cli/tests/test_database_*.py`, `cli/tests/resources/test_show.py`,
`cli/tests/test_resource_show.py`, and `cli/tests/test_doctor_schema.py`. The production surfaces
are `cli/agentworks/db/**`, the resource show and inspect paths, and doctor's schema-drift checks.

Three boundary calls are recorded rather than buried, because each could reasonably go the other way
and the lead may want one back.

1. **`test_doctor_env_and_secrets.py` goes to secrets-preview, not instance-model.** Its rows are
   about which secret SOURCE doctor says it would attempt and why a source is not ready, which the
   backend-contract rewrite changes directly. The re-scope message assigns "doctor's drift
   reporting" to instance-model, and these rows are not drift reporting.
2. **`test_doctor.py` and `test_doctor_cli.py` stay in the sweep.** Their rows are about the report
   and check projection machinery, ANSI styling, and the label column over a synthetic report, none
   of which is resource-attributable. The one row worth a second look is C-061, whose structural
   half is the "Secret sources" group placeholder; it stays in the sweep, and if the secrets-preview
   effort renames that group it becomes a trivial rebase rather than a collision.
3. **`test_secrets_eager_resolve_*.py` goes to secrets-preview.** These drive VM, agent, session and
   console creation rather than the backends, but what they assert is resolution outcomes, which is
   exactly what the contract rewrite changes.

`cli/tests/resources/test_show.py`, `cli/tests/test_resource_show.py`,
`cli/tests/resources/test_secret_backend_kind.py`,
`cli/tests/capabilities/test_secret_backend_relocation.py` and
`cli/tests/plugins/test_onepassword.py` are in the subtracted estates and carry no rows, so they
subtract nothing; they are named so the owning efforts know the sweep looked at them and found
nothing to hand over.

### Group by group

| Group    | Subtracted | To secrets-preview | To instance-model | Live rows left |
| -------- | ---------: | -----------------: | ----------------: | -------------: |
| Group 1  |         25 |                 16 |                 9 |            182 |
| Group 2  |          0 |                  0 |                 0 |              0 |
| Group 3  |         59 |                 28 |                31 |            322 |
| Group 4  |         16 |                 14 |                 2 |            228 |
| Group 5  |          7 |                  6 |                 1 |            200 |
| Group 6  |         10 |                  8 |                 2 |             52 |
| Deferred |          0 |                  0 |                 0 |             25 |

"Live rows left" excludes both the subtracted rows and the `[dead]` ones, so it is the executable
set the restart actually owns. Group 2's zero is the completeness re-scan's finding, not the
subtraction's: every one of its rows is dead.

The subtracted rows, by group and owner:

- **Group 1 to secrets-preview** (16): L-016, L-018, G1-K08, G1-020, G1-054, G1-055, G1-087, G1-104,
  G1-105, G1-106, G1-107, G1-108, G1-109, G1-110, G1-111, G1-156
- **Group 1 to instance-model** (9): L-002, L-003, L-010, L-020, G1-K13, G1-083, G1-084, G1-085,
  G1-088
- **Group 3 to secrets-preview** (28): B-049, B-050, B-051, B-060, B-061, B-093, B-094, B-095,
  B-100, B-102, B-103, B-104, B-105, B-106, B-107, B-108, B-109, B-110, B-111, B-112, B-113, B-114,
  B-116, C-071, C-072, C-073, C-074, C-075
- **Group 3 to instance-model** (31): A-065, A-067, A-068, A-069, A-070, A-071, A-072, A-073, A-074,
  A-075, A-076, A-077, A-078, A-081, A-082, A-083, A-084, A-085, A-086, A-087, A-088, A-089, A-090,
  A-091, A-092, C-065, C-066, C-067, C-068, C-069, C-070
- **Group 4 to secrets-preview** (14): B-059, B-062, B-063, B-064, B-065, D-141, D-142, D-143,
  D-144, D-145, D-146, D-147, D-148, D-149
- **Group 4 to instance-model** (2): A-064, A-079
- **Group 5 to secrets-preview** (6): B-096, B-097, B-098, B-099, B-101, B-115
- **Group 5 to instance-model** (1): A-066
- **Group 6 to secrets-preview** (8): L-116, L-117, L-118, L-119, L-120, L-121, L-122, L-123
- **Group 6 to instance-model** (2): A-080, L-113

### By file, for the owning efforts

Each row below is a file the owning effort inherits, with what the sweep had decided for it. This is
the hand-off the re-scope message asks each effort's definition of done to absorb.

**One finding rides with the hand-off rather than being rediscovered on the other side.** The
injected-marker screen finds eleven sites inside subtracted DELETE rows whose matched text is a
marker the test wrote, not prose we ship: nine `api-key` sites across the three
`test_secrets_eager_resolve_*` files (G1-106, G1-107, G1-108) going to secrets-preview, and two
`boom` and `snapshot unavailable` sites in `test_database_migration_safety.py` (G1-084) going to
instance-model. By the same rule that keeps G1-I01 to G1-I19 those eleven keep, so the delete
disposition their rows carry is wrong on its own terms. Two further injected sites in these estates
sit in L-010, which already keeps them for this reason. The owning efforts inherit the correction
with the rows; `sweep-screen.py injected` reproduces it.

**To `2026-08-18-secret-preview-contract`:**

| File                                                        | Rows | delete | convert | keep |
| ----------------------------------------------------------- | ---: | -----: | ------: | ---: |
| `cli/tests/capabilities/test_secret_backend_conformance.py` |    9 |      0 |       9 |    0 |
| `cli/tests/orchestration/test_secrets.py`                   |    4 |      2 |       0 |    2 |
| `cli/tests/secrets/test_backends.py`                        |    1 |      0 |       1 |    0 |
| `cli/tests/secrets/test_disabled_plugin_attribution.py`     |    1 |      1 |       0 |    0 |
| `cli/tests/secrets/test_resolution_lifecycle.py`            |    3 |      3 |       0 |    0 |
| `cli/tests/secrets/test_resolver_seed.py`                   |    1 |      1 |       0 |    0 |
| `cli/tests/secrets/test_sources.py`                         |    7 |      3 |       1 |    3 |
| `cli/tests/test_doctor_env_and_secrets.py`                  |    6 |      2 |       3 |    1 |
| `cli/tests/test_secret_describe.py`                         |    8 |      5 |       0 |    3 |
| `cli/tests/test_secret_sources_drive_harness.py`            |    3 |      1 |       0 |    2 |
| `cli/tests/test_secret_verify.py`                           |   10 |      4 |       2 |    4 |
| `cli/tests/test_secrets_eager_resolve_console.py`           |    1 |      1 |       0 |    0 |
| `cli/tests/test_secrets_eager_resolve_sessions.py`          |    1 |      1 |       0 |    0 |
| `cli/tests/test_secrets_eager_resolve_vm_agent.py`          |    9 |      1 |       8 |    0 |
| `cli/tests/test_secrets_env_var.py`                         |    1 |      0 |       0 |    1 |
| `cli/tests/test_secrets_inspect.py`                         |    3 |      1 |       1 |    1 |
| `cli/tests/test_secrets_orchestration.py`                   |    2 |      2 |       0 |    0 |
| `cli/tests/test_secrets_prompt.py`                          |    1 |      1 |       0 |    0 |
| `cli/tests/test_secrets_resolver.py`                        |    1 |      1 |       0 |    0 |

**To `2026-08-19-instance-model`:**

| File                                                       | Rows | delete | convert | keep |
| ---------------------------------------------------------- | ---: | -----: | ------: | ---: |
| `cli/tests/test_database_backup.py`                        |    5 |      2 |       1 |    2 |
| `cli/tests/test_database_cli.py`                           |    7 |      5 |       2 |    0 |
| `cli/tests/test_database_migration_safety.py`              |   17 |      5 |       3 |    9 |
| `cli/tests/test_db.py`                                     |    1 |      0 |       0 |    1 |
| `cli/tests/test_db_migration_harness_integration_state.py` |    3 |      1 |       1 |    1 |
| `cli/tests/test_db_migration_vm_sites.py`                  |    4 |      2 |       1 |    1 |
| `cli/tests/test_doctor_schema.py`                          |    8 |      4 |       3 |    1 |

### What is unaffected

Everything in a disjoint estate: sessions, workspaces, agents, consoles, transports, VMs and
platforms, schema and manifests, capabilities outside the secret-backend package, completions, the
website, and the source guards. The stack topology below is stated over the groups as they were and
needs re-cutting for the restart, which is a sequencing decision and not this map's to make.

## Group 1: mechanical `match=` narrowing

204 live rows: 146 delete, 7 convert, 51 keep, out of a ledger of 231 (two `[dead]`, 25
`[subtracted]`). The group splits in three. The 33 rows immediately below are the sites the taxonomy
does NOT decide mechanically; the 23 `G1-K` rows after them are the sites whose matched text varies
with the test's input; the 152 rows in the mechanical batch last are one per file and share one
justification. A reviewer who reads the first two tables has read all the judgment in this PR.

**This group is no longer the no-judgment batch its name promises.** The callee-side raise screen
found that four in five of the sites it can resolve are multi-raise-path, so the mechanical batch's
deletions rest on `hla.md` case 2's fallback rather than its case 1. The injected-marker screen then
found 30 sites where the batch's other claim was false outright. The edit is the same and the review
is not.

**Operator disposition, 2026-08-19: group 1 executes as TWO PRs.** The mechanical batch goes at
mechanical speed as one, and the judgment rows go as the other with their own review depth: G1-C01
to G1-C04, G1-M01 to G1-M07, G1-I01 to G1-I19, and group 1's remaining converts and keeps, which are
the rows whose evidence a reviewer has to read rather than scan. The split follows the screens: what
is left in the mechanical batch after three screens have taken their sites out is the part that
genuinely needs no judgment.

**Corrected 2026-08-16, during execution.** The re-check that produced the `G1-K` rows found 30
input-varying sites. There are 43, plus two of the same shape in unittest's spelling. It had missed
a `manifests/` and `vms/` cluster, and the misses were sitting in delete rows, described by the
mechanical batch's shared justification as fixed literals that vary with nothing. What found them
was not re-reading the rows: it was partitioning every site in the estate by AST and classifying
each `match=` argument as a string literal or not. `ruff` corroborated one independently, raising
`B007` after the deletion because the loop variable existed only to feed the assertion. Two of the
affected tests say in their own docstrings that the label is deliberately carried into the failure
so each case reads as its own problem rather than as one sentence covering four. The lesson is the
method: a shape claim about a batch this size is checkable mechanically and should be checked that
way rather than asserted from reading.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| L-001 | `cli/tests/guide/test_read_only_database.py::L48` | **[line-anchored: file gone]** **[dead]** `match=` on sqlite's own `readonly` text | keep | Upstream tool output, the rule's one exception. The read-only open is enforced by SQLite, not by us, and `sqlite3.OperationalError` alone does not say the refusal was the read-only attachment. |
| L-002 | `cli/tests/test_db.py::test_migration_19_fails_with_legacy_agent_session::IntegrityError::2c1282,test_agent_session_requires_socket_path::IntegrityError::2c1282` | **[subtracted: instance-model]** `match="CHECK"` on sqlite's constraint text | keep | Upstream tool output. `IntegrityError` covers every constraint class; `CHECK` is the narrow token distinguishing a check constraint from a uniqueness or FK violation. |
| L-003 | `cli/tests/test_database_migration_safety.py::test_database_closes_connection_when_migration_raises::ProgrammingError::c3eefb` | **[subtracted: instance-model]** `match="closed"` on sqlite's `ProgrammingError` | keep | Upstream tool output, pinned at the one token the assertion depends on: the connection was closed rather than failing for another programming reason. |
| L-004 | `cli/tests/plugins/gcp/test_instance.py::test_insert_retains_typed_request_and_ownership_before_wait::KeyboardInterrupt::6c45cb` | `match=` on a `KeyboardInterrupt` the test injected | keep | Not authored prose: the fake raises `KeyboardInterrupt("stop")` and the assertion proves that exact interrupt propagated out of `insert_instance_reconciled` rather than being swallowed and re-raised. Value identity. |
| L-005 | `cli/tests/plugins/gcp/test_network.py::test_wait_interrupt_leaves_operation_ownership_for_safe_rollback::KeyboardInterrupt::6c45cb` | injected `KeyboardInterrupt` | keep | Same shape as L-004, on the network reconcile path. |
| L-006 | `cli/tests/plugins/gcp/test_platform.py::test_first_interrupt_carries_operation_ownership_through_platform_rollback::KeyboardInterrupt::d62e9a,test_second_interrupt_abandons_platform_rollback_without_mutating_survivors::KeyboardInterrupt::a7937b` | injected `KeyboardInterrupt`, one parameterized by stage | keep | The match string is the parameter that selected which stage's fake raises, so it proves the interrupt from THAT stage escaped. Nothing authored is pinned. |
| L-007 | `cli/tests/assistance/test_generation.py::test_atomic_write_preserves_destination_when_replace_fails::OSError::83c874` | injected `OSError("refused ...")` | keep | The test monkeypatches `os.replace` to raise; the match proves the injected failure surfaced rather than being converted, alongside the real assertion that the destination is intact. **Anchors re-derived at HEAD, 2026-08-19.** Was `:178`. |
| L-008 | `cli/tests/test_ssh_logger.py::test_write_error_propagates_and_closes_handler::OSError::8c96d7` | injected `OSError("disk full")` | keep | Same shape. The surrounding assertions cover handler teardown; the match covers which exception escaped. |
| L-009 | `cli/tests/vms/test_verify_connection.py::test_verify_connection_surfaces_failure_without_activation_or_mutation::type::d62e9a` | `pytest.raises(type(failure), match=str(failure))` | keep | Fully derived from the injected failure object. Pins nothing; this is the ideal form of L-004's family. |
| L-010 | `cli/tests/test_database_backup.py::test_failed_backup_and_absent_restore_remove_incomplete_files::BackupError::38c863*2` | **[subtracted: instance-model]** `match="forced"` against `BackupError("forced copy failure")` raised by the test's own fake at `:379` | keep | Injected sentinel, verified at HEAD: the string is authored by the test three lines above, so the assertion proves the injected copy failure escaped both the backup and the restore path. |
| L-011 | `cli/tests/test_name_filter_validation.py::test_console_list_rejects_unknown_workspace_and_agent::NotFoundError::58dd61,test_console_list_rejects_unknown_workspace_and_agent::NotFoundError::84b334` | two same-type `NotFoundError` branches in one function | convert | Verified by execution: both raises carry `entity_kind` (`'workspace'` / `'agent'`) and `entity_name='nope'` from `name_filters.py:48`. Replace both `match=` with `caught.value.entity_kind` assertions. Structural handle already in production, already the house pattern at 75 existing test sites, so no production change. |
| L-012 | `cli/tests/manifests/test_reference.py::test_an_unknown_kind_and_an_unknown_implementation_are_typed_refusals::ValidationError::0b5f13,test_an_unknown_kind_and_an_unknown_implementation_are_typed_refusals::ValidationError::ad65ce,test_an_unknown_kind_and_an_unknown_implementation_are_typed_refusals::ValidationError::9d6855` | three same-type `ValidationError` branches in one function | convert | Verified by execution: the three carry `entity_kind`/`entity_name` of `('resource','nope')`, `('vm-platform','nope')`, `('resource','secret/npm-token')`, which fully discriminate the three refusals. Assert those attributes instead. |
| L-013 | `cli/tests/test_session_liveness.py::test_ensure_pid_raises_on_unresolvable::Exception::e210f7` | `pytest.raises(Exception, match="alive but PID/boot ID recovery failed")` | convert | The bare `Exception` makes the authored sentence the only discriminator. Narrow the `raises` to the real type and drop the `match`: hla case 2's "the exception type" handle, no production change. |
| L-014 | `cli/tests/capabilities/test_capability_descriptors.py::test_mapping_descriptor_requires_a_declarable_mapping_shaped_host::StateError::464033,test_mapping_descriptor_requires_a_declarable_mapping_shaped_host::StateError::543322` | two same-type `StateError` branches | delete | Verified by execution: neither raise sets `entity_kind`/`entity_name`, so only authored wording discriminates. R2.2 forbids adding a discriminator, so hla case 2's fallback applies and the branch coverage goes with the assertion. |
| L-015 | `cli/tests/capabilities/test_capability_descriptors.py::test_mapping_descriptor_key_is_a_hard_uses_reference::StateError::be88df,test_mapping_descriptor_key_is_a_hard_uses_reference::StateError::a5eb83` | two same-type `StateError` branches | delete | Same, verified the same way. |
| L-016 | `cli/tests/secrets/test_sources.py::test_backend_selection_rejects_framework_row_and_class_corruption::StateError::b765f3,test_backend_selection_rejects_framework_row_and_class_corruption::StateError::6a1b4d` | **[subtracted: secrets-preview]** two same-type `StateError` branches | delete | Verified: no structural handle on either raise. |
| L-017 | `cli/tests/sessions/test_session_nodes.py::test_session_factory_requires_exactly_one_launch_identity::StateError::e31c28*2` | **[1-raise]** two `StateError` sites, identical `'exactly one'` text | delete | Verified: no handle, and the two matches are the same string, so the assertion never distinguished the branches it appears to. |
| L-018 | `cli/tests/secrets/test_resolution_lifecycle.py::test_illegal_outcome_tuple_is_rejected::ValueError::062469*2,test_illegal_outcome_tuple_is_rejected::ValueError::41cf67,test_illegal_outcome_tuple_is_rejected::ValueError::3b6f0b*2` | **[subtracted: secrets-preview]** five `ValueError` branches in one function | delete | `ValueError` is not an `AgentworksError`, so there is no `entity_*` handle at all. Only our own `OUTCOME_RULES` wording discriminates. **Anchors re-derived at HEAD, 2026-08-19.** Was `:443,451,458,467,476`, none of which held a site: the five branches are `test_illegal_outcome_tuple_is_rejected`'s, now at `:548-581`. The file's other three sites (`:595`, `:611`, `:621`) had no row at all and now have G1-156. |
| L-019 | `cli/tests/test_consoles_specs_db.py::test_reorder_console_sessions_rejects_wrong_member_set::ValueError::49379f*3` | three `ValueError` sites, the same `'full member list'` text | delete | The three matches are identical, so they never discriminated. |
| L-020 | `cli/tests/test_doctor_schema.py::test_schema_check_rejects_invalid_scalar_versions::StateError::09c09f*2` | **[subtracted: instance-model]** two `StateError` sites, identical text | delete | Same shape as L-019. |
| L-021 | `cli/tests/manifests/test_samples.py::test_bare_sample_requires_kind_or_all::ValidationError::11be58,test_bare_sample_requires_kind_or_all::ValidationError::175f8c` | two `ValidationError` branches | delete | Verified: no `entity_*` handle. |
| L-022 | `cli/tests/manifests/test_samples.py::test_write_sample_refuses_escapes_and_suffixes::ValidationError::9c035e*2,test_write_sample_refuses_escapes_and_suffixes::ValidationError::e67b5d` | three `ValidationError` branches | delete | Verified: no `entity_*` handle. **Anchors re-derived at HEAD, 2026-08-19.** Was `:437,439,441`. |
| L-023 | `cli/tests/resources/test_registry_lifecycle.py::test_add_rejects_names_containing_slash::ConfigError::5bb7c2*2` | two `ConfigError` sites, identical `"contains '/'"` text | delete | Identical matches; the real invariant (a `/` is refused at the single publisher choke point) survives on the `raises` alone. |
| L-024 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_port_forward_bad_spec_fails_with_zero_resolves_and_zero_gate::ValidationError::1d87e5,test_port_forward_bad_spec_fails_with_zero_resolves_and_zero_gate::ValidationError::c8007a` | two `ValidationError` branches | delete | Verified: both carry `entity_kind='vm'`, `entity_name='box'`, identical across the two branches, so the handle exists but does not discriminate. |
| L-025 | `cli/tests/vms/test_vm_site_kind.py::test_site_names_obey_the_freeform_name_rules::ConfigError::46ede9,test_site_names_obey_the_freeform_name_rules::ConfigError::de487f,test_site_names_obey_the_freeform_name_rules::ConfigError::24f3ac` | three name-rule `ConfigError` branches | delete | Verified: no `entity_*` on the name-validation raises. Only the authored rule sentence separates lowercase, consecutive-hyphen, and length. |
| L-026 | `cli/tests/vms/test_platform_config_contract.py::test_lima_rejects_bad_host_and_unknown_keys::ConfigError::0bfdec,test_lima_rejects_bad_host_and_unknown_keys::ConfigError::26185c,test_azure_requires_the_three_location_keys_and_defaults_auth::ConfigError::a7c9ee,test_aws_ec2_requires_region::ConfigError::ea9010,test_aws_ec2_requires_region::ConfigError::a7c9ee,test_gcp_gce_location_auth_and_subnet_are_shape_checked::ConfigError::5d37ba,test_gcp_gce_location_auth_and_subnet_are_shape_checked::ConfigError::5eebcd,test_proxmox_validation_errors::ConfigError::e42340,test_proxmox_validation_errors::ConfigError::6d841c,test_proxmox_validation_errors::ConfigError::9e39e9,test_proxmox_no_longer_accepts_a_quoted_template_vmid::ConfigError::4aa455*2,test_proxmox_no_longer_reads_a_string_verify_ssl_as_true::ConfigError::b4d44b,test_proxmox_no_longer_reads_a_string_verify_ssl_as_true::ConfigError::cd085b` | 14 same-type `ConfigError` sites across seven functions | delete | Verified by execution and it is the interesting one: every raise DOES carry `entity_kind='vm-site'`, `entity_name='t'`, but identically for all fourteen, so the handle does not discriminate. (Two further sites in this file, `:130` and `:182`, were on this row until the second re-check moved them to G1-K22: their matched text interpolates a loop variable, so the parameter discriminates where the handle does not.) The information that would (`_Problem.path`, the operator's dotted field address) is consumed inside `schema/errors.py:167` and never reaches `ConfigError`. Attaching it would be a shipped-contract change, which R2.2 excludes. Cost is stated plainly: a config rejected for the wrong reason will still pass. |
| L-401 | `website/tests/test_site_build.py::BuildAndInstallTests.test_favicon_rejects_every_unreviewed_svg_node::ValueError::83fb7e,BuildAndInstallTests.test_favicon_rejects_non_path_canonical_mark_geometry::ValueError::eeab47,BuildAndInstallTests.test_favicon_rejects_noncanonical_rocket_root_or_mark_hierarchy::ValueError::f261ac,BuildAndInstallTests.test_unapproved_external_url_fails_before_output_changes::ValueError::766f6a,BuildAndInstallTests.test_absent_local_route_has_no_partial_manifest_suppression::ValueError::fe209c,BuildAndInstallTests.test_cross_document_fragment_must_exist_on_its_actual_target::ValueError::2ef080,BuildAndInstallTests.test_source_heading_cannot_duplicate_a_shell_id::ValueError::a78aaf,BuildAndInstallTests.test_rendered_reference_validation_retains_duplicate_ids::ValueError::a78aaf,BuildAndInstallTests.test_manifest_without_root_index_cannot_suppress_root_reference_failure::ValueError::fe209c,BuildAndInstallTests.test_shared_css_cannot_conceal_reviewed_shell_content::ValueError::cbe359,BuildAndInstallTests.test_same_document_fragment_must_resolve_before_output_changes::ValueError::ee89e6,BuildAndInstallTests.test_duplicate_rendered_references_cannot_hide_first_browser_destination::ValueError::20a7d7,BuildAndInstallTests.test_output_inside_repository_is_rejected_without_source_writes,BuildAndInstallTests.test_output_accepts_ordinary_spaced_and_hidden_directory_names,BuildAndInstallTests.test_every_static_module_import_resolves_to_the_literal_manifest::ValueError::456e31,BuildAndInstallTests.test_model_ships_byte_for_byte_without_generated_proof_composition,BuildAndInstallTests.test_collision_and_world_ship_separately_in_the_exact_manifest,BuildAndInstallTests.test_retired_route_projection_sources_are_absent,BuildAndInstallTests.test_authored_lander_modules_and_tests_stay_below_the_review_ceiling,BuildAndInstallTests.test_output_rejects_dot_traversal::ValueError::c6b9ac,BuildAndInstallTests.test_output_rejects_parent_symlink_into_repository_when_supported::ValueError::e2885f,BuildAndInstallTests.test_existing_top_level_directory_symlink_is_rejected_without_touching_target::ValueError::4c5159,BuildAndInstallTests.test_existing_top_level_broken_symlink_is_rejected_without_replacement::ValueError::4c5159,BuildAndInstallTests.test_existing_output_may_contain_only_selected_owned_entries::ValueError::1ffbe9,BuildAndInstallTests.test_content_or_template_failure_leaves_existing_output_byte_identical,BuildAndInstallTests.test_symlink_entry_in_existing_output_is_rejected_when_supported::ValueError::cc52c0,BuildAndInstallTests.test_fifo_entry_in_existing_output_is_rejected_when_supported::ValueError::25bf38,BuildAndInstallTests.test_non_directory_output_is_rejected::ValueError::4c5159,BuildAndInstallTests.test_remote_runtime_asset_references_fail_before_output_changes::ValueError::3a6b8d,BuildAndInstallTests.test_harmless_javascript_line_comment_is_allowed,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output::OSError::9e8486,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output::RuntimeError::4183b7` | 24 `assertRaisesRegex` sites | delete | Every regex matches wording `site_builder` and `site_validation` author. The raised type is `ValueError` throughout, so the type does not discriminate and no structural handle exists, which is `hla.md` case 2's fallback: the assertion goes and the branch coverage with it (R2.4). The `assertRaises` stays. Where lane F's rows delete the whole test, the regex goes with it and this row does not double-count. |
| L-403 | `website/tests/test_site_content.py::SourceContractTests.test_assistance_projection_drift_and_framing_fail_closed::ValueError::d9687c,SourceContractTests.test_assistance_source_requires_exact_regular_lf_utf8::ValueError::0d71ce,SourceContractTests.test_missing_or_symlinked_assistance_source_fails_closed::ValueError::5f411f*2,SourceContractTests.test_unsupported_markdown_and_hard_breaks_fail_closed::ContractError::2fc8fe,SourceContractTests.test_unmatched_reserved_inline_markers_fail_instead_of_rendering_literally::ContractError::60945d,SourceContractTests.test_source_relative_links_are_explicitly_mapped_and_others_fail::ContractError::766f6a,SourceContractTests.test_invalid_utf8_bom_missing_and_symlinked_input_fail_closed::ContractError::05ac6a,SourceContractTests.test_invalid_utf8_bom_missing_and_symlinked_input_fail_closed::ContractError::8d8b89*2,SourceContractTests.test_security_reference_and_github_only_reporting_are_exact::ContractError::61b82e` | 11 `assertRaisesRegex` sites | delete | **Corrected 2026-08-19 (map re-baseline).** Sites listed rather than ranged, and re-derived at HEAD. The old range `:176-346` contained G1-K23's two keeps, so an executor working from the range would have deleted them. These eleven are the file's other `assertRaisesRegex` sites, over `site_content`'s authored refusals; the raised type is shared and no structural handle exists, which is `hla.md` case 2's fallback. Two of them (`:244`, matching `"UTF-8\|NUL-free\|LF-terminated"`, and `:395`) are alternations over authored phrases, a wording blacklist wearing a regex, which the rule calls worse than useless. |
| RB-011 | `cli/tests/test_grok_build_integration.py::test_validation_rejects_non_list_extra_args::ConfigError::d0b029` | `match="extra_args: must be a list"` on `ConfigError` | delete | Added by the 2026-08-19 re-baseline: the Grok Build integration landed at `703fb625` (2026-08-17), after this inventory's basis, so no row claimed the file and it is the one file at HEAD with a `match=` site and no group-1 row. The call is the generic schema validator (`validate_capability_config` through the test's own `_validate` helper at `:61-66`), which raises one `ConfigError` per document however many problems it found, so the raised type discriminates nothing: this is hla case 2, not case 1. No `entity_*` handle is attached, and the address that would discriminate (`_Problem.path`) is consumed inside `schema/errors.py` before the `ConfigError` is built, so case 2's fallback applies and the branch coverage goes with the assertion (R2.4). Cost stated: a regression that rejects `extra_args` as an unknown field instead would still pass. The file's three sibling arms at `:111`, `:117` and `:127` already assert the bare `raises`, so deletion makes this site match its own file rather than diverge from it. |
| G1-C01 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_grant_empty_request_fails_with_zero_resolves_and_zero_gate::ValidationError::c671fd,test_revoke_unknown_agent_fails_with_zero_resolves_and_zero_gate::NotFoundError::23a674` | two refusals reachable from several raises of the asserted type | convert | **Callee-side raise screen, 2026-08-19,** re-derived by `sweep-screen.py screen`. Two sites, two different operations, and the row states them separately because they do not share a callee. `:384` drives `grant_workspaces` (`cli/agentworks/agents/grants.py:54-123`), which reaches three `ValidationError` raises: its own empty-request refusal at `:75`, carrying `entity_kind="agent"`, and the two name-rule raises at `cli/agentworks/naming.py:136` and `:139`, which carry no `entity_kind` at all. `:404` drives `revoke_workspaces` (`grants.py:124-199`), which reaches three `NotFoundError` raises: the missing agent at `:150` (`entity_kind="agent"`), the missing workspace at `:235` inside `remove_from_workspace_group` (`"workspace"`), and the missing VM at `:385` inside `_require_vm` (`"vm"`). In both cases the asserted type does not discriminate and the handle does, so assert `caught.value.entity_kind` in place of each sentence. Under the rubric these are the only probe for a behavior that can regress: without `:404`'s, a run where `ws1` stops resolving satisfies the agent-not-found test. No production change. |
| G1-C02 | `cli/tests/test_agents.py::test_grant_workspaces_rejects_empty_request::ValidationError::c671fd,test_revoke_workspaces_rejects_empty_request::ValidationError::c671fd` | two same-type refusals the callee raises from several paths | convert | **Callee-side raise screen, 2026-08-19,** re-derived by `sweep-screen.py screen`. The grant path's empty-request refusal is `cli/agentworks/agents/grants.py:75` and the revoke path's is `:142`, both `ValidationError` with `entity_kind="agent"`, and both operations also reach `cli/agentworks/naming.py:136` and `:139`, which raise the same type for a malformed name with no `entity_kind` at all. Three reachable raises per site, so the type does not discriminate and the handle does. Assert `caught.value.entity_kind == "agent"` in place of each sentence. This row is the whole of its file's group 1 content, so no mechanical row remains for it. No production change. |
| G1-C03 | `cli/tests/test_env_show.py::test_unknown_vm_raises_validation_error::ValidationError::623d9d` | one of two same-type refusals in one call | convert | **Callee-side raise screen, 2026-08-19,** re-derived by `sweep-screen.py screen`. `show_env` reaches nine `ValidationError` raises: six in `cli/agentworks/env/show.py` (`:190`, `:202`, `:213`, `:224`, `:239`, `:245`), the two name-rule raises in `cli/agentworks/naming.py`, and one in `cli/agentworks/secrets/line_safety.py:38`. The targeted one, `show.py:245`, is the only one carrying `entity_kind="VM"`, so assert that instead of `"VM 'nope' not found"`. The file's other site, `:116`, keeps its delete under case 2's fallback: it targets `show.py:190`, and `:239` carries no `entity_kind` either, so nothing tells the two apart and the branch coverage goes with the assertion (R2.4). No production change. |
| G1-C04 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_unknown_workspace_fails_with_zero_resolves_and_zero_gate::NotFoundError::b21f99` | one of two same-type refusals in one call | convert | **Callee-side raise screen, 2026-08-19,** re-derived by `sweep-screen.py screen`. The repair path reaches exactly two `NotFoundError` raises, `cli/agentworks/workspaces/manager/repair.py:91` for the missing workspace and `:99` for the missing VM behind it. Only `:91` carries `entity_kind="workspace"`, so assert that rather than `"workspace 'ghost' not found"`. Without it a regression that loses the VM instead of the workspace passes this test. The file's other ten sites stay with G1-155. No production change. |
| G1-M01 | `cli/tests/orchestration/test_activation.py::test_gate_reader_is_scoped_to_declared_gate_secrets::StateError::afdbd7` | scoping refusal, one of two same-type raises | keep | **Mutation-proved 2026-08-19.** `ScopedSecrets.get` raises `StateError` twice: the undeclared-name refusal at `cli/agentworks/orchestration/secrets.py:238` and the declared-but-unresolved one at `:248`. Bypassing the refusal (`if False:` at `:237`) leaves the second raise standing, so the bare `raises` still passes and only the `match=` fails. Measured: `test_gate_reader_is_scoped_to_declared_gate_secrets` fails with the assertion as shipped and PASSES once the mechanical row's edit lands. Under the rubric this is the only probe for a behavior that can regress, so it keeps. |
| G1-M02 | `cli/tests/orchestration/test_activation.py::test_auto_start_reader_refuses_undeclared_names::StateError::afdbd7` | declaration refusal, single raise path | delete | **Mutation-proved 2026-08-19, and the disposition stands.** This site targets a different class from G1-M01: `activation.py:177`, the lazy gate-and-repair reader, which has exactly one `raise StateError`. Bypassing its declaration check (`if True:` at `:174`) is caught by `test_auto_start_reader_refuses_undeclared_names` with the `match=` and without it, so the assertion adds nothing the raised type does not already carry. Recorded rather than left in the batch because the four sites around it went the other way and a reader will ask. |
| G1-M03 | `cli/tests/vms/test_add_git_credential_orchestrated.py::test_nodes_receive_only_their_declared_secrets._probing_runup::StateError::afdbd7` | scoping refusal inside a monkeypatched runup | delete | **Mutation-proved 2026-08-19, and the disposition stands.** Same `ScopedSecrets` mutation as G1-M01. `test_nodes_receive_only_their_declared_secrets` still fails after the `match=` goes, because the same test also asserts the token it DID receive, which the mutation changes. The row survives on that sibling assertion, not on the wording. |
| G1-M04 | `cli/tests/vms/test_add_git_credential_orchestrated.py::test_stopped_vm_gate_resolves_once_and_seeds_the_boundary._status::StateError::afdbd7` | scoping refusal inside a monkeypatched status probe | keep | **Mutation-proved 2026-08-19.** Same mutation, opposite result: `test_stopped_vm_gate_resolves_once_and_seeds_the_boundary` fails as shipped and passes after the edit. Nothing else in it distinguishes the scoping refusal from the unresolved-name one. |
| G1-M05 | `cli/tests/vms/test_shell_exec_orchestrated.py::test_shell_platform_transport_hands_a_secret_bearing_ctx::StateError::76cf8a` | scoping refusal on the platform-transport context | keep | **Mutation-proved 2026-08-19.** `test_shell_platform_transport_hands_a_secret_bearing_ctx` fails as shipped and passes after the edit. This is the site whose `match=` carries the longest needle of the family (`not declared by this node`), and the length is not why it keeps: the second raise at `secrets.py:248` is what makes the type non-discriminating. |
| G1-M06 | `cli/tests/vms/test_sites_dispatch.py::test_ops_read_the_token_through_the_context::StateError::afdbd7` | scoping refusal on a bare run context | keep | **Mutation-proved 2026-08-19.** `test_ops_read_the_token_through_the_context` fails as shipped and passes after the edit. Its neighbor at `:123` is a `ConfigError` on a different arm and is unaffected; only this line probes the scoping contract. |
| G1-M07 | `cli/tests/vms/test_lima_remote_create_interrupt.py::test_cleanup_failure_warns_and_does_not_mask_the_original::SSHError::37a06e` | injected marker distinguishing two same-type failures | keep | **Mutation-proved 2026-08-19, and an injected-marker site as well.** Both `SSHError("original failure")` and `SSHError("cleanup broke")` are strings this test writes, so the `match=` is what proves the ORIGINAL failure propagated rather than the cleanup one, which is the property the test is named for. Making the cleanup re-raise over the original (`raise` after the warn at `cli/agentworks/capabilities/vm_platform/lima.py:659`) leaves all 16 tests in the file passing once the assertion goes; with it, `test_cleanup_failure_warns_and_does_not_mask_the_original` fails. `test_wsl2_create_interrupt.py:226` is the same property on the WSL2 platform and keeps as G1-I20. |

### Sites the re-check pulled out of the mechanical batch

43 of the `match=` sites in this group match a string that VARIES with the test's input (an f-string
over a field name, a parametrize variable, an interpolated production constant). Those probe that
the diagnostic tracks the input, which is behavior, so they keep. Two `assertRaisesRegex` sites of
the same shape sit in L-403's range and keep with them, at G1-K23. What is left after this section,
the mutation screen's and the injected-marker screen's takes out is the mechanical batch, whose
sites match a fixed literal in a single-input test.

**One reason serves every row here, so it is stated once rather than 23 times.** These are not fixed
phrasing: the matched text varies with the case, so each assertion fails when the diagnostic stops
tracking the input, not only when someone rewords it. The cost is stated with it: they break on a
rewording too, and whoever rewords must update them. Each row below carries only what is specific to
its sites.

Rows G1-K01 through G1-K16 are the first pass; G1-K17 through G1-K23 are the thirteen `match=` and
two `assertRaisesRegex` sites the second re-check added, as the group header explains.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| G1-K01 | `cli/tests/plugins/gcp/test_compute.py::test_missing_location_is_pre_mutation_config_error::ConfigError::d62e9a,test_required_live_machine_fields_fail_by_provider_shape_branch::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=helper` is the parameter selecting which location lookup failed, so it proves the `ConfigError` names project versus zone correctly. |
| G1-K02 | `cli/tests/plugins/gcp/test_platform.py::test_required_machine_shape_failure_has_zero_provider_mutations::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=message` is parameterized per failure case. |
| G1-K03 | `cli/tests/plugins/test_manifest_parity.py::test_ensure_recipe_enabled_excludes_capability_nodes::StateError::570349` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `f"enable plugin '{PLUGIN}'"` names the plugin the operator must enable, derived from the fixture constant. |
| G1-K04 | `cli/tests/plugins/test_plugin_framework.py::test_rejects_bad_plugin_name::PluginError::d62e9a,test_malformed_secret_backend_registration_is_atomic::PluginError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=repr(bad_name)` proves the rejection names the offending value, and `match=expected` proves each malformed backend class produces its own `PluginError` reason where the type is shared. |
| G1-K05 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestFailClosedBootstrap.test_bootstrap_failure_removes_private_stage_and_rolls_back::ProvisioningError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=message` is parameterized per failure case. |
| G1-K06 | `cli/tests/resources/test_install_resource_kinds.py::test_install_resource_kind_synthesize_raises::NoUnreferencedDefaultError::3e38ce` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `f"the {kind_name} kind has no reserved default name"` names the kind under test. |
| G1-K07 | `cli/tests/resources/test_template_kinds.py::test_inherits_typo_fires_framework_miss_policy::ConfigError::570533` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `f"{spec.kind} kind only auto-declares"` names the kind under test. |
| G1-K08 | `cli/tests/secrets/test_sources.py::test_source_key_miss_schedule_preserves_cross_row_first_target_order::ConfigError::d62e9a` | **[subtracted: secrets-preview]** `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=expected` is `"mapping-missing"` or `"ordinary-missing"` depending on row insertion order, so the assertion proves `finalize` reports the RIGHT missing host independently of ordering. Nothing else probes that. |
| G1-K09 | `cli/tests/test_aws_ec2_ops.py::TestCreateRollback.test_readiness_failure_is_safe_and_rolls_back_before_key_delivery::EC2Error::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=message` is parameterized per failure case. |
| G1-K10 | `cli/tests/test_codex_integration.py::test_validation_rejects_non_string_flag_fields::ConfigError::f275a0,test_new_fields_reject_wrong_types::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** Two parameterized sites. `f"{field_name}: must be a string"` and `match=field` over six fields prove the codex `_validate` names the field that was wrong; the type is the same for all six. **Anchors re-derived at HEAD, 2026-08-19.** Was `:212,837`. |
| G1-K11 | `cli/tests/test_config.py::test_focused_database_config_rejects_unsafe_input::ConfigError::d62e9a,test_proxmox_config::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** Two parameterized sites driven by case tables of (bad config, expected diagnosis). They prove per-case routing: each malformed input produces ITS OWN diagnosis rather than a catch-all. **Anchors re-derived at HEAD, 2026-08-19.** Was `:588` for the second site. |
| G1-K12 | `cli/tests/test_consoles_restore.py::test_restore_session_refuses_and_leaves_the_model_intact::StateError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=match` is parameterized per failure case. |
| G1-K13 | `cli/tests/test_database_migration_safety.py::test_prepare_refuses_unopenable_state_before_writable_open::StateError::d62e9a` | **[subtracted: instance-model]** `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=match` is parameterized per failure case. |
| G1-K14 | `cli/tests/test_git_credential_scoping.py::test_invalid_scopes_rejected::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** `match=match` is parameterized per failure case. |
| G1-K15 | `cli/tests/test_operation_scope.py::test_system_scope_forbids_deeper_names::StateError::88ef74,test_vm_scope_forbids_deeper_names::StateError::88ef74,test_workspace_scope_requires_its_chain::StateError::e6fb33,test_workspace_scope_forbids_deeper_names::StateError::88ef74,test_agent_scope_requires_its_chain::StateError::e6fb33,test_agent_scope_forbids_deeper_and_sideways_names::StateError::88ef74,test_session_scope_requires_its_chain::StateError::e6fb33` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** Seven parameterized sites over `OperationScope.__post_init__`'s level rules (`capabilities/base.py:140-150`). `f"forbids '{field}'"` and `f"requires '{field}'"` name WHICH field violated the level's rule, across four fields; `StateError` is identical for all of them and nothing else probes the rules table. |
| G1-K16 | `cli/tests/test_session_create_ephemeral_vm_anchor.py::test_ephemeral_workspace_name_uses_workspace_cap::ValidationError::144bdc,test_ephemeral_agent_name_uses_agent_cap::ValidationError::7ad9dc,test_defaulted_ephemeral_agent_name_overflow_hints_override_flag::ValidationError::7ad9dc,test_defaulted_ephemeral_workspace_name_overflow_hints_override_flag::ValidationError::144bdc,test_session_name_uses_socket_derived_cap::ValidationError::d7848d` | `match=` varying with the test's input | keep | **Rubric re-check, 2026-08-16.** Five sites matching `f"max {MAX_*_NAME_LENGTH}"`, which interpolate the real production constants. The assertion proves the refusal reports the LIMIT ACTUALLY IN FORCE rather than a number that drifted from it. |
| G1-K17 | `cli/tests/manifests/test_capability_shape.py::test_a_retired_session_selector_is_an_unknown_field_at_its_own_location::ConfigError::5a19d5,test_tagged_table_requires_a_string_name_key::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16 (see the group header).** `:154` is `rf"res\.yaml:2:.*{key}: unknown field"` over the three `_RETIRED_SELECTORS`, and the test's own docstring says the label is carried into the failure "so the spelling that stopped being refused still names itself". `:194` is `match=message` over four parametrize cases with four distinct messages, and that docstring says each way of getting it wrong must read "as that field's own problem rather than as one hand-written sentence covering four cases". Deleting them collapses each table to one assertion repeated. |
| G1-K18 | `cli/tests/manifests/test_decode_shapes.py::test_a_stray_top_level_key_names_the_one_field_this_kind_has::ConfigError::8fa35c,test_a_metadata_field_written_in_spec_is_refused::ConfigError::6bd113` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** `f"{key}: unknown field; expected one of: provider"` over the `type`/`org` loop and `f"{field} belong\(s\) in metadata, not in spec"` over the `description`/`name` loop. Both prove the refusal names WHICH field was misplaced; `ConfigError` is identical across the arms of each loop. |
| G1-K19 | `cli/tests/manifests/test_kind_models.py::test_a_malformed_expiry_is_refused::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** `match=message` over two parametrize cases whose messages differ in kind, not wording: pydantic's own `"Input should be a valid datetime"` versus our `"must be a date or an RFC 3339 timestamp"`. The assertion is what proves the bare-int carve-out produces its own diagnosis rather than the generic one. This row is the whole of its file's group 1 content, so no mechanical row remains for it. |
| G1-K20 | `cli/tests/manifests/test_loader_and_envelope.py::test_envelope_rejections::ConfigError::d62e9a,test_mise_inputs_fail_at_manifest_decode::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** Two parametrized sites taking `match=match` from their case tables: envelope mutations at `:164` and mise decode inputs at `:216`. Each table's cases share `ConfigError`, so the parameter is the only thing proving a case routes to its own diagnosis. |
| G1-K21 | `cli/tests/sessions/test_session_template_surface.py::test_an_unknown_key_in_the_harness_block_errors_at_build::ConfigError::f62e7e` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** `f"{field}: unknown field; expected one of:"` over the parametrized field, proving the refusal names the offending key inside `harness_integration`. |
| G1-K22 | `cli/tests/vms/test_platform_config_contract.py::test_azure_requires_the_three_location_keys_and_defaults_auth::ConfigError::24281b,test_azure_service_principal_arm_is_shape_checked::ConfigError::889881,test_azure_rejects_malformed_service_principal::ConfigError::d62e9a,test_gcp_gce_location_auth_and_subnet_are_shape_checked::ConfigError::b161f2,test_aws_ec2_rejects_malformed_credentials::ConfigError::d62e9a` | `match=` varying with the test's input | keep | **Second re-check, 2026-08-16.** Three interpolated loops (`:130` azure's three location keys, `:145` the service-principal identifiers, `:182` gcp's `project_id`/`zone`) and two parametrized tables (`:164`, `:227`) over seven and eight malformed-credential cases. This is the file L-026 covers, and L-026's finding stands for its remaining fourteen: `entity_kind` is identical on all of them. These five are the sites where the matched text is not identical, so the parameter still discriminates where the handle cannot. |
| G1-K23 | `website/tests/test_site_content.py::SourceContractTests.test_document_structure_failures_are_closed::ContractError::d62e9a,SourceContractTests.test_home_selection_still_fails_on_missing_duplicate_or_drift::ContractError::48374d` | `assertRaisesRegex` varying with the input | keep | **Second re-check, 2026-08-16.** The same shape in unittest's spelling, inside the range L-403 used to carry. `:270` takes `reason` from a five-case table of document-structure defects; `:329` is `rf"HOME_IDENTITY README.md .*{reason}"` over three, and additionally names the contract and source path. `ContractError` is shared by every case in both. **Corrected 2026-08-19 (map re-baseline).** Lines re-derived at HEAD from `:221,280`. |

### Sites the injected-marker screen pulled out of the mechanical batch

30 further sites over 19 files match a string the test file itself wrote and handed to an exception
constructor. For those the mechanical batch's shared justification is false in the way that decides
the row: the matched text is not prose this repository authors, it is a marker, and the assertion is
the proof that the INJECTED failure is the one observed. That is word for word why L-004, L-008,
L-009 and L-010 already keep; the screen finds the rest of that family instead of leaving it to
whoever happens to read a row closely.

`sweep-screen.py injected` derives them: a site qualifies when its needle appears in a string its
own module passes to an exception constructor AND appears in no string production emits. Both halves
matter. Without the first it is not a marker; without the second the test may be quoting a shipped
sentence through a fake, which is what the batch deletes. Comments and docstrings are excluded from
the production side, because a phrase discussed in a docstring is not a phrase the code says: two
production docstrings mention "original failure" and no production message contains it, which is the
difference between keeping G1-M07 and deleting it.

Thirteen further injected-marker sites sit in already-subtracted rows and are not rowed here. Eleven
of those carry a delete disposition that this rule makes wrong, and the subtraction section's
hand-off names them so the owning effort inherits the correction rather than rediscovering it. The
other two are already keeps (L-010), which is the screen agreeing with a row that was read closely
enough the first time.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| G1-I01 | `cli/tests/orchestration/test_activation.py::test_gate_span_closes_on_failure_in_the_body::RuntimeError::81f523` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`boom`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-018. |
| G1-I02 | `cli/tests/orchestration/test_readiness.py::test_skip_and_degrade_lets_non_rejections_propagate::RuntimeError::304fb2` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`{}: not a rejection`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-019. |
| G1-I03 | `cli/tests/plugins/gcp/test_compute.py::test_unexpected_proto_presence_failure_is_not_masked_as_absence::RuntimeError::034471` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`provider bug`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-023. |
| G1-I04 | `cli/tests/plugins/test_plugin_framework.py::test_seated_plugin_round_trips_on_exception::RuntimeError::81f523` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`boom`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-031. |
| G1-I05 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestPlainFailure.test_failure_before_the_clone_makes_no_cleanup_calls::ProxmoxAPIError::d9562b` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`cluster unreachable`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-034. |
| G1-I06 | `cli/tests/sessions/test_create_resume_orchestrated.py::test_create_failure_cleans_session_slice_then_unwinds_ephemerals::RuntimeError::5df21f` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`tmux exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-057. |
| G1-I07 | `cli/tests/test_azure_create_interrupt.py::TestPlainFailureArmUnchanged.test_failure_still_cleans_up_and_wraps::AzureError::d467cd,TestVMDeleteFailureWarns.test_vm_delete_failure_during_failure_rollback_warns_and_sweep_continues::AzureError::6b3572` | 2 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`nic exploded`, `wait exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-067. |
| G1-I08 | `cli/tests/test_azure_nsg_exposure.py::TestTransientRoute.test_exit_removes_allow_when_body_raises::RuntimeError::59596d` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`kaboom`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-070. |
| G1-I09 | `cli/tests/test_operational_json_boundaries.py::test_vm_describe_propagates_operator_abort_in_service_and_both_cli_formats::UserAbort::5e2619` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`operator declined`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-099. |
| G1-I10 | `cli/tests/test_session_create_ephemeral_prompts.py::test_admin_non_interactive_on_vm_with_agents_does_not_prompt::RuntimeError::ea04fc,test_workspace_prompt_picks_create_new::RuntimeError::2acb09,test_mode_prompt_picks_create_new::RuntimeError::7829c5` | 3 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`stop after agent realize`, `stop after mode-prompt gate`, `stop after workspace realize`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-113. |
| G1-I11 | `cli/tests/test_session_create_ephemeral_rollback.py::test_failure_after_ephemeral_create_rolls_back_ephemerals::RuntimeError::daeb30,test_new_agent_inherits_vm_from_existing_workspace::RuntimeError::7829c5` | 2 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`simulated session-internal failure`, `stop after agent realize`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-114. |
| G1-I12 | `cli/tests/test_session_create_ephemeral_vm_anchor.py::test_explicit_vm_agreeing_with_workspace_passes_anchor_check::RuntimeError::c9441c,test_no_vm_anchor_with_single_vm_auto_selects::RuntimeError::18946c,test_new_agent_with_explicit_agent_name::RuntimeError::7829c5,test_ephemeral_agent_name_defaults_to_session_name::RuntimeError::756fb1` | 4 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`stop after VM resolution`, `stop after agent realize`, `stop after anchor check`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-115. |
| G1-I13 | `cli/tests/test_vm_shell_platform.py::test_native_transport_closes_allow_on_exception_for_azure::RuntimeError::dbbbc8` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`simulated post-open failure`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-123. |
| G1-I14 | `cli/tests/test_wsl2_create_interrupt.py::test_failure_mid_provision_cleans_up_and_reraises::RuntimeError::b98d9a,test_cleanup_failure_warns_and_does_not_mask_the_original::RuntimeError::37a06e` | 2 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`apt exploded`, `original failure`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-125. |
| G1-I15 | `cli/tests/test_wsl2_keepalive.py::test_vm_active_terminates_on_exception::RuntimeError::81f523` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`boom`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-126. |
| G1-I16 | `cli/tests/vms/test_bootstrap_vm_hooks.py::test_failed_path_secures_the_kept_vm::RuntimeError::c1fee0,test_failed_path_hook_failure_does_not_mask::RuntimeError::c1fee0` | 2 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`phase a exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-135. |
| G1-I17 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_rollback_failure_warns_and_never_masks::ProvisioningError::870493,test_create_init_failure_keeps_the_row::ExternalError::f79466,test_create_phase_a_failure_maps_to_provisioning_error::ProvisioningError::a4de2d` | 3 injected-marker sites | keep | The matched text is a marker this test file writes and hands to an exception constructor (`backend exploded`, `bootstrap exploded`, `init exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-136. |
| G1-I18 | `cli/tests/vms/test_create_vm_dispatch.py::test_proxmox_token_resolves_end_to_end::ProvisioningError::d1bfad` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`halt after binding`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-137. |
| G1-I19 | `cli/tests/vms/test_lima_create_interrupt.py::test_failure_mid_create_cleans_up_and_reraises::SSHError::f65498` | 1 injected-marker site | keep | The matched text is a marker this test file writes and hands to an exception constructor (`provision exploded`), and it appears in no string production emits, so the assertion is proof that the INJECTED failure is the one observed. That is the reason L-004 and L-008 through L-010 already keep, and it makes the mechanical batch's shared justification false here: the matched string is not prose this repository authors. Pulled out of G1-140. |

### The mechanical batch

454 live `match=` sites over 131 files, one row per file, out of a ledger of 517 over 149. The gap
is the dead and subtracted rows, and it is the number to size a PR from. Every one is `delete`, and
the justification is shared rather than restated 152 times: the matched string is a fixed literal
that varies with nothing, and it is prose this repository authors, which no case in the taxonomy
licenses. The `raises` stays; only the `match=` argument goes.

Each row's cell lists the sites that row owns, generated from the estate minus the sites the
judgment and keep rows above claim. Read it as the row's claim; there is no range to over-apply, and
`sweep-screen.py attribute` fails loudly if a site ends up owned by two rows or by none.
**`sweep-screen.py generate` is that derivation**, so a batch cut at a later tree is emitted rather
than assembled by hand: it prints these rows in the row grammar, with the header and the
prettier-ignore line, ready to paste. Then it runs three checks over the claims plus what it just
generated, none of them a count comparison, because the generated set is the complement of the
claims and adds up by construction whatever the claims are:

1. **Ownership.** Every `match=` site owned by exactly one row, which is what catches two claim rows
   reaching the same site.
2. **Widened claims.** A group-1 claim that reaches a site through a span or a line anchor, which
   covers every assertion its test gains after the row was written rather than the one it was
   written about.
3. **Stale claims.** A group-1 claim anchor that does not resolve at this tree, because its sites
   fall into the batch as ordinary deletes and the judgment that pulled them out goes with no word
   said. A `[dead]` row is not a claim and is not checked; its file is gone, so it can neither own a
   site nor lose one. `[subtracted]` and `[deferred]` rows are still claims, because their sites are
   real and must not fall into the batch.

**At `a64b1b9c` the third check refuses, on 12 anchors over 9 rows, and that is work the fresh cut
owes**: L-013, L-018 (3), L-026, G1-M03, G1-M04, G1-K04 (2), G1-K14, G1-I02 and G1-I06. Each is a
judgment row whose evidence no longer reaches an assertion that exists, so the cut has to re-decide
it rather than carry it. `generate` also reports 8 group-1 span claims that resolve and cover no
site; those do not refuse, because one tree cannot tell a test that lost its sites from one that
never had any.

**Corrected 2026-08-19 by the callee-side raise screen.** This paragraph used to say that no test
function in the set raises the same type twice, and conclude from that that each site is `hla.md`
case 1, the raised type already discriminating. The premise is a fact about the CALLER and the
conclusion is a claim about the CALLEE, and the screen shows the conclusion is false for four in
five of the sites it can resolve: the operations these tests call raise the asserted type from
several paths, so the type discriminates nothing. What that changes is the reason and the cost, not
the edit. Where a handle tells the targeted raise apart the row has been pulled out to G1-C01
through G1-C04 and converts; everywhere else this batch is `hla.md` case 2's fallback rather than
its case 1, which R2.4 resolves to the same deletion with the branch coverage going too. **Stated
plainly, because it is what this batch now costs: after the edit, a same-type failure raised from a
different path in the same operation will satisfy these tests.** Rows carrying `[1-raise]` are the
twelve the screen verified as genuinely single-path; the rest of the batch is unscreened and the
executor owes it the screen per row before the edit lands.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition |
| --- | --- | --- | --- |
| G1-001 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_create_mutation_failure_cleans_up_and_leaves_no_row::ExternalError::faf476,test_reinit_mutation_failure_wraps_and_keeps_the_agent::ExternalError::5d8d65,test_reinit_unknown_update_template_raises_and_keeps_the_row::NotFoundError::788028,test_reinit_update_template_persists_before_convergence_so_a_mid_failure_keeps_the_new_binding::ExternalError::5d8d65,test_create_agent_on_disabled_plugin_recipe_refuses_before_any_work::StateError::c11533,test_reinit_update_template_to_disabled_recipe_refuses_before_persist::StateError::c11533` | 6 `match=` site(s) over ExternalError, NotFoundError, StateError | delete |
| G1-002 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_delete_sessions_guard_refuses_with_zero_resolves_and_zero_gate::StateError::54d6d7,test_delete_declined_confirm_aborts_with_zero_resolves_and_zero_gate::UserAbort::0cf0fb,test_delete_nested_rejects_a_mismatched_vm_node::StateError::207681` | 3 `match=` site(s) over StateError, UserAbort | delete |
| G1-003 | `cli/tests/agents/test_shell_exec_orchestrated.py::test_exec_dash_prefixed_command_fails_with_zero_resolves_and_zero_gate::ValidationError::05d8e1,test_exec_missing_command_after_double_dash_fails_pre_gate::ValidationError::1cfdd7,test_missing_grant_fails_with_zero_resolves_and_zero_gate::AuthorizationError::40fe55` | 3 `match=` site(s) over AuthorizationError, ValidationError | delete |
| G1-004 | `cli/tests/assistance/test_generation.py::test_unexpected_generated_file_is_rejected::GenerationError::d094e2,test_readme_requires_one_ordered_marker_pair::GenerationError::2b7814,test_metadata_schema_rejects_unknown_fields::GenerationError::1cc88e` | 3 `match=` site(s) over generator.GenerationError | delete |
| G1-005 | `cli/tests/capabilities/test_capability_config.py::test_an_unknown_field_names_the_fields_that_are_valid::ConfigError::bc57ee,test_the_arm_is_selected_by_the_capability_name_not_by_the_blob::ConfigError::b118d9,test_construct_time_validation_raises_the_same_framed_error::ConfigError::57f465` | 3 `match=` site(s) over ConfigError | delete |
| G1-006 | `cli/tests/capabilities/test_capability_descriptors.py::test_descriptor_for_raises_on_a_kind_with_no_record::StateError::d8ae40,test_mapping_descriptor_requires_contract_and_host_together::StateError::173cbe,test_mapping_descriptor_requires_unique_host_field_ownership::StateError::b5796e` | 3 `match=` site(s) over StateError | delete |
| G1-007 | `cli/tests/capabilities/test_declare_once_end_to_end.py::test_the_declarations_constraint_reaches_validation::ConfigError::7f6e39` | 1 `match=` site(s) over ConfigError | delete |
| G1-008 | `cli/tests/guide/test_read_only_database.py::L224,L231` | **[line-anchored: file gone]** **[dead]** 2 `match=` site(s) over StateError | delete |
| G1-009 | `cli/tests/manifests/test_capability_shape.py::test_a_kind_owned_key_inside_the_table_is_the_platforms_to_refuse::ConfigError::55786f` | 1 `match=` site(s) over ConfigError | delete |
| G1-010 | `cli/tests/manifests/test_decode_shapes.py::test_a_kind_owned_key_inside_the_provider_block_is_the_providers_to_refuse::ConfigError::b90cf6,test_git_credential_token_in_provider_config::ConfigError::0c2a11,test_provider_must_be_a_tagged_table::ConfigError::14fa63` | 3 `match=` site(s) over ConfigError | delete |
| G1-011 | `cli/tests/manifests/test_describe_kind.py::test_the_quoted_boolean_warning_describes_this_loader::ConfigError::96ba2c` | 1 `match=` site(s) over ConfigError | delete |
| G1-012 | `cli/tests/manifests/test_emit.py::test_a_capability_key_the_schema_rejects_is_rejected_on_every_host::ConfigError::6476e6` | 1 `match=` site(s) over ConfigError | delete |
| G1-014 | `cli/tests/manifests/test_loader_and_envelope.py::test_duplicate_within_one_file_errors::ConfigError::809c7f,test_invalid_yaml_reports_location::ConfigError::ecf065,test_unknown_kind_gets_kebab_hint::ConfigError::0b5f13,test_no_capability_kind_can_be_declared::ConfigError::8ef97a,test_singleton_kind_rejects_non_default_name::ConfigError::5ab464,test_unknown_metadata_key_rejected::ConfigError::ee6f26,test_missing_spec_key_rejected::ConfigError::d5bd17,test_duplicate_mapping_key_rejected::ConfigError::17aa38,test_unhashable_mapping_key_surfaces_clean_error::ConfigError::74d7af,test_merge_keys_rejected::ConfigError::6ffd92` | 10 `match=` site(s) over ConfigError | delete |
| G1-015 | `cli/tests/manifests/test_samples.py::test_all_kinds_concatenation_and_unknown_kind::ValidationError::0b5f13,test_write_sample_refuses_unloadable_dot_paths::ValidationError::253a7b` | 2 `match=` site(s) over ValidationError | delete |
| G1-016 | `cli/tests/manifests/test_spec_model.py::test_a_capability_kind_has_no_row_to_describe::ValidationError::e20e73` | **[1-raise]** 1 `match=` site(s) over ValidationError | delete |
| G1-017 | `cli/tests/manifests/test_spec_templates.py::test_an_inheriting_row_refuses_a_default_template_at_import::StateError::89f386,test_an_empty_auth_key_never_reaches_the_resolved_template::ConfigError::11505c` | 2 `match=` site(s) over ConfigError, StateError | delete |
| G1-018 | `cli/tests/orchestration/test_activation.py::test_operator_stopped_refusal_propagates_from_the_node::StateError::836f41,test_refusal_precedes_the_span::StateError::836f41` | 2 `match=` site(s) over StateError | delete |
| G1-019 | `cli/tests/orchestration/test_readiness.py::test_sweep_propagates_the_first_failure::ConfigError::898bdc,test_sweep_fails_fast_non_interactively_on_a_prompt_only_secret::ConfigError::68c2e2` | 2 `match=` site(s) over ConfigError | delete |
| G1-020 | `cli/tests/orchestration/test_secrets.py::test_require_predicted_refs_prompt_only_fails_fast_when_non_interactive::ConfigError::68c2e2,test_require_predicted_refs_without_config_is_loud::ConfigError::6d09f8,test_scoped_reader_refuses_undeclared_names::StateError::afdbd7,test_scoped_reader_is_loud_on_unresolved_declared_names::StateError::04ca9d,test_scoped_reader_satisfies_the_secret_reader_protocol::StateError::afdbd7,test_require_declared_refs_says_nothing_about_resolvability::ConfigError::c0139c` | **[subtracted: secrets-preview]** 6 `match=` site(s) over ConfigError, StateError | delete |
| G1-021 | `cli/tests/orchestration/test_walk.py::test_cycle_is_a_loud_error::StateError::3afe5d,test_self_cycle_is_a_loud_error::StateError::c50193,test_cycle_report_trims_the_acyclic_prefix::StateError::dfc6bd,test_two_objects_sharing_a_key_is_a_loud_error::StateError::c39242,test_two_objects_sharing_a_key_across_roots_is_loud_too::StateError::159139` | 5 `match=` site(s) over StateError | delete |
| G1-022 | `cli/tests/plugins/gcp/test_bootstrap.py::test_utf8_size_gate_accepts_exact_limit_and_rejects_plus_one::ConfigError::428af0` | **[1-raise]** 1 `match=` site(s) over ConfigError | delete |
| G1-023 | `cli/tests/plugins/gcp/test_compute.py::test_unrecognized_sdk_shape_treats_required_scalar_as_absent::ConfigError::7d834a,test_live_machine_architecture_mismatch_fails_before_mutation::ConfigError::b151c3,test_image_architecture_mismatch_is_rejected::ConfigError::54740a,test_exact_instance_collision_and_not_found_paths::AlreadyExistsError::321425,test_external_ipv4_is_read_live_from_named_nat_config::GCEError::11c86c` | 5 `match=` site(s) over AlreadyExistsError, ConfigError, GCEError | delete |
| G1-024 | `cli/tests/plugins/gcp/test_config.py::test_selection_failure_is_typed_and_names_the_largest_entry::ConfigError::6bda8b` | **[1-raise]** 1 `match=` site(s) over ConfigError | delete |
| G1-025 | `cli/tests/plugins/gcp/test_instance.py::test_indeterminate_wait_reconciles_only_matching_provider_id::AlreadyExistsError::4287e8,test_incomplete_operation_identity_is_never_owned::GCEOperationError::363691` | 2 `match=` site(s) over AlreadyExistsError, GCEOperationError | delete |
| G1-026 | `cli/tests/plugins/gcp/test_network.py::test_zone_to_region_is_exact_and_invalid_shape_rejected::ConfigError::071588,test_omitted_subnet_requires_and_resolves_default_network::ConfigError::e9eb7e,test_selected_network_is_re_read_and_identity_checked::ConfigError::ee2d42,test_shared_vpc_host_project_subnet_is_explicitly_unsupported::ConfigError::db3a37,test_network_policy_must_be_after_classic_firewall::ConfigError::f7cce7,test_applicable_priority_zero_allow_is_rejected::ConfigError::080e68,test_priority_zero_deny_overlapping_operator_ssh_is_rejected::ConfigError::604b28,test_firewall_name_collision_and_absence::AlreadyExistsError::321425,test_indeterminate_insert_mismatch_is_collision_and_never_deleted::AlreadyExistsError::c7f2c0,test_definite_already_exists_is_collision_without_shape_reconciliation::AlreadyExistsError::321425,test_same_request_retry_already_exists_remains_collision::AlreadyExistsError::321425,test_incomplete_or_wrong_operation_identity_is_never_owned::GCEOperationError::363691` | 12 `match=` site(s) over AlreadyExistsError, ConfigError, GCEOperationError | delete |
| G1-027 | `cli/tests/plugins/gcp/test_platform.py::test_known_machine_incompatibility_stays_pre_mutation::ConfigError::a89f38,test_delete_retains_same_name_different_id_instance_and_lifetime_deny::GCEOperationError::54eaaa,test_delete_refuses_noncanonical_persisted_allow_sources_before_any_mutation::StateError::a804c3,test_delete_keeps_deny_when_allow_read_and_instance_delete_are_indeterminate::GCEOperationError::54eaaa` | 4 `match=` site(s) over ConfigError, GCEOperationError, StateError | delete |
| G1-028 | `cli/tests/plugins/test_enablement_producer.py::test_disabled_plugin_platform_withholds_its_config_implied_secret::StateError::a2f8a5,test_mapping_to_a_disabled_plugin_backend_is_validated_like_any_other::ConfigError::8a9e29,test_mapping_to_an_absent_backend_reports_the_dangling_edge_not_a_shape_error::ConfigError::ee26df,test_resolve_git_credential_providers_refuses_a_disabled_provider::StateError::a2f8a5,test_ensure_harness_integration_enabled_raises_for_a_disabled_plugin_integration::StateError::a2f8a5` | 5 `match=` site(s) over ConfigError, StateError | delete |
| G1-029 | `cli/tests/plugins/test_gcp.py::test_gcloud_recipe_is_gated_until_gcp_is_enabled::StateError::3dba48,test_disabled_gcp_site_is_not_ready_and_refused_with_enable_hint::StateError::3dba48` | **[1-raise]** 2 `match=` site(s) over StateError | delete |
| G1-031 | `cli/tests/plugins/test_plugin_framework.py::test_rejects_instance_instead_of_class_the_secret_backend_trap::PluginError::819288,test_rejects_impl_with_missing_name::PluginError::f2c5d2,test_rejects_impl_with_slash_bearing_name::PluginError::c14cdd,test_rejects_intra_descriptor_collision::PluginError::b287b3,test_a_non_conforming_impl_is_refused_before_any_registry_write::PluginError::26905d,test_a_mixed_valid_and_version_one_plugin_contribution_seats_nothing::PluginError::fc8a23,test_secret_backend_subclass_under_a_taken_name_is_not_idempotent::PluginError::16a5bb` | 7 `match=` site(s) over PluginError | delete |
| G1-032 | `cli/tests/plugins/test_proxmox_api.py::TestErrorHandling.test_http_error_raises::ProxmoxAPIError::dcaada,TestErrorHandling.test_task_failure_raises::ProxmoxAPIError::973420,TestErrorHandling.test_task_timeout_raises::ProxmoxAPIError::3dcd80` | 3 `match=` site(s) over ProxmoxAPIError | delete |
| G1-033 | `cli/tests/plugins/test_proxmox_bootstrap_staging.py::test_failure_output_is_not_repeated_to_observable_error_text::ProvisioningError::47b304,test_forged_success_ip_is_not_returned_or_repeated::ProvisioningError::b7e6ca,test_execute_unwind_removes_stage::ProvisioningError::2ebb05` | 3 `match=` site(s) over ProvisioningError | delete |
| G1-034 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestFailClosedBootstrap.test_bootstrap_failure_cleanup_survivor_keeps_primary_and_manual_guidance::ProvisioningError::2ebb05` | 1 `match=` site(s) over ProvisioningError | delete |
| G1-035 | `cli/tests/plugins/test_publish.py::test_dirty_bundle_raises_config_error_not_assertion::ConfigError::7fa4b1,test_plugin_path_gates_missing_subdir_but_builtin_path_does_not::ConfigError::f18e8a,test_disabled_plugin_harness_integration_reaches_use_gate_not_unknown::StateError::5af925` | 3 `match=` site(s) over ConfigError, StateError | delete |
| G1-036 | `cli/tests/resources/test_admin_template_plurified.py::test_admin_template_kind_errors_on_unreserved_name_reference::ConfigError::ce1f89` | 1 `match=` site(s) over ConfigError | delete |
| G1-037 | `cli/tests/resources/test_cycle_detection.py::test_two_node_cycle_detected::ConfigError::c50193,test_three_node_cycle_detected::ConfigError::c50193` | 2 `match=` site(s) over ConfigError | delete |
| G1-038 | `cli/tests/resources/test_effective_template_dependencies.py::test_a_kind_nobody_registered_is_refused_rather_than_answered_empty::StateError::9707c5,test_the_effective_resolve_is_total_over_a_cyclic_chain::InheritanceCycleError::7b8fd2,test_a_cyclic_chain_still_reports_the_cycle_rather_than_a_degraded_graph::ConfigError::2a2415` | 3 `match=` site(s) over ConfigError, InheritanceCycleError, StateError | delete |
| G1-039 | `cli/tests/resources/test_finalize_pass.py::test_admin_template_rejects_non_default_name::ConfigError::901d43` | 1 `match=` site(s) over ConfigError | delete |
| G1-040 | `cli/tests/resources/test_git_credential_provider_kind.py::test_unknown_provider_errors_with_framework_shape::ConfigError::754f75` | 1 `match=` site(s) over ConfigError | delete |
| G1-041 | `cli/tests/resources/test_graph.py::test_graph_is_frozen_and_registry_rejects_refinalize::RuntimeError::77be2a,test_graph_property_before_finalize_raises::RuntimeError::7527a6` | 2 `match=` site(s) over RuntimeError | delete |
| G1-042 | `cli/tests/resources/test_harness_integration_kind.py::test_capability_kind_envelope_rejection::ConfigError::8ef97a` | 1 `match=` site(s) over ConfigError | delete |
| G1-043 | `cli/tests/resources/test_inheritance_layers.py::test_the_resolver_path_refuses_the_same_cycle::InheritanceCycleError::47b52f` | 1 `match=` site(s) over InheritanceCycleError | delete |
| G1-044 | `cli/tests/resources/test_inheritance_traversal.py::test_enablement_still_propagates_across_the_inheritance_edge::StateError::415a4a,test_the_gate_does_not_refuse_over_an_ancestor_leaf_the_child_overrode::StateError::a300da` | **[1-raise]** 2 `match=` site(s) over StateError | delete |
| G1-045 | `cli/tests/resources/test_install_resource_kinds.py::test_apt_package_typo_errors_with_source::ConfigError::9cf61e,test_system_install_command_typo_errors_with_source::ConfigError::eba334,test_user_install_command_typo_in_admin_errors::ConfigError::12d895,test_user_install_command_typo_in_agent_errors::ConfigError::12d895,test_unknown_apt_source_reference_errors_via_framework::ConfigError::a90f49` | 5 `match=` site(s) over ConfigError | delete |
| G1-046 | `cli/tests/resources/test_kind_synthesize_empty.py::test_secret_kind_raises_on_empty_requirements::NoUnreferencedDefaultError::884b23` | 1 `match=` site(s) over NoUnreferencedDefaultError | delete |
| G1-047 | `cli/tests/resources/test_readiness_fold.py::test_r9_9_mapping_is_validated_whether_or_not_its_backend_is_enabled::ConfigError::0efcc1,test_fold_does_not_throw_on_malformed_platform_config::ConfigError::fb3a37,test_r5_ready_site_with_unknown_field_fails_validation::ConfigError::1d0222` | 3 `match=` site(s) over ConfigError | delete |
| G1-048 | `cli/tests/resources/test_registry_collisions.py::test_builtin_over_operator_is_an_ordering_conflict::ConfigError::3cc894,test_operator_collision_has_no_singleton_exemption::ConfigError::f9adaf,test_line_zero_origin_collides_on_every_kind::ConfigError::52d9a9,test_two_system_plugins_on_one_name_is_a_curation_error::ConfigError::1a4114,test_system_plugin_over_builtin_collides::ConfigError::afdff2,test_builtin_over_system_plugin_collides_same_message::ConfigError::afdff2,test_operator_over_reserved_system_plugin_errors::ConfigError::cc2017,test_enabled_plugin_over_operator_reserved_kind_still_errors::ConfigError::df3328` | 8 `match=` site(s) over ConfigError | delete |
| G1-049 | `cli/tests/resources/test_registry_lifecycle.py::test_add_after_finalize_errors::RuntimeError::ffb304,test_finalize_twice_errors::RuntimeError::77be2a,test_unknown_kind_in_requirement_errors_clearly::ConfigError::39f539` | 3 `match=` site(s) over ConfigError, RuntimeError | delete |
| G1-050 | `cli/tests/resources/test_registry_weak_rows.py::test_weak_survivor_with_no_source_is_a_state_error::StateError::133b21,test_two_enabled_plugin_strong_rows_still_collide::ConfigError::1a4114` | 2 `match=` site(s) over ConfigError, StateError | delete |
| G1-051 | `cli/tests/resources/test_template_kinds.py::test_inherits_cycle_caught_by_framework::ConfigError::c6414d` | 1 `match=` site(s) over ConfigError | delete |
| G1-052 | `cli/tests/resources/test_vm_template_kind.py::test_non_default_self_reference_caught_by_framework::ConfigError::c6414d,test_inherits_cycle_through_default_caught_at_build_registry::ConfigError::c50193` | 2 `match=` site(s) over ConfigError | delete |
| G1-053 | `cli/tests/schema/test_shorthand.py::test_a_value_that_is_neither_spelling_is_still_refused::ValueError::be051b,test_a_boolean_is_not_an_integer_shorthand::ValueError::be051b,test_a_shorthand_folding_into_a_field_that_does_not_exist_is_refused_at_import::StateError::fde132,test_a_shorthand_spelled_as_something_no_document_carries_is_refused::StateError::4d99c7,test_the_declaration_is_the_only_place_the_spelling_is_written::ValueError::be051b,test_an_arm_shorthand_alone_does_not_dispatch_a_tagged_union::ValueError::be051b,test_a_collection_union_documents_only_its_selected_scalar_arm::ValueError::be051b,test_a_collapsed_collection_union_keeps_scalar_dispatch_explicit::ValueError::be051b` | 8 `match=` site(s) over StateError, ValueError | delete |
| G1-156 | `cli/tests/secrets/test_resolution_lifecycle.py::test_guidance_is_rejected_outside_the_allowed_detail::ValueError::8c2cbd,test_forged_timeout_guidance_is_rejected_at_construction::ValueError::f8aac5,test_forged_timeout_guidance_never_reaches_a_resolution_outcome::ValueError::f8aac5` | **[subtracted: secrets-preview]** 3 `match=` site(s) over ValueError | delete |
| G1-054 | `cli/tests/secrets/test_resolver_seed.py::test_unseeded_pre_pass_read_still_raises::StateError::52c863,test_seeding_after_the_pass_is_a_loud_error::StateError::8087b7` | **[subtracted: secrets-preview]** 2 `match=` site(s) over StateError | delete |
| G1-055 | `cli/tests/secrets/test_sources.py::test_direct_secret_decl_requires_exact_string_keys::ValidationError::aa4c28,test_mapping_validation_does_not_accept_another_registered_models_shape::ConfigError::80464d,test_mapping_key_collector_rejects_unknown_source_even_for_false::ConfigError::35edc2` | **[subtracted: secrets-preview]** 3 `match=` site(s) over ConfigError, ValidationError | delete |
| G1-056 | `cli/tests/sessions/test_console_attach_orchestrated.py::test_named_console_no_tailscale_fails_with_zero_resolves_and_zero_gate::StateError::d60755` | 1 `match=` site(s) over StateError | delete |
| G1-057 | `cli/tests/sessions/test_create_resume_orchestrated.py::test_resume_missing_binary_aborts_with_the_old_session_running::StateError::2dc28d,test_create_new_agent_on_disabled_plugin_recipe_refuses_before_any_work::StateError::c11533` | 2 `match=` site(s) over StateError | delete |
| G1-058 | `cli/tests/sessions/test_effective_config_validation.py::test_a_parent_that_cannot_stand_alone_is_itself_a_load_error::ConfigError::89deb5` | 1 `match=` site(s) over ConfigError | delete |
| G1-059 | `cli/tests/sessions/test_session_nodes.py::test_scope_less_context_is_a_loud_error::StateError::2685b8,test_missing_command_is_a_typed_error::StateError::28899f,test_admin_mode_error_names_the_vm::StateError::2dc28d,test_missing_transport_defers_at_preflight_and_is_loud_at_runup::StateError::84d1fe,test_live_session_admin_mode_comes_from_the_row::StateError::220806,test_live_session_agent_row_with_no_agent_node_is_loud::StateError::1e8222,test_live_session_admin_row_with_an_agent_node_is_loud::StateError::01cebb,test_live_session_agent_name_mismatch_is_loud::StateError::211d74` | 8 `match=` site(s) over StateError | delete |
| G1-060 | `cli/tests/sessions/test_session_template_surface.py::test_manifest_flat_field_is_rejected::ConfigError::c1d69f,test_manifest_unknown_harness_name_errors_at_finalize::ConfigError::de2824` | 2 `match=` site(s) over ConfigError | delete |
| G1-061 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_batch_operator_stopped_vm_aborts_before_the_probes::StateError::836f41,test_batch_gate_refuses_an_undeclared_outside_union_name::StateError::c04fb6,test_attach_session_reachable_vm_is_one_boundary_burst::StateError::415ed7,test_session_logs_reachable_vm_is_one_boundary_burst::StateError::415ed7,test_unknown_session_refuses_with_zero_resolves_and_zero_gate::NotFoundError::7a369b,test_unknown_workspace_refuses_with_zero_resolves_and_zero_gate::NotFoundError::efb2b2,test_no_tailscale_vm_fails_pre_gate_with_zero_resolves::StateError::d60755` | 7 `match=` site(s) over NotFoundError, StateError | delete |
| G1-063 | `cli/tests/test_apt.py::test_apt_source_rejects_unsafe_source_file::ConfigError::b9f663,test_apt_source_requires_key_url::ConfigError::dd46e2,test_apt_source_must_be_table::ConfigError::d65852,test_apt_package_apt_must_be_list::ConfigError::805256,test_bad_apt_source_reference_errors_at_build_registry::ConfigError::7945bc` | 5 `match=` site(s) over ConfigError | delete |
| G1-064 | `cli/tests/test_aws_credentials.py::TestCredentialSelection.test_context_without_resolved_secrets_is_typed::ConfigError::880d18` | 1 `match=` site(s) over ConfigError | delete |
| G1-065 | `cli/tests/test_aws_ec2_ops.py::TestCreate.test_no_default_subnet_is_typed::EC2Error::cb137f,TestCreate.test_user_data_over_cap_is_typed::EC2Error::165daa,TestCreate.test_rejects_a_name_collision::StateError::321425,TestCreate.test_arch_cross_check_rejects_a_mismatch::ConfigError::808d46,TestCreate.test_disk_describe_images_failure_is_typed::EC2Error::ba1935,TestTransientRoute.test_requires_a_security_group_id::StateError::346c89` | 6 `match=` site(s) over ConfigError, EC2Error, StateError | delete |
| G1-066 | `cli/tests/test_aws_instance_type_selection.py::TestInstanceCatalog.test_arch_error_names_the_ec2_vocabulary::ConfigError::8ceb92` | 1 `match=` site(s) over ConfigError | delete |
| G1-067 | `cli/tests/test_azure_create_interrupt.py::TestFailureDuringInlineWait.test_non_ssh_error_escaping_the_wait_rolls_back_vm_first_and_wraps::AzureError::7f5a55,TestFailureDuringInlineWait.test_ssh_readiness_exhaustion_fails_closed_without_key_delivery::AzureError::3cd0dc,TestFailureDuringInlineWait.test_cloud_init_wait_failure_fails_closed_without_key_delivery::AzureError::71cfc9` | 3 `match=` site(s) over AzureError | delete |
| G1-068 | `cli/tests/test_azure_credentials.py::TestCredentialSelection.test_service_principal_rejection_surfaces_through_the_ops::AzureError::453d97,TestCredentialSelection.test_service_principal_without_delivered_secrets_is_typed::ConfigError::880d18` | 2 `match=` site(s) over AzureError, ConfigError | delete |
| G1-069 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_unidentified_failure_raises_azure_error::AzureError::646976,TestSurvivingVMRaises.test_malformed_details_classify_as_generic_without_raising::AzureError::f9fd6c,TestSurvivingVMRaises.test_silent_survival_raises_even_without_a_captured_failure::AzureError::44f7d1,TestSurvivingVMRaises.test_unconfirmable_probe_raises::AzureError::724ede` | 4 `match=` site(s) over AzureError | delete |
| G1-070 | `cli/tests/test_azure_nsg_exposure.py::TestPrefixAssembly.test_invalid_extra_rejected::ConfigError::f5f627` | 1 `match=` site(s) over ConfigError | delete |
| G1-071 | `cli/tests/test_azure_vm_size_selection.py::TestValidateConfig.test_rejects_malformed_vm_sizes_at_load::ConfigError::f980ff,TestValidateConfig.test_still_rejects_unknown_field::ConfigError::1d0222` | 2 `match=` site(s) over ConfigError | delete |
| G1-072 | `cli/tests/test_capability_base.py::test_construct_validates_config::ConfigError::2e4421` | 1 `match=` site(s) over ConfigError | delete |
| G1-073 | `cli/tests/test_capability_config_contract.py::test_azdo_org_required::ConfigError::ce67de,test_azdo_rejects_unknown_blob_fields_yaml::ConfigError::118431,test_a_capability_with_no_config_rejects_every_key::ConfigError::13bc20,test_github_rejects_unknown_blob_fields::ConfigError::9218cd,test_unknown_provider_defers_to_miss_policy::ConfigError::8415f4,test_a_broken_blob_is_refused_whatever_the_plugins_state::ConfigError::570578,test_cycle_reported_before_malformed_block::ConfigError::c6414d,test_construct_time_validation_survives_the_flip::ConfigError::ce67de,test_env_var_mapping_validated_at_build_registry::ConfigError::f82117,test_prompt_rejects_any_mapping::ConfigError::4da920,test_declared_mapping_for_non_opted_in_source_is_validated_at_build::ConfigError::f82117` | 11 `match=` site(s) over ConfigError | delete |
| G1-074 | `cli/tests/test_claude_code_integration.py::test_validation_rejects_unknown_field::ConfigError::de5fea,test_validation_rejects_non_list_extra_args::ConfigError::d0b029,test_construct_revalidates_config::ConfigError::0aeeb1,test_probe_keeps_find_failure_distinct_from_a_clean_no_match::StateError::cff336,test_probe_that_could_not_execute_raises_rather_than_guessing::StateError::cff336,test_readiness_missing_claude_is_a_typed_error::StateError::89607a` | 6 `match=` site(s) over ConfigError, StateError | delete |
| G1-075 | `cli/tests/test_codex_integration.py::test_validation_rejects_unknown_field::ConfigError::cc73ed,test_validation_rejects_non_list_extra_args::ConfigError::d0b029,test_construct_revalidates_config::ConfigError::0aeeb1,test_recorder_file_that_exists_but_will_not_read_raises::StateError::8b3e04,test_discovery_with_an_unresolvable_workspace_raises::StateError::7c211d,test_discovery_probe_failure_raises_rather_than_guessing::StateError::cff336,test_rollout_probe_that_could_not_execute_raises_rather_than_guessing::StateError::cff336,test_rollout_probe_keeps_find_failure_distinct_from_a_clean_no_match::StateError::cff336,test_resume_without_a_launch_target_raises_rather_than_guessing::StateError::1a1ba7,test_merge_config_never_launders_an_invalid_writable_dirs_entry::ConfigError::26b714,test_sh_probe_missing_workspace_dir_raises::StateError::7c211d,test_readiness_missing_codex_is_a_typed_error::StateError::c3366c` | 12 `match=` site(s) over ConfigError, StateError | delete |
| G1-076 | `cli/tests/test_completions.py::TestGeneration.test_unsupported_shell_raises::ValueError::999d26` | 1 `match=` site(s) over ValueError | delete |
| G1-077 | `cli/tests/test_config.py::test_cycle_detection::ConfigError::c50193,test_invalid_git_credential_type::ConfigError::2a899e,test_unexpected_top_level_keys_fail::ConfigError::74d21b,test_dotfiles_keeps_its_removal_guidance::ConfigError::4c886a,test_extra_ssh_public_keys_missing_file::ConfigError::8a3dd8,test_workload_gated_issues_fatal_true_still_raises_by_default::ConfigError::5d6e5d,test_ssh_allow_cidrs_invalid_entry_rejected::ConfigError::2bad2a,test_ssh_allow_cidrs_scalar_rejected::ConfigError::84e943,test_user_section_is_rejected::ConfigError::973a2a,test_claude_marketplaces_rejects_string::ConfigError::0eeb61,test_named_console_tmux_layout_rejects_unknown::ConfigError::17b7bf,test_named_console_section_unexpected_keys_are_refused::ConfigError::68bf79` | 12 `match=` site(s) over ConfigError | delete |
| G1-078 | `cli/tests/test_config_env_and_secrets.py::test_invalid_env_key_raises::ConfigError::c44c01,test_env_inline_table_unknown_key_rejected::ConfigError::4e980d,test_env_secret_must_be_string::ConfigError::2bb80d,test_secret_true_in_backend_mappings_rejected::ConfigError::b5bea4,test_unknown_source_name_raises::ConfigError::f9f541,test_unreachable_secret_raises::ConfigError::5e6017,test_session_template_required_commands_must_be_list::ConfigError::7cef1e,test_session_template_required_commands_must_be_strings::ConfigError::ecfca8,test_explicit_secret_declaration_invalid_still_raises::ConfigError::65cc69` | 9 `match=` site(s) over ConfigError | delete |
| G1-079 | `cli/tests/test_config_plugins.py::test_plugins_section_not_a_table_raises::ConfigError::36e825,test_plugins_system_not_a_list_of_strings_raises::ConfigError::0e20d3,test_plugins_system_list_with_non_string_element_raises::ConfigError::0e20d3,test_plugins_unknown_key_is_a_hard_config_error::ConfigError::501ed4,test_plugins_old_enabled_key_is_a_hard_config_error::ConfigError::8824b4,test_plugins_unknown_key_alongside_valid_system_still_raises::ConfigError::501ed4` | 6 `match=` site(s) over ConfigError | delete |
| G1-080 | `cli/tests/test_config_vm_workspaces.py::test_rejects_home_paths::ConfigError::186931,test_rejects_home_path_on_windows_style_host::ConfigError::186931,test_load_config_rejects_home_vm_workspaces::ConfigError::186931` | 3 `match=` site(s) over ConfigError | delete |
| G1-081 | `cli/tests/test_consoles_orchestration.py::test_running_session_names_raises_on_unreachable::ConnectivityError::8b373f,test_infer_vm_from_session_specs::ValidationError::617c1d,test_create_console_rejects_empty_without_all::ValidationError::7a888b,test_create_console_rejects_empty_fill_all::ValidationError::972d37,test_create_console_rejects_duplicate_name::AlreadyExistsError::321425,test_create_console_rejects_unknown_vm::NotFoundError::907ba7,test_create_console_rejects_unknown_session::NotFoundError::907ba7,test_create_console_rejects_cross_vm_session::ValidationError::b9fa0d,test_create_console_rejects_dup_in_args::ValidationError::7ed09c,test_create_console_rolls_back_on_failure::AlreadyExistsError::321425,test_add_sessions_rejects_duplicate::AlreadyExistsError::9f23d7,test_remove_sessions_rejects_non_member::NotFoundError::588bcf,test_reorder_sessions_rejects_non_member::NotFoundError::588bcf,test_reorder_sessions_rejects_duplicates::ValidationError::7ed09c,test_reorder_sessions_rejects_missing_console::NotFoundError::f78595,test_reorder_sessions_rejects_empty_input::ValidationError::52d9aa,test_add_shell_rejects_non_member::NotFoundError::588bcf` | 17 `match=` site(s) over AlreadyExistsError, ConnectivityError, NotFoundError, ValidationError | delete |
| G1-082 | `cli/tests/test_consoles_restore.py::test_restore_session_errors_when_console_missing::NotFoundError::f78595,test_restore_session_errors_when_session_not_member::NotFoundError::b24eed,test_restore_session_errors_when_tmux_not_running::StateError::f48f2b,test_restore_session_strict_on_untagged_pane::StateError::ddb535,test_restore_session_strict_on_out_of_range_tag::StateError::ddaf92,test_restore_session_strict_on_duplicate_tags::StateError::3d949f,test_restore_session_strict_message_when_configured_zero::StateError::973299,test_restore_session_rebuild_raises_when_new_window_fails::ExternalError::d24675,test_restore_session_rebuild_raises_when_shell_split_fails::ExternalError::7377b4,test_restore_session_rebuild_raises_when_shell_tag_fails::ExternalError::6e5226,test_restore_session_rebuild_refuses_when_session_row_gone::StateError::9f5d13,test_restore_session_refuses_when_session_pane_killed::StateError::50fcd9,test_restore_session_refuses_when_session_pane_killed_leaves_duplicate::StateError::50fcd9,test_restore_session_refuses_killed_session_pane_under_pane_base_index_one::StateError::881ffb,test_restore_session_raises_when_split_returns_no_pane_id::ExternalError::e04143` | 15 `match=` site(s) over ExternalError, NotFoundError, StateError | delete |
| G1-083 | `cli/tests/test_database_backup.py::test_restore_rejects_generic_sqlite_before_destination_open::StateError::052c20,test_restore_rejects_current_common_sentinel_lookalike::StateError::e8cb9b,test_restore_rejects_v1_with_first_committed_v2_ddl_before_destination_open::StateError::4f8486,test_restore_rejects_v3_with_first_committed_v4_table_before_destination_open::StateError::e08628,test_restore_rejects_missing_column_before_destination_open::StateError::93d619,test_restore_refuses_future_version_before_destination_open::StateError::a57e10,test_restore_rejects_identical_paths::ValidationError::5bf356,test_automatic_backup_refuses_a_version_that_does_not_match_its_source::StateError::aebe96,test_missing_or_malformed_backup_source_creates_nothing::StateError::60ec9b,test_restore_held_destination_lock_honors_fixed_deadline::BackupError::952480` | **[subtracted: instance-model]** 10 `match=` site(s) over BackupError, StateError, ValidationError | delete |
| G1-084 | `cli/tests/test_database_migration_safety.py::test_stale_inspector_waits_then_refuses_changed_state::StateError::fa2a54,test_prepare_refuses_changed_stale_state_when_lock_released_before_first_acquire::StateError::fa2a54,test_prepare_refuses_identical_tainted_baseline_after_lock_released_before_first_acquire::StateError::4f8486,test_safe_open_refuses_changed_but_still_stale_state::StateError::b4799c,test_partial_migration_failure_reports_exact_backup_recovery::StateError::4f19d8,test_partial_migration_failure_without_backup_says_so::StateError::4f19d8,test_completion_probe_unavailable_state_fails_before_database_caller::StateError::5240c0,test_database_refuses_future_schema_without_advancing_it::StateError::a57e10,test_database_closes_connection_when_migration_raises::RuntimeError::81f523,test_get_db_invalid_focused_config_stops_before_migration::ConfigError::37643a,test_get_db_selected_backup_failure_prevents_migration_and_guides_retry::BackupError::239dfc` | **[subtracted: instance-model]** 11 `match=` site(s) over BackupError, ConfigError, RuntimeError, StateError | delete |
| G1-085 | `cli/tests/test_db_migration_vm_sites.py::test_unknown_platform_fails_loudly_and_retry_works_after_fix::IntegrityError::508dc2,test_remote_lima_site_name_collision_fails_loudly::IntegrityError::dcfd35,test_earlier_versions_checkpoint_when_a_later_one_fails::IntegrityError::508dc2` | **[subtracted: instance-model]** 3 `match=` site(s) over sqlite3.IntegrityError | delete |
| G1-086 | `cli/tests/test_declared_resource.py::test_something_that_is_neither_is_a_loud_failure::StateError::6faf1e` | **[1-raise]** 1 `match=` site(s) over StateError | delete |
| G1-087 | `cli/tests/test_doctor_env_and_secrets.py::test_mapping_to_undeclared_kind_hard_errors_at_build::ConfigError::9da0f5,test_mapping_to_multiple_undeclared_kinds_hard_errors_at_build::ConfigError::62f437` | **[subtracted: secrets-preview]** 2 `match=` site(s) over ConfigError | delete |
| G1-088 | `cli/tests/test_doctor_schema.py::test_schema_check_distinguishes_absent_table_from_malformed_shape::StateError::7dd5c5,test_schema_check_rejects_a_non_table_schema_entry::StateError::7dd5c5` | **[subtracted: instance-model]** 2 `match=` site(s) over StateError | delete |
| G1-089 | `cli/tests/test_env_compose.py::test_compose_env_raises_loudly_on_uncovered_secret::RuntimeError::0b7a46` | **[1-raise]** 1 `match=` site(s) over RuntimeError | delete |
| G1-090 | `cli/tests/test_env_entry.py::test_neither_value_nor_secret_raises::ValueError::1229e2,test_both_value_and_secret_raises::ValueError::9f7534` | 2 `match=` site(s) over ValueError | delete |
| G1-091 | `cli/tests/test_env_merge.py::test_admin_and_agent_both_set_raises::ValueError::011b66` | **[1-raise]** 1 `match=` site(s) over ValueError | delete |
| G1-092 | `cli/tests/test_env_show.py::test_no_flags_raises_validation_error::ValidationError::ac308c` | 1 `match=` site(s) over ValidationError | delete |
| G1-093 | `cli/tests/test_git_credential_scoping.py::test_empty_token_rejected_by_validation::ConfigError::496396,test_scope_collision_is_loud::ConfigError::8339a1,test_manifest_scope_validation_has_file_line::ConfigError::fa7fba,test_unsafe_scope_values_rejected_at_build::ConfigError::629e3f,test_azdo_org_charset_validated::ConfigError::fa7fba` | 5 `match=` site(s) over ConfigError | delete |
| G1-094 | `cli/tests/test_git_token_verification.py::test_github_401_is_definitive_rejection::TokenRejectedError::328b45,test_github_runup_without_secrets_is_error::ConfigError::880d18,test_azdo_rejection_statuses::TokenRejectedError::8fa86f` | 3 `match=` site(s) over ConfigError, TokenRejectedError | delete |
| G1-095 | `cli/tests/test_initializer_mise.py::test_mise_validation_rejects_control_characters::ValueError::72b864` | 1 `match=` site(s) over ValueError | delete |
| G1-096 | `cli/tests/test_install_commands.py::test_legacy_test_field_rejected::ConfigError::16eca7,test_system_command_requires_command::ConfigError::412326` | 2 `match=` site(s) over ConfigError | delete |
| G1-097 | `cli/tests/test_name_filter_validation.py::test_session_list_rejects_unknown_vm::NotFoundError::308f1a,test_session_list_rejects_unknown_workspace::NotFoundError::58dd61,test_session_list_rejects_unknown_agent::NotFoundError::84b334,test_session_list_csv_with_one_bad_element_rejects::NotFoundError::308f1a,test_stop_all_sessions_rejects_unknown_vm::NotFoundError::308f1a,test_resume_all_sessions_rejects_unknown_vm::NotFoundError::308f1a,test_agent_list_rejects_unknown_vm::NotFoundError::308f1a,test_workspace_list_rejects_unknown_vm::NotFoundError::308f1a,test_console_list_rejects_unknown_vm::NotFoundError::308f1a` | 9 `match=` site(s) over NotFoundError | delete |
| G1-098 | `cli/tests/test_operation_scope.py::test_vm_scope_requires_its_vm::StateError::dea803,test_admin_is_session_vocabulary_only::StateError::8c6976,test_error_names_every_violation_at_once::StateError::ef410c,test_workspace_scope_forbids_admin::StateError::8c6976,test_agent_scope_forbids_admin::StateError::8c6976,test_session_scope_requires_exactly_one_launch_identity::StateError::410516` | 6 `match=` site(s) over StateError | delete |
| G1-100 | `cli/tests/test_resource_edit.py::test_builtin_capability_has_no_file_to_edit::ValidationError::5c73a5,test_builtin_declarable_resource_points_at_sample::ValidationError::5c73a5,test_auto_declared_resource_has_no_file_to_edit::ValidationError::6a7add` | 3 `match=` site(s) over ValidationError | delete |
| G1-101 | `cli/tests/test_resource_list.py::test_unknown_kind_filter_raises_unknown_kind_error::NotFoundError::189de4` | **[1-raise]** 1 `match=` site(s) over NotFoundError | delete |
| G1-102 | `cli/tests/test_run_context.py::test_secret_without_a_reader_is_a_typed_error::ConfigError::880d18` | 1 `match=` site(s) over ConfigError | delete |
| G1-103 | `cli/tests/test_sample_config_git_credentials.py::test_empty_token_string_rejected::ConfigError::496396` | 1 `match=` site(s) over ConfigError | delete |
| G1-104 | `cli/tests/test_secret_sources_drive_harness.py::test_child_environment_drops_inherited_home_credentials_and_import_paths::RuntimeError::954fbe` | **[subtracted: secrets-preview]** 1 `match=` site(s) over RuntimeError | delete |
| G1-105 | `cli/tests/test_secret_verify.py::test_verify_looks_up_every_unique_name_before_source_work::NotFoundError::f5fd2c,test_outcome_rejects_every_splitlines_separator::ValueError::bf2dd5` | **[subtracted: secrets-preview]** 2 `match=` site(s) over NotFoundError, ValueError | delete |
| G1-106 | `cli/tests/test_secrets_eager_resolve_console.py::test_console_add_shell_eager_resolve_fires_before_db_update::SecretUnavailableError::8c2840,test_attach_console_build_path_eager_resolves_before_tmux::SecretUnavailableError::8c2840,test_console_add_sessions_with_shells_eager_resolves::SecretUnavailableError::8c2840,test_restore_session_window_missing_branch_eager_resolves::SecretUnavailableError::8c2840` | **[subtracted: secrets-preview]** 4 `match=` site(s) over SecretUnavailableError | delete |
| G1-107 | `cli/tests/test_secrets_eager_resolve_sessions.py::test_session_create_eager_resolve_fires_before_db_insert::SecretUnavailableError::8c2840,test_session_resume_broken_no_force_bails_before_eager_resolve::BrokenStateError::f52679,test_session_resume_eager_resolve_fires_before_kill::SecretUnavailableError::8c2840` | **[subtracted: secrets-preview]** 3 `match=` site(s) over BrokenStateError, SecretUnavailableError | delete |
| G1-108 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_vm_shell_eager_resolve_fires_before_ssh::SecretUnavailableError::8c2840,test_vm_exec_eager_resolve_fires_before_ssh::SecretUnavailableError::8c2840,test_agent_exec_eager_resolve_fires_before_ssh::SecretUnavailableError::8c2840` | **[subtracted: secrets-preview]** 3 `match=` site(s) over SecretUnavailableError | delete |
| G1-109 | `cli/tests/test_secrets_orchestration.py::test_unknown_reference_raises_instead_of_dropping::StateError::82e531,test_admin_and_agent_in_same_target_raises::ValueError::d9c27d` | **[subtracted: secrets-preview]** 2 `match=` site(s) over StateError, ValueError | delete |
| G1-110 | `cli/tests/test_secrets_prompt.py::test_allowed_prompt_without_broker_is_state_error::StateError::371b3e` | **[subtracted: secrets-preview]** 1 `match=` site(s) over StateError | delete |
| G1-111 | `cli/tests/test_secrets_resolver.py::test_get_before_resolve_raises::StateError::52c863,test_get_unregistered_name_raises::StateError::88c7aa,test_late_registration_then_resolve_raises::StateError::89be6e` | **[subtracted: secrets-preview]** 3 `match=` site(s) over StateError | delete |
| G1-112 | `cli/tests/test_session_create_ephemeral_prompt_filtering.py::test_workspace_prompt_filters_by_vm_flag::RuntimeError::391447,test_workspace_prompt_filters_by_existing_agent_vm::RuntimeError::391447,test_mode_prompt_filters_by_resolved_vm_with_info_line::RuntimeError::391447,test_vm_and_existing_agent_mismatch_fails_before_workspace_prompt::ValidationError::342797,test_mode_prompt_picks_existing_agent_pins_vm_no_vm_prompt::RuntimeError::391447,test_mode_prompt_picks_admin_then_vm_prompt_fires::RuntimeError::391447` | 6 `match=` site(s) over RuntimeError, ValidationError | delete |
| G1-113 | `cli/tests/test_session_create_ephemeral_prompts.py::test_mode_required_in_non_interactive::ValidationError::09b0a2,test_workspace_required_in_non_interactive::ValidationError::62b672,test_workspace_prompt_picks_existing::RuntimeError::391447,test_mode_prompt_picks_admin::RuntimeError::391447,test_mode_prompt_picks_existing_agent::RuntimeError::391447` | 5 `match=` site(s) over RuntimeError, ValidationError | delete |
| G1-114 | `cli/tests/test_session_create_ephemeral_rollback.py::test_validation_failure_does_not_trigger_rollback::ValidationError::342797` | 1 `match=` site(s) over ValidationError | delete |
| G1-115 | `cli/tests/test_session_create_ephemeral_vm_anchor.py::test_cross_vm_existing_workspace_and_agent_fails_upfront::ValidationError::342797,test_explicit_vm_disagreeing_with_workspace_fails_upfront::ValidationError::342797,test_no_vm_anchor_with_multiple_vms_raises_in_non_interactive::ValidationError::b8d7d0,test_no_vm_anchor_with_zero_vms_raises::NotFoundError::87b24e,test_workspace_and_new_workspace_mutex::ValidationError::d0944f,test_workspace_template_requires_new_workspace::ValidationError::54f54b,test_admin_and_agent_mutex::ValidationError::a07a37,test_agent_template_requires_new_agent::ValidationError::a98c4c,test_ephemeral_workspace_name_collision_raises::AlreadyExistsError::229a18` | 9 `match=` site(s) over AlreadyExistsError, NotFoundError, ValidationError | delete |
| G1-116 | `cli/tests/test_session_liveness.py::test_legacy_admin_session_without_socket_raises_state_error::StateError::596b15` | 1 `match=` site(s) over StateError | delete |
| G1-117 | `cli/tests/test_session_transport.py::test_create_session_aborts_on_missing_required_command::StateError::4a8f54` | 1 `match=` site(s) over StateError | delete |
| G1-118 | `cli/tests/test_sessions_tmux_create.py::test_probe_transport_failure_raises_could_not_verify_without_kill::StateError::6ddd56,test_dead_pane_no_main_output_falls_back_to_alternate_screen::StateError::9fb10c,test_dead_pane_with_no_output_at_all_says_so::StateError::4c1706` | 3 `match=` site(s) over StateError | delete |
| G1-119 | `cli/tests/test_shell_integration.py::test_validation_rejects_unknown_field::ConfigError::07875e,test_validation_rejects_deprecated_runtime_field::ConfigError::971740,test_validation_rejects_non_string_command::ConfigError::734417,test_validation_rejects_non_string_required_commands::ConfigError::ecfca8,test_construct_revalidates_config::ConfigError::0aeeb1,test_merge_never_launders_an_invalid_required_commands_entry::ConfigError::5b23b8,test_missing_command_is_a_typed_error_naming_the_vm::StateError::28899f,test_agent_mode_missing_command_names_the_agent::StateError::2dc28d,test_scope_less_context_is_a_loud_error::StateError::2685b8,test_agent_mode_absent_target_is_a_loud_error::StateError::000689,test_missing_transport_defers_at_preflight_and_is_loud_at_runup::StateError::84d1fe,test_identity_guard_raises_on_vm_mismatch::StateError::f9c333,test_identity_guard_raises_on_agent_mismatch::StateError::445105,test_identity_guard_raises_on_mode_mismatch::StateError::8c6976` | 14 `match=` site(s) over ConfigError, StateError | delete |
| G1-120 | `cli/tests/test_templates.py::test_unknown_template::NotFoundError::4a43d4` | 1 `match=` site(s) over NotFoundError | delete |
| G1-121 | `cli/tests/test_topics.py::test_empty_prose_is_refused_rather_than_rendered_blank::StateError::2a6f8f` | 1 `match=` site(s) over StateError | delete |
| G1-122 | `cli/tests/test_traversal.py::test_a_hook_that_raises_refuses_the_input::ValueError::47b52f` | 1 `match=` site(s) over ValueError | delete |
| G1-124 | `cli/tests/test_workspace_rooted_shells.py::test_shell_vm_workspace_unknown_raises_not_found::NotFoundError::ca3704,test_shell_vm_workspace_cross_vm_raises_validation::ValidationError::ff94b2,test_exec_vm_workspace_cross_vm_raises_validation::ValidationError::ff94b2,test_exec_agent_workspace_cross_vm_raises_validation::ValidationError::ff94b2,test_exec_agent_workspace_missing_grant_raises_authz::AuthorizationError::40fe55,test_exec_vm_rejects_dash_prefixed_command::ValidationError::05d8e1,test_exec_agent_rejects_dash_prefixed_command::ValidationError::05d8e1` | 7 `match=` site(s) over AuthorizationError, NotFoundError, ValidationError | delete |
| G1-125 | `cli/tests/test_wsl2_create_interrupt.py::test_pre_mutation_failure_makes_no_cleanup_calls::StateError::7a14f6` | 1 `match=` site(s) over StateError | delete |
| G1-126 | `cli/tests/test_wsl2_keepalive.py::test_vm_active_fast_fails_if_keepalive_subprocess_dies_immediately::RuntimeError::d2c99f,test_vm_active_closes_job_handle_on_fast_fail::RuntimeError::d2c99f` | 2 `match=` site(s) over RuntimeError | delete |
| G1-127 | `cli/tests/test_wsl2_paths.py::test_local_app_data_raises_when_missing::RuntimeError::d43a7c` | **[1-raise]** 1 `match=` site(s) over RuntimeError | delete |
| G1-128 | `cli/tests/transports/test_factories.py::test_transport_for_user_raises_state_error_without_tailscale_host::StateError::24e277,test_native_transport_empty_ssh_host_raises_typed_state_error::StateError::75d039` | 2 `match=` site(s) over StateError | delete |
| G1-129 | `cli/tests/transports/test_lima.py::test_run_check_true_raises_on_nonzero::SSHError::5da585,test_copy_to_raises_on_failure::SSHError::a107de` | 2 `match=` site(s) over SSHError | delete |
| G1-130 | `cli/tests/transports/test_ssh.py::test_run_check_true_raises_on_nonzero::SSHError::8aed2b,test_copy_to_raises_on_scp_failure::SSHError::19768f` | 2 `match=` site(s) over SSHError | delete |
| G1-131 | `cli/tests/transports/test_wsl2.py::test_run_check_true_raises_on_nonzero::SSHError::569c1e,test_copy_to_raises_on_failure::SSHError::f59051` | 2 `match=` site(s) over SSHError | delete |
| G1-132 | `cli/tests/vms/test_add_git_credential_orchestrated.py::test_scoped_credential_refused_before_any_resolve_or_gate::ValidationError::abc3d8,test_runup_rejection_is_fatal_and_writes_nothing::TokenRejectedError::328b45,test_operator_stopped_vm_refuses_via_the_reread_race_guard::StateError::836f41` | 3 `match=` site(s) over StateError, TokenRejectedError, ValidationError | delete |
| G1-133 | `cli/tests/vms/test_backup_vm.py::test_missing_tailscale_fails_before_the_boundary::StateError::d60755` | 1 `match=` site(s) over StateError | delete |
| G1-134 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_helper_redacts_every_non_redacting_progress_surface::SSHError::151d5f` | 1 `match=` site(s) over SSHError | delete |
| G1-136 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_reinit_errors_cleanly_when_the_stored_admin_template_is_gone::NotFoundError::00e13e,test_reinit_refuses_an_operator_stopped_vm_at_the_gate::StateError::836f41` | 2 `match=` site(s) over NotFoundError, StateError | delete |
| G1-137 | `cli/tests/vms/test_create_vm_dispatch.py::test_not_ready_site_errors_before_tailscale_and_slug_prompt::StateError::18aaaa` | 1 `match=` site(s) over StateError | delete |
| G1-138 | `cli/tests/vms/test_ensure_tailscale_wording.py::test_rejoin_rejects_a_redaction_free_operation_logger::StateError::70df2a` | 1 `match=` site(s) over StateError | delete |
| G1-139 | `cli/tests/vms/test_legacy_site_sections.py::test_defaults_platform_is_rejected::ConfigError::c3de67,test_defaults_vm_host_is_a_hard_error::ConfigError::c8dde5` | 2 `match=` site(s) over ConfigError | delete |
| G1-141 | `cli/tests/vms/test_lima_remote_create_interrupt.py::test_pre_mutation_failure_makes_no_cleanup_calls::StateError::321425` | 1 `match=` site(s) over StateError | delete |
| G1-142 | `cli/tests/vms/test_platform_config_contract.py::test_wsl2_accepts_no_configuration::ConfigError::13bc20,test_aws_ec2_optional_subnet_id_is_shape_checked::ConfigError::aa0236,test_aws_ec2_rejects_the_removed_ami_override::ConfigError::628c06,test_aws_ec2_rejects_bad_instance_type_arch::ConfigError::03a257,test_a_catalog_entry_is_addressed_by_its_index::ConfigError::638095,test_a_catalog_must_be_a_list_not_a_tuple_shaped_thing::ConfigError::2c8290` | 6 `match=` site(s) over ConfigError | delete |
| G1-143 | `cli/tests/vms/test_platform_runup.py::test_proxmox_runup_rejection_is_fatal::TokenRejectedError::601819,test_proxmox_runup_without_secrets_is_error::ConfigError::880d18,test_azure_runup_without_the_client_secret_is_typed::ConfigError::880d18,test_aws_ec2_runup_auth_rejection_is_fatal::TokenRejectedError::c08b29,test_aws_ec2_runup_without_the_secret_is_typed::ConfigError::880d18` | 5 `match=` site(s) over ConfigError, TokenRejectedError | delete |
| G1-144 | `cli/tests/vms/test_platform_support.py::test_declared_site_on_unsupported_platform_registers_not_ready::StateError::18aaaa,test_unknown_platform_site_is_a_hard_error::ConfigError::657953,test_bundled_site_names_are_reserved_on_every_host::ConfigError::c0d03c,test_defaults_site_naming_a_disabled_site_is_valid_config::StateError::90cec4,test_select_site_errors_with_reasons_when_none_ready::ValidationError::3b09ce` | 5 `match=` site(s) over ConfigError, StateError, ValidationError | delete |
| G1-145 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_rekey_running_check_runs_after_the_resolve_boundary::StateError::68e45c,test_rekey_missing_key_fails_at_the_one_resolve_before_status::SecretUnavailableError::b2dbc1` | 2 `match=` site(s) over SecretUnavailableError, StateError | delete |
| G1-146 | `cli/tests/vms/test_shell_exec_orchestrated.py::test_exec_dash_prefixed_command_fails_with_zero_resolves_and_zero_gate::ValidationError::05d8e1,test_exec_missing_command_after_double_dash_fails_pre_gate::ValidationError::1cfdd7,test_cross_vm_workspace_mismatch_fails_with_zero_resolves_and_zero_gate::ValidationError::1ebb9d,test_shell_no_tailscale_fails_before_any_resolve::StateError::ab1f5e` | 4 `match=` site(s) over StateError, ValidationError | delete |
| G1-147 | `cli/tests/vms/test_sites_dispatch.py::test_resolve_site_unknown_raises_the_stranded_error::ConfigError::2f3f80,test_ops_read_the_token_through_the_context::ConfigError::2d0517,test_select_site_errors_between_several_when_non_interactive::ValidationError::ce6d45,test_select_site_errors_when_none_declared::ValidationError::3b09ce,test_resolving_a_not_ready_site_names_the_requirement::StateError::18aaaa` | 5 `match=` site(s) over ConfigError, StateError, ValidationError | delete |
| G1-148 | `cli/tests/vms/test_system_slug.py::test_validate_slug_bounds::ValidationError::b88487` | 1 `match=` site(s) over ValidationError | delete |
| G1-149 | `cli/tests/vms/test_tailscale_stdin_join.py::test_readiness_exhaustion_raises_typed_without_delivering_key::ProvisioningError::3cd0dc,test_cloud_init_wait_failure_raises_typed_without_delivering_key::ProvisioningError::71cfc9,test_gce_readiness_timeout_is_typed_labeled_and_stops_before_stdin::ProvisioningError::c88dd7` | 3 `match=` site(s) over ProvisioningError | delete |
| G1-150 | `cli/tests/vms/test_verify_connection.py::test_verify_connection_missing_vm::NotFoundError::4e84aa` | **[1-raise]** 1 `match=` site(s) over NotFoundError | delete |
| G1-151 | `cli/tests/vms/test_vm_nodes.py::test_manually_stopped_raises_and_skips_the_ping::StateError::836f41,test_flag_is_reread_before_auto_start::StateError::8322e8,test_pending_vm_realization_is_one_way::StateError::a6ae2c` | 3 `match=` site(s) over StateError | delete |
| G1-152 | `cli/tests/vms/test_vm_platform_kind.py::test_vm_platform_is_not_manifest_declarable::ConfigError::8ef97a` | 1 `match=` site(s) over ConfigError | delete |
| G1-153 | `cli/tests/vms/test_vm_site_kind.py::test_platform_named_site_must_declare_that_platform::ConfigError::262be5,test_decode_requires_platform::ConfigError::b139a2,test_a_kind_owned_key_inside_the_block_is_the_platforms_to_refuse::ConfigError::55786f,test_decode_rejects_stray_spec_keys::ConfigError::8f6c72,test_build_registry_validates_the_blob_via_the_capability::ConfigError::56c8a8,test_unknown_platform_site_hard_errors_at_finalize::ConfigError::a848db,test_bundled_sites_are_reserved::ConfigError::a940e6` | 7 `match=` site(s) over ConfigError | delete |
| G1-154 | `cli/tests/workspaces/test_create_orchestrated.py::test_create_bad_template_bails_before_any_prompt_or_start::NotFoundError::ca3704,test_create_mutation_failure_cleans_up_and_leaves_no_row::ExternalError::666da0` | 2 `match=` site(s) over ExternalError, NotFoundError | delete |
| G1-155 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_create_rollback_removes_the_fresh_workspace_group::ExternalError::f5ed6a,test_create_rollback_tolerates_a_groupdel_failure::ExternalError::f5ed6a,test_create_rollback_on_clone_failure_leaves_no_residue::SSHError::73777c,test_delete_sessions_guard_refuses_with_zero_resolves_and_zero_gate::StateError::54d6d7,test_delete_declined_confirm_aborts_with_zero_resolves_and_zero_gate::UserAbort::0cf0fb,test_rehome_overlapping_paths_fail_with_zero_resolves_and_zero_gate::ValidationError::361297,test_delete_nested_rejects_a_mismatched_vm_node::StateError::207681,test_rehome_confirm_sits_inside_the_span_after_the_dir_checks::UserAbort::9a39bc,test_copy_refusals_fail_with_zero_resolves_and_zero_gate::NotFoundError::29ecb8,test_copy_refusals_fail_with_zero_resolves_and_zero_gate::AlreadyExistsError::a9ad3d` | 10 `match=` site(s) over AlreadyExistsError, ExternalError, NotFoundError, SSHError, StateError, UserAbort, ValidationError | delete |

## Group 2: guide and migration topics

0 live rows, out of a ledger of 50: every row in this group is `[dead]`, because `4ac084cd` deleted
the whole guide-test estate it was written against. **There is no group 2 PR to cut.** The rows stay
for the R4 reassessment to audit, and the six guide test files that replaced the deleted ones carry
no in-scope site; see the completeness re-scan.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-001 | `cli/tests/guide/test_migration_topic.py::L19-25` | **[line-anchored: file gone]** **[dead]** curated cross-topic link + block-type presence | convert | Link resolution is already production-enforced (`guide/catalog.py:133-151` raises `BrokenTopicLinkError` for trusted topics), so the curated-pair restatement is dead weight; the real unguarded invariant is that a topic declaring `related_topics` also carries a `TopicLinks` block. Verified by executing: a contribution with `related_topics` and no `TopicLinks` block parses cleanly and renders with the links silently dropped. Replacement: one catalog-wide check over `build_authored_catalog()` that `topic.related_topics` implies a `TopicLinks` block. No production change. |
| A-002 | `cli/tests/guide/test_migration_topic.py::L32-45` | **[line-anchored: file gone]** **[dead]** action id + consent ordering pin | keep | Consent boundaries and action-record identity/ordering are what the rule says to test instead of prose. Nothing in production ties an action's consent to its operation (`guide/contract.py:507-518` only checks enum membership), so reclassifying a mutating step as a read is caught only here. |
| A-003 | `cli/tests/guide/test_migration_topic.py::L46-47` | **[line-anchored: file gone]** **[dead]** command/manual-steps XOR restatement | delete | Verified by executing: `validate_guide_action` and `build_authored_catalog(strict=True)` both reject an action carrying both (`guide/contract.py:521-530`), and the catalog re-parses every trusted contribution, so this cannot fail before production does. |
| A-004 | `cli/tests/guide/test_migration_topic.py::L48-61` | **[line-anchored: file gone]** **[dead]** command-token tuple restatement | delete | **Ruled by the lead 2026-08-16**, and the ruling reversed this row. R2.3 permits an addition only where a real invariant loses its ONLY guard in the same change, and that is not the situation here: these tuples are compared against literal copies of themselves, so they can only fail when someone edits the guide, and the invariant a CLI-resolution test would add (a guide action names a real command and real flags) has no guard today and so loses none. That makes it new coverage, which a subtraction pass does not add opportunistically. The drift risk is recorded rather than closed: a flag rename leaves both copies agreeing and the guide wrong, and nothing detects it. |
| A-005 | `cli/tests/guide/test_migration_topic.py::L70-74` | **[line-anchored: file gone]** **[dead]** required-input identity pin | delete | Restates authored data. The half that has consequence is production-enforced: a command token `$X` must name a declared input and must not name a sensitive one (`guide/contract.py:438-458`), verified by executing (marking `BACKUP_PATH` sensitive is rejected at parse). |
| A-006 | `cli/tests/guide/test_migration_topic.py::L76-82` | **[line-anchored: file gone]** **[dead]** manual-steps and expected-state substring pins | delete | Seven substring asserts on authored inventory prose. Nothing behavioral; the rule's target exactly. |
| A-007 | `cli/tests/guide/test_migration_topic.py::L85` | **[line-anchored: file gone]** **[dead]** required-input name pin | delete | Same as A-005. |
| A-008 | `cli/tests/guide/test_migration_topic.py::L86-89` | **[line-anchored: file gone]** **[dead]** manual-steps substring pins | delete | Three substring asserts on authored backup-config prose. |
| A-009 | `cli/tests/guide/test_migration_topic.py::L92-95` | **[line-anchored: file gone]** **[dead]** required-input identity pin | delete | Same as A-005. |
| A-010 | `cli/tests/guide/test_migration_topic.py::L96-98` | **[line-anchored: file gone]** **[dead]** manual-steps substring pins | delete | Two substring asserts on authored backup-resources prose. |
| A-011 | `cli/tests/guide/test_migration_topic.py::L101` | **[line-anchored: file gone]** **[dead]** consent boundary assert | keep | Same invariant as A-002 for `verify-migration-inputs`: a read-classified action must not drift to a mutating boundary, and no production check enforces it. |
| A-012 | `cli/tests/guide/test_migration_topic.py::L102-107` | **[line-anchored: file gone]** **[dead]** manual-steps substring pins | delete | Five substring asserts on authored verification prose. |
| A-013 | `cli/tests/guide/test_migration_topic.py::L110-115` | **[line-anchored: file gone]** **[dead]** required-input identity pin | delete | Same as A-005. |
| A-014 | `cli/tests/guide/test_migration_topic.py::L116-124` | **[line-anchored: file gone]** **[dead]** manual-steps, description and expected-state substring pins | delete | Eight substring asserts on authored edit-one-manifest prose, including a phrase-level restatement of the retired-line rewrite. |
| A-015 | `cli/tests/guide/test_migration_topic.py::L127-128` | **[line-anchored: file gone]** **[dead]** duplicate command and verification pin | delete | Duplicates lines 48-49 in the same file; the same command is additionally executed end to end through `CliRunner` in `test_json_action_contract.py:239-253`, which is the only assertion here with teeth. |
| A-016 | `cli/tests/guide/test_migration_topic.py::L131-141` | **[line-anchored: file gone]** **[dead]** duplicate consent, input and command pins | delete | Consent duplicates A-002's list, inputs are A-005's shape, and the command duplicates lines 50-58 while being executed for real at `test_json_action_contract.py:192-201`. |
| A-017 | `cli/tests/guide/test_migration_topic.py::L144-146` | **[line-anchored: file gone]** **[dead]** duplicate command pin plus expected-state substrings | delete | Command duplicates line 59; the two expected-state substrings are authored prose, and the behavior they describe is asserted for real at `test_json_action_contract.py:322-341`. |
| A-018 | `cli/tests/guide/test_migration_topic.py::L149-167` | **[line-anchored: file gone]** **[dead]** wording blacklist across later actions | delete | Four `not in` phrase blacklists over concatenated authored prose, the shape the rule calls worse than useless: any reworded restatement of the same bad idea passes. The property it gestures at (only the inventory action authors the expected manifest path) has no structural form that does not require new production surface, so under R2.4 it goes with the wording. |
| A-019 | `cli/tests/guide/test_migration_topic.py::L175-182` | **[line-anchored: file gone]** **[dead]** mutating-action id list | delete | Derivable from, and therefore a duplicate of, the consent list kept in A-002: this list is exactly the `MUTATE_AGENTWORKS` filter of lines 32-45. |
| A-020 | `cli/tests/guide/test_migration_topic.py::L183` | **[line-anchored: file gone]** **[dead]** command/manual-steps XOR restatement | delete | Same production enforcement as A-003, verified by executing. |
| A-021 | `cli/tests/guide/test_migration_topic.py::L184-189` | **[line-anchored: file gone]** **[dead]** input-name substrings in prose plus duplicate command pins | delete | Lines 184-187 assert authored manual steps mention a variable name; lines 188-189 duplicate lines 60-61. |
| A-022 | `cli/tests/guide/test_migration_topic.py::L197-200` | **[line-anchored: file gone]** **[dead]** required-input tuple (with sensitive flag) plus duplicate command | delete | The sensitive flag is production-enforced for any input referenced by a command token, verified by executing (`guide/contract.py:457-458` rejects `BACKUP_PATH` marked sensitive because the command interpolates it); the rest restates authored data and duplicates line 60. |
| A-023 | `cli/tests/guide/test_migration_topic.py::L201-204` | **[line-anchored: file gone]** **[dead]** expected-state and refusal-alternative substrings | delete | Four substring asserts on authored restore prose. The behavior they narrate (restore needs its own confirmation, takes no implicit backup) is asserted for real in `test_database_cli.py:87-110`. |
| A-024 | `cli/tests/guide/test_migration_topic.py::L206-208` | **[line-anchored: file gone]** **[dead]** empty-input pin plus duplicate command | delete | Duplicates line 61; `required_inputs == ()` restates authored data. |
| A-025 | `cli/tests/guide/test_migration_topic.py::L209-218` | **[line-anchored: file gone]** **[dead]** required-phrase pins on precondition, expected state and refusal | delete | Ten substring asserts on authored completion-refresh prose, including one pinning a quoted PowerShell profile line. Completion install behavior itself is covered by `tests/test_completions.py`. |
| A-026 | `cli/tests/guide/test_migration_topic.py::L221-235` | **[line-anchored: file gone]** **[dead]** tautology plus expected-state substrings | delete | Lines 232-233 compare two tuples the test itself just wrote (they cannot fail); 234-235 pin authored comparison prose. Nothing here observes production behavior at all. |
| A-033 | `cli/tests/guide/test_migration_topic.py::L363-379` | **[line-anchored: file gone]** **[dead]** required-phrase list plus wording blacklist | delete | Eight required substrings and four forbidden ones over one action's authored manual steps. The two forbidden field spellings are the same retired-name blacklist as A-028. |
| A-034 | `cli/tests/guide/test_migration_topic.py::L382-422` | **[line-anchored: file gone]** **[dead]** 30-entry required-phrase list plus blacklist | delete | The largest single concentration in this scope: 30 required substrings over flowed teaching markdown, two forbidden retired spellings, and one forbidden phrase. Every field-path claim in the list has a structural twin in A-027/A-029, which assert the live schema rather than our sentences about it. **Corrected 2026-08-19 (map re-baseline).** The tuple was counted rather than eyeballed and holds 30 entries at the basis `c686cd6d`, not the 31 this row claimed nor the 47 `findings.md` G12 claims. The file was deleted by `4ac084cd`, so the row is dead and the correction is a matter of record only. |
| A-035 | `cli/tests/guide/test_migration_topic.py::L425-432` | **[line-anchored: file gone]** **[dead]** mode payload parity | keep | Human and agent renderings must carry byte-identical action payloads; this is derivation parity, and a renderer that started tailoring action records per mode would be caught only here and in `test_render_service.py:39-42`. |
| A-036 | `cli/tests/guide/test_authored_coverage.py::L23-25` | **[line-anchored: file gone]** **[dead]** import-order registration guard | keep | Behavioral: importing the secrets submodule must not empty the contribution registry. A real import-order regression (a module-level rebind) is invisible to types and is what this catches. |
| A-037 | `cli/tests/guide/test_authored_coverage.py::L31-32` | **[line-anchored: file gone]** **[dead]** curated cross-topic link pin | delete | Same as A-001's first half: link resolution is production-enforced at `guide/catalog.py:133-151`, and the TopicLinks-presence gap is closed once by A-001's replacement. |
| A-038 | `cli/tests/guide/test_authored_coverage.py::L33-35` | **[line-anchored: file gone]** **[dead]** block-composition set equality | convert | Rubric re-check, 2026-08-16. `:35` is the only probe that the manifesto topic carries NO consent-bearing `ActionList`, and `:34` the only probe that it carries exactly one `AgentContract`. Keep `:34-35` as structural block-type assertions, drop the set equality at `:33`. No production change. |
| A-039 | `cli/tests/guide/test_authored_coverage.py::L41-43` | **[line-anchored: file gone]** **[dead]** block-composition set equality | convert | Rubric re-check, 2026-08-16. Same shape for `concept-reporting-bugs`: `:43` is what the test's own name asserts. Keep `:42-43`, drop the set equality at `:41`. No production change. |
| A-040 | `cli/tests/guide/test_authored_coverage.py::L48-63` | **[line-anchored: file gone]** **[dead]** onboarding action id, consent and verification-presence pin | keep | Same invariant as A-002 for the onboarding path, plus `all(action.refusal_alternative ...)`: every action keeps a refusal alternative, which is the consent-boundary contract agents rely on. The embedded verification command tuples travel with A-004's replacement. |
| A-041 | `cli/tests/guide/test_authored_coverage.py::L64-96` | **[line-anchored: file gone]** **[dead]** command-token tuple restatement | delete | Thirty-three pinned CLI tokens for the two mutating onboarding actions, nothing executes them, and they restate authored data. Same ruling as A-004: the replacement it pointed at is not being added, so this is a plain deletion carrying the same recorded drift risk. |
| A-043 | `cli/tests/guide/test_json_action_contract.py::L119` | **[line-anchored: file gone]** **[dead]** operation shape assert | keep | Every covered action's operation must end in `--output json`; that is the structural precondition for the envelope parsing the rest of the file does. |
| A-044 | `cli/tests/guide/test_json_action_contract.py::L120-124` | **[line-anchored: file gone]** **[dead]** required-phrase list on expected_state | delete | Five substring asserts on authored action prose, run over every covered action from line 156. What the prose describes (exact v1 envelope) is proven for real by `_parse_exact_v1` against actual CLI output in the same file. |
| A-045 | `cli/tests/guide/test_json_action_contract.py::L142-149` | **[line-anchored: file gone]** **[dead]** covered-action identity and ordering | keep | Identity and ordering of the action records that carry a machine-parsed operation: if an action silently stops requesting JSON, or a new one appears uncovered, this list moves. |
| A-046 | `cli/tests/guide/test_json_action_contract.py::L151-153` | **[line-anchored: file gone]** **[dead]** duplicate command pin | delete | Duplicates lines 146 and 48/127 of `test_migration_topic.py`; the same command is invoked for real 90 lines below. |
| A-047 | `cli/tests/guide/test_json_action_contract.py::L342-344` | **[line-anchored: file gone]** **[dead]** expected-state substring pins | delete | Three authored sentences pinned immediately after the same facts were proven from real CLI output (lines 338-341). |
| A-048 | `cli/tests/guide/test_json_action_contract.py::L367` | **[line-anchored: file gone]** **[dead]** expected-state substring pin | delete | Same shape as A-047; the stale-schema behavior is asserted structurally at lines 364-366. |
| A-049 | `cli/tests/guide/test_render_service.py::L73` | **[line-anchored: file gone]** **[dead]** names-only derivation parity | keep | The rendered names-only stream must equal the authored catalog's names exactly; a renderer that added a heading or dropped a topic breaks completions, and this is derivation against the canonical source. |
| A-050 | `cli/tests/guide/test_render_service.py::L74` | **[line-anchored: file gone]** **[dead]** slug-prefix convention pin | delete | Production already rejects any topic slug that is not `concept-` or `plugin/`-prefixed (`guide/contract.py:779-780`), so this restates a parser rule that the contract tests own. |
| A-051 | `cli/tests/guide/test_render_service.py::L88-97` | **[line-anchored: file gone]** **[dead]** control-character sanitizer | keep | Injection defense: every disallowed terminal control codepoint is stripped and LF/TAB survive. Security test wearing a blacklist's clothes, explicitly a keep. |
| A-054 | `cli/tests/guide/test_release_notes.py::L192-196` | **[line-anchored: file gone]** **[dead]** escaped-injection assertions | keep | Redaction and injection defense over content the test itself injected: the markdown link, script tag and fenced command must arrive escaped and the raw link must not appear. Value identity, not authored prose. |
| A-058 | `cli/tests/guide/test_trail_sign.py::L61-62` | **[line-anchored: file gone]** **[dead]** rendered destination presence | keep | Structural presence derived from the production list: every destination the mode exposes must actually appear in the no-topic trail sign, so a renderer that dropped one is caught. Rewrite the loop over `trail_destinations(mode)` when A-057 removes `expected`. |
| A-059 | `cli/tests/guide/test_source_review_offer.py::L38-42` | **[line-anchored: file gone]** **[dead]** canonical-path presence plus path existence | keep | Derived, not restated: the focused review steps must enumerate every path in `FOCUSED_SOURCE_REVIEW_PATHS`, and each of those paths must exist in the repository. The second half is the path-existence assertion the charter blesses, and it catches a real defect (the guide pointing at a moved directory). |
| A-060 | `cli/tests/guide/test_source_review_offer.py::L58-61` | **[line-anchored: file gone]** **[dead]** index absence and action ordering | keep | Structural: source-review actions must not leak into the no-topic index (a consent-surface invariant) and must render in declared order when the topic is selected. |
| A-061 | `cli/tests/guide/test_installer_plugin_guides.py::L52-54` | **[line-anchored: file gone]** **[dead]** consent and inertness assertions | keep | Same invariant as A-002 for plugin-contributed verification actions: they stay read-classified and command-free, which is the boundary that keeps a plugin from shipping an executable step. |
| A-062 | `cli/tests/guide/test_installer_plugin_guides.py::L55-57` | **[line-anchored: file gone]** **[dead]** required-input tuple pin | delete | Restates authored plugin data; same reasoning as A-005. |

## Group 3: report lines and hints

322 live rows: 158 delete, 88 convert, 76 keep, out of a ledger of 382 (one `[dead]`, 59
`[subtracted]`). This is the largest convert population in the map.

**Precondition, operator disposition 2026-08-19: the recipe verification is re-run before this group
executes.** Its recipes were verified in the #573 round, four of them carry a recorded trap, and 22
sites in those four have never been sampled. Re-running the verification is what discharges that,
and it happens before the conversions land rather than after. **This group lands as four PRs, not
one** (effort lead, 2026-08-16): it is too much for one review round and it is the judgment-heavy
batch, so the cut is by subsystem INSIDE the single shape. That keeps the kind of judgment uniform
within a round while making each round readable. Rows carry their file path, which is what assigns
them.

**Corrected 2026-08-19 (map re-baseline).** The sub-batch table below never reconciled with the
group: it summed to 378 rows against a stated 372, and its per-disposition columns summed to 225
deletes where the group holds 158. Sub-batch membership is now DERIVED from each row's file path
rather than tallied by hand, on the stated scopes below, so the four rows sum to the group by
construction. Both a live and a ledger column are given, because the re-scope subtraction lands
almost entirely in 3a and 3d.

| Sub-batch | Scope                                                                                                                                                                                                                                                                                           | Live | delete | convert | keep | Ledger |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---: | -----: | ------: | ---: | -----: |
| 3a        | Consoles, sessions and secrets: `cli/tests/sessions/`, `cli/tests/secrets/`, `cli/tests/transports/`, `orchestration/test_secrets.py`, and the `test_consoles_*`, `test_console_*`, `test_session*`, `test_secret*`, `test_tmux*`, `test_claude_code_*`, `test_codex_*` and `test_grok_*` roots |   71 |     36 |      20 |   15 |     94 |
| 3b        | VMs and platforms: `cli/tests/vms/`, `cli/tests/plugins/`, and the `test_aws_*`, `test_azure_*`, `test_wsl2_*`, `test_vm_*`, `test_lima*`, `test_proxmox*`, bootstrap and cloud-init roots                                                                                                      |  121 |     77 |      32 |   12 |    121 |
| 3c        | Workspaces and agents: `cli/tests/workspaces/`, `cli/tests/agents/`, `test_agents.py`                                                                                                                                                                                                           |   25 |     12 |       7 |    6 |     25 |
| 3d        | Everything else in the group: database, doctor, git credentials and the core CLI surfaces                                                                                                                                                                                                       |  105 |     33 |      29 |   43 |    142 |

3c is small enough to ride with 3a as one round if three PRs suits the review cadence better; the
other three are each a full round on their own.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-055 | `cli/tests/guide/test_probe_free_registry.py::L42` | **[line-anchored: file gone]** **[dead]** verbatim readiness-reason pin | delete | An authored sentence pinned by equality. The invariant is line 41 (`not readiness.is_available`) plus the `shutil.which` trap above it, both of which stay. |
| A-065 | `cli/tests/test_database_backup.py::test_restore_rejects_v1_with_first_committed_v2_ddl_before_destination_open,test_restore_rejects_v3_with_first_committed_v4_table_before_destination_open,test_restore_rejects_missing_column_before_destination_open` | **[subtracted: instance-model]** verbatim hint equality (three sites) | delete | The same authored remediation sentence pinned by equality in three qualification tests. The behavioral content of each test is the raise plus `not live.exists()`, both of which stay; `hint is not None` is the surviving presence form used elsewhere (for example `test_database_migration_safety.py:710`). |
| A-067 | `cli/tests/test_database_cli.py::test_database_backup_stdout_is_only_the_completed_path` | **[subtracted: instance-model]** verbatim stderr equality | delete | Pins an authored progress sentence. The stream invariant is line 81 (stdout is exactly the path), which stays. |
| A-068 | `cli/tests/test_database_cli.py::test_database_restore_yes_uses_stderr_and_creates_no_implicit_backup` | **[subtracted: instance-model]** labelled path substrings | convert | The substance is that the restore names both files it touched; the labels are authored. Replacement: assert `str(selected)` and `str(live)` appear in stderr and drop the `Backup:`/`Live database:` prefixes. No production change. |
| A-069 | `cli/tests/test_database_cli.py::test_database_restore_yes_uses_stderr_and_creates_no_implicit_backup` | **[subtracted: instance-model]** verbatim completion sentence | delete | Authored sentence; exit code (104), restored value (109) and unchanged backup set (110) carry the behavior. |
| A-070 | `cli/tests/test_database_cli.py::test_database_restore_decline_prompts_on_stderr_and_changes_nothing` | **[subtracted: instance-model]** prompt wording pin | delete | **Recipe verification, 2026-08-16.** **The recorded conversion is vacuous.** The added `result.stderr != ""` cannot bite, because `database.py:52-53` echoes `Backup:` and `Live database:` to stderr unconditionally, so silencing the prompt entirely left the test green. The half that does bite is the PRE-EXISTING `result.stdout == ""` at `:131`, which fails if the prompt is routed to stdout. Delete the wording pin at `:132` and add nothing; `:131` and `:133-134` already carry the stream-discipline invariant. |
| A-071 | `cli/tests/test_database_cli.py::test_database_restore_non_interactive_without_yes_refuses_cleanly` | **[subtracted: instance-model]** refusal wording substrings | delete | Two authored substrings. Exit code 1 (154), empty stdout (156) and `not live.exists()` (159) already prove the clean non-interactive refusal. |
| A-072 | `cli/tests/test_database_cli.py::test_interactive_migration_notice_and_prompt_keep_stdout_machine_pure` | **[subtracted: instance-model]** migration notice and prompt wording | delete | **Corrected 2026-08-19 (map re-baseline).** Was delete over `:192-193`. `:192` keeps: `f"version {LATEST_VERSION - 1} to {LATEST_VERSION}"` interpolates the production constant imported at `:18` from `agentworks.db.migrations`, so it proves the migration notice reports the version pair actually in force and fails when the notice drifts from the migration table, not only on a rewording. Same shape as G1-K16. Drop `:193` alone, the prompt text, whose behavioral twin is `test_database_migration_safety.py:1002-1031` (the confirm response actually drives whether a backup is created). Stdout purity, the point of this test, is `:189`/`:191`. |
| A-073 | `cli/tests/test_database_cli.py::test_interactive_migration_notice_and_prompt_keep_stdout_machine_pure` | **[subtracted: instance-model]** backup-completion sentence | convert | Replacement: assert a backup file now exists in `backup_directory(live)` instead of asserting the sentence that says so. That is the fact the operator cares about and the test currently never checks. No production change. |
| A-074 | `cli/tests/test_db_migration_vm_sites.py::test_prints_site_manifest_snippets` | **[subtracted: instance-model]** stream discipline and snippet dedup count | keep | Migrations run on every `Database()` open including under stdout-capturing completion helpers, so `captured_output.info == []` is a real channel invariant; the `count(...) == 1` is the dedup invariant (one snippet per distinct host-named site), not a wording pin. |
| A-075 | `cli/tests/test_db_migration_vm_sites.py::test_prints_site_manifest_snippets` | **[subtracted: instance-model]** emitted YAML snippet pins | convert | The substance is that the printed snippet carries the ssh target derived from the legacy row the test seeded. Replacement: assert `me@gpu-box` and `me@wsl2-host` appear in the emitted output and drop the exact `placement: { mode: ssh, host: ... }` spelling. No production change. |
| A-076 | `cli/tests/test_db_migration_vm_sites.py::test_prints_site_manifest_snippets` | **[subtracted: instance-model]** verbatim warning sentence | delete | Pins the shadow-name warning wording. The behavior it narrates is asserted structurally by its sibling: `test_backfill_and_site_rename` requires `by_name["wvm"].site == "wsl2-host"` (line 126), which fails if the shadow suffix regresses. |
| A-077 | `cli/tests/test_db_migration_harness_integration_state.py::test_v31_rejects_an_unexpected_state_column_set` | **[subtracted: instance-model]** exception-message substrings | delete | Asserts our own migration's `IntegrityError` text names both columns. The invariant is the raise itself (line 154), which stays; the wording adds nothing a reviewer would not see in the diff. |
| A-078 | `cli/tests/test_db_migration_harness_integration_state.py::test_malformed_blob_degrades_to_empty_with_a_warning` | **[subtracted: instance-model]** warning substring with authored fragment | convert | The `"bad"` half is value identity (the row the test corrupted); `detail_fragment` is our authored phrasing, parametrized. Replacement: keep `any("bad" in message for message in captured_output.warnings)` so degradation still cannot go silent, and drop the fragment from the parametrization. No production change. |
| A-081 | `cli/tests/test_database_migration_safety.py::test_busy_state_error_message_and_hint_are_not_caller_suppliable` | **[subtracted: instance-model]** constructibility, hint presence, entity kind | keep | Behavioral: `BusyStateError()` constructs with no arguments, always carries a hint, and reports `entity_kind == "database"`, which is the structured field callers and doctor branch on. |
| A-082 | `cli/tests/test_database_migration_safety.py::test_prepare_refuses_changed_stale_state_when_lock_released_before_first_acquire` | **[subtracted: instance-model]** verbatim hint equality | delete | Authored remediation sentence pinned by equality; the raise, the unchanged version and the absent backup directory (520-527) carry the behavior. |
| A-083 | `cli/tests/test_database_migration_safety.py::test_prepare_refuses_identical_tainted_baseline_after_lock_released_before_first_acquire` | **[subtracted: instance-model]** verbatim hint equality | delete | Same shape as A-082 for the tainted-baseline path; lines 563-564 and 569-570 hold the observable content. |
| A-084 | `cli/tests/test_database_migration_safety.py::test_partial_migration_failure_reports_exact_backup_recovery` | **[subtracted: instance-model]** hint equality around a derived command | convert | The load-bearing part is that the hint carries the exact restore command for the backup that actually exists, which is real operator recovery, not prose. Replacement: `render_restore_command(backups[0]) in raised.value.hint`, dropping the authored prefix. No production change. |
| A-085 | `cli/tests/test_database_migration_safety.py::test_partial_migration_failure_without_backup_says_so` | **[subtracted: instance-model]** authored sentence substring | delete | Line 710 (`hint is not None`) and 712 (no backup directory) already state the fact; the sentence is ours to review. |
| A-086 | `cli/tests/test_database_migration_safety.py::test_base_exception_migration_failure_preserves_identity_and_visible_recovery` | **[subtracted: instance-model]** notice equality with a hardcoded recovery sentence | convert | The invariant worth keeping is that recovery instructions still reach the operator on the notice channel even when the interrupt propagates. Replacement: assert `captured_output.notices` is non-empty and, in the backup arm, that `render_restore_command(backups[0])` appears in the last notice; drop the authored sentence in the no-backup arm. No production change. |
| A-087 | `cli/tests/test_database_migration_safety.py::test_recovery_command_rendering_quotes_posix_and_powershell` | **[subtracted: instance-model]** rendered-command quoting equality | keep | Shell quoting of a path containing a space and an apostrophe, per platform. The escaping is the invariant (a regression yields a command that breaks or misdirects when pasted), and the inputs are test-injected. |
| A-088 | `cli/tests/test_database_migration_safety.py::test_get_db_interactive_default_yes_selection_and_notice` | **[subtracted: instance-model]** migration-notice wording substring | keep | Rubric re-check, 2026-08-16. The only probe that the migration notice reports the ACTUAL version span: `cli/agentworks/cli/_helpers.py:64` interpolates `plan.inspection.current_version` and `.latest_version`, while `:1027` proves only that the migration ran and `:1031` only counts notices. A-072 stands because this one is kept. Cost: breaks if the notice is reworded. |
| A-089 | `cli/tests/test_database_migration_safety.py::test_get_db_interactive_default_yes_selection_and_notice` | **[subtracted: instance-model]** notice count | keep | Channel discipline: exactly one notice when the operator declines the backup and two when they accept, so a stray or missing notice is caught without asserting wording. |
| A-090 | `cli/tests/test_database_migration_safety.py::test_get_db_interactive_default_yes_selection_and_notice` | **[subtracted: instance-model]** notice equality around a derived path | convert | Replacement: `format_host_path(backups[0]) in captured_output.notices[-1]`, keeping the path identity (the operator must be told where the backup landed) and dropping the authored prefix. No production change. |
| A-091 | `cli/tests/test_database_migration_safety.py::test_get_db_selected_backup_failure_prevents_migration_and_guides_retry` | **[subtracted: instance-model]** branch-discriminating hint substrings | delete | The interactive and non-interactive arms are distinguished only by our own hint wording, which is the case operator ruling 10 and R2.4 resolve by deleting the assertion and accepting the reduced branch coverage. `hint is not None` (1129) and the unchanged schema version (1135) stay. The `auto_backup_before_migration = false` fragment is a config-key mention, not a behavioral handle. |
| A-092 | `cli/tests/test_database_migration_safety.py::test_notice_survives_machine_presentation_suppression` | **[subtracted: instance-model]** injected notice identity | keep | The strings are the test's own; the invariant is that `suppress_presentation()` hides info and lets notices through. |
| B-001 | `cli/tests/sessions/test_console_attach_orchestrated.py::test_named_console_attach_holds_across_the_interactive_attach` | report-line substring | delete | The test's subject is the hold span, carried structurally by `events == ["hold-open", "interactive", "hold-close"]` on line 151. The announce line adds nothing that can regress independently. |
| B-002 | `cli/tests/sessions/test_console_attach_orchestrated.py::test_restore_session_stopped_vm_drives_the_real_gated_composition` | report-line substring | delete | The gated-composition claim is carried by lines 299-304 (`events`, `resolve_counter`, three tmux commands). The "already matches config" line is the report only. |
| B-003 | `cli/tests/sessions/test_create_resume_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary` | header-banner substring pins | convert | Rubric re-check, 2026-08-16. `(Role.HEADER, 0, "Resolving Secrets")` is asserted structurally only for the RESUME site (`test_create_resume_orchestrated.py:879`); the CREATE site `_create.py:129` loses every probe. Add the `Role.HEADER` tuple to the existing structural block, the form B-004 already keeps. No production change. B-120 stands only because this conversion lands. |
| B-004 | `cli/tests/sessions/test_create_resume_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary` | structural presence on (role, level, message) | keep | The invariant is the output tree's shape: which phases are sections, at what nesting level, and that the closing line is a `RESULT` at level 0. A regression that emits a phase as body text, or nests the result, is caught here and nowhere else. The message parts are `startswith` on a resource key the test declared. |
| B-005 | `cli/tests/sessions/test_create_resume_orchestrated.py::test_resume_stopped_vm_gate_seeds_and_env_pass_is_the_only_other` | structural presence on (role, level, message) | keep | Same invariant as B-004 for the resume path; the resume note's level-1 nesting under `Starting Session` is the regressable part. |
| B-006 | `cli/tests/sessions/test_create_resume_orchestrated.py::test_resume_broken_force_kill_warning_nests_under_starting_session` | role+level structural presence with wording substring | convert | The invariant (a force-kill warning nests under `Starting Session` rather than escaping to level 0) is real and role/level already carry it; drop the `"force-killing via PID" in msg` clause and assert `role is Role.WARNING and level == 1`. VERIFIED by executing: it passes. No production change. |
| B-007 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_offered_and_deleted_interactive,test_now_empty_agent_offered_and_deleted_interactive,test_agent_becomes_candidate_once_grant_removed,test_delete_session_interactive_offers_now_empty_workspace` | interactive-offer wording substring | convert | The real invariant is that the operator was ASKED rather than silently acted upon; `calls`/`db.get_*` siblings cannot see a prompt. Replace with a prompt-count assertion. VERIFIED by executing: `len(prompts) == 1` passes at 217/458/618; at 749 the end-to-end flow issues two prompts, so that site needs `== 2`. No production change. |
| B-008 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_created_interactive_offer_notes_provenance,test_now_empty_agent_created_interactive_offer_notes_provenance` | provenance-cue wording pin | delete | The whole assertion is the sentence `(created with this session)`. The provenance branch it fronts has no other observable, so R2.4 applies; `calls == ["ws-vm1"]` beside it keeps the behavioral half. |
| B-009 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_interactive_offer_discloses_grants,test_now_empty_workspace_interactive_offer_plain_with_only_grant_all_row` | disclosure wording pin plus phrase blacklist | delete | Line 358 pins a full authored sentence; 382 is a phrase-absence blacklist (`"revokes explicit grant" not in p`), the shape the rule names as worse than useless. The grant-guard behavior itself is pinned structurally at 312-313 and 339-340. |
| B-010 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_yes_not_created_reports_but_keeps,test_now_empty_workspace_yes_created_kept_when_external_explicit_grant,test_now_empty_agent_yes_not_created_reports_but_keeps,test_delete_session_yes_reports_now_empty_uncreated_workspace` | report-but-keep warning substring triples | delete | Every one of these sits directly under `calls == []` and `db.get_workspace(...) is not None` (or the agent equivalent), which is the entire report-but-keep invariant. The warning text and the suggested `agw ... delete` command are the report. |
| B-011 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_now_empty_workspace_delete_failure_warns,test_now_empty_agent_delete_failure_warns,test_delete_session_warns_when_now_empty_workspace_delete_raises` | failure-warning substring | convert | Rubric re-check, 2026-08-16. `cleanup_now_empty_resource`'s `except AgentworksError -> output.warn` branch (`sessions/_resource_cleanup.py:82-85`) has no other observable: swallowing silently and swallowing with a warning are indistinguishable in the surviving row assertions. Convert to a warning-count assertion. No production change. B-010 and B-083 stand only because this lands. |
| B-012 | `cli/tests/sessions/test_delete_resource_cleanup.py::test_non_interactive_without_yes_reports_and_never_prompts_or_deletes` | report-but-keep warning substring | convert | Rubric re-check, 2026-08-16. The mechanism test for the same shared helper; its report-but-keep branch's only effect is the warning, since `confirms == []` and `deletes == []` are identical between reporting and doing nothing. Same conversion. No production change. |
| B-015 | `cli/tests/sessions/test_session_list_harness_integration.py::test_names_only_stays_pure_and_pays_no_registry_cost` | exact output equality | keep | `captured_output.info == ["s1"]` is the `--names-only` contract from the `cli-conventions` rule: exactly one line per row, no header, no decoration. The value is the session name the test seeded, so this is value identity plus arity, not wording. |
| B-016 | `cli/tests/sessions/test_session_nodes.py::test_missing_command_is_a_typed_error` | hint substring | keep | `"--template" in exc.value.hint` names a real CLI flag, not prose about it. A rename that broke the hint's advice would be caught; treat it as the option-name reference it is. Same shape as B-062. |
| B-017 | `cli/tests/sessions/test_session_nodes.py::test_admin_mode_error_names_the_vm` | error-message substring | delete | `"for VM 'box'."` pins the authored trailing clause. The typed error and its `entity_name` already discriminate the branch. |
| B-018 | `cli/tests/sessions/test_session_nodes.py::test_pending_agent_teardown_failure_names_the_artifact` | error-message substring pair | delete | `"left standing"` is authored prose; `"ephemeral agent 'dev'"` is the artifact name, already carried by the exception's entity fields in the same test file's neighbors. Nothing structural is lost. |
| B-019 | `cli/tests/sessions/test_session_nodes.py::test_pending_session_teardown_warns_and_never_raises` | warning-message substring pair | delete | The invariant (a failed pending-session teardown warns and never raises) is carried by the call returning at all; `"db locked"` is the fixture's own injected error string, which survives as value identity if the row is trimmed rather than dropped. |
| B-020 | `cli/tests/sessions/test_session_nodes.py::test_a_non_dict_namespace_value_degrades_to_empty_with_a_warning` | degraded-value warning substring | delete | The degradation itself (namespace value becomes empty) is the invariant and is asserted structurally beside it; `"got str"` pins our diagnostic's wording. |
| B-022 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_stop_all_reachable_vms_is_one_boundary_burst,test_batch_empty_vm_set_is_a_complete_noop,test_stop_all_mixed_site_batch_resolves_the_union_once` | report-line substring | delete | Each sits under `resolve_counter == [["proxmox-token"]]` (and, at 255, a full no-op assertion set), which is the whole subject of the boundary-burst tests. The "No running sessions to stop" line is the report. |
| B-023 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_stop_session_reachable_vm_is_one_boundary_burst` | report-line substring | delete | Same shape; the burst count is the invariant, the "already stopped" line is the report. |
| B-024 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_session_logs_writes_exact_utf8_bytes_on_a_legacy_console` | report-line substring | delete | The hold-span claim is `events == ["hold-open:box", "probe", "hold-close:box"]` on 633. The rendered `Status:     stopped` line, whitespace column and all, is pure layout. |
| B-025 | `cli/tests/sessions/test_singular_batch_orchestrated.py::test_session_logs_writes_the_whole_capture_through_a_short_writing_pipe` | report-line pin plus line-order pin | keep | Rubric re-check, 2026-08-16. Scoped: `"Harness integration: shell" in info` is the only probe that a session's RESOLVED harness integration reaches describe, because the golden-bytes sibling at `test_machine_output_cli.py:360` stubs a description whose harness integration is `None`, so a regression rendering `-` for every session passes everywhere else. The `:663-667` ordering pin still goes, genuinely covered by the golden bytes. Cost: the kept line breaks if that label is reworded. |
| B-026 | `cli/tests/agents/test_agent_home_permissions.py::test_shared_primary_group_warns,test_private_primary_group_does_not_warn` | warning-wording substring plus absence pin | keep | Rubric re-check, 2026-08-16. The shared-primary-group post-condition guard changes no command and no state, so the warning IS the behavior. With these gone `test_shared_primary_group_warns` asserts nothing and the private/shared fork is unprobed. Cost: breaks if the warning is reworded. |
| B-027 | `cli/tests/agents/test_agent_home_permissions.py::test_create_absent_user_reports_created_not_creating,test_reinit_gone_user_reports_created_not_silently_reinitialized,test_reinit_converged_user_reports_already_exists_and_skips_usermod,test_reinit_diverged_shell_reports_fixed_and_runs_usermod` | Created/already exists/Fixed report-line pins with wording blacklists | delete | Four tests whose entire subject is which of three authored report lines fires. The `not any("Creating user" ...)` clauses are wording blacklists. The behavioral half (whether `usermod` ran) is asserted separately in each test on `target.commands`; the OK/Created/Fixed distinction is a sentence-only observable, so R2.4 applies. See summary: this is the same family as B-034. |
| B-028 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary,test_reinit_stopped_vm_gate_resolves_once_and_seeds_the_boundary` | header-banner and "Checking ..." substring pins | convert | Rubric re-check, 2026-08-16. `:240-241` duplicate only the Preflight header; the Resolving Secrets and Agent Initialization sections (`agents/manager/lifecycle.py:171` and `:531`) lose every probe, and reinit keeps no structural header assertion at all. Same `Role.HEADER` conversion as B-003. No production change. |
| B-029 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary,test_reinit_stopped_vm_gate_resolves_once_and_seeds_the_boundary,test_reinit_reconcile_is_idempotent_across_repeated_reinits,test_reinit_skips_stale_grant_and_reconciles_the_rest` | structural presence on (role, level, message) | keep | Section/level/result-role shape, the same invariant as B-004: a result emitted as body text, or a check line escaping to level 0, is caught here. |
| B-030 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_reinit_stopped_vm_gate_resolves_once_and_seeds_the_boundary` | report-line substring plus prefix blacklist | delete | 267 is redundant with the `Role.RESULT` tuple on 269. 272 (`not any(m.startswith("Reconciled "))`) is a phrase blacklist whose real subject, that no grant was reconciled, is a DB fact the test can read directly. |
| B-031 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_reinit_skips_stale_grant_and_reconciles_the_rest` | warning substring pair | delete | The stale-grant skip is observable in the DB rows the test already checks; `"skipping stale grant"` is the report. |
| B-032 | `cli/tests/agents/test_create_reinit_orchestrated.py::test_reinit_reconciles_grant_all_agent_via_materialized_rows` | count-in-report-line pin | delete | "Reconciled 1 workspace grant" restates the grant row the test seeded and can assert on directly. |
| B-033 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_grant_reachable_vm_is_one_boundary_burst,test_revoke_reachable_vm_is_one_boundary_burst,test_delete_choreography_end_to_end_standalone,test_grant_all_sets_flag_and_adds_every_vm_workspace,test_grant_missing_workspace_warns_and_skips,test_revoke_named_workspace_with_implicit_access_keeps_membership,test_revoke_all_warns_about_remaining_implicit_access` | Granted/Revoked/Deleted report-line pins | delete | Every one of these has a DB-state or call-spy sibling in the same test (grant rows, session rows, the delete spy). The rendered per-item lines and the "(still has implicit access via sessions)" parenthetical are the report. |
| B-034 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_grant_reachable_vm_is_one_boundary_burst,test_revoke_reachable_vm_is_one_boundary_burst` | Role.RESULT tuple equality carrying a full authored sentence | delete | Unlike B-004, the assertion here is `(Role.RESULT, 0, "Agent 'a1' granted access to 1 workspace")`: the role and level are incidental and the sentence is the payload. The role-and-level invariant is already covered by B-029 in the sibling file. |
| B-035 | `cli/tests/agents/test_delete_grant_revoke_orchestrated.py::test_delete_socket_dir_runs_even_when_userdel_fails,test_grant_missing_workspace_warns_and_skips,test_revoke_all_warns_about_remaining_implicit_access` | warning-wording substring | convert | Three tests where "did it warn at all" is the invariant (a failed userdel must not be silent, a missing workspace must not be silently skipped, remaining implicit access must be surfaced) and no sibling sees it. VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes at all three. No production change. |
| B-036 | `cli/tests/agents/test_shell_exec_orchestrated.py::test_exec_dash_prefixed_command_fails_with_zero_resolves_and_zero_gate` | hint substring pair | keep | `"put '--' before"` and `"sh -c"` are the literal argv syntax the operator must type, and the test's subject is that a dash-prefixed command fails with a recovery instruction rather than a bare parse error. Trim to the `sh -c` token if the lead wants it narrower. Flagged: `"put '--' before"` is closer to prose than to a token. |
| B-037 | `cli/tests/workspaces/test_create_orchestrated.py::test_create_stopped_vm_gate_resolves_once_and_seeds_the_boundary` | count-of-report-line plus prefix blacklist | delete | `sum(... "Workspace 'ws1' created" ...) == 1` pins the sentence and its non-duplication; the no-section-header clause is a form blacklist. The create itself is a DB fact asserted beside it. |
| B-038 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_reachable_vm_is_one_boundary_burst` | report-line pin | delete | `resolve_counter` and the `chmod -c 2770` command on 243-244 carry the boundary-burst claim; the "Repairing workspace ..." announce line is the report. |
| B-039 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_skips_git_identity_when_not_a_repo` | warning-absence substring | convert | The invariant (identity on a non-repo workspace is a quiet no-op) is real and only the warning stream sees it. VERIFIED by executing: `assert captured_output.warnings == []` passes. No production change. |
| B-040 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_git_identity_warns_on_unexpected_probe_failure` | warning-presence substring | convert | Companion to B-039: the branch that must NOT be silent. VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes. No production change. |
| B-041 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_healthy_workspace_reports_ok_for_every_step,test_repair_fully_damaged_workspace_reports_fixed_per_step,test_repair_partial_damage_reports_only_the_diverged_step,test_repair_sgid_only_damage_reports_permissions_fixed,test_repair_mode_only_damage_reports_permissions_fixed,test_repair_owner_only_damage_reports_permissions_fixed,test_repair_hardens_other_bits_on_a_pre254_workspace` | `OK:` / `Fixed:` / `Repaired N issue(s)` report-line pins | keep | **Operator ruling, 2026-08-16**, applying the rubric that this row provoked. These assertions do not fail only on a rewording: each tracks a DETECTION OUTCOME, whether a repair step found something to converge or found the state already correct, and `repair_workspace` returns `None` (`workspaces/manager/repair.py:27-33`) so nothing else observes it. Convergence is idempotent, so the emitted command list is identical either way, which is what makes this the only probe. The three `*_only_damage` tests pin the owner/mode/sgid OR-collapse at `repair.py:189` against an AND-collapse regression the code comments record as having really happened. **What the keep costs, stated so nobody later reads this as a prose pin nobody noticed**: these assertions break if someone rewords `OK:`, `Fixed:` or `Repaired N issue(s)`, and whoever does that must update them. That is the price of the only probe there is, not an oversight. |
| B-042 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_acl_churn_does_not_report_false_fixed` | OK/Fixed report-line pins | convert | The invariant here IS separable from the report: `_acls_changed` is a module-level helper (`repair.py:340`). VERIFIED by calling it directly at HEAD: churn-only snapshots give `False`, a real per-path ACL change gives `True`, empty-vs-empty gives `False`. Replace the whole test with a direct `_acls_changed` unit test over the three inputs. No production change. |
| B-043 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_repair_acls_indeterminate_when_getfacl_fails` | OK-absence, Fixed-prefix blacklist, warning substring, qualified closing-line pin | keep | Moves with B-041 under the same ruling: the indeterminate ACL branch must warn and must NOT report a verified `OK:`, and that outcome has no observable but the report line. The `Fixed:`-prefix blacklist half is the exception and still goes, because a blacklist cannot hold the property and the surviving `setfacl` convergence command at `:694` carries what it was reaching for. Same rewording cost as B-041. |
| B-044 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_first_repair_after_create_is_a_noop,test_first_repair_after_rehome_is_a_noop,test_first_repair_after_copy_is_a_noop` | first-repair-is-a-noop report pins | keep | Moves with B-041: the claim is that the first repair after create, rehome and copy finds NOTHING to fix, which is exactly a detection outcome and is exactly what a regression in the canonical ACL spec would break. The `setfacl` assertions at `:856-857` and `:1992-1993` prove the spec was applied, not that repair then saw it as already correct, so they are not a sibling for this. Same rewording cost as B-041. |
| B-045 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_delete_reachable_vm_is_one_boundary_burst,test_delete_group_removal_failure_does_not_break_the_delete,test_copy_cross_vm_runs_two_sequential_boundaries_with_nested_holds,test_copy_same_vm_reuses_the_source_composition` | closing report-line pins | delete | "Workspace 'ws1' deleted" / "copied to 'ws2'" restate DB facts the same tests assert directly. |
| B-046 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_delete_group_removal_failure_does_not_break_the_delete,test_create_rollback_tolerates_a_groupdel_failure` | warning-absence blacklist | convert | The invariant (a tolerated groupdel failure does not surface as a rollback warning) is real and only the warning stream sees it. VERIFIED by executing: `assert captured_output.warnings == []` passes at both sites. No production change. |
| B-046a | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_create_rollback_on_clone_failure_leaves_no_residue` | warning-absence blacklist | convert | Same invariant, different count. VERIFIED by executing: `warnings == []` FAILS here because an unrelated git-credential hint warning always fires; `assert len(captured_output.warnings) == 1` passes. This premise-check changed the replacement I would have written. |
| B-046b | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_copy_without_grant_all_agents_makes_no_grant_calls` | detail-absence blacklist | delete | I expected to need a call-spy replacement; VERIFIED by executing that the test ALREADY has one, `calls == []` plus `db.list_agent_grants("dev") == []` on 1713-1714, which is the whole invariant. (`detail == []` fails here: an unrelated VS Code workspace line is emitted.) Clean delete. |
| B-047 | `cli/tests/workspaces/test_lifecycle_orchestrated.py::test_copy_materializes_grant_all_agents_on_the_dest_vm` | count-in-report-line pin | delete | "Added 1 grant-all agent(s) to workspace" restates the grant row the test can read from the DB. |
| B-048 | `cli/tests/orchestration/test_activation.py::test_operator_stopped_refusal_propagates_from_the_node` | hint equality | keep | Rubric re-check, 2026-08-16. **The row was mistaken.** The pinned hint is authored by the test's own `_Target` double at `orchestration/test_activation.py:71`, not by production, so it is an injected sentinel. It probes that `ensure_active` propagates the node's refusal untouched, which nothing else does once G1-018 removes the `match=`. |
| B-049 | `cli/tests/orchestration/test_secrets.py::test_require_predicted_refs_refuses_with_owner_usage_framing` | **[subtracted: secrets-preview]** verbatim error and hint equality | delete | The docstring says the shape is "preserved VERBATIM", which is exactly the two-file ceremony the rule targets. The usage-propagation half of this (that the declared description reaches the message) is kept by B-050. |
| B-050 | `cli/tests/orchestration/test_secrets.py::test_require_declared_refs_refuses_a_dangling_reference` | **[subtracted: secrets-preview]** injected-value substring | keep | `"the Proxmox API token"` is the description the TEST declared in its own manifest fixture; the assertion proves that declared usage propagates into the dangling-reference diagnostic. Value identity, not authored prose (the charter's named keep case). |
| B-051 | `cli/tests/orchestration/test_secrets.py::test_prediction_skips_a_not_ready_source` | **[subtracted: secrets-preview]** frozen record equality carrying an injected reason | keep | `SkippedSource(source="op", reason="op CLI not installed")` is a shipped record whose `reason` the fixture supplied; the assertion is record identity and field propagation. |
| B-052 | `cli/tests/orchestration/test_readiness.py::test_skip_and_degrade_skips_rejected_and_reports_them` | tuple equality carrying an injected reason | keep | `rejected == [("gh", "gh: token rejected")]` pins the identity and pairing of the rejection record; the string is the fixture's own. |
| B-053 | `cli/tests/orchestration/test_unwind.py::test_failed_teardown_warns_and_never_masks` | warning substring | convert | "a failed teardown warns and never masks the original error" is the invariant; the never-masks half is structural (the original error propagates) but the warns half only the warning stream sees. VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes. No production change. |
| B-054 | `cli/tests/transports/test_factories.py::test_native_transport_none_raises_typed_state_error` | hint substring | convert | Rubric re-check, 2026-08-16. The claim is that the PLATFORM's own hint is carried, and `hint is not None` at `:219` does not carry it. Assert `ProxmoxPlatform.no_native_transport_hint in exc_info.value.hint`, deriving from the canonical constant the test already reads at `:210`. No production change. |
| B-055 | `cli/tests/transports/test_factories.py::test_native_transport_probe_exhaustion_warns_platform_hint` | warning list membership on an authored phrase | keep | Rubric re-check, 2026-08-16. **The row's premise was wrong.** `"scoped allow guidance"` is the test's OWN fixture value, set at `transports/test_factories.py:297`, not authored prose, so it is value identity of the kind L-010 keeps, and it is the only probe that `probe_failure_hint` reaches the operator. |
| B-058 | `cli/tests/transports/test_ssh.py::test_interactive_reports_a_dropped_connection` | warning substring pair | convert | The "dropped connection is reported, not swallowed" invariant is only visible in the warning stream. Replace with a warning-count assertion; VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes. `"agentworks@vm1"` is the host/user the test injected and can stay as value identity if the lead wants the propagation kept. |
| B-060 | `cli/tests/secrets/test_disabled_plugin_attribution.py::test_declared_source_accepts_string_subclass_plugin_attribution` | **[subtracted: secrets-preview]** formatted-line suffix pin | delete | `format_outcome(outcome).endswith("remediation=enable plugin 'Vault.Plugin'")` pins the formatter's authored layout; `outcome.remediation` and `outcome.remediation_target` are the shipped fields and are asserted in the sibling test at `test_resolution_lifecycle.py:542`'s neighborhood. |
| B-061 | `cli/tests/secrets/test_resolution_lifecycle.py::test_exact_five_categories_and_exhaustive_detail_table` | **[subtracted: secrets-preview]** formatted-line equality | delete | Same shape as B-060: `format_remediation(outcome) == "enable plugin 'Vault.Plugin'"`. The subject, that a registered string-subclass plugin name survives into the remediation target, is the `remediation_target` field. |
| B-066 | `cli/tests/assistance/test_generation.py::test_command_check_exits_nonzero_without_repairing_drift` | path-in-output substring | keep | The asserted string is a repository path (`plugins/codex/agentworks/.codex-plugin/plugin.json`), and the test's subject is that the drift check names the file that drifted while exiting nonzero without repairing. Path reference, not prose (the charter's named keep case). Recommend pairing it with a path-existence assertion. |
| B-067 | `cli/tests/test_agents.py::test_assert_agent_ssh_works_raises_on_ssh_transport_failure,test_assert_agent_ssh_works_wraps_ssh_error_as_connectivity` | hint substring | delete | `"agent reinit claude" in hint` and `"alpha" in hint` pin fragments of authored recovery sentences; the typed `ConnectivityError` and its entity fields already discriminate, and the agent name is asserted through `entity_name` in the same tests' neighbors. |
| B-069 | `cli/tests/test_codex_integration.py::test_raise_hints_name_a_recovery_the_operator_can_actually_take` | hint substring plus wording blacklist | convert | Rubric re-check, 2026-08-16. The one test in the file where three same-type `StateError`s are discriminated only by hint, and `test_recorder_file_that_exists_but_will_not_read_raises` delegates its discrimination to it by name at `test_codex_integration.py:316-319`. Keep the fixture-determined path at `:339` and replace the wording clauses with a distinctness check over the three hints. No production change. |
| B-070 | `cli/tests/test_codex_integration.py::test_raise_hints_name_a_recovery_the_operator_can_actually_take` | hint substring and hint verbatim equality | convert | Rubric re-check, 2026-08-16. Same test and same replacement as B-069; treat the two as one edit. |
| B-071 | `cli/tests/test_codex_integration.py::test_recorder_content_that_is_not_a_uuid_is_treated_as_unbound,test_single_candidate_is_adopted_and_resumed,test_zero_candidates_launches_fresh,test_several_candidates_open_the_picker_instead_of_raising,test_a_candidate_whose_filename_has_no_uuid_opens_the_picker,test_discovery_without_a_sessions_dir_launches_fresh,test_gone_rollout_drops_the_id_and_falls_through_to_a_fresh_launch,test_a_dropped_stale_binding_is_reported_alongside_whatever_replaced_it,test_recorder_never_breaks_the_turn_and_writes_nothing_on_bad_input` | pane-echo wording substrings | delete | Every one of these sits beside the structural claim the test is actually about: `state == {}` or `state == {"session_id": _SID}`, plus `f"resume {_SID}" in command` / `"resume" not in _sh_argv(...)`. The echo text is what the operator reads in the pane, and the adopted uuid (value identity) already rides in the command. |
| B-073 | `cli/tests/test_codex_integration.py::test_config_string_fields_escape_toml_structural_characters` | argv token containing a space | keep | `"/data/shared dir" in argv` proves a spaced writable dir survives as ONE argv token; the string is the path the test injected and the invariant is quoting, not wording. |
| B-076 | `cli/tests/test_consoles_attach.py::test_attach_console_announces_build_path,test_attach_console_announces_attach_path,test_attach_console_announces_recreate_path` | build/attach/recreate path announce pins plus wording blacklist | delete | Three tests announcing which path was taken; the paths themselves are observable in the tmux commands the same tests already assert (`new-session` vs `has-session` vs `kill-session`). Line 268 is a wording blacklist. |
| B-077 | `cli/tests/test_consoles_attach.py::test_attach_console_warns_when_list_windows_fails,test_attach_console_skips_missing_session_with_warning,test_attach_console_skips_window_when_agent_missing,test_attach_console_continues_when_one_shell_split_fails` | warning substring | convert | Four degrade-rather-than-fail branches (list-windows failed, ghost session skipped, missing agent, shell split without a pane id) where "did it warn" is the invariant and no sibling sees it. VERIFIED by executing: `assert len(captured_output.warnings) == 1` passes at all four. No production change. |
| B-077a | `cli/tests/test_consoles_attach.py::test_attach_console_keeps_placeholder_when_all_members_fail` | warning substring | convert | Same shape, different count. VERIFIED by executing: this branch emits TWO warnings (the failed add-window plus the placeholder-kept notice), so the replacement is `len(captured_output.warnings) == 2`. |
| B-078 | `cli/tests/test_consoles_attach.py::test_describe_console_shows_admin_shell_state` | describe-line report pin | delete | `"Admin shell: no"` / `"Admin shell: yes"` restate a DB column the test set; layout only. |
| B-079 | `cli/tests/test_consoles_layout.py::test_apply_layout_aw_session_vertical_silent_on_single_pane,test_apply_layout_aw_session_vertical_warns_on_genuine_failure,test_apply_layout_aw_session_vertical_applies_under_pane_base_index_one` | warning substring and absence | convert | The invariant, that a genuine layout failure warns while a single-pane no-op and a `pane-base-index 1` layout do not, is real and only the warning stream sees it. VERIFIED by executing: `captured_output.warnings == []` at 187 and 233, `len(...) == 1` at 209, all pass. No production change. |
| B-080 | `cli/tests/test_consoles_live_sync.py::test_add_session_live_sync_adds_window_for_bare_spec,test_reorder_sessions_live_sync_bails_on_duplicate_window_names,test_kill_session_windows_transport_failure_warns` | warning substring and absence | convert | Same shape as B-079: live-sync silently-succeeded vs bailed-with-a-reason. VERIFIED by executing: `warnings == []` at 87 and `len(...) == 1` at 368 and 462-464 all pass. The window names `alpha`/`beta` at 462-464 are the test's own and can stay as value identity. No production change. |
| B-081 | `cli/tests/test_consoles_live_sync.py::test_delete_session_reports_affected_consoles,test_delete_session_yes_reports_but_keeps_empty_console,test_remove_sessions_warns_when_offered_console_delete_raises` | report-line equality | delete | `report == ["Removed 's' from consoles: alpha, beta"]` and its two siblings pin an authored sentence that restates console membership the tests read from the DB directly. |
| B-082 | `cli/tests/test_consoles_live_sync.py::test_delete_session_offers_and_deletes_now_empty_console,test_delete_session_does_not_offer_console_with_remaining_sessions,test_remove_sessions_empties_console_offers_and_deletes,test_remove_sessions_with_remaining_not_offered` | prompt-wording substring and absence | convert | Same invariant and same replacement as B-007 (was the operator offered the delete, or not), VERIFIED there. Replace with prompt-count assertions. No production change. |
| B-083 | `cli/tests/test_consoles_live_sync.py::test_delete_session_yes_reports_but_keeps_empty_console,test_delete_session_warns_when_offered_console_delete_raises,test_remove_sessions_yes_reports_but_keeps_empty_console,test_remove_sessions_warns_when_offered_console_delete_raises,test_remove_sessions_non_interactive_without_yes_reports_but_keeps` | report-but-keep warning triples | delete | Identical family to B-010: every one sits under a console-row assertion that carries the keep. |
| B-084 | `cli/tests/test_consoles_orchestration.py::test_reorder_sessions_noop_when_already_in_requested_order` | report-line substring | delete | "already in the requested order" restates the no-op the test asserts structurally (no tmux move commands, unchanged rows). |
| B-085 | `cli/tests/test_consoles_orchestration.py::test_describe_console_uses_iteration_index` | rendered-line list equality | convert | Rubric re-check, 2026-08-16. The iteration-index-not-stored-position invariant is real (a remove leaves a position gap in the DB) and `:384-387` is its only probe. Keep the leading `[N]` indices, drop the surrounding sentence. The row already prescribed this; the re-check makes it binding. No production change. |
| B-086 | `cli/tests/test_consoles_restore.py::test_restore_session_noop_when_live_matches_config,test_restore_session_healthy_under_pane_base_index_one,test_restore_session_is_a_noop_on_a_healthy_model` | report-line substring | delete | Three no-op tests whose structural half (no rebuild commands issued) is asserted beside them. |
| B-087 | `cli/tests/test_consoles_restore.py::test_restore_session_rebuild_raises_when_new_window_fails,test_restore_session_rebuild_raises_when_shell_split_fails,test_restore_session_rebuild_raises_when_shell_tag_fails` | report-line absence | delete | `not any("Rebuilt window" in m ...)` is a phrase blacklist standing in for "the rebuild did not complete", which the raised error and the absent tmux commands already carry. |
| B-088 | `cli/tests/test_consoles_restore.py::test_restore_session_rebuild_raises_when_shell_split_fails,test_restore_session_rebuild_raises_when_shell_tag_fails,test_restore_session_refuses_when_session_pane_killed` | hint substring naming a CLI command | keep | `"agw console attach con --recreate" in hint` is the literal recovery command an operator types, keyed on the console name the test seeded. Same class as B-016: a command reference, not prose about one. |
| B-089 | `cli/tests/test_consoles_restore.py::test_restore_session_reorders_relative_to_the_session_pane` | warning-absence blacklist | convert | Replace with `captured_output.warnings == []`. VERIFIED by executing: it passes. No production change. |
| B-090 | `cli/tests/test_consoles_shell_panes.py::test_split_shell_pane_preserve_probe_warns_once_per_console_build,test_split_shell_pane_agent_branch_no_probe_without_composed_env` | phrase-filtered warning counting | delete | I expected a plain warning count to work here; VERIFIED by executing that it does not. The build emits five warnings at 271 and one unrelated warning at 294, so the count only means anything through the phrase filter. With no way to identify the once-per-build warning except by its wording, and no production change permitted to give it one (R2.2), R2.4 directs deletion. This premise-check changed my disposition. |
| B-091 | `cli/tests/test_consoles_shell_panes.py::test_split_shell_pane_warns_when_split_returns_no_pane_id,test_split_shell_pane_warns_when_set_option_fails` | required-phrase lists in warnings | convert | Rubric re-check, 2026-08-16. At `:477` the phrase list is the test's only assertion, so deleting it leaves `test_split_shell_pane_warns_when_set_option_fails` empty and the tag-failure degrade unprobed. Convert to a warning-count assertion. No production change. |
| B-092 | `cli/tests/test_consoles_shell_panes.py::test_split_shell_pane_agent_branch_warns_and_falls_back_when_setenv_missing` | required-phrase list on a warning | delete | `"Defaults:admin setenv"`, `"agw vm reinit vm1"`, `"not allowed to set the following environment variables"`: the first two are sudoers syntax and a CLI command (keepable as tokens), the third is authored prose. The fall-back behavior itself is asserted through the emitted command in the same test. |
| B-093 | `cli/tests/test_secret_describe.py::test_multiple_usages_render_one_row_each` | **[subtracted: secrets-preview]** rendered usage-text list equality | keep | `texts == ["the ADMIN_KEY env var", "the TEMPLATE_KEY env var"]` pins that one row is produced per usage, in declaration order, with the usage strings the test's own manifests declared. Arity and ordering, with value identity for the strings. |
| B-094 | `cli/tests/test_secret_describe.py::test_not_ready_source_annotated_and_skipped_in_preview` | **[subtracted: secrets-preview]** injected reason equality and record membership | keep | `not_ready_reason` and `SkippedSource(...)` are shipped fields carrying the fixture's own reason; record identity, not wording. |
| B-095 | `cli/tests/test_secret_describe.py::test_render_shows_not_ready_annotation_and_skip` | **[subtracted: secrets-preview]** rendered-line verbatim pins | delete | `"onepassword (onepassword, declared): op://Vault/api/field (not ready: op CLI not installed)"` pins the renderer's exact composition. The data half is B-094; what remains is layout. |
| B-100 | `cli/tests/test_secret_describe.py::L729` | **[line-anchored: between functions]** **[subtracted: secrets-preview]** hint substring naming a CLI command | keep | `"agw secret list" in hint` is the literal next command; same class as B-088. |
| B-102 | `cli/tests/test_secret_sources_drive_harness.py::test_fake_provider_path_is_closed_and_cannot_fall_through` | **[subtracted: secrets-preview]** injected-sentinel equality | keep | `completed.stdout == "fixture-sentinel"` proves the fake provider path is closed and cannot fall through to a real one. Value identity on a string the test injected (the charter's named keep case). |
| B-103 | `cli/tests/test_secret_verify.py::test_verify_disabled_plugin_source_discards_printable_readiness_reason,test_verify_declared_source_retains_disabled_backend_plugin_remediation,test_verify_refuses_interactive_source_without_construction` | **[subtracted: secrets-preview]** formatted-line suffix pin and enum-token substring | convert | `format_outcome(...).endswith("remediation=enable plugin 'onepassword'")` restates fields already asserted structurally on the lines above (`outcome.remediation`, `outcome.remediation_target`), so those clauses delete. `"source-backend-plugin-disabled" in result.stdout` is the enum's own wire value: VERIFIED at HEAD that `ResolutionDetail.SOURCE_BACKEND_PLUGIN_DISABLED.value == "source-backend-plugin-disabled"`. Replace the literal with the enum member's `.value` so the assertion derives from the canonical source. VERIFIED by executing the rewrite across this file: all 99 tests pass. No production change. |
| B-104 | `cli/tests/test_secret_verify.py::test_verify_declared_source_retains_disabled_backend_plugin_remediation` | **[subtracted: secrets-preview]** phrase-occurrence counting | delete | `_plain(result.stdout).count("enable plugin 'onepassword'") == 1` counts an authored phrase to prove non-duplication; the row count is already pinned structurally elsewhere in this file (e.g. line 812's `len(result.stdout.splitlines()) == 3`). |
| B-105 | `cli/tests/test_secret_verify.py::test_secret_verify_cli_renders_variadic_success_batch` | **[subtracted: secrets-preview]** table-header list equality | keep | Column set and arity: a renderer change that silently drops the REMEDIATION column would hide every remediation the operator needs, and nothing else in the suite sees it. This is the charter's synthetic-fixture-layout keep, and the row-slicing beneath it depends on the header being one line. |
| B-106 | `cli/tests/test_secret_verify.py::test_secret_verify_cli_renders_variadic_success_batch,test_secret_verify_cli_mixed_batch_renders_all_rows_then_exits_one,test_secret_verify_cli_unicode_separator_cannot_forge_a_row` | **[subtracted: secrets-preview]** row-identity, ordering and row-count assertions | keep | `[line.split()[0] for line in rows] == ["beta", "alpha"]` pins de-duplication and input ordering; `len(splitlines()) == 3` pins that a unicode line separator inside a backend payload cannot forge an extra row. Structural, and the second is a genuine injection defense. |
| B-107 | `cli/tests/test_secret_verify.py::test_secret_verify_cli_renders_variadic_success_batch,test_secret_verify_cli_mixed_batch_renders_all_rows_then_exits_one,test_secret_verify_cli_unicode_separator_cannot_forge_a_row,test_secret_verify_cli_allow_interaction_resolves_interactive_source,test_secret_verify_cli_onepassword_plugin_requires_and_honors_opt_in,test_secret_verify_cli_provider_payload_never_reaches_renderer` | **[subtracted: secrets-preview]** sentinel-absence assertions | keep | Redaction defenses: a resolved secret value, a forged row payload and a hostile provider payload must never reach rendered output. The charter names this shape as explicitly not prose policing. |
| B-108 | `cli/tests/test_secret_verify.py::test_secret_verify_cli_unicode_separator_cannot_forge_a_row,test_secret_verify_cli_default_refusal_does_not_read_global_state,test_secret_verify_cli_allow_interaction_resolves_interactive_source,test_secret_verify_cli_onepassword_plugin_requires_and_honors_opt_in,test_secret_verify_cli_provider_payload_never_reaches_renderer` | **[subtracted: secrets-preview]** enum-token substring in stdout | convert | Same treatment as B-103: `"unavailable"`, `"resolved"`, `"refused-interaction"`, `"backend-protocol"`, `"resolution-failure"`, `"unexpected"` are all `ResolutionCategory`/`ResolutionDetail` wire values (VERIFIED by enumerating both enums at HEAD). Derive the literal from the enum member rather than restating it. VERIFIED by executing the rewrite across this file: all 99 tests pass. `ResolutionCategory` is already imported here. No production change. |
| B-109 | `cli/tests/test_secret_verify.py::test_invalid_name_error_is_safe_and_context_free` | **[subtracted: secrets-preview]** verbatim error-message equality | delete | A three-line verbatim pin of the name-validation error sentence. The invariant that matters (the error is context-free and leaks no caller data) is a redaction property better expressed as an absence check on the offending input, which the same test can do; the sentence itself is authored prose. |
| B-110 | `cli/tests/test_secret_verify.py::test_secret_verify_cli_requires_at_least_one_name` | **[subtracted: secrets-preview]** upstream Typer/Click message substring | keep | `"Missing argument 'NAMES...'" in result.stderr` is Click's own output, not ours: the one sanctioned exception. Narrow, and paired with the exit code. |
| B-111 | `cli/tests/test_secret_verify.py::test_secret_verify_cli_invalid_name_has_no_table_or_echo,test_secret_verify_cli_unknown_name_has_no_table` | **[subtracted: secrets-preview]** authored error substring in stderr | delete | `"invalid secret name"` and `"secret 'missing' not found"` are our prose; the tests' real subject (no table is rendered, exit code 1) is asserted beside them. |
| B-112 | `cli/tests/test_secrets_env_var.py::test_unset_env_is_soft_miss,test_internal_whitespace_is_preserved` | **[subtracted: secrets-preview]** detail-enum value and whitespace-preserving value equality | keep | `batch.outcomes[0].detail.value == "soft-miss"` is an enum wire value (recommend deriving from `ResolutionDetail.SOFT_MISS`), and `values == {"token": "  internal value  "}` proves internal whitespace survives resolution: value identity on an injected string. |
| B-113 | `cli/tests/test_secrets_inspect.py::test_no_operator_secrets_still_shows_auto_declared` | **[subtracted: secrets-preview]** auto-declared description verbatim equality | convert | Rubric re-check, 2026-08-16. The auto-declared description is SYNTHESIZED from the requirement's usage and source, and `:88-92` do not probe the synthesis: name presence and `auto_count >= 1` both survive an empty description. Assert the `(auto)` prefix plus the source substring. No production change. |
| B-114 | `cli/tests/test_secrets_inspect.py::test_grid_cell_not_ready_wins_over_identifier,test_grid_cell_wont_attempt_wins_over_not_ready` | **[subtracted: secrets-preview]** injected-reason equality | keep | `not_ready_reason == "op CLI not installed"` is the fixture's own reason propagating into the grid cell; value identity. |
| B-116 | `cli/tests/test_secrets_orchestration.py::test_resolve_for_command_rejects_multiline_environment_target_after_delivery`, `cli/tests/test_secrets_resolver.py::test_operation_resolver_rejects_multiline_environment_target_after_delivery` | **[subtracted: secrets-preview]** error-message substring | delete | `"cannot be used for environment injection"` is authored prose; both tests already discriminate on the raised type, and the multiline-value refusal is the behavior. |
| B-117 | `cli/tests/test_session_agent_filter.py::test_session_stop_agent_without_all_errors,test_session_resume_agent_without_batch_errors,test_session_stop_admin_and_agent_are_mutually_exclusive,test_session_stop_admin_without_all_errors,test_session_resume_admin_and_agent_are_mutually_exclusive` | flag-name substring in CLI error output | keep | Each asserts that the error output names the flag(s) involved in a mutually-exclusive or missing-companion combination. These are option names, not prose, and they pair with `exit_code != 0` in every case. Flagged as borderline: if the lead reads them as prose pins, the exit-code siblings make them clean deletes. |
| B-118 | `cli/tests/test_session_create_ephemeral_prompt_filtering.py::test_workspace_prompt_filters_by_vm_flag,test_workspace_prompt_filters_by_existing_agent_vm,test_mode_prompt_filters_by_resolved_vm_with_info_line` | info-line substring | delete | "Only showing workspaces on VM 'vm-A'" restates the filtering the same tests assert by checking the option list they were offered. |
| B-119 | `cli/tests/test_session_create_ephemeral_prompt_filtering.py::test_workspace_prompt_filters_by_vm_flag,test_mode_prompt_filters_by_resolved_vm_with_info_line,test_mode_prompt_picks_existing_agent_pins_vm_no_vm_prompt,test_mode_prompt_picks_admin_then_vm_prompt_fires._choose_spy` | prompt-option and prompt-label equality | keep | `opts[-1] == "[Create new workspace]"` pins that the create-new escape hatch is last in the list (position is the invariant, and the option string is a menu token the code branches on by index). `msg == "Run session as:"` is a weaker case; recommend trimming that one to an option-list assertion. |
| B-120 | `cli/tests/test_session_create_ephemeral_rollback.py::test_session_create_frames_phases_like_a_plan` | header-banner and "Checking ..." substring pins | delete | Identical to B-003: lines 191-201 already carry the same phases structurally through `captured_output.lines`. |
| B-121 | `cli/tests/test_session_create_ephemeral_rollback.py::test_session_create_frames_phases_like_a_plan` | structural presence on (role, level, message) | keep | Same invariant as B-004: the plan-shaped output tree (which phases are sections, what nests at level 1). |
| B-122 | `cli/tests/test_session_create_ephemeral_vm_anchor.py::test_defaulted_ephemeral_agent_name_overflow_hints_override_flag,test_defaulted_ephemeral_workspace_name_overflow_hints_override_flag` | hint substring pairs | convert | `"--agent-name"` / `"--workspace-name"` are the override flags the operator needs and stay; `"defaulted to the session name"` is authored prose and goes. The name-overflow branch is discriminated by the typed error. No production change. |
| B-123 | `cli/tests/test_session_liveness.py::test_legacy_admin_session_without_socket_raises_state_error` | hint substring plus wording blacklist | delete | `"session resume" in hint` with `"session delete" not in hint` is a blacklist pair pinning which command the hint recommends. The legacy-socket branch is discriminated by the typed `StateError`. |
| B-124 | `cli/tests/test_session_resume_cli.py::test_restart_is_an_unknown_command` | upstream Click message substring | keep | `"No such command 'restart'" in result.output` is Click's own output proving the retired command is genuinely absent from the tree, not merely hidden. Sanctioned exception, and paired with a nonzero exit. |
| B-125 | `cli/tests/test_session_transport.py::test_resume_dead_workload_error_propagates` | error substring plus wording blacklist | delete | `"exited immediately after launch (status 2)"` is our authored wrapper text and `"already has an active tmux server" not in msg` is a blacklist. The propagation invariant is carried by the injected pane text asserted in the sibling at `test_sessions_tmux_create.py:367`. |
| B-126 | `cli/tests/test_sessions_tmux_create.py::test_dead_pane_at_first_check_captures_and_skips_grant,test_dead_pane_at_post_unset_recheck_captures_and_raises,test_probe_transport_failure_raises_could_not_verify_without_kill` | error substring plus wording blacklist | delete | Same family as B-125; 444's `"exited immediately" not in str(...)` is a blacklist standing in for "a transport failure took the other branch", which the raised type discriminates. |
| B-127 | `cli/tests/test_sessions_tmux_create.py::test_dead_pane_at_first_check_captures_and_skips_grant` | injected upstream text substring | keep | `"error: invalid value 'on-failure' for '--ask-for-approval <APPROVAL_POLICY>'"` is the codex CLI's own error, injected by the test's fake pane; the invariant is that captured pane output propagates verbatim into our diagnostic. Value identity plus the outside-prose exception. |
| B-128 | `cli/tests/test_sessions_tmux_create.py::test_pane_command_wraps_in_login_interactive_shell,test_tmux_env_flags_quotes_values_with_spaces,test_admin_create_session_passes_env_to_run_command,test_alive_pane_remain_on_exit_lifecycle,test_trim_captured_output_strips_blanks_and_caps` | emitted-command and captured-output token pins | keep | `cd /workspace && exec claude`, `-e 'GREET=hello world'`, `remain-on-exit on`, and the trim helper's last-line identity are the commands and data the code produces at the tmux boundary. Behavior, not prose. |
| B-129 | `cli/tests/test_shell_integration.py::test_missing_command_is_a_typed_error_naming_the_vm` | error substring plus hint substring | convert | Keep `"--template" in hint` (an option name, per B-016); delete `"for VM 'box'." in str(exc.value)`, which pins the authored trailing clause. No production change. |
| B-130 | `cli/tests/test_templates.py::test_unknown_template` | hint prefix pin | delete | `hint.startswith("available workspace templates: ")` pins the authored lead-in. |
| B-131 | `cli/tests/test_templates.py::test_unknown_template` | injected-name membership loop | keep | The loop asserts every declared template name appears in the hint; the names come from the test's own config fixture, so this is value identity and it is the substance of "the hint lists the declared names". |
| B-132 | `cli/tests/test_templates.py::test_unknown_template_hint_when_none_declared` | hint verbatim equality | keep | Rubric re-check, 2026-08-16. The empty-set branch raises the same `NotFoundError` as the populated branch, so the hint is its only discriminator and `:161` is the test's only assertion. Cost: breaks if the hint is reworded. |
| B-134 | `cli/tests/test_tmuxinator.py::test_ensure_agent_socket_root_missing_warns_by_default,test_ensure_agent_socket_root_misconfigured_warns_even_when_missing_suppressed,test_ensure_agent_socket_root_probe_failed_warns_even_when_missing_suppressed,test_ensure_agent_socket_dir_missing_warns_by_default,test_ensure_agent_socket_dir_misconfigured_warns_even_when_missing_suppressed,test_ensure_agent_socket_dir_probe_failed_warns_even_when_missing_suppressed` | warning-classification substrings | convert | Six branches distinguished only by which word appears in the warning (`missing`, `misconfigured`, `probe failed`) plus a suppression flag. The invariant, that suppressing the missing-warning does not suppress the misconfigured or probe-failed ones, is real and structural in nature. VERIFIED by executing: `assert len(warnings) == 1` passes at all six sites, so each branch's warning is the only one in its run and the count carries the classification. No production change. |
| B-135 | `cli/tests/test_tmux_model.py::test_new_window_reports_the_session_pane_index_via_dispatch` | dispatch-result equality | keep | `res.stdout.strip() == "1"` is the pane index the fake tmux returned; value identity, and the invariant is that the dispatch reports the session pane index rather than a fixed 0. |
| B-136 | `cli/tests/test_sources.py::test_fetch_file_clones_copies_and_cleans_git_source` | emitted-command substring | keep | `"git clone --depth 1 --branch v1"` is the command the fetcher runs at the subprocess boundary. Behavior. |
| C-039 | `cli/tests/test_completions.py::TestOptionFlagsInSpec.test_session_resume_is_discoverable_and_restart_is_absent,L604` | **[line-anchored: between functions]** message pin | delete | `"unable to detect the shell" in err` pins our authored abort wording; the exit code 1 asserted beside it is the invariant. |
| C-040 | `cli/tests/test_completions.py::TestUninstall.test_uninstall_when_nothing_installed_is_clean` | message pin | keep | **Corrected 2026-08-19 (map re-baseline).** Was delete, and re-derived at HEAD (the file moved from `:741`). `shell` is the parametrize parameter at `:902`, so `f"no {shell} completions found"` proves each shell's empty-state message names the shell the operator asked about; a powershell uninstall reporting about bash passes without it, and the exit-code assertion the old justification pointed at cannot see that. Under the rubric its failure mode is not only a rewording. Same family as G1-K06 and G1-K07. |
| C-041 | `cli/tests/test_completions.py::TestResolveShell.test_autodetect_failure_exits_with_message` | message pin | delete | `"could not determine PowerShell $PROFILE path" in stderr` pins wording; `exit_code != 0` carries the "fails loudly rather than silently succeeding" invariant. |
| C-051 | `cli/tests/test_completions.py::TestStaticChoiceCompletion.test_all_shells_emit_shell_choices` | authored warning pin used as a control | convert | Replace `"Config: unexpected keys in [operator]" in direct.stderr` with `direct.stderr != ""`. Its role is to show the direct invocation does emit a warning, so the wrapper's `2>/dev/null` suppression asserted below it means something. No production change. |
| C-056 | `cli/tests/test_doctor.py::test_machine_output_serializes_the_same_health_check_facts` | projection value identity | keep | The message and hint the test itself put on the group must survive the JSON projection: field-carrying behavior, not authored prose. |
| C-057 | `cli/tests/test_doctor.py::test_config_exception_becomes_one_shared_health_fact,test_config_exception_becomes_one_shared_health_fact.fail_load_config` | shared-fact propagation | keep | A config exception's message and hint reach the projected check with the injected marker intact: one health fact rather than a reworded copy per surface. |
| C-058 | `cli/tests/test_doctor.py::test_fresh_init_shaped_config_reaches_doctors_own_ssh_key_checks,L155` | **[line-anchored: between functions]** message substring beside an asserted status | delete | `"not found on this machine"` and `"stale"` are authored wording; the INFO-versus-WARN status asserted one line above discriminates exactly the same branch. |
| C-059 | `cli/tests/test_doctor.py::test_run_checks_returns_report,test_run_checks_group_order_and_config_failure_placeholder` | group ordering | keep | The report's group list is an ordered identity check, and the point of the test is that no registry-dependent group vanishes in degraded mode: each keeps its presentation slot. Ordering of records is structure. |
| C-060 | `cli/tests/test_doctor.py::test_run_checks_group_order_and_config_failure_placeholder` | required-phrase pair | delete | `"skipped" in message` and `"Configuration" in message` police the placeholder's wording; the structural facts beside them (exactly one check, status INFO) prove the group renders a placeholder instead of vanishing. |
| C-061 | `cli/tests/test_doctor.py::test_run_checks_group_order_and_config_failure_placeholder` | verbatim HealthCheck equality | convert | Replace with `[(c.name, c.status) for c in source_placeholder.checks] == [("Declared sources", doctor.Status.INFO)]`. "Secret sources" is the one group the loop above does not cover, so the structural half must stay; the embedded sentence goes. No production change. |
| C-062 | `cli/tests/test_doctor.py::test_run_checks_group_order_and_config_failure_placeholder` | cross-group parity | keep | Asserting the Secrets skip message equals the VM sites one pins that both come from one shared helper rather than two hand-written sentences; it fixes no wording of its own. |
| C-063 | `cli/tests/test_doctor_cli.py::TestDoctorColorOnATty.test_ok_label_is_green,TestDoctorColorOnATty.test_warn_label_is_yellow,TestDoctorColorOnATty.test_fail_label_is_red,TestDoctorColorOnATty.test_info_label_is_unstyled,TestDoctorColorOnATty.test_label_column_width_unaffected_by_ansi,TestDoctorColorOnATty.test_summary_counts_are_colored` | synthetic-fixture formatting | keep | The charter's explicit carve-out: check names and messages are the test's own fixture, and the assertions are about ANSI styling, the `[ok]` and its two trailing spaces/`[warn]` label column, and that `.ljust(6)` alignment survives ANSI stripping. |
| C-064 | `cli/tests/test_doctor_cli.py::TestDoctorPlainFallback.test_non_tty_stdout_is_byte_plain,TestDoctorPlainFallback.test_no_color_env_forces_byte_plain_even_on_a_tty,TestDoctorPlainFallback.test_non_interactive_forces_byte_plain_even_on_a_tty` | TTY fallback behavior | keep | No ANSI may reach a piped stream, `NO_COLOR`, or non-interactive mode, and the plain layout must stay byte-exact: terminal behavior over a synthetic report. |
| C-065 | `cli/tests/test_doctor_schema.py::test_schema_check_distinguishes_absent_table_from_malformed_shape,test_schema_check_rejects_invalid_scalar_versions,test_installed_doctor_reports_malformed_schema_in_human_and_json` | **[subtracted: instance-model]** redaction defense | keep | The private column name and the absolute database path must not reach the operator or any output stream. A leak defense, not prose policing. |
| C-066 | `cli/tests/test_doctor_schema.py::test_doctor_distinguishes_absent_stale_and_newer_schema` | **[subtracted: instance-model]** expected-text column beside an asserted status | delete | The parametrization already discriminates absent, stale and newer schema by `Status.OK`/`WARN`/`FAIL`; `expected_text in check.message` adds only our wording. |
| C-067 | `cli/tests/test_doctor_schema.py::test_doctor_warns_without_echoing_an_unexpected_vm_initialization_state` | **[subtracted: instance-model]** verbatim message equality | convert | The test's own name says "without echoing": replace `warning.message == "unexpected initialization state"` with `"PRIVATE_FUTURE_STATE" not in (warning.message or "")` beside the asserted WARN status, turning a wording pin into the redaction defense it stands in for. No production change. |
| C-068 | `cli/tests/test_doctor_schema.py::test_doctor_database_errors_remain_shared_actionable_facts` | **[subtracted: instance-model]** authored prefix around an injected marker | convert | Replace the three `== f"could not check the database: {marker}"` equalities with `marker in message`. The invariant is that one database error reaches the system, database and vm-sites rows intact; the prefix is ours to reword. No production change. |
| C-069 | `cli/tests/test_doctor_schema.py::test_installed_doctor_reports_malformed_schema_in_human_and_json` | **[subtracted: instance-model]** human-output sentence pin | delete | The malformed-schema fact is asserted structurally in the JSON projection below and by the shared exit code; these two pin the human render's wording. |
| C-070 | `cli/tests/test_doctor_schema.py::test_installed_doctor_reports_malformed_schema_in_human_and_json` | **[subtracted: instance-model]** verbatim check dict | convert | Replace the dict equality with `list(checks[0]) == ["name", "status", "message", "hint"]` plus `(checks[0]["name"], checks[0]["status"]) == ("Database", "fail")`, keeping the projection's key shape and the failure identity and dropping the sentence. No production change. |
| C-071 | `cli/tests/test_doctor_env_and_secrets.py::test_focused_checks_are_the_exact_bulk_resource_rows` | **[subtracted: secrets-preview]** tuple equality carrying an authored message | convert | Replace with the `(name, status)` pair plus `"env-var" in (message or "")`. Which source doctor says it would attempt is load-bearing; "would attempt via" is ours. No production change. |
| C-072 | `cli/tests/test_doctor_env_and_secrets.py::test_auto_declared_secrets_are_reported,test_secret_resolves_via_env_var_when_set,_sp_site_config,L193` | **[line-anchored: between functions]** **[subtracted: secrets-preview]** authored plan-sentence pins | convert | The "would attempt via <source>" family: assert the source name appears in the row's message, not the sentence around it. No production change. |
| C-073 | `cli/tests/test_doctor_env_and_secrets.py::test_secret_not_available_when_env_var_unset_and_prompt_opted_out,L249` | **[line-anchored: between functions]** **[subtracted: secrets-preview]** readiness-reason and enablement wording | delete | `== "not ready: op CLI not installed"` and `"not enabled in [plugins].system" in message` pin authored reason text; the INFO status asserted beside each discriminates the same branch. |
| C-074 | `cli/tests/test_doctor_env_and_secrets.py::test_check_secrets_flags_a_not_ready_only_source` | **[subtracted: secrets-preview]** authored fragment around an injected field name | convert | Replace `"bogus: unknown field" in message` with `"bogus" in message`: the injected field name propagating is the invariant. No production change. |
| C-075 | `cli/tests/test_doctor_env_and_secrets.py::test_mapping_to_undeclared_kind_hard_errors_at_build,test_manifest_issues_surface_as_doctor_rows,L422-423,L537` | **[line-anchored: between functions, module level]** **[subtracted: secrets-preview]** injected-value propagation | keep | Each asserts a name the test itself created (an env var, a manifest filename, a site name, the injected issue text) reaches the doctor row that should carry it. Value identity. |
| C-078 | `cli/tests/test_resource_edit.py::test_cli_edit_launches_editor_on_manifest,test_missing_ssh_keys_do_not_block_resource_edit` | report-line pin | delete | The material fact (which file the editor was launched on) is asserted structurally at 144 and 258 by the recorded editor argv; the "Editing secret/npm-token (~/...:1)" line is a sentence-only observable. Its operator-path rendering is separately guarded in `test_operator_path_rendering.py`. |
| C-079 | `cli/tests/test_resource_edit.py::test_cli_edit_names_the_manifest_home_relative` | message pin | convert | Rubric re-check, 2026-08-16. Verified by execution: with `$EDITOR` unset the invocation fails as `SystemExit(1)`, and with `$EDITOR` set the SAME invocation fails as `NotFoundError`, also exit code 1, so `:203` alone does not prove the `$EDITOR` gate fired. Assert `isinstance(result.exception, SystemExit)`. No production change. |
| C-080 | `cli/tests/test_resource_edit.py::test_cli_edit_rejects_invalid_identity` | report-line pins | convert | Rubric re-check, 2026-08-16. The two sentences are the only witness that the fallback path was taken at all, and the fixture can rot silently: if `prompt: broken-on-purpose` ever validates, the strict path succeeds and the test stops testing the fallback. Assert the precondition directly with `pytest.raises(ConfigError)` on the strict build. No production change. |
| C-081 | `cli/tests/test_resource_edit.py::test_fallback_scan_tolerates_broken_sibling_files` | injected-value propagation | keep | `"broken.yaml" in str(exception)` names the file the test made unreadable, so the operator can find it. Value identity. |
| C-082 | `cli/tests/test_resource_edit.py::test_fallback_scan_tolerates_broken_sibling_files` | hint pin | delete | `"edit the file directly" in hint` is authored remediation with no structural counterpart. |
| C-083 | `cli/tests/test_resource_list.py::test_description_populated_for_operator_and_auto_resources` | authored display-prefix pin | delete | `ts.description.startswith("(auto) ")` pins a display convention on synthesized descriptions; `origin.variant == "auto-declared"` is the structural fact and is asserted elsewhere in the file. |
| C-085 | `cli/tests/test_resource_list.py::test_cli_empty_kind_csv_is_rejected` | allowed-list echo | delete | `"operator" in str(exception)` asserts the rejection enumerates valid origins; the typed `ValidationError` asserted beside it is the invariant. |
| C-086 | `cli/tests/test_resource_kinds.py::test_missing_ssh_keys_do_not_block_kind_listing` | table layout | keep | Column headers and per-row `split()` positions assert the kinds table's shape and alignment: the synthetic-fixture formatting carve-out. |
| C-087a | `cli/tests/test_resource_render.py::test_format_origin_line_handles_none,test_format_origin_line_operator_declared_with_file_and_line,test_format_origin_line_operator_declared_without_file_returns_bare_label,test_format_origin_line_auto_declared_with_source,test_format_origin_line_built_in_with_source,test_format_origin_line_system_plugin_with_source,test_format_origin_line_system_plugin_without_source_returns_bare_label,test_format_origin_line_system_plugin_without_plugin_degrades_gracefully` | formatter label equality | delete | **Ruled by the lead 2026-08-16** (formatter output splits on derivation): these pin the formatter's output as text (`== "unknown"`, `== "operator-declared"`, `.startswith("operator-declared (")`) with no canonical source behind the labels, so they are authored wording. The `Origin` variant contract itself keeps its own guard: `project_origin`'s variant checks were deleted and restored in PR #548 for exactly this reason. |
| C-087b | `cli/tests/test_resource_render.py::test_format_origin_line_operator_declared_with_file_and_line` | origin data projecting into the rendered cell | keep | The other half of the same ruling. `.endswith(":42)")` proves the origin's file path and line number reach the rendered cell rather than being dropped, which is derivation from a canonical source: the `Origin` record built at `:26`. Keep the projection, drop the label text around it. |
| C-088 | `cli/tests/test_list_truncation.py::_row_for,_assert_truncated_and_aligned,_seed_vm_rows,test_vm_list_truncates_long_name_and_stays_aligned,test_vm_list_names_only_emits_full_name,_seed_agent_rows,test_agent_list_truncates_long_name_and_stays_aligned,test_agent_list_names_only_emits_full_name,_seed_agent_with_grant,test_agent_list_shows_legend_when_an_implicit_grant_is_marked,test_agent_list_omits_legend_when_no_grant_is_marked,test_agent_list_legend_matches_visibility_when_marker_is_truncated_away` | column truncation and alignment | keep | Ellipsis placement, truncated prefix length, per-column caps, and `--names-only` emitting the untruncated name are layout behavior over a synthetic fixture. |
| C-089 | `cli/tests/test_output.py::test_count_pluralizes_regular_nouns,test_count_multiword_noun,test_count_irregular_plural,test_render_table_headers_and_rule_line,test_render_table_short_column_keeps_natural_width,test_render_table_caps_over_width_cell_with_ellipsis,test_render_table_exact_cap_cell_not_truncated,test_truncate_returns_fitting_text_unchanged,test_truncate_overflow_is_exactly_width_with_ellipsis,test_truncate_degenerate_widths_never_overflow,test_render_table_degenerate_max_col_width,test_render_table_over_cap_column_pads_short_cells_to_cap` | pluralization, truncation, table layout | keep | Pure formatting functions whose return value is the behavior, plus header, rule and column alignment; every string is the test's own input. |
| C-090 | `cli/tests/test_output.py::test_headerless_section_pushes_without_header,test_no_section_emits_at_column_zero,test_primitives_capture_role_and_ambient_level,test_detail_nests_via_headerless_section,test_style_status_delegates_to_current_handler,test_result_always_reports_level_zero,test_warn_from_thread_pool_worker_is_captured,test_warn_from_thread_pool_worker_is_captured.worker,test_default_handler_body_byte_identical_at_level_zero,test_default_handler_body_indents_with_level,test_default_handler_detail_matches_legacy_indent,test_default_handler_warn_byte_identical_at_level_zero,test_default_handler_warn_indents_with_level,test_default_handler_top_level_header,test_default_handler_header_decoration_by_level,test_default_handler_result_renders_at_column_zero,test_default_handler_prompt_byte_identical_at_level_zero,test_default_handler_prompt_indents_label_with_level,test_default_secret_prompt_abort_is_context_free_user_abort,test_default_secret_prompt_abort_is_context_free_user_abort.abort,test_default_handler_style_status_never_colorizes` | role, level and channel routing | keep | The assertions are about which role and indent level a line carries and which stream it lands on; the text is the test's own fixture. |
| C-091 | `cli/tests/test_typer_output.py::test_header_role_is_bold_on_a_tty,test_warning_role_prefix_is_yellow_on_a_tty,test_error_role_prefix_is_red_on_a_tty,test_result_role_is_dim_green_on_a_tty,test_detail_role_is_dim_on_a_tty,test_body_role_is_never_colored_on_a_tty,test_style_status_good_is_green_on_a_tty,test_style_status_warn_is_yellow_on_a_tty,test_style_status_bad_is_red_on_a_tty,test_style_status_neutral_is_unstyled_on_a_tty,test_style_status_returns_plain_text_off_tty,test_style_status_returns_plain_text_under_no_color,test_style_status_returns_plain_text_under_non_interactive,_assert_all_roles_byte_plain,test_no_color_env_forces_byte_plain_even_on_a_tty,test_non_tty_stream_is_byte_plain,test_non_interactive_forces_byte_plain_even_on_a_tty,test_confirm_resets_mouse_tracking_before_prompting_on_a_tty,test_confirm_emits_no_escape_when_stream_is_not_a_tty,test_confirm_emits_no_escape_under_non_interactive_even_on_a_tty` | ANSI styling and plain-text parity | keep | Each case pins the styled bytes against `click.style` and the same line with ANSI stripped, plus the mouse-disable escape emitted before a confirm prompt: formatting and terminal behavior over fixture text. |
| C-092 | `cli/tests/test_machine_output_cli.py::test_malformed_config_doctor_json_and_human_share_structured_facts` | authored message inside a JSON check | delete | `"invalid config file" in message` pins wording; `status == "fail"` on the same check is the invariant. |
| C-093 | `cli/tests/test_machine_output_cli.py::test_config_failure_json_writes_no_stdout_before_service_work` | message pin | delete | `"Configuration error: configuration is invalid" in captured.err` pins wording; the assertions around it (nothing on stdout, no ANSI in stderr) are the boundary. |
| C-094 | `cli/tests/test_operational_json_boundaries.py::_assert_json_envelope_only` | **[unverified]** chatter blacklist | delete | `b"Resolved " not in stdout` / `b"skipping " not in stdout` blacklist two human-path phrases someone already thought of; the envelope assertions in the same helper (single trailing newline, exactly one line, exactly the three envelope keys) already prove stdout is nothing but the document. Not executed: reasoned from the helper. |
| C-095 | `cli/tests/test_operational_json_boundaries.py::test_vm_describe_json_suppresses_the_ordinary_resolver_presentation,test_session_describe_uses_the_same_real_gate_chain_for_both_formats,test_session_list_status_and_late_repair_use_the_same_resolution_path` | human-path sentence pins used as controls | convert | Replace `human.stdout_bytes.startswith(b"Name:")` and the `b"VM 'box' is stopped. Starting..."` line with `human.stdout_bytes != b""`. Their role is to show the human path emits what the JSON path must not. No production change. |
| C-096 | `cli/tests/test_operational_json_boundaries.py::test_vm_describe_json_suppresses_the_ordinary_resolver_presentation,test_session_list_status_and_late_repair_use_the_same_resolution_path,test_session_json_suppresses_activation_output_around_the_envelope,test_machine_presentation_suppression_keeps_prompts_interactive_on_stderr` | sentinel redaction | keep | Platform secrets, boot ids and activation output injected by the test must not appear in the machine stream: security assertions wearing a blacklist's clothes. |
| C-109 | `cli/tests/test_env_show.py::test_secret_redacted_by_default` | placeholder equality | keep | The unrevealed rendering must be a placeholder naming the secret rather than its value; this is `env show`'s redaction boundary, and the equality is what proves no value leaked into the cell. |
| C-110 | `cli/tests/test_env_show.py::test_secret_reveal_rejects_multiline_before_rendering,test_secret_reveal_guard_rejects_nul_before_building_render_input` | verbatim error text in a value and a dict | convert | Replace with the structural pair: the failing key set (`set(errors) == {"shared-token"}`) and the resolved value absent from `rendered_value`. Keeps "which secrets failed" and the no-leak boundary; drops the sentence. No production change. |
| C-111 | `cli/tests/test_env_show_flag.py::test_help_shows_resolve` | help-text flag pin plus retired-spelling blacklist | delete | `"--resolve" in plain` / `"--reveal-secrets" not in plain` police rendered help for a flag whose behavior is asserted at 54-70 through the captured call. |
| C-114 | `cli/tests/test_error_wrapper.py::test_record_unhandled_error_writes_traceback_with_context` | separator-form pin | delete | `"=" * 40 in text` pins the log file's decorative rule; the diagnostic content assertions around it are the invariant. |
| C-115 | `cli/tests/test_error_wrapper.py::test_record_unhandled_error_writes_traceback_with_context,test_main_wrapper_catches_unhandled_exception` | diagnostic capture | keep | The crash log must carry the argv, the exception line and a traceback: a real diagnostic contract with no other guard, and "Traceback" is Python's own output. |
| C-116 | `cli/tests/test_error_wrapper.py::test_main_wrapper_lets_vendored_abort_through,test_main_wrapper_maps_escaped_abort_to_exit_1` | message pin | delete | `"Aborted." in err` pins our abort wording; `"Traceback" not in err` (Python's own text) and the absence of an error log are the invariants. |
| C-117 | `cli/tests/test_error_wrapper.py::test_main_wrapper_renders_click_usage_error_cleanly` | our own label restated | delete | `"Error:" in err` restates the prefix already pinned as formatting at 389 and 409. |
| C-118 | `cli/tests/test_error_wrapper.py::test_main_wrapper_renders_click_usage_error_cleanly` | upstream error text | keep | `"'bogus' is not one of 'comment', 'delete'." in err` is Click's own message reaching the operator without a traceback: the rule's outside-the-repository exception. |
| C-119 | `cli/tests/test_error_wrapper.py::test_domain_error_renders_red_error_prefix_on_a_tty,test_domain_error_renders_plain_off_tty` | styled-output equality | keep | The `Error:` prefix styled red on a TTY and plain off it, carrying the message the test injected: formatting plus value identity. |
| C-120 | `cli/tests/test_error_wrapper.py::test_create_session_surfaces_dead_workload_output_through_rollback` | message pin | convert | Replace `"exited immediately after launch (status 2)" in msg` with `"status 2" in msg` (the injected exit status). No production change. |
| C-121 | `cli/tests/test_error_wrapper.py::test_create_session_surfaces_dead_workload_output_through_rollback` | command-effect absence | keep | That no `server-access` or `chmod g+rwx` command ran during rollback asserts what the code did, not what it said. |
| C-122 | `cli/tests/test_operator_path_rendering.py::_assert_home_relative,test_doctor_spells_every_path_on_one_screen_the_same_way` | path-leak defense with a witness | keep | The file's whole invariant: no absolute `$HOME` path reaches operator-facing text, and the surface still renders a path so the guard has a subject. A leak defense over data, and the bug it caught was real. |
| C-123 | `cli/tests/test_operator_path_rendering.py::test_doctor_names_a_missing_config_file_home_relative,test_doctor_names_an_unreadable_ssh_key_home_relative` | branch-witness sentences | convert | Replace `"not found" in rows["Config file"]` and `"not readable" in rows["SSH public key"]` with the check's status: `doctor.py:524-531` fails the missing-config row and oks the present one, and `doctor.py:645-649` fails an existing-but-unreadable key, so status plus the file's existence pins the branch. Keeping the check objects instead of flattening to messages is test-side only. |
| C-124 | `cli/tests/test_operator_path_rendering.py::test_the_pre_cli_config_load_failures_name_the_file_home_relative,test_config_edit_names_the_missing_file_home_relative` | branch-witness sentences | delete | Each branch is fixed by the scenario the test sets up (config unlinked, invalid TOML written, `config edit` on a missing file), and each output is already asserted to render a `~/`-spelled path; the sentences add only wording. |
| C-125 | `cli/tests/test_operator_path_rendering.py::test_a_config_error_names_the_setting_the_way_the_operator_wrote_it` | setting name plus sentence | convert | Replace with `"operator.ssh_public_key" in message`: naming the setting the way the operator wrote it is the invariant the test's own name states. No production change. |
| C-126 | `cli/tests/test_name_filter_validation.py::test_unknown_vm_error_shape,test_repeated_unknown_name_reported_once` | error-sentence and hint pins | delete | `entity_kind`/`entity_name` are asserted on the same exception (59-60), so the sentence adds nothing, and `"agw vm list" in hint` is authored remediation. |
| C-127 | `cli/tests/test_name_filter_validation.py::test_multiple_unknown_names_all_reported,test_repeated_unknown_name_reported_once` | value identity and non-duplication | keep | That the message names every unknown filter value, does not name the valid one, and names each unknown exactly once are data-level properties of the aggregation. |
| C-128 | `cli/tests/test_name_filter_validation.py::test_multiple_unknown_names_all_reported,test_repeated_unknown_name_reported_once` | singular-versus-plural wording | delete | `"unknown workspaces" in message` and `"unknown VMs" not in message` police pluralization: a sentence-only observable (R2.4). |
| C-129 | `cli/tests/test_name_filter_validation.py::test_session_list_valid_filter_empty_result_succeeds,test_stop_all_sessions_valid_filter_empty_result_succeeds,test_resume_all_sessions_valid_filter_empty_result_succeeds,test_list_commands_valid_filter_empty_result_succeeds,test_agent_filter_valid_for_agent_with_no_sessions` | empty-state message pins | convert | Rubric re-check, 2026-08-16. **The row's justification was inaccurate**: five of these six sites are the only assertion in their test, so the delete leaves assertion-free tests. Convert each to a channel assertion on the captured output. No production change. |
| C-136 | `cli/tests/test_ssh_config.py::test_sync_ssh_config_announce_controls_the_synced_line` | message pin | convert | Rubric re-check, 2026-08-16. `:145` is the only witness of the announcing arm; the test exists to prove `announce` CONTROLS the line, and `:152` carries only the silent arm. Capture the channel before and assert it grew. No production change. |
| C-139 | `cli/tests/test_ssh_input.py::test_ssh_run_translates_sensitive_native_failure_without_exception_link,test_ssh_run_sensitive_timeout_drops_partial_output_and_native_exception` | verbatim error equality | convert | Replace with the exception type plus the injected command substring (`"cat > /tmp/template.yaml" in str(caught.value)`); the sentence around it is ours. No production change. |
| C-140 | `cli/tests/test_ssh_logger.py::test_close_appends_traceback_when_called_inside_except,test_close_without_exception_omits_traceback_block,test_ssh_logger_structurally_satisfies_bootstrap_progress,test_path_retargeting_moves_subsequent_records` | log-structure markers | keep | `EXCEPTION:` and `# Finished:` are the log file's own structure: their absence means a truncated or silently swallowed run, which is what the log exists to reveal. |
| C-141 | `cli/tests/test_ssh_logger.py::test_write_sink_sanitizes_every_persistent_surface,test_warning_text_is_sanitized_before_durable_logging,test_redactions_cannot_be_registered_after_writes,test_shell_quoted_secret_is_redacted_from_command,test_close_redacts_secrets_from_traceback` | redaction defense | keep | Secret values may appear only as `[REDACTED]` in the durable log, including on the retry and warning paths, plus the occurrence threshold. Security, not prose. |
| C-143 | `cli/tests/test_admin_home_permissions.py::test_harden_admin_home_shared_primary_group_warns,test_harden_admin_home_private_primary_group_is_quiet,test_harden_admin_home_ssh_error_warns_not_raises` | warning wording pins | convert | Rubric re-check, 2026-08-16. Verified by execution on a scratch copy: the shared-group and SSH-error arms emit exactly one warning each and the healthy arm none, so the warn-versus-quiet discrimination, which is security-relevant (a 0750 home readable by a shared primary group), converts exactly to a warning count. No production change. |
| C-147 | `cli/tests/test_git_token_verification.py::test_github_200_verifies_with_login_and_expiry,test_expiry_header_format_drift_tolerated,test_azdo_200_verifies,test_runup_and_filter_keeps_verified` | authored prefix around fake-supplied data | convert | Keep the values the fake API returned (the login, the expiry date, the credential name) and drop the "Verified git token for" prefix and the `login`/`expires` labels, asserting the values propagate. The `"expires" not in out` drift case goes with the prefix (R2.4). No production change. |
| C-148 | `cli/tests/test_git_token_verification.py::test_github_401_is_definitive_rejection` | hint pin | delete | `"runup_git_credentials = false" in hint` pins authored remediation; the typed error and the names in it (87-88) are the invariant. |
| C-149 | `cli/tests/test_git_token_verification.py::test_github_401_is_definitive_rejection` | injected-value propagation | keep | The credential and secret names the test declared must reach the error so the operator knows which token failed. |
| C-150 | `cli/tests/test_git_token_verification.py::test_github_other_status_warns_and_continues,test_network_failure_warns_and_continues,test_runup_and_filter_skips_rejected` | message pins | convert | Rubric re-check, 2026-08-16. `:98` and `:108` are the only assertions in their tests besides the implicit no-raise, so they are the sole probe that a 503 or a network failure WARNS rather than passing silently. Convert to a channel assertion. No production change. |
| C-152 | `cli/tests/test_initializer_reconcile.py::test_apply_sve_mask_installs_dropin_as_root_and_warns_restart` | warning sentence pin | delete | `"Restart the VM and reinit" in warned` pins remediation prose; line 143 already asserts the warning names the kernel flag that changed. |
| C-153 | `cli/tests/test_initializer_reconcile.py::test_apply_sve_mask_warns_and_stops_on_update_grub_failure` | warning sentence pin | delete | **Recipe verification, 2026-08-16: the recorded conversion is vacuous, so the row becomes a delete.** Executed both branches of `test_initializer_reconcile.py`: the SVE-mask failure branch emits one warning and the SUCCESS branch also emits one warning, so `warnings != []` and `len(warnings) == 1` both pass when the behavior regresses. The discriminator that actually works is already in the test at `:159` (`not any("/proc/cmdline" in ...)`), which is `False` in the failure branch and `True` in the success branch, and `:156` (`logger.warning.assert_called_once()`) stays with it. Nothing is lost by deleting `:157`. This is the row that produced the screening question now recorded with the recipes: does the HEALTHY branch emit zero warnings? |
| C-155 | `cli/tests/test_initializer_env_fragments.py::test_sshd_accept_env_restores_prior_file_on_validation_failure,test_sudoers_env_keep_rejects_on_visudo_failure` | warning keyword pins | convert | Replace `any("reinit" in w.lower() ...)` and `any("visudo" in w ...)` with `logger.warnings != []`. Both tests already assert the structural outcome (the backup moved, the staged sudoers file not installed); what remains is that the operator is told. No production change. |
| C-156 | `cli/tests/test_initializer_hardening.py::test_vm_initialization_step_and_subresult_roles` | progress-line pins | convert | Rubric re-check, 2026-08-16. These tuples are not sentence pins so much as the only probe of output ROLE and nesting LEVEL (`Role.BODY` at level 1 for steps, `Role.DETAIL` for the sub-result), which is the bug the comment at `:218` names. Assert the role-and-level sequence from `captured_output.lines`. No production change. |
| C-159 | `cli/tests/test_tailscale_dns.py::test_check_warns_when_dns_fails_and_resolv_unreadable,test_check_warns_when_dns_fails_but_resolv_not_tailscaled` | keyword-in-warning | delete | `"DNS" in warning_text` asserts one word appears; the structural facts (which probe commands ran and which did not) are asserted beside it. |
| C-160 | `cli/tests/test_tailscale_dns.py::test_check_warns_when_resolved_not_active` | disjunction of authored phrasings | delete | `"latched" in w or "issue #117" in w` and `"not implemented" in w or "No heal" in w` are a phrase blacklist's mirror image: two spellings someone already thought of, with a third sailing through. |
| C-161 | `cli/tests/test_tailscale_dns.py::test_check_raises_state_error_with_heal_hint` | hint command pins | keep | Rubric re-check, 2026-08-16. Scoped to `:366`. The three heal commands at `:358-360` still delete, their only failure mode being a hint edit. `:366` keeps: the comment at `:361-366` records a real prior regression, a simple-block hint that locked the operator out of the very Tailscale-SSH session running the first heal command, and nothing else probes it. |
| C-163 | `cli/tests/test_vm_cli_surface.py::test_vm_create_template_override_flags_removed,test_vm_create_platform_flag_removed,test_vm_shell_provisioner_alias_is_removed,test_vm_shell_help_only_lists_platform` | help-text flag pins and retired-flag blacklist | delete | `--platform` and `--provisioner` presence and absence in rendered help police the help surface; the flags' behavior is asserted at 46-119 through the captured call. |
| C-164 | `cli/tests/test_vm_cli_surface.py::test_doctor_vm_sites_defers_on_pending_migration,test_doctor_system_group,test_doctor_vm_sites_not_ready_and_preflight_rows` | doctor message wording | delete | "pending database migration", "will ask", "declined" and "preflight" are authored row text; the check's status discriminates each branch. |
| C-165 | `cli/tests/test_vm_cli_surface.py::test_doctor_system_group,test_doctor_vm_sites_group,test_doctor_vm_sites_not_ready_and_preflight_rows` | data projections into a row | keep | Derivation from a canonical source, which the lead's 2026-08-16 formatter ruling keeps and asks to be named: the source is the finalized registry plus the platform's own readiness verdict, and each assertion proves that value (the injected site, the platform, the placement, the missing tool) reaches the row an operator acts on. The reason text is produced by the platform, not authored here. |
| C-166 | `cli/tests/test_vm_cli_surface.py::test_doctor_vm_sites_group` | hint form pin | delete | `"name: gone-box" in hint` pins a YAML fragment's spelling inside a hint; line 263 already asserts the stranded VM is named. |
| C-167 | `cli/tests/test_vm_cli_surface.py::test_doctor_warns_on_references_to_not_ready_sites` | reason propagation | keep | Same ruling, same canonical source: one readiness reason produced by the platform must reach both the VM row and the default-site row. The test proves the derived copies agree with it, which is propagation between surfaces rather than a fixed sentence. |
| C-168 | `cli/tests/test_vm_shell_platform.py::test_shell_vm_raises_when_no_tailscale_host_and_no_platform_flag,test_native_transport_wraps_missing_native_transport,test_native_transport_proxmox_hint_points_at_web_console,test_native_transport_raises_defensively_on_empty_host,test_shell_vm_warns_but_continues_on_failed_init,test_shell_vm_still_raises_on_failed_provisioning,test_exec_vm_warns_but_continues_on_failed_init` | error and hint sentence pins | convert | Rubric re-check, 2026-08-16. The only evidence the warn path fired: a silent `allow_failed_init` softening would pass, and no other site in `cli/tests` touches it. Convert to a warning-count assertion. No production change. |
| C-169 | `cli/tests/test_remote_exec.py::test_stale_status_cleared_when_reuse_disabled` | warning sentence pin | convert | Rubric re-check, 2026-08-16. `:207` proves a fresh run started but nothing witnesses that the stale state was REPORTED, and the docstring's contract is both. Assert the warning channel is non-empty. No production change. |
| C-170 | `cli/tests/test_remote_exec.py::test_run_detached_fresh_start,test_polling_survives_transient_ssh_failures,test_run_detached_timeout_kills_and_returns_partial` | injected-output propagation | keep | The strings are what the fake target emitted, so asserting they reach `result.output` proves the streaming path carries remote output, including after recovery and on partial failure. |
| C-171 | `cli/tests/test_workspace_rooted_shells.py::test_exec_vm_rejects_dash_prefixed_command,test_exec_agent_rejects_dash_prefixed_command` | hint wording pins | delete | Five authored fragments telling the operator where to put `--`; the exit code and the unparsed argv assertions carry the behavior. |
| C-173 | `cli/tests/test_azure_create_interrupt.py::TestInterruptDuringInlineWait.test_vm_delete_failure_during_rollback_warns_and_reraises` | injected-value propagation | keep | The VM name, resource group and the injected `vm delete boom` failure text must reach the may-remain report: without them an operator cannot finish the cleanup by hand. |
| C-176 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_rbac_denial_raises_authorization_error,TestSurvivingVMRaises.test_denied_probe_after_rbac_delete_failure_raises_authorization_error,TestSurvivingVMRaises.test_denied_probe_alone_raises_authorization_error` | authored preamble and success-line pins | delete | **Deduplicated 2026-08-16.** Two inventory lanes both covered this file, a scoping error in the fan-out rather than a disagreement; the platform lane's rows are kept because they are finer-grained and this file was its assignment. Narrowed to the three lines the platform lane did not row: E-166, E-167, E-168, E-169 and E-170 now own 125, 134, 197, 216, 228, 251, 286 and 298. These three are fixed preamble text whose only failure mode is a rewording. |
| C-179 | `cli/tests/test_azure_nsg_exposure.py::TestCreate.test_create_fails_typed_when_detection_fails_and_no_extras,TestPrefixAssembly.test_detection_failure_without_extras_is_typed_error` | authored narration pins | delete | **Deduplicated 2026-08-16.** Two inventory lanes both covered this file, a scoping error in the fan-out rather than a disagreement; the platform lane's rows are kept because they are finer-grained and this file was its assignment. Narrowed: line 231 moves to E-172, which the re-check flips to convert. 207 and 237 are fixed narration with no other failure mode. |
| D-043 | `cli/tests/manifests/test_samples.py::test_capability_kinds_report_no_sample` | two authored-phrase pins inside a real test | delete | **Corrected 2026-08-19 (map re-baseline).** Range narrowed from `:90-99`, which swept up every line the justification keeps. Drop `:95` (`"capability kind"`) and `:96` (`"no sample manifest"`). Keep `:94` (`kind in str(err)`, value identity on the looped kind), `:97` and `:98-99` (every declarable kind's name appears in the hint, derived from `declarable_kinds()`), which are the invariants that can actually regress. |
| D-047 | `cli/tests/manifests/test_samples.py::test_writing_into_a_file_that_exists_and_is_blank_emits_no_separator` | full-sentence pin of a CLI error line | delete | The test's contract is exit code 1, no traceback escaping, and no `error.log` written; those are asserted at :426, :429-430 and :432 and all survive. The sentence adds nothing the diff did not show. |
| D-066 | `cli/tests/manifests/test_describe_kind.py::test_secret_source_kind_describes_the_backend_union_and_override_provenance,test_a_kind_points_at_the_sample_that_renders_the_same_fields` | pointer and title pins | convert | The real invariant is that a kind's page points at the sample command for THAT kind; assert the pointer is built from the target (`f"agw resource sample {kind}"` derived from the loop variable) rather than spelled per kind. |
| D-081 | `cli/tests/manifests/test_describe_kind.py::test_unknown_kind_hint_enumerates_every_kind_in_sorted_order` | hint enumeration, derived | keep | Builds the expected kind list from `KIND_REGISTRY` and asserts the hint enumerates exactly it in sorted order. Derivation parity against a canonical source; a kind added to the registry and missing from the hint fails. |
| D-082 | `cli/tests/manifests/test_describe_kind.py::test_describe_kind_is_a_clean_cli_error_for_an_unknown_target` | CLI contract plus one sentence pin | delete | Drop :468 (`"unknown kind 'vm-platfrom'"`). Keep :466 (exit code 1) and :469 (`"Traceback" not in err`, which is the interpreter's own output and the thing separating a domain error from an unexpected one). |
| D-092 | `cli/tests/manifests/test_schema_command.py::test_write_tolerates_missing_ssh_keys,test_write_with_a_kind_is_a_clean_refusal,test_a_bad_kind_is_a_clean_domain_error` | parametrized phrase pins plus a wording blacklist | convert | **Recipe verification, 2026-08-16.** **Vacuous as recorded, salvageable test-side.** `_errors.py:41` writes to `CONFIG_DIR / "logs" / "error.log"`, and the test never points `CONFIG_DIR` at `tmp_path`, so the asserted path can never exist: making `_require_emittable` raise left the test green. Corrected form, verified in both halves: take the `tmp_path` fixture and `monkeypatch.setattr("agentworks.config.CONFIG_DIR", tmp_path)` before asserting the file. No production change. |
| D-096 | `cli/tests/manifests/test_contributed_validation.py::test_the_shared_diagnostic_is_the_good_one` | phrase pins on a shared diagnostic | keep | `"key_dearmor"` is the field the test misspells and `error.hint` carrying the sample pointer is the same derived-pointer invariant as D-066. The load-bearing claim (a contributed bundle gets the SAME diagnostic an operator gets) is asserted structurally at :92 (`contributed.hint == operator.hint`), which is derivation parity and the strongest thing here. |
| D-103 | `cli/tests/manifests/test_spec_apt.py::test_every_rejection_points_at_the_sample_surface` | hint equality | convert | The pointer discipline is real; derive it (`f"'agw resource sample {kind}' prints this kind's fields"` from the kind under test, or better, assert every kind's rejection hint names that kind's own sample command) instead of spelling one kind's sentence. |
| E-005 | `cli/tests/resources/test_inherited_reference_attribution.py::test_a_miss_on_an_inherited_name_blames_the_template_that_wrote_it` | miss-message substring carrying injected identities | keep | Invariant: an inherited-name miss is blamed on the template that WROTE it (`base`), not the one that inherited it; `base`/`nope` are test-injected. Narrow the assertion to those identities and drop the surrounding authored sentence frame. |
| E-008 | `cli/tests/resources/test_inherited_reference_attribution.py::test_reference_formatter_says_an_inherited_reference_was_inherited` | rendered-line equality | convert | Replace with assertions on the `ReferenceEntry` fields the renderer reads (`entry.source`, `entry.declared_by`; see cli/agentworks/resources/render.py:39-42): the inherited edge carries `declared_by == ("vm-template", "base")` and the declarer's own edge carries none. No production change. |
| E-009 | `cli/tests/resources/test_inherited_reference_attribution.py::test_a_row_that_declared_the_name_itself_is_blamed_for_it` | miss-message substring carrying injected identities | keep | The override case: `kid` declared the name itself and must be blamed. Same narrowing as E-005. |
| E-015 | `cli/tests/resources/test_registry_collisions.py::test_operator_over_operator_errors_with_both_locations` | authored phrase substring | delete | `"duplicate secret"` is our own error wording; `pytest.raises(ConfigError)` plus the two location assertions below already carry the collision claim. |
| E-016 | `cli/tests/resources/test_registry_collisions.py::test_operator_over_operator_errors_with_both_locations` | injected file:line coordinates in a message | keep | Invariant: an operator-over-operator collision names BOTH origins with their real file and line. The coordinates come from this test's own `_operator(1)` / `_operator(9)` origins, so this is value identity, and losing it would let a collision report one location. |
| E-017 | `cli/tests/resources/test_registry_lifecycle.py::test_error_miss_policy_includes_reference_usage` | miss-message substrings carrying injected values | keep | The kind, the missing name, and the reference usage are all defined by this test's `_RefEmitter`; the invariant is that a finalize miss names the unknown target AND the usage that asked for it. |
| E-019 | `cli/tests/plugins/test_aws.py::test_site_on_disabled_aws_is_not_ready_with_hint,test_resolve_site_refuses_disabled_aws_with_hint` | enable-hint substring | delete | Both sit beside the structural claim (`not is_ready(...)` / `pytest.raises(StateError)`); `"enable plugin 'aws'"` is authored remediation wording. |
| E-020 | `cli/tests/plugins/test_aws.py::test_doctor_roster_lists_the_aws_plugin` | doctor-message substring | delete | `row.status is Status.INFO` / `Status.OK` on the adjacent lines is the enablement claim; the message text adds only wording. |
| E-022 | `cli/tests/plugins/test_azure.py::test_site_on_disabled_azure_is_not_ready_with_hint,test_resolve_site_refuses_disabled_azure_with_hint,test_credential_on_disabled_azdo_is_not_ready_with_hint,test_recipe_gate_refuses_disabled_az_cli_with_hint` | enable-hint substring (four one-line tests, one idiom) | delete | Each sits beside `not is_ready(...)` or `pytest.raises(StateError)`; the hint sentence is authored remediation. |
| E-023 | `cli/tests/plugins/test_azure.py::test_resolve_git_credential_refuses_disabled_azdo` | authored phrase substring | delete | `"not ready" in str(exc.value)` is our own wording; the `StateError` type already discriminates the refusal. |
| E-025 | `cli/tests/plugins/test_azure.py::test_doctor_roster_lists_the_azure_plugin` | doctor-message substring | delete | Same as E-020; `Status.INFO` / `Status.OK` carry it. |
| E-027 | `cli/tests/plugins/test_claude.py::test_ensure_harness_integration_enabled_refuses_disabled_claude_code_with_hint,test_recipe_gate_refuses_disabled_claude_install_with_hint` | enable-hint substring | delete | Structural siblings are the raised `StateError` and the disabled enablement mark. |
| E-029 | `cli/tests/plugins/test_claude.py::test_doctor_roster_lists_the_claude_plugin` | doctor-message substring | delete | Status assertions carry it. |
| E-031 | `cli/tests/plugins/test_codex.py::test_ensure_harness_integration_enabled_refuses_disabled_codex_with_hint,test_recipe_gate_refuses_disabled_codex_install_with_hint` | enable-hint substring | delete | Same as E-027. |
| E-033 | `cli/tests/plugins/test_codex.py::test_doctor_roster_lists_the_codex_plugin` | doctor-message substring | delete | Status assertions carry it. |
| E-035 | `cli/tests/plugins/test_enablement_producer.py::test_mapping_to_an_absent_backend_reports_the_dangling_edge_not_a_shape_error` | wording blacklist | convert | Rubric re-check, 2026-08-16. A structural handle the row assumed away: the shape error chains a pydantic `ValidationError` while the dangling-edge raise chains nothing, so assert `exc.value.__cause__ is None`. No production change. |
| E-039 | `cli/tests/plugins/test_gcp.py::test_disabled_gcp_site_is_not_ready_and_refused_with_enable_hint,test_doctor_roster_tracks_gcp_enablement` | enable-hint and doctor-message substrings | delete | Same as E-019/E-020 for gcp. |
| E-040 | `cli/tests/plugins/test_manifest_parity.py::test_ensure_reference_enabled_refuses_disabled_row_with_enable_hint,test_ensure_recipe_enabled_refuses_disabled_contribution_in_closure` | enable-hint substring | keep | **Corrected 2026-08-19 (map re-baseline).** Was delete, and it contradicted G1-K03, which keeps the identical expression at `:195` of this same file. Resolved toward the keep: `f"enable plugin`{{PLUGIN}}`"` interpolates the fixture constant naming the disabled plugin, so all three sites prove the refusal names the plugin the operator must enable rather than a fixed sentence. The row also already kept the neighboring fixed literals at `:128`, `:129` and `:141` (contribution kind and name, value identity on the call's own arguments), so keeping `:130` and `:142` is what makes the row internally consistent as well. |
| E-041 | `cli/tests/plugins/test_proxmox.py::test_site_on_disabled_proxmox_is_not_ready_with_hint,test_resolve_site_refuses_disabled_proxmox_with_hint,test_doctor_roster_lists_the_proxmox_plugin` | enable-hint and doctor-message substrings | delete | Same idiom as E-019/E-020 for proxmox. |
| E-044 | `cli/tests/plugins/test_publish.py::test_bad_plugin_manifest_anchor_raises_typed_plugin_attributed_error,test_plugin_manifest_anchor_without_subdir_raises_typed_plugin_attributed_error` | authored phrase substrings | convert | Rubric re-check, 2026-08-16. **The row's 'R2.2 forbids adding a discriminator' premise was wrong**: the handle already exists. `manifests/package.py:51` raises `from exc` and `:52-56` does not, so `isinstance(__cause__, ImportError)` versus `__cause__ is None` discriminates. No production change. |
| E-045 | `cli/tests/plugins/test_publish.py::test_plugin_manifest_anchor_without_subdir_raises_typed_plugin_attributed_error` | wording blacklist | delete | `"no-op" not in message` tries to enforce "loud, not silent" by forbidding one word. The loudness is already proven by `pytest.raises(ConfigError)`. |
| E-047 | `cli/tests/plugins/test_surfaces.py::test_list_renders_from_plugin_provenance` | rendered-cell substring carrying an injected plugin name | keep | Invariant: the rendered DESCRIPTION cell attributes a plugin row to its plugin, which is the operator's only provenance signal in `resource list`. `alpha` is the fixture's name. |
| E-048 | `cli/tests/plugins/test_surfaces.py::test_include_disabled_render_marks_disabled_and_not_ready_distinctly` | rendered state-marker presence and absence | keep | Invariant: disabled, not-ready-but-enabled, and ready rows render three distinct signals per row rather than only in the count line. This is synthetic-fixture rendering (the absorbed FRD's R5 carve-out); the marker text is authored, and a deliberate rename is the accepted cost. |
| E-049 | `cli/tests/plugins/test_surfaces.py::test_full_fixture_end_to_end,test_roster_lists_enabled_ok_and_disabled_info,test_default_config_builds_green_with_zero_enabled_system_plugins` | doctor-message substrings | delete | Each sits beside the `Status` assertion that carries the enablement claim. |
| E-050 | `cli/tests/plugins/test_surfaces.py::test_roster_lists_enabled_ok_and_disabled_info,test_roster_empty_index_renders_present_but_empty` | authored group-title equality | delete | `group.name == "System plugins"` pins a heading; `_check_plugins` returns exactly one group, so nothing selects on it. |
| E-052 | `cli/tests/plugins/test_surfaces.py::test_roster_empty_index_renders_present_but_empty` | authored empty-state sentence equality | delete | Keep the structural presence (one check in the group); the sentence itself is prose. |
| E-054 | `cli/tests/plugins/test_surfaces.py::test_roster_required_scopes_inert_when_empty` | phrase blacklist over check names | convert | The claim is that no scope row is emitted for a plugin with no reserved scopes. Replace with a count/identity assertion over `checks` (the bare plugin yields exactly its roster row), which does not depend on the row's title. No production change. |
| E-056 | `cli/tests/plugins/test_proxmox_bootstrap_staging.py::test_write_failure_is_safe_and_removes_private_stage,test_standalone_removal_failure_surfaces_and_models_residue` | authored error-message equality | delete | The typed raise plus the residue assertions carry both cases; the sentence is our own wording. |
| E-057 | `cli/tests/plugins/test_proxmox_bootstrap_staging.py::test_removal_failure_does_not_mask_primary,test_removal_control_flow_does_not_mask_primary` | verbatim warning list equality | convert | The real invariant is "warn exactly once and never mask the primary". Replace the list-of-sentences equality with `len(warnings) == 1`, keeping the existing `caught.value is failure` and the secret-absence check. No production change. |
| E-058 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestInterruptDuringBootstrapWait.test_stops_and_deletes_the_cloned_vm_and_reraises` | warning-phrase presence/absence | delete | The path is already pinned structurally in the same test: `("stop_vm", ...)` and `("delete_vm", ...)` in `fake.calls`, the stop-before-delete index check, and `"stop_task" not in fake.ops()`. |
| E-059 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestInterruptDuringBootstrapWait.test_second_interrupt_abandons_cleanup_loudly` | phrase-selected warning plus injected coordinates | delete | **Was a convert; executing the test flipped it.** `test_second_interrupt_abandons_cleanup_loudly` emits TWO warnings, not one, so a count selector picks the wrong one, and both warnings carry `Proxmox VM 100 on node 'pve1'`, so the injected coordinates do not select either. The only discriminator is our own `"Cleanup abandoned:"` versus `"Interrupted: cleaning up..."`, which R2.4 resolves by deletion. The abandon behavior survives structurally: `exc.value is interrupt`, `"delete_vm" not in fake.ops()`, `"stop_task" not in fake.ops()`. |
| E-060 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestFailClosedBootstrap.test_cloud_init_timeout_rolls_back` | authored error-message equality | delete | `pytest.raises(ProvisioningError, match=...)` is group 1's; the full-sentence equality here restates our own timeout wording, and the rollback behavior is asserted structurally below. |
| E-061 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestFailClosedBootstrap.test_bootstrap_failure_cleanup_survivor_keeps_primary_and_manual_guidance` | phrase-selected warning plus injected coordinates | convert | Same treatment as E-059: keep the `VM {_NEWID}` / `node 'pve1'` identity, drop the `"delete it there manually"` remediation sentence, and select the warning by count. |
| E-062 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestEscalationFromFailureRollback.test_interrupt_during_the_failure_arms_rollback_escalates` | warning-phrase presence | delete | The escalation is proven by the raised interrupt identity and the recorded API calls. |
| E-063 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestOrphanBackstopWarning.test_interrupt_path_warns_when_the_vm_survives_the_delete,TestOrphanBackstopWarning.test_failure_path_warns_when_the_vm_survives_the_delete,TestTransportFailureDuringRollback.test_interrupt_path_absorbs_it_with_the_abandon_warning,TestTransportFailureDuringRollback.test_interrupt_path_probe_blip_still_warns_via_containment` | phrase-selected warning (`may remain` / `Cleanup abandoned` / `Rollback incomplete`) | convert | Rubric re-check, 2026-08-16. **The row's stated sibling does not exist in these four tests**: with the warning legs gone they assert nothing but `exc.value is ...`, and the file's own comment at `:576-582` records that a swallowed containment produces zero warnings. Convert to warning counts, verified against the real counts (proxmox abandon emits 2). No production change. |
| E-064 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestTransportFailureDuringRollback.test_failure_path_warns_and_reraises_the_original_error,TestTransportFailureDuringRollback.test_failure_path_probe_blip_still_warns_via_containment` | compound warning-phrase predicate | convert | Rubric re-check, 2026-08-16. Same as E-063 for the remaining two tests. No production change. |
| E-065 | `cli/tests/plugins/test_proxmox_create_interrupt.py::TestPlainFailure.test_failure_mid_create_cleans_up_and_reraises_the_typed_error` | detail/warning phrase presence and absence | delete | The plain-failure path is fully pinned structurally in the same test (`("delete_vm", ...) in fake.calls`, `"stop_vm" not in fake.ops()`, template untouched). |
| E-067 | `cli/tests/plugins/gcp/test_auth.py::test_service_account_remediation_accepts_downloaded_json_without_compaction` | hint required-phrase pair | delete | Authored remediation wording; the `ProvisioningError` raise carries the refusal, and E-066 carries the behavior the wording talks about. |
| E-068 | `cli/tests/plugins/gcp/test_compute.py::test_known_live_machine_incompatibility_is_actionable_detached_config_error` | hint required-phrase list | delete | Four sentences of authored guidance beside a `ConfigError` raise; the discriminating value (`caller-authored-type`, line 295) is injected and stays. |
| E-069 | `cli/tests/plugins/gcp/test_compute.py::test_exact_instance_collision_and_not_found_paths` | required-phrase pair plus wording blacklist | delete | `"Do not delete the instance by name"` is authored safety prose and `"remove" not in hint.lower()` is a blacklist that any rephrasing walks through. `AlreadyExistsError` is the discriminator that survives. |
| E-070 | `cli/tests/plugins/gcp/test_errors.py::test_done_http_503_capacity_is_definitive_typed_safe_and_uses_no_refresh` | hint equality | delete | `GCECapacityError` already discriminates the branch and `_ZONE in str(caught.value)` (line 131) carries the zone-naming claim. |
| E-071 | `cli/tests/plugins/gcp/test_errors.py::test_global_operation_capacity_guidance_does_not_claim_the_vm_zone` | hint equality | delete | The real invariant in this test is the line above it (`_ZONE not in f"{caught.value} {caught.value.hint}"`, no false zone attribution for a global operation), which stays. |
| E-072 | `cli/tests/plugins/gcp/test_errors.py::test_non_done_timeout_is_the_only_indeterminate_operation_outcome,test_non_done_provider_poll_failure_is_indeterminate_and_safe` | hint substring | delete | `GCEIndeterminateOperationError` is the discriminator; the "inspect the named resource" sentence is authored. |
| E-074 | `cli/tests/plugins/gcp/test_instance.py::test_done_capacity_failure_never_reconciles_matching_instance_to_success` | hint substring | delete | `GCECapacityError` plus `[name for name, _ in client.calls] == ["insert"]` carry the branch and the no-retry claim. |
| E-075 | `cli/tests/plugins/gcp/test_instance.py::test_residual_definitive_insert_rejection_has_fixed_machine_disk_guidance` | hint required-phrase list | delete | Authored guidance beside `type(caught.value) is GCEOperationError` and the injected `caller-authored-type`, both of which stay. |
| E-076 | `cli/tests/plugins/gcp/test_instance.py::test_residual_definitive_insert_rejection_has_fixed_machine_disk_guidance` | sentence-order pin via `.index()` | delete | This pins that one authored sentence precedes another inside a hint. hla.md deletes lexical order pins outright; there is no behavior behind it. |
| E-077 | `cli/tests/plugins/gcp/test_network.py::test_firewall_name_collision_and_absence` | required-phrase pair plus wording blacklist | delete | The firewall twin of E-069, same reasoning. |
| E-078 | `cli/tests/plugins/gcp/test_network.py::test_done_capacity_firewall_failure_does_not_attribute_the_vm_zone` | hint equality | delete | The zone-attribution invariant is the line above (`"us-central1-a" not in f"{caught.value} {caught.value.hint}"`), which stays. |
| E-081 | `cli/tests/plugins/gcp/test_platform.py::test_combined_progress_does_not_claim_completion_when_readiness_fails` | progress/output sentence presence and absence | delete | The two prose legs of the same test; the mutation and delivery claims are carried by E-080 and the surviving cache assertions. |
| E-082 | `cli/tests/plugins/gcp/test_platform.py::test_second_interrupt_abandons_platform_rollback_without_mutating_survivors` | warning-phrase presence plus destructive-command absence | delete | Redundant here: lines 568-570 already assert the instance survived, both firewall rules survived, and no delete request was issued. See E-093 for where this family's real property lands. |
| E-083 | `cli/tests/plugins/gcp/test_platform.py::test_residual_definitive_insert_rejection_rolls_back_and_stays_detached` | hint required-phrase pair | delete | Duplicate of E-075's guidance sentences at the platform level; the rollback behavior is asserted structurally in the same test. |
| E-084 | `cli/tests/plugins/gcp/test_platform.py::test_delete_retains_same_name_different_id_instance_and_lifetime_deny` | hint substring | delete | The retention claim is structural on the lines above (`cache.instances.delete_requests == []`, the deny rule still in `cache.firewalls.resources`). |
| E-085 | `cli/tests/plugins/gcp/test_platform.py::test_delete_retains_same_id_mutated_allow_but_still_deletes_instance_and_deny,test_delete_continues_after_allow_read_failure_and_reports_retained_allow,test_delete_keeps_deny_when_allow_read_and_instance_delete_are_indeterminate` | guidance-sentence substrings | delete | Three tests, same shape. Each sits directly beneath the mutation record that proves the behavior (which rules survived, which delete requests were issued). |
| E-087 | `cli/tests/plugins/gcp/test_platform.py::test_stable_allow_close_hooks_fail_closed_on_provider_read_error,test_transient_route_close_failure_warns_and_retains_only_its_exact_rule` | warning-phrase presence | convert | The claim worth keeping is that these fail-closed paths warn at all rather than failing silently. Replace `"not proven absent" in "\n".join(warnings)` with a non-empty/count assertion on `warnings`; the retained-rule identity (`route_name in message`, line 985) is injected and stays. No production change. |
| E-088 | `cli/tests/plugins/gcp/test_platform.py::test_stable_allow_close_hooks_fail_closed_on_provider_read_error,test_transient_route_close_failure_warns_and_retains_only_its_exact_rule` | redaction defense | keep | Same invariant as E-086 on the hook and transient-route paths: a provider's private detail must never reach operator output. Regrouped from `source-guard` to `report-hints` by the lead, since it lives in the file the report-lines PR edits. |
| E-089 | `cli/tests/plugins/gcp/test_cleanup.py::test_ordinary_cleanup_cannot_replace_primary_failure` | coordinate and guidance sentence substrings | delete | Ten substring pins of one authored guidance string. The coordinates (`project-a`, `vm-a`, the provider IDs) are test-injected and could be narrowed to, but the sentences around them are ours and the branch is already fixed by the test's fixture states. |
| E-090 | `cli/tests/plugins/gcp/test_cleanup.py::test_ordinary_cleanup_cannot_replace_primary_failure` | destructive-command absence | convert | Folded into the single property test named in E-093: with ownership unknown, the guidance names no `gcloud ... delete` for any resource. Verified by execution. |
| E-091 | `cli/tests/plugins/gcp/test_cleanup.py::test_interrupt_after_partial_ordinary_rollback_converges_and_supersedes_primary,test_first_interrupt_rolls_back_and_reraises_original_identity` | verbatim warning list equality | convert | Replace with `len(warnings) == 1` (the first interrupt announces cleanup exactly once before proceeding); the mutation record in each test carries the rest. No production change. |
| E-092 | `cli/tests/plugins/gcp/test_cleanup.py::test_second_interrupt_after_partial_ordinary_rollback_is_only_abandon_signal,test_second_interrupt_abandons_promptly_with_exact_safe_actions` | guidance sentence substrings on the abandon path | convert | Rubric re-check, 2026-08-16. Scoped: the `:514-517` leg stands, the `:559-565` leg converts, being the only evidence its warn path fired. E-082 stands only because this converts. No production change. |
| E-093 | `cli/tests/plugins/gcp/test_cleanup.py::test_second_interrupt_abandons_promptly_with_exact_safe_actions,test_manual_guidance_recommends_delete_only_for_verified_owned_survivors,test_manual_guidance_for_provider_id_collisions_is_inspect_and_escalate_only,test_manual_guidance_for_unknown_provider_ids_is_inspect_and_escalate_only` | ~35 hint-equality and destructive-command pins across four tests | convert | Replace all four with one state-driven property over `manual_cleanup_guidance`: for every combination of `InstanceState`, `FirewallState`, ownership presence, and observed provider ID, the guidance names a `gcloud ... delete` for a resource IF AND ONLY IF that resource is proven owned (realized/surviving, ownership known, observed id == expected id). VERIFIED by executing that property against HEAD: it holds across the full state space. This is a real safety invariant (never recommend deleting a resource whose ownership is unproven), it needs no production change, and it subsumes every individual pin. The residual prose (which sentence explains why, and the deny rule's "keep it until the instance is absent" precondition) goes per R2.4. |
| E-094 | `cli/tests/plugins/gcp/test_cleanup.py::test_manual_guidance_for_provider_id_collisions_is_inspect_and_escalate_only,test_manual_guidance_for_unknown_provider_ids_is_inspect_and_escalate_only` | authored phrase count | delete | `message.count("do not delete by name; escalate ownership") == 3` counts occurrences of one authored sentence. The "all three resources got the escalate treatment" claim is carried by E-093's iff property. |
| E-096 | `cli/tests/vms/test_add_git_credential_orchestrated.py::test_disabled_runup_rejects_multiline_before_transport_or_write,test_conformant_provider_cannot_inject_returned_line_before_transport_or_write` | authored refusal-message substring | delete | Both tests already assert the security property structurally: `target.opens == 0`, `target.runs == []`, `target.writes == []`, plus the secret-absence checks. The refusal sentence adds nothing. |
| E-097 | `cli/tests/vms/test_add_git_credential_orchestrated.py::test_operator_stopped_vm_refuses_via_the_reread_race_guard` | hint substring naming a CLI command | delete | `"agw vm start box" in hint` is authored remediation; the gate's refusal is carried by the raised `StateError` and `started == []` / `target.writes == []`. |
| E-100 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_structured_failure_output_is_redacted_from_console_log_and_error,test_reflected_copy_failure_is_secret_free_across_every_surface,test_invalid_reflected_tailscale_ip_never_reaches_output_or_return,test_standalone_guest_removal_failure_surfaces_and_models_residue,test_local_removal_failure_surfaces_after_guest_cleanup` | authored error-message equality | delete | Five verbatim pins of our own error sentences. In each case the surrounding assertions (typed raise, secret absence, residue model) carry the real claim. |
| E-102 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_real_wsl_timeout_drops_reflected_output_and_removes_both_stages,test_invalid_reflected_tailscale_ip_never_reaches_output_or_return` | prefix assertions on an error and a detail line | delete | `str(...).startswith("WSL2 command timed out after 900s:")` and the "Tailscale IP:" prefix absence pin authored wording; the timeout and the never-returned-IP claims are carried by the typed raise and the return value. |
| E-103 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_guest_removal_failure_does_not_mask_primary,test_guest_removal_control_flow_does_not_mask_primary,test_local_removal_failure_does_not_mask_staging_primary` | verbatim warning list equality | convert | Same treatment as E-057: `len(warnings) == 1`, keeping `caught.value is failure` and the `_AUTH_KEY not in repr(warnings)` redaction check. No production change. |
| E-104 | `cli/tests/vms/test_bootstrap_vm_hooks.py::test_failed_path_hook_failure_does_not_mask,test_interrupt_hook_failure_does_not_mask_the_interrupt` | warning-phrase presence | convert | Rubric re-check, 2026-08-16. A silently swallowed `secure_failed_vm` failure would otherwise pass. Convert to a warning-count assertion. No production change. |
| E-105 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_failure_closes_logger_once_without_replacing_primary` | log-path substring in a warning and an error | keep | Invariant: a failed create tells the operator where the provisioning log is. The path is derived from the test's own VM name and log directory, and losing it would let the log path silently stop being reported. Narrow to the path, drop the `"Log: "` / `"Details: "` framing. |
| E-106 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_failure_closes_logger_once_without_replacing_primary,test_successful_create_propagates_fresh_logger_close_interrupt,test_logger_close_warning_failure_does_not_replace_primary` | verbatim warning equality and absence | convert | **Narrowed after executing each of the three tests.** `:331` emits zero warnings, so `captured_output.warnings == []` is exact; `:377-379` emits one, so a count holds. `:280-283` is struck from this row and moves to E-106b: its test emits THREE warnings under the `[interrupt]` parameter, so no count selector works there. `closes == ["close"]` and the primary-identity assertions stay in all three. No production change. |
| E-106b | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_failure_closes_logger_once_without_replacing_primary` | verbatim warning equality | delete | Split out of E-106 by execution: `test_create_failure_closes_logger_once_without_replacing_primary[interrupt]` emits three warnings, so no count selector distinguishes the logger-close warning from the other two, and the discriminator is our own wording. `closes == ["close"]` and the primary-identity assertion carry the invariant the test is named for. |
| E-107 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_rollback_on_keyboard_interrupt_unwinds_the_row` | warning-phrase presence | delete | The rollback is proven by `db.get_vm(...) is None` in the same test. |
| E-108 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_rollback_failure_warns_and_never_masks` | authored warning prefix | delete | `warning.startswith("rollback: teardown of vm/wvm failed:")` pins our own prefix; the two lines below it carry the substance. |
| E-109 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_rollback_failure_warns_and_never_masks` | injected identities in a rollback warning | keep | Invariant: a failed rollback names what survived (the DB row for the named VM) and chains the underlying cause. `wvm` and `"db locked"` are both injected by the test. |
| E-110 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_phase_a_failure_maps_to_provisioning_error,test_reinit_refuses_an_operator_stopped_vm_at_the_gate` | hint substring naming a CLI command | delete | Same as E-097; the gate behavior is carried by the typed raise and the row's `provisioning_status`. |
| E-111 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_phase_a_sync_failure_is_non_fatal` | warning-phrase presence | convert | Rubric re-check, 2026-08-16. A silently swallowed SSH-config sync failure would otherwise pass. Convert to a warning-count assertion. No production change. |
| E-112 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_create_provisioning_section_ends_with_ssh_config_synced,test_create_provisioning_section_ends_with_ssh_config_synced._fake_create,test_create_provisioning_section_ends_with_ssh_config_synced._FakeTS.__init__,test_create_provisioning_section_ends_with_ssh_config_synced._FakeTS.run` | narration-structure test (whole function) | delete | `test_create_provisioning_section_ends_with_ssh_config_synced` asserts a section header sentence, that one authored body line appears exactly once, that it is last, and that another authored line is a body rather than a detail. No structural sync observable exists in this test (the sync is not faked here), so nothing survives conversion. The line's own emission is covered at its unit in cli/tests/test_ssh_config.py:132-151, and `sync_ssh_config` is recorded structurally in cli/tests/vms/test_lifecycle_orchestrated.py:217; only the narration ordering is lost, which R2.4 accepts. |
| E-113 | `cli/tests/vms/test_create_reinit_orchestrated.py::test_reinit_runs_initialization_through_the_gate` | result-line sentence pins | convert | Rubric re-check, 2026-08-16. Convert to a role-and-level predicate over `captured_output.lines`, keeping the routing claim without the sentence. No production change. |
| E-115 | `cli/tests/vms/test_delete_vm_gating.py::test_hold_failure_does_not_skip_delete` | warning-phrase presence | delete | The invariant (a broken keepalive hold does not skip the delete) is `counts["delete"] == 1` and `db.get_vm(...) is None` on the two lines above. |
| E-116 | `cli/tests/vms/test_delete_vm_gating.py::test_stranded_site_warns_with_hint_and_still_deletes_row` | warning substrings including a manifest fragment | convert | Keep the stranded-site claim structurally (`resolve_counter == []`, the row deleted) and, for the `"kind: vm-site"` fragment, fold into E-127's approach: parse the emitted manifest hint and validate it rather than pinning its text. No production change. |
| E-117 | `cli/tests/vms/test_ensure_tailscale_wording.py::test_already_running_probe_says_verifying_not_reconnect,test_cold_start_probe_keeps_reconnect_wording,test_start_vm_through_wsl2_shaped_hold_verifies_exactly_once` | wording-only test file | delete | Three tests whose entire subject is which sentence the probe prints (the file's docstring says so). The behavioral core, `_tailscale_rejoin_required(...) is False` and the once-only verification, is asserted separately; the exactly-once claim at line 223 counts an authored line and goes with it per R2.4. |
| E-118 | `cli/tests/vms/test_legacy_site_sections.py::test_manifest_token_secret_conforming_emits_no_warning` | phrase blacklist over issue text | convert | Rubric re-check, 2026-08-16. Line 98 is the test's ONLY assertion, so the delete empties it. Assert `manifests.issues == ()`. No production change. |
| E-119 | `cli/tests/vms/test_lifecycle_orchestrated.py::test_start_stopped_vm_resolves_once_starts_and_clears_flag,test_start_running_vm_short_circuits_but_still_clears_flag,test_stop_running_vm_resolves_once_stops_and_sets_flag` | info-line presence and absence | delete | Three start/stop tests whose structural siblings (`events == [...]`, `row.operator_stopped`, `resolve_counter`) already carry every claim; the phrases only assert which sentence was chosen. |
| E-120 | `cli/tests/vms/test_lifecycle_orchestrated.py::test_stop_sets_flag_before_already_stopped_shortcut` | required-phrase pair | delete | The recorded intent is asserted structurally (`row.operator_stopped is True`, `events == ["status"]`); the two phrases police the wording of the explanation. |
| E-121 | `cli/tests/vms/test_lifecycle_orchestrated.py::test_stop_of_a_manually_stopped_vm_is_a_true_noop` | authored message equality (sole assertion) | convert | The claim is that a re-stop of an already-manually-stopped VM is a true no-op. Capture the `_fake_power` events (the fake already records them, as the sibling tests do) and assert `events == ["status"]` with no stop op, instead of pinning the sentence. No production change. |
| E-123 | `cli/tests/vms/test_lima_create_flow.py::test_probe_failure_warns_and_does_not_restart` | warning substring | delete | `"needs a restart to finish provisioning"` is authored wording; the no-restart behavior is line 122's command-absence check. |
| E-123b | `cli/tests/vms/test_lima_create_flow.py::test_probe_failure_warns_and_does_not_restart` | injected cause substring | keep | `"connection reset"` is the probe failure this test injected; its presence proves the cause is chained into the warning rather than swallowed. |
| E-125 | `cli/tests/vms/test_lima_create_flow.py::test_ip_probe_failure_returns_missing_ip_after_successful_join` | warning-phrase presence | delete | The behavior (missing IP returned after a successful join) is the return value asserted above it. |
| E-127 | `cli/tests/vms/test_lima_create_interrupt.py::test_failure_mid_create_cleans_up_and_reraises,test_interrupt_during_start_cleans_up_and_reraises_the_original,test_second_interrupt_abandons_cleanup_loudly` | phrase-selected warnings | delete | `"Cleanup abandoned"` / `"Ctrl-C again to abandon"` name branches whose behavior is already pinned by `_deletes(ran)` and `len(_deletes(ran)) == 1` in the same tests; the quoted `'limactl delete --force myvm'` inside the abandon warning is the tool command and can be kept by selecting the sole warning positionally. |
| E-128 | `cli/tests/vms/test_lima_remote_create_interrupt.py::test_remote_abandon_warning_names_the_vm_host,test_remote_interrupt_preserves_original_when_artifact_cleanup_fails,test_remote_exception_kills_cleans_artifacts_then_deletes_without_masking,test_cleanup_failure_warns_and_does_not_mask_the_original,test_pre_mutation_failure_makes_no_cleanup_calls` | phrase-selected warnings and detail lines | convert | Rubric re-check, 2026-08-16. Same family: a swallowed teardown failure leaving a partial instance. Convert to a warning-count assertion. No production change. |
| E-138 | `cli/tests/vms/test_platform_runup.py::test_proxmox_runup_other_status_warns,test_proxmox_runup_network_warns,test_aws_ec2_runup_unreachable_warns,test_aws_ec2_runup_non_auth_client_error_warns` | stderr warning substrings | convert | Four runup tests whose real invariant is "warns rather than raising" (each is annotated `# no raise`). Assert that stderr is non-empty instead of pinning which sentence; the API-error vs network-error distinction is authored-only and goes per R2.4. No production change. |
| E-139 | `cli/tests/vms/test_platform_runup.py::test_azure_runup_missing_group_is_fatal` | hint required-phrase pair | delete | The typed error's structural fields are already asserted (`entity_kind == "resource-group"`, `entity_name == "rg-dev"`); the two hint sentences are authored remediation. |
| E-143 | `cli/tests/vms/test_platform_support.py::test_doctor_lists_platforms_and_not_ready_sites` | doctor-message substring | delete | `"not ready"` is our own framing, sitting beside `by_name["wsl2"].status is doctor.Status.INFO`, which carries the state. |
| E-143b | `cli/tests/vms/test_platform_support.py::test_doctor_lists_platforms_and_not_ready_sites` | injected reason substring | keep | `"Windows only"` is the reason `_support()` injected; the assertion proves doctor surfaces the platform's own reason rather than a generic one. |
| E-144 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_describe_running_vm_is_one_boundary_burst_and_reads_only,test_describe_operator_stopped_vm_never_gates` | describe-report line substrings | delete | Both tests already pin the behavior (`resolve_counter`, `events == ["status"]`, no live read on a stopped VM); the rendered status text is the report's wording. |
| E-145 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_rekey_one_boundary_burst_covers_key_and_site_secret` | result-line phrase presence | delete | The rekey behavior is fully asserted above it (the exact sensitive stdin call, the key never in a rendered command, the row's new Tailscale host). |
| E-146 | `cli/tests/vms/test_remaining_commands_orchestrated.py::test_rekey_wraps_steps_in_a_section,test_backup_wraps_phases_in_a_section` | narration-structure tests with sentence pins | convert | Two "wraps in a section" tests. Keep the Role/level structure claims (a level-0 HEADER exists, the IP is a DETAIL not a step, the terminal is a level-0 RESULT), drop the four authored step-sentence assertions (438, 439, 718, 719) and the header/result text. No production change. |
| E-148 | `cli/tests/vms/test_shell_exec_orchestrated.py::test_exec_dash_prefixed_command_fails_with_zero_resolves_and_zero_gate` | hint required-phrase pair | delete | Authored remediation beside a `pytest.raises(ValidationError, match=...)` (group 1) and the zero-resolve/zero-gate assertions that carry the pre-gate refusal. |
| E-150 | `cli/tests/vms/test_sites_dispatch.py::test_resolving_a_not_ready_site_names_the_requirement` | negative fragment presence | delete | `"kind: vm-site" not in hint` distinguishes the not-ready hint from the undeclared-site hint; the `StateError` vs `ConfigError` types already discriminate those two paths. |
| E-152 | `cli/tests/vms/test_tailscale_logout_wording.py::test_deregistered_prints_inside_the_route_window,test_nullcontext_route_platform_output_is_unchanged,test_failed_dispatch_still_warns_without_success_line` | full info-transcript equality (three tests) | convert | The load-bearing claim is ordering: the logout dispatch and its success report happen INSIDE the transient route window. Replace the transcript equality with one ordered event log recorded by the test's own fakes (route open, transport command, route close) and assert the command index falls between open and close, plus that the failure path issues no success event. Both fakes already record; no production change. hla.md explicitly prefers this observational twin to a lexical order pin. |
| E-154 | `cli/tests/vms/test_tailscale_reachable.py::test_missing_binary_warns_once_and_degrades` | warning-phrase substring | delete | The degradation claim is the function's return value; the warning sentence is authored. |
| E-156 | `cli/tests/vms/test_verify_connection.py::test_verify_connection_cli_reports_service_result` | CLI stdout equality | delete | `result.stdout == "VM 'worker' connection verified via ssh.\n"` pins an authored CLI sentence; the exit code and the service result are the behavior. |
| E-157 | `cli/tests/vms/test_vm_nodes.py::test_manually_stopped_raises_and_skips_the_ping` | error and hint substrings | delete | The gate's behavior is `platform.start_calls == 0` plus the typed raise (whose `match=` is group 1's); `"not be auto-started"` and `"agw vm start gvm"` are authored. |
| E-160 | `cli/tests/test_aws_ec2_ops.py::TestCreateRollback.test_second_interrupt_abandons_cleanup_loudly` | verbatim abandon-warning equality | delete | **Was a convert; executing flipped it.** `test_second_interrupt_abandons_cleanup_loudly` emits two warnings under both parameters, so neither a count nor a positional selector is stable. The abandon claim survives structurally in `cleanup_calls == [{"InstanceIds": ["i-123"]}]`, which proves nothing further was touched. |
| E-161 | `cli/tests/test_aws_ec2_ops.py::TestCloseProvisioningHooks.test_hook_tolerates_missing_security_group,TestDelete.test_unexpected_terminate_failure_warns` | warning-phrase presence | convert | Rubric re-check, 2026-08-16. Same warn-fired family. Convert to a warning-count assertion. No production change. |
| E-162 | `cli/tests/test_azure_create_interrupt.py::TestInterruptDuringInlineWait.test_rolls_back_the_full_resource_set_and_reraises,TestInterruptDuringResourceCreation.test_rolls_back_what_exists_and_reraises,TestFailureDuringInlineWait.test_non_ssh_error_escaping_the_wait_rolls_back_vm_first_and_wraps,TestPlainFailureArmUnchanged.test_failure_still_cleans_up_and_wraps` | warning-phrase presence and absence | delete | Four tests where the deleted-resource lists and the propagated exception identity already fix the path; `"Ctrl-C again to abandon"`, `"cleaning up partial Azure resources"`, and `"Cleanup abandoned" not in` only name branches. |
| E-163 | `cli/tests/test_azure_create_interrupt.py::TestInterruptDuringInlineWait.test_second_interrupt_abandons_cleanup_loudly` | verbatim abandon-warning equality | delete | **Was a convert; executing flipped it.** `test_second_interrupt_abandons_cleanup_loudly` emits three warnings under both parameters. `network_interfaces.deleted == []` and `public_ip_addresses.deleted == []` carry the abandon. |
| E-164 | `cli/tests/test_azure_create_interrupt.py::TestInterruptDuringInlineWait.test_vm_delete_failure_during_rollback_warns_and_reraises` | phrase-selected warning with injected coordinates | convert | Rubric re-check, 2026-08-16. **Corrected against keep row C-173**, which keeps `:240-242`. Keep those, delete 243 and 248, and replace the phrase selector at `:239` with a predicate over the injected coordinate tokens. Supersedes this row's earlier delete-by-count reasoning, which execution had already shown unusable (three warnings, not one). No production change. |
| E-166 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_rbac_denial_raises_authorization_error` | hint substrings | delete | `"Contributor"` and `"re-run 'agw vm delete'"` are authored remediation; `AuthorizationError` plus `"AuthorizationFailed"` (the Azure SDK's own code, line 123) discriminate the branch. |
| E-167 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_rbac_denial_raises_authorization_error,TestGoneVMSucceeds.test_clean_delete_succeeds` | success-line absence and presence | keep | Rubric re-check, 2026-08-16. The only probe that a raising `delete()` never prints its success line. Verified by execution that the preamble and the success line are both `Role.BODY` at level 0, so no role-and-level handle can replace it. Line 228 must stay as the positive twin, without which the negative assertion goes vacuous. Cost: a rewording breaks two tests. (C-177 was a duplicate of this row and is removed.) |
| E-168 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_denied_probe_after_rbac_delete_failure_raises_authorization_error,TestSurvivingVMRaises.test_denied_probe_alone_raises_authorization_error` | same-type branch discriminators in our own prose | convert | Rubric re-check, 2026-08-16. `__cause__` is the injected delete failure in one test and the probe failure in the other (`plugins/azure/network.py:658-673`), so the two do NOT collapse to differing only in setup as the row assumed. Assert the cause. No production change. (Absorbs C-176's 197 and 216.) |
| E-169 | `cli/tests/test_azure_delete_verify.py::TestGoneVMSucceeds.test_sweep_straggler_warns_without_failing_the_delete,TestGoneVMSucceeds.test_disk_sweep_failure_still_warns` | phrase-selected straggler warnings | convert | The invariant is "a genuine straggler failure warns exactly once and never fails the delete" (and its twin, `warnings == []` when the resource was already gone, which is already structural). Select by count instead of by `"vm1-nic"` / `"OS disk"`, and drop the `"delete it there manually"` / `"agentworks-tagged disk"` phrases. No production change. |
| E-170 | `cli/tests/test_azure_delete_verify.py::test_no_resource_id_short_circuits` | warning-phrase presence | delete | The short-circuit is `fakes.events == []` on the line above: nothing was touched backend-side. |
| E-171 | `cli/tests/test_azure_nsg_exposure.py::TestCreate.test_create_announces_the_bootstrap_route_open` | announcement-line equality (whole test) | delete | `test_create_announces_the_bootstrap_route_open` asserts only that one authored sentence was printed. The behavior it narrates (the NSG allow is created scoped to exactly those prefixes) is asserted structurally by `test_create_provisions_deny_baseline_and_scoped_bootstrap_allow` at line 149 through `_assert_rule` (line 125, `rule.source_address_prefixes == prefixes`). |
| E-172 | `cli/tests/test_azure_nsg_exposure.py::TestPrefixAssembly.test_detection_failure_with_extras_proceeds_with_warning` | compound warning-phrase predicate | convert | Rubric re-check, 2026-08-16. The only evidence the egress-detection warning fired; a silent degradation would pass. Convert to a warning-count assertion. No production change. (Absorbs C-179's line 231.) |
| E-173 | `cli/tests/test_azure_nsg_exposure.py::TestPerOperationAllows.test_band_exhaustion_raises_typed_error` | error-message substring | delete | `"no free NSG priority" in str(exc.value)` is authored; the `StateError` raise plus the `TRANSIENT_ALLOW_RULE_PREFIX` hint check (a production constant) carry the band-exhaustion claim. |
| E-174 | `cli/tests/test_azure_vm_size_selection.py::TestCreateProvisioningOutput.test_exact_match_emits_spec_without_requested,TestCreateProvisioningOutput.test_round_up_warns_and_line_shows_selected_spec,TestCreateProvisioningOutput.test_non_burstable_override_selected_and_emitted` | provisioning-line equality plus a wording blacklist | delete | Three tests pinning the announcement sentence. VERIFIED: the selection invariant they claim to protect is covered structurally by `TestSelectVMSize` (lines 36-70, `_select_vm_size` asserted directly, including the 4/8 round-up to `Standard_B4ms`); that class runs green on its own. `"for requested" not in line` is a blacklist. |
| E-175 | `cli/tests/test_azure_vm_size_selection.py::TestCreateProvisioningOutput.test_round_up_warns_and_line_shows_selected_spec,TestCreateOSDiskClamp.test_below_floor_clamps_up_and_warns` | verbatim warning list equality | convert | The invariant is "warn if and only if the request was rounded up or clamped". Replace the sentence equality with `len(captured_output.warnings) == 1`, which pairs with the existing `assert not captured_output.warnings` in the no-round-up and at-or-above-floor twins. No production change. |
| E-178 | `cli/tests/test_wsl2_create_interrupt.py::test_failure_mid_provision_cleans_up_and_reraises,test_interrupt_during_primary_bootstrap_cleans_up_and_reraises_the_original,test_second_interrupt_abandons_cleanup_loudly,test_cleanup_failure_warns_and_does_not_mask_the_original` | phrase-selected warnings | convert | Rubric re-check, 2026-08-16. Scope corrected to include line 174, which the duplicate C-187 row covered. A swallowed teardown failure leaving a partial distro would otherwise pass silently. Convert to a warning-count assertion. No production change. |
| L-301 | `cli/tests/test_git_credential_scoping.py::test_core_credential_materialization_rejects_line_unsafe_token_for_shipped_provider,test_direct_builder_rejects_line_injected_by_conformant_provider` | authored refusal sentence via `str(caught.value)` | delete | The `pytest.raises` type carries the refusal; the sentence is ours. |
| L-302 | `cli/tests/test_git_credential_scoping.py::test_repo_scope_selected_by_path` | `"no repository path" in err` | delete | Authored diagnostic wording; the surrounding exit-code assertion carries the refusal. |
| L-303 | `cli/tests/test_git_credential_scoping.py::test_scoped_only_out_of_scope_serves_nothing` | `"no credential is scoped to github.com/other/repo" in err` | convert | Rubric re-check, 2026-08-16. `:412` carries the no-leak security invariant and stands, but `:413` is the only witness that the helper DIAGNOSES rather than failing silently, which the docstring names as half the behavior. Convert to a stderr non-empty assertion. No production change. |
| L-304 | `cli/tests/test_git_credential_scoping.py::test_include_is_only_usehttppath,test_initializer_writes_all_three_files` | `"useHttpPath = true" in gitconfig_content` | keep | Not our prose: `useHttpPath` is git's own config spelling, and path-keyed scoping does not work without it. This is the rule's outside-the-repository exception, pinned at the one token the behavior depends on. |
| L-305 | `cli/tests/test_git_credential_scoping.py::test_helper_warns_on_foreign_username` | two authored warning sentences, the test's ONLY assertions | convert | The behavioral distinction is real and this file proves it three ways: warn on a foreign embedded username, stay silent for a foreign credential (`:672`), stay silent when nothing is scoped (`:686`). Replace the two wording substrings with `err != ""`, which is the actual contrast the sibling tests assert as `err == ""`. No production change. |
| L-306 | `cli/tests/test_git_credential_scoping.py::test_helper_erase_deletes_nothing_and_diagnoses` | `"rejected git credential 'acme-bot'"`, `"secret 'git-token-acme-bot'"`, `"agw agent reinit"` | convert | Rubric re-check, 2026-08-16. `:654-655` carry the erase contract, but the sibling test at `:661-670` asserts `err == ""` for a foreign credential, so deleting all three phrases collapses the diagnose-versus-silent discrimination into two arms asserting the same thing. Keep the contrast structurally. No production change. |
| L-307 | `cli/tests/test_git_credential_scoping.py::test_helper_without_scopes_serves_but_never_warns` | `"bypasses" not in err` plus `"rejected git credential 'gh'" in err` | convert | Same treatment as L-305: `err == ""` for the get branch is the real claim, and the erase branch's sentence pin goes. |
| L-308 | `cli/tests/test_git_credential_scoping.py::test_initializer_writes_all_three_files` | emitted `git config` command strings | keep | These are commands we RUN, not prose we display, and their exact spelling is what makes the helper install idempotent (`grep -qxF` before `--add include.path`). Behavioral. |
| L-309 | `cli/tests/test_git_credential_scoping.py::test_generated_materials_work_with_real_git` | `"is not a git command" not in err` | keep | External prose: that sentence is git's own error for a malformed helper invocation, so the assertion pins an upstream token to prove our helper was invoked correctly at all. Narrow and load-bearing. |
| L-310 | `cli/tests/test_git_credential_scoping.py::test_generated_materials_work_with_real_git` | `"username=acme-bot" in out`, `"password=tokS" in out` | keep | The git credential helper protocol's wire format, defined by git. Asserting the served credential is the whole point of the test. |
| L-311 | `cli/tests/test_git_credential_scoping.py::test_generated_materials_work_with_real_git` | `"bypasses git credential scoping" in/not in err` | convert | **Recipe verification, 2026-08-16.** Scoped by verification. The `err == ""` conversion at `:903` holds and bites. The `in err` clause at `:914` is vacuous and is now a plain delete: with our helper's warning removed in production the test still passed, because GIT ITSELF writes `fatal: could not read Password for ...: terminal prompts disabled` to that same stream. L-305 and L-307 already prove the warned-versus-unwarned contrast twice in this file. |
| L-312 | `cli/tests/test_git_credential_scoping.py::test_generated_materials_work_with_real_git` | `"rejected git credential 'acme-bot'"`, `"secret '...'"` in the real-git erase contract | delete | The contract is asserted structurally on the two lines above: exit code 0 and the store file byte-identical. That is the invariant the helper exists for. |
| RB-001 | `cli/tests/sessions/test_claude_code_orchestrated.py::test_create_produces_launch_string_and_persists_the_minted_id` | launch-note decision line riding inside a behavioral test | delete | Added by the 2026-08-19 re-baseline; the file carried no row. `"starting new session s1" in captured["command"]`. The decision the line reports is already structural in the same test: `f"--session-id {sid}"` at `:174` and `"--resume" not in` at `:177` are the argv `claude` actually receives (`cli/agentworks/plugins/claude/harness_integration.py:194-200`). What the assertion adds beyond that is the sentence we author at `:199`, so its only failure mode is a rewording. Same shape and same disposition as B-068 on the sibling `test_claude_code_integration.py`. |
| RB-002 | `cli/tests/sessions/test_claude_code_orchestrated.py::test_create_resumes_when_a_transcript_exists` | launch-note decision line riding inside a behavioral test | delete | `"resuming session s1"`, on the transcript-present create path. `f"--resume {sid}"` at `:202` carries the decision; only the wording is left. |
| RB-003 | `cli/tests/sessions/test_claude_code_orchestrated.py::test_resume_reads_stored_id_and_resumes_after_the_kill` | launch-note decision line riding inside a behavioral test | delete | Same, on the restart path. `:261` asserts the stored id reached the argv verbatim, and `:266` holds the kill-before-detect ordering the test is really about. |
| RB-004 | `cli/tests/sessions/test_claude_code_orchestrated.py::test_resume_of_a_pre_column_session_mints_and_persists_the_id` | launch-note decision line riding inside a behavioral test | delete | Same, on the pre-column mint path. `:295-296` assert a fresh id was minted and used. |
| RB-005 | `cli/tests/sessions/test_codex_orchestrated.py::test_create_launches_fresh_without_probing_any_session_state` | launch-note decision line riding inside a behavioral test | delete | `"starting new session s1"` on the create path. The fresh launch is structural in the same test: `_SID not in command` at `:239` proves nothing was adopted, and the `rm -f "$HOME"/.agentworks/codex/s1.thread` at `:236` is emitted only by `_fresh_command` (`cli/agentworks/plugins/codex/harness_integration.py:739-755`). |
| RB-006 | `cli/tests/sessions/test_codex_orchestrated.py::test_resume_adopts_the_recorded_thread_id_and_persists_it` | launch-note decision line riding inside a behavioral test | delete | `"resuming session s1"` on the recorder-binding path; `f"resume {_SID}"` at `:256` carries it. |
| RB-007 | `cli/tests/sessions/test_codex_orchestrated.py::test_resume_resumes_the_stored_id_without_a_recording` | launch-note decision line riding inside a behavioral test | delete | Same, on the stored-id path; `:280` carries it. |
| RB-008 | `cli/tests/sessions/test_codex_orchestrated.py::test_resume_adopts_a_discovered_rollout_when_nothing_is_recorded` | launch-note decision line riding inside a behavioral test | delete | `"identified this session"` on the discovery-adoption path. Which source bound the id is already structural: `:302` resumes `_SID`, `:304` orders `read_recorder` before `discover`, and the fixture supplies `_SID` through discovery alone (`recorded` is unset at `:297`). The rest is the sentence composed at `cli/agentworks/plugins/codex/harness_integration.py:602-603`. |
| RB-009 | `cli/tests/sessions/test_codex_orchestrated.py::test_resume_with_several_candidates_opens_the_picker_and_binds_nothing` | launch-note decision line riding inside a behavioral test | delete | `"could not identify this session"` on the picker path. `:323` asserts the bare `codex resume -c tui.resume_cwd=current` head that only `_picker_command` emits (`cli/agentworks/plugins/codex/harness_integration.py:721-737`), and `:327` that nothing bound. |
| RB-010 | `cli/tests/sessions/test_codex_orchestrated.py::test_resume_with_a_gone_rollout_drops_the_id_and_launches_fresh` | launch-note decision line riding inside a behavioral test | convert | The one that is not a rewording. `"archived or gone; starting new session s1"` is the ONLY command assertion in its test, so deleting it leaves nothing proving a fresh launch happened at all; under the rubric it is the only probe for a behavior that can regress. `_fresh_command` builds its head as `()` (`cli/agentworks/plugins/codex/harness_integration.py:739-755`), so `f"resume {_SID}" not in captured["command"]` replaces it structurally with no production change. The stale-specific half of the line has no handle: `self._decision` is set to `"stale"` at `:616` but never leaves the object, and exposing it would be the shipped-contract change R2.2 forbids, so that one distinction goes under R2.4. |

## Group 4: schema, manifests, capabilities and platforms

228 live rows: 52 delete, 81 convert, 95 keep, out of a ledger of 250 (six `[dead]`, 16
`[subtracted]`). Once group 3 splits into its sub-batches this is the largest single PR by converts.

**Precondition, operator disposition 2026-08-19: the recipe verification is re-run before this group
executes**, on the same terms as group 3's.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-027 | `cli/tests/guide/test_migration_topic.py::L285-289` | **[line-anchored: file gone]** **[dead]** live schema-path presence | keep | Structural derivation check: the manifest field tree produced by `reference_for` must still expose the nested credential and placement paths the platforms declare. A model or reference-generator regression that stopped surfacing `auth.secret` would be caught here and nowhere in this file. |
| A-028 | `cli/tests/guide/test_migration_topic.py::L290-293` | **[line-anchored: file gone]** **[dead]** retired-spelling absence list | delete | A blacklist of pre-0.14 field paths. Reintroducing `service_principal.secret` would require re-adding a model field, which is a reviewed authored edit, and the positive assertions in A-027 already state what the schema is. |
| A-029 | `cli/tests/guide/test_migration_topic.py::L306-311` | **[line-anchored: file gone]** **[dead]** token-union structure pin | keep | Structural: exactly one `token` entry, `{"mode": "secret"}` default, one `secret` alternative arm. This is the shipped declaration shape, and a regression to the retired minted arm changes what operators may write. |
| A-030 | `cli/tests/guide/test_migration_topic.py::L312` | **[line-anchored: file gone]** **[dead]** wording blacklist over a repr | delete | `"minted" not in repr(...)` is a substring blacklist over a generated repr; its structural twin sits two lines above (A-029's single `secret` alternative) and its behavioral twin at lines 340-345, where `mode: minted` raises `ConfigError`. |
| A-031 | `cli/tests/guide/test_migration_topic.py::L314-345` | **[line-anchored: file gone]** **[dead]** capability-reference behavior and rejection | keep | Behavioral: default and explicit token forms resolve to the right secret reference, and stored/minted modes raise. No wording asserted. |
| A-032 | `cli/tests/guide/test_migration_topic.py::L348-360` | **[line-anchored: file gone]** **[dead]** declarable reference structure | keep | Same structural invariant as A-029 for the `git-credential` declarable: the nested provider token union stays reachable in the reference tree. |
| A-064 | `cli/tests/test_database_backup.py::test_schema_sentinels_match_every_historical_version` | **[subtracted: instance-model]** schema-sentinel parity | keep | Derivation parity: the hand-maintained `SCHEMA_SENTINELS` must match the schema produced by replaying `MIGRATIONS` at every historical version, and `validate_restore_source` must agree. This is the guard that finding D2's proposed derivation would replace, not one this sweep should touch. |
| A-079 | `cli/tests/test_db_migration_harness_integration_state.py::test_v31_renames_column_preserving_raw_and_decoded_state` | **[subtracted: instance-model]** persisted column shape | keep | Schema shape of persisted state that crosses executions: column renamed, old name gone, NOT NULL with the `'{}'` default. A migration regression here corrupts real operator databases. |
| B-013 | `cli/tests/sessions/test_effective_config_validation.py::test_an_error_on_an_inherited_key_names_the_template_that_declared_it` | error-message provenance substring | convert | The provenance attribution is a real invariant with a shipped structural handle: `effective_template(...).harness.provenance`. VERIFIED by executing a probe: for the inherited lineage it yields `{'command': 'base', 'timeout': 'base'}`. Replace the message reads with a provenance-map assertion on a lineage that resolves. No production change. |
| B-014 | `cli/tests/sessions/test_effective_config_validation.py::test_a_key_the_child_overrode_is_not_attributed_to_the_parent` | error-message substring plus phrase blacklist | convert | Same handle as B-013; VERIFIED the overriding lineage yields `{'command': 'base', 'timeout': 'kid'}`, which is exactly what `"inherited from" not in message` was groping for, without a phrase blacklist. |
| B-021 | `cli/tests/sessions/test_session_template_surface.py::test_declared_harness_integration_emits_a_reference` | usage-string equality | keep | `harness_refs[0].usage == "the session harness integration"` is the declared usage that flows into secret diagnostics; it is a shipped data field on a reference record, and the test is pinning the identity of that field, not a rendered sentence. Flagged as borderline: if the lead reads `usage` as authored prose, this becomes a delete. |
| B-059 | `cli/tests/secrets/test_backends.py::test_direct_backend_name_in_chain_gets_source_rewrite` | **[subtracted: secrets-preview]** diagnostic substring pair | convert | The invariant is real and structural in nature: a bare backend name used where a source name belongs produces a rewrite diagnostic naming `kind: secret-source`. Both strings interpolate names the test declared. Trim to the injected-name half (`'onepassword'` appears, referring to the fixture's backend) and drop the authored framing clauses. No production change. |
| B-062 | `cli/tests/secrets/test_sources.py::test_source_backend_must_use_the_tagged_table_shape,test_source_backend_config_errors_keep_manifest_location` | **[subtracted: secrets-preview]** manifest-diagnostic substring | keep | `"secret-source/ci-env.backend: must be a table"` and `".unknown: unknown field"` are keyed on the manifest path the test wrote plus the field name; the invariant is that a config error names the exact manifest location that caused it, which is the operator's only route back to their file. Recommend trimming the trailing authored clause and keeping the `kind/name.field` prefix. |
| B-063 | `cli/tests/secrets/test_sources.py::test_mapping_reference_extraction_uses_the_same_source_selector,test_descriptor_derived_mapping_key_collector_is_ordered_and_value_blind` | **[subtracted: secrets-preview]** reference-tuple equality carrying declared usage | keep | These pin the identity, ordering, and source attribution of extracted reference records; the usage strings are the fixture's own declarations. Structural presence, not wording. |
| B-064 | `cli/tests/secrets/test_sources.py::test_unknown_source_uses_identical_source_key_framing,test_same_target_mapping_validation_and_candidate_emit_one_diagnostic,test_mapping_key_collection_runs_for_later_materialized_host` | **[subtracted: secrets-preview]** phrase-occurrence counting (`.count(...) == 1`) | convert | The real invariant is de-duplication: one diagnostic per unknown source, not one per validation pass. Counting an authored phrase is the wrong instrument. Replace with a count of the diagnostics the error aggregates (the error already aggregates a list; count entries mentioning the source name the test declared). No production change. Flagged as needing the lead's eye: if the aggregate is not reachable as a list, this falls back to delete. |
| B-065 | `cli/tests/secrets/test_sources.py::test_direct_onepassword_table_mapping_gets_exact_source_rewrite` | **[subtracted: secrets-preview]** rewrite-hint verbatim pins | delete | `'account: "work.example.com"'` and `'<source-name>: "op://Work/Fixture/password"'` pin the exact YAML snippet our hint suggests. That snippet is authored content whose correctness is a review concern; the branch is discriminated by the raised type and the named source. |
| B-133 | `cli/tests/test_templates.py::test_git_identity_resolves,test_git_identity_inherits_and_overrides,test_unknown_workspace_template_key_is_refused` | injected-value equality and manifest-diagnostic substring | keep | `git_user_name == "Ada Lovelace"` / `"Base Bot"` are values the test's manifests declared (inheritance and override propagation). Line 239's `"workspace-template/default.git_user_emial: unknown field; expected one of: "` should be trimmed to the `kind/name.field` prefix per B-062, but the manifest-location half earns its place. |
| C-008 | `cli/tests/test_config_setting_references.py::test_every_setting_reference_source_is_covered` | quantifier parity | keep | Pins that the module's case tables cover `_SETTING_REF_SOURCES` exactly, so a settings value added to production cannot ship with no dangling-name case exercising it. This is the quantifier every behavioral test in the file leans on. |
| C-009 | `cli/tests/test_config_setting_references.py::test_every_referenced_kind_is_a_registered_kind` | structural presence | keep | Each declared reference source must name a kind in `KIND_REGISTRY`. A typo'd kind reports every value of that setting as dangling, including correct ones, and is invisible until an operator hits it. |
| C-010 | `cli/tests/test_config_setting_references.py::test_dangling_setting_reference_is_a_hard_error` | report-line substring | keep | **Corrected 2026-08-19 (map re-baseline).** Was delete. `source` is resolved per parametrize case at `:98` from the production table `_SETTING_REF_SOURCES`, so `f"references unknown {{source.kind}}"` varies with the setting under test and fails when the dangling-reference diagnostic names the wrong kind. Nothing else in the file checks the kind once C-013's convert narrows `:211` to the injected token, and deleting it would leave `source` an unused local. Lines `:102` and `:104` were never this row's and stay: the setting spelling and the operator's own value are value identity. |
| C-011 | `cli/tests/test_config_setting_references.py::test_dangling_hint_enumerates_what_is_declared` | hint content | keep | The hint must enumerate the rows the kind actually has (`_RESOLVING_NAME[setting] in hint`), which is registry-derived data, not authored prose; the remediation silently degrades to "declare one" if `_dangling_hint`'s enumeration is dropped. |
| C-012 | `cli/tests/test_config_setting_references.py::test_declarable_kind_hint_points_at_the_sample_command,test_secret_source_hint_points_at_the_declarable_sample` | hint command pin | delete | `"agw resource sample vm-site"` / `"agw resource sample secret-source"` pin the authored remediation spelling. The branch they distinguish (declarable versus app-provided kind) has no production handle, and R2.2 forbids adding one, so the coverage goes with the sentence per R2.4. |
| C-013 | `cli/tests/test_config_setting_references.py::test_a_misspelled_source_reports_as_a_bad_name_not_an_unreachable_secret` | sentence pin plus wording blacklist | convert | Rubric re-check, 2026-08-16. `:210-212` are the ONLY assertions in their test, so the delete removes the whole test and with it the ordering invariant that `validate_setting_references` runs before `secret` chain validation. Keep the injected token `envvar`: the unreachable path names `npm-token` instead. No production change. |
| C-015 | `cli/tests/test_builtin_entries_parity.py::test_installer_plugin_rows_match_oracle_and_are_disabled_by_default` | provenance and enablement structure | keep | That the moved rows carry `variant == "system-plugin"`, the right plugin and manifest source, and `Enablement.disabled` by default is structure the registry computes, and is what makes the installer plugins genuinely opt-in. |
| C-016 | `cli/tests/test_builtin_entries_parity.py::test_remaining_builtin_manifest_origin_is_truthful` | origin identity | keep | The built-in publisher must report its real package-and-file source rather than a generic label; this is a reference to a real shipped file, and provenance is what `agw resource list` renders. |
| C-076 | `cli/tests/test_resource_edit.py::test_builtin_capability_has_no_file_to_edit` | hint wording pin plus blacklist | delete | `"capability provided by the app" in hint` with `"resource sample" not in hint` polices the authored non-editable hint; the typed error with its `entity_kind`/`entity_name` (109-115) carries the branch. |
| C-077 | `cli/tests/test_resource_edit.py::test_builtin_declarable_resource_points_at_sample` | hint command pin | delete | Same shape as C-012: the declarable-kind hint's command spelling has no production handle, and R2.2 forbids adding one. |
| C-084 | `cli/tests/test_resource_list.py::test_cli_kind_csv_filter_tolerates_whitespace` | error-sentence pin | convert | Replace with `isinstance(result.exception, NotFoundError)` plus `result.exception.entity_name == "harness"`: `resources/inspect.py:193-198` already sets `entity_kind="resource-kind"` and `entity_name`, so the handle exists today. No production change. |
| C-097 | `cli/tests/test_config.py::test_git_credential_conforming_name_no_warning,test_git_credential_nonconforming_name_with_explicit_token_no_derived_warning,test_git_credential_name_deriving_secret_at_length_cap_no_warning` | negative wording match | convert | Replace each `not any("does not follow the naming rules" in issue ...)` with `_manifest_issues(config_file) == ()`. Verified by execution: a probe calling `_manifest_issues` on all three configs printed `()` for each, so the stronger structural form holds today. No production change. |
| C-098 | `cli/tests/test_config.py::test_orphaned_key_under_commented_section` | keyword-in-issue | keep | `"platform"` and `"operator"` are the config sections being blamed, so the assertion is that each bad section produced its own issue: the tokens are data. |
| C-099 | `cli/tests/test_config.py::test_ssh_allow_cidrs_normalized` | message pin | delete | `"is too long" in message` is our wording; the raised type and the cap echo on the next line carry the behavior. |
| C-100 | `cli/tests/test_config.py::test_ssh_allow_cidrs_normalized` | production-constant echo | keep | `f"max {MAX_SECRET_NAME_LENGTH}" in message` guards that the cap the error reports tracks the real constant rather than a stale hand-typed number. |
| C-101 | `cli/tests/test_config.py::test_user_section_is_rejected,test_user_section_is_rejected::ConfigError::973a2a` | message pin around an injected key | convert | Replace `"unexpected keys in [paths]: code_workspaces" in str(exception)` with `"code_workspaces" in str(exception)`: the retired key being named is the invariant. Line 655 (nothing created on disk) stays. No production change. |
| C-102 | `cli/tests/test_config_env_and_secrets.py::test_agentworks_prefix_env_emits_warning,test_plaintext_env_with_newline_warns` | injected name plus authored fragment | convert | Drop the `and "identity variable" in issue` / `and "newline" in issue` halves, keeping the injected-name half (`AGENTWORKS_VM`, `MULTILINE`). The invariant is that the offending entry is named. No production change. |
| C-103 | `cli/tests/test_config_env_and_secrets.py::test_reachability_keying_is_would_attempt_readiness_blind` | readiness-reason equality | convert | Replace `readiness_of(...).reason == "op CLI not installed"` with `not readiness_of(...).is_ready`. The verdict is structural; the reason sentence belongs to review. No production change. |
| C-104 | `cli/tests/test_config_env_and_secrets.py::test_unreachable_secret_error_message_and_hint` | required-phrase list over an error and hint | delete | `"unreachable secret"`, `"active source chain"`, `"attemptable secret-source"` and `"remove"` are a four-phrase checklist over authored text with no structural counterpart. |
| C-105 | `cli/tests/test_config_env_and_secrets.py::test_unreachable_secret_error_message_and_hint` | injected-value propagation | keep | The test's own secret name (`stranded`) and the configured source name (`env-var`) must appear so the operator knows which secret and which chain; value identity. |
| C-106 | `cli/tests/test_config_env_and_secrets.py::test_agentworks_prefix_warning_fires_for_every_scope` | quantified coverage | keep | `unwarned == []` is computed by iterating every env table, so it cannot go stale as tables are added; that quantifier is the point of the test. |
| C-107 | `cli/tests/test_config_env_and_secrets.py::test_vm_template_tailscale_auth_key_nonconforming_warns_but_loads` | authored fragment | delete | `any("the Tailscale auth key" in issue ...)` pins wording; line 861 already asserts the issue names both the secret and the row that misuses it. |
| C-108 | `cli/tests/test_config_env_and_secrets.py::test_conforming_secret_ref_names_emit_no_warning` | **[unverified]** negative wording match | convert | Same shape and same replacement as C-097 (`issues == ()` for the clean config). Not executed: this config was not in the executed probe. |
| C-112 | `cli/tests/test_env_block_references.py::test_env_entry_secret_ref_emits_secret_requirement,test_vm_template_dependencies_uses_template_name_in_source,test_undeclared_env_secret_auto_declares_through_build_registry` | usage-template equality | delete | `usage == "the API_KEY env var"` and `== "the Tailscale auth key"` pin an authored template; the reference's name, kind and source assertions beside them carry the structure. |
| C-113 | `cli/tests/test_env_block_references.py::test_multiple_env_refs_from_one_resource_each_contribute_usage` | usage-template list equality | convert | Replace `texts == ["the KEY_A env var", "the KEY_B env var"]` with `len({u.usage for u in dependents}) == 2`. The invariant is that two env refs from one row contribute two distinguishable usages. No production change. |
| C-146 | `cli/tests/test_git_credentials_typo_errors.py::test_admin_referencing_undeclared_git_credential_errors_at_finalize,test_agent_template_referencing_undeclared_git_credential_errors` | dangling-reference identity | keep | The error must name both the missing credential and the row referencing it; every asserted token is a name the test declared, and this is what turns a typo into a findable one. |
| C-175 | `cli/tests/test_azure_delete_verify.py::TestSurvivingVMRaises.test_rbac_denial_raises_authorization_error` | provider error code | keep | `"AuthorizationFailed" in str(exc.value)` pins Azure's own error taxonomy reaching the operator: the rule's outside-the-repository exception covers exactly this. |
| C-180 | `cli/tests/test_azure_nsg_exposure.py::_assert_deny_shape,_assert_allow_shape,TestCreate.test_create_provisions_deny_baseline_and_scoped_bootstrap_allow,TestCloseProvisioningAccessHooks.test_post_tailscale_ready_deletes_the_bootstrap_allow,TestCloseProvisioningAccessHooks.test_secure_failed_vm_deletes_the_bootstrap_allow,TestTransientRoute.test_enter_converges_then_pokes_and_exit_removes,TestTransientRoute.test_poke_attempt_cleanup_failure_warns,TestTransientRoute.test_exit_removes_allow_when_body_raises,TestTransientRoute.test_exit_removes_allow_when_body_raises::RuntimeError::59596d,TestTransientRoute.test_exit_removal_failure_warns_with_rule_and_prefixes` | NSG rule shape and lifecycle | keep | Access, direction, protocol, port, the created and deleted rule identities, and the injected CIDR appearing in the warning are the exposure boundary itself: a wrong value here opens SSH to the world. |
| C-182 | `cli/tests/test_azure_nsg_exposure.py::TestPerOperationAllows.test_band_exhaustion_raises_typed_error` | production-constant echo | keep | `TRANSIENT_ALLOW_RULE_PREFIX in hint` compares against the production constant, so it is parity rather than a copy, and it is what points the operator at the rules to clean up. |
| C-183 | `cli/tests/test_azure_nsg_exposure.py::TestPerOperationAllows.test_description_carries_timestamp_marker` | timestamp shape in a rule description | keep | The transient rule's description carries a UTC timestamp the sweep path relies on to age rules out; the regex pins the shape, not the wording around it. |
| C-185 | `cli/tests/test_aws_ec2_ops.py::TestCreate.test_user_data_over_cap_is_typed,TestCreate.test_disk_describe_images_failure_is_typed` | message and IAM-action pins | convert | Rubric re-check, 2026-08-16. `"10-byte"` is NOT authored wording: `10` is the value the test monkeypatches into `_MAX_USER_DATA_BYTES` at `:227`, and production interpolates the real constant. Assert against the monkeypatched cap so it stays derived; drop `:288`. No production change. |
| C-186 | `cli/tests/test_aws_ec2_ops.py::TestCreate.test_provisions_and_records_metadata_without_public_ip_fields,TestCreate.test_security_group_is_created_with_no_ingress,TestCreate.test_bootstrap_allow_scoped_to_detected_and_configured,TestCreate.test_launch_pins_a_permanent_auto_public_ip,TestCreate.test_resolves_default_subnet_when_unconfigured,TestCreate.test_no_default_subnet_is_typed,TestCreate.test_no_default_subnet_is_typed::EC2Error::cb137f,TestCreate.test_default_subnet_pick_is_deterministic_lowest_az,TestCreate.test_user_data_is_gzipped_and_under_cap_with_a_large_key,TestCreate.test_user_data_over_cap_is_typed,TestCreate.test_user_data_over_cap_is_typed::EC2Error::165daa,TestCreate.test_always_resolves_debian_ami_from_ssm_for_arch,TestCreate.test_rejects_a_name_collision,TestCreate.test_rejects_a_name_collision::StateError::321425,TestCreate.test_collision_check_fails_closed,TestCreate.test_arch_cross_check_rejects_a_mismatch,TestCreate.test_arch_cross_check_rejects_a_mismatch::ConfigError::808d46,TestCreate.test_disk_request_sizes_the_root_volume,TestCreate.test_every_create_sizes_the_root_volume,TestCreate.test_disk_describe_images_failure_is_typed,TestCreate.test_disk_describe_images_failure_is_typed::EC2Error::ba1935,TestCreate.test_tailscale_path_waits_for_bootstrap` | SDK call and payload assertions | keep | Which EC2 calls were made, with which security group, subnet, image, device mapping and user-data encoding, is the platform's behavior against the provider's own contract. |
| C-188 | `cli/tests/test_capability_config_contract.py::test_cycle_reported_before_malformed_block` | negative wording match | delete | `"unknown shell harness field" not in str(exc.value)` identifies a branch by the absence of one phrasing, which is the blacklist failure mode; the raised type is what the test should lean on. |
| C-189 | `cli/tests/test_capability_config_contract.py::test_capability_refs_attributed_to_consuming_resource` | auto-description template | convert | Replace `"the signing key for git-credential/signer" in decl.description` with `"git-credential/signer" in decl.description`: the auto-declared row must name its source, which is data. No production change. |
| C-190 | `cli/tests/test_capability_config_contract.py::test_azdo_org_required,test_azdo_rejects_unknown_blob_fields_yaml,test_a_broken_blob_is_refused_whatever_the_plugins_state,test_a_valid_blob_on_a_disabled_plugin_loads_with_the_row_marked` | injected-value propagation | keep | The manifest filename and the platform name the test declared must reach the error and the not-ready reason: value identity across the manifest boundary. |
| C-191 | `cli/tests/test_name_validation.py::test_valid_names,test_invalid_names,test_single_letter,test_single_digit,test_single_hyphen,test_single_underscore,test_double_hyphen_allowed_when_flag_set,test_double_hyphen_flag_does_not_relax_other_rules,test_name_at_cap_ok_over_cap_rejected,test_kind_caps_have_expected_values,test_agent_cap_yields_max_length_username,test_workspace_cap_yields_max_length_group,test_vm_cap_yields_max_length_azure_vnet_name,test_session_cap_yields_max_length_socket_path,test_secret_cap_is_larger_than_agent_cap,test_secret_name_over_agent_cap_passes,test_secret_cap_error_message_reports_secret_max,test_secret_cap_does_not_relax_character_rules` | validator behavior | keep | The name grammar, the caps, the prefix arithmetic against `MAX_*_LENGTH`, and the boundary cases (exactly at cap, one over) are the validator's behavior at a real operator-input boundary. |
| D-001 | `cli/tests/schema/test_errors.py::test_an_unknown_key_names_the_fields_that_are_valid` | whole-rendered-line pin | convert | The invariant is that an unknown key is addressed at its own path and enumerates the container's real fields; replace with `_problems` `path`/`unknown_field` plus a field list derived from `PrincipalLike.model_fields`. Verified by execution: `_problems` returns `('client_ids', ..., unknown_field=True)`. |
| D-002 | `cli/tests/schema/test_errors.py::test_structural_collapse_uses_unknown_field_state_not_rendered_wording` | monkeypatched wording probe + `(path, message)` equality | convert | Keep `[(p.path, p.unknown_field)] == [("secrit", True)]` (the structural-collapse invariant) and drop both the monkeypatch and the message. **Verified by execution: the monkeypatch is inert.** `_structural_shape_problems` (`schema/errors.py:343-359`) builds its own `f"unknown field; expected one of: {expected}"` and never calls the patched `_unknown_field` (`errors.py:533` is its only caller), so the test's stated premise is false and the message pin is a second copy of a string production authors 14 lines away. |
| D-003 | `cli/tests/schema/test_errors.py::test_a_wrong_type_says_what_the_field_must_be,test_a_missing_required_field_says_it_is_required` | whole-rendered-line pins | delete | "must be a string" / "is required" are authored normalizations of a pydantic error type; the path halves are unremarkable and are re-asserted by the path-rendering section. Nothing structural is lost. |
| D-004 | `cli/tests/schema/test_errors.py::test_a_missing_required_UNION_also_names_the_choices_it_is_between` | whole-rendered-line pin | convert | Rubric re-check, 2026-08-16. **Verified by execution**: with `_missing_union_block` stubbed to return `None`, exactly ONE assertion fails across the full 7,424-test suite, this one. Replace with a tags-derived-from-`SiteLike` cross-check. No production change. |
| D-005 | `cli/tests/schema/test_errors.py::test_a_bad_capability_name_lists_the_names_that_are_registered` | whole-rendered-line pin | delete | Same shape as D-004 for a misspelled tag. The registry itself is proven non-empty by D-006's cross-check, which survives. |
| D-006 | `cli/tests/schema/test_errors.py::test_an_absent_capability_name_lists_the_same_names_a_wrong_one_does` | two spelled literals plus a line-to-line equality | delete | **Corrected 2026-08-19 (map re-baseline).** The range read `:169-187` while the prose kept the non-vacuity guard and also listed `:186` for deletion, and `:186` IS that guard. Drop `:184` alone, the spelled `absent == [...]` literal. Keep `:186` (`"'lima', 'proxmox'" in misspelled[0]`), which the comment at `:185` names as the non-vacuity guard, and `:187` (`absent[0].endswith(misspelled[0].split("; ", 1)[1])`), which pins no wording at all: it says one union answers with one list, which is the invariant and the thing that can regress. **The kept line's own disposition is DEFERRED, and recorded rather than left silent:** the 2026-08-19 charter covered the contradiction between this row's range and its prose, not the question of whether `:186` should itself convert. It pins `'lima', 'proxmox'`, which is value identity on the fixture's own registered arms, so deriving it from that registry instead is a real option and nobody has ruled on it. The row keeps `:186` as it stands until someone does. |
| D-007 | `cli/tests/schema/test_errors.py::test_a_capability_name_the_loader_read_as_a_boolean_is_not_quoted_back_as_python_wrote_it` | pyyaml-derived assertions plus two spelled literals | delete | **Corrected 2026-08-19 (map re-baseline).** Range narrowed from `:190-216` to the two lines it actually drops, because the range swept up the three the justification keeps. Drop `:215` (`"quote it to name one"`) and `:216` (`"registered: 'lima', 'proxmox'"`), which are authored sentence fragments. Keep `:211-212`, whose spellings derive from `yaml.constructor.SafeConstructor.bool_values`, an upstream table and the sanctioned external exception, and `:214` (`repr(str(written)) not in line`), a guard against a Python repr reaching an operator rather than a wording pin. |
| D-008 | `cli/tests/schema/test_errors.py::test_one_error_renders_in_decodes_shipped_single_line_shape,test_one_error_without_a_location_is_just_the_owner_framed_line` | full-message equality, one-error framing | convert | The invariant is decode's single-line framing (`<location>: <owner>.<path>:` plus a trailing space), not the normalized message; assert the framing prefix and let the message tail go. No production change: the prefix is composed from `SourceLocation` and `RefOwner`, both already constructed in the test. |
| D-009 | `cli/tests/schema/test_errors.py::test_a_synthesized_location_frames_nothing` | full-message equality, synthesized sentinel | convert | Real invariant (a synthesized location must render no navigable path). Replace the spelled message with the derived equality `str(_raised(..., location=synthesized())) == str(_raised(..., location=None))`, which states the property exactly and pins no wording. |
| D-010 | `cli/tests/schema/test_errors.py::test_a_location_with_no_line_still_names_the_file` | full-message equality, `line == 0` sentinel | convert | Real invariant and NOT the same answer as D-009 (file named, position dropped). Replace with `startswith("sites.yaml: ")` plus `":0" not in`, the shape `test_error_framing.py:187-188` already uses for the same sentinel. |
| D-011 | `cli/tests/schema/test_errors.py::test_the_owner_rides_along_as_the_errors_entity,test_a_hint_is_carried_through` | attribute equality | keep | `entity_kind`, `entity_name` and `hint` are structured fields of `ConfigError` carrying the owner and a caller-supplied hint; a bridge that stopped populating them would break every downstream consumer silently. Not prose. |
| D-012 | `cli/tests/schema/test_errors.py::test_several_errors_render_under_one_located_header` | verbatim four-line block pin | delete | Keep the batch's structural facts (a located header, one indented line per problem, problem count), which D-013's two tests already assert at `:298-313`, and drop the spelled body. The three problem paths can be asserted through `_problems` if the lead wants the address coverage retained. **Corrected 2026-08-19 (map re-baseline).** The 2026-08-18 cross-group re-check listed this row as a range hazard; it is not one. `:287-295` is one test whose whole body is the verbatim block pin, and the structural facts live in D-013's separate tests, which this row does not touch. |
| D-013 | `cli/tests/schema/test_errors.py::test_no_line_of_a_multi_error_batch_is_unlocated,test_a_multi_error_batch_without_a_location_still_reads_as_one_message` | header/indent structure | keep | The design's load-bearing invariant: no line of a multi-error batch is unlocated. Asserted as `startswith(location)` plus all-body-indented, which pins layout and no wording, and fails when a call-site framing regresses. |
| D-014 | `cli/tests/schema/test_errors.py::test_a_capped_batch_says_how_many_it_did_not_show` | count assertions plus `"  ... and 3 more"` literal | convert | Keep 325-326 (both derived from `MAX_ERROR_LINES`). Replace 327 with a check that the final line carries the residual count (`str(3)`), so the cap's arithmetic stays guarded without pinning the sentence around it. |
| D-015 | `cli/tests/schema/test_errors.py::test_the_cap_hides_nothing_from_the_rendering_core,test_every_error_the_exception_carries_gets_a_problem` | `_problems` cardinality | keep | The cap is presentation only: every problem survives for a diagnostic surface, and `len(_problems(...)) == exc.error_count()` catches a walk that drops errors. Pure counting. |
| D-016 | `cli/tests/schema/test_errors.py::test_the_union_arm_tag_is_dropped_from_the_path,test_an_unknown_key_inside_a_union_arm_lists_that_arms_fields` | whole-line pins carrying a path | convert | The union-arm-tag drop is real logic that regresses to `platform.lima.vm_host`; assert `[p.path for p in _problems(...)]`. Verified by execution: paths are `platform.vm_host` and `platform.regions`. |
| D-017 | `cli/tests/schema/test_errors.py::test_a_nested_block_renders_as_a_dotted_path,test_a_deeper_block_keeps_its_whole_path` | whole-line pins carrying a path | convert | Same conversion; verified path `accounts_by_name.prod.secret` and `middle.deep.entry`. The dotted-path walk is the code under test and the message is incidental. |
| D-018 | `cli/tests/schema/test_errors.py::test_a_list_index_belongs_to_the_field_that_holds_it,test_a_marked_list_element_is_addressed_by_its_index` | whole-line pins carrying a path | convert | Same conversion; verified paths `vm_sizes[1].cpus` and `inherits[1]`. Index placement is exactly what an index-walk bug moves. |
| D-019 | `cli/tests/schema/test_errors.py::test_a_whole_document_problem_carries_no_path_and_is_located_like_any_other` | whole-message equality | convert | **Recipe verification, 2026-08-16.** Convert, with the guard stated honestly: the `path == ""` half cannot bite, because pydantic's `loc` for a root model's own problem is empty and the input forces it. The half that does bite is the located framing, verified against a `noloc` break. State the invariant as the location half. |
| D-020 | `cli/tests/schema/test_errors.py::test_a_root_model_addresses_the_fields_of_what_it_wraps` | whole-line pin carrying a path | convert | The walk has to start inside what a root model wraps or it loses the first segment; assert the path. |
| D-021 | `cli/tests/schema/test_errors.py::test_a_single_arm_unions_tag_is_dropped_like_any_other` | whole-line pins carrying a path | convert | The single-arm-union collapse is a live shape (one registered implementation); verified path `platform.bogus` with `unknown_field=True`. Assert path plus the unknown-field flag. |
| D-022 | `cli/tests/schema/test_errors.py::test_an_undiscriminated_unions_arm_name_is_dropped_too,test_an_undiscriminated_union_keeps_its_arms_field_list` | indented-line substring pins | convert | Same: the class-name segment drop is observable as `path == "platform.vm_host"` / `"platform.bogus"` on the `_problems` record (verified). The indentation half is already covered by D-013. |
| D-023 | `cli/tests/schema/test_errors.py::test_a_constrained_table_key_is_addressed_without_pydantics_key_marker,test_a_table_key_spelled_like_the_marker_is_still_addressed` | whole-line pins carrying a path | convert | The `[key]` marker drop is a path-walk rule; verified paths `env.lower` and `env.[key]`. The message tail is the fixture's own validator text (test-injected), so it may ride along or go, but the path is what earns the test. |
| D-024 | `cli/tests/schema/test_errors.py::test_a_nested_tables_key_keeps_the_segments_the_walk_cannot_tell_apart` | parametrized whole-line pins | convert | The last-segment condition is the rule under test and it is entirely a path fact (`outer.a.[key].[key]` vs `outer.a.low.[key]`); assert paths. |
| D-025 | `cli/tests/schema/test_errors.py::test_a_value_matching_no_alternative_of_a_union_is_one_problem,test_a_collapsed_union_line_replaces_pydantics_member_labels` | whole-line pin plus problem counts | convert | Keep 516-523 verbatim (`error_count() == 3`, `len(_problems(...)) == 1`), which is the collapse invariant stated structurally. Convert 507-513 to the same counting plus `alternatives`, which `_Problem` exposes (verified `('a string', 'a table', 'False')`). |
| D-026 | `cli/tests/schema/test_errors.py::test_the_arm_that_got_past_the_shape_answers_for_the_whole_union` | whole-line pin | convert | The rule ("the arm that got furthest answers") is observable as one problem at path `reference` (verified). Assert cardinality plus path; the message is incidental. |
| D-027 | `cli/tests/schema/test_errors.py::test_a_shape_rejection_is_noise_whichever_end_of_the_batch_it_lands_on` | whole-line pin | convert | Mirror case, same conversion: one problem, empty path (verified). |
| D-028 | `cli/tests/schema/test_errors.py::test_a_union_no_arm_could_enter_still_lists_every_alternative` | whole-line pin | convert | "Every alternative survives when no arm entered" is structurally exposed as `_Problem.alternatives` (verified `('a string', 'a table')`), so the invariant keeps a guard without the sentence. |
| D-029 | `cli/tests/schema/test_errors.py::test_container_types_read_as_the_shapes_an_operator_writes,test_a_non_table_where_a_capability_block_belongs_says_table` | parametrized normalization-message pins | convert | **Corrected 2026-08-19 (map re-baseline).** Was delete on a false premise. `_Problem.path` is a structural handle for the address half, `_problems` is already imported by this file, and `blob`/`expected` are columns of a parametrize table, so each case proves its own malformed input routes to its own address (`vm_sizes` versus `vm_sizes[0]` versus `accounts_by_name`). That is per-case routing, which G1-K11 keeps for the same reason. Convert on D-016's recipe: assert `[p.path for p in _problems(...)]` per case. Only the phrasing `_TYPE_MESSAGES` supplies (`schema/errors.py:87-100`) has no handle, and that half goes under R2.4. |
| D-030 | `cli/tests/schema/test_errors.py::test_closed_choices_and_emptiness_are_normalized` | parametrized normalization-message pins | convert | **Corrected 2026-08-19 (map re-baseline).** Same correction as D-029 and the same recipe: the two cases route to `.arch` and `.label`, which `_Problem.path` carries. Cost stated: the rendered choices list (`'arm64' or 'x86_64'`) loses its only assertion here, and the choices themselves stay covered structurally by `test_spec_admin_and_console.py:41-47` for its own model. |
| D-031 | `cli/tests/schema/test_errors.py::test_a_constrained_string_shape_reads_as_the_rule_it_must_match,test_a_pattern_containing_a_slash_is_still_readable` | whole-line pins around a pattern | convert | The load-bearing half is that the field's OWN pattern reaches the operator unmangled, and the pattern is supplied by the test's fixture (`Field(pattern=...)`), so assert the fixture's pattern string appears in the rendered line and drop the "must match `...`" framing around it. Both tests convert identically. |
| D-032 | `cli/tests/schema/test_errors.py::test_a_length_floor_above_one_keeps_pydantics_exact_wording` | upstream-message pin, spelled | convert | The invariant (a `min_length` above 1 is not paraphrased) is real, but the assertion spells pydantic's sentence instead of deriving it. Convert to the shape its own sibling at 625-631 already uses: read `detail["msg"]` off the pydantic error and compare. |
| D-033 | `cli/tests/schema/test_errors.py::test_an_unmapped_error_type_falls_through_verbatim` | fall-through parity, derived | keep | Derives the expectation from pydantic's own `detail["msg"]` rather than spelling it, and guards that an unmapped error type is carried through instead of silently blanked. Derivation parity against an external source. |
| D-034 | `cli/tests/schema/test_errors.py::test_every_corpus_shape_renders_something_an_operator_can_read` | non-emptiness over a corpus | keep | Guards that no corpus shape falls through the walk to an empty message. Asserts existence, never content. |
| D-035 | `cli/tests/schema/test_errors.py::Exclusive._mutually_exclusive,test_a_validators_own_message_is_carried_without_pydantics_prefix` | rendered-line pin of a fixture-authored message | keep | The message is raised by the test's own `Exclusive._mutually_exclusive` validator at :661, so this is value identity proving pydantic's "Value error, " prefix is read off the context rather than sliced off the text. A regression re-introduces the prefix and this catches it. |
| D-036 | `cli/tests/schema/test_errors.py::test_the_bridge_produces_a_plain_config_error` | exception-type identity | keep | `type(raised) is ConfigError` exactly, never a subclass: the finalize pass has one framing to leave alone. Error-type assertion, explicitly a keep shape. |
| D-037 | `cli/tests/schema/test_errors.py::test_an_owner_label_overrides_the_kind_slash_name_framing` | prefix assertion on a test-injected label | keep | `"[azure]"` is constructed by the test at :695; asserting it reaches the front of the message proves the label override propagates. Value identity, not authored prose. |
| D-038 | `cli/tests/schema/test_block.py::test_an_omitted_tag_reads_as_a_missing_field,test_an_empty_tag_is_refused,test_a_non_table_reads_as_a_table_requirement` | three whole-message equalities | convert | **Corrected 2026-08-19 (map re-baseline).** The "or delete" is closed toward convert, which also discharges the row's unverified tag: the duplication claim against `test_errors.py` no longer carries the disposition, so the row is no longer one of the reasoned-not-executed set. The block-level facts are ADDRESSES: an omitted tag reads at `platform.name` rather than as a shape error at `platform`, and a bare string reads at `platform`. `_Problem.path` carries that and varies with the defect, so under `hla.md` case 2 the handle exists and the row converts on D-016's recipe. Cost stated: `is required` versus `must not be empty` (pydantic's `missing` versus its length rule) share the path, and no handle exposes the error type, so that one distinction goes under R2.4. The framing half is disclaimed by the file's own helper docstring at `:28-33`. |
| D-039 | `cli/tests/schema/test_structural_union.py::test_overlapping_table_languages_are_loud_even_without_markers,test_optional_overlapping_table_languages_stay_loud_without_markers` | reason substring naming fixture classes | keep | `First` and `Second` are classes the test defines at :193-199 and :210-215; the diagnostic naming the two colliding arms is what a plugin author needs to find the collision, and the assertion is value identity on test-injected names. The `structural_union_error(...) is not None` half already carries the branch. |
| D-040 | `cli/tests/schema/test_structural_union.py::test_a_marker_free_validation_alias_is_still_loud` | reason substring naming a fixture field | keep | `Aliased.value` is defined by the test at :285-289; the reason naming the aliased field is value identity and the actionable half of the diagnostic. |
| D-041 | `cli/tests/schema/test_structural_union.py::_assert_selector_is_refused` | authored-phrase pins | delete | `"selector-free"` is authored wording with nothing injected; `"combines StructuralUnion with discriminator 'mode'"` is authored framing whose only injected token is the fixture's discriminator name. The family is already discriminated by calling `structural_union_error` directly (:309). Keep the `is not None`; drop both phrases. |
| D-042 | `cli/tests/schema/test_shorthand.py::test_an_arm_shorthand_alone_does_not_dispatch_a_tagged_union` | authored-phrase pin | delete | `"declares no UnionScalarShorthand"` is our own sentence; `union_scalar_shorthand_error(Holder)` returning non-None already says which check fired, since calling the specific checker is the discriminator. Verified by execution (experiment D): the three sub-checkers each answer for exactly one family. |
| D-044 | `cli/tests/manifests/test_samples.py::test_all_kinds_concatenation_and_unknown_kind,test_all_kinds_concatenation_and_unknown_kind::ValidationError::0b5f13` | per-kind header regex | keep | Structural presence derived from the registry: every declarable kind appears as its own document header in `--all`. A kind silently missing from the concatenation is the regression, and the pattern is built with `re.escape(kind)` rather than spelled. |
| D-048 | `cli/tests/manifests/test_reference.py::test_an_authored_example_is_what_the_sample_writes,test_a_closed_field_with_ONE_value_writes_it` | rendered-line pins of fixture values | keep | `me@gpu-box` is the fixture's own `Field(examples=...)` at :48 and `lima` is the fixture union's tag; asserting they reach the rendered document is value identity proving the authored example propagates, which is the whole point of the example plumbing. |
| D-049 | `cli/tests/manifests/test_reference.py::test_a_closed_field_with_SEVERAL_values_writes_its_default,test_a_default_worth_showing_is_shown_and_an_empty_one_is_not,test_a_field_with_no_default_and_no_example_gets_a_typed_placeholder` | rendered-line pins of fixture defaults | keep | `mode: safe`, `replicas: 3`, `enabled: false`, `tags: [<string>]`, `note: <string>` all restate values the fixture model declares at :50-53; what is guarded is which default is written and whether a falsy one is treated as absent, which `worth_showing` (:177-182) can and did get wrong. |
| D-050 | `cli/tests/manifests/test_reference.py::test_only_required_fields_are_live_document_lines` | live-versus-commented partition | keep | "Only required fields are live document lines" is the property the whole sample design rests on, and the assertion computes the partition from the prefix rather than pinning prose. A renderer that emitted an optional field live fails here. |
| D-051 | `cli/tests/manifests/test_reference.py::test_an_owner_templated_field_is_not_the_operators_to_write` | authored explanatory sentence, reflowed | convert | Keep :203 (the owner-templated field renders commented, not live). Replace :207 with a check that the fixture's own `default_template` (`git-token-{owner_name}`, declared on `GithubLike`) reaches the rendered text, so what is guarded is template propagation rather than the sentence wrapped around it. |
| D-052 | `cli/tests/manifests/test_reference.py::test_a_reference_field_says_what_it_names` | rendered-line substring | keep | `"names a vm-template"` is `f"names a {kind}"` where the kind comes from the fixture's `ResourceRef`; the only load-bearing token is the fixture-supplied kind, so this is value identity. Trim to the kind if the lead wants it tighter. |
| D-053 | `cli/tests/manifests/test_reference.py::test_a_nested_block_renders_as_a_block,test_a_collection_of_blocks_renders_one_element_under_a_placeholder` | rendered YAML-structure pins | keep | These pin generated document STRUCTURE (a nested block opens as a block; a sequence element has no key, which `-:` would silently change into a different document; a table element hangs under `<key>`), all derived from fixture models. Structural presence, and the sequence/mapping distinction is a real defect this caught. |
| D-055 | `cli/tests/manifests/test_reference.py::test_an_uncommented_required_list_of_tables_loads_and_validates` | uncomment-and-validate | keep | Derivation-and-behavior: the skeleton is stripped by the documented rule, parsed as YAML, and validated against the model. Pins no wording and is the module's headline promise. |
| D-056 | `cli/tests/manifests/test_reference.py::test_an_enum_default_renders_as_the_value_a_document_carries` | rendered-line pin of a fixture enum default | keep | `layout: vertical` is the fixture's declared default; the guarded defect (an enum MEMBER reaching pyyaml and taking the document down with a `RepresenterError`) is a real crash, not a wording question. |
| D-057 | `cli/tests/manifests/test_reference.py::test_one_arm_is_rendered_and_the_rest_are_listed` | authored "One of / Shown here" sentence plus structure | convert | Keep :288 and :289 (exactly one arm is written into the document, and the other arm is not). Replace :287 with a derived check that every `alternatives` name from the record appears in the rendered text, which preserves "the operator is told the other arms exist" without pinning the sentence template. Not redundant with :292-297, which asserts the record only. |
| D-058 | `cli/tests/manifests/test_reference.py::test_the_rendered_arm_is_the_first_registered_one,test_an_alternative_gets_an_address_only_when_the_address_exists,test_a_table_of_tagged_blocks_hangs_its_element_under_a_placeholder_key,test_the_tree_offers_every_element_arm_the_stream_does,test_a_tagged_collection_arm_reachable_from_itself_stops_rather_than_recurring,test_two_sibling_fields_sharing_an_arm_each_expand_it` | record-based assertions | keep | Assert `rendered`, `alternatives`, `target`, `recurring`, `fields` and cross-check the tree against the field-doc stream. Structural, derived, and the model the rest of this file should be converted toward. |
| D-059 | `cli/tests/manifests/test_reference.py::test_a_collection_of_tagged_blocks_renders_an_element_naming_its_arms` | authored "One of / Shown here" sentence plus structure | convert | Same as D-057: keep `#  nodes:`, `#    -`, `#      name: group` (generated structure, fixture values), replace the sentence pin with the derived arm-name presence check. The placeholder sentence at :495 goes with D-054. |
| D-061 | `cli/tests/manifests/test_reference.py::test_an_unexpanded_arm_does_not_name_what_is_shown` | absence assertion on a Python repr | keep | `"Shown here: None" not in flowed` is not a wording blacklist: it guards a Python `None` leaking into operator-facing text, the same defect class as `test_errors.py:214`. What regresses is a renderer branch, not a sentence. |
| D-062 | `cli/tests/manifests/test_reference.py::test_a_nested_tagged_union_expands_every_arm_because_none_is_addressable` | docstring pin of a shipped model | delete | `"Run limactl on another host over SSH."` is `SshPlacement`'s own docstring maintained a second time inside a test. The structural half (:665-670: arm names, targets, per-arm field lists) carries the invariant and survives. |
| D-063 | `cli/tests/manifests/test_describe_kind.py::test_a_kind_shows_its_prose_then_its_two_blocks` | title, summary and overview pins | convert | Keep :132-133 (the two blocks are present, structural). :127's `startswith` is half derived (target, category) and half authored title; :130-131 are `topics.py` prose maintained twice. Convert to a check that the rendered head carries the kind's own registered title/summary read from the registry, which is derivation parity rather than a second copy. |
| D-064 | `cli/tests/manifests/test_describe_kind.py::test_a_field_carries_its_type_requiredness_and_description` | rendered-line pins of shipped field docs | convert | The invariant worth keeping is that the type/requiredness/example parenthetical is rendered from the model (`e.g.` values as YAML, not Python `repr`), and `FieldEntry`/`doc` carry all of it. Assert against the record for the same field. :142 is the shipped `hint` description maintained twice and simply goes. |
| D-065 | `cli/tests/manifests/test_describe_kind.py::test_secret_source_kind_describes_the_backend_union_and_override_provenance` | shipped capability descriptions pinned three times | delete | `"env-var: resolves from ..."`, `"prompt: ..."`, `"onepassword: ..."` are the shipped backends' `description` ClassVars restated in a test, and :150 is a sentence from `topics.py`. That every registered backend is named at all is worth keeping and is a derived check (see D-066); the descriptions are not. |
| D-067 | `cli/tests/manifests/test_describe_kind.py::test_an_optional_root_union_keeps_its_outer_null_spelling` | rendered parenthetical pin | delete | **Recipe verification, 2026-08-16.** **The recorded conversion is not bitable and the row therefore becomes a delete.** The pure-record form (`doc.required is False`, `NoneType in get_args(doc.annotation)`) reads the declaration straight off the model, so it restates what it claims to check and no break makes it fail. Pairing it with a rendered check would reintroduce the message assertion this sweep is removing. Nothing real is lost: the declaration is the source of truth and mypy carries it. |
| D-068 | `cli/tests/manifests/test_describe_kind.py::test_a_field_that_folds_a_scalar_offers_both_spellings` | six rendered-line pins over a shipped model | convert | The arm summaries at :170 and :172 are shipped docstrings maintained twice and go. The field/type/requiredness lines are structured facts of `vm-template`'s `env` field; assert them on the record, which `test_reference.py` already does for the same shape. |
| D-069 | `cli/tests/manifests/test_describe_kind.py::test_secret_backend_describes_its_source_config,test_tag_only_secret_backend_source_config_is_a_table` | rendered parenthetical pins of shipped constraints | delete | **Corrected 2026-08-19 (map re-baseline).** The "or delete" is closed toward delete. `min length 1`, `default 30.0` and `gt 0` restate constraints the model declares, and each half that could regress is held elsewhere by construction rather than by a second copy. Constraint EXTRACTION is generic: `_constraints_in` (`cli/agentworks/schema/fields.py:481-491`) loops over the `_CONSTRAINT_KEYS` tuple at `:70-77`, which contains `gt`, so a spelling asserted for one key is asserted for all of them, and `cli/tests/schema/test_fields.py:361-366,479-488,491-515` asserts that loop on `min_length`, `max_length`, `ge`, `le` and `pattern`. Constraint RENDERING is equally generic: `cli/agentworks/manifests/describe.py:188-189` iterates `entry.doc.constraints.items()` with no per-key branch. The default is the one half that renders on its own branch (`describe.py:180-185`), and `worth_showing(default)` is exercised across this file's neighboring rows. What is left on these lines is the spelling of the parenthetical, whose only failure mode is a rewording. |
| D-070 | `cli/tests/manifests/test_describe_kind.py::test_a_root_value_that_may_be_a_table_says_what_is_in_the_table,test_a_scalar_root_value_has_no_table_form` | rendered-line pins over test fixtures | keep | `StringOrTableRoot` / `StringRoot` are fixtures, and the asserted lines restate their declared fields; :208's `"as a table:" not in text` is the load-bearing half (a scalar-only root must not advertise a table form), a structural absence rather than a wording blacklist. |
| D-071 | `cli/tests/manifests/test_describe_kind.py::test_a_capability_kind_is_an_index_of_implementations,test_a_host_kind_lists_every_arm_it_could_hold` | implementation-index pins | convert | Keep "the index lists every registered implementation", derived from `registered_implementations(kind)` rather than spelling `lima` / `proxmox` and their shipped one-liners at :223, :269-270. The description text is authored prose maintained twice. |
| D-072 | `cli/tests/manifests/test_describe_kind.py::test_an_implementation_shows_the_config_it_declares` | rendered parenthetical pin | convert | **Recipe verification, 2026-08-16.** Conversion holds, with one correction to its detail: `FieldDoc.default` is the model instance `LimaLocalPlacement(mode='local')`, not the literal dict the row implied, so the record assertion must route through `agentworks.manifests.yaml_value.render_value`, the same function `describe._facts_of` uses. No production change; just do not write the literal dict. |
| D-073 | `cli/tests/manifests/test_describe_kind.py::test_a_nested_tagged_union_renders_every_arm_with_its_own_fields` | six rendered-line pins plus one absence | convert | :246 (`endswith` a shipped docstring) and :241-242 (shipped arm summaries) are second copies. :243-245 (each arm shows its OWN `mode` const and `ssh`'s required `host`) and :248 (`"vm_host" not in text`, a retired field must not resurface) are real; both are expressible on the arm records, which this file's own :273-305 does. |
| D-074 | `cli/tests/manifests/test_describe_kind.py::test_a_nested_union_arm_summary_reaches_the_terminal_as_plain_text` | markdown-normalization pin | keep | `"``" not in text` guards that RST double-backticks are normalized before reaching a terminal, which is a transform, not a wording choice; the `- ambient:` half is the arm whose summary legitimately carries code spans. Trim the sentence tail if the lead wants it narrower, but the transform guard earns its place. |
| D-075 | `cli/tests/manifests/test_describe_kind.py::test_every_arm_this_surface_names_is_one_it_also_answers_for` | record walk over every describable target | keep | The file's best test and the pattern the rest should follow: every named arm is answered for by an address, its fields, or a recurrence marker, asserted over the record across every registry target, so a NEW union nobody wrote a case for fails. |
| D-076 | `cli/tests/manifests/test_describe_kind.py::test_a_capability_kind_summary_does_not_enumerate_its_implementations` | absence assertion on a summary | convert | The invariant (a capability summary must not carry a second enumeration that can go stale beside the live registry list) is real. `"(" not in summary` is a crude wording proxy for it; assert instead that no registered implementation name appears in the summary line, derived from `registered_implementations`. Keep :333's `"codex" in text` only as the non-vacuity guard it is, or derive it too. |
| D-077 | `cli/tests/manifests/test_describe_kind.py::test_a_capability_no_config_enables_still_documents_itself` | rendered-line pins of test-fixture text | keep | `"a fixture platform no config opts into"` (:63) and `"Where this fixture platform creates its VMs."` (:56) are declared by this file's own fixture models, and `region (string, optional, default westus2)` restates the fixture field. Value identity proving a capability documents itself with no config and no registry, which is this surface's whole promise. |
| D-078 | `cli/tests/manifests/test_describe_kind.py::test_a_collection_of_tagged_blocks_names_its_arms_and_expands_one,test_each_element_arm_shows_that_arms_own_fields` | rendered-line pins of test-fixture text | keep | Same: `LocalDisk` / `NetworkDisk` and their docstrings are defined at :37-58 of this file, so every asserted string is test-injected. The guarded defect (a tagged collection rendered as an opaque "list of table", and `volume` documented nowhere) is structural. |
| D-079 | `cli/tests/manifests/test_describe_kind.py::test_a_boolean_warns_that_quoted_strings_are_invalid` | required-phrase list plus wording blacklist | delete | `'Write booleans unquoted; quoted strings such as'`"no"`'are invalid.'` asserted across ten targets is a required-phrase list, and `"used to" not in entry` is a wording blacklist that rejects only the phrasings someone already thought of. The behavior the sentence describes is separately and properly guarded by :415-432, which loads a real manifest both ways. |
| D-080 | `cli/tests/manifests/test_describe_kind.py::test_the_quoted_boolean_warning_describes_this_loader,test_the_quoted_boolean_warning_describes_this_loader::ConfigError::96ba2c` | loader behavior for bare versus quoted booleans | keep | The observational twin of D-079: a bare `no` decodes to `False` and a quoted `"no"` is refused. Behavior at the loader boundary, no wording involved. |
| D-083 | `cli/tests/manifests/test_describe_kind.py::test_the_command_completes_its_argument` | completion-registration assertion | keep | Structural: the command's argument has a completer registered, read from `DYNAMIC_COMPLETIONS`. Not prose. |
| D-084 | `cli/tests/manifests/test_error_framing.py::test_every_manifest_error_frames_its_path_through_the_shared_helper` | framed-prefix plus absolute-path absence, over a refusal corpus | keep | A redaction defense in the charter's exact sense: no manifest error may show an operator an absolute path, asserted as a derived home-relative prefix plus `str(home) not in message`. Nothing authored is pinned. |
| D-085 | `cli/tests/manifests/test_error_framing.py::test_the_advisory_channel_frames_the_same_way` | same defense on the advisory channel | keep | Same invariant on the non-raising warning channel, which the refusal sweep cannot reach. |
| D-086 | `cli/tests/manifests/test_error_framing.py::test_an_unreadable_file_names_the_file_and_no_line` | framed-prefix pin plus `:0` absence | keep | `"not valid UTF-8"` rides along, but the assertion's substance is the `line == 0` sentinel rendering the file without a line no editor has. Trim the message tail from :187 if the lead wants it minimal; the `":0" not in message` half is the guard. |
| D-087 | `cli/tests/manifests/test_error_framing.py::test_a_yaml_error_without_a_problem_mark_names_the_file_and_no_line` | whole-message equality | convert | Same sentinel on the other read path, but asserted as a full authored message. Convert to the sibling's shape at :187-188 (framed prefix plus `":0" not in`), keeping the pyyaml-supplied `"boom"` if the propagation matters. |
| D-088 | `cli/tests/manifests/test_error_framing.py::test_the_duplicate_error_frames_both_locations` | whole-message equality | convert | The stated invariant is that BOTH locations are present and neither is absolute, and that a later "fix" cannot satisfy the sweep by dropping the inline one. Assert both framed paths appear; drop the sentence between them. |
| D-089 | `cli/tests/manifests/test_emit.py::test_the_capability_union_replaces_the_open_block_at_the_host_field` | shipped description pinned in a test | convert | The invariant (field descriptions reach the emitted schema) is real and structural; derive the expectation from the model's own field description rather than restating it. Everything else in this test (:383-388, discriminator name, mapping set, absent `CapabilityBlock`) is already structural and stays. |
| D-090 | `cli/tests/manifests/test_emit.py::test_the_modeline_is_relative_to_the_manifest` | modeline equality | keep | The `# yaml-language-server: $schema=` directive is an external editor extension's format, the sanctioned exception, and the path halves are composed from `SCHEMA_DIRNAME` / `ENVELOPE_SCHEMA_FILENAME` rather than spelled. |
| D-091 | `cli/tests/manifests/test_emit.py` | n/a | keep | Not inventoried: C10's finding against this file's 22 load cycles is **withdrawn**, and they are the loader half of a two-parser soundness pairing. I read the file only for prose assertions and found none beyond D-089 and D-090. |
| D-093 | `cli/tests/manifests/test_loader_and_envelope.py::test_duplicate_across_files_cites_both_locations` | phrase pin plus filename presence | delete | Drop :92 (`"duplicate secret"`); keep :93-94, which assert both files are cited and are value identity on names the test wrote. The duplicate refusal's framing is separately guarded by D-088. |
| D-094 | `cli/tests/manifests/test_loader_and_envelope.py::test_secret_name_over_secret_cap_errors` | phrase pin plus derived cap | delete | **Corrected 2026-08-19 (map re-baseline).** Range narrowed from `:115-127`. Drop `:125` (`"is too long"`). Keep `:126` (`f"max {MAX_SECRET_NAME_LENGTH}"`, derived from the production constant, so it catches the wrong cap being reported, which is the defect this test names) and `:127` (`"a.yaml:2"`, the location). |
| D-095 | `cli/tests/manifests/test_loader_and_envelope.py::test_an_advisory_carries_the_documents_location` | advisory sentence pin | convert | **Corrected 2026-08-19 (map re-baseline).** Was delete over `:433-453`, which is not executable as written: the location prefix the justification keeps and the authored tail it drops are ONE assertion at `:453`, so the row is a conversion, not a deletion. Replace `"a.yaml:2: vm-template/t1.env sets agentworks-managed identity variable" in manifests.issues[0]` with `manifests.issues[0].startswith("a.yaml:2: vm-template/t1.env")`, which keeps the navigability invariant the docstring names and drops the advisory sentence. The count at `:452` is untouched. No production change. |
| D-097 | `cli/tests/manifests/test_capability_shape.py` | n/a | keep | Inventoried and nothing found in my lane. C10's `:21-32` anchor is stale: at HEAD those lines are the `_load_one` helper and a parametrize opening. This file's message assertions are all `match=` sites (:154, :194, :261), which belong to group 1, and :307's `"session-template/htop" in result.output` is value identity on a resource the test declared. Raising it so the lead knows C10's third bullet resolves to group 1, not to this sweep. |
| D-098 | `cli/tests/manifests/test_spec_apt.py::test_an_unknown_key_names_the_fields_that_are_valid` | framed whole-message equality | delete | `rejection()` returns the rendered string and `ConfigError` carries no structured problems, so preserving "which field" would need a production handle, which R2.2 forbids. Behavior (`ConfigError` raised) survives via `rejection`'s own `pytest.raises`. R2.4 deletion. |
| D-099 | `cli/tests/manifests/test_spec_apt.py::test_a_missing_required_field_says_it_is_required` | framed multi-line batch equality | convert | The batch structure (four problems, one line each, under one located header) is the D-013 invariant at the decode layer and is worth keeping as counts plus indentation; the four `is required` tails are authored. |
| D-100 | `cli/tests/manifests/test_spec_apt.py::test_a_bare_string_where_a_list_belongs_is_refused` | framed whole-message equality | delete | Same family as D-098; the behavioral claim (a bare string is not wrapped into a list) is the raise plus the absence of a decoded row, not the sentence. |
| D-101 | `cli/tests/manifests/test_spec_apt.py::test_a_source_file_with_a_path_separator_is_refused` | framed whole-message equality carrying a pattern | convert | Keep the pattern itself (`^[a-zA-Z0-9][a-zA-Z0-9._-]*$` is the production constraint, and a `source_file` reaching a shell command is a real boundary) by asserting the field's declared pattern appears; drop the framing sentence. Same conversion as D-031. |
| D-102 | `cli/tests/manifests/test_spec_apt.py::test_a_description_written_in_spec_is_sent_to_metadata` | framed whole-message equality | delete | `"description belong(s) in metadata, not in spec"` is an authored steer with no structural handle. |
| D-104 | `cli/tests/manifests/test_spec_hosts.py::test_a_table_with_no_tag_reads_as_a_missing_field` | per-kind derived message comparison | convert | Not a spelled pin: the expected string is built from the loop's `kind`/`name`/`field`, so what is asserted is cross-kind UNIFORMITY of the missing-tag refusal, which is a real invariant a new host kind can break. Convert to comparing each kind's message against one kind's actual message with the identifiers substituted, so the uniformity survives and the authored tail is no longer spelled. |
| D-105 | `cli/tests/manifests/test_spec_hosts.py::test_a_non_table_reads_as_a_table_requirement` | per-kind derived message comparison | convert | Same conversion as D-104 for the non-table case. |
| D-106 | `cli/tests/manifests/test_spec_hosts.py::test_a_host_that_requires_a_capability_says_so` | per-kind derived message comparison | convert | Same conversion as D-104 for the required-capability case, including the deliberate `session-template` exclusion. |
| D-107 | `cli/tests/manifests/test_spec_hosts.py::test_a_top_level_token_keeps_its_steer` | framed whole-message equality | delete | The "'token' is provider config now" steer exists only as authored prose; the raise is the behavior. R2.4. |
| D-108 | `cli/tests/manifests/test_spec_hosts.py::test_a_site_named_after_a_platform_must_declare_it` | framed whole-message equality | delete | The shadowing rule is real and the refusal survives as `ConfigError`; the sentence explaining it does not need a test. |
| D-109 | `cli/tests/manifests/test_spec_hosts.py::test_a_site_name_takes_the_freeform_cap` | cap boundary, `endswith` on the framed tail | convert | The cap arithmetic (accepted at the cap, refused one over, using the freeform constant) is a real threshold and is asserted from `MAX_FREEFORM_NAME_LENGTH`; keep the boundary pair and drop the authored tail from the `endswith`. |
| D-110 | `cli/tests/manifests/test_spec_hosts.py::test_a_non_conforming_secret_inside_the_block_is_warned_about` | advisory `startswith` pin | convert | Keep the location prefix and the offending secret name (`'Bad_Name'`, written by the test); drop the authored explanation after it. Same shape as D-095. |
| D-111 | `cli/tests/manifests/test_spec_secret.py::test_a_missing_description_says_it_is_required,test_an_empty_description_is_refused_too` | framed whole-message equality | delete | Missing and empty `description`; both are the authored normalization family (D-003, D-030) at the decode layer. |
| D-112 | `cli/tests/manifests/test_spec_secret.py::test_a_non_table_backend_mappings_says_table` | framed whole-message equality | delete | Same family (a non-table `backend_mappings` says table). |
| D-113 | `cli/tests/manifests/test_spec_secret.py::test_a_non_conforming_declared_name_is_refused,test_the_secret_cap_is_the_larger_one` | naming refusal and cap boundary | convert | The cap pair keeps its threshold assertion derived from `MAX_SECRET_NAME_LENGTH` (this is the cap that differs from the freeform one, which is the point); the `startswith`/`endswith` authored tails go. |
| D-114 | `cli/tests/manifests/test_spec_admin_and_console.py::test_an_unknown_layout_lists_the_ones_that_exist` | framed message pin listing valid layouts | delete | **Corrected 2026-08-19 (map re-baseline).** The "or delete" is closed toward delete, and the row is narrowed: it read `:33-38` and swept up two lines that keep. Drop `:36` alone, the framed sentence prefix (`message.startswith("res.yaml:7: named-console-template/default.tmux_layout: must be one of: ")`). Its two halves are covered elsewhere on D-003's reasoning: the authored normalization ("must be one of") is the same family D-003 deletes as a paraphrase of a pydantic error type, and the located address prefix is re-asserted by the path-rendering section D-003 names, so neither half is this row's only probe. Keep `:37-38`: the loop over `VALID_TMUX_LAYOUTS` is derived from the production constant, so it proves the refusal enumerates the layouts actually in force, which is the same shape as G1-K16 and is not a rewording. The `choices` half is additionally structural at `:47`, inside `test_the_layouts_reach_the_explain_surface_as_choices` (`:41-47`). |
| D-115 | `cli/tests/manifests/test_spec_admin_and_console.py::test_a_bad_mise_package_says_what_the_syntax_is,test_a_bad_mise_install_before_says_what_it_accepts` | framed whole-message equality | delete | Two authored "says what the syntax is" messages from field validators. Sentence-only observables. |
| D-116 | `cli/tests/manifests/test_spec_admin_and_console.py::test_a_bad_mise_lockfile_carries_the_source_refs_own_message` | authored-phrase substring | delete | `"mise_lockfile is invalid: "` is our own framing; the refusal is the behavior. |
| D-117 | `cli/tests/manifests/test_spec_admin_and_console.py::test_an_unknown_admin_key_names_the_fields_that_are_valid` | framed whole-message equality | delete | Unknown-key field list at the decode layer, same as D-098. |
| D-118 | `cli/tests/manifests/test_spec_install_commands.py::test_a_missing_command_says_it_is_required` | per-kind derived message comparison | convert | Same shape and conversion as D-104: the expected string is built from the loop's kind, so the invariant is cross-kind uniformity of the required-field refusal. |
| D-119 | `cli/tests/manifests/test_spec_install_commands.py::test_a_bare_test_key_keeps_its_remedy,test_an_unknown_key_names_the_fields_that_are_valid` | framed whole-message equality | delete | A remedy sentence and an unknown-key field list; both authored, both leaving `ConfigError` behind. |
| D-120 | `cli/tests/manifests/test_spec_install_commands.py::test_a_non_string_command_is_no_longer_coerced` | framed whole-message equality | delete | "no longer coerced" is proven by the raise; the message is authored normalization. |
| D-121 | `cli/tests/manifests/test_spec_templates.py::test_a_quoted_integer_is_no_longer_coerced` | framed whole-message equality, two inputs | delete | Both a quoted integer and a bool must be refused rather than coerced; the two raises carry that and the identical spelled message carries nothing extra. |
| D-122 | `cli/tests/manifests/test_spec_templates.py::test_a_non_string_auth_key_is_refused` | framed whole-message equality | delete | Same family. |
| D-123 | `cli/tests/manifests/test_spec_templates.py::test_a_non_conforming_auth_key_is_warned_about_and_kept` | advisory `startswith` plus kept-value assertion | convert | Keep :76 (the non-conforming key is warned about and still decoded, which is the behavioral claim) and the location prefix; drop the authored tail. |
| D-124 | `cli/tests/manifests/test_spec_templates.py::test_a_bad_mise_package_says_what_the_syntax_is` | framed whole-message equality | delete | Same as D-115. |
| D-125 | `cli/tests/manifests/test_spec_templates.py::test_a_key_that_never_did_anything_is_now_refused` | per-field derived `startswith` comparison | convert | The expected prefix is built from the loop's field name, so this is the uniformity family (D-104): every retired key is refused the same way. Convert to a substituted-identifier comparison. |
| D-126 | `cli/tests/manifests/test_spec_templates.py::test_an_empty_auth_key_is_refused_rather_than_overriding_the_default` | framed whole-message equality | delete | An empty auth key is refused rather than silently overriding the default; the raise is the behavior and :198-216's sibling asserts the resolved-template consequence structurally. |
| D-127a | `cli/tests/manifests/test_spec_workspace_template.py::test_an_env_entry_with_neither_source_field_is_refused_as_one_problem,test_an_env_entry_with_both_source_fields_is_refused_as_one_problem,test_a_malformed_selected_env_arm_keeps_only_its_real_error` | union-collapse cardinality behind a whole-message equality | convert | **Ruled by the lead 2026-08-16**: cardinality is a structural claim about output shape, it is cheap, and it fails when the collapse actually breaks. These are the three sites that carry it, and their own names say so (`..._refused_as_one_problem` twice, `..._keeps_only_its_real_error`). Convert each to a problem count plus the offending path, following the shape D-026 already converts to; drop the message text. |
| D-127b | `cli/tests/manifests/test_spec_workspace_template.py::test_an_unknown_key_names_the_fields_that_are_valid,test_an_env_key_that_is_not_a_variable_name_is_refused,test_an_env_entry_that_is_neither_shape_is_refused,test_an_env_entry_with_an_unknown_inner_key_is_refused,test_a_bare_string_where_the_inherits_list_belongs_is_refused` | framed whole-message equality | delete | The other five sites of the same family, verified individually: none carries a cardinality claim. Each pins one authored normalization (unknown-field alternatives, the env-var-name regex, `must be a table`, an unknown inner key, `must be a list`), and the refusal itself survives on the raise. |
| D-128 | `cli/tests/manifests/test_spec_workspace_template.py::test_an_agentworks_prefixed_key_is_warned_about,test_a_newline_in_a_plaintext_value_is_warned_about,test_a_non_conforming_secret_name_is_warned_about` | advisory `startswith` pins | convert | Keep the location prefix and the test-written identifiers (`Bad_Name!`, the prefixed key, the newline-bearing value); drop the authored explanations. Same shape as D-110. |
| D-129 | `cli/tests/manifests/test_spec_workspace_template.py::test_a_clean_document_earns_no_advisories` | advisory absence | keep | "A clean document earns no advisories" is a structural emptiness assertion. |
| D-130 | `cli/tests/capabilities/test_conformance.py::test_a_capability_name_that_is_not_a_registry_key_is_refused` | authored-phrase pin plus injected-value pin | convert | Keep :180 (`repr(name) in reason`, the bad name the test supplied) and the `is not None`; drop :181 (`"'name' class attribute"`). This rule has no sub-checker to call, so the family discrimination goes with it, which R2.4 accepts. |
| D-131 | `cli/tests/capabilities/test_conformance.py::test_a_config_model_that_cannot_be_built_is_refused` | authored-phrase pin | delete | The structural discriminator is already asserted in this test: `not model_is_complete(Unbuildable)` at :202 and `reason is not None` at :204. `"cannot be built"` adds only our wording on top of a branch that is already pinned two lines up. |
| D-132 | `cli/tests/capabilities/test_conformance.py::test_a_marker_nested_below_the_config_model_is_refused_too` | reason substring naming a fixture field | keep | `MarkedBlock.credentials` is defined by the test; the diagnostic naming the offending field is value identity and the actionable half. |
| D-133 | `cli/tests/capabilities/test_conformance.py::test_overlapping_structural_arms_are_refused_at_registration_without_markers` | authored-phrase pin plus fixture-identifier pin | convert | Drop :330 (`"invalid structural union"`, `conformance_error`'s own framing); keep :331 (`"overlapping arms First and Second"`, test-defined classes). Add `structural_union_error(Overlapping) is not None` to keep the family discriminated. **Verified by execution:** `structural_union_error` answers for this model while `union_scalar_shorthand_error` and `reference_marker_error` both return `None`. |
| D-134 | `cli/tests/capabilities/test_conformance.py::test_a_discriminated_structural_union_is_refused_at_registration_without_markers` | two authored-phrase pins | convert | Neither phrase carries an injected identifier. Replace both with `structural_union_error(Selected) is not None` (verified non-None), which discriminates the family; within-family discrimination (overlapping versus discriminated) is wording-only and goes per R2.4. |
| D-135 | `cli/tests/capabilities/test_conformance.py::test_a_marker_on_a_structural_union_holder_is_refused_at_registration` | authored-phrase pin | convert | Same: replace `"selects a structural union arm"` with the specific checker call. The structural half of this test (:376-377, the marker shadows arm traversal so `extract_references` returns `()`) is the real invariant and already stands. |
| D-136 | `cli/tests/capabilities/test_conformance.py::test_an_aliased_structural_arm_is_refused_before_marker_placement` | authored-phrase pin plus fixture-identifier pin | convert | Drop :391; keep :392 (`_AliasedSecretArm.secret_value declares validation alias`, a fixture identifier). Add the sub-checker call for the family. |
| D-137 | `cli/tests/capabilities/test_conformance.py::test_a_marker_bearing_scalar_structural_arm_is_refused` | authored-phrase pin plus fixture-identifier pin | convert | **Recipe verification, 2026-08-16.** The recorded conversion named the wrong checker: executed at HEAD, `union_scalar_shorthand_error(...)` returns `None` for `_ScalarSecretHolder`, so the replacement would have passed without ever biting. The checker that actually answers is `reference_marker_error`. Corrected replacement: `assert reference_marker_error(_ScalarSecretHolder) is not None`, which passes on HEAD and fails under a dropped-marker break. All three checkers are public exports of `agentworks.schema`, so no production change either way. |
| D-138 | `cli/tests/capabilities/test_conformance.py::test_a_marker_no_walker_could_ever_read_is_refused` | authored-phrase pin | delete | **Disposition changed after reading HEAD:** the test already calls the specific sub-checker at :452 and asserts non-None at :453, so the family is discriminated without any wording, and :455 and :457 carry the rest. Verified by execution (experiment D) that `reference_marker_error` is the only one of the three that answers for this model. Nothing replaces `"cannot classify"` because nothing is lost. |
| D-139 | `cli/tests/capabilities/test_conformance.py::test_a_default_template_on_an_element_marker_is_refused` | authored-phrase pin | delete | Same as D-138: `reference_marker_error(TemplatedElements)` is called at :641 and asserted non-None at :642, so `"default template"` is surplus. The premise assertions above it (:634-639) are the behavioral core and stay. |
| D-140 | `cli/tests/capabilities/test_conformance.py::test_every_structural_union_on_the_shipped_surface_is_a_true_one_of,test_every_union_scalar_shorthand_on_the_shipped_surface_is_complete` | shipped-surface conformance sweeps | keep | `assert not faults` over every declarable kind's metadata and spec models, with the reasons only used to BUILD the failure report. Structural, and the non-vacuity guard at :289 keeps it honest. |
| D-141 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_a_structural_lookalike_is_rejected_before_its_members_are_considered` | **[subtracted: secrets-preview]** authored-phrase pin | convert | Rubric re-check, 2026-08-16. `:63` is the only assertion in its test and, because it reads `str(conformance_error(...))`, doubles as the non-None check, so unlike D-131 and D-138 there is no adjacent `is not None` to fall back on. Replace with an `is not None` assertion on `conformance_error`. No production change. |
| D-142 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_an_abstract_backend_is_rejected_as_not_constructible` | **[subtracted: secrets-preview]** authored-phrase `startswith` plus identifier | convert | Drop `reason.startswith("it is abstract")`; keep `"create_client" in reason`, which names the abstract member the test left unimplemented (value identity). |
| D-143 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_a_non_callable_factory_hits_the_generic_operation_check_first` | **[subtracted: secrets-preview]** authored framing plus injected operation name | convert | Keep `"create_client"` (the operation the test nulled out); drop `"required secret-backend operations: "`. |
| D-144 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_a_missing_required_class_attribute_is_rejected` | **[subtracted: secrets-preview]** whole-reason equality | convert | Keep `"interactive"` (the attribute the fixture omits); drop the sentence around it. |
| D-145 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_interactive_must_be_exactly_bool` | **[subtracted: secrets-preview]** whole-reason equality with an injected repr | convert | Keep `repr(value)` (the parametrized bad value) and the attribute name; drop `"is ... , not a bool"`. |
| D-146 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_every_secret_reference_shape_in_source_config_is_rejected` | **[subtracted: secrets-preview]** authored-phrase pin plus derived path | convert | Keep :252 (`path in reason`, where `path` is the parametrized structural address such as `root.tokens{value}`): that address is derived by the walker and is exactly what regresses. Drop :251 (`"references forbidden kind 'secret'"`), which is authored framing. |
| D-147 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_python_only_mapping_annotation_families_are_rejected` | **[subtracted: secrets-preview]** authored-phrase pin plus path fragment | convert | **Recipe verification, 2026-08-16.** Verified but thin: `"at root"` is the walk's zero value, a much weaker address than D-146's `root.tokens{value}` or D-148's `root.rows[]{value}`. Tighten it to the model's own address when converting, rather than accepting the zero value as the assertion. |
| D-148 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_a_non_string_mapping_key_is_rejected_with_its_nested_path,test_a_python_only_type_nested_in_a_container_is_rejected` | **[subtracted: secrets-preview]** whole-sentence pins carrying an address | convert | The addresses `root.lookup{key}` and `root.rows[]{value}` are the derived observables worth keeping (nested-path computation is the code under test); the surrounding wording is not. Keep the address substrings only. |
| D-149 | `cli/tests/capabilities/test_secret_backend_conformance.py::test_source_contract_operations_must_be_classmethods` | **[subtracted: secrets-preview]** whole-reason equality, parametrized | convert | Keep the injected `operation` name; drop `"must be declared as @classmethod"`. The parametrization already discriminates which operation was mis-declared. |
| D-150 | `cli/tests/capabilities/test_capability_config.py::test_a_bad_field_is_owner_framed_and_located` | whole-message equality | convert | Same family as D-008: the invariant the docstring names is that the bridge owns the framing and the finalize pass adds none of its own, which is a prefix-and-shape fact. Assert the framing, drop the normalized tail. |
| D-151 | `cli/tests/capabilities/test_capability_config.py::test_an_unregistered_name_is_rejected_by_the_union_naming_what_is_registered` | `startswith` plus registered-name presence | convert | Keep `"'fixture-platform'" in message` (the seated fixture's own name, value identity, and the proof the assembled union dispatches on the tag); drop the authored `"unknown name 'nope'; registered: "` framing, keeping only the `'nope'` echo if the lead wants the input reflected. |
| D-152 | `cli/tests/capabilities/test_declare_once_end_to_end.py::test_the_rendered_sample_offers_the_capability_as_an_alternative,test_no_sample_mentions_the_capability_when_it_is_not_seated` | presence and absence of a fixture name in generated text | keep | `"declare-once"` is the seated fixture platform's own name; the paired presence/absence assertions prove the sample renders from the live registry rather than from a file. Value identity, and the absence half is not a wording blacklist but a seating-state fact. |
| D-153 | `cli/tests/capabilities/test_declare_once_end_to_end.py::test_the_field_reference_reads_the_same_one_declaration,test_the_field_reference_makes_the_same_templated_subtraction` | rendered-line pins built from fixture constants | keep | `REGION_DEFAULT` and `REGION_DESCRIPTION` are module constants the fixture declares, and the test asserts the record (`region.doc.default`, `region.doc.description`) beside the rendering. This is declared-once/rendered-everywhere parity, which is exactly the derivation-parity keep shape. The `defaults to 'declare-once-<name>'` fragment at :272 is the one authored tail here and could be trimmed to the fixture's template. |
| D-154 | `cli/tests/capabilities/test_capability_descriptors.py` | n/a | keep | Inventoried and nothing found: the assertions are registry/descriptor structure, non-vacuity guards, and cache identity. No authored-prose pins. |
| D-155 | `cli/tests/schema/test_fields.py::test_a_models_own_description_is_the_first_docstring_paragraph,test_each_arm_carries_its_own_identity,test_each_element_arm_carries_its_own_identity` | description equality against fixture docstrings | keep | Every asserted string is a docstring on a model this file or `_fixture_models.py` defines, so these prove docstring-to-`FieldDoc` propagation (including the two-source-line wrap case). Value identity on test-injected text. |
| D-156 | `cli/tests/schema/test_base.py::test_attribute_docstrings_become_descriptions,test_an_explicit_description_wins_over_the_docstring` | description equality against fixture docstrings | keep | Same: the `AgwModel` base's docstring-to-description behavior, asserted with strings the test declares. |
| D-157 | `cli/tests/manifests/test_accepted_type_parity.py` | n/a | keep | Inventoried and nothing found. These assert reference-edge identity, JSON-schema shape, fill behavior, marker payloads, per-kind parity and modeline paths composed from production constants. No authored-prose or authored-form pins in my lane. |
| E-003 | `cli/tests/resources/test_graph.py::test_chain_edges_dependents_and_reachability` | edge tuple equality incl. usage | keep | `usage="c needs d"` is defined by this test's own fixture; the assertion proves the edge's source and usage survive the graph round trip. |
| E-004 | `cli/tests/resources/test_graph.py::test_full_incoming_edges_preserve_authoritative_parallel_facts` | usage-list equality | keep | All three usage strings are injected by this file (lines 135, 141, 148); the invariant is that parallel incoming edges are retained in order, not that any sentence reads a certain way. |
| E-010 | `cli/tests/resources/test_readiness_fold.py::test_not_ready_platform_dependency_propagates_its_reason` | reason equality against an injected reason | keep | Invariant (R9.1): a site passes the platform's verdict through without re-wrapping. The reason string is the one this test handed to `Readiness.blocked`, so equality is pass-through identity. |
| E-011 | `cli/tests/resources/test_readiness_fold.py::test_disabled_platform_node_folds_end_to_end_to_enable_its_unit` | authored reason equality | convert | Rubric re-check, 2026-08-16. `:170` is the ONLY assertion in `test_disabled_platform_node_folds_end_to_end_to_enable_its_unit`; the structural siblings the row cited are not in that test. Replace with a `readiness_of(...).is_ready` assertion off the graph. No production change. |
| E-012 | `cli/tests/resources/test_readiness_fold.py::test_typo_in_the_placement_host_is_refused_rather_than_changing_readiness` | required-phrase pair plus a wording blacklist | delete | `"placement.hst: unknown field"` / `"placement.host: is required"` are authored validation prose and `"limactl" not in message` is a blacklist; the surviving `pytest.raises(ConfigError)` carries the real claim that a typo is refused rather than silently changing readiness. |
| E-013 | `cli/tests/resources/test_readiness_fold.py::test_not_ready_is_total_over_malformed_config` | reason-set membership | convert | The invariant is totality (`not_ready` returns rather than raising over malformed config). Replace the `in (None, "limactl not installed")` membership with `isinstance(LimaPlatform.not_ready(config), Readiness)`. No production change. |
| E-014 | `cli/tests/resources/test_reference_relationship.py::test_inherits_reference_marks_the_relationship_and_takes_the_kind_from_the_source` | authored usage-string equality | delete | The test's stated claim (relationship marked, kind taken from the source) is fully carried by lines 61-63; `usage == "a parent template"` restates a literal in cli/agentworks/resources/reference.py:230. |
| E-018 | `cli/tests/resources/test_template_kinds.py::test_template_dependencies_emits_template_requirement` | authored usage-string equality | delete | The parametrized claim is kind and source per inherited name (lines 131-132); the usage literal is production prose restated. |
| E-024 | `cli/tests/plugins/test_azure.py::test_operator_override_of_az_cli_wins` | value identity of an injected override | keep | Invariant: an operator-declared install-command overrides the disabled plugin row. `"echo operator-az"` is the value this test wrote into config, so the assertion proves precedence, not wording. |
| E-028 | `cli/tests/plugins/test_claude.py::test_operator_override_of_claude_install_wins` | value identity of an injected override | keep | Same invariant as E-024 for the claude install command. |
| E-032 | `cli/tests/plugins/test_codex.py::test_operator_override_of_codex_install_wins` | value identity of an injected override | keep | Same invariant as E-024. |
| E-034 | `cli/tests/plugins/test_enablement_producer.py::test_vm_site_on_disabled_plugin_platform_is_not_ready_with_enable_plugin,test_git_credential_on_disabled_plugin_provider_is_not_ready,test_git_credential_not_ready_falls_back_when_mark_absent` | authored reason equality | convert | Rubric re-check, 2026-08-16. All three sites are the only assertion in their test, so the delete empties three tests, and `:419-421` is the only probe anywhere that a git-credential on a disabled provider is not ready. Replace with `assert not verdict.is_ready`. No production change. |
| E-036 | `cli/tests/plugins/test_enablement_producer.py::test_second_stub_source_composes_through_finalize_and_precedence_holds` | enable-hint substring used as a precedence discriminator | delete | Redundant: `verdict.reason is not None` (line 558) plus `"stub-reason" not in verdict.reason` (line 560) already prove the plugin source won over the stub. |
| E-037 | `cli/tests/plugins/test_enablement_producer.py::test_second_stub_source_composes_through_finalize_and_precedence_holds` | negative value identity of an injected sentinel | keep | Invariant: source precedence, first source wins. `stub-reason` is the sentinel this test's stub emitted, so its absence is what proves the losing source did not supply the reason. |
| E-042 | `cli/tests/plugins/test_publish.py::test_not_enabled_plugin_row_and_manifest_present_but_disabled` | authored reason equality | delete | Redundant with the two structural assertions above it (row present-but-disabled, `enablement_of(...) is Enablement.disabled`). |
| E-043 | `cli/tests/plugins/test_publish.py::test_bad_plugin_manifest_anchor_raises_typed_plugin_attributed_error,test_plugin_manifest_anchor_without_subdir_raises_typed_plugin_attributed_error` | injected plugin name in an attributed error | keep | Invariant: a manifest-anchor failure is attributed to the plugin that caused it rather than escaping as a bare `ImportError`. `PLUGIN` is this file's own constant, so the assertion is attribution identity. |
| E-046 | `cli/tests/plugins/test_surfaces.py::test_list_hides_disabled_but_shows_not_ready_enabled` | injected not-ready reason substring | keep | The fixture capability supplies `"unsupported on this host (fixture)"`; the assertion proves the plugin-published row's readiness reason reaches the resource listing. |
| E-051 | `cli/tests/plugins/test_surfaces.py::test_roster_lists_enabled_ok_and_disabled_info` | value identity of the fixture's own description | keep | The fixture plugin declares `"the opted-in fixture plugin"`; the assertion proves the declared description reaches the roster row. |
| E-053 | `cli/tests/plugins/test_surfaces.py::test_roster_required_scopes_informational_when_set` | value identity of injected scopes | keep | The fixture plugin declares `required_scopes = ("system", "vm")`; the assertion proves declared scopes reach the roster. |
| E-055 | `cli/tests/plugins/test_proxmox_bootstrap_staging.py::test_success_stages_mode_0600_and_removes_verified_path` | exact guest command tuple | keep | Invariant: the staging file is removed AND its absence verified (`rm -f -- "$1" && test ! -e "$1"`) with the path passed as an argument rather than interpolated. This is the behavior, not a description of it. |
| E-066 | `cli/tests/plugins/gcp/test_auth.py::test_real_env_source_and_operation_resolver_deliver_exact_downloaded_json` | exact credential-factory input dict | keep | Invariant: the downloaded service-account JSON reaches Google's factory unmodified and without whitespace stripping, including the trailing newline and the private-key block. Every value is test-injected, and this is the security property the remediation hint below only describes. |
| E-073 | `cli/tests/plugins/gcp/test_instance.py::test_final_instance_body_is_key_free_and_pins_security_fields` | provider request-body field equality | keep | Invariant: the insert body carries the security-relevant GCE fields (no service account, `block-project-ssh-keys`, oslogin off, IPv4-only, the exact injected SSH key) and no key material. `"External NAT"` and `"ONE_TO_ONE_NAT"` are GCE's own vocabulary, not ours. |
| E-079 | `cli/tests/plugins/gcp/test_platform.py::test_combined_progress_precedes_readiness_and_fixed_stdin` | ordering pin over a recorded event log | convert | The ordering claim is real and observational, but two of its four legs are authored progress/output sentences. Keep the legs keyed on the imported production constants (`GCE_READINESS_COMMAND` before `TAILSCALE_JOIN_STDIN_COMMAND`) and drop the `progress:` and `output:` legs. No production change. |
| E-080 | `cli/tests/plugins/gcp/test_platform.py::test_combined_progress_does_not_claim_completion_when_readiness_fails` | negative event presence | keep | Invariant: when the startup-marker readiness check fails, the Tailscale auth key is never delivered over stdin. Keyed on a production command constant, not on prose. |
| E-095 | `cli/tests/vms/test_add_git_credential_orchestrated.py::test_orchestrated_add_writes_the_credential` | helper-command substrings | keep | Invariant: the credential helper is installed with `--replace-all credential.helper` and guarded by an executable test, which is what stops a stacked or duplicated helper. This is the command the VM runs, not a description of it. |
| E-098 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_success_uses_private_random_stages_and_removes_both` | exact guest cleanup command | keep | Invariant: the staging file is removed and its absence verified on the guest. Same family as E-055. |
| E-114 | `cli/tests/vms/test_create_vm_dispatch.py::test_create_vm_request_shape_and_row` | value identity of an injected key | keep | `request.ssh_public_key == "public ssh key"` proves the operator's key reaches the provision request; the value is the test's. |
| E-122 | `cli/tests/vms/test_lima_create_flow.py::test_no_restart_when_sentinel_absent,test_probe_failure_warns_and_does_not_restart` | command absence in a recorded call log | keep | Invariant: no restart is issued when the sentinel is absent or the probe fails. `limactl restart` is the tool's command, recorded from the fake, not our prose. |
| E-124 | `cli/tests/vms/test_lima_create_flow.py::test_submitted_lima_configuration_never_contains_tailscale_key` | exact sensitive-command list | keep | Invariant: the auth key is delivered only through the fixed stdin command and never appears in a rendered command line. Values are injected. |
| E-126 | `cli/tests/vms/test_lima_create_interrupt.py::test_local_create_streams_template_without_persistent_file,test_local_lima_sensitive_stdin_failure_omits_reflected_key,test_remote_lima_failure_omits_sensitive_input_and_raw_results,test_failure_mid_create_cleans_up_and_reraises,test_interrupt_during_start_cleans_up_and_reraises_the_original,test_interrupt_during_post_start_steps_cleans_up_too,test_interrupt_during_ephemeral_tailscale_join_cleans_up_and_does_not_render_key,test_ephemeral_tailscale_join_failure_cleans_up_without_key_in_error,L787` | **[line-anchored: between functions]** recorded `limactl` command equality | keep | These pin the commands actually issued to the tool (create over stdin, forced delete, no persistent template file), which is the rollback behavior itself. `limactl` syntax is upstream, not ours. Lines 202/228 pin an authored error sentence and should be trimmed to the typed raise. |
| E-129 | `cli/tests/vms/test_lima_remote_create_interrupt.py::test_remote_interrupt_kills_the_detached_limactl_before_deleting,test_remote_stdin_write_failure_removes_partial_template_and_preserves_error,test_remote_template_staging_is_mode_0600_stdin_and_verified_cleanup,test_remote_template_uses_private_random_directory_not_predictable_path,test_remote_template_rejects_untrusted_allocation_path,test_remote_interrupt_preserves_original_when_artifact_cleanup_fails,test_delete_op_issues_the_shared_teardown_command` | recorded remote command equality | keep | Invariant: the remote template is staged into a private random directory with `umask 077`, written over stdin, and removed with verification; the detached `limactl` is killed before delete. These are the commands executed on the remote host, and the predictable-path test proves a legacy path is not reused. |
| E-130 | `cli/tests/vms/test_lima_template.py::test_subuid_cap_step_present_and_first` | script-content substring | keep | Invariant: the subuid capability step is present and first in the generated template; `awk -F:` is the step's own content, and its absence would mean the step vanished. Weak but structural; a count/position assertion would serve as well. |
| E-131 | `cli/tests/vms/test_platform_config_contract.py::test_proxmox_no_longer_reads_a_string_verify_ssl_as_true` | authored remediation substring | delete | `"write it unquoted" in str(excinfo.value)` is our own hint text beside a `pytest.raises(ConfigError, match=...)` that group 1 owns. |
| E-132 | `cli/tests/vms/test_platform_config_contract.py::test_an_unknown_mode_names_the_modes_there_are` | message-prefix pin with a computed address | convert | The invariant worth keeping is the ADDRESS: the error names `vm-site/t.{field}` with no arm-tag segment spliced in. Narrow the `startswith` to the computed address prefix (`f"vm-site/t.{field}:"`) and drop the authored tail. No production change. |
| E-133 | `cli/tests/vms/test_platform_config_contract.py::test_an_unknown_mode_names_the_modes_there_are` | required-phrase list over the union's tags | delete | **Corrected 2026-08-19 (map re-baseline).** Premise corrected, disposition unchanged. The old reason ("a usability property expressed only in prose") was wrong: the loop varies per parametrize case. The reason it still deletes is that `_MODES` at `:419-424` is a second copy of the union's arms maintained BY HAND in the test, which is the shape `no-prose-policing-tests` names explicitly ("do not maintain a second copy inside a test"). Deriving the arms instead would need an accessor this pass may not add (R2.2), so the enumeration goes and E-132's computed address is what survives at `:374`. |
| E-134 | `cli/tests/vms/test_platform_config_contract.py::test_a_field_from_the_other_arm_is_refused,test_an_extra_field_inside_an_arm_is_refused` | message-prefix pin with a computed address | convert | Same as E-132: keep `f"vm-site/t.{field}.{stray}:"` and `f"vm-site/t.{field}.hst:"` as the address claim (this is the closed-arm invariant that actually bit lima), drop the `"; expected one of: ..."` tail. No production change. |
| E-135 | `cli/tests/vms/test_platform_config_contract.py::test_gcp_gce_returns_one_complete_service_account_secret_reference,test_aws_ec2_returns_the_secret_access_key_reference` | authored usage-string equality | delete | The extraction claims are `(ref.kind, ref.name)` on the adjacent lines; the usage sentence is production prose restated. |
| E-136 | `cli/tests/vms/test_platform_config_contract.py::test_azure_extraction_is_total_on_malformed_config,test_proxmox_extraction_matches_valid_config_extraction` | whole-`ConfigReference` equality including usage | convert | Both are edge-identity golden values. Compare `(kind, name)` (and emptiness) rather than the full dataclass, so the usage prose is not pinned. No production change. |
| E-137 | `cli/tests/vms/test_platform_idempotency_guards.py::test_lima_start_proceeds_when_stopped` | recorded command substring | keep | Invariant: a stopped lima VM is started (the idempotency guard does not swallow the start). `limactl start` is the tool's command from the recorded call log. |
| E-140 | `cli/tests/vms/test_platform_support.py::test_not_ready_reasons_chain,test_declared_site_on_unsupported_platform_registers_not_ready,test_resource_layer_surfaces_not_ready_state` | reason equality against an injected reason | keep | `"limactl not installed"` is the string `_support()` injected into `LimaPlatform.not_ready`; equality proves it propagates to the site verdict and to the rendered row unmodified. |
| E-141 | `cli/tests/vms/test_platform_support.py::test_not_ready_reasons_chain,test_r9_5_host_unsupported_platform_is_present_and_not_ready_on_the_graph,test_r9_5_host_unsupported_platform_renders_not_ready_row,test_declared_site_on_unsupported_platform_registers_not_ready,test_resource_layer_surfaces_not_ready_state` | reason equality mixing an injected token with an authored wrapper | convert | The injected half is `"Windows only"`; the wrapper `"platform 'wsl2' is unsupported here: ..."` is production prose. Replace with (a) the injected token present in the reason and (b) `site_verdict.reason == platform_verdict.reason`, which is the propagation invariant these six copies are really asserting. No production change. |
| E-142 | `cli/tests/vms/test_platform_support.py::test_declared_site_on_unsupported_platform_registers_not_ready,test_select_site_errors_with_reasons_when_none_ready` | injected reason substrings inside a raised error | keep | Invariant: refusing to use a not-ready site, and refusing to auto-select when none is ready, both surface the underlying platform reasons rather than a generic message. Both tokens are injected. |
| E-147 | `cli/tests/vms/test_shell_exec_orchestrated.py::test_exec_copied_workspace_pin_no_longer_crashes,test_exec_reachable_vm_is_one_boundary_burst,test_exec_double_dash_separator_strips_only_the_first,test_exec_double_dash_separator_preserves_an_adjacent_second,test_exec_bare_flag_after_command_word_still_works` | executed command equality | keep | Invariant: argv assembly, `--` separator handling (only the first is consumed), and the copied-workspace pin produce exactly these remote command lines. This is the behavior. |
| E-149 | `cli/tests/vms/test_sites_dispatch.py::test_resolve_site_unknown_raises_the_stranded_error,test_site_manifest_hint_carries_the_host_in_the_ssh_arm` | manifest-fragment substrings in a hint | convert | The real invariant is that the stranded-site hint is a VALID, current-schema vm-site manifest. Replace the fragment pins by parsing the hint as YAML and validating it through the manifest reader, asserting it yields a `vm-site` named `gpu-box` with the ssh placement (and the local arm for the no-host case). This is stronger than the pins and needs no production change. |
| E-153 | `cli/tests/vms/test_tailscale_logout_wording.py::test_deregistered_prints_inside_the_route_window,test_nullcontext_route_platform_output_is_unchanged` | recorded command substring | keep | `"tailscale down && tailscale logout"` is the command actually dispatched to the guest, recorded from the fake transport. |
| E-155 | `cli/tests/vms/test_tailscale_stdin_join.py::test_initializer_join_uses_fixed_stdin_once_then_updates_the_ip,test_readiness_exhaustion_raises_typed_without_delivering_key,test_cloud_init_wait_failure_raises_typed_without_delivering_key,test_default_readiness_contract_preserves_azure_and_aws_behavior,test_gce_readiness_runs_after_ssh_and_before_fixed_stdin_join,test_gce_readiness_timeout_is_typed_labeled_and_stops_before_stdin` | recorded call-log equality against production constants | keep | Invariant: the readiness command runs after SSH and before the fixed-stdin join, the auth key is never delivered when readiness fails, and the default/GCE readiness commands are the imported constants. Ordering asserted through a recorded call log, not through source or prose. |
| E-158 | `cli/tests/vms/test_vm_nodes.py::test_template_node_declares_the_key_and_the_sweep_predicts_it` | reference-tuple equality including usage | delete | `(ref.kind, ref.name)` carries the declaration; the usage sentence is production prose (the comment even says "usage prose included"). |
| E-159 | `cli/tests/vms/test_vm_site_kind.py::test_decode_nests_platform_config` | value identity of an injected description | keep | `"Dev subscription"` is the description this test's manifest declared; the assertion proves decode carries it through the nested platform block. |
| E-165 | `cli/tests/test_azure_create_interrupt.py::TestFailureDuringInlineWait.test_ssh_readiness_exhaustion_fails_closed_without_key_delivery,TestFailureDuringInlineWait.test_cloud_init_wait_failure_fails_closed_without_key_delivery` | recorded call-log equality | keep | Invariant: readiness exhaustion and a cloud-init wait failure fail closed with no `input_text` ever delivered. Commands are the production constants. |
| E-176 | `cli/tests/test_wsl2_create_interrupt.py::_assert_teardown_ran,test_delete_op_issues_the_shared_teardown_sequence` | recorded `wsl` command equality | keep | Invariant: teardown issues exactly `wsl --unregister <distro>` and `delete` reuses the same shared sequence. Upstream tool syntax from a recorded call log. |
| E-177 | `cli/tests/test_wsl2_create_interrupt.py::test_success_runs_primary_bootstrap_before_create_returns` | bootstrap kwargs equality | keep | Invariant: the primary bootstrap runs before create returns, with exactly the injected admin user, key, and options. All values are the test's. |

## Group 5: authored-artifact form policing

200 live rows: 95 delete, 11 convert, 94 keep, out of a ledger of 214 (seven `[dead]`, seven
`[subtracted]`).

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-042 | `cli/tests/guide/test_authored_coverage.py::L103-104` | **[line-anchored: file gone]** **[dead]** catalog closure plus retired-id blacklist | delete | Line 103 cannot fail: the parser's discriminator set is closed to exactly those six block types (`guide/contract.py:606-616`), so every catalog block is already one of them. Line 104 is a blacklist on one retired block id. |
| A-052 | `cli/tests/guide/test_release_notes.py::L130-137` | **[line-anchored: file gone]** **[dead]** authored shell-snippet substring pins | delete | Pins that three hand-written completion snippets spell `agw guide --names-only`. Verified duplicate: `tests/test_completions.py:163-171` asserts the same four things verbatim. A derivation-based replacement would need a shared production constant, which R2.2 forbids adding for a test. |
| A-053 | `cli/tests/guide/test_release_notes.py::L78-104` | **[line-anchored: file gone]** **[dead]** changelog-versus-tag parity | keep | Derivation parity against an external source (git tags), with the constant only a fallback: a changelog section without a tag, or a dropped historical section, fails here. |
| A-056 | `cli/tests/guide/test_probe_free_registry.py::L70` | **[line-anchored: file gone]** **[dead]** production-constant restatement | delete | A second copy of `HOST_PROBING_CAPABILITY_KINDS` inside the test. It can only fail when someone edits the constant it restates, which is exactly the shape the amended principle 3 calls cost rather than coverage. |
| A-057 | `cli/tests/guide/test_trail_sign.py::L47-60` | **[line-anchored: file gone]** **[dead]** hardcoded copy of a production constant | convert | Restates `TRAIL_DESTINATIONS` per mode. The real invariant is the mode filter (`guide/trail_sign.py:33-37` returns only destinations with a `human_choice` for human mode). Replacement, no production change: assert the human tuple is a strict subset of the agent tuple and that both are non-empty, which fails if the filter degenerates in either direction. |
| A-063 | `cli/tests/guide/test_package_data.py::L22-37` | **[line-anchored: file gone]** **[dead]** packaging inclusion and changelog parity | keep | Packaging invariants that regress silently: authored guide markdown must be inside the wheel, the lint config must not be, and the packaged `CHANGELOG.md` must be byte-identical to the canonical one. The last is derivation parity. |
| A-066 | `cli/tests/test_database_backup.py::test_backup_names_are_disjoint_and_collisions_are_reserved` | **[subtracted: instance-model]** backup filename format pins | convert | The filename format is load-bearing (retention parses it with `_AUTOMATIC_NAME`, `db/backup.py:35-36`), but its functional consequences are already asserted behaviorally at lines 326-366, where a manual backup and a timestamp lookalike both survive retention. What is left unique here is collision reservation. Replacement: assert the two manual backups differ and both exist, and that the automatic name is disjoint from them. No production change. |
| B-056 | `cli/tests/transports/test_factories.py::test_tailscale_wait_wording_is_a_closed_set` | closed-set verbatim wording pin | delete | `test_tailscale_wait_wording_is_a_closed_set` exists only to pin four operator-facing strings byte for byte; its docstring says so. The canonical text of the enum is the enum. Delete the test. |
| B-057 | `cli/tests/transports/test_factories.py::test_wait_for_reconnect_default_context_is_reconnect,test_wait_for_reconnect_verify_context_emits_verify_wording` | wording substring plus wording blacklist | convert | The invariant is that `context=` selects the message pair, which a regression that ignores the parameter would break. Replace substring literals with derivation from the canonical source: `TailscaleWait.VERIFY.waiting_message in captured_output.detail`, and the blacklist with `TailscaleWait.RECONNECT.waiting_message not in captured_output.detail`. VERIFIED by executing: both tests pass under this rewrite. No production change. |
| B-068 | `cli/tests/test_claude_code_integration.py::test_present_transcript_resumes,test_absent_transcript_launches_fresh` | launch-note verbatim equality | delete | Two tests whose entire body is `launch_note() == "<authored sentence>"`. Sentence-only observable, R2.4. Delete both tests. |
| B-072 | `cli/tests/test_codex_integration.py::test_create_is_always_fresh_even_with_a_recording_and_a_candidate,test_a_dropped_stale_binding_is_reported_alongside_whatever_replaced_it,test_launch_note_reports_resume,test_launch_note_reports_fresh_start,test_launch_note_reports_adoption_and_names_the_adopted_id,test_launch_note_reports_the_picker_including_what_esc_does` | launch-note verbatim equality and required-phrase list | delete | Five tests (`test_launch_note_*`) exist only to pin the note text, and `test_a_dropped_stale_binding_...` pins two full sentences plus a four-clause required-phrase list at 616-619. The dropped-stale-binding test's non-note half is `state`/`command` and is duplicated by B-071's siblings. See summary: the "both halves are reported" invariant is genuinely wording and dies here. |
| B-074 | `cli/tests/test_consoles_attach.py::test_attach_loop_wrapper_format` | authored shell-script banner pins plus phrase blacklist | delete | Split from B-075. `"Press enter" not in wrapper` (blacklist), `"Waiting for session backend to come up"`, `"Session backend exited cleanly"`, `"exited (status $rc)"`, `"Waiting for session to resume"` pin the banner prose the wrapper echoes into the pane. |
| B-075 | `cli/tests/test_consoles_attach.py::test_attach_loop_wrapper_format` | shell-behavior token pins | keep | These are the wrapper's semantics, not its prose: nesting is only allowed with `TMUX` unset, the loop must be unbounded (`while true`, no `break`), the back-off must be 2s, and the socketed variant must thread `-S` through both `has-session` and `attach`. A regression in any one changes what the pane does. |
| B-096 | `cli/tests/test_secret_describe.py::test_resolution_preview_not_available_when_no_source_attempts` | **[subtracted: secrets-preview]** section-heading required-phrase list plus ordering pin | delete | `test_render_emits_header_usages_mappings_preview` is a full required-phrase list over authored section headings (`Secret:`, `Kind:`, `Description:`, `Origin:`, `Referenced by:`, `Backend mappings:`, `Resolution preview:`) plus a `Description` before `Origin` ordering pin. Wholly a rendering-layout test; delete it. |
| B-097 | `cli/tests/test_secret_describe.py::test_describe_secret_used_by_is_none_without_db` | **[subtracted: secrets-preview]** section-order pin | delete | `out.index("Referenced by:") < out.index("Used by ...") < out.index("Backend mappings:")` is the sentence-order shape named in the doctrine. |
| B-098 | `cli/tests/test_secret_describe.py::test_render_emits_used_by_section_when_populated` | **[subtracted: secrets-preview]** rendered friendly-message pin | delete | `"(no live sessions reach this secret)"` is the authored empty-state sentence. The structural half, `desc.used_by == ()` on line 631, stays. |
| B-099 | `cli/tests/test_secret_describe.py::test_render_emits_used_by_section_when_populated` | **[subtracted: secrets-preview]** section presence and absence pins | delete | `"Used by" not in out` is a phrase blacklist standing in for "the section is omitted". The `used_by is None` distinction is a shipped field the test can read; the rendering branch is a sentence-only observable, R2.4. |
| B-101 | `cli/tests/test_secret_sources_drive_harness.py::test_unsupported_windows_refuses_before_environment_or_provider_work` | **[subtracted: secrets-preview]** harness-constant equality | keep | `captured.err == f"{HARNESS._UNSUPPORTED_HOST_MESSAGE}\n"` derives the expected text from the canonical constant instead of restating it, which is exactly the derivation the rule prescribes; the invariant is that the refusal reaches stderr and nothing else runs. |
| B-115 | `cli/tests/test_secrets_inspect.py::test_render_grid_uses_readiness_vocabulary` | **[subtracted: secrets-preview]** rendered-vocabulary substring | delete | `test_render_grid_uses_readiness_vocabulary` names its own shape: it asserts the renderer uses particular authored words. The underlying precedence (readiness wins over identifier, wont-attempt wins over not-ready) is pinned structurally at 392 and 404. |
| C-001 | `cli/tests/test_sample_config.py::test_sample_config_parses_and_loads_as_shipped` | production-loader round trip | keep | The shipped sample must parse and load through the real loader, and its top-level roots must equal `EXPECTED_TOP_LEVEL_KEYS`: derivation parity against a production constant, not a copy of the file's text. Catches a settings root added to the loader but never surfaced in the sample. |
| C-002 | `cli/tests/test_sample_config.py::test_sample_config_examples_uncomment_cleanly` | uncomment-convention round trip | keep | Stripping one `#` from every `#<toml>` line must still parse and load, and the fully-uncommented roots must equal `EXPECTED_TOP_LEVEL_KEYS`. The assertion runs the production parser rather than restating text, and catches the real regression: an example written `# key = value` no longer uncomments in place. |
| C-003 | `cli/tests/test_package_exports.py::test_every_exported_name_resolves` | structural presence | keep | Every package's `__all__` must name a symbol that exists. Ruff's F822 is disabled exactly inside `__init__.py`, so this is the only guard, and the failure it catches (a stale export raising AttributeError on `import *`) has already happened once. |
| C-014 | `cli/tests/test_builtin_entries_parity.py::test_installer_plugin_rows_match_oracle_and_are_disabled_by_default,L37-191` | **[line-anchored: module level]** verbatim pin of authored manifests | delete | The `EXPECTED_*` constants are, by the module docstring's own admission, hand-transcribed copies of the bundled plugin manifests kept inside the test. That is the second copy the rule forbids, not derivation parity: the canonical source is the YAML, so any legitimate manifest edit becomes a two-file ceremony. |
| C-017 | `cli/tests/test_completions.py::TestTopLevelGroups.test_expected_top_level_groups_match` | hand-maintained copy of the command tree | delete | `EXPECTED_GROUPS` is a second copy of the CLI's own group list. It can only fail when someone deliberately changes the tree and then pastes the change into the test; the half-rename story in its docstring is caught by the renamed command's own tests. |
| C-018 | `cli/tests/test_completions.py::TestTopLevelGroups.test_expected_top_level_groups_match,TestRetiredCommandsAbsent.test_workspace_group_carries_no_shell_or_console,TestRetiredCommandsAbsent.test_vm_group_carries_no_console` | retired-surface absence | delete | "`workspace shell` / `vm console` are still gone" restates a deletion nobody undoes by accident. Reintroducing a command is a deliberate act with its own review. |
| C-019 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_all_keys_resolve_to_real_commands` | structural validity of a mapping table | keep | Every `DYNAMIC_COMPLETIONS` key must resolve to a real command path and a real param on it; a rename leaves a silently dead completion that nothing else catches. |
| C-020 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_all_keys_resolve_to_real_commands,TestDynamicCompletionsMapping.test_completer_ids_are_known` | cross-map completeness | keep | Every completer id must have a snippet in all three shell maps. The bash generator silently skips unknown ids, so a rename missed in bash alone ships as dead completion. |
| C-021 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_completer_ids_are_known,TestDynamicCompletionsMapping.test_guide_show_topics_use_the_list_stream_in_every_shell` | invocation contract | keep | The guide-topic completer must invoke `agw guide --names-only` in every shell, or completion shells out to the prose-rendering path and dumps rendered guide text into the candidate list. The pinned substring is the command the snippet runs, not wording. |
| C-022 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_show_topics_use_the_list_stream_in_every_shell,TestDynamicCompletionsMapping.test_guide_uses_ordinary_subcommands_and_one_dynamic_show_argument` | derivation parity against a production set | keep | The probe marker and stderr redirection must be present exactly for the completers in `DATABASE_BACKED_DYNAMIC_COMPLETERS` and absent elsewhere, with the three shell maps' key sets equal. The expectation is computed from the production constant, so this is parity, not a copy. |
| C-023 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_uses_ordinary_subcommands_and_one_dynamic_show_argument` | generated-invocation guard | keep | Every generated database-backed invocation must carry the probe marker; a marker-free one opens the state database during tab completion, the exact bug the probe exists to prevent. |
| C-024 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_guide_uses_ordinary_subcommands_and_one_dynamic_show_argument,TestDynamicCompletionsMapping.test_generated_bash_guide_completion_follows_the_group_grammar,TestDynamicCompletionsMapping.test_generated_bash_guide_completion_follows_the_group_grammar.complete` | native-file-completion contract | keep | `database restore`'s path argument must route to each shell's native file completion (`compgen -f`, `_files`, `CompleteFilename`). Those tokens are the shells' own vocabulary, and losing them silently breaks path completion. |
| C-025 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_bash_guide_completion_follows_the_group_grammar` | **[unverified]** weak presence pins on a generated script | delete | `"database"`, `"restore"`, `"--yes"` in the script restate the completion tree's contents on a script this same file exercises end to end. Not executed: the "TestCompleteness already covers it" half was reasoned, and that test covers zsh and powershell only. |
| C-026 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_bash_guide_completion_follows_the_group_grammar,TestDynamicCompletionsMapping.test_generated_zsh_guide_completion_dispatches_through_global_mode,TestDynamicCompletionsMapping.test_generated_zsh_guide_completion_dispatches_through_global_mode.complete` | executed behavior | keep | Runs the generated bash and asserts a filename containing spaces survives completion; the only assertion that proves the emitted quoting works. |
| C-027 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_zsh_guide_completion_dispatches_through_global_mode.complete,TestDynamicCompletionsMapping.test_generated_zsh_guide_completion_dispatches_through_global_mode` | generated variadic-position contract | keep | The `-ge` operator, its threshold, and zsh's `*:` catchall are what make a variadic positional complete at every trailing position: the generator's behavior expressed in the target shell's syntax, unobservable any other way in CI. |
| C-028 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_zsh_guide_completion_dispatches_through_global_mode.complete,TestDynamicCompletionsMapping.test_generated_zsh_guide_completion_dispatches_through_global_mode` | invocation contract | keep | `agw secret list --names-only` must be what the secret completer runs in all three shells, for the same reason as C-021. |
| C-029 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_zsh_guide_completion_dispatches_through_global_mode` | flag spelling pin plus typo blacklist | delete | `"--allow-interaction" in block` with `"--allow-interactive" not in block` restates the option's spelling and blacklists one misspelling someone already thought of; the tree is generated from Typer, so the flag reaches it by construction. |
| C-030 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_zsh_guide_completion_dispatches_through_global_mode,TestDynamicCompletionsMapping.test_generated_powershell_guide_completion_follows_the_group_grammar` | consent boundary plus catalog parity | keep | The completion stream must render from the authored catalog with `load_config`/`load_registry` forbidden: a real confinement boundary plus byte parity against `build_authored_catalog().names()`. |
| C-031 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_powershell_guide_completion_follows_the_group_grammar` | content blacklist over a canonical source | delete | `"vm-template" not in response.markdown.splitlines()` polices the authored catalog's contents; the equality one line above already fixes the output exactly. |
| C-032 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_generated_powershell_guide_completion_follows_the_group_grammar,TestDynamicCompletionsMapping.test_database_backed_snippets_share_hidden_probe_contract` | per-flag spec restatement plus retired-spelling blacklist | delete | `--resolve` present and `--reveal-secrets` absent restate the `env show` command definition; the spec is introspected live from Typer, so presence is by construction and absence is a blacklist. |
| C-033 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_database_backed_snippets_share_hidden_probe_contract` | per-flag spec restatement | delete | `--include-disabled` in the extracted opts is the same shape as C-032: it can only fail when someone edits the command it restates. |
| C-034 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_database_backed_snippets_share_hidden_probe_contract,TestDynamicCompletionsMapping.test_generated_database_backed_invocations_all_carry_hidden_probe_marker` | closed-choice contract over an enumerated surface | keep | Every JSON v1 command must expose `--output` with exactly `["human", "json"]`. That is the machine-output contract, and the path list is the quantifier that stops a command dropping the flag unnoticed. |
| C-035 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_database_restore_uses_native_file_completion_in_every_shell` | presence plus retired-surface absence | delete | `session resume` present and `session restart` absent restate the command tree and the completion table; C-019 already proves every table key resolves to a real command. |
| C-036 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_database_restore_uses_native_file_completion_in_every_shell` | shell-required header directives | keep | `#compdef` and `Register-ArgumentCompleter` are the shells' own requirements for a script to be loaded at all, and the version stamp is what doctor's staleness check reads. |
| C-037 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_secret_verify_variadic_completion_contract_in_every_shell` | cross-artifact sourcing contract | keep | `f"--kind {kind}" in snippet` proves each registry-backed completer sources from `agw resource list --kind X` rather than scraping config.toml; the expected kind comes from the case table, and a drift here ships wrong candidates. |
| C-038 | `cli/tests/test_completions.py::TestDynamicCompletionsMapping.test_secret_verify_variadic_completion_contract_in_every_shell,L501` | **[line-anchored: between functions]** technique blacklist | delete | `"sed " not in snippet` and `"Select-String" not in snippet` ban the superseded scraping technique by substring; C-037 already asserts the shape the snippets must have. |
| C-042 | `cli/tests/test_completions.py::TestResolveShell.test_autodetect_failure_exits_with_message,TestCompletionCli.test_show_with_explicit_shell_prints_script,TestCompletionCli.test_show_autodetect_failure_exits_1,TestCompletionCli.test_show_pwsh_alias_produces_powershell_script` | installer-line surgery | keep | The `$PROFILE` strip must remove the installer's exact dot-source line and preserve user lines that merely mention the filename. Every string asserted is one the test itself wrote into the profile: value identity over a real edit. |
| C-043 | `cli/tests/test_completions.py::TestCompletionCli.test_show_pwsh_alias_produces_powershell_script,TestInstall.test_bash_install_drops_agw_alias_symlink` | generated variadic syntax | keep | Same invariant as C-027 for `console create`'s variadic positional across the three shells. |
| C-044 | `cli/tests/test_completions.py::TestInstall.test_zsh_install_drops_agw_alias_symlink` | invocation contract | keep | The kinds completer must call the config-free `resource kinds --names-only` path; calling the config-loading one makes completion depend on a loadable config. |
| C-045 | `cli/tests/test_completions.py::TestInstall.test_zsh_install_drops_agw_alias_symlink` | technique blacklist | delete | `"resource list" not in source` bans the superseded scrape by substring, the same shape as C-038. |
| C-046 | `cli/tests/test_completions.py::TestUninstall.test_bash_uninstall_removes_script_and_alias` | upstream-extraction guard | keep | `by_name["shell"] == [...]` proves Typer's `FuncParamType` is still unwrapped so `click.Choice` values reach the tree. The regression is driven by an upstream version bump rather than our editing, and it is silent: every Choice-typed option loses static completion. |
| C-047 | `cli/tests/test_completions.py::TestUninstall.test_powershell_uninstall_removes_script_and_profile_line` | option-list restatement | delete | `[opt for param in schema.params ...] == ["--install"]` pins the complete option list of `resource schema`, a restatement of the command definition. |
| C-048 | `cli/tests/test_completions.py::TestUninstall.test_powershell_uninstall_removes_script_and_profile_line` | choice and suggestion mechanism | keep | That `depth` carries `suggestions` and no `choices` while `direction` and `output_format` carry closed `choices` pins two different completion mechanisms; collapsing them makes `--depth 7` unusable. |
| C-049 | `cli/tests/test_completions.py::TestUninstall.test_powershell_uninstall_removes_script_and_profile_line` | vacuous presence assertion | delete | Verified by execution: a probe over `generate(shell)` found `"1"` 7-80 times, `"2"` 28-163, `"3"` 2-49 and `"all"` 20-74 in each generated script, so the depth half passes even with depth suggestions removed entirely. The direction half duplicates C-048. |
| C-050 | `cli/tests/test_completions.py::TestUninstall.test_powershell_uninstall_removes_script_and_profile_line,TestUninstall.test_uninstall_when_nothing_installed_is_clean` | per-shell emission shape | keep | Each generator's own syntax for a static choice list (`:shell:(...)`, `compgen -W`, one `CompletionResult` per value) is behavior invisible from the spec; a regression here silently drops static completion. |
| C-052 | `cli/tests/test_terminal.py::test_sanitize_supersets_the_mouse_reset` | shared-constant superset | keep | `MOUSE_TRACKING_DISABLE in TERMINAL_SANITIZE` proves the attach payload reuses the prompt-level constant instead of a divergent copy, and `;1006` is SGR mouse mode, the externally defined sequence that garbles a dropped attach. |
| C-053 | `cli/tests/test_terminal.py::test_sanitize_clears_each_mode_a_remote_program_can_leave_set` | externally-defined escape inventory | keep | Each pinned escape is a terminal-defined reset for a mode a remote program can leave set. The payload's effect is unobservable in CI, so this table is the only guard that sanitize still restores each mode. |
| C-054 | `cli/tests/test_terminal.py::test_sanitize_grounds_the_parser_first,test_sanitize_leaves_the_alternate_screen_before_resetting_margins,test_sanitize_brackets_the_cursor_homing_resets,test_sanitize_resets_rendering_after_the_cursor_restore` | ordering of an escape sequence | keep | The `.index()` comparisons encode terminal semantics, not lexical taste: ground the parser first, leave the alternate screen before resetting margins, bracket the cursor-homing resets in DECSC/DECRC, and put the rendering resets after DECRC (which restores charset and SGR). Reordering the constant corrupts a real terminal and nothing else would catch it. |
| C-055 | `cli/tests/test_terminal.py::test_sanitize_does_not_hard_reset` | single-token blacklist | keep | `"\x1bc" not in TERMINAL_SANITIZE` bans RIS, an externally defined sequence with one spelling and a destructive effect (it clears the scrollback the operator most wants after a dropped attach). Unlike a phrase blacklist there is no paraphrase to slip through. |
| C-130 | `cli/tests/test_bootstrap_script.py::test_generate_bootstrap_script_all_steps,test_generate_bootstrap_script_preserves_ssh_host_keys,test_generate_bootstrap_script_swap_disabled,test_generate_bootstrap_script_writes_shell_rc_seeds` | step-marker checklist | delete | The `##STEP##` labels are a required-phrase list over a script we author. Verified by search: nothing in `cli/agentworks` reads `##STEP##` or `##SUCCESS##` (only `capabilities/vm_platform/bootstrap_script.py` emits them), so they are display-only, and each step's payload is asserted separately. |
| C-131 | `cli/tests/test_bootstrap_script.py::test_generate_bootstrap_script_all_steps,test_generate_bootstrap_script_masks_sve_gated_on_apple,test_authorized_keys_install_is_idempotent,test_ssh_key_install_is_skipped_when_the_key_is_empty,test_swap_fstab_append_is_guarded_against_re_execution` | generated-script behavior | keep | `set -euo pipefail`, the injected auth key and swap size, the SVE probe paths, the idempotent `grep -qxF ... \|\| echo` append and its single-append count are what the script does on a real VM, unobservable in CI any other way. |
| C-132 | `cli/tests/test_bootstrap_script.py::test_generate_bootstrap_script_can_omit_join_from_retained_payload,test_ssh_key_install_is_skipped_when_the_key_is_empty` | echoed sentence pins | delete | `"Tailscale join deferred to platform"` and `"SSH key provisioned out of band"` pin operator-facing prose inside the script; each branch is pinned structurally beside it (`TAILSCALE_AUTH_KEY=''`, `SSH_PUBLIC_KEY=''`). |
| C-133 | `cli/tests/test_bootstrap_script.py::test_sve_gate_matches_sve_and_sve2_as_whole_words,test_sve_gate_matches_sve_and_sve2_as_whole_words.fires` | probe regex against real `/proc/cpuinfo` shapes | keep | The SVE gate is evaluated against real feature strings including the near-miss `sveaes`-without-`sve` case: behavior over externally shaped input. |
| C-134 | `cli/tests/test_cloud_init.py::test_provisioning_packages_minimal,test_init_system_packages` | membership pins on two authored constants | convert | Replace the per-package membership and absence pins with the boundary they express, `set(PROVISIONING_PACKAGES).isdisjoint(INIT_SYSTEM_PACKAGES)`, keeping only the packages bootstrap cannot proceed without (`openssh-server`, `ca-certificates`). Otherwise the test is a second copy of two lists. No production change. |
| C-135 | `cli/tests/test_ssh_config.py::test_ensure_include_adds_to_top` | marker literal restated | delete | `lines[0] == "# Added by agentworks"` restates `ssh_config.py:25`'s `_INCLUDE_COMMENT`; it can only fail when someone edits that constant, and the include directive's own assertions carry the behavior. |
| C-137 | `cli/tests/test_ssh_config.py::test_rebuild_config_dir` | header prose | delete | `"Managed by agentworks" in content` pins the generated block's authored header; the `Host`/`User`/address assertions are the behavior. |
| C-138 | `cli/tests/test_ssh_config.py::test_remove_legacy_section,test_rebuild_config_dir,test_rebuild_config_dir_emits_per_agent_blocks,test_rebuild_config_dir_no_agent_blocks_when_vm_has_none,test_rebuild_config_dir_agent_blocks_global_namespace,test_legacy_rebuild_emits_per_agent_blocks,test_legacy_rebuild_no_agents_no_per_agent_blocks` | generated-config behavior | keep | Host aliases, users, addresses, the count of address occurrences, and the absence of retired composite aliases are the SSH config's actual content; wrong output breaks every `ssh awvm--x`. |
| C-142 | `cli/tests/test_authorized_keys.py::test_header_present` | generated-file header prose | delete | "Managed by agentworks", "manual edits will be overwritten" and the setting name pin an authored header block; the keys, path, ownership and mode assertions are the behavior. |
| C-144 | `cli/tests/test_admin_home_permissions.py::test_bootstrap_forces_private_group_idempotently` | superseded-form blacklist | delete | `"useradd -m -U" not in script` bans the old spelling by substring; 193-194 already pin the `groupadd`/`useradd -g` pair the script must emit. |
| C-145 | `cli/tests/test_admin_home_permissions.py::test_bootstrap_forces_private_group_idempotently` | order in a generated script | keep | The index comparison is over generated shell, which executes top to bottom, so the order is the behavior (create the group before the user that references it), not a lexical pin on our own source. |
| C-151 | `cli/tests/test_initializer_reconcile.py::test_preserve_ssh_host_keys_emits_same_bytes_as_phase_a,test_apply_sve_mask_emits_same_grub_bytes_as_phase_a` | derivation parity between two production constants | keep | `"".join(f"{line}\n" for line in LINES) == CONTENT` proves the rendered blob is derived from the line list rather than a divergent second copy: exactly the shape the rule endorses. |
| C-154 | `cli/tests/test_initializer_reconcile.py::test_preserve_ssh_host_keys_writes_dropin,test_preserve_ssh_host_keys_emits_same_bytes_as_phase_a,test_apply_sve_mask_noop_when_gate_closed,test_apply_sve_mask_emits_same_grub_bytes_as_phase_a,test_install_claude_plugins_runs_when_claude_present,test_reconcile_authorized_keys_direct_write_when_owner_none,test_reconcile_authorized_keys_stage_and_install_when_owner_set` | command-effect assertions | keep | Which command ran, with which sudo flag, path, owner, mode and staging file, is the reconcile pass's behavior against a real target. |
| C-157 | `cli/tests/test_initializer_hardening.py::test_sysctl_baseline_writes_when_missing,test_sysctl_baseline_noop_when_content_matches,test_sysctl_baseline_rewrites_when_content_differs,test_fstab_hidepid_appends_when_no_proc_line,test_fstab_hidepid_adds_option_when_proc_line_has_no_hidepid,test_fstab_hidepid_upgrades_0_to_1,test_fstab_hidepid_noop_when_already_1,test_fstab_hidepid_preserves_admin_set_hidepid_2,test_ensure_proc_hidepid_appends_when_no_proc_line,test_ensure_proc_hidepid_added_option,test_ensure_proc_hidepid_upgraded,test_ensure_proc_hidepid_no_op` | fstab and sysctl edit behavior | keep | The hidepid option's append, add, upgrade and no-op outcomes, the preserved unrelated fstab line, and the remount commands are the behavior of a real edit against a real file. |
| C-158 | `cli/tests/test_tailscale_dns.py::test_dropin_content_orders_after_network_online,test_dropin_content_includes_nss_lookup_target,test_dropin_content_does_not_use_requires` | systemd unit directives | keep | `After=`/`Wants=network-online.target`, `nss-lookup.target`, and the absence of `Requires=` are systemd's own vocabulary and decide boot ordering; the effect is not observable in CI any other way. |
| C-162 | `cli/tests/test_tailscale_dns.py::test_dropin_written_when_missing,test_dropin_noop_when_content_matches,test_dropin_rewritten_when_content_differs,test_dropin_does_not_restart_tailscaled,test_apply_invokes_dropin_install,test_check_silent_when_dns_works,test_check_warns_when_dns_fails_and_resolv_unreadable,test_check_dns_probe_runs_first` | command-effect and ordering | keep | Which install, daemon-reload, restart and probe commands ran, in which order, and which did not run, is the drop-in reconcile's behavior. |
| C-192 | `cli/tests/test_topics.py::test_overview_is_dedented_so_prose_can_be_written_where_it_belongs,test_summary_of_reads_the_declaration_that_already_exists` | fixture prose equality | keep | The pinned text is the test's own fixture docstring, so this asserts the extractor returns what it was given: value identity, not our shipped prose. |
| D-045 | `cli/tests/manifests/test_samples.py::test_a_field_that_folds_a_scalar_names_both_spellings_and_shows_the_block` | verbatim pin of generated comment lines, plus one absence blacklist | delete | This is C10's "generated comments pinned line by line" at HEAD. Every literal here is renderer-authored text in a fully commented block, so no uncomment-and-load test can ever be affected by it, and `:152` is a wording blacklist on our own output. The renderer's real invariants survive in `:123-127` (every line inert) and `:165-181` (uncommented samples load). |
| D-046 | `cli/tests/manifests/test_samples.py::test_appending_to_a_hand_written_manifest_keeps_it_declaring` | verbatim pin of written file bytes | delete | **Verified by execution.** With `_joined` broken so the separator lands on the operator's last line (`cpus: 4#---`, the case the docstring names), the activate-and-load half at :303-307 raises `ConfigError: duplicate mapping key 'apiVersion'`. With only the blank line dropped, nothing breaks at all. So this pin's sole unique coverage is a cosmetic blank line in a generated file. |
| D-054 | `cli/tests/manifests/test_reference.py::test_a_collection_of_blocks_renders_one_element_under_a_placeholder` | authored placeholder sentences | delete | `"one element, as an example:"` / `"one entry, as an example:"` are renderer prose; the structural twins that say a placeholder was emitted at all are the surviving `#    # -` and `#    # <key>:` assertions beside them. |
| D-060 | `cli/tests/manifests/test_reference.py::test_an_unexpanded_arm_does_not_name_what_is_shown` | authored sentence fragment | delete | `"One of: group."` is renderer prose; the arm inventory it stands for is asserted structurally two tests above. |
| E-001 | `cli/tests/resources/test_always_materialize.py::test_polish_empty_usage_format` | authored description-format pin | convert | The claim is that the polish fires on an empty description; replace with "polished.description is non-empty and differs from the stub's input" instead of pinning `"(auto) auto-declared default vm-template"`. No production change. |
| E-002 | `cli/tests/resources/test_always_materialize.py::test_polish_skips_operator_set_description` | value identity of an injected string | keep | Invariant: the polish never overwrites an operator-set description. `"operator's own text"` is the value the test injected, so this is propagation identity, not authored prose. |
| E-006 | `cli/tests/resources/test_inherited_reference_attribution.py::test_an_auto_declared_row_records_the_declaring_template_as_its_origin` | authored description-format pin | delete | The attribution claim is already carried structurally by `row.origin.source == ("vm-template", "base")` on the line above; the description sentence adds only wording. |
| E-007 | `cli/tests/resources/test_inherited_reference_attribution.py::test_two_descendants_of_one_declarer_do_not_read_as_two_declarations` | authored description-format pin (sole assertion) | convert | Rubric re-check, 2026-08-16. The asserted description varies with a real computed quantity, the distinct-declarer count feeding `registry.py:935-937`'s more-declarers suffix, and it is the only probe of it. Verified by execution: `registry.graph.dependents_of("secret", "base-secret")` returns three entries with three distinct `source` values and exactly one distinct `declared_by`. No production change. |
| E-021 | `cli/tests/plugins/test_azure.py::test_all_three_rows_disabled_system_plugin_by_default` | verbatim content pin of a shipped manifest | delete | The comment calls it a byte-for-byte content pin so "a typo is caught here". It is not: the pin was written by the same author in the same change as the manifest. The structural claims (row present, origin `system-plugin`, plugin `azure`, disabled) sit directly above. |
| E-026 | `cli/tests/plugins/test_claude.py::test_install_command_row_is_disabled_system_plugin_by_default` | verbatim content pin of a shipped install command | delete | Same shape as E-021: an authored manifest value copied into a test. |
| E-030 | `cli/tests/plugins/test_codex.py::test_install_command_row_is_disabled_system_plugin_by_default` | verbatim content pin of a shipped install command | delete | Same as E-021/E-026. |
| E-038 | `cli/tests/plugins/test_gcp.py::test_gcp_bundle_publishes_disabled_gcloud_apt_resources` | verbatim content pin of a shipped apt-source line | delete | Same shape as E-021: an authored manifest value copied into a test, with the row's presence and provenance already asserted around it. |
| E-151 | `cli/tests/vms/test_system_slug.py::test_slug_prompt_matches_the_frd_text` | verbatim prompt pin (whole test) | delete | `test_slug_prompt_matches_the_frd_text` pins an operator prompt paragraph verbatim, with a docstring that says the pin exists because "drift is user-visible". This is the canonical shape the rule forbids; the slug's real contract (bounds, validation, non-interactive behavior, blank-is-final) is covered by the tests around it. |
| F-001 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_all_pages_have_metadata_landmarks_skip_link_and_one_h1` | verbatim pin | keep | **Corrected 2026-08-19 (map re-baseline).** Was delete. `name` is the loop variable over the built documents and `expected` is the per-page table at `:342-351`, so the assertion is per-page routing: this page carries this title. The same table's canonical-URL half at `:364` is asserted the same way, is covered by no row, and survives, so deleting `:361-362` would split one property in two. |
| F-002 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_all_pages_have_metadata_landmarks_skip_link_and_one_h1` | verbatim pin (second copy) | convert | **Ruled by the lead 2026-08-16**: a shipped security header's security-relevant directives are behavior, not form. Assert at directive level (that `script-src` carries no `unsafe-inline`, and the equivalent for the other directives the policy relies on); drop the comparison against `site_test_support.CSP`, which is a hand-maintained second copy of the template's policy. F-145 retires the copy in the shared support module. **Note after the W2 deferral**: F-046 held the third copy (`policies == [CSP, CSP]`) and is now deferred with the rest of `test_site_templates.py`, so `site_test_support.CSP` cannot be deleted outright in this sweep. Convert F-002 and F-145 off it, and leave the constant in place with its last consumer until R4 decides that file. |
| F-003 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_all_pages_have_metadata_landmarks_skip_link_and_one_h1` | structural presence | keep | Every built page keeps one header/main/footer/h1, unique element ids, a canonical link, a description, and a skip link to `#main-content`. A shell-template refactor can drop a landmark or duplicate an id and nothing else notices. |
| F-004 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths` | verbatim pin | delete | Pins the exact heading text of the home page. Review owns the wording. |
| F-005 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths` | id blacklist | delete | Asserts the ids `problem` and `principles` and any `<article>` are absent, i.e. that a retired home-page design has not returned. A blacklist of shapes someone already thought of; nothing forces them back. |
| F-006 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths` | derivation parity | keep | The served onboarding prompt must equal `packaging/agentworks/assistance.md` byte for byte. Build-time escaping or whitespace handling can silently ship a prompt that differs from the file operators are told to copy. |
| F-007 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths` | exact attribute-dict equality | delete | **Recipe verification, 2026-08-16.** **The row's justification is factually wrong.** The a11y attributes it proposes to assert are enforced unconditionally by the builder, so the replacement passes always and can only fail through a `RuntimeError` in `build.py`. Delete rather than convert. |
| F-008 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths` | structural presence | keep | The onboarding section is labelled by exactly one `h2` that is its own descendant. A template refactor can break the `aria-labelledby` wiring with no visible symptom. |
| F-009 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact` | verbatim pin | keep | **Corrected 2026-08-19 (map re-baseline).** Was delete, and re-derived at HEAD (the file moved from `:441-444`). `current_labels` is the per-page table at `:477-483` and is consumed nowhere else, so deleting the row orphans it. The assertion proves the breadcrumb marks the current page's OWN label as current; the structural half at `:497-498` (exactly one `aria-current` span) cannot tell which page got marked, so a breadcrumb marking a constant label would pass without this. |
| F-010 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths` | verbatim pin | delete | Counts occurrences of three authored strings ("Product of Wayfarer Labs, LLC" and two link texts). The link-uniqueness property is already held by the href counts at :429-438, keyed on destinations rather than on prose. |
| F-011 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths` | structural presence | keep | Each shared destination appears exactly once per page; service icons stay `aria-hidden`/`focusable="false"`; the decorative header mark keeps an empty `alt`; the footer game link keeps a non-empty accessible name equal to its `title`. These are the a11y and navigation invariants a shell edit can regress silently. Note the accessible-name check is the third of three copies (see F-043 and F-136). |
| F-012 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_home_onboarding_preserves_both_progressively_enhanced_paths,GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact` | derivation parity | keep | The rendered long-form page is a complete, ordered projection of its Markdown source (blocks and links). Verified load-bearing: with `site_validation._validate_template` stubbed out, injecting `<h1>Template title</h1>` into `manifesto.html` still fails here (`('h1','Template title')` against `('h1','The Agentworks Manifesto')`). |
| F-013 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact` | derivation parity | keep | The table of contents is derived from the source h2/h3 structure and every entry resolves to exactly one id on the page. A dangling anchor is invisible in review. |
| F-014 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact` | retired-shape blacklist | delete | Asserts a retired `home-link` id and `error-code` class are absent. Nothing reintroduces them. |
| F-015 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact` | structural presence | keep | The 404 page keeps exactly one home link and one module script. The 404 is the page nobody looks at; a broken fallback ships unnoticed. |
| F-016 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_shared_header_footer_breadcrumb_icons_and_logo_exception_are_exact,GeneratedDocumentTests.test_long_form_pages_are_complete_semantic_source_projections` | byte parity | keep | The Lander and 404 pages must serve one byte-identical game subtree, and only the pages meant to carry scripts carry them. Verified: with the template validator stubbed, dropping `{{LANDER_GAME}}` from `404.html` fails here. |
| F-017 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_long_form_contents_navigation_matches_source_h2_h3_structure` | CSS declaration pin | delete | Pins `display: inline-flex`, `width: 24px`, `height: 24px` and the absence of `position:` in one rule. Authored CSS whose form review owns; the browser tests measure real geometry where it matters. |
| F-018 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_long_form_contents_navigation_matches_source_h2_h3_structure,GeneratedDocumentTests.test_404_retains_fallback_and_has_only_its_local_module` | privacy defense (blacklist) | convert | **Recipe verification, 2026-08-16.** PARTIAL, and the caveats must ride with the row. Consolidating the runtime-surface scan DROPS the `.html` coverage the two scans have between them, and deleting the canaries removes the only non-vacuity guard: a deliberately typo'd pattern made the scan pass green. Convert only if the consolidated scan keeps `.html` in scope AND keeps at least one canary; otherwise both rows are deletes. |
| F-019 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_404_retains_fallback_and_has_only_its_local_module,GeneratedDocumentTests.test_lander_and_404_share_one_byte_identical_game_subtree,GeneratedDocumentTests.test_lander_and_404_share_one_byte_identical_game_subtree.subtree` | verbatim pin (nine CSS declarations) | delete | The plan names this as a sweep row. Nine exact declaration strings from the authored stylesheet, including a full font stack; the reflow properties they stand for are measured for real by the Chromium geometry test at :636-653. |
| F-020 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_lander_and_404_share_one_byte_identical_game_subtree` | wording blacklist | delete | The four-word `fake_terminal` blacklist. Already ruled by the plan: this is the shape `no-prose-policing-tests` calls worse than useless, since a fifth spelling of the same idea sails through. |
| F-021 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_footer_game_target_and_focus_area_are_pinned_in_ordinary_flow,GeneratedDocumentTests.test_runtime_assets_are_local_and_privacy_surfaces_are_absent` | verbatim pin (CSS declarations) | delete | Twelve exact declarations across six rules, including `clamp()` arguments. Pins the stylesheet's spelling; the only real property (the identity panel becomes two columns at desktop width) is a layout fact a browser test can measure. |
| F-022 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_runtime_assets_are_local_and_privacy_surfaces_are_absent,GeneratedDocumentTests.test_chromium_geometry_keeps_wide_body_beside_toc_and_narrow_toc_inline` | verbatim pin (CSS declarations) | delete | Eleven exact grid declarations. `findings.md` W8's own correction says the Chromium geometry test at :636-653 is the observational twin and this is the pin: only the browser test catches an override or a cascade change. |
| F-023 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_chromium_geometry_owns_the_devtools_process_and_cleanup` | argv blacklist | delete | Asserts `--dump-dom` is absent and `about:blank` is last in the Chromium argv our own helper builds. A retired-flag blacklist over our own code; the behavior that mattered (readiness and shutdown ownership) is asserted by the cleanup assertions beside it. |
| F-024 | `website/tests/test_site_documents.py::GeneratedDocumentTests.test_chromium_geometry_owns_the_devtools_process_and_cleanup,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.StuckProcess.__init__,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.StuckProcess.poll,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.StuckProcess.terminate,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.StuckProcess.kill,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.StuckProcess.wait,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.NeverReadyConnection.__init__,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.NeverReadyConnection.call,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.NeverReadyConnection.evaluate,GeneratedDocumentTests.test_chromium_geometry_bounds_readiness_and_kills_a_stuck_process.NeverReadyConnection.close,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors.FakeProcess.__init__,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors.FakeProcess.poll,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors.FakeProcess.terminate,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors.FakeProcess.wait,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors.run,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors.run.FailingConnection.call,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors.run.FailingConnection.evaluate,GeneratedDocumentTests.test_chromium_geometry_preserves_primary_errors_and_surfaces_cleanup_errors.run.FailingConnection.close,GeneratedDocumentTests.test_palette_contrast_meets_text_and_non_text_thresholds` | resource-lifecycle behavior | keep | The geometry harness closes its DevTools connection, terminates and then kills a stuck browser, joins its server thread, bounds readiness polling, and preserves the primary error over cleanup errors. Leaked Chromium processes and swallowed errors are exactly the CI defect the 2026-08-15 checklist entry records. |
| F-027 | `website/tests/test_site_templates.py::TemplateContractTests.test_unknown_missing_duplicate_wrong_template_and_brace_tokens_fail,TemplateContractTests.test_extracted_content_cannot_expand_a_template_token` | injection defense | keep | Content extracted from README/manifesto/SECURITY must not be able to expand a template token. The input is prose that doc authors edit without ever running the build, so a `{{...}}` sequence arriving in content is a real accident, and re-expansion would splice source text into a position nobody reviewed. |
| F-050 | `website/tests/test_site_build.py::BuildAndInstallTests.test_dual_base_builds_have_the_exact_complete_manifest` | second copy of a production constant | delete | **Recipe verification, 2026-08-16.** **Recorded as a convert, but the assertion it proposes is already enforced unconditionally by production**, so the replacement can only go red via a `RuntimeError` from `build.py`, never from the assertion itself. That makes it untestable rather than structural, so the row is a plain delete. |
| F-051 | `website/tests/test_site_build.py::BuildAndInstallTests.test_dual_base_builds_have_the_exact_complete_manifest` | structural presence | keep | No symlink lands in the output, no unsubstituted `{{` survives, and the site base reaches the rendered hrefs. All three ship silently broken otherwise. |
| F-054 | `website/tests/test_site_build.py::BuildAndInstallTests.test_repeated_builds_are_byte_deterministic` | byte determinism | keep | Repeated builds are byte-identical, in place and to a fresh directory. This is what the CI double-build-and-diff gate depends on. |
| F-055 | `website/tests/test_site_build.py::BuildAndInstallTests.test_favicon_rejects_every_unreviewed_svg_node,BuildAndInstallTests.test_favicon_rejects_every_unreviewed_svg_node::ValueError::83fb7e` | mutate-one-string-assert-raises | delete | Six mutations of the favicon SVG proving the asset validator rejects extra nodes. In-tree asset, validated on every build; this only proves the guard fires. |
| F-056 | `website/tests/test_site_build.py::BuildAndInstallTests.test_favicon_rejects_non_path_canonical_mark_geometry,BuildAndInstallTests.test_favicon_rejects_non_path_canonical_mark_geometry::ValueError::eeab47,BuildAndInstallTests.test_favicon_rejects_noncanonical_rocket_root_or_mark_hierarchy,BuildAndInstallTests.test_favicon_rejects_noncanonical_rocket_root_or_mark_hierarchy::ValueError::f261ac` | mutate-one-string-assert-raises | delete | Three rocket-SVG structure mutations, same shape as F-055. |
| F-057 | `website/tests/test_site_build.py::BuildAndInstallTests.test_unapproved_external_url_fails_before_output_changes,BuildAndInstallTests.test_unapproved_external_url_fails_before_output_changes::ValueError::766f6a,BuildAndInstallTests.test_absent_local_route_has_no_partial_manifest_suppression,BuildAndInstallTests.test_absent_local_route_has_no_partial_manifest_suppression::ValueError::fe209c,BuildAndInstallTests.test_cross_document_fragment_must_exist_on_its_actual_target,BuildAndInstallTests.test_cross_document_fragment_must_exist_on_its_actual_target::ValueError::2ef080,BuildAndInstallTests.test_source_heading_cannot_duplicate_a_shell_id,BuildAndInstallTests.test_source_heading_cannot_duplicate_a_shell_id::ValueError::a78aaf,BuildAndInstallTests.test_rendered_reference_validation_retains_duplicate_ids,BuildAndInstallTests.test_rendered_reference_validation_retains_duplicate_ids::ValueError::a78aaf,BuildAndInstallTests.test_manifest_without_root_index_cannot_suppress_root_reference_failure,BuildAndInstallTests.test_manifest_without_root_index_cannot_suppress_root_reference_failure::ValueError::fe209c` | link and reference integrity | keep | Unapproved external links, absent local routes, absent cross-document fragments, and duplicate element ids all fail the build before any output changes. This is the half of `site_validation.py` that W2 says is real and stays: a broken internal link or a colliding heading id ships silently and no reviewer of a prose edit would see it. |
| F-058 | `website/tests/test_site_build.py::BuildAndInstallTests.test_shared_css_cannot_conceal_reviewed_shell_content,BuildAndInstallTests.test_shared_css_cannot_conceal_reviewed_shell_content::ValueError::cbe359` | adversarial obfuscation corpus | delete | Eighteen mutations feeding CSS escape sequences and comment-split property names to prove concealment cannot slip past. The stylesheet is in this tree and changes in the same commits; the threat model for in-tree artifacts is accidental regression, and no accident writes `d\69 splay: none`. |
| F-059 | `website/tests/test_site_build.py::BuildAndInstallTests.test_same_document_fragment_must_resolve_before_output_changes,BuildAndInstallTests.test_same_document_fragment_must_resolve_before_output_changes::ValueError::ee89e6` | reference integrity | keep | Renaming the main landmark id breaks the skip link, and the build refuses before touching existing output. Same family as F-057. |
| F-060 | `website/tests/test_site_build.py::BuildAndInstallTests.test_duplicate_rendered_references_cannot_hide_first_browser_destination,BuildAndInstallTests.test_duplicate_rendered_references_cannot_hide_first_browser_destination::ValueError::20a7d7` | bypass canary | convert | Rubric re-check, 2026-08-16. Not an in-tree tampering test: it feeds four SYNTHETIC duplicate-attribute canaries to `_validate_local_references`, so the same-tree argument does not apply. If `_attribute_map`'s check (`site_validation.py:159-163`) were dropped, `dict(attrs)` keeps the LAST, benign attribute and the canaries raise nothing. A repo-wide grep finds only two probes for that raise, this one and F-047, and F-047 is inside the deferred block. Convert per L-401's own rule that the `assertRaises` stays. No production change. |
| F-061 | `website/tests/test_site_build.py::BuildAndInstallTests.test_output_inside_repository_is_rejected_without_source_writes,BuildAndInstallTests.test_output_accepts_ordinary_spaced_and_hidden_directory_names,BuildAndInstallTests.test_output_rejects_dot_traversal,BuildAndInstallTests.test_output_rejects_dot_traversal::ValueError::c6b9ac,BuildAndInstallTests.test_output_rejects_parent_symlink_into_repository_when_supported,BuildAndInstallTests.test_output_rejects_parent_symlink_into_repository_when_supported::ValueError::e2885f,BuildAndInstallTests.test_existing_top_level_directory_symlink_is_rejected_without_touching_target,BuildAndInstallTests.test_existing_top_level_directory_symlink_is_rejected_without_touching_target::ValueError::4c5159,BuildAndInstallTests.test_existing_top_level_broken_symlink_is_rejected_without_replacement,BuildAndInstallTests.test_existing_top_level_broken_symlink_is_rejected_without_replacement::ValueError::4c5159,BuildAndInstallTests.test_existing_output_may_contain_only_selected_owned_entries,BuildAndInstallTests.test_existing_output_may_contain_only_selected_owned_entries::ValueError::1ffbe9,BuildAndInstallTests.test_symlink_entry_in_existing_output_is_rejected_when_supported,BuildAndInstallTests.test_symlink_entry_in_existing_output_is_rejected_when_supported::ValueError::cc52c0,BuildAndInstallTests.test_fifo_entry_in_existing_output_is_rejected_when_supported,BuildAndInstallTests.test_fifo_entry_in_existing_output_is_rejected_when_supported::ValueError::25bf38,BuildAndInstallTests.test_non_directory_output_is_rejected,BuildAndInstallTests.test_non_directory_output_is_rejected::ValueError::4c5159` | boundary guard | keep | The output path is operator-supplied input: dot traversal, symlinked parents, symlinked or broken output roots, FIFOs, non-directories, and unowned entries are all rejected without writing or destroying anything. This is `development-principles` principle 3's operator-input boundary, and the assertions prove the source tree and pre-existing files survive. |
| F-062 | `website/tests/test_site_build.py::BuildAndInstallTests.test_every_static_module_import_resolves_to_the_literal_manifest,BuildAndInstallTests.test_every_static_module_import_resolves_to_the_literal_manifest::ValueError::456e31` | reference integrity | keep | Every JavaScript module import resolves to a file in the shipped manifest. A renamed module ships a page whose script silently 404s. |
| F-063 | `website/tests/test_site_build.py::BuildAndInstallTests.test_model_ships_byte_for_byte_without_generated_proof_composition,BuildAndInstallTests.test_collision_and_world_ship_separately_in_the_exact_manifest` | byte parity | keep | The Lander model, world, and collision modules ship byte for byte from source and are not inlined into one another. Guards against the build quietly reacquiring a composition step. |
| F-065 | `website/tests/test_site_build.py::BuildAndInstallTests.test_collision_and_world_ship_separately_in_the_exact_manifest` | schema shape | keep | `validate_game_manifest` rejects a manifest missing the collision module or carrying an extra one. Structural check on a production function, not on any authored spelling. |
| F-067 | `website/tests/test_site_build.py::BuildAndInstallTests.test_authored_lander_modules_and_tests_stay_below_the_review_ceiling` | style-rule threshold | delete | Asserts every authored Lander module and test is under 1,000 lines. **Ruled by the lead 2026-08-16**: a style rule mechanized as a test is not a doctrine 2 threshold, because style belongs in the linter and a rule enforced by one package's test suite is enforced inconsistently anyway (no other package in the repository carries this check). The ceiling itself does matter, so if it is worth keeping it moves to `scripts/lint-files.sh` where it would bind the whole repository, rather than being lost here. |
| F-068 | `website/tests/test_site_build.py::BuildAndInstallTests.test_content_or_template_failure_leaves_existing_output_byte_identical,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output::OSError::9e8486,BuildAndInstallTests.test_failure_before_and_during_each_swap_boundary_restores_exact_output::RuntimeError::4183b7` | failure-injection / atomicity | keep | A content, template, rename, or verification failure leaves the previously installed output byte identical. Whether this machinery should exist at all is W7, deferred; while it exists these are the only tests of its failure paths, and a half-installed site is the failure it prevents. |
| F-069 | `website/tests/test_site_build.py::BuildAndInstallTests.test_backup_cleanup_failure_warns_after_successful_commit` | verbatim pin (our own warning text) | delete | Pins the exact wording of the retained-backup warning. The behavioral half of the test (a cleanup failure does not fail the build, and the output is still correct) is at :506, :508-511 and stays. |
| F-070 | `website/tests/test_site_build.py::BuildAndInstallTests.test_cli_requires_full_shape_and_reports_one_line_errors,BuildAndInstallTests.test_cli_requires_full_shape_and_reports_one_line_errors::assertRegex::aad29f` | exit code + output format | keep | The CLI exits 1 and writes exactly one line to stderr. This asserts the shape (single line, `error:` prefix, no traceback), not the message, and a leaked traceback is a real regression. |
| F-071 | `website/tests/test_site_build.py::BuildAndInstallTests.test_remote_runtime_asset_references_fail_before_output_changes,BuildAndInstallTests.test_remote_runtime_asset_references_fail_before_output_changes::ValueError::3a6b8d` | boundary guard | keep | No shipped CSS or JS may reference a remote origin, and the build refuses before touching output. This is the site's self-containment guarantee at the point where it can actually be enforced. |
| F-072 | `website/tests/test_site_build.py::BuildAndInstallTests.test_harmless_javascript_line_comment_is_allowed` | negative control | keep | An ordinary local comment survives the remote-reference scan. Proves F-071's guard is not over-broad, which is the failure mode that would otherwise be discovered by a blocked PR. |
| F-073 | `website/tests/test_site_content.py::BreakParser.__init__,BreakParser.handle_startendtag,BreakParser.handle_starttag` | vocabulary set pin | delete | Restates the seven content token names. Verified by execution: dropping `HOME_IDENTITY` from the substitutions fails the build with `missing substitutions for required tokens`, so the vocabulary is enforced by building. |
| F-074 | `website/tests/test_site_content.py::L47-48` | **[line-anchored: between functions]** verbatim pin | delete | Pins two authored README phrases (`<strong>Durable agents</strong>`, `<strong>SSH-over-Tailscale control plane</strong>`) inside the rendered identity block. The drift contract at :266-283 already ties the rendered identity to the README. |
| F-075 | `website/tests/test_site_content.py::SourceContractTests.test_permanent_sources_render_the_complete_content_vocabulary` | derivation parity | keep | Meta descriptions and the onboarding prompt are exactly the HTML-escaped canonical values, each document renders exactly one `h1`, and the security content carries the reporting URL. Escaping regressions are silent and security-relevant. |
| F-076 | `website/tests/test_site_content.py::SourceContractTests.test_contents_navigation_derives_nested_h2_h3_links_and_escapes_labels` | derivation parity | keep | The prompt served in the page is byte-identical to the packaged source, through the README projection and the HTML escape. This is the single-source-of-truth guarantee for the one string operators paste into an agent. |
| F-077 | `website/tests/test_site_content.py::SourceContractTests.test_document_without_h2_or_h3_omits_contents_navigation` | structural presence | keep | The template carries exactly one prompt token and no inlined copy of the prompt body. Cheap, structural, and it is what makes F-076's parity meaningful. |
| F-078 | `website/tests/test_site_content.py::SourceContractTests.test_document_without_h2_or_h3_omits_contents_navigation,SourceContractTests.test_manifesto_source_path_is_single` | whole-tree duplicate scan | delete | Walks every non-test file under `website/` asserting the prompt body appears nowhere. A stray duplicate is a review finding; the parity check at F-076 is what guarantees the served copy is canonical. |
| F-080 | `website/tests/test_site_content.py::SourceContractTests.test_manifesto_source_path_is_single,SourceContractTests.test_crlf_does_not_break_complete_documents,SourceContractTests.test_assistance_projection_is_exactly_canonical_and_html_escaped` | derivation parity | keep | The README projection of the assistance prompt must not drift from the packaged source, and the markers and fence must stay well formed. Two files that must agree, with the check on the derivation rather than on either copy's wording. |
| F-081 | `website/tests/test_site_content.py::SourceContractTests.test_assistance_projection_is_exactly_canonical_and_html_escaped,SourceContractTests.test_website_sources_do_not_duplicate_the_authored_prompt_body` | boundary guard | keep | The assistance source must be UTF-8, NUL-free, LF-terminated, present, and not a symlink. A BOM or CRLF here silently changes the bytes agents receive, and an editor introduces both by accident. |
| F-082 | `website/tests/test_site_content.py::SourceContractTests.test_document_structure_failures_are_closed,SourceContractTests.test_document_structure_failures_are_closed::ContractError::d62e9a,SourceContractTests.test_unsupported_markdown_and_hard_breaks_fail_closed` | drift detection | keep | The home identity fails closed when the README heading goes missing, duplicates, or drifts from the contract. The input is prose edited by people who never build the site, which is exactly where a silent break happens. |
| F-083 | `website/tests/test_site_content.py::SourceContractTests.test_unmatched_reserved_inline_markers_fail_instead_of_rendering_literally` | injection defense | keep | A reviewed destination URL is escaped into exactly one `href` attribute and cannot break out into an event handler. Security test, correctly narrow. |
| F-084 | `website/tests/test_site_content.py::SourceContractTests.test_unmatched_reserved_inline_markers_fail_instead_of_rendering_literally,SourceContractTests.test_unmatched_reserved_inline_markers_fail_instead_of_rendering_literally::ContractError::60945d,SourceContractTests.test_home_selection_still_fails_on_missing_duplicate_or_drift,SourceContractTests.test_home_selection_still_fails_on_missing_duplicate_or_drift::ContractError::48374d` | boundary guard | keep | Invalid UTF-8, a BOM, a missing file, and a symlinked source each fail closed. Same boundary as F-081, on the document inputs. |
| F-085 | `website/tests/test_site_content.py::SourceContractTests.test_home_selection_still_fails_on_missing_duplicate_or_drift,SourceContractTests.test_source_relative_links_are_explicitly_mapped_and_others_fail,SourceContractTests.test_source_relative_links_are_explicitly_mapped_and_others_fail::ContractError::766f6a,SourceContractTests.test_reviewed_link_url_is_escaped_as_one_href_attribute` | content contract | keep | The security document keeps exactly one reference definition pointing at the reporting URL, and an email reporting path fails the build. SECURITY.md is edited by people writing prose, and a stray contact address is a real, silent policy break. The positive case at :352-357 keeps it from over-firing. |
| F-086 | `website/tests/test_site_content.py::SourceContractTests.test_permanent_sources_render_the_complete_content_vocabulary,SourceContractTests.test_getting_started_introduction_projects_from_readme,SourceContractTests.test_supported_source_additions_render_without_contract_edits,SourceContractTests.test_contents_navigation_derives_nested_h2_h3_links_and_escapes_labels,SourceContractTests.test_website_sources_do_not_duplicate_the_authored_prompt_body,SourceContractTests.test_assistance_projection_drift_and_framing_fail_closed,SourceContractTests.test_assistance_projection_drift_and_framing_fail_closed::ValueError::d9687c,SourceContractTests.test_assistance_source_requires_exact_regular_lf_utf8,SourceContractTests.test_assistance_source_requires_exact_regular_lf_utf8::ValueError::0d71ce,SourceContractTests.test_missing_or_symlinked_assistance_source_fails_closed,SourceContractTests.test_missing_or_symlinked_assistance_source_fails_closed::ValueError::5f411f*2,SourceContractTests.test_document_structure_failures_are_closed,SourceContractTests.test_unsupported_markdown_and_hard_breaks_fail_closed,SourceContractTests.test_unsupported_markdown_and_hard_breaks_fail_closed::ContractError::2fc8fe,SourceContractTests.test_invalid_utf8_bom_missing_and_symlinked_input_fail_closed,SourceContractTests.test_invalid_utf8_bom_missing_and_symlinked_input_fail_closed::ContractError::05ac6a,SourceContractTests.test_invalid_utf8_bom_missing_and_symlinked_input_fail_closed::ContractError::8d8b89*2` | parser behavior over synthetic fixtures | keep | Nine tests exercising the Markdown subset renderer against documents the test itself writes: heading structure, list and emphasis rendering, ToC derivation and escaping, CRLF handling, unsupported-syntax refusal, unmatched-marker refusal, link mapping, and reference-definition consumption. The asserted strings are the renderer's output for the test's own input, which is value identity rather than authored prose. W3 defers the question of whether this renderer should exist; while it does, these are its only tests. |
| F-087 | `website/tests/test_lander_404.py::SiteBaseTests.test_root_and_project_bases_pass,SiteBaseTests.test_invalid_bases_are_rejected` | boundary guard | keep | `validate_site_base` rejects 28 malformed or hostile `--site-base` values. A CLI argument is operator-authored input, the canonical trust boundary. |
| F-088 | `website/tests/test_lander_404.py::BuildTests.test_root_and_project_builds_have_the_exact_output_set,L142` | **[line-anchored: between functions]** second copy of a production constant | delete | **Recipe verification, 2026-08-16.** Same as F-050: the proposed replacement restates something `build.py` enforces on every run, so it cannot fail on its own. Delete. |
| F-089 | `website/tests/test_lander_404.py::BuildTests.test_root_and_project_builds_have_the_exact_output_set` | structural presence (duplicate) | keep | Both site bases render the 404 and Lander routes with every asset href rebased and no unsubstituted token. Overlaps test_site_build.py:7-18 (F-050/F-051) but runs against the real repository rather than a fixture copy; worth one row, and the duplication is flagged in the summary. |
| F-090 | `website/tests/test_lander_404.py::BuildTests.test_builder_replaces_only_an_owned_output_set,BuildTests.test_builder_rejects_every_repository_output_before_writing` | boundary guard | keep | The builder refuses every output path inside the real repository and leaves the real source files and staging directories untouched. Unlike F-061 this runs against the working tree, which is the case that would actually destroy someone's work. |
| F-091 | `website/tests/test_lander_404.py::BuildTests.test_closed_template_vocabulary_and_required_references_fail_closed` | token-set pin + mutations | delete | The 404 template's token set restated, plus mutations proving `_validate_template` rejects an unknown token and a missing required reference. Same shape and same reasoning as F-025 and F-026. |
| F-092 | `website/tests/test_lander_404.py::StaticDocumentTests.test_no_javascript_document_is_a_useful_semantic_404,StaticDocumentTests.test_preflight_controls_are_hidden_but_scene_and_breadcrumb_are_not` | structural presence | keep | Without JavaScript the 404 is still a page with landmarks, one home link and a visible message, and the game's preflight controls start hidden while the scene does not. This is the no-JS contract the page exists to honour. |
| F-093 | `website/tests/test_lander_404.py::StaticDocumentTests.test_accessible_names_live_region_and_initial_focus_surface_are_pinned` | exact attribute pins + tag blacklist | delete | **Recipe verification, 2026-08-16.** Same shape as F-007: production enforces the attributes unconditionally, so the proposed assertion cannot bite. Delete. |
| F-094 | `website/tests/test_lander_404.py::StaticDocumentTests.test_scene_geometry_and_start_target_are_fixed_and_responsive,StaticDocumentTests.test_scene_geometry_and_start_target_are_fixed_and_responsive::assertNotRegex::9ff0a4,StaticDocumentTests.test_scene_geometry_and_start_target_are_fixed_and_responsive::assertNotRegex::e16855` | verbatim pin (CSS and SVG coordinates) | delete | Roughly forty exact pins: `viewBox`, CSS percentages, transform origins, terrain vertex coordinates, path prefixes, and an `M` count of 75. Verified by execution: `site_game_validation.py:183` enforces the `0 0 1000 640` viewBox at every build (drifting it to `0 0 1000 641` fails `build_site`), and the scaffold/terrain paths are held by derivation parity in the `.mjs` suite (F-131, F-137). |
| F-095 | `website/tests/test_lander_404.py::StaticDocumentTests.test_css_has_only_bounded_keyframes_and_reduced_motion_preserves_live_plumes` | verbatim pin (CSS declarations) | delete | Keyframe-name set, animation shorthands, and reduced-motion declarations pinned as strings. The reduced-motion behavior has observational twins that run in a real browser (test_lander_phase4j_browser.py:196, test_lander_phase4l_browser.py:47, test_lander_phase4q_browser.py:75-115). |
| F-096 | `website/tests/test_lander_404.py::StaticDocumentTests.test_engine_transforms_keep_both_nozzle_anchors_fixed` | computed geometric invariant | keep | Executes each engine's CSS transform list against its declared origin and proves the nozzle anchor is fixed for every thrust angle and plume scale. This computes a property rather than pinning a value, and a wrong transform makes the plume visibly detach. |
| F-098 | `website/tests/test_lander_404.py::StaticDocumentTests.test_forbidden_runtime_surfaces_are_absent` | source blacklist + self-testing canaries | convert | **Recipe verification, 2026-08-16.** Same PARTIAL as F-018 and the same two conditions. This is the copy with the real coverage, established earlier by injecting `window["fetch"]`, so if only one survives it is this one. |
| F-099 | `website/tests/test_lander_404.py::RocketAssetTests.test_root_is_self_contained_and_named` | verbatim pin | delete | Pins the rocket SVG's `viewBox` and the absence of `width`/`height`. Authored asset geometry; scaling behavior is visible on the page. |
| F-100 | `website/tests/test_lander_404.py::RocketAssetTests.test_root_is_self_contained_and_named,RocketAssetTests.test_root_is_self_contained_and_named::assertNotRegex::afa8f9` | self-containment guard | keep | The inlined rocket asset carries `role="img"` with a labelling pair and contains no `<script>`, `<image>`, `<animate>`, or remote URL. An SVG re-export from a design tool routinely adds exactly those, and the asset is inlined into every page. |
| F-101 | `website/tests/test_lander_404.py::RocketAssetTests.test_stable_global_ids_and_group_hierarchy_are_exact` | id-set pin | delete | **Recipe verification, 2026-08-16.** **The row's justification is factually wrong** in the same way as F-007: the SVG fragment contract it proposes to derive is already enforced unconditionally at build time. Delete. |
| F-102 | `website/tests/test_lander_404.py::RocketAssetTests.test_selected_geometry_and_temperature_layers_are_exact` | verbatim pin (SVG path data and fills) | delete | Nine full path strings and seven fill colors copied into the test. Authored artwork; every future logo tweak becomes a two-file ceremony. |
| F-103 | `website/tests/test_lander_404.py::FaviconAssetTests.test_favicon_is_a_self_contained_flame_free_mark,FaviconAssetTests.test_favicon_is_a_self_contained_flame_free_mark::assertNotRegex::54b0b7` | self-containment guard | keep | The favicon is a bare `viewBox` root with four known ids and no script, style, image, use, or remote reference. Same real risk as F-100, on the asset browsers fetch on every page. |
| F-104 | `website/tests/test_lander_404.py::FaviconAssetTests.test_favicon_is_a_self_contained_flame_free_mark,FaviconAssetTests.test_favicon_is_a_self_contained_flame_free_mark::assertNotRegex::c5bf42` | wording blacklist | delete | `assertNotRegex(source, r"plume\|engine\|#d94a1e\|#ff7a00\|#ffe09a")`: a five-token blacklist standing in for "the favicon has no flame". A sixth spelling passes; the id-set assertion above it is the structural form of the same fact. |
| F-105 | `website/tests/test_lander_404.py::FaviconAssetTests.test_favicon_geometry_matches_the_selected_mark_exactly` | derived-copy parity | keep | Every shared mark element in the favicon has attributes identical to the rocket's. This is the sanctioned parity shape: two artifacts that must agree, checked by derivation rather than by a third copy. |
| F-106 | `website/tests/test_lander_404.py::StaticDocumentTests.test_fixed_color_contrast_meets_text_and_graphic_thresholds` | three-decimal ratio pin | convert | `math.isclose(antenna, 3.788, abs_tol=0.001)` is the exact shape W4 converted in `test_site_documents.py`, surviving here because W4's landed change did not touch this file. Replacement: keep the two WCAG inequalities, drop the 3.788 pin, and read the colors out of the built `lander.css` (they are literals there) instead of restating them. No production change. See the overlap note in the summary. |
| F-107 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_scene_stage_contains_world_and_chrome_before_the_normal_flow_rail` | source-order pin + CSS declaration pins | delete | Pins the source order of twelve element ids in the fragment and six exact CSS declarations. Lexical order of our own markup plus stylesheet spelling; the layout consequences are measured by the phase 4J browser suite. |
| F-108 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_fuel_has_one_hidden_non_live_value_and_one_silent_gauge` | tag blacklist + exact attribute dicts + JS source pins | delete | `assertNotIn("output"/"meter"/"progress")`, whole-dict equality on three fuel elements, and three assertions on `lander-game.js` source text (`this.lander_fuel_value.textContent` present, `.value` absent, `data-fuel-band` absent anywhere). Spelling pins and a retired-shape blacklist. |
| F-109 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_fuel_has_one_hidden_non_live_value_and_one_silent_gauge` | structural presence | keep | Exactly one polite live region and one `role="status"` in the whole fragment. Two live regions is a real screen-reader defect that nothing else catches. |
| F-110 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_action_source_order_identities_and_static_focus_exclusion_are_exact` | source-order pin + exact attribute dicts + CSS pins | delete | Whole-dict equality on the restart and exit buttons and three CSS declarations including `min-inline-size: 44px`. The touch-target minimum has an observational twin that measures the real rect: test_lander_phase4j_browser.py:550-551 and test_lander_phase4l_browser.py:313-314 (both verified passing against a real Chromium in this worktree). |
| F-112 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_mutations_catch_duplicate_authorities_wrong_order_and_missing_static_agent_state` | mutate-one-string-assert-raises | delete | Five fragment mutations proving `validate_game_contract` rejects wrong element types, a missing agent state, a moved button, and a duplicate live region. The `site_game_validation.py` counterpart runs on every build; the same in-tree-tampering reasoning as F-026. |
| F-113 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_exact_action_rail_shape_rejects_anonymous_wrappers_and_interposed_children` | mutate-one-string-assert-raises | delete | Eleven mutations wrapping or interposing elements in the action rail. Pins the fragment's exact child structure through the validator. |
| F-114 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_control_line_sources_reject_empty_duplicate_and_moved_structure` | mutate-one-string-assert-raises | delete | Three control-line mutations (emptied, duplicated, moved). Same shape. |
| F-115 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_reviewed_root_and_required_tag_identities_reject_stray_structure` | mutate-one-string-assert-raises | delete | Nine mutations changing root framing and element tag names. Same shape. |
| F-116 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_gauge_has_independent_graphite_boundary_level_color_and_height` | verbatim pin (CSS declarations) | delete | Seven exact gauge declarations including `height: min(7rem, calc(100cqh - 1rem))`. Stylesheet spelling. |
| F-117 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_gauge_has_independent_graphite_boundary_level_color_and_height` | threshold | convert | **Recipe verification, 2026-08-16.** Verified, with a trap to record: the token regex also matches `--fuel-level-color: var(...)`, so without an explicit non-vacuity assertion the loop can iterate over nothing and assert nothing. Add one. |
| F-118 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_visually_hidden_fuel_and_direction_use_clipping_not_removal,ArcadeCssTests.test_visually_hidden_fuel_and_direction_use_clipping_not_removal::assertNotRegex::3f9115` | verbatim pin (CSS declarations) + property blacklist | delete | Six exact declarations of the `.visually-hidden` rule plus a three-property regex blacklist. The behavior (the hidden fuel text stays measurable and readable to assistive tech) is measured for real at test_lander_phase4j_browser.py:585-590, which asserts the 1px rect, the clip, and that display and visibility are not removed. |
| F-119 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_pseudo_can_is_the_exact_six_layer_graphite_orange_silhouette,ArcadeCssTests.test_pseudo_can_is_the_exact_six_layer_graphite_orange_silhouette::assertNotRegex::51c557` | verbatim pin (six-layer gradient list) | delete | Pins six `linear-gradient` layers with exact colors and positions, plus a four-property blacklist. Pixel art expressed as CSS; the browser test at :575-581 renders it and counts changed pixels, which is the property that matters. |
| F-120 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_arcade_font_banner_rail_and_touch_scope_have_no_external_surface,ArcadeCssTests.test_arcade_font_banner_rail_and_touch_scope_have_no_external_surface::assertNotRegex::386c80` | verbatim pin (font stack, colors, selectors) | delete | Pins the font stack, two banner data attributes, two inset shadows, and the single `touch-action` selector. The no-external-resource half (`@font-face\|@import\|url(`) duplicates the build-time guard verified at F-071, which fails the build rather than a test. |
| F-121 | `website/tests/test_lander_phase4j.py::ArcadeCssTests.test_installed_and_absent_paints_are_exact_and_add_no_css_generated_text` | verbatim pin (CSS declarations) | delete | Seven exact paint declarations for the two agent states, plus a `content: ""` count. Stylesheet spelling. |
| F-123 | `website/tests/test_lander_phase4j_browser.py::L575-578,L583` | **[line-anchored: between functions]** computed-style verbatim pin | delete | `backgroundPosition == "6px 2px, 4px 0px, ..."`, the six-item `backgroundRepeat` list, and `assertIn("Cascadia Mono", fontFamily)`. These are the authored CSS values read back out of the browser; the real assertions beside them (two distinct colors rendered, more than 100 pixels changed) stay. |
| F-124 | `website/tests/test_lander_phase4j_browser.py` | browser-observed behavior | keep | Seven tests driving a real Chromium over the built site: focus order, tab order, departure authorities, live-region description updates, computed touch-target rects, hidden-fuel clipping, and reduced-motion behavior. These are the observational twins the CSS-text pins above are being deleted in favour of; verified passing in this worktree (7 tests, 21 s). |
| F-125 | `website/tests/test_lander_phase4k_browser_cleanup.py::Phase4KBrowserCleanupTests.test_null_root_navigation_race_times_out_without_evaluation_or_cleanup_failure` | verbatim pin (our own expression text) | delete | **Corrected 2026-08-19 (map re-baseline).** Re-derived at HEAD (the file moved) and narrowed off `:397`. `:399-401` assert three substrings of the readiness expression the harness itself builds, two lines after `:397` compares the collected expressions against `_readiness_expression(loaded_url)` exactly. That comparison holds the invariant and stays; these three restate its contents. |
| F-126 | `website/tests/test_lander_phase4k_browser_cleanup.py::Phase4KBrowserCleanupTests.test_devtools_target_bounds_an_accepting_but_stalled_browser_socket` | source blacklist | delete | Asserts the probe source does not mention three controller methods, i.e. that the probe drives the page through real events. A three-name blacklist over our own harness source; a fourth spelling passes and the probe's realism is a review concern. |
| F-127 | `website/tests/test_lander_phase4k_browser_cleanup.py` | resource-lifecycle behavior | keep | The phase 4K harness owns and tears down its browser, profile, server, and thread, and surfaces the primary error. Same invariant as F-024, for the other harness. |
| F-128 | `website/tests/test_lander_phase4l_browser.py` | browser-observed behavior and harness plumbing | keep | Examined in full for the target shapes and none found beyond F-122/F-123/F-125/F-126: these files assert measured geometry, computed styles compared against each other, input outcomes, and DevTools plumbing. They are the observational twins that make the CSS and source pins deletable. |
| F-129 | `website/tests/onboarding-copy.test.mjs` | **[dead]** non-example | keep | No rows. This file is already the shape the rule prescribes: it asserts that status text is non-empty and that the three outcomes are mutually distinct, without pinning a single word. Worth citing as the pattern for the sentence-only observables elsewhere. |
| RB-012 | `website/tests/onboarding.test.mjs` | non-example | keep | Added by the 2026-08-19 re-baseline as F-129's successor: `onboarding-copy.test.mjs` was replaced by this file at `1b9bbb9b` (2026-08-17). It keeps F-129's property (status text asserted as non-empty and mutually distinct, never word by word, at `:71-72` and `:92-93`) and adds ARIA structure assertions at `:103-118` that are structural rather than prose. No rows, and still the pattern worth citing for a sentence-only observable. |
| F-130 | `website/tests/lander-model.test.mjs::L712-728` | **[line-anchored: not Python]** derivation parity | keep | The static scaffold path in the template equals the path the controller renders from the model, member for member. This is the real guarantee behind every scaffold coordinate pinned elsewhere, and it is the reason those pins are deletable. |
| F-131 | `website/tests/lander-phase4i.test.mjs::L272-306` | **[line-anchored: not Python]** derivation parity | keep | The no-script template's scaffold, battery, and antenna geometry equals what the model computes for the same site, and the colliders contain the members. This is the cross-artifact agreement the static SVG exists to satisfy; nothing else holds it. |
| F-132 | `website/tests/lander-phase4i.test.mjs::L307-314` | **[line-anchored: not Python]** verbatim pin (CSS declarations) | delete | Six regexes pinning exact antenna rules with colors (`.antenna-signal-1 { stroke: #d94a1e; opacity: 1; }`). Stylesheet spelling; the staged-signal behavior is exercised in the browser suites. |
| F-134 | `website/tests/lander-phase4l.test.mjs::L70-78` | **[line-anchored: not Python]** structural presence | keep | The controls element has exactly two leaf lines with non-empty text and stable class names, asserted through the real controller. Structural, and it does not touch the copy itself. |
| F-135 | `website/tests/lander-phase4l.test.mjs::L79-87` | **[line-anchored: not Python]** verbatim pin (CSS declarations) | delete | Two regexes pinning `display: block; white-space: nowrap` and a media-query grid declaration. Stylesheet spelling; test_lander_phase4l_browser.py measures the rendered result. |
| F-137 | `website/tests/lander-phase4m.test.mjs::L240-246` | **[line-anchored: not Python]** derivation parity | keep | The template's static sky paths equal the projection the world module computes for the static seed, and each layer appears once. Same family as F-130 and F-131. |
| F-138 | `website/tests/lander-phase4m.test.mjs::L247-255` | **[line-anchored: not Python]** structural presence (third copy) | delete | Asserts each shell template's footer Lander link has a non-empty `aria-label` equal to its `title`. The third of three copies of one fact; test_site_documents.py:458-466 (F-011) holds it on the built output, which is where it ships. |
| F-139 | `website/tests/lander-phase4s.test.mjs::L28-36` | **[line-anchored: not Python]** module-graph structure | keep | The collision module imports nothing and the world module imports it exactly once. A real layering invariant that an ordinary refactor can break and that the type system cannot express. |
| F-142 | `website/tests/lander-world.test.mjs::L202-209` | **[line-anchored: not Python]** derived path shape | keep | The terrain surface is one open path with no curve or close commands, derived from the vertex list. A shape property computed from the derivation, not a coordinate pin. |
| F-143 | `website/tests/lander-world.test.mjs::L210-211` | **[line-anchored: not Python]** verbatim pin (template attributes) | delete | Pins `stroke-linejoin="miter"` and `stroke-miterlimit="2"` on the terrain layer and the layer's path count. Authored markup attributes. |
| F-144 | `website/tests/lander-model.test.mjs` | state-machine and physics behavior | keep | Examined in full; outside the target family. These assert simulation outcomes, collision classification, retention and camera bounds, input mixing, determinism, and controller state transitions against computed expectations and the suite's own fixtures. Nothing here pins authored copy. |
| F-145 | `website/tests/site_test_support.py::L54-57,L58-75` | **[line-anchored: module level]** second copies of production constants | delete | **Recipe verification, 2026-08-16.** The `EXPECTED_FILES` half retires under F-050, which is now itself a delete for the reason recorded there. The `CSP` half is covered by F-002's verified directive-level conversion, and the constant survives this sweep anyway because its third consumer (F-046) sits in the deferred block. Nothing here needs a conversion. |
| L-201 | `cli/tests/test_skel.py::test_seeds_open_with_managed_by_agentworks_marker` | header-marker pin | delete | Pins an authored comment line in a shell seed. |
| L-202 | `cli/tests/test_skel.py::test_seeds_reference_agentworks_rc_filename` | `AGENTWORKS_RC in BASHRC/ZSHRC` | convert | **Recipe verification, 2026-08-16.** Verified by seeding a temp HOME with the real seeds, driving `_write_agentworks_rc` twice through a transport that actually executes the emitted grep-or-append command via `sh -c`, and asserting the source-line count is unchanged, plus a non-vacuity assertion that the seeds carry a source line to begin with. Renaming `AGENTWORKS_RC` in `vms/initializer/shell_env.py:23` makes it fail. **Record the trap**: the first attempt counted lines containing the literal current filename and PASSED while the invariant was broken, because the appended duplicate carries the NEW name. The counter must be filename-agnostic. Design cost: this is the only row in its group that runs a real local shell and writes into a tmp HOME. |
| L-203 | `cli/tests/test_skel.py::test_seeds_use_guarded_source_line` | exact guarded source-line pin | delete | Pins authored shell text. The property (a shell opening before init does not error) is only observable by executing a shell, which R2.4 does not ask us to build. |
| L-204 | `cli/tests/test_skel.py::test_seeds_banner_has_agw_prefix_and_mode_label` | banner prefix and mode labels | delete | Authored prompt text. |
| L-205 | `cli/tests/test_skel.py::test_seeds_banner_uses_labeled_status_fields` | labeled status fields (`vm:`, `ws:`, `ag:`, `se:`, each preceded by a space) | delete | Authored prompt text. |
| L-206 | `cli/tests/test_skel.py::test_seeds_banner_field_order_is_vm_ws_ag_se` | `find()`-based field-ORDER pin over the seed text | delete | A lexical order pin over an authored artifact, the doctrine's explicit delete. |
| L-207 | `cli/tests/test_skel.py::test_seeds_mode_coded_colors` | ANSI color-code pins | delete | Authored presentation detail. |
| L-208 | `cli/tests/test_skel.py::test_seeds_vm_hostname_fallback` | hostname-fallback expansion pins | delete | Authored shell text. |
| L-209 | `cli/tests/test_skel.py::test_seeds_status_fields_render_conditionally` | conditional-render pins | delete | Authored shell text. |
| L-210 | `cli/tests/test_skel.py::test_seeds_have_git_branch_support` | git-branch support pins | delete | Authored shell text, including a hardcoded Debian path. |
| L-211 | `cli/tests/test_skel.py::test_seeds_two_line_prompt_with_banner_above` | two-line prompt layout pin | delete | Authored presentation detail. |
| L-212 | `cli/tests/test_skel.py::test_seeds_use_prompt_command_or_precmd_hooks` | `PROMPT_COMMAND` / `precmd` hook pins | delete | Authored shell text. |
| L-213 | `cli/tests/test_skel.py::test_bash_seed_keeps_debian_baselines` | Debian baseline pins in the bash seed | delete | Authored shell text. |
| L-214 | `cli/tests/test_skel.py::test_zsh_seed_includes_compinit` | `compinit` pin in the zsh seed | delete | Authored shell text. |
| L-215 | `cli/tests/test_skel.py::test_write_skel_seeds_writes_both_files_to_etc_skel,test_write_skel_seeds_skel_paths_are_etc_skel` | `_write_skel_seeds` writes both files, via `sudo tee`, chmod 644 | keep | Behavioral: the writer's effects are what make `useradd -m` inherit the seed. Asserted on recorded calls, not on text. |
| L-216 | `cli/tests/test_skel.py::test_write_skel_seeds_runs_after_apt_install_in_initializer` | seed write ordering relative to apt install in the initializer | keep | Behavioral ordering observed through the recorded call sequence, which is the observational form the doctrine prefers over a lexical pin. |

## Group 6: source guards

52 live rows: 25 delete, 4 convert, 23 keep, out of a ledger of 63 (one `[dead]`, ten
`[subtracted]`).

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| A-080 | `cli/tests/test_database_migration_safety.py::test_busy_state_error_message_and_hint_are_not_caller_suppliable` | **[subtracted: instance-model]** constructor-signature pin | keep | Rubric re-check, 2026-08-16. Not text at all: `list(inspect.signature(BusyStateError.__init__).parameters) == ["self"]` varies with the real constructor shape and is the sole enforcement of an invariant `errors.py:106-107` states in prose alone, that no caller can inject remediation prose. A-081 still passes if a defaulted `message` parameter is added, so it does not carry it. Cost: any signature change, including a good one, breaks it. |
| C-004 | `cli/tests/test_obtain_token_removed.py::test_obtain_token_method_gone_from_base` | identifier blacklist over module source | delete | Reads `git_credential/base.py` source text and asserts four identifier spellings are absent. This is the doctrine's "pins how our code is written": a rename to `fetch_token` sails through, and the in-tree threat model is accidental regression, not adversarial reintroduction. |
| C-005 | `cli/tests/test_obtain_token_removed.py::test_obtain_token_method_gone_from_concrete_providers` | **[unverified]** identifier blacklist over module source | delete | Same shape over the two concrete providers. The property that matters (tokens resolve through the framework) is observable and covered by `test_git_credentials_token_resolve.py`, which runs the resolve pass instead of reading source. Not executed: the sibling-coverage claim was read, not executed. |
| C-006 | `cli/tests/test_obtain_token_removed.py::test_manager_modules_have_no_obtain_token_calls` | identifier blacklist over module source | delete | Asserts four manager modules contain no `obtain_token` substring. Substring absence in source is not an invariant; a reintroduced call changes which tokens reach the target, which the resolve tests assert directly. |
| C-007 | `cli/tests/test_obtain_token_removed.py::test_legacy_env_var_names_gone_from_git_credentials_module` | retired-name blacklist over module source | convert | Rubric re-check, 2026-08-16. Kind-2 capable and today the ONLY probe: every test in `test_git_credentials_token_resolve.py` sets `AW_SECRET_*`, so a legacy fallback firing only when the primary is absent passes all of them. Verified by execution: with the two legacy variables set and the framework name unset, `Resolver.resolve()` under `REFUSE` raises `SecretUnavailableError: secret resolution failed for: git-token-github`. That observation is the replacement. It also subsumes C-004's `_prompt_token` half, which is why C-004 stands. |
| E-086 | `cli/tests/plugins/gcp/test_platform.py::test_delete_continues_after_allow_read_failure_and_reports_retained_allow,test_delete_keeps_deny_when_allow_read_and_instance_delete_are_indeterminate` | redaction defense | keep | Invariant: a provider's private error detail never reaches an operator-facing warning or hint. `provider-private-detail` is the sentinel this test injected into the fake's error. |
| E-099 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_helper_does_not_close_manager_owned_progress` | log-marker presence and absence | keep | Invariant: the helper does not close a manager-owned progress logger (the `# Finished:` marker appears only after the owner closes it). The marker is a structural sentinel in a durable log, not operator prose. |
| E-101 | `cli/tests/vms/test_bootstrap_file_lifecycle.py::test_helper_redacts_every_non_redacting_progress_surface` | redaction defense | keep | Invariant: every progress surface (steps, outputs, warnings, errors) is redacted; `_AUTH_KEY` is the injected secret and `[REDACTED]` is the substitution being proven. |
| E-179 | `cli/tests/test_wsl2_paths.py::test_ps_quote_escapes_embedded_single_quote` | quoting-function equality | keep | Invariant: `_ps_quote` doubles embedded single quotes. This is an injection defense at a shell boundary, not prose. |
| F-052 | `website/tests/test_site_build.py::BuildAndInstallTests.test_builder_has_no_partial_output_api_or_cli_option` | source-text scan for retired spellings | delete | Reads `build.py` as text and asserts three retired identifiers are absent. Pins the spelling of our own source; the absence is visible in the diff that removed them. |
| F-053 | `website/tests/test_site_build.py::BuildAndInstallTests.test_builder_has_no_partial_output_api_or_cli_option` | retired-CLI-option guard | delete | Runs the builder with `--only 404` and asserts argparse rejects it. Argparse rejects every unknown flag by construction, so this can only fail if someone adds the option back deliberately. The stderr assertion is stdlib prose (the outside-input exception) but guards nothing of ours. |
| F-064 | `website/tests/test_site_build.py::BuildAndInstallTests.test_model_ships_byte_for_byte_without_generated_proof_composition` | source-text scan for retired spellings | delete | `assertNotIn("lander-route-proofs")` and `assertNotIn("REFERENCE_PROOF")` over `lander-model.js` source. Retired-spelling pin; duplicated at lander-phase4u.test.mjs:319-325 (F-138). |
| F-066 | `website/tests/test_site_build.py::BuildAndInstallTests.test_retired_route_projection_sources_are_absent` | retired-path absence | delete | Asserts three deleted files do not exist. Nothing recreates them; the deletion is in the history. |
| F-079 | `website/tests/test_site_content.py::SourceContractTests.test_contents_navigation_derives_nested_h2_h3_links_and_escapes_labels` | constant pin + source-text scan | delete | Asserts `MANIFESTO_CONTRACT.source == Path("docs/manifesto.md")` (a restatement of the production constant) and scans `site_content.py` source for a retired path string. Both are spelling pins over our own code. |
| F-097 | `website/tests/test_lander_404.py::StaticDocumentTests.test_input_clear_restores_zero_command_and_renders,StaticDocumentTests.test_native_actions_share_keyboard_controller_operations_and_focus_lifecycle` | source-text substring pins | delete | About thirty assertions slicing `lander-game.js` source and asserting exact statements appear in it (`this.lander_start.focus({ preventScroll: true })`, `commanded: { ...ZERO_INPUT }`, and so on). This is the spelling of our own source in the plainest form; the behavior is exercised for real by the browser suites (focus order, exit/restart, input clearing) and by `lander-model.test.mjs`. |
| F-111 | `website/tests/test_lander_phase4j.py::ArcadeMarkupTests.test_banner_projection_uses_model_state_flags_without_status_copy` | JS source substring pin | delete | Slices the banner assignment out of `lander-game.js` source and asserts which identifiers appear in it. Pins the shape of one expression in our own code. |
| F-122 | `website/tests/test_lander_phase4j_browser.py::ArcadeBrowserTests.test_active_description_resolves_only_visible_conditional_relationships,ArcadeBrowserTests.test_narrow_and_full_width_chrome_stays_inside_the_stage_above_the_rail` | production-source mutation + verbatim pin | delete | Pins four exact `this.listen(...)` statements from `lander-game.js`, then rewrites the shipped file to delete each one and asserts the browser contract fails. Mutating our own source to prove a listener is load-bearing pins the statement's spelling; the browser contract already fails if input stops working, which is the assertion that matters. |
| F-133 | `website/tests/lander-phase4k.test.mjs::L70-84` | **[line-anchored: not Python]** retired-spelling blacklist over five source files | delete | Asserts `lander-launch` and `launch-button` appear in none of the template, controller, stylesheet, Python validator, or fake DOM. A two-name blacklist proving a retired control did not come back. |
| F-136 | `website/tests/lander-phase4m.test.mjs::L42-45` | **[line-anchored: not Python]** retired-spelling source pin | delete | `!source.includes("legDepartureFuel")` over two production modules. Retired identifier blacklist. |
| F-140 | `website/tests/lander-phase4s.test.mjs::L37-39` | **[line-anchored: not Python]** source-text scan of `build.py` from a JS test | delete | `assert.match(build, /Path\("static\/lander-collision\.js"\)/)` and a retired-symbol `doesNotMatch`. Pins Python source spelling from the Node suite; the manifest fact is held structurally at test_site_build.py:252-263 (F-063, F-065). |
| F-141 | `website/tests/lander-phase4u.test.mjs::L319-325` | **[line-anchored: not Python]** retired-symbol blacklist over three sources | delete | Seven retired identifiers asserted absent from two modules and `build.py`. Duplicates F-064 and F-066; same reasoning. |
| L-101a | `cli/tests/resources/test_graph_guard.py` | AST guards pinning call-graph shape, plus their positive honest-path pins | delete | The three call-shape patterns: the `dependencies()` re-walk (`find_dependencies_calls:156`, `test_pattern1:437`), the lazy readiness recompute (`_is_not_ready_call:236`, `find_not_ready_calls:241`, `test_pattern3:465`), and the `references` field or `getattr` (`find_getattr_references:251`, `find_references_fields:269`, `test_pattern4:474`), with their three allow-lists (`_DEPENDENCIES_ALLOWLIST:302`, `_NOT_READY_ALLOWLIST:402`, `_REFERENCES_FIELD_ALLOWLIST:410`), their three `test_every_exemption_is_load_bearing` parameters, and the five positive pins that assert the honest call appears (`:524`, `:535`, `:547`, `:554`, `:560`). Each says which call shapes may appear in which module, which is `hla.md`'s delete side. |
| L-101b | `cli/tests/resources/test_graph_guard.py` | AST reachability guard over the capability registries | keep | **Was part of a whole-file delete row; the lead accepted the reservation 2026-08-16 and corrected `hla.md`'s delete bullet with it.** A registry probed outside the builder and publishers is a rule about what a module may REACH, not how the reaching is spelled, which puts it beside the import-boundary guard on the keep side. It is also the one guard in this inventory with no observational twin available even in principle: a consumer that re-derives the graph correctly agrees with it until the two diverge, so no behavioral test can tell a second derivation from the first. Keeps both detectors (`find_registry_reads:199` with `_registry_aliases:171` and `_is_registry_read:186`, and `find_descriptor_registry_calls:213`, which watches the same probe with the registry's name filed off), `test_pattern2:447`, both allow-lists (`:319`, `:368`), the two function-scoping tests (`:571`, `:608`), the two matching `test_every_exemption_is_load_bearing` parameters, and the shared `_iter_agentworks_modules:417` / `_scan:421` plus the non-vacuity coverage for these two detectors. The module docstring loses its three deleted patterns and keeps this one. |
| L-102 | `cli/tests/guide/test_power_import_boundary.py::L1-227` | **[line-anchored: file gone]** **[dead]** AST import-and-call boundary over the guide package, plus five detector self-tests | keep | `hla.md`'s standing keep example. The invariant is layering the type system cannot express: the guide package stays inert, reaching neither `subprocess`, sockets, nor secrets. An ordinary edit adding an import regresses it and nothing else catches that. Threat model is accidental regression, which an in-tree guard can hold; it is not claimed as a defense against a hostile committer. |
| L-103 | `cli/tests/schema/test_package_boundary.py::_agentworks_imports,test_the_schema_package_imports_only_top_level_leaves,test_the_import_scan_sees_real_imports` | per-module import scan plus a subprocess import check | keep | Layering boundary: the schema package is a leaf, and a reach upward makes importing any capability module a circular import. The two halves cover different mistakes, and the file says so: the subprocess check catches the cycle, the scan catches a function-local import in a branch nothing calls, which the subprocess check cannot reach. |
| L-104 | `cli/tests/test_registry_warning_boundary.py::test_build_registry_use_detection,test_build_registry_use_detection_ignores_unrelated_references,test_only_intentional_pure_callers_use_build_registry` | enumerated-caller scan for `build_registry` plus two detector self-tests | keep | The invariant is that the old-selector warning renders once at the request boundary; a new direct `build_registry` caller on a request path leaks or duplicates it. No observational test can detect the absence of a caller, so the doctrine's twin tiebreaker leaves this standing. |
| L-105 | `cli/tests/test_registry_warning_boundary.py::test_build_registry_with_explicit_manifest_set_does_not_render_warnings` | observational: explicit manifest set renders no warning | keep | The behavioral half of the same boundary, and the fixture deliberately carries a real advisory so an issue-free set cannot pass vacuously. |
| L-106 | `cli/tests/sessions/test_harness_integration_gate_drift.py::test_every_session_factory_caller_gates_the_harness_integration` | per-function AST scan that every session-node factory caller gates the harness integration | keep | Verified: `plugins/test_enablement_producer.py:487` proves the gate FUNCTION refuses a disabled integration, and nothing proves every call site calls it. That is the gap this guard holds, and no observational test can observe a caller that does not exist. Carries its own non-vacuity assertion. |
| L-107 | `cli/tests/sessions/test_harness_integration_gate_drift.py::test_guard_is_not_vacuous` | detector self-test | keep | Non-vacuity for L-106; a scan that silently matched nothing would pass forever. |
| L-108 | `cli/tests/agents/test_recipe_gate_drift.py::test_runner_caller_sets_are_exactly_the_enumerated_entries` | enumerated caller-set pin for the recipe runners | keep | Same reasoning as L-106: `plugins/test_manifest_parity.py:133` proves `ensure_recipe_enabled` refuses, and only this guard proves no path reaches, without its gate, a declarable-consuming runner. The comment block names the real hole it was written for (`session create --new-agent`, whose realize and gate sit in different functions). |
| L-109 | `cli/tests/agents/test_recipe_gate_drift.py::test_every_command_entry_gates_its_recipe` | gate-presence assertion per command entry | keep | Presence at each of the six entries; this is reachability, not spelling. |
| L-110 | `cli/tests/agents/test_recipe_gate_drift.py::test_every_command_entry_gates_its_recipe` | `assert calls[_GATE] < calls[realize]`, a lexical statement-order pin | convert | `hla.md` is explicit: delete the pin that says a statement comes first, keep the test that says what goes wrong when it does not. Replace with the house observational form already used at `cli/tests/vms/test_add_git_credential_orchestrated.py:242` (`test_runup_rejection_is_fatal_and_writes_nothing`): drive the entry with a disabled recipe and assert nothing was persisted. No production change. |
| L-111 | `cli/tests/agents/test_recipe_gate_drift.py::test_guard_is_not_vacuous` | detector self-test | keep | Non-vacuity for L-108 and L-109. |
| L-112 | `cli/tests/plugins/test_plugin_framework.py::test_each_derived_site_reads_the_descriptor_table_in_its_own_body` | `test_each_derived_site_reads_the_descriptor_table_in_its_own_body`, an AST pin that each switchboard body references the table by name | delete | This pins how the code is written, and its own docstring concedes the point: the sibling set-equality test against the real descriptor table catches drift, and this one exists only to forbid a hand-written enumeration that stays correct. A correct enumeration is not a bug; it becomes one when it drifts, and the sibling catches that. |
| L-113 | `cli/tests/test_database_migration_safety.py::test_production_has_one_writable_database_construction_site` | **[subtracted: instance-model]** AST scan enumerating writable `Database(...)` construction sites | keep | Reachability, not spelling: write access to the state database is confined to one construction site, and an ordinary edit adding `Database(path)` elsewhere silently grants write access on a read-only path. No runtime test can observe the absence of such a site. |
| L-114 | `cli/tests/test_tailscale_legacy_removed.py::_read_module_source,test_initializer_has_no_read_env_with_legacy_for_tailscale,test_vm_manager_module_does_not_reference_legacy_tailscale_env_var,test_vm_manager_does_not_read_legacy_env_for_tailscale_in_collect` | three `getsource` identifier-spelling tripwires for the removed `AW_TAILSCALE_AUTH_KEY` fallback | convert | **Recipe verification, 2026-08-16.** **The replacement recorded earlier was verified at the wrong level and would have retired a guard that was doing real work.** Driving the RESOLVER directly does bite a legacy fallback inside `Resolver.resolve`, but it PASSES when the fallback is reintroduced inside `create_vm`, which is where the removed code actually lived. Corrected recipe, verified in both halves: drive `create_vm` end to end with the platform, Tailscale transport and Phase B faked, `AW_SECRET_TAILSCALE_AUTH_KEY` deleted and `AW_TAILSCALE_AUTH_KEY` set, and assert `SecretUnavailableError` plus no VM row. That bites BOTH reintroduction sites and is strictly stronger than all three greps combined. No production change. Separate finding about the file being deleted: `_MODULES_TO_SCAN` at `:18-22` lists `agentworks.secrets.resolver`, but no test in the file ever scans it, so the third entry is dead and the greps miss a resolver-level reintroduction. |
| L-115 | `cli/tests/test_git_credential_scoping.py::test_add_git_credential_never_downgrades_helper` | `getsource` pin on the shell command spelling inside `add_git_credential` | convert | An observational twin already exists at `cli/tests/vms/test_add_git_credential_orchestrated.py:153`, which captures the emitted helper command. Move the one claim the twin lacks (no unconditional `credential.helper store` downgrade) onto the captured command there. Asserting on an emitted git command is behavior, not authored prose. |
| L-116 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_vm_create_does_not_eager_resolve_operator_env` | **[subtracted: secrets-preview]** `getsource` scan for `SecretTarget(` across the vm-create call chain | convert | **Recipe verification, 2026-08-16.** **PARTIAL as recorded, and the correction matters: the register-targets spy is blind to half the family.** L-118 and L-119's guards name `resolve_for_command`, a FREE function at `secrets/orchestration.py:218`, so a spy on `Resolver.register_targets` cannot see it: injecting a live `resolve_for_command` walk into `create_agent` left the spy green. L-116 and L-117 verified only because their guards name the `SecretTarget` mechanism the spy does watch. Corrected recipe covering all four, verified against BOTH mechanisms on every row: declare an operator-env secret that nothing can resolve and assert provisioning still completes. No production change. |
| L-117 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_vm_reinit_does_not_eager_resolve_operator_env` | **[subtracted: secrets-preview]** `getsource` mirror for `reinit_vm` | convert | **Recipe verification, 2026-08-16.** Same corrected recipe as L-116 (declare an unresolvable operator-env secret, assert provisioning still completes), verified on the `reinit_vm` path against both reintroduction mechanisms. |
| L-118 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_agent_create_does_not_eager_resolve_operator_env` | **[subtracted: secrets-preview]** `getsource` scan for `resolve_for_command` in `create_agent` | convert | **Recipe verification, 2026-08-16.** **The recorded replacement FAILED to bite.** This guard names `resolve_for_command`, a free function at `secrets/orchestration.py:218`, which a `Resolver.register_targets` spy cannot observe; a live `resolve_for_command` walk injected into `create_agent` left it passing green. Use L-116's corrected recipe instead: declare an unresolvable operator-env secret and assert provisioning still completes. Verified to bite both mechanisms. |
| L-119 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_agent_reinit_does_not_eager_resolve_operator_env` | **[subtracted: secrets-preview]** `getsource` mirror for `reinit_agent` | convert | **Recipe verification, 2026-08-16.** **Same failure as L-118** and the same correction: the register-targets spy is blind to `resolve_for_command`. Use L-116's corrected recipe on the agent reinit path. |
| L-120 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_agent_setup_runners_have_no_env_injection` | **[subtracted: secrets-preview]** `getsource` scan for `env=agent_env` / `agent_env = compose_env` in `create_agent_on_vm` | convert | **Recipe verification, 2026-08-16.** Verified, and it bites when an operator-env `env=` kwarg is added to the claude-plugins runner. **Record the vacuity trap with the row**: every runner in `create_agent_on_vm` is configuration-gated, so under a default template none of them fire and the assertion covers nothing. The first break attempt PASSED for exactly that reason. The replacement is honest only if the fixture declares an admin and agent template carrying `user_install_commands`, `dotfiles_source` plus `dotfiles_install_cmd`, `mise_packages`, `claude_marketplaces` and `claude_plugins`, and the dotfiles runner additionally needs a real local source directory and a `copy_dir_to` on the fake transport. A reviewer should check the fixture turns every gated runner ON, because the grep it replaces covered the whole function unconditionally. |
| L-121 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_vm_provisioning_runners_have_no_env_injection` | **[subtracted: secrets-preview]** `getsource` scan for `env=admin_env` in `_phase_b_setup` | convert | **Recipe verification, 2026-08-16.** Same recipe and the same vacuity trap as L-120, on `_phase_b_setup`. Verified: adding an operator-env `env=` to `_admin_run_cmd` makes it fail. |
| L-122 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_phase_b_setup_ends_with_ensure_files_sourced` | **[subtracted: secrets-preview]** `getsource` presence pin for `_ensure_agentworks_files_sourced` in `_phase_b_setup` | convert | **Recipe verification, 2026-08-16.** Verified by spying the function and asserting exactly one call carrying the expected `home`; replacing the call site with `pass` makes it fail. Note the driver path uses a module-level import so patch `driver._ensure_agentworks_files_sourced`. Carry the row's naming note forward: this observes PRESENCE, not "ends with", so either assert it is the last recorded transport interaction or rename the test (principle 2). |
| L-123 | `cli/tests/test_secrets_eager_resolve_vm_agent.py::test_create_agent_on_vm_ends_with_ensure_files_sourced` | **[subtracted: secrets-preview]** `getsource` presence pin, agent mirror | convert | **Recipe verification, 2026-08-16.** Same recipe, agent path, and **the verification showed the replacement is strictly stronger than the grep it replaces**: with the call site replaced by `pass`, the original `test_create_agent_on_vm_ends_with_ensure_files_sourced` still PASSED, because the surviving `from ... import _ensure_agentworks_files_sourced` line inside the function body satisfies the substring scan. The grep could not tell an import from a call. Patch `vm_initializer._ensure_agentworks_files_sourced`, since this path imports the name inside the function body. |
| L-124 | `cli/tests/vms/test_phase_a_platform_bootstrap.py::test_fallback_era_contract_shapes_are_structurally_absent` | structural half: dataclass field presence/absence and the `_phase_a_bootstrap` parameter set | keep | Real structural invariant over live types: the fallback-era `bootstrap_complete` result field and the `BootstrapCompletion` attribute are gone, and Phase A's signature is exactly the seven parameters. Asserted against real objects, not source text. |
| L-125 | `cli/tests/vms/test_phase_a_platform_bootstrap.py::test_fallback_era_contract_shapes_are_structurally_absent` | `request_fields["tailscale_auth_key"].type == "str"` | delete | An annotation-equality assertion, the same shape PR #546 deleted from `secret_backend/conformance.py`. Under strict typing mypy carries this; the assertion compares an annotation STRING and breaks on a purely cosmetic re-spelling. |
| L-126 | `cli/tests/vms/test_phase_a_platform_bootstrap.py::test_fallback_era_contract_shapes_are_structurally_absent` | `"run_wsl2_bootstrap" not in driver_source`, `"generate_bootstrap_script" not in driver_source` | keep | Rubric re-check, 2026-08-16. **The row's premise was false at HEAD.** `run_wsl2_bootstrap` is genuinely gone and that half still deletes, but `generate_bootstrap_script` is a LIVE symbol, exported by `capabilities/vm_platform/bootstrap_script.py` and called by the proxmox, azure and aws platforms, so a reintroduced call in `vms/initializer/driver` would import cleanly and pass silently rather than failing to import as the row claimed. That half is the only probe and keeps. |
| L-127 | `cli/tests/test_config_resource_read_guard.py::test_no_config_resource_reads_outside_publishers` | regex source scan for `config.<resource-attr>` reads with an EMPTY allowlist | delete | **Verified by execution and it sharpens the row.** `Config` is a dataclass whose fields are exactly `config_issues, database, defaults, deprecation_issues, enabled_system_plugins, operator, paths, secret_config_data, session, source_path`: none of the fifteen forbidden attributes exists. A reintroduced read is a mypy error and an `AttributeError`, so the guard protects a surface the type system already closed, while its regex can still false-positive on any local named `config` or `cfg` that legitimately has a `.vm` or `.admin` attribute. |
| L-128 | `cli/tests/resources/test_phase0_vocabulary.py::test_kind_registry_keys_are_lower_kebab` | `test_kind_registry_keys_are_lower_kebab` | keep | Structural check over the live registry, and the registry keys are the shipped manifest `kind:` vocabulary. A newly registered kind spelled `vm_template` is an ordinary edit that regresses an operator-facing contract. |
| L-129 | `cli/tests/resources/test_phase0_vocabulary.py::test_kind_handlers_match_their_registry_key` | `test_kind_handlers_match_their_registry_key` | keep | Structural: every handler answers to the key it is filed under. A mismatch mis-dispatches at runtime and no other test asserts the pairing. |
| L-130 | `cli/tests/resources/test_phase0_vocabulary.py::test_code_declared_vocabulary_is_gone` | source-wide grep for `code-declared` / `code_declared` | delete | Retired-spelling grep over source text, the doctrine's identifier-spelling delete. The `Origin` variant set is a real enum; a reintroduced variant would have to be added to it, and `test_origin.py` covers the live variants. |
| L-131 | `cli/tests/resources/test_phase3_naming_consistency.py::test_no_framework_module_has_old_vocabulary` | `_BANNED_SUBSTRINGS` scan over public symbol names across thirteen modules | delete | A wording blacklist over identifiers, which is both the doctrine's delete side and the shape `no-prose-policing-tests` calls worse than useless: it rejects `Requirement` and `UsageEntry` and waves through any other spelling of the same mistake. Eleven of the thirteen modules carry no such symbol at all. |
| L-132 | `cli/tests/resources/test_phase3_naming_consistency.py::test_resources_package_has_no_old_vocabulary_in_source` | `test_resources_package_has_no_old_vocabulary_in_source` | delete | Its own docstring says the target is prose in comments and docstrings, so this is a forbidden-wording blacklist over authored text, exactly what the rule names. The banned list includes the bare word `requirements`, which any legitimate future use trips. |
| L-133 | `cli/tests/resources/test_phase3_naming_consistency.py::test_resource_reference_carries_usage_field_not_text` | `test_resource_reference_carries_usage_field_not_text` | delete | Restates the dataclass definition: it can only fail when someone edits the fields it lists, and every consumer of `ResourceReference.usage` fails first. The `text` half is a retired-spelling pin. |
| L-134 | `cli/tests/resources/test_phase3_naming_consistency.py::test_graph_node_producers_expose_dependencies_not_referenced_resources` | `test_graph_node_producers_expose_dependencies_not_referenced_resources` | delete | `hasattr` presence pins on methods the graph builder calls on every build; the builder's own tests fail loudly if `dependencies` disappears. The `not hasattr` halves are retired-spelling pins. |
| L-135 | `cli/tests/resources/test_phase3_naming_consistency.py::test_resource_kinds_carry_no_inbound_reference_field` | `test_resource_kinds_carry_no_inbound_reference_field` | keep | This one is not a spelling pin: it asserts across every resource kind that inbound references live on the graph rather than on the dataclass. A new kind declaring a `references` field would silently reintroduce a second source of truth for `dependents_of`, and nothing else checks the whole kind set. |
| L-136 | `cli/tests/assistance/test_contract.py::test_generator_has_no_network_or_process_boundary` | import boundary on the package generator script | keep | The generator must stay inert: no `subprocess`, sockets, or network. Layering boundary the type system cannot express. |
| L-137 | `cli/tests/assistance/test_contract.py::test_generated_packages_are_inert_and_share_the_canonical_handoff` | `body == BODY` plus generated file-set equality | keep | Derivation parity, which the rule endorses by name: the two shipped plugin skills are proven byte-identical to the canonical `packaging/agentworks/assistance.md`, and the file-set equality proves nothing executable was added beside them. |
| L-138 | `cli/tests/assistance/test_contract.py::test_catalogs_and_manifests_have_neutral_identity_and_complete_codex_policy` | `METADATA`-derived assertions on the catalogs and manifests | keep | Parity against the canonical `metadata.json`: display name, publisher, description and the interface block are proven to be projections rather than copies. |
| L-139 | `cli/tests/assistance/test_contract.py::test_catalogs_and_manifests_have_neutral_identity_and_complete_codex_policy` | `codex_catalog == {...}`, a verbatim dict literal | delete | **Corrected 2026-08-19 (map re-baseline).** Re-derived at HEAD and narrowed off L-138's range. `:74-85` is the `codex_catalog == {...}` literal: a second copy of an authored config file maintained inside the test, which R1.2 generalized the rule to cover. The `METADATA`-derived parity L-138 keeps is at `:70`, `:87-89` and `:91`, all outside this row. |
| L-140 | `cli/tests/assistance/test_contract.py::test_readme_projection_is_exact` | `test_readme_projection_is_exact` | keep | Derivation parity again: the README's generated block is proven to be `BODY` plus its fence. This is the canonical example the rule points at. |
| L-141 | `cli/tests/test_shell_integration.py::test_capability_imports_neither_sessions_nor_orchestration` | subprocess probe asserting `agentworks.sessions` / `agentworks.orchestration` are not imported | keep | An OBSERVATIONAL import boundary: it launches a real interpreter and reads `sys.modules` rather than reading source. This is the twin form `hla.md` prefers, not a source guard. |

## Deferred: held for the R4 reassessment

**These rows are NOT part of the sweep's executable set.** They are listed here, out of their
groups, so that an executor scanning for delete rows cannot pick them up by accident.

**Operator ruling, 2026-08-16.** `website/tests/test_site_templates.py` is held until the
reassessment decides the fate of `site_validation.py`'s element-by-element half, which the FRD lists
as a deferred subsystem redesign. The reason is an asymmetry rather than a doubt about the rows: if
R4 deletes that validator, these tests die with it at no cost, and if R4 keeps it, deleting them now
permanently removes its negative-path coverage. Deferring is safe in both directions and nothing
else is.

R4 also inherits an open question with them, deliberately not chased here: whether these 22 police
the authored form of the templates rather than the validator's rejection behavior. The answer does
not change the deferral, which is why it was left for whoever takes W2.

Two rows beyond the operator's 22 are held with them, and this is the lead's extension rather than
the ruling: **F-030 and F-039 are converts**, and rewriting two tests in a file that R4 may delete
wholesale is work R4 may discard. **L-402 is group 1's**, covering the 12 `assertRaisesRegex` sites
in this same file; most of those regexes sit inside tests that are now deferred, so the row cannot
be executed independently of them. Reverse either if you would rather they rode with their groups.

<!-- prettier-ignore -->
| id | file and anchors | shape | disposition | justification |
| --- | --- | --- | --- | --- |
| F-025 | `website/tests/test_site_templates.py::TemplateContractTests.test_each_template_has_only_its_closed_vocabulary` | **[deferred]** token-set pin (second copy) | delete | Asserts each template's token set equals `site_validation.TEMPLATE_TOKENS`, a production constant. Verified by execution: a token with no substitution fails the build outright (`index.html: missing substitutions for required tokens: ['{{HOME_IDENTITY}}']`), so the agreement is enforced by building, not by this restatement. |
| F-026 | `website/tests/test_site_templates.py::TemplateContractTests.test_unknown_missing_duplicate_wrong_template_and_brace_tokens_fail` | **[deferred]** mutate-one-string-assert-raises | delete | Six template mutations proving `_validate_template` rejects unknown, missing, duplicate, foreign, and brace-like tokens. The templates live in this tree and are validated on every build; this only proves the guard fires, and the guard's own half of `site_validation.py` is the deferred W2 deletion candidate. |
| F-028 | `website/tests/test_site_templates.py::TemplateContractTests.test_extracted_content_cannot_expand_a_template_token,TemplateContractTests.test_extracted_content_cannot_expand_a_template_token::ValueError::d71096,TemplateContractTests.test_content_tokens_cannot_move_to_hostile_or_unreviewed_contexts` | **[deferred]** mutate-one-string-assert-raises | delete | Ten mutations moving content tokens into `<script>`, `<style>`, and containers nobody reviewed. Adversarial tampering with an in-tree template: anything that can make this edit can delete this test. |
| F-029 | `website/tests/test_site_templates.py::TemplateContractTests.test_content_tokens_cannot_move_to_hostile_or_unreviewed_contexts,TemplateContractTests.test_onboarding_paths_copy_icon_and_shared_destinations_are_guarded` | **[deferred]** mutate-one-string-assert-raises | delete | Ten mutations renaming onboarding ids, classes, and the module src. Pins the template's spelling through the validator; the onboarding structure that matters is asserted on the built page (F-007, F-008). |
| F-030 | `website/tests/test_site_templates.py::TemplateContractTests.test_onboarding_paths_copy_icon_and_shared_destinations_are_guarded` | **[deferred]** mutate-one-string-assert-raises | convert | The property is real: a service link's visible label must match the destination it points at, and swapping them is a plausible copy-paste error with a user-visible consequence. Replacement: on the built page, assert the anchor whose text is "GitHub" carries `REPOSITORY_URL` and the one whose text is "PyPI" carries `PYPI_URL`. Structural, on output, no production change. |
| F-031 | `website/tests/test_site_templates.py::TemplateContractTests.test_onboarding_paths_copy_icon_and_shared_destinations_are_guarded,TemplateContractTests.test_shell_destination_labels_are_bound_to_their_reviewed_hrefs,TemplateContractTests.test_shell_destination_labels_are_bound_to_their_reviewed_hrefs::ValueError::b5c755,TemplateContractTests.test_shell_css_classes_landmark_order_and_breadcrumb_order_fail_closed` | **[deferred]** mutate-one-string-assert-raises | delete | Ten-plus mutations per shell template renaming CSS classes and reordering landmarks. Pinning the spelling of our own class names is the exact target of the amended `no-prose-policing-tests` rule. |
| F-032 | `website/tests/test_site_templates.py::TemplateContractTests.test_shell_css_classes_landmark_order_and_breadcrumb_order_fail_closed,TemplateContractTests.test_detail_page_headings_reject_provenance_eyebrows` | **[deferred]** mutate-one-string-assert-raises | delete | Proves the validator rejects an eyebrow paragraph above a detail-page `h1`. A house style rule enforced by a test; review owns it. |
| F-033 | `website/tests/test_site_templates.py::TemplateContractTests.test_detail_page_headings_reject_provenance_eyebrows,TemplateContractTests.test_detail_page_headings_reject_provenance_eyebrows::ValueError::5b0daa,TemplateContractTests.test_long_form_templates_allow_only_the_complete_source_document` | **[deferred]** mutate-one-string-assert-raises | delete | Verified by execution: with `_validate_template` stubbed, injecting `<h1>Template title</h1>` before the content token is still caught by `test_long_form_pages_are_complete_semantic_source_projections` (F-012), which compares the rendered blocks against the source. The observational twin exists. |
| F-034 | `website/tests/test_site_templates.py::TemplateContractTests.test_long_form_templates_allow_only_the_complete_source_document,TemplateContractTests.test_long_form_templates_allow_only_the_complete_source_document::ValueError::70cc4b,TemplateContractTests.test_service_ctas_reject_hidden_ancestors_and_icon_bypasses` | **[deferred]** mutate-one-string-assert-raises | delete | Twelve mutations per template hiding service CTAs via `hidden`, inline style, injected `<style>`, and extra icons. Defends an in-tree template against deliberate concealment; that guarantee cannot be held by a test living beside the template. |
| F-035 | `website/tests/test_site_templates.py::TemplateContractTests.test_service_ctas_reject_hidden_ancestors_and_icon_bypasses,TemplateContractTests.test_reviewed_shell_text_cannot_come_from_hidden_or_structural_descendants` | **[deferred]** mutate-one-string-assert-raises | delete | Six mutations sourcing reviewed shell text from hidden or `<template>` descendants. Same adversarial shape as F-034. |
| F-036 | `website/tests/test_site_templates.py::TemplateContractTests.test_reviewed_shell_text_cannot_come_from_hidden_or_structural_descendants,TemplateContractTests.test_service_icon_paths_are_exact_single_direct_children` | **[deferred]** verbatim pin (SVG path data) | delete | Pins the exact `d` attribute of each service icon and six mutations around it. Authored artwork; a wrong icon is a review finding, not a test finding. |
| F-037 | `website/tests/test_site_templates.py::TemplateContractTests.test_service_icon_paths_are_exact_single_direct_children,TemplateContractTests.test_service_icon_paths_are_exact_single_direct_children::ValueError::cda99a,TemplateContractTests.test_rocket_inventory_rejects_unclassified_extra_and_misplaced_images` | **[deferred]** mutate-one-string-assert-raises | delete | Rocket-image inventory mutations (extra, unclassified, misplaced). The built-page assertions at test_site_documents.py:410-413 and :451-454 already hold the placement and `alt` facts that ship. |
| F-038 | `website/tests/test_site_templates.py::TemplateContractTests.test_rocket_inventory_rejects_unclassified_extra_and_misplaced_images` | **[deferred]** mutate-one-string-assert-raises | delete | One mutation moving the scene SVG out of its section. In-tree template tampering. |
| F-039 | `website/tests/test_site_templates.py::TemplateContractTests.test_rocket_inventory_rejects_unclassified_extra_and_misplaced_images,TemplateContractTests.test_fragment_scene_svg_cannot_move_outside_its_reviewed_section` | **[deferred]** mutate-one-string-assert-raises | convert | The property is a real a11y invariant (three accessible names must be non-blank), and blanking a label is a plausible edit. Replacement: assert on the built Lander page that `#lander-game`, `#lander-start`, and the scene `<title>` have non-blank text; test_lander_404.py:259-260 already asserts two of the three on the template, so this is one added assertion moved to output. No production change. |
| F-040 | `website/tests/test_site_templates.py::TemplateContractTests.test_fragment_scene_svg_cannot_move_outside_its_reviewed_section,TemplateContractTests.test_fragment_accessible_name_sources_must_be_structural_and_nonempty,TemplateContractTests.test_fragment_support_and_battery_geometry_fail_closed` | **[deferred]** verbatim pin (SVG coordinates) | delete | Pins literal path coordinates (`M312 457.21999999999997H498`) and battery-bar geometry through the validator. Authored artwork geometry; and the real agreement (the static SVG matches what the model renders) is held by derivation parity in the `.mjs` suite (F-131, F-137). |
| F-041 | `website/tests/test_site_templates.py::TemplateContractTests.test_fragment_support_and_battery_geometry_fail_closed,TemplateContractTests.test_fragment_variants_cannot_duplicate_local_route_destinations` | **[deferred]** mutate-one-string-assert-raises | delete | Verified by execution: with `_validate_template` stubbed, a non-relative `/manifesto/#...` link fails at `_validate_local_references` ("local reference is outside site base") and an aliased duplicate fails there too. The link-integrity half of the validator, which W2 says stays, is the guard. |
| F-042 | `website/tests/test_site_templates.py::TemplateContractTests.test_fragment_variants_cannot_duplicate_local_route_destinations` | **[deferred]** mutate-one-string-assert-raises | delete | Verified by execution: with the validator stubbed, removing `{{LANDER_GAME}}` fails `test_lander_and_404_share_one_byte_identical_game_subtree` (F-016). |
| F-043 | `website/tests/test_site_templates.py::TemplateContractTests.test_shared_game_fragment_has_one_exact_shell_placement,TemplateContractTests.test_footer_game_link_is_final_icon_only_and_accessibly_named` | **[deferred]** mutate-one-string-assert-raises | delete | Five mutations around the footer game link. The built-page assertions at test_site_documents.py:458-466 hold the same accessible-name and destination facts, and lander-phase4m.test.mjs:247-255 holds them a third time. |
| F-044 | `website/tests/test_site_templates.py::TemplateContractTests.test_footer_game_link_is_final_icon_only_and_accessibly_named,TemplateContractTests.test_footer_game_link_is_final_icon_only_and_accessibly_named::ValueError::55bf99,TemplateContractTests.test_shell_title_and_canonical_metadata_fail_closed` | **[deferred]** mutate-one-string-assert-raises | delete | Title and canonical-link mutations. The canonical URL per page is asserted on the built output at test_site_documents.py:363-364; the empty-title case is a review concern. |
| F-045 | `website/tests/test_site_templates.py::TemplateContractTests.test_shell_title_and_canonical_metadata_fail_closed,TemplateContractTests.test_shell_title_and_canonical_metadata_fail_closed::ValueError::44f5d9,TemplateContractTests.test_shell_favicon_is_exact_and_shared` | **[deferred]** verbatim pin + mutations | delete | Pins the exact favicon `<link>` markup and five mutations of it. Authored markup spelling. |
| F-046 | `website/tests/test_site_templates.py::TemplateContractTests.test_shell_favicon_is_exact_and_shared,TemplateContractTests.test_shell_favicon_is_exact_and_shared::ValueError::053233,TemplateContractTests.test_game_shell_description_structure_and_csp_are_shared` | **[deferred]** mutate-one-string-assert-raises + verbatim pin | delete | CSP and description mutations on two templates, plus `policies == [CSP, CSP]`, a third copy of the policy string. The built-page CSP check (F-002) covers all five pages, and the description-presence fact is held at test_site_documents.py:365. |
| F-047 | `website/tests/test_site_templates.py::TemplateContractTests.test_game_shell_description_structure_and_csp_are_shared,TemplateContractTests.test_game_shell_description_structure_and_csp_are_shared::ValueError::f837f1,TemplateContractTests.test_duplicate_attributes_cannot_bypass_template_contracts` | **[deferred]** mutate-one-string-assert-raises | delete | Proves the validator's own parser is not fooled by duplicate HTML attributes. Guarding the guard against a bypass nobody is attempting; the same shape appears at test_site_build.py:201-217 (F-060). |
| F-048 | `website/tests/test_site_templates.py::TemplateContractTests.test_duplicate_attributes_cannot_bypass_template_contracts,TemplateContractTests.test_duplicate_attributes_cannot_bypass_template_contracts::ValueError::20a7d7` | **[deferred]** phrase canary | delete | Injects an email address into the security template and expects rejection. The production `github_only_reporting` contract still runs on every build and is exercised on its real input at test_site_content.py:332-350 (F-092); this template-side copy adds a second canary for the same rule. |
| F-049 | `website/tests/test_site_templates.py::TemplateContractTests.test_security_template_rejects_email_address_reporting_canary,TemplateContractTests.test_security_template_rejects_email_address_reporting_canary::ValueError::39f0fc,TemplateContractTests.test_long_form_tokens_cannot_move_out_of_reviewed_article_or_metadata` | **[deferred]** mutate-one-string-assert-raises | delete | Long-form token context mutations, the manifesto/security twin of F-028. |
| L-402 | `website/tests/test_site_templates.py::TemplateContractTests.test_extracted_content_cannot_expand_a_template_token,TemplateContractTests.test_extracted_content_cannot_expand_a_template_token::ValueError::d71096,TemplateContractTests.test_shell_destination_labels_are_bound_to_their_reviewed_hrefs,TemplateContractTests.test_shell_destination_labels_are_bound_to_their_reviewed_hrefs::ValueError::b5c755,TemplateContractTests.test_detail_page_headings_reject_provenance_eyebrows,TemplateContractTests.test_detail_page_headings_reject_provenance_eyebrows::ValueError::5b0daa,TemplateContractTests.test_long_form_templates_allow_only_the_complete_source_document,TemplateContractTests.test_long_form_templates_allow_only_the_complete_source_document::ValueError::70cc4b,TemplateContractTests.test_service_icon_paths_are_exact_single_direct_children,TemplateContractTests.test_service_icon_paths_are_exact_single_direct_children::ValueError::cda99a,TemplateContractTests.test_footer_game_link_is_final_icon_only_and_accessibly_named,TemplateContractTests.test_footer_game_link_is_final_icon_only_and_accessibly_named::ValueError::55bf99,TemplateContractTests.test_shell_title_and_canonical_metadata_fail_closed,TemplateContractTests.test_shell_title_and_canonical_metadata_fail_closed::ValueError::44f5d9,TemplateContractTests.test_shell_favicon_is_exact_and_shared,TemplateContractTests.test_shell_favicon_is_exact_and_shared::ValueError::053233,TemplateContractTests.test_game_shell_description_structure_and_csp_are_shared,TemplateContractTests.test_game_shell_description_structure_and_csp_are_shared::ValueError::f837f1,TemplateContractTests.test_duplicate_attributes_cannot_bypass_template_contracts,TemplateContractTests.test_duplicate_attributes_cannot_bypass_template_contracts::ValueError::20a7d7,TemplateContractTests.test_security_template_rejects_email_address_reporting_canary,TemplateContractTests.test_security_template_rejects_email_address_reporting_canary::ValueError::39f0fc,TemplateContractTests.test_long_form_tokens_cannot_move_out_of_reviewed_article_or_metadata,TemplateContractTests.test_long_form_tokens_cannot_move_out_of_reviewed_article_or_metadata::ValueError::8e634c` | **[deferred]** 12 `assertRaisesRegex` sites | delete | Same, over the template validator's authored refusals (`"brace-like token syntax"`, `"reviewed literals"`). Note the interaction: 22 of this file's 25 tests are group 5 delete rows, so most of these regexes disappear with their tests rather than needing a separate edit. **Corrected 2026-08-19 (map re-baseline).** Re-derived at HEAD as an explicit site list: the file changed after this map's basis and one of its 14 sites was already narrowed to a bare `assertRaises` by unrelated work, leaving 12. The old `:39-624` range would also have swept whatever the file grew in between. |

## Stack topology and recipe verification

Recorded for the executors of the sweep's PRs. Like the rest of this file it dies when the sweep
closes.

**The stack topology was re-cut out of this section on 2026-08-19 rather than re-derived.** It was
measured over nine groups with no re-scope subtraction, and both premises are gone, so its numbers
described a sweep that no longer exists. What survives below is the measurement method and the
findings that outlive the counts.

**Operator disposition, 2026-08-19: the re-cut happens at the restart, not in the PR that re-derived
this map.** The restart owns choosing the order, and it inherits the method here plus the group-1
split the group 1 header records.

### What the overlap measurement found

The matrix and the stack diagram that used to sit here were measured over nine groups with no
re-scope subtraction, and both are gone rather than left to mislead: group 2 has no live row and 117
rows moved to two other efforts, so every cell in them was wrong. The measurement's METHOD survives
and is what a re-cut should repeat: count only the files a group actually EDITS, its delete and
convert rows, since keeps need no edit, and split group 3 into its four sub-batches.

Four facts it established, three of which do not depend on the counts that went stale:

1. **Group 1 is the hub.** It touched more files than any other group and overlapped every group
   except group 2. It goes at the bottom regardless of judgment weight.
2. **The four group 3 sub-batches have ZERO overlap with each other.** They are genuinely
   independent and never need to stack.
3. **Group 6 is not independent**, which was the standing guess: it shared files with both group 5
   and group 1. Group 3c was the closest thing to independent, overlapping group 1 alone.
4. **Group 4 carries the rework risk.** Group 3 holds more converts in total, 88 against group 4's
   81, but group 3 lands as four PRs and its largest sub-batch carries 32, so **group 4 is the
   largest single PR by converts** and the likeliest to need rework. That is why the old stack put
   it at the top with only leaves above it. Whatever order the re-cut chooses, nothing should be
   built on group 4 that a rework of its converts would drag with it.

### Recipe verification

Every distinct convert recipe was executed against a sample, in both halves: it must PASS against
unmodified production AND FAIL when the invariant it claims to guard is broken. A conversion that
passes but never bites is the failure this effort has hit repeatedly, so the second half is the one
that counts.

**55 distinct recipes. The nine listed below did not hold as recorded; the other 46 verified.** Zero
required a production change, so R2.2 is clean throughout.

**Corrected 2026-08-19 (map re-baseline).** This paragraph read "44 verified as recorded, 11 did not
hold" over a list of nine, so the three figures could not all be right. The list is the evidence and
the counts now follow it. Re-running the verification to decide whether 55 or 9 was the wrong number
was outside the re-baseline's reach, so if two further failures existed they are unrecorded and
their rows carry the verified recipe.

A second sentence here used to add that nine convert rows became deletes and fourteen kept their
conversion. It is gone rather than footnoted: it did not reconcile with the nine failed recipes,
which name sixteen rows between them, and no record this file carries says which population it
counted. The row dispositions are authoritative.

The nine that did not hold:

| Recipe                                                   | Rows                | What execution showed                                                                                                                                                                                        | Resolution                                                                                                                         |
| -------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| Hermetic provisioning via a `register_targets` spy       | L-116 to L-119      | **The spy is blind to half the family.** L-118 and L-119's guards name `resolve_for_command`, a free function at `secrets/orchestration.py:218`; a live walk injected into `create_agent` left the spy green | Corrected for all four: declare an unresolvable operator-env secret and assert provisioning still completes. Bites both mechanisms |
| Tailscale legacy var, observed at the resolver           | L-114               | Bites a resolver-level reintroduction but MISSES one inside `create_vm`, which is where the removed code lived                                                                                               | Corrected to drive `create_vm` end to end; bites both sites                                                                        |
| `warnings != []` on the reconcile SVE path               | C-153               | Both branches emit exactly one warning, so it passes when the behavior regresses                                                                                                                             | Withdrawn; the row is a `delete` and `:159` already discriminates                                                                  |
| `FieldDoc` record read                                   | D-067               | Reads the declaration straight off the model, so no break makes it fail                                                                                                                                      | Withdrawn; the row is a `delete`                                                                                                   |
| `union_scalar_shorthand_error` for family discrimination | D-137               | Returns `None` for the fixture, so the replacement never bites                                                                                                                                               | Corrected to `reference_marker_error`                                                                                              |
| Retire the duplicate manifest constant                   | F-050, F-088, F-145 | Production enforces the assertion unconditionally, so it can only fail via a `RuntimeError` in `build.py`                                                                                                    | Withdrawn; all three are `delete`                                                                                                  |
| Assert the a11y attributes                               | F-007, F-093        | Same: enforced unconditionally at build time. Both justifications were factually wrong                                                                                                                       | Withdrawn; both are `delete`                                                                                                       |
| Derive the SVG fragment contract                         | F-101               | Same                                                                                                                                                                                                         | Withdrawn; `delete`                                                                                                                |
| Consolidate the runtime-surface scan                     | F-018, F-098        | The consolidation drops `.html` coverage, and deleting the canaries removes the only non-vacuity guard (a typo'd pattern made the scan pass green)                                                           | Convert only if the merged scan keeps `.html` and one canary; otherwise both are deletes                                           |

**The four trap-carrying recipes owe 22 sites a screening, and that debt is still open.** It was
recorded in the #573 round as a gating condition on the PRs carrying those conversions rather than
something that round closed, and the re-baseline neither closed it nor carried it forward until now.
It is NOT superseded by the callee-side raise screen: that screen asks whether a delete row's raised
type discriminates, and this asks whether a convert row's replacement actually bites at every site
the recipe covers. Different question, different rows, both open. Treat it exactly as the 532-site
screen debt above: per-site, owed by the PR that carries the conversion, and paid by sampling the
unsampled sites rather than by re-reading the recipe.

Four verified recipes carry a trap, each of which was silently vacuous on the first attempt. These
are recorded on their rows because a reviewer cannot see them in the diff:

- **L-120, L-121**: every provisioning runner is configuration-gated, so under a default template
  the assertion covers nothing and the first break PASSED. The fixture must turn every gated runner
  on, which additionally needs a real local source directory and a `copy_dir_to` on the fake
  transport.
- **L-202**: a duplicate-line counter keyed on the current filename PASSED while the invariant was
  broken, because the appended duplicate carries the NEW name. The counter must be
  filename-agnostic.
- **F-117**: the token regex also matches `--fuel-level-color: var(...)`, so the loop can iterate
  over nothing and assert nothing without an explicit non-vacuity assertion.
- **Every warning presence or count assertion**: the screening question is whether the HEALTHY
  branch emits zero warnings. C-153 is the row where the answer was no, and it is why that recipe
  was withdrawn rather than reused.

Two verifications also graded the guards being retired, and both came out in the replacement's
favour. Breaking the `_ensure_agentworks_files_sourced` call site left the ORIGINAL source-scanning
test passing, because the surviving import line satisfies its substring scan: the grep cannot tell
an import from a call. And `test_tailscale_legacy_removed.py`'s `_MODULES_TO_SCAN` lists
`agentworks.secrets.resolver`, but no test in that file ever scans it, so that entry is dead and the
greps miss a resolver-level reintroduction outright.

One methodological note worth keeping while the sweep runs: a break that is not a FUNCTIONAL
reintroduction proves nothing. Two first attempts bit nothing because an earlier line raised first,
which looks identical to a recipe that does not work.

## Sequencing between the groups

Four constraints bind the order the sweep's PRs land in, beyond the plan's existing ordering against
the `phase7` item, the contained cli items, and P5.

1. **Group 1 lands first, and no other group may lean on it.** A `match=` is a fallback that is
   about to be deleted. Where a row in group 3 or group 4 justified deleting a prose assertion by
   pointing at the `match=` on the same `raises`, that justification expires the moment group 1
   lands. Those rows were re-read and now cite the surviving `raises` type or a structural sibling
   instead. Hold any new row to the same rule.
2. **Group 2 sits downstream of the guide work item.** Resolved rather than binding: the guide item
   landed as `4ac084cd` and deleted the whole estate, so every group 2 row is `[dead]` and there is
   no group 2 PR to order.
3. **Group 5 carries the W2 question**, which is an ordering decision rather than something this
   inventory can settle. See the open questions.
4. **Group 3 is too big for one PR at 322 live rows.** It is the judgment-heavy batch and the
   largest by a wide margin. The natural cut is by subsystem inside the one shape (consoles and
   sessions; workspaces and agents; vms and platforms; the rest), which keeps the review's judgment
   uniform while making each round reviewable.

## Overlaps found

Per the plan, the sweep records an overlap, keeps the file, and raises the ordering. Ten turned up.

1. **`cli/tests/test_completions.py:163-171` duplicates
   `cli/tests/guide/test_release_notes.py:130-137` line for line**: the same four assertions on the
   dynamic completion snippets, written two ways. Both are in the sweep's scope, so this is an
   ordering note only; the inventory deletes the guide-side copy and leaves the completions-side
   decision with its own row.
2. **`cli/tests/guide/test_contract_catalog.py`** (exclusion list, guide item) is the structural
   home that several group 2 deletes rest on, specifically the block-discriminator closure at
   `:248-250` and the broken-link handling at `:303-319`. Sequencing constraint 2 above.
3. **`cli/tests/secrets/test_resolution_lifecycle.py:542`** is a `format_remediation` pin in the
   same family as S1's `test_resolution_timeout_cleanup.py` wording trims. Different file, so it is
   inventoried here rather than deferred, but the two should be read together.
4. **P5 (gcp fixture extraction)** shares five files with this sweep's gcp rows. The plan already
   orders the sweep first. One fact for whoever runs P5: the E-093 replacement asserts through the
   public `manual_cleanup_guidance`, so it does not touch the per-file `_api_error` or
   extended-operation fakes and rebases cleanly under the extraction.
5. **P4 (gcp's five-class error taxonomy)** is load-bearing for roughly fifteen delete rows that
   rest on "the raised type already discriminates". Nothing in production dispatches on those
   subclasses today, so after this sweep the tests become the taxonomy's only consumer. If the
   reassessment collapses it toward the sibling shape, that branch coverage goes too. Recorded for
   R4.
6. **D2 (deriving `SCHEMA_SENTINELS` by replaying `MIGRATIONS`)** would rewrite
   `cli/tests/test_database_backup.py:61-79`, which this inventory keeps as derivation parity. If D2
   ever runs, that test is its territory.
7. **The CLI grammar rewrite** (saga `phasing.md` puts it directly after wave 1) churns exactly the
   rows that pin the command tree and per-flag help. They are deletes either way, so landing them
   before the rewrite strictly reduces its diff.
8. **`cli/tests/test_ssh_config.py:132-151`** is the unit home of the `"SSH config synced"` line
   that a group 3 row deletes at the orchestration level. Both are inventoried; they should land
   together.
9. **`cli/tests/capabilities/test_secret_backend_conformance.py`** is the same family as
   `test_conformance.py` and larger, and it was never on the absorbed survey's list. Inventoried
   here; recorded so the reassessment knows the survey's file list was not exhaustive.
10. **The website overlaps**, verified at HEAD rather than taken from the plan. The three the plan
    recorded all hold, with one correction and three additions:
    - `test_lander_404.py` (W5) has landed: line 5 is `from site_test_support import *` and no
      duplicated fixture block remains. Inventoried entirely from HEAD.
    - `test_site_documents.py`'s two owners confirmed. The function the plan names is alive at
      **576-597**, not where the plan's prose implies: the nine-declaration CSS list at 578-589 and
      the four-word `fake_terminal` blacklist at 590-597 are the sweep's, both delete. W4's palette
      test at 825-839 and W8's browser row at 636-653 are the trims item's and are not inventoried.
    - PR #559 merged and carries none of the sweep's website rows, so they are wholly the sweep's.
    - **New**: W4's conversion did not reach `test_lander_404.py:425-430`, which still carries
      `math.isclose(antenna, 3.788, abs_tol=0.001)` and three palette hex literals restated from
      `lander.css`. The lead may prefer this ride with W4's pattern rather than with the sweep.
    - **New**: the W8/W10 boundary. The plan assigned "the W8 browser row" (singular, 636-653); the
      three chromium-harness cleanup tests at 655-823 are separate tests from the 2026-08-15
      reliability correction and are inventoried here. If the trims item considers the whole harness
      family its own, those two rows move.
    - **New**: W6 residue. `lander-model.js` still duplicates a status literal between two of its
      own branches, but no test asserts it, so there is no sweep row and it stays a website-source
      defect.

## Open questions

Consolidated here because a question dropped at the end of a commit message is a question nobody
answers. Most are now closed; what remains is listed last.

### Ruled by the operator, 2026-08-16

Both escalations came back, and the first came back as a rubric rather than a row decision, so it
was applied across the whole inventory rather than to the three rows that raised it. The rubric is
recorded once in [hla.md](hla.md)'s doctrine 2, beside the `match=` taxonomy:

> **Keep where the assertion is the only probe for a behavior that has demonstrably regressed or
> plausibly can. Delete where the assertion's only failure mode is a rewording.**

1. **The workspace-repair report family keeps** (B-041, and B-043 and B-044 with it, since they turn
   on the same observable). These assertions track a DETECTION OUTCOME, not a sentence: whether a
   repair step found something to converge. `repair_workspace` returns `None` and convergence is
   idempotent, so nothing else observes it. Each row states what the keep costs, which is that it
   breaks when someone rewords `OK:` or `Fixed:`, so no later reader mistakes it for a prose pin
   nobody noticed.
2. **The 22 `test_site_templates.py` rows defer to R4** rather than landing either way. The reason
   is an asymmetry: if R4 deletes `site_validation.py`'s element-by-element half, these tests die
   with it at no cost, and if R4 keeps it, deleting them now permanently removes its negative-path
   coverage. They are moved out of the executable set into the deferred block below, so an executor
   scanning for delete rows cannot pick them up. The operator's question about whether they police
   authored template form rather than rejection behavior is deliberately not chased; R4 inherits it
   with the rows.

**Applying the rubric to every delete row, and then verifying every convert recipe, moved the
dispositions substantially.** The split that came out of that pass was 564 delete, 225 convert, 357
keep across 1,146 executable rows; the Totals section carries the current figures, which the
2026-08-19 re-baseline re-derived. The rows that moved are the coverage this sweep would otherwise
have destroyed quietly, and each says which behavior it is the only probe for.

### Ruled by the effort lead, 2026-08-16

Each was ruled for the family rather than the row, and the rows now carry the ruling.

1. **A shipped security header is behavior, not form.** Its security-relevant directives are the
   invariant; the header's exact text is not. F-002 converts to directive-level assertions (that
   `script-src` carries no `unsafe-inline`, and so on), and the verbatim pins plus the
   `site_test_support.CSP` second copy goes with F-145. F-046 held a third copy and is deferred with
   the W2 block, so the constant itself survives this sweep by one consumer.
2. **A style rule mechanized as a test is not a doctrine 2 threshold.** Style belongs in the linter,
   and a rule enforced by one package's suite is enforced inconsistently anyway. F-067 deletes, and
   the row says that the 1,000-line ceiling should move to `scripts/lint-files.sh` if it is worth
   keeping, rather than being lost.
3. **Formatter-output equality splits on derivation.** Keep where the test proves a derived copy
   still matches its canonical source, which doctrine 2 names as a keep shape, and delete where it
   pins the formatter's output as text; where a row is genuinely both, keep it and name the
   canonical source. This split C-087 in two (the label equalities delete, the assertion that the
   origin's file and line reach the rendered cell stays) and made C-165 and C-167 name theirs.
4. **Union-collapse cardinality converts.** `len(splitlines()) == 1` is a structural claim about
   output shape rather than about wording, it is cheap, and it fails when the collapse actually
   breaks. Verified per site: three of the eight carry the cardinality claim and convert (D-127a),
   the other five carry only authored normalization and delete (D-127b).
5. **A subtraction pass does not add coverage opportunistically.** R2.3 permits an addition only
   where a real invariant loses its ONLY guard in the same change. A-004 and A-041 are not that
   case, because the invariant a CLI-resolution test would add has no guard today and so loses none,
   so both became plain deletions carrying a recorded drift risk.
6. **The graph-guard reservation is accepted**, and it corrected `hla.md` rather than just this row.
   Banned pattern 2 governs what a module may reach rather than how the reaching is spelled, and it
   has no observational twin available even in principle. That detector stays; the other three
   patterns go. Rows L-101a and L-101b carry the split, and `hla.md`'s delete bullet says so too.

### Still open

1. **Does the synthetic-fixture carve-out cover per-row state markers?** Two rows keep rendered
   `(disabled)` / `(not ready)` markers on that basis, and the marker strings are authored. The lead
   let the recorded decision stand rather than overruling it without the rows in front of him, so it
   is recorded as decided-by-default rather than settled.
2. **Are `agentworks-manual-*` / `agentworks-pre-migration-*` backup filenames a documented operator
   interface?** If they are, one pin is a real contract check rather than a naming restatement.
3. **Rows that were reasoned rather than executed** and whose disposition turns on a coverage claim:
   four, each carrying `[unverified]` in its shape cell, and named here so the count is checkable
   against the markers rather than asserted: C-005, C-025, C-094, C-108.

   **Operator disposition, 2026-08-19: left to their execution PRs, with the `[unverified]` tag as
   the gate.** The eighteen rows the coverage-claim scan below returns are not tagged and are not
   gated; the four that carry the marker are, and the PR that carries each one executes its claim
   before the edit lands.

   **Corrected 2026-08-19.** This item read "eight in total" from the inventory's first revision
   onward while five rows ever carried the tag, and no three further rows are recoverable: none of
   the untagged justifications says its coverage claim went unexecuted. The fifth, D-038, was
   discharged when its "or delete" ambiguity closed toward convert on `_Problem.path`, because the
   duplication claim it could not verify no longer carries the disposition. Four is the measured
   number.

   The concern behind the eight is real and larger than the tag, and the scan behind that claim is
   stated here so it can be re-run rather than believed. Count the rows whose JUSTIFICATION cell
   contains any of the literal strings `already covers`, `already asserts`, `covered by` or
   `observational twin`, excluding any row that carries `verified` or `by execution` in that same
   cell OR the `[unverified]` marker in its SHAPE cell. That returns **18 rows**. Reading the
   justification cell alone for the exclusion answers 20, because the four `[unverified]` rows carry
   their marker in column 3. Loosening the phrase set changes the answer too, which is the point:
   the population is a judgment about wording, not a measurement, and whether it wants tagging,
   executing, or leaving is the effort lead's call and is not decided here.

## Corrections to lead-owned artifacts

Re-derived at HEAD and contradicting `findings.md` or the plan's estate enumeration. Flagged rather
than fixed, because those artifacts belong to the effort lead. The two marked **applied** were
carried into `hla.md` on 2026-08-19 under the effort lead's explicit authorization for exactly those
edits; everything else here is still a flag.

- **The #470 manifesto pin, which the plan names as part of this sweep's estate, is already gone.**
  Commit `8043d438 refactor(guide): remove command-owned fact views` replaced the verbatim
  manifesto-text pin in `cli/tests/guide/test_authored_coverage.py` with a structural link
  assertion. That file carries no verbatim prose pins at HEAD.
- **G12's counts are stale, and its subject is gone.** The `test_migration_topic.py` required-phrase
  list was 30 entries at this inventory's basis, not 47 and not the 31 this file claimed until the
  2026-08-19 re-baseline counted the tuple; `test_render_service.py` had one prose-shaped line
  rather than "scattered pins". Both files were deleted by `4ac084cd`, so the finding is historical
  and its rows are `[dead]`.
- **C10's `cli/tests/capabilities/test_retired_shapes.py` no longer exists.** It and its production
  module were both deleted by `ab0a6303 feat(config)!: remove retired compatibility rewrites`, so
  the "three blacklists redundant with its own structural tests" item is moot and its redundancy
  claim could not be tested.
- **C10's `test_capability_shape.py:21-32` anchor is stale.** Those lines are a helper and a
  `parametrize` opening at HEAD, and every message assertion in that file is a `match=` site, so the
  finding resolves into group 1 rather than into its own rows.
- **APPLIED. `hla.md`'s site counts were both wrong.** `match=` is 664 at HEAD, all under
  `cli/tests` and none under `website/tests`, after the wave 1 landings and the one site the Grok
  Build integration added, where `hla.md` said 663. The website suite carries 49 `assertRaisesRegex`
  sites, not the 51 it recorded. And nine further regex-family sites in that suite were in neither
  count, one `assertRegex` and eight `assertNotRegex`, all of them already covered by group 5 rows.
  All three corrections are now in `hla.md`.
- **APPLIED. `hla.md`'s case 1 needed the callee-side condition.** "The raised type already
  discriminates" is a claim about the callee, and the map established it from the call site, which
  does not follow. The callee-side raise screen found the single-path premise false for roughly four
  in five resolvable sites. Case 1 now carries the condition and points at the screen.
- **The absorbed purge FRD's R4 item 2 is already satisfied and needs no production change.**
  `agentworks.schema.errors._problems` exposes `path`, `unknown_field`, `alternatives` and
  `union_path`, and `cli/tests/schema/test_errors.py` already imports and asserts on it. That is why
  group 4 carries so many converts: the structural seam the purge wanted promoted exists.
- **The absorbed survey's file list was not exhaustive.**
  `capabilities/test_secret_backend_conformance.py` is the same family as `test_conformance.py`,
  larger, and unlisted.
