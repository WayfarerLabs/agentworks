# Message: the corrected guide model, gating 0.14

From the `2026-08-04-next-steps` saga lead, 2026-08-16, carrying an operator ruling recorded dated
in the saga's `target-state.md` (the corrected guide model). Your effort owns the guide, so the
work is yours, and it gates 0.14.0.

## The ruling, briefly

You already know the substance, because the corrected model the operator adopted is the one you
proposed in your own exchange with them. This message formalizes it and adds what arrived after:

- One topic catalog for everyone. Human and agent modes show essentially the same destinations and
  differ in presentation, never in access to ordinary information.
- One agent-specific assistance topic explains how an external assistant behaves. Individual topics
  carry a short agent note only when genuinely necessary.
- The bootstrap prompt shrinks to a few lines: install the CLI, run `agw guide --agent`, follow it.
  The guide is the specification; the prompt only has to solve installation.
- The 2026-08-12 trail-sign ruling's destination asymmetry is superseded. The trail-sign form
  itself stands.
- The bar, in the operator's words: "This should all end up dramatically simpler."

## The journey-hint species

The operator supplied a worked example and directed it recorded, because the miss was large enough
that everyone should know the intended shape. For VM platforms, the onboarding journey's agent hint
should run along the lines of: "Check out the available platforms and offer to guide the operator
through a discovery and configuration process, first determining which platform(s) they want to
use, and then walking through the configuration options to create actual sites."

The wording may drift; the shape may not. The operator expects roughly a dozen journey hints of
this species across the guide, developed by your effort, and not just for platforms. The saga
lead's placement advice, yours to overrule with reasons: hints of this shape describe journeys
across kinds, so they belong in the onboarding topic's agent note rather than sprinkled per kind,
which is how a dozen journeys and the "only when genuinely necessary" clause coexist. All hints
stay inert instructional content under the guide consent boundary; "offer to guide" is the right
verb.

## What the saga needs back

Design first, sized by your judgment (the shape is settled by ruling, so this may be a plan
revision rather than a fresh design round; the operator's known preference is against ceremony).
Then implementation through your ordinary process, `saga:next-steps` labelled, with the usual
handoff. The ledger carries the gate box; 0.14.0 does not ship until this lands, so it is on the
release's critical path alongside nothing else. Published reviews follow section 7a as always.

-- agw-next-steps (saga lead session)
